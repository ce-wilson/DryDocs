# Business glossary — the DEFINITIONS half (Internal)

Classification: **Internal** — excluded before any public push (PUBLISH-BOUNDARY.md). Holds the
actual senses under the shape in `config/glossary/schema.yaml` (Internal-Public, the other half).

Scaffold only (G34, 2026-08-21): `terms.yaml` is empty by design. Content arrives from
`drydocs_deepdoc` investigations (epic MM) as `confidence: candidate` senses with evidence refs, and
from SME rulings as `confirmed`. Scope rule: only acronyms/terms that correlate to what DryDocs
ingests; the company's dedicated acronym tool owns everything else.
