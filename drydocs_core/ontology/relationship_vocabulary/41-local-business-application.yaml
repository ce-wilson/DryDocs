
  # ── Business application (SEAL is the company instance name) ─────────────

  - id:           seal_has_port
    neo4j_label:  HAS_PORT
    inverse_label: "port of"
    role:         ~
    from_node:    BusinessApplication
    to_node:      Port
    prov_maps_to: ~
    note: >
      Application exposes a port. Maps to dprod:hasPort pattern. Port kind is
      stored as a node property — kinds: EventProcessing | BatchProcessing
      (data ports) + Technology, the infrastructure attachment surface (kind
      WIDENED by ruled amendment at gate server-location-ontology §C2,
      2026-08-19: the SME directed the app->server link through a technology
      port rather than a direct edge; this entry is REUSED for that hop per
      the C8 identical-triple rule, and the port->server leg is
      infra_port_runs_on_server in 51-local-infrastructure.yaml). The Z3
      loader mints one Technology port per application. No PROV-O equivalent;
      structural dprod relationship.
    supplement:   seal_ontology_supplement.cypher
    loader:       seal_applications.cypher
    domain:       business_application
    status:       deprecated
    deprecated_at: 2026-08-21
    superseded_by: business_application_has_port
    deprecation_note: >
      ID MIGRATION ONLY — THE CONCEPT IS UNCHANGED. Gate vocabulary-domains-and-id-policy
      §B3 (SIGNED 2026-08-12) ruled the epoch-tag ids FORCE-migrated onto the
      domain-derived scheme; G101 applies it (2026-08-21). Add-new + deprecate-old, never
      a rename in place: the id is the join key across the taxonomy-ontology-map, the
      generated schema_graph.cypher, the SQLite mirror and the append-only gate log.
      Live entry is `business_application_has_port` below — same shape, same active status.
      DATA: edges already in a graph carry r.vocab_id = 'seal_has_port'; loaders write
      'business_application_has_port' from this date. drydocs/loaders/cypher/migrate_vocab_ids_g101.cypher
      re-stamps the old value (idempotent, one-time per environment).

  - id:           business_application_has_port
    neo4j_label:  HAS_PORT
    inverse_label: "port of"
    role:         ~
    from_node:    BusinessApplication
    to_node:      Port
    prov_maps_to: ~
    note: >
      Application exposes a port. Maps to dprod:hasPort pattern. Port kind is
      stored as a node property — kinds: EventProcessing | BatchProcessing
      (data ports) + Technology, the infrastructure attachment surface (kind
      WIDENED by ruled amendment at gate server-location-ontology §C2,
      2026-08-19: the SME directed the app->server link through a technology
      port rather than a direct edge; this entry is REUSED for that hop per
      the C8 identical-triple rule, and the port->server leg is
      infra_port_runs_on_server in 51-local-infrastructure.yaml). The Z3
      loader mints one Technology port per application. No PROV-O equivalent;
      structural dprod relationship.
      REPLACES seal_has_port at G101 (2026-08-21) — the seal_ family §B3's census left out; same rule, gate
      vocabulary-domains-and-id-policy §B1/§B3 (SIGNED 2026-08-12): the prefix is
      now the entry's own `domain:` field. Same label, direction, active status
      and semantics; history before 2026-08-21 is filed under the old id.
    supplement:   seal_ontology_supplement.cypher
    loader:       seal_applications.cypher
    domain:       business_application
    status:       active

  - id:           seal_requires_scheduler
    neo4j_label:  REQUIRES_SCHEDULER
    inverse_label: "required by"
    role:         ~
    from_node:    Port                       # as registered; the pre-gate HOIST direction (to :BusinessApplication) was absorbed by the reg_uses_software landing, so no reshape ever applied here
    to_node:      SchedulerKind              # RETIRED TARGET — :SchedulerKind deprecated at C12 (seeds retired in ontology.cypher; the scheduler_kind constraint was then REMOVED 2026-07-23 in the C13 leftover sweep — pre-C13 graphs are wiped and rebuilt from bootstrap, so nothing creates or holds the label)
    prov_maps_to: ~
    note: >
      Registered per backlog C6 (2026-07-09) to close the drift where
      REQUIRES_SCHEDULER appeared in README.md:263 and docs/history/LoadPlanV3.md:82,393
      but was absent from this registry. Original semantics: the SEAL batch
      :Port (kind=BatchProcessing) points at a scheduler technology,
      :SchedulerKind {name: ControlM|Autosys|Airflow}, then seeded in
      ontology.cypher; null-PROV infrastructure placement (the SCHEDULED_ON
      family). The three C6 open questions (attach point, target node,
      null-PROV) were all CLOSED by the deprecation below — the
      requires-scheduler map entry keeps the question-by-question record.
    deprecation_note: >
      DEPRECATED at C12 (platforms-taxonomy gate, SIGNED OFF 2026-07-21 pm;
      config/gate-log.md) — retired UN-WIRED: status was planned throughout,
      no loader ever ran and no graph data exists (nothing to migrate; the
      'data kept in graph' clause of the deprecated lifecycle is vacuous
      here). The fact lands on the ACTIVE registry edge (:BusinessApplication)
      -[:USES_SOFTWARE {source: 'batch-port'}]->(:SoftwareProduct
      {role: orchestrator}) — reg_uses_software — meaning carried by the
      target's role (SME: role over class; capability node layer NOT
      adopted); zero new edge or node types. Supersedes the 07-09 Ais*
      interim retarget, which was removed unbuilt. Loader migration =
      backlog C14.
    superseded_by: reg_uses_software
    supplement:   ~                          # never declared — retired before any supplement block landed
    loader:       ~
    domain:       business_application
    status:       deprecated
    deprecated_at: 2026-07-21

  # ── SEAL — PROPOSED reshape (GATE-BOUND, 2026-07-08 review) ────────────────
  # Nothing below is active. The :BusinessApplication node stays classed prov:SoftwareAgent (see
  # node_classifications) until the SME confirms; these entries exist ONLY so the proposed
  # edges type-check under the PROV matrix once that reclass is confirmed. All carry
  # supplement: ~ / loader: ~ on purpose — no supplement/loader claims for a planned-only
  # proposal. See config/taxonomy-ontology-map.yaml (application-as-dataproduct,
  # seal-tom-attribution, seal-doc-source-of-record) and
  # config/gate-prompts/seal-tom-attribution-reshape.yaml.

  - id:           seal_had_primary_source
    neo4j_label:  HAD_PRIMARY_SOURCE
    inverse_label: "primary source for"
    role:         ~
    from_node:    BusinessApplication
    to_node:      Document
    prov_maps_to: "prov:hadPrimarySource"
    note: >
      ACTIVE (K4, 2026-07-15; gate 2026-07-10 §D). Entity -> Entity, a sub-property of
      prov:wasDerivedFrom: the BusinessApplication record (reclassed prov:Entity/
      dprod:DataProduct at K4) was primarily sourced from a scraped SEAL/PAT Document.
      Generalizes to any fact/entity extracted from a scraped page; BusinessApplication is
      the concrete instance in scope. EDGE-ACTIVE ONLY: the Document node + loader wait on
      the drydocs-docmeta component (the gate promoted docmeta from parked to a REQUIRED
      dependency of the provenance goal) — see knowledge/upgrade-plans/docmeta-component.md.
      Matrix row: Entity -> Entity = prov:wasDerivedFrom family.
    supplement:   seal_ontology_supplement.cypher
    loader:       ~
    domain:       business_application
    status:       deprecated
    deprecated_at: 2026-08-21
    superseded_by: business_application_had_primary_source
    deprecation_note: >
      ID MIGRATION ONLY — THE CONCEPT IS UNCHANGED. Gate vocabulary-domains-and-id-policy
      §B3 (SIGNED 2026-08-12) ruled the epoch-tag ids FORCE-migrated onto the
      domain-derived scheme; G101 applies it (2026-08-21). Add-new + deprecate-old, never
      a rename in place: the id is the join key across the taxonomy-ontology-map, the
      generated schema_graph.cypher, the SQLite mirror and the append-only gate log.
      Live entry is `business_application_had_primary_source` below — same shape, same active status.
      DATA: edges already in a graph carry r.vocab_id = 'seal_had_primary_source'; loaders write
      'business_application_had_primary_source' from this date. drydocs/loaders/cypher/migrate_vocab_ids_g101.cypher
      re-stamps the old value (idempotent, one-time per environment).

  - id:           business_application_had_primary_source
    neo4j_label:  HAD_PRIMARY_SOURCE
    inverse_label: "primary source for"
    role:         ~
    from_node:    BusinessApplication
    to_node:      Document
    prov_maps_to: "prov:hadPrimarySource"
    note: >
      ACTIVE (K4, 2026-07-15; gate 2026-07-10 §D). Entity -> Entity, a sub-property of
      prov:wasDerivedFrom: the BusinessApplication record (reclassed prov:Entity/
      dprod:DataProduct at K4) was primarily sourced from a scraped SEAL/PAT Document.
      Generalizes to any fact/entity extracted from a scraped page; BusinessApplication is
      the concrete instance in scope. EDGE-ACTIVE ONLY: the Document node + loader wait on
      the drydocs-docmeta component (the gate promoted docmeta from parked to a REQUIRED
      dependency of the provenance goal) — see knowledge/upgrade-plans/docmeta-component.md.
      Matrix row: Entity -> Entity = prov:wasDerivedFrom family.
      REPLACES seal_had_primary_source at G101 (2026-08-21) — the seal_ family §B3's census left out; same rule, gate
      vocabulary-domains-and-id-policy §B1/§B3 (SIGNED 2026-08-12): the prefix is
      now the entry's own `domain:` field. Same label, direction, active status
      and semantics; history before 2026-08-21 is filed under the old id.
    supplement:   seal_ontology_supplement.cypher
    loader:       ~
    domain:       business_application
    status:       active

  # ── Downstream consumer contact on a data port (SME direction 2026-08-12) ──

  - id:           business_application_port_has_contact_point
    neo4j_label:  HAS_CONTACT_POINT
    inverse_label: "contact point for"
    role:         ~
    from_node:    Port
    to_node:      DistributionList
    prov_maps_to: ~
    note: >
      The downstream consumer contact for a data port — the DPROD shape the
      captured MFTS standard already uses (ex:consumerContact hung off a
      dprod:outputPort). SME DIRECTION 2026-08-12 (config/gate-log.md RECORD;
      email-dl-contact-point §G5): the contact attaches to a :Port, NOT to the
      job or folder (§B1's shape). Two grounds — the contact is UNSTRUCTURED,
      and NO SUPERNODES: the job/folder shape produces one at estate scale
      because a shared distribution list accretes an inbound edge from every
      job that notifies it, and the most-shared DLs are the ones the whole
      batch estate points at. Port-scoping bounds the fan-in.
      HONEST CAVEAT, because the direction's own reasoning does not fully
      settle it: port-scoping REDUCES fan-in (ports are far fewer than jobs)
      but does not eliminate it — one DL shared by many applications is still
      a shared node with many inbound edges. If "unstructured" was meant to
      rule out the node entirely, the alternative is a PORT PROPERTY and no
      edge at all, which removes the supernode by construction and would
      retire this entry. That is exactly what the gate must rule, and it is
      why this stays planned.
      VALUE SOURCE (§G2 direction, same day): the known email_dl comes from
      FOLDER METADATA — the folder variables L2_EMAIL_DL_NM / L3_EMAIL_DL_NM
      — not the per-job Description tokens. Precedence when folder and job
      disagree is still open.
      Entity→Entity (Port and DistributionList are both prov:Entity), so
      prov_maps_to is null on the m3_belongs_to_application / SCHEDULED_ON
      precedent rather than reaching for a weak PROV verb.
      NOTHING LOADS: status planned until email-dl-contact-point signs off;
      §G5's "does the port leg split to its own gate" half is also unruled.
    supplement:   ~
    loader:       ~
    domain:       business_application
    status:       planned

  # ── Deployment Module CI (K22 PROPOSAL — rider G0d-RIDER on gate
  # tom-roles-enumeration-and-cardinality, 2026-08-26) ──────────────────────

  - id:           business_application_instantiates_deployment_module
    neo4j_label:  INSTANTIATES
    inverse_label: "instance of"
    role:         ~
    from_node:    BusinessApplication
    to_node:      DeploymentModule
    prov_maps_to: ~
    note: >
      PROPOSED at K22 (2026-08-26), routed via the tom-roles gate's G0d-RIDER —
      the CI topology exactly as the SME gave it (2026-08-10, K21): Business
      Application [Instantiates] Deployment Module, inverse [Instance of], and
      above it [Contained by] area product (the parent is the AREA PRODUCT, a
      node DryDocs already holds — no second containment edge is registered
      here). The inverse label is THIS INSTANCE's ([Instance of]); the public
      material writes [Instantiated by] — recorded because C10's rule (the
      label is not the identity) is the whole reason we carry our own stable
      names. Entity -> Entity structural containment/instantiation; no PROV
      verb reached for (the m3_belongs_to_application precedent). THE TARGET'S
      KEY IS THE CI's OWN ID, never the composite display name — see the
      DeploymentModule node-class note. NOTHING LOADS: status planned until
      the rider is ruled; G13/G14/G15's plausibly-shared subject (§G0d) is the
      question the gate still owns, and this entry does not preempt it.
    supplement:   ~
    loader:       ~
    domain:       business_application
    status:       planned

  - id:           business_application_contained_by_ci
    neo4j_label:  CONTAINED_BY
    inverse_label: "contains"
    role:         ~
    from_node:    BusinessApplication
    to_node:      "AreaProduct | BusinessApplication"
    prov_maps_to: ~
    note: >
      G73 (gate tom-roles-enumeration-and-cardinality SS-E4, SIGNED 2026-08-11):
      the MODELLED ANCESTOR — the cmdb containment hop from a seeded CI to its
      parent CI, named for what the SOURCE asserts (cmdb_rel_ci "Contained by",
      taken endpoints-in-seed by K21 sect. 7.4), never for the computation over
      it. It is what makes "why does this application show a different owner
      than its parent" TRAVERSABLE (SS-D2's operator question): TOM inheritance
      is COMPUTED by walking this edge upward — one mechanism applied
      repeatedly up an N-level chain (SS-E1e), nearest ancestor holding the
      role wins, and overrides PROPAGATE (a child inherits the ancestor's
      CURRENT value even when that value is itself an override). The
      DeploymentModule rung of the same chain is NOT re-registered here —
      business_application_instantiates_deployment_module already carries that
      pair, and the C8 identical-triple rule says reuse, traversed against the
      arrow. DISTINCT FROM K22's declined second containment edge, on K22's
      own terms: that decline was the app->area-product CATALOG rollup, which
      the catalog family already links; this is the cmdb containment CHAIN
      whose ancestors include CIs the catalog family will never hold — CIs
      owned by teams DryDocs does not support, taken FOR THEIR TOM ROWS ONLY
      (the K21 sect. 7.4 amendment this item writes). A foreign ancestor's
      node class is deliberately unresolved until the company pull builds it
      (the K22 deferral idiom); to_node names the classes DryDocs holds.
      Entity -> Entity structural containment; no PROV verb reached for (the
      m3_belongs_to_application precedent). Attribution rows carry
      assertion_mode 'direct' | 'overridden' (the source's BLANK is Direct;
      'inherited' is NEVER stored — SS-E: authored rows only, inheritance is a
      computation, and storing a derived fact as though asserted is how a
      graph starts disagreeing with its source). NOTHING LOADS producer-side:
      status planned until the company TOM pull lands (the G72
      servicenow-tom entry is the surface it will stamp).
    supplement:   ~
    loader:       ~
    domain:       business_application
    status:       planned

  # ── FID / functional-id family (gate fid-identity-and-scope, SIGNED 2026-08-19) ──

  - id:           seal_appuser_belongs_to_application
    neo4j_label:  BELONGS_TO_APPLICATION
    inverse_label: "owns account"
    role:         service_account
    from_node:    AppUser
    to_node:      BusinessApplication
    prov_maps_to: ~
    note: >
      The directory's application (SEAL) assignment for a functional id — an
      OWNERSHIP-RECORD fact, as of the extract (gate fid-identity-and-scope
      §C1-§C3, SIGNED 2026-08-19). ASSERTS REGISTRATION, NEVER CONSUMPTION
      (§G1): edge property assignment_kind: 'registration' rides on the data,
      plus as_of (the EXTRACT date, never load date) and origin: 'declared'
      (§B1). Same label as m3_belongs_to_application (ControlMFolder -> Port)
      but a different triple — the C8 identical-triple rule; role
      disambiguates. Target is :BusinessApplication, NOT the BatchProcessing
      Port: ownership record, not batch wiring (§C2). N:1, graph-test only —
      never a schema constraint (§C3, the TOM-roles lesson). NO TRANSITIVE
      READ onto jobs (§G2): the registration and the folder attribution meet
      ON THE JOB and nowhere else. Agent -> Entity, the family with no PROV
      row (reg_uses_software's recorded reasoning). Feeds TierReconcilers.fid
      from the SAME crosswalk (§C4) under the amended K2 scope (fill gaps
      only, never override a confirmed mapping — the G3 amendment).
      NOTHING LOADS: planned until the build item lands (N13
      flips-are-follow-ups).
    supplement:   ~
    loader:       ~
    domain:       business_application
    status:       deprecated
    deprecated_at: 2026-08-21
    superseded_by: business_application_appuser_belongs_to_application
    deprecation_note: >
      ID MIGRATION ONLY — THE CONCEPT IS UNCHANGED. Gate vocabulary-domains-and-id-policy
      §B3 (SIGNED 2026-08-12) ruled the epoch-tag ids FORCE-migrated onto the
      domain-derived scheme; G101 applies it (2026-08-21). Add-new + deprecate-old, never
      a rename in place: the id is the join key across the taxonomy-ontology-map, the
      generated schema_graph.cypher, the SQLite mirror and the append-only gate log.
      Live entry is `business_application_appuser_belongs_to_application` below — same shape, same planned status.

  - id:           business_application_appuser_belongs_to_application
    neo4j_label:  BELONGS_TO_APPLICATION
    inverse_label: "owns account"
    role:         service_account
    from_node:    AppUser
    to_node:      BusinessApplication
    prov_maps_to: ~
    note: >
      The directory's application (SEAL) assignment for a functional id — an
      OWNERSHIP-RECORD fact, as of the extract (gate fid-identity-and-scope
      §C1-§C3, SIGNED 2026-08-19). ASSERTS REGISTRATION, NEVER CONSUMPTION
      (§G1): edge property assignment_kind: 'registration' rides on the data,
      plus as_of (the EXTRACT date, never load date) and origin: 'declared'
      (§B1). Same label as m3_belongs_to_application (ControlMFolder -> Port)
      but a different triple — the C8 identical-triple rule; role
      disambiguates. Target is :BusinessApplication, NOT the BatchProcessing
      Port: ownership record, not batch wiring (§C2). N:1, graph-test only —
      never a schema constraint (§C3, the TOM-roles lesson). NO TRANSITIVE
      READ onto jobs (§G2): the registration and the folder attribution meet
      ON THE JOB and nowhere else. Agent -> Entity, the family with no PROV
      row (reg_uses_software's recorded reasoning). Feeds TierReconcilers.fid
      from the SAME crosswalk (§C4) under the amended K2 scope (fill gaps
      only, never override a confirmed mapping — the G3 amendment).
      NOTHING LOADS: planned until the build item lands (N13
      flips-are-follow-ups).
      REPLACES seal_appuser_belongs_to_application at G101 (2026-08-21) — the seal_ family §B3's census left out; same rule, gate
      vocabulary-domains-and-id-policy §B1/§B3 (SIGNED 2026-08-12): the prefix is
      now the entry's own `domain:` field. Same label, direction, planned status
      and semantics; history before 2026-08-21 is filed under the old id.
    supplement:   ~
    loader:       ~
    domain:       business_application
    status:       planned

