# =============================================================================
# config/taxonomy-ontology-map.yaml — the HITL-confirmed bridge.
#
# THE central artifact of the configuration layer. Each entry says:
#   "this imported TAXONOMY element  →  apply THIS ONTOLOGY rule."
#
# Lifecycle of every entry:
#   proposed   → ontology-mapper drafted it from the PROV matrix
#   confirmed  → SME approved via the guided gate (docs/restructure/03-hitl-sme-flow.md)
#   applied    → a loader has written it to the graph
#   rejected   → SME declined; kept for audit, never loaded
#
# A taxonomy NEVER becomes graph edges until its mapping is `confirmed`.
# This is the fix for the POC drift (relationships that ignored taxonomy/ontology).
# =============================================================================

schema: drydocs.taxonomy-ontology-map.v1
updated: 2026-08-22                        # vendor-docs-entity-spine CONFIRMED (gate SIGNED OFF 21/21 same day as the Q14 draft; vocab stays planned until Q24/Q25). Prior 2026-08-19: email-concerns-subject CONFIRMED (gate email-folder-assignment, 8/8 — Q10's gated half; vocab stays planned until the writer build); prior 2026-08-04: +product-has-application (K13 — the map entry recording the K7 §G6 support-reading ruling; semantics only, loading waits on the C9 extract condition); prior 2026-08-03: app-code-defined-mapping CONFIRMED (K7 close-out gate seal-app-ref-edge-reshape, 24/24); prior 2026-07-27: +business-application-identity (S3 gate SIGNED OFF — property binding, no edge); prior 2026-07-22: +app-code-defined-mapping, +dl-contact-point (both proposed — attribution close-out + email-DL gates); prior: C12/C13 requires-scheduler rejected (platforms-taxonomy gate)

