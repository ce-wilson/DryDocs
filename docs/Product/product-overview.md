# Product Overview: Function and Roles — RELOCATED STUB

> 🔒 **The full capture moved to `internal/org/product-overview.md`
> (Internal-Confidential; J14 follow-up (1), 2026-07-27).** The original file
> at this path was a verbatim 2026-06-09 transcription of internal product
> operating-model material and carried real rosters: ~25 named people (the
> Operations Strategy Cabinet Mapping and ODPM/ERM alignment tables), two
> real SIDs, and an internal contact DL — none of which may sit in a
> publishable tier (PUBLISH-BOUNDARY.md; the `internal/org/` charter is
> exactly "real rosters, team names, people").
>
> This stub stays at the old path so existing references keep resolving:
> the doc-inventory row (2026-07-22), the `git-readme.md` §docs/Product
> table, and the PAT/catalog ontology citations.

## What the capture contains (mechanism summary — no values)

The relocated document is the org/product operating-model source that fed the
PAT ontology analysis and the catalog ontology supplement
(`drydocs_core/schema/catalog_ontology_supplement.cypher`, whose source
comments point at the internal twin):

1. **Product roles** — Product Management / Product Delivery / Product
   Portfolio Operations: descriptions and key responsibilities (the Quad
   model; PDLC accountability split).
2. **Technology standardized roles** — Head of Technology, Tech Partner, and
   team-level engineering roles.
3. **Product Cabinets — CCB Operations engagement and readiness** — the
   Operations Strategy Lead responsibilities, the cabinet-mapping table
   (product/platform → named cabinet member — REAL NAMES, internal twin
   only), ODPM responsibilities + alignment table (real names, internal twin
   only), and the Home Lending ERM model.
4. **Align & Jira standards** — the work hierarchy (Theme / Initiative /
   Epic / Story) and people hierarchy (Enterprise-LOB / Portfolio / Team of
   Teams / Team), the Area-Product-Group naming convention.

The cabinet-mapping tables are also the candidate **person-level cabinet
extract** the K5 `product-cabinet-attribution` gate left as the missing feed
for K6 — when that loader lands, it reads the internal twin, never this stub.
