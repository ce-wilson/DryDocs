# =============================================================================
# backlog.yaml — the machine-readable backlog agents pull from.
#
# This is the SOURCE OF TRUTH for work items. 02-backlog.md is the human-readable
# view of the same items (superseded by the rendered board once I2 lands);
# IDEAS.md is the inbox that feeds this file.
#
# HOW AN AGENT PULLS WORK:
#   1. Find the next item with status: todo whose every depends_on id is status: done.
#   2. Set it status: in_progress, commit, and PUSH that change BEFORE starting work — not at
#      close. This file's status is the only claim channel between concurrent sessions on the
#      two machines and git is the only sync layer, so a local-only claim is invisible to the
#      other machine (2026-07-28: two sessions independently built C19 ten minutes apart).
#   3. Do exactly that item — stay inside the named agent's layer. Anything ambiguous
#      goes to the HITL gate (docs/restructure/03-hitl-sme-flow.md), not auto-decided.
#   4. Meet the `acceptance` test, then set status: done.
#   5. LIVE-VERIFICATION CLAIMS name their venue (J18). A close note that says
#      "verified live" names the machine + container + database it ran on (e.g.
#      "desktop, neo4jtest, drydocs DB") — the two machines hold INDEPENDENT graphs,
#      so an untagged claim invites the other machine's audit to read ITS graph and
#      file the difference as a defect (nearly happened to G33, desktop-built,
#      audited from the laptop). Prefer sample-reproducible evidence
#      ("ingest-controlm on the bundled samples reproduces X") over machine-pinned
#      claims — a reader can re-run those anywhere. Worked examples: P5 (tagged at
#      close), P3 and G33 (retro-tagged 2026-08-04).
#
# STATUS lifecycle (mirrors relationship_vocabulary.yaml's idiom on purpose):
#   todo | in_progress | blocked | done
#
# FIELDS (v2 — validated by tests/unit/test_backlog.py):
#   id          stable short id (matches 02-backlog.md). Epic-letter series = PRODUCER-
#               allocated; the DD-series (DD1, DD2, …) is RESERVED for company-side-only
#               items and is never allocated here (user decision 2026-07-20, first
#               bundle-port reconcile — see git-readme.md §backlog id allocation)
#   title       REQUIRED plain-English one-liner — never rely on the id/codename alone
#   type        requirement (future capability ask) | task | chore | bug
#   module      which module/work area it lands in — must be in `modules:` below
#   phase       which plan phase it belongs to — must be an id in `plan.phases` below
#   epic        grouping
#   agent       which sub-agent owns it (.claude/agents/) or "main" for the Opus session
#   model       suggested model tier (haiku | sonnet | opus)
#   priority    p0 (blocker) | p1 | p2 | p3
#   status      todo | in_progress | blocked | done
#   depends_on  list of ids that must be `done` first ([] = startable now)
#   inputs      files/paths/registry keys the agent needs
#   acceptance  the pass/fail test for "done"
#   notes       optional
#
# The `summary:` roll-up and `next_ready:` list at the bottom are COMPUTED views —
# test_backlog.py fails if they drift from the items, so update them with any status change.
# =============================================================================

schema: drydocs.backlog.v2
updated: 2026-08-18

# ── The high-level project plan (rendered as the board's roadmap strip) ───────
plan:
  phases:
    - id: 0
      title: Restructure scaffolding
      goal: "Four-layer model, external/internal split, config layer, publish boundary, sub-agents."
      status: done
    - id: 1
      title: Taxonomy capture
      goal: "Live sources captured in config/taxonomy/ as pure classification (Epic B)."
      status: done
    - id: 2
      title: Ontology mapping + HITL
      goal: "Every graph edge traces to an SME-confirmed taxonomy→ontology mapping (Epic C)."
      status: in_progress   # re-opened 2026-07-08: C5 (same-row-derived relationship rule)
    - id: 3
      title: Config-driven loaders
      goal: "Precedence + source gating read config/, not hardcoded logic (Epic D)."
      status: done
    - id: 4
      title: Context graph pilot
      goal: "SOSA/SSN observation modeling answers one live support question (Epic E)."
      status: todo
    - id: 5
      title: Orchestrator expansion
      goal: "AutoSys + Airflow land as BMC-baseline concepts via confirmed crosswalks (Epic F)."
      status: todo
    - id: 6
      title: Modular architecture / component topology
      goal: "ADR 0002 — core extraction, multi-DB topology, component packages (Epic G)."
      status: in_progress
    - id: 7
      title: Review toolkit back-flow
      goal: "Company SME-review/HITL toolkit reproduced generically, offline (Epic H)."
      status: done
    - id: 8
      title: Planning & release infrastructure
      goal: "Repo-native project board + capture loop; groundwork for versioned releases (Epic I)."
      status: in_progress
      release: v0.3.0
    - id: 9
      title: Application attribution
      goal: "Jobs attributed to SEAL applications (seal_app_ref) end-to-end — gate-confirmed match policy, loader, verify coverage (Epic K)."
      status: in_progress   # K1-K6 done (K5 gate signed off + K6 supplement 2026-07-20); live load is Track-2; Cabinet loader blocked company-side
    - id: 10
      title: Documentation infrastructure
      goal: "Every design doc (TDD/Runbook/BRD) conforms to a canonical outline; deterministic md→HTML/PDF render; HITL markup loop (digital + pen/paper) re-attaches to a requirement-traceability spine (Epic L)."
      status: in_progress
    - id: 11
      title: Source governance ledgers
      goal: "Per-source governance as test-enforced config: audit envelope + edge diet (doc 06, Epic M) and column-mapping ledgers (doc 08, Epic N); property-term bindings via the gate."
      status: in_progress   # doc-06 Phases 0-1 (Control-M envelope) shipped 2026-07-07 pre-groom
    - id: 12
      title: Web console / graph visualization
      goal: "Browser front-end over the knowledge graph: navigable C4/graph views, with a decided browser↔Neo4j access path (bolt-from-browser vs a thin API)."
      status: in_progress   # design pass in flight (feat/web-console-design-pass); Epic O / O1
    - id: 13
      title: Runtime topology & maintenance windows
      goal: "Control-M host topology (CM_HOSTS) + runtime-stats supplement (CM_AVG_RUN) loaded per their gates; the maintenance-window question (best patch window for a host/group) answerable from the graph (Epic P)."
      status: in_progress   # PLAN CHANGE groomed 2026-07-09 (autonomous session) -- ratify; hosts gate signed off same day, avg-run gate awaiting SME
    - id: 14
      title: Document ingestion & doc-graph benchmarks
      goal: "Unstructured documents as graph corpora (docmeta): the P0 benchmark verdict (traversal vs manifest-routed md vs vector RAG) with its evidence experiments, and the landing zone for the docmeta component's P1+ phases once the verdict + docmeta ADR gate them (Epic Q)."
      status: in_progress   # PLAN CHANGE user-approved 2026-07-16 (Essential-GraphRAG groom); bmc-docs lexical load (P0 corpus) shipped + gate-accepted 2026-07-08 pre-groom
    - id: 15
      title: Agentic Q&A console
      goal: "Free-text questions answered by a tiered read-only agent (QuerySpec router → schema-grounded text2cypher → bounded graph-of-thoughts loop) with every executed Cypher exposed via ephemeral specs, per-run question/context/memory/token telemetry, and stale-source rescrape (Epic R; ADR 0007)."
      status: todo          # PLAN CHANGE user-directed 2026-07-23 (llm-graph-builder vs KGoT analysis session); ADR 0007 PROPOSED, gate = R1
    - id: 16
      title: Self-documentation — DryDocs' own code in the graph
      goal: "DryDocs documents ITSELF the way it documents Control-M: the project's code-dependency snapshot enters the knowledge graph under the software ontology (SWO), under an explicit :Project root, so 'which module loads this job' is a query rather than a JSON file a human reads (Epic U)."
      status: todo          # PLAN CHANGE user-ratified 2026-07-26 — groomed into phase 6 the same day, then re-phased on the user's call: this is a NEW CAPABILITY, "similar to a major version change of the snapshot ritual", not an ADR 0002 component-topology follow-up

# ── Module / work-area registry (item.module must be one of these) ───────────
# Code components mirror MODULE_MAP.md; the rest are repo work areas so that
# non-code requirements still align to something a future phase can pick up.
modules:
  - drydocs-core         # parse/model/config/driver layer (MODULE_MAP.md)
  - drydocs-load         # loaders + cli + snapshots
  - drydocs-review       # graph_verify / review_labels / graph_review / gate_pages / publishing
  - drydocs-plan         # future: the project-board renderer (Epic I)
  - drydocs-docgen       # design-doc outline validation + deterministic render + HITL markup (Epic L; distinct from the planned drydocs-docmeta, which INGESTS docs)
  - drydocs-lineage      # future component (G4)
  - drydocs-deepdoc      # future component (G4)
  - drydocs-remediation  # future component (G3)
  - drydocs-web          # JS/TS console under web/ (Epic O, phase 12) — folder name ≠ module name by rule: MODULE_MAP.md §Naming (S7)
  - drydocs-api          # planned thin-API component (ADR 0005; working name — final at O5 scaffold): server-side graph access for the console
  - drydocs-agents       # Google ADK agent service under agents/ (graph_qa, core_ingest, controlm_fix; Epic R, phase 15) — own venv; folder name ≠ module name by rule: MODULE_MAP.md §Naming (S7)
  - drydocs-docmeta      # document-INGESTION component — BUILT at Q6 (ADR 0006): connectors, cleaner, manifest, policy, registry, tokenizer + live CLI verbs. No longer 'planned'; corrected 2026-08-04 at V1, whose 'if still placeholder' re-check caught it. Name still WORKING pending the Q4 gate (docmeta vs deepdoc fold-in). Distinct from drydocs-docgen, which renders docs
  - reference            # reference/ + external/ vendor & standards material
  - taxonomy             # config/taxonomy/ captures
  - ontology             # drydocs_core/schema/, relationship vocabulary, mappings
  - config               # config/ precedence, registries, crosswalks
  - graph-infra          # Neo4j topology, provisioning, deployment targets
  - docs                 # plans, ADRs, process docs, skills, rituals

items:

  # ── Epic A — Reference hygiene (reference-librarian, haiku) ────────────────
  - id: A1
    epic: reference-hygiene
    title: "Audit reference registry paths for staleness"
    type: chore
    module: reference
    phase: 0
    agent: reference-librarian
    model: haiku
    priority: p2
    status: done
    depends_on: []
    inputs: [reference/REGISTRY.yaml]
    acceptance: "every entry marked ok/stale; stale paths fixed or flagged"
    notes: "Done 2026-06-21. All local mirrors + platform/standard doc paths resolve; no stale entries. verified: 2026-06-21 in REGISTRY.yaml."

  - id: A2
    epic: reference-hygiene
    title: "Account for every BMC doc in the source manifest"
    type: chore
    module: reference
    phase: 0
    agent: reference-librarian
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [external/orchestration/bmc-controlm/SOURCE-MANIFEST.md]
    acceptance: "every .md in bmc-controlm/ accounted for in the manifest"
    notes: "Done 2026-06-21. All 26 bmc-controlm .md files are referenced in SOURCE-MANIFEST.md (0 uncovered)."

  - id: A3
    epic: reference-hygiene
    title: "Seed the research index with linked rationale"
    type: chore
    module: reference
    phase: 0
    agent: reference-librarian
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [reference/research/README.md, docs/restructure/00-conceptual-model.md]
    acceptance: ">=5 rows, each with a working link and a 'why it matters'"
    notes: "Done 2026-06-21. 7 rows with working links (3 Neo4j blogs + PROV-O/SOSA/ORG/agent-memory) + why-it-matters."

  - id: A4
    epic: reference-hygiene
    title: "REGISTRY.yaml points the dcat row at a per-standard README that was never written"
    type: chore
    module: reference
    phase: 0
    agent: reference-librarian
    model: haiku
    priority: p3
    status: done   # closed 2026-08-07 (desktop, same session as the claim). RESOLUTION (a) — the per-standard page was written, reference/standards/dcat/README.md, because DCAT is LOAD-BEARING here, not decorative: the namespace is registered in namespaces.py and `asset_type: dcat:Dataset` is stamped on ~42 registry dataset rows, with Distribution/DataService/Catalog/CatalogRecord in the catalog crosswalk, contactPoint in the pending email-DL gate direction, and mediaType on capture adapters — a standard carrying that weight deserves the dcmi-terms treatment (M4 pattern: IRI + spec source_url + where-used table + discipline block), not a README re-point. REGISTRY.yaml's dcat row is UNCHANGED — its docs: path simply resolves now. Guards green.
    depends_on: []
    inputs: [reference/REGISTRY.yaml, reference/standards/README.md, reference/standards/dcmi-terms/README.md]
    acceptance: >
      reference/REGISTRY.yaml's dcat row no longer cites a path that does not exist.
      Exactly one of two resolutions, and the choice is recorded in the close note:
      (a) write reference/standards/dcat/README.md following the dcmi-terms pattern
      from M4 — namespace, what we use it for, the specific terms we reference,
      source_url — or (b) re-point the row's `docs:` at reference/standards/README.md
      the way the skos row already does. Nothing else in the registry changes.
    notes: >
      Groomed 2026-08-04 (weekly groom); noticed during the M4 build. VERIFIED at the
      groom: reference/standards/ holds dcmi-terms, dprod-ekgf, prov-o, sosa-ssn,
      w3c-org and README.md — no dcat directory — while REGISTRY.yaml:88 reads
      `docs: reference/standards/dcat/README.md`. dcat already has a registry row and a
      row in the standards README table, so the gap is the per-standard file only.
      Which resolution is right is the reference-librarian's call at build: write the
      stub if DCAT is load-bearing for us (dcat:Distribution appears in the company
      filename-component standard, dcat:contactPoint in the email-DL gate prompt), or
      re-point the row if it is a cited vocabulary only, like skos. Worth doing inside
      A1's registry-path audit if that item is picked up first — every `docs:` path in
      the registry should resolve, and this is the one that does not.

  # ── Epic B — Taxonomy capture (taxonomy-importer, sonnet) — Phase 1 ────────
  - id: B1
    epic: taxonomy-capture
    title: "Capture Control-M folders/jobs/conditions as taxonomy"
    type: task
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: ["drydocs/data/samples/controlm_*.csv", "config/source-registry.yaml#controlm-psgmgr"]
    acceptance: "faithful classification of the source CSVs; zero meaning edges in the file"
    output: config/taxonomy/controlm.yaml
    notes: >
      Done 2026-06-20. Captured 4 servers / 8 folders / 13 jobs / 15 conditions (7 distinct names),
      classification only. Count drift RESOLVED: bundled sample treated as canonical; README
      m3-verify block reconciled (8/13/15/10). Residual flagged: sample is reduced (folders 161020,
      160501 active-but-jobless -> m3-verify empty=2) — regenerate from psgmgr to restore. See
      controlm.yaml open_questions#count-drift-vs-doc (status: resolved-2026-06-20).

  - id: B2
    epic: taxonomy-capture
    title: "Capture SEAL business applications as taxonomy"
    type: task
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: ["drydocs/data/samples/seal_*.csv", "config/source-registry.yaml#seal-extract"]
    acceptance: "every BusinessApplication has its two-port classification; no ownership semantics asserted"
    output: config/taxonomy/business-application.yaml
    notes: >
      Done 2026-06-20. Generic concept BusinessApplication (SEAL = source of record, id = SEALID,
      per SME note). 3 apps / 2 ports each / 15 memberships / 6 roles. Employees by SID (real
      names confidential -> internal/). classification: Internal-Confidential. Noted SEAL's own
      info_classification correlates with the sensitivity axis (flag for C2).

  - id: B3
    epic: taxonomy-capture
    title: "Capture the LOB→Product→Team hierarchy as taxonomy"
    type: task
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: ["drydocs/data/samples/{catalog_lobs,product_lines,products,dev_teams}_*.csv", "config/source-registry.yaml#catalog-pat"]
    acceptance: "hierarchy matches catalog samples; confidential names referenced by id, not value"
    output: config/taxonomy/lob-product-team.yaml
    notes: >
      Done 2026-06-20. LOB ▸ ProductLine ▸ Product ▸ DevTeam: 5/5/6/6. AreaProduct + BusinessSegment
      NOT in sample (flagged OQ). classification: Internal-Confidential.

  - id: B4
    epic: taxonomy-capture
    title: "Capture the Oracle schema shape template as taxonomy"
    type: task
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: ["config/source-registry.yaml#oracle-schemas", "internal/schemas/"]
    acceptance: "structure present; no real schema/table/SID values committed outside internal/"
    output: config/taxonomy/oracle-schemas.yaml
    notes: >
      Done 2026-06-20 as a SHAPE TEMPLATE (no sample; source confirmed:false). Schema ▸ Table; Script
      with placeholder ids; real names -> internal/schemas/ by id. classification: Internal-Confidential.

  # ── Epic C — Ontology mapping + HITL (ontology-mapper, sonnet) — Phase 2 ───
  - id: C1
    epic: ontology-mapping
    title: "Confirm Control-M edge mappings through the HITL gate"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p1
    status: done
    depends_on: [B1]
    inputs: [config/taxonomy/controlm.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, docs/RELATIONSHIP_GUIDE.md]
    acceptance: "each Control-M edge type traces to a matrix row or recorded alias; gate run logged"
    output: config/taxonomy-ontology-map.yaml
    notes: >
      Done 2026-06-21. 4 edges confirmed via HITL gate (batch — all reuse active vocab terms):
      SCHEDULED_ON (null PROV, infra), REQUIRES_IN_CONDITION (prov:used), EMITS_OUT_CONDITION
      (prov:generated), WAS_INFORMED_BY (prov:wasInformedBy). CONTAINS_JOB already applied.
      Gate run logged in config/gate-log.md.

  - id: C2
    epic: ontology-mapping
    title: "Confirm SEAL mappings against DPROD/ORG terms"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p1
    status: done
    depends_on: [B2]
    inputs: [config/taxonomy/business-application.yaml, reference/standards/dprod-ekgf/README.md, reference/standards/w3c-org/README.md]
    acceptance: "DPROD/ORG terms cited; mappings confirmed"
    output: config/taxonomy-ontology-map.yaml
    notes: >
      Done 2026-06-21 via HITL gate. 4 confirmed (batch, reuse active seal_*): HAS_PORT (dprod),
      HAS_MEMBERSHIP (org), OF_ROLE (org), HELD_BY (org). Concept BusinessApplication / SEALID.

  - id: C3
    epic: ontology-mapping
    title: "Confirm catalog hierarchy mappings; resolve SUPPORTS range"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p1
    status: done
    depends_on: [B3]
    inputs: [config/taxonomy/lob-product-team.yaml, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: "no free-text union ranges (resolve SUPPORTS Product-vs-AreaProduct); each edge has precise from/to + authority tag"
    output: config/taxonomy-ontology-map.yaml
    notes: >
      Done 2026-06-21 via HITL gate. 4 hierarchy edges confirmed + SUPPORTS range RESOLVED:
      SUPPORTS → AreaProduct ('aligned to' = team_type=aligned); NEW DevTeam DEVELOPS
      BusinessApplication joined by SEALID. C4 must set catalog_supports_area_product + arch_develops
      active and deprecate catalog_supports (DevTeam→Product). Gate logged.

  - id: C4
    epic: ontology-mapping
    title: "Reconcile the relationship vocabulary with the confirmed map"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p1
    status: done
    depends_on: [C1, C2, C3]
    inputs: [drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml]
    acceptance: "test_schema.py drift guard green; map summary == vocabulary active count"
    notes: >
      Done 2026-06-21. Reconciled vocabulary to the confirmed map: catalog_supports_area_product
      + arch_develops set active (added DEVELOPS block to catalog_ontology_supplement.cypher);
      catalog_supports (DevTeam→Product) deprecated. Drift guard green (9 passed); 164 passed.
      Remaining phase-2 `planned` entries (m3_invokes/triggers, p2_*, etc.) stay planned until
      their taxonomy is captured — not re-gated now (no data). Epic C complete.

  - id: C5
    epic: ontology-mapping
    title: "Rule for relationships between nodes derived from the SAME source row (city/state/country pattern)"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/RELATIONSHIP_GUIDE.md, drydocs/loaders/cypher/controlm_folders.cypher, config/taxonomy-ontology-map.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      A generic rule for same-row-derived node relationships (chain the hierarchy vs
      star-to-entity; when a direct derived-node edge restates a join and is banned) is
      documented in RELATIONSHIP_GUIDE.md and SME-confirmed via the HITL gate; existing
      loaders audited against it (live case: CM_DEF_VTAB row -> folder + ControlMServer +
      ControlMApplication — should app<->server relate directly or only through the
      folder?); an m3-verify or graph-tests invariant added, or its omission recorded in
      the gate log. Full suite green.
    notes: >
      Groomed 2026-07-08 from the IDEAS question (captured 2026-07-07 while landing the
      folder-pass ControlMApplication extension). Re-opens phase 2 — methodology gap, not
      a new source.
      2026-07-18 — DONE (gate same-row-derived-edges SIGNED OFF; ontology-mapper
      drafted, SME confirmed as written). Rule live in RELATIONSHIP_GUIDE.md
      (hierarchy: chain, no skip-level; star: satellites relate to the subject
      only; exceptions gate-only with an independent source). Live case: NO
      direct ControlMApplication<->ControlMServer edge — the SME's what-would-
      it-mean probe resolved to "nothing the per-folder traversal doesn't
      already say"; m3-verify guard added. Audit: 22 cypher files, all conform
      EXCEPT pat_product_mapping.cypher (writes the 2026-06-21-deprecated
      catalog_supports every run + planned catalog_has_application) — SME
      supplied PAT evidence reframing it as possibly independently-asserted;
      spun out as C9 (p1, fable) rather than fixed blind. Vocabulary untouched.

  - id: C6
    epic: ontology-mapping
    title: "Register REQUIRES_SCHEDULER (BatchProcessing → SchedulerKind) as planned; gate before wiring"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs_core/ontology/relationship_vocabulary.yaml, docs/RELATIONSHIP_GUIDE.md, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      REQUIRES_SCHEDULER (:BatchProcessing → :SchedulerKind), which appears in README/plans
      but is NOT in relationship_vocabulary.yaml, is registered there status: planned with its
      from/to nodes + a PROV mapping (or a recorded null-PROV/infra rationale); the post-load
      step that would WRITE the edge is NOT wired until the HITL gate confirms it; gate routing
      recorded (gate-log or map open-question). Drift guards green.
    notes: >
      Groomed 2026-07-09 from the IDEAS line (2026-07-01 Control-M naming review). Verified
      2026-07-09 still absent from relationship_vocabulary.yaml. Ontology/edge-meaning decision
      ⇒ the gate, never auto-decided; re-opens phase 2 alongside C5.
      DONE 2026-07-09: registered seal_requires_scheduler (BatchProcessing → SchedulerKind,
      status: planned, prov_maps_to: ~ null-PROV/infra — same family as SCHEDULED_ON, loader
      NOT wired) in relationship_vocabulary.yaml, plus a proposed `requires-scheduler` entry in
      config/taxonomy-ontology-map.yaml (summary proposed 6→7) carrying the 3 gate open-questions
      (domain node :Application-vs-:BatchProcessing per README.md:263 vs LoadPlanV3; SchedulerKind
      node_classifications gap; null-PROV confirmation). No graph write until the HITL gate. Drift
      guards green (test_schema.py skips planned entries; prov_matrix unaffected).
      2026-07-09 user clarifications (same day, both captured as gate open-questions + IDEAS,
      nothing wired): (a) SEAL two-port model is (:Application)-[:HAS_PORT]->(:Port {kind}) —
      EventProcessing (events/API) + BatchProcessing (batch); REQUIRES_SCHEDULER is batch-only, so
      from_node refined BatchProcessing→Port (the batch :Port). (b) :SchedulerKind slated for
      deprecation → :AisCapability + :AiTool (not yet defined), so the to_node target is provisional
      and re-targets at the gate — SchedulerKind-deprecation captured as its own IDEAS item.

  - id: C10
    epic: ontology-mapping
    title: "Convert the external/ServiceNow doc set to readable text and mine each file for CMDB/CSDM concepts DryDocs should adopt"
    type: task
    module: reference
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-20 same session — housing + extraction + analysis committed
    depends_on: []
    inputs: ["external/ServiceNow/ (6 files, downloaded 2026-07-20: CMDB - Process Guide.docx, CMDB - Product Architecture.pptx, CMDB Data Manager.pptx, CMDB Governance Workshop.pptx, ITAM - SAM - Product Integration Options - Yokohama.pptx, What are services and service offerings.pdf)", knowledge/upgrade-plans/generic-terminology-research.md, external/orchestration/bmc-controlm/SOURCE-MANIFEST.md, config/classification.yaml, reference/REGISTRY.yaml]
    acceptance: >
      (1) HOUSING: external/ServiceNow/ is a registered source — a README/SOURCE-MANIFEST
      per the bmc-controlm pattern records per-file provenance (source_url where known,
      download date 2026-07-20, vendor = ServiceNow), the trust axis, and a sensitivity
      classification SET at execution (External expected — vendor material — but the
      PDF/PPTX redistribution question is decided and recorded, not assumed);
      classification guards green. (2) CONVERSION: every file has a readable-text twin
      (extracted/*.txt or .md — the SDLC-Docs/extracted idiom; pdf via the pypdf
      precedent, docx/pptx extractor chosen at execution) so agents can grep and read it
      without binary parsing. (3) ANALYSIS: a per-file detailed analysis note exists
      (knowledge/upgrade-plans/servicenow-cmdb-analysis.md or similar) — for EACH file
      the key concepts it defines (service vs service offering, CMDB CI classes, CSDM
      domains, data-manager/governance practices, ITAM/SAM integration shapes), each
      dispositioned incorporate/park/reject with its DryDocs target (taxonomy capture,
      ontology term, terminology-research cross-ref, or ops/console practice); the CSDM
      service/service-offering model explicitly cross-referenced against the
      generic-terminology research note (Business Application / Application Portfolio)
      and our existing labels. (4) THE HARD RULE: nothing is adopted by this item — any
      concept implying node/edge meaning lands as status: planned/proposed and routes to
      the HITL gate; the analysis note lists those candidates. Full suite green.
    notes: >
      Groomed 2026-07-20 directly from chat (the C9 direct-promotion precedent). User
      context: during full-circle-docs planning the user considered shipping DryDocs as
      a ServiceNow Marketplace app (research only — no plan existed) and referenced the
      CMDB for the taxonomy to create a corresponding ontology; this doc set is that
      reference material, downloaded 2026-07-20. Feeds the parked generic-terminology
      idea (SEAL -> Application Portfolio / PAT -> Product Taxonomy) — the CSDM
      service/service-offering layer is the missing piece of that research. Files stay
      UNTRACKED until this item's housing step decides classification (no binaries
      committed before the redistribution question is answered; the extracted text
      twins may become the committed form if the binaries stay local). Epic/phase call:
      ontology-mapping / phase 2 because the mining target is ontology correspondence —
      a future productization epic may adopt the follow-on work.
      DONE 2026-07-20: (1) HOUSING — external/ServiceNow/README.md is the SOURCE-MANIFEST
      (Now Create asset numbers 0003948/0002024/0003551/0001261/0001397; classification
      External, captured_at 2026-07-20; REDISTRIBUTION DECIDED per the BMC-poster rule:
      binaries AND verbatim extracted twins gitignored local-only — a full-text dump is
      the vendor's words; committed record = manifest + analysis). (2) CONVERSION —
      scripts/extract_office_text.py (stdlib docx/pptx + pypdf) wrote 6 local text twins
      (~200k chars). (3) ANALYSIS — knowledge/upgrade-plans/servicenow-cmdb-analysis.md:
      per-file concepts dispositioned incorporate/park/reject + the CSDM<->DryDocs
      correspondence table (BusinessApplication ALIGNED with cmdb_ci_business_app;
      Information Object ~= DataAsset; CCB/RACI validates the HITL gate; Yokohama
      renamed Application Service -> Service Instance — adopt concepts, not vendor
      labels). (4) Five gate-bound/follow-up candidates listed — NOTHING adopted:
      deployed-instance concept + attestation practice = gate; per-attribute lineage ->
      provenance-plan next touch; verify-vocabulary alignment + InformationObject
      annotation = docs/note-level. Two large files read via subagents. Suite 738.

  # ── Epic D — Config-driven loaders (pipeline-config + main) — Phase 3 ──────
  - id: D1
    epic: config-loaders
    title: "Land the P0 loader fixes (SCHEDULED_ON, datetime, stale edges)"
    type: bug
    module: drydocs-load
    phase: 3
    agent: main
    model: opus
    priority: p0
    status: done
    depends_on: []
    inputs: [drydocs/loaders/cypher/controlm_folders.cypher, drydocs/loaders/cypher/controlm_jobs.cypher, docs/reviews/drydocs-consolidated-plan.md]
    acceptance: "RUNS_ON->SCHEDULED_ON + datetime() + stale_edge_cleanup landed; migrations idempotent; m3-verify green; tests green"
    notes: >
      Done 2026-06-21. B.1: folders.cypher writes SCHEDULED_ON (was RUNS_ON — a real bug;
      cli.py m3-verify already expected SCHEDULED_ON) + idempotent migrate_runs_on_to_scheduled_on.cypher.
      B.2: stale_edge_cleanup.cypher (for the incremental loader). B.3: empty-safe, space->T-tolerant
      datetime() on capture_date/last_updated/active_from/active_till; version_timestamp left as string
      (compact YYYYMMDDHHMMSS, not ISO). 164 tests pass. m3-verify needs a live Neo4j+APOC to run; the
      loader now matches its SCHEDULED_ON expectation. (D2/D3 = config-driven loaders, next.)

  - id: D2
    epic: config-loaders
    title: "Drive catalog reconciliation from precedence.yaml"
    type: task
    module: config
    phase: 3
    agent: pipeline-config
    model: sonnet
    priority: p2
    status: done
    depends_on: [C3, D1]
    inputs: [config/precedence.yaml, drydocs/loaders/cypher/catalog_lobs.cypher]
    acceptance: "flipping order: in precedence.yaml changes RECONCILES_TO resolution with no code edit"
    output: drydocs_core/precedence.py
    notes: >
      Done 2026-06-21. New drydocs_core/precedence.py: PrecedenceResolver reads config/precedence.yaml
      (order + active toggles + conflict_policy); resolve(claims) -> highest active authority with
      an opinion wins, losers kept as aliases (skos:closeMatch, never dropped). Wired into the
      RECONCILES_TO path: CatalogLOBsLoader.to_params resolves via resolve_lob_reconciliation;
      catalog_lobs.cypher now records r.authority + r.aliases. test_precedence.py (10 tests) proves
      the acceptance — flipping order: in a YAML (loaded via from_yaml) flips both the resolver
      winner AND the catalog reconciled segment, no code edit. pyyaml promoted to a runtime dep.
      174 passed.

  - id: D3
    epic: config-loaders
    title: "Fail fast when loading an unconfirmed source"
    type: task
    module: config
    phase: 3
    agent: pipeline-config
    model: sonnet
    priority: p2
    status: done
    depends_on: [C4, D1]
    inputs: [config/source-registry.yaml, drydocs/cli.py]
    acceptance: "loading a source with confirmed:false fails fast with a clear message"
    output: drydocs_core/source_registry.py
    notes: >
      Done 2026-06-21. New drydocs_core/source_registry.py: SourceRegistry.require_confirmed(id)
      raises UnconfirmedSourceError (clear message + crosswalk pointer) for confirmed:false or
      missing-flag (fail-closed), UnknownSourceError for undeclared ids. cli.py: LOADER_SOURCE
      map + _gate_source() wired into load / refresh-reference / ingest-controlm — gate fires
      BEFORE any adapter/Neo4j touch (exit 2). test_source_registry.py (6) incl. a CliRunner
      test proving `drydocs load` fails fast for an unconfirmed source. 180 passed.

  - id: D4
    epic: config-loaders
    title: "Fix psgmgr version filter: IS_CURRENT_VERSION = 'Y' (was '1') across extracts, DDL, cypher, and references"
    type: bug
    module: drydocs-load
    phase: 3
    agent: main
    model: sonnet
    priority: p1
    status: done          # done 2026-07-15, same session as the groom (main/fable)
    depends_on: []
    inputs: [drydocs/loaders/sql/, drydocs/loaders/cypher/controlm_jobs.cypher, docs/design/controlm-ingestion-tdd.md, docs/controlm-staging-ingestion-flow.md, .claude/skills/controlm-db/SKILL.md]
    acceptance: >
      All psgmgr version filters compare IS_CURRENT_VERSION = 'Y': extracts
      (controlm_folders/jobs/conditions_in/conditions_out/variables/variables_scenarios
      + adhoc probes), staging DDL views, controlm_jobs.cypher .active derivation,
      models/controlm.py field description, controlm-db skill (SKILL.md, query-cookbook,
      schema-crosswalk, ingest), the TDD "two filters" section, and C3/status + port-guide
      docs; no IS_CURRENT_VERSION = '1' remains outside history/audit prose;
      test_controlm_cypher asserts 'Y'; staging-ingestion-flow preflight 0.3 marked
      RESOLVED = 'Y'; the CM_DEF_SETVAR_VW extracts carry a comment flagging the view's
      own domain for a one-line company preflight (SELECT DISTINCT IS_CURRENT_VERSION)
      before the production variables run; design-doc renders regenerated; full suite green.
    notes: >
      Groomed 2026-07-15 from the finalized company Control-M ingestion TDD (captured
      LOCAL-ONLY at internal-local/company-backflow/controlm-ingestion-tdd.md — gitignored).
      Evidence: the company's finalized extracts filter 'Y' on CM_DEF_VJOB (incl. the
      JOB_ID=1 header join) and CM_DEF_LNKI/LNKO_P_VW, and their worked example returned
      the real 2-folder/8-job population — 'Y' is live-verified; producer '1' never ran
      live (the 2026-07-10 live check verified the SETVAR view's name/columns, not the
      value domain). Closes staging-ingestion-flow open point 0.3.
      Done 2026-07-15: flipped 11 SQL files (extracts + DDL views + adhoc probes incl.
      profile_cm_avg_run), controlm_jobs.cypher .active derivation, constraints.cypher
      comment, models description, source-mappings ledger (gate §Q2 probe closed),
      controlm-db + reconcile-port skill refs, TDD/staging-flow/C3/git-readme docs;
      fixtures flipped '1'/'0'→'Y'/'N' (36 rows, 4 CSVs); lineage extractor accepts
      ('Y','1') for legacy synthetic CSVs; SETVAR preflight comment added; superseded
      docs/reviews left as history. 655 tests pass; design renders regenerated.

  # (D5/D6 groomed 2026-07-15 pm — findings from the first live EE bootstrap, O6 session.)
  - id: D5
    epic: config-loaders
    title: "Harden Neo4jClient.run_script against APOC's comment-';' statement split (Cypher 25 rejects the empty fragment)"
    type: bug
    module: drydocs-core
    phase: 3
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-18 (main fable session, D5-D7 batch)
    depends_on: []
    inputs: [drydocs_core/neo4j_client.py, drydocs/loaders/base.py, drydocs_core/schema/]
    acceptance: >
      run_script no longer hands the raw script to apoc.cypher.runMany: it splits
      statements CLIENT-SIDE with a comment/string-aware scanner (generalize
      base.py::_code_semicolons into one shared helper — split points, not just a count)
      and executes per-statement, preserving the DDL+DML-in-one-file behavior the
      docstring promises. A unit test (duck-typed client, no live DB) proves a script
      containing an end-of-line ';' inside a // comment plus a trailing ';' yields no
      empty statement and every real statement exactly once; the loaders' run()/runMany
      dispatch keeps its existing guard tests. The protective wording added to
      ontology_supplement.cypher (8478204) may then be relaxed to a plain comment.
      Full suite green.
    notes: >
      Groomed 2026-07-15 pm from the same-day inbox bug. Seen live: the first EE-container
      bootstrap (Neo4j 2026.05.0 / Cypher 25) failed apply-ontology-supplement because
      runMany splits on every end-of-line ';' including inside // comments, and the new
      server rejects the comment-only fragment as an empty statement ("CYPHER 25" +
      nothing) — 5.x tolerated it. One-line comment reword shipped on main (8478204) as
      the unblock; THIS item is the class fix so supplement/schema files can't re-trip it.
      DONE 2026-07-18: drydocs_core/cypher_split.py — the ONE shared comment/string-aware
      scanner (split points + count + has_code + split_statements); run_script now splits
      CLIENT-SIDE and executes per-statement in auto-commit transactions (DDL+DML-in-one-
      file preserved; the APOC runMany dependency dropped from run_script entirely, so a
      comment ';' can never shear again and comment-only fragments never reach the
      server); base.py imports the shared count (private alias kept — tested surface);
      ontology_supplement.cypher protective wording relaxed to a plain note.
      tests/unit/test_cypher_split.py covers the acceptance script shape incl. the
      duck-typed-driver run_script proof; existing dispatch guards untouched. Suite 707.

  - id: D6
    epic: config-loaders
    title: "Make the README quick start verify clean: 2 active sample folders have no jobs, failing m3-verify"
    type: chore
    module: drydocs-load
    phase: 3
    agent: main
    model: haiku
    priority: p3
    status: done          # DONE 2026-07-18 (main fable session, D5-D7 batch)
    depends_on: []
    inputs: [drydocs/data/samples/controlm_folders__sample.csv, drydocs/data/samples/controlm_jobs__sample.csv, drydocs/cli.py]
    acceptance: >
      EITHER folders 161020 and 160501 each gain at least one sample job row (conditions/
      dependencies CSVs and any fixture-count tests updated to match) OR m3-verify
      downgrades the "active folders contain at least one job" invariant to a WARNING for
      the CSV sample adapter (kept failing for Oracle runs) — either/or decided at
      execution. Result either way: the README quick-start chain (bootstrap -> supplements
      -> refresh-reference -> ingest-controlm -> m1-verify -> m3-verify) exits 0 against
      the bundled samples on a fresh database. Full suite green.
    notes: >
      Groomed 2026-07-15 pm from the same-day inbox chore (seen at the first live EE
      bootstrap: empty=2 total=7, exit 1 — pre-existing sample-data gap, not a loader
      bug). Sample rows are SYNTHESIZED — keep the publish boundary (no real names).
      MERGED 2026-07-18 (EE re-bootstrap inbox line, 2026-07-16, its own suggestion):
      the quick-start/bootstrap sequence this item makes verify clean also gains the
      demonstrable-content reloads — load-software-registry + load-bmc-docs (+
      optionally load-essential-graphrag, ddcontext) — so a fresh container PROMPTS
      their reload instead of silently losing corpora (found at Q3: the fresh O6
      container had no bmc-docs corpus; the P0 spike re-ran both loads, proving the
      gap and the two-command fix).
      DONE 2026-07-18: either/or decided as ADD JOBS (invariant stays strict) —
      folders 161020/160501 gained two synthesized jobs each (PROV + AUT-TRUST
      pairs, naming grammar kept, publish boundary clean); fixture counts updated
      (smoke.ps1 + run-drydocs SKILL.md 13->17; the depgraph-oracle parser test
      extended: 17 INVOKES, 10 ABINITIO + 7 SHELL, 16 distinct children); README
      expected m3-verify output reconciled to live (17 jobs, empty=0, 8 distinct
      WAS_INFORMED_BY edges from the 10 dependency rows, apps=0 sample-mode note)
      and quick-start gained step 3b (the merged demonstrable-content reloads with
      the re-bootstrap warning). VERIFIED LIVE: ingest-controlm + m3-verify exit 0,
      every invariant yes incl. "active folders contain at least one job" empty=0.

  - id: D7
    epic: config-loaders
    title: "Incremental-delete path for removed-from-source jobs: full-diff soft-delete mark + retention-window hard-delete sweep"
    type: requirement
    module: drydocs-load
    phase: 3
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-18 (main fable session, D5-D7 batch)
    depends_on: []
    inputs: [drydocs/loaders/base.py, drydocs/loaders/, knowledge/upgrade-plans/neo4j-advisor-confirmation-2026-07-17.md]
    acceptance: >
      After an ingest run whose extract no longer contains a previously loaded job or
      folder, the graph node is NOT silently left active: a full-diff ID set (graph
      minus extract, scoped to what the extract declares — full vs folder-filtered)
      marks it soft-deleted (property mark + timestamp; no node deletion) in the same
      run, and a separate sweep command hard-deletes marks older than a configurable
      retention window. Both paths report counts (marked / re-activated / swept /
      retained — never silent drops, the STG_PARSE_QUALITY house rule); unit tests
      cover mark, re-appear (mark cleared), filtered-extract scoping (out-of-scope
      nodes untouched), and sweep retention. Property-only mechanism — any edge-meaning
      change aborts to the HITL gate. Full suite green.
    notes: >
      Groomed 2026-07-18 from the GraphAcademy advisor findings (2026-07-17 inbox;
      confirmation doc in knowledge/upgrade-plans/). Today removed-from-source jobs
      stay `active` forever. Soft-delete property naming is loader mechanics (no new
      edge types); surface at a gate only if a meaning question appears.
      DONE 2026-07-18: BaseLoader opt-in mark pass — sweep_label +
      sweep_scope_property classvars; the full-diff falls out of the last_run_id
      bookkeeping every template writes (in-scope node on an older run id =
      removed from source). Jobs scope to the extract's own folder set; unscoped
      loaders (folders) mark only under full_extract=True, which ingest-controlm
      passes when no --folder filter is set. Mark = removed_from_source_at +
      removed_by_run_id (property-only, checksum-excluded); re-appeared nodes
      auto-clear; counts reported on LoadSummary + the JobRun envelope + console.
      sweep_removed() + CLI `sweep-removed` (--days/--label/--dry-run/--yes;
      DETACH DELETE past retention; swept/retained table). 9 unit tests
      (mark/reactivate/scoping/full-extract gate/empty-extract-declares-nothing/
      sweep + dry-run). VERIFIED LIVE: a reduced extract marked exactly
      AUTO_TRUST_LOAD (160501/12, scope folder_id x7), the full reload
      reactivated it, sweep --dry-run reported 0/0. Suite 707 passed.

  # ── Epic E — Context graph pilot (ontology-mapper + main) — Phase 4 ────────
  - id: E1
    epic: context-graph
    title: "Register SOSA/SSN observation terms via the gate"
    type: task
    module: ontology
    phase: 4
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: in_progress
    depends_on: [C1]
    inputs: [reference/standards/sosa-ssn/README.md, drydocs_core/ontology/relationship_vocabulary/]
    acceptance: "sosa:* terms present with IRIs; status planned->confirmed via gate"
    notes: >
      Registration step DONE (main, 2026-06-22): sosa/ssn prefixes in namespaces.py;
      terms seeded in drydocs_core/schema/sosa_experimental_supplement.cypher (opt-in
      `drydocs apply-sosa-supplement`, NOT in bootstrap; every term adoption:"experimental");
      4 context relationships in relationship_vocabulary.yaml (sosa_observes/has_result/
      made_by_sensor/observed_property, status planned, domain context); README split into
      Declared-Adopted vs Experimental/Early-Adoption. REMAINING for ontology-mapper: take the
      `jobrun-observation` map entry through the HITL gate (resolve ControlMJobRun-as-Observation
      vs separate-Observation-node; run-history source), then planned->confirmed.
      GATE SESSION DEFERRED 2026-07-14 (SME: not ready yet) — page stays rendered/pending;
      nothing flips; re-present at a future session (see gate-log 2026-07-14 deferral note).

  - id: E2
    epic: context-graph
    title: "Answer one folder health/freshness question with a context query"
    type: requirement
    module: ontology
    phase: 4
    agent: main
    model: opus
    priority: p2
    status: todo
    depends_on: [E1]
    inputs: [config/taxonomy-ontology-map/]
    acceptance: "one Cypher query returns latest result + resultTime per folder on sample data; documented in docs/"

  # ── Epic F — Orchestrator expansion (pipeline-config) — Phase 5 ────────────
  - id: F1
    epic: orchestrator-expansion
    title: "Map the AutoSys crosswalk to the BMC baseline"
    type: requirement
    module: config
    phase: 5
    agent: pipeline-config
    model: sonnet
    priority: p3
    status: done          # gate autosys-crosswalk SIGNED OFF 2026-07-14 (gate-log); autosys-export confirmed: true
    depends_on: [C1]
    inputs: [external/orchestration/autosys/README.md, config/source-registry.yaml]
    acceptance: "AutoSys crosswalk fully mapped to baseline; no invented concepts; gate logged; confirmed:true"

  - id: F2
    epic: orchestrator-expansion
    title: "Map the Airflow/MWAA crosswalk to the BMC baseline"
    type: requirement
    module: config
    phase: 5
    agent: pipeline-config
    model: sonnet
    priority: p3
    status: done          # gate airflow-crosswalk SIGNED OFF 2026-07-14 (gate-log); airflow-mwaa confirmed: true
    depends_on: [C1]
    inputs: [external/orchestration/airflow/README.md, config/source-registry.yaml]
    acceptance: "Airflow/MWAA crosswalk fully mapped to baseline; no invented concepts; gate logged; confirmed:true"

  # ── Epic G — Modular architecture / component topology (main + ADR 0002) ───
  # Groomed 2026-06-26 from ADR 0002 follow-ups 1–6 (docs/decisions/0002-*.md), accepted
  # via the SME gate (config/gate-log.md, 2026-06-26). Edition = Neo4j Enterprise (committed).
  - id: G1
    epic: component-topology
    title: "Author and locally validate the multi-DB topology"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: []
    inputs: [docs/decisions/0002-component-database-topology.md, drydocs_core/schema/, drydocs_core/neo4j_client.py]
    acceptance: >
      Provisioning authored + validated on a LOCAL Neo4j Enterprise (Docker, free dev license):
      CREATE DATABASE drydocs + drydocs_context, CREATE COMPOSITE DATABASE drydocs_all aliasing
      both; assetId/jobId uniqueness constraints + indexes in BOTH data DBs (proxy-node keys);
      a drydocs_all smoke query reads both constituents and writes neither. Scripts are
      target-agnostic (run unchanged against any multi-DB-capable DBMS).
    notes: >
      ADR 0002 follow-up #1, split for infra reality. TESTING is capped at ONE database on the live
      Aura tier (single node) — multi-DB + composite are Enterprise-only features. So the topology is
      authored + proven on a LOCAL Enterprise instance now; the LIVE deploy is G7 (blocked on infra).
      No data load — provisioning + proxy-node constraints only. See memory drydocs1-live-neo4j.
    progress: >
      2026-06-28 — AUTHORED: drydocs_core/schema/provisioning/ (01_databases.cypher, 02_proxy_constraints.cypher,
      smoke_drydocs_all.cypher, provision.ps1, README.md). Proxy-node keys reuse canonical identity
      (DataAsset.assetId, ControlMJob(folder_id,job_id)) — no identity invented.
      2026-07-01 — VALIDATED, DONE: scripts executed in provision.ps1 order via cypher-shell inside a
      local Docker Neo4j 2026.05.0 ENTERPRISE (container neo4j-drydocs-ee, host ports 7476/7689).
      drydocs + ddlineage + ddcontext + ddall composite all online; proxy constraints verified in all
      three data DBs (controlmjob_key NODE_KEY, dataasset_id UNIQUE); federated smoke over ddall read
      all constituents (0/0/0, empty topology) and wrote nothing. Live deploy remains G7 (blocked).

  - id: G7
    epic: component-topology
    title: "Deploy the multi-DB topology to the live target"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [G1]
    inputs: [docs/decisions/0002-component-database-topology.md]
    acceptance: >
      The G1 topology (drydocs + drydocs_context + drydocs_all composite + proxy-node constraints)
      is deployed to the production target and a drydocs_all smoke query passes there.
    notes: >
      ADR 0002 follow-up #1 (LIVE deploy). Was BLOCKED on the Aura single-DB tier; Aura was
      dropped 2026-07-06 (Docker EE is the committed enterprise path), which RE-SCOPES the
      producer-side live target to the self-managed EE container. DONE 2026-07-08, verified
      live: container neo4jtest (neo4j:5.26-enterprise-ubi10, named volume neo4j-testdata) —
      drydocs + ddlineage + ddcontext + ddall composite all online; the provisioning smoke
      read all three constituents through ddall (0/0/0, pre-ingest) and wrote nothing.
      The COMPANY-side live target remains a port concern (multi-DB Enterprise there —
      see docmeta plan / port-prompt), not a producer backlog item.

  - id: G2
    epic: component-topology
    title: "Extract drydocs-core behind the boundary shim"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: []
    inputs: [docs/decisions/0002-a-drydocs-core-extraction-plan.md, drydocs/, tests/]
    acceptance: >
      Per 0002-A done criteria: drydocs-core imports cleanly with NO component dependency;
      drydocs-load runs the existing pipeline unchanged; tests/unit/test_module_boundary.py
      passes; gates green (pytest -q, package import, --help).
    output: drydocs_core/ (physical core) + drydocs/ (component remainder — thin layout per ADR 0002-A-1)
    notes: >
      ADR 0002 follow-up #2; plan = 0002-A (thin, zero behavior change). BLOCKS G3 + G4. Shim/
      re-export first, then relocate; borderline staging.py + snapshots/writer.py → load (0002-A §6).
    progress: >
      2026-06-28 — STEP 1 DONE (0002-A §6 step 1, the zero-risk shim): drydocs_core/ package added,
      re-exports the core surface (models, adapters, controlm parser, neo4j, config/precedence/
      source_registry, ontology) from drydocs/.* with NO file moves; pyproject packages updated;
      test_module_boundary.py extended to scan drydocs_core (passes, stdlib). REMAINING: physical
      relocate (Phase B step 2+, when core stabilizes) + run full gates (poetry pytest -q, import,
      --help) to confirm pipeline unchanged — deferred (poetry/deps absent in the authoring env).
      2026-07-10 — FULL GATES RUN, ALL GREEN (toolchain fixed 2026-07-09): poetry run pytest -q =
      483 passed / 3 skipped (expected gitignored-CSV skips); test_module_boundary.py 4 passed;
      import drydocs.cli + drydocs_core OK; drydocs --help renders and the CLI exits 0 via
      CliRunner (the Store-venv drydocs.cmd wrapper mis-reports exit -1 — wrapper artifact, not
      a CLI defect). REMAINING: only the physical relocate (Phase B step 2+, when core stabilizes).
      2026-07-10 — PHYSICAL RELOCATE DONE (branch feat/g2-core-relocate; thin variant ratified as
      ADR 0002-A-1, amends 0002-A §6 step 4): 42 git renames move models/adapters/controlm/ontology/
      schema/neo4j_client/config/precedence/source_registry into drydocs_core/ (shim -> real
      package); staging.py -> drydocs/staging.py (load side, §6 borderline as decided); remainder
      KEEPS the drydocs name (4 component groups; rename + per-component packaging = Phase C).
      Boundary test enforces CORE_PREFIXES=drydocs_core; PORT-MANIFEST re-pathed (the promised
      path-column diff) + git-readme rename-wave rules. ALL GATES GREEN post-move: 483/3, imports,
      --help. ITEM COMPLETE — G3 + G4 unblocked.

  - id: G3
    epic: component-topology
    title: "Rebase the remediation spinoff onto drydocs-core"
    type: requirement
    module: drydocs-remediation
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: done
    depends_on: [G2]
    inputs: [docs/decisions/0002-b-spinoff-rebase-checklist.md, "ce-wilson/DryDocs-v0-archive@controlm-spinoff"]
    acceptance: >
      Per 0002-B: drydocs-remediation runs detect → transform → prove → Jira using ONLY
      drydocs-core for parsing + read-only corroboration; no-graph-write test, Jira-only test,
      and offline-equivalence proof all pass; boundary test holds for the new package.
    output: remediation/ (drydocs-remediation)
    notes: >
      ADR 0002 follow-up #3; checklist = 0002-B. Re-home its Control-M parsing onto core (drop the
      spinoff's own parser); keep only remediation logic. Writes NO graph; Jira = SoR. See
      reconcile-port skill. On done: mark the controlm-spinoff archive superseded.
      RE-TIERED opus -> fable 2026-07-10 (user call): the matrix top slot (boundary-changing
      judgment) now points at the Mythos-class tier; G2 (same class) ran on fable end-to-end.
      MODELS enum extended in test_backlog.py + validate.py; G4/K4 are re-tier candidates at
      their next groom touch.
    progress: >
      2026-07-10 — PULLED; 0002-B preconditions all ticked (ADR 0002 ACCEPTED; core physical;
      archive readable at 3e6a39a). STEP 2 INVENTORY DONE (0002-B §4 filled): the archive has NO
      remediation code — its IP is the R1-R29 rules registry (the Gate-2/Gate-3 rule source) +
      the governance corpus (10 docs, NOT carried) + 6 remediation plan docs (~630 lines).
      Parser diff: current core strictly ahead, zero archive-only deltas -> §2 step 3 is a no-op,
      no core PRs. 2026-07-10 (later) — DOC PORT DONE: 17 docs VERBATIM -> internal/remediation/
      (+governance/), classification Internal (content check found real folder/job/QR/VR values
      throughout — nothing knowledge/-eligible unsanitized); provenance README records the rule
      (unratified R-rules stay gate-bound; TDD wins on conflict). HELD for user classification
      review: governance/escalation-scim-reference.md + scim-hpsm-queue-registry.md (candidate
      Internal-Confidential). Held-docs decision: KEPT OUT (user call, same day) — re-entry is
      G10 (sanitized). 2026-07-10 (later) — §2 STEP 1 SCAFFOLD DONE (in-monorepo, user call):
      top-level drydocs_remediation/ package (naming per the drydocs_core convention, note in
      0002-B) — formats (DefinitionFormat seam) / detect / transform / equivalence / jira (only
      side-effect boundary), contracts in docstrings, bodies NotImplementedError; pyproject +
      boundary-test `remediation` group + MODULE_MAP row + test_remediation_scaffold.py.
      2026-07-10 (later) — M0 SLICE IMPLEMENTED: TranscriptDefinitionFormat (gate-1 fallback;
      XML impl BLOCKED on vendor schema acquisition — corpus stub), R1 dot-smuggling detector
      (core classifier), order-paired equivalence proof (core resolver, __DRYDOCS_WATCH__
      probe). Engine run on the REAL M0 unit reproduces the worked example verbatim — legacy
      baseline clean, R1 fires, shipped modern rewrite still diverges (MATCH=False) — recorded
      internal/remediation/m0/engine-run-2026-07-10.md; sanitized twins pinned as
      test_remediation_m0.py (6 tests). Verdict PENDING A3 ground truth + B1 var.text rule
      (company-side; core resolver deliberately untouched). 2026-07-10 (later) — TIER-1/HANDOFF
      SLICE DONE (TDD FR-REM-3/6 + NFR-REM-1; archive "M1 repo split" noted SUPERSEDED by
      0002-B): ratified-only Tier-1 engine + canonical-rename rule (idempotent, equivalence-
      preserving; unratified skipped loudly; rule values company-side, registry shape = OQ-2
      open), Jira handoff boundary (pure render, JiraSubmitter sole wire, unproven packages
      refuse, ownership surfaced-not-guessed), structural no-graph-write guard; TDD traceability
      matrix statuses updated + re-rendered. 2026-07-10 (close) — CORROBORATION WIRED
      (reconcile_variables + ReadOnlyGraph: the sole graph path, write Cypher refused BEFORE the
      driver — the runtime half of NFR-REM-1; live schema queries = Track-2 company-side) and
      G3 CLOSED: 0002-B §2/§3 all ticked, status DONE; controlm-spinoff archive recorded
      SUPERSEDED in ADR 0002 (stop maintaining; readable as source material only). Acceptance
      met: detect -> transform -> prove -> Jira all core-only; no-graph-write (structural +
      runtime), Jira-only, equivalence tests pass; boundary holds. CARRIED FORWARD (not G3):
      Tier-2 agentic lane (FR-REM-4, TDD), XML I/O (schema acquisition), A3/B1 ground truth +
      the real M0 unit's verdict (company-side), M2 generalization — TDD tracks these.

  - id: G4
    epic: component-topology
    title: "Scaffold the lineage and deepdoc component packages"
    type: requirement
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p3
    status: done
    depends_on: [G1, G2]
    inputs: [docs/decisions/0002-component-database-topology.md, drydocs_core/controlm/commands.py]
    acceptance: >
      drydocs-lineage and drydocs-deepdoc exist as separate packages that import ONLY drydocs-core
      and SHARE the core command-line/lineage parser; lineage writes curated lineage to drydocs;
      deepdoc writes to drydocs_context stamping reliability/trust; neither imports the other;
      boundary test passes.
    notes: >
      ADR 0002 follow-up #4 (D2). Scaffold only — wiring the real triggers (phased curation for
      lineage; on-failure for deepdoc) is later. deepdoc needs drydocs_context (G1). The lineage
      package is then populated by re-homing the depgraph prototype — see G9 + ADR 0002-C.
      RE-TIERED opus -> fable 2026-07-10 on pull (groom-touch policy from the G3 re-tier).
    progress: >
      2026-07-10 — SCAFFOLDED + DONE (commit range 22a35df..): top-level drydocs_lineage/
      (extract candidates via the shared core parser; CurationStatus proposed/confirmed/rejected;
      writer = sole write boundary, CONFIRMED-only -> drydocs) and drydocs_deepdoc/ (investigate
      on-failure via the shared parser; ContextFinding with mandatory reliability/trust stamps,
      SYNTHESIZED-origin; writer sole boundary -> drydocs_context; proxy-node URN keys; promotion
      = HITL gate, never cross-DB edit). Boundary test: lineage + deepdoc COMPONENT_GROUPS —
      the components-don't-import-each-other rule IS the D2 separation. Parser sharing asserted
      by identity in test_lineage_deepdoc_scaffold.py (5 tests). pyproject 5 packages;
      MODULE_MAP future-rows made real. Acceptance met at scaffold scope per notes: packages
      exist, import ONLY core, share the parser, declared write targets across the trust
      boundary, boundary test green (521/3 full suite). Trigger wiring + population deferred
      by design: lineage -> G9/0002-C (now READY), deepdoc on-failure -> future item.

  - id: G8
    epic: component-topology
    title: "Fold the depgraph parser deltas into the core Control-M parser"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/decisions/0002-c-depgraph-lineage-rehome.md, drydocs_core/controlm/commands.py, "ce-wilson/depgraph@feat/controlm-lineage:depgraph/controlm/commands.py"]
    acceptance: >
      The three depgraph parser deltas are folded into the drydocs-core Control-M parser
      (drydocs_core/controlm/commands.py, re-exported by drydocs_core): (1) .pset -> ABINITIO launcher
      rule; (2) _looks_script() so `spark-submit --master yarn x.py` resolves x.py NOT yarn (a live
      correctness bug) — with a regression test; (3) +pset in the direct-script extension regex.
      Core parser reproduces depgraph's test_controlm.py outputs (25 processes / 13 INVOKES on the
      13-job sample). Gates green; boundary test unaffected.
    notes: >
      ADR 0002-C §3. Independently valuable — fixes a live spark-submit bug DryDocs still has and
      collapses the dual-parser sync tax depgraph itself flags ("keep in sync with DryDocs"). Fold
      is a CORE change, never a component-side parser fork (0002-A §4). Do early; does not need the
      G2 physical relocate (the shim re-exports the file). BLOCKS the clean G9 re-home.

  - id: G9
    epic: component-topology
    title: "Re-home the depgraph lineage assets onto drydocs-core"
    type: requirement
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p3
    status: done
    depends_on: [G4, G8]
    inputs: [docs/decisions/0002-c-depgraph-lineage-rehome.md, "ce-wilson/depgraph@feat/controlm-lineage"]
    acceptance: >
      depgraph's lineage assets are re-homed into the lineage/ package on drydocs-core (per
      0002-C §4): model v2 (ProcessNode/DataAssetNode/typed rels) reconciled to DryDocs identity
      (assetId / ControlMJob(folder_id,job_id) URNs), controlm_inventory extractor, html-review SME
      surface, the curated drydocs-DB load (depgraph's planned Fork-3 profile), and the RHEL
      run-as-user collector. lineage imports ONLY drydocs_core.* and contains no Control-M parse
      code of its own; writes ground truth ONLY to drydocs; shared-parser + boundary + ground-truth
      tests pass; gates green. depgraph feat/controlm-lineage recorded as superseded source material.
    notes: >
      ADR 0002-C (Option A — absorb). Re-home, not merge: histories disjoint, use reconcile-port.
      Parser is dropped (already in core via G8). Validate the ProcessNode/DataAsset -> DCAT/PROV/SWO
      mapping with neo4j-modeling before authoring load Cypher with neo4j-cypher (MERGE, UNWIND, no
      CYPHER 25). On done: mark depgraph PR #2 superseded for the lineage assets in ADR 0002 rollout.
      RE-TIERED opus -> fable 2026-07-10 on pull (groom-touch policy).
    progress: >
      2026-07-10 — PULLED; branch port/depgraph-lineage-rehome (external-work-in rule); 0002-C
      preconditions ticked; depgraph@feat/controlm-lineage cloned read-only (tip 5b09a0d). SLICE 1
      DONE (29faffe): model.py ported (lineage layer only; identity = ControlMJob NODE-KEY
      composite; rels normalized to the REGISTERED planned vocabulary m3_invokes/m3_triggers/
      m3_reads_from/m3_writes_to — no new edge types invented; code layer left behind) +
      extractors/controlm_inventory.py (shared core parser; scaffold extract.py superseded).
      Core fold delta #4: Invocation.target property + regression test (0002-C §3 rule). Oracle
      ported + GREEN against core (ADR count correction recorded: real figures 4 jobs / 7
      processes / 4 INVOKES); §5 gates: shared-parser, boundary, parser-equivalence, suite all
      ticked; ground-truth-only pends the writer. Suite 532/3. REMAINING for G9 close: review.py
      (html_review port), collect/ port, Fork-3 writer build (rel vocab gate-bound — no live
      load before the HITL gate), 0002-C DONE + depgraph PR #2 superseded record, merge --no-ff.
      2026-07-11 — SLICE 2 + CLOSE: review.py ported (SME surface; node_target wording, registered
      rel spellings; CLI drydocs lineage-review) + collect/ ported (shell verbatim, LF pinned,
      bundle schema unchanged) + Fork-3 writer BUILT (plan_curated pure review + write_curated:
      constraint-on-key MERGE/UNWIND, job endpoints MATCHed, vocab_id stamped; REFUSES live load
      while m3_* ids are planned — GateBoundVocabularyError — and any DB but drydocs —
      TrustBoundaryError). §5 ground-truth-only gate ticked structurally (test_lineage_writer).
      0002-C DONE; depgraph PR #2 (tip 5b09a0d) recorded SUPERSEDED in 0002-C §6 + ADR 0002
      affects. Suite 566/3. Acceptance met — curate() cadence stub is G4 trigger-wiring scope,
      not G9.

  - id: G5
    epic: component-topology
    title: "Document the context→ground-truth promotion path"
    type: chore
    module: docs
    phase: 6
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/restructure/03-hitl-sme-flow.md, docs/decisions/0002-component-database-topology.md]
    acceptance: >
      03-hitl-sme-flow.md gains a "promotion: drydocs_context → drydocs" section stating an
      uncertain node is promoted ONLY via a gate-confirmed WRITE to drydocs (status: proposed →
      loader), never an in-place cross-DB edit; includes the per-item decision presentation + the
      gate-log audit requirement.
    notes: >
      ADR 0002 follow-up #5. Documents the trust-promotion path (trust axis = DB boundary). No
      code. Writable now; references G1's DBs conceptually, not as a hard dep.

  - id: G6
    epic: component-topology
    title: "Cross-link the rejected topology alternatives"
    type: chore
    module: docs
    phase: 6
    agent: reference-librarian
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [docs/decisions/0002-component-database-topology.md]
    acceptance: >
      The three rejected alternatives (Community single-DB, two-mode capability, polyrepo) are
      discoverable from the architecture entry point (linked/cross-referenced from the restructure
      index or README), not only inside the ADR — so they are not re-litigated.
    notes: >
      ADR 0002 follow-up #6. The ADR already records them in Options Considered; this just adds a
      durable pointer. Small hygiene task.

  - id: G10
    epic: component-topology
    title: "Re-review the two governance reference docs held out of the G3 doc port"
    type: task
    module: drydocs-remediation
    phase: 6
    agent: main
    model: fable
    priority: p3
    status: done
    depends_on: [G3]
    inputs: [internal/remediation/README.md]
    acceptance: >
      The two governance reference documents deliberately held out of the repo at the
      2026-07-10 G3 doc port (identified ONLY in internal/remediation/README.md §HELD — do not
      name them here) are re-reviewed with the SME; each is EITHER landed under internal/ with
      an explicit sensitivity classification (test_classification green) OR recorded as
      permanently out-of-repo with the engine's out-of-band read path documented; the README's
      §HELD section is updated with the outcome either way.
    notes: >
      Created 2026-07-10 (user call: keep them out of the repo for now). This item is
      deliberately SANITIZED — the doc identities, content class, and durable location live in
      the publish-excluded internal/remediation/README.md, not here. Timing: gated on G3 because
      the remediation engine's escalation-facing features are what would need them.
      CLOSED 2026-07-12 at the SME gate: outcome recorded in the README §HELD (sanitized here).

  - id: G11
    epic: component-topology
    title: "Lineage inventory extractor reports its skips — coverage accounting, never silent drops"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [G9]
    inputs: [drydocs_lineage/extractors/controlm_inventory.py, tests/unit/test_lineage_inventory.py, tests/fixtures/lineage/jobs.csv]
    acceptance: >
      ControlMInventoryExtractor.extract exposes a coverage summary — rows read, current
      jobs added, and every skip BY REASON (stale version_serial, nameless row, invocation
      with no resolvable target, unparsed cmd_line) — so nothing is dropped silently (the
      STG_PARSE_QUALITY / UNMATCHED house rule: report, never drop). Counts pinned by tests
      on the synthetic fixture (5 rows -> 4 current jobs + 1 stale skip); the
      `drydocs lineage-review` CLI prints the summary alongside the graph stats. Full suite
      green.
    notes: >
      Groomed 2026-07-11 from the 2026-07-10 /tech-debt review of G9 slice 1 (finding #3;
      findings #1/#4 were fixed same day, #2 merged into N2). Non-HITL: pure reporting on
      the candidate side — curation/write semantics untouched. DONE 2026-07-11 (same day):
      ExtractCoverage dataclass (rows/jobs/skips-by-reason/invocations incl. unresolved +
      no-target, plus empty/unparsed command buckets); extract() returns it; CLI prints
      "coverage: ..." after the graph stats; fixture counts pinned (5 rows -> 4 jobs +
      1 stale) + synthetic nameless/empty/unknown case + row-bucket totality check.

  # (G12/G13 groomed 2026-07-16 — the two pre-flip writer build blockers recorded by the
  #  2026-07-15 lineage vocab gate + resolved/refined by the 2026-07-16 cmdline-lineage-review.)
  - id: G12
    epic: component-topology
    title: "Lineage writer ETLProcess endpoint class — TRIGGERS targets stop collapsing to :Script"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_lineage/writer.py, drydocs_lineage/model.py, tests/unit/test_lineage_writer.py, config/gate-log.md, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: >
      drydocs_lineage.writer maps TRIGGERS targets to :ETLProcess endpoints per the two
      2026-07 gate decisions (gate-log 2026-07-15 "Lineage rel vocabulary gate" +
      2026-07-16 "cmdline-lineage-review"): identity = the kind-scoped stable token
      (Ab Initio pset/graph BASENAME; DPL -pipeline GUID) with kind
      (etl|utility|notification) stamped and full path / dataflow / config JSON kept as
      properties; every other non-job process still MERGEs as :Script keyed by full path
      (multi-mount duplicates stay separate for review). The gate-bound refusal is
      UNCHANGED — m3_* stay planned and write_curated keeps raising
      GateBoundVocabularyError until the flips land with the curated-load build. Unit
      tests prove the endpoint mapping on plan_curated output (duck-typed, no live DB);
      full suite green.
    notes: >
      Groomed 2026-07-16 from the 2026-07-15 inbox line (lineage vocab gate residual);
      the business-key half was decided at cmdline-lineage-review and implemented
      extractor-side (_stable_invocation_key) — THIS item is the writer half. Shapes are
      gate-confirmed, so sonnet with a written acceptance; no ontology decision remains.

      DONE 2026-07-19 — executed by a general-purpose subagent (sonnet), diff reviewed
      in-session. Classification is node-kind-based: abinitio|dpl processes MERGE as
      :ETLProcess {token} (the extractor's _stable_invocation_key token; kind/engine/name/
      path/dataflow/config_path as properties, etlprocess_token constraint); every other
      non-job process stays :Script by full path. Consequence: INVOKES targets of those
      kinds also land on :ETLProcess (wrapper-payload expansion) — the m3_invokes to_node
      broadening question and the kind='etl' constant discriminator are inboxed in
      IDEAS.md for the next gate/groom. Gate-bound refusal and vocabulary untouched.
      Suite 730 green (+2 endpoint-mapping tests).

  - id: G13
    epic: component-topology
    title: "Lineage writer file-ops resolution — script-level reads/writes attribute to the owning job"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_lineage/writer.py, tests/unit/test_lineage_writer.py, config/gate-log.md, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: >
      Per the 2026-07-15 gate EDIT (m3_reads_from / m3_writes_to endpoints are
      "ETLProcess | ControlMJob" -> DataAsset): plan_curated resolves script-level
      READS_FROM / WRITES_TO candidates to the OWNING ControlMJob via its INVOKES edge
      before write — a :Script (prov:Entity) never carries prov:used/generated; the ETL
      case keeps its ETLProcess Activity. A unit test proves a script->asset candidate
      plans as job->asset and an ETL-case candidate stays ETLProcess->asset; refusal
      semantics unchanged (m3_* planned until the curated-load build). Full suite green.
    notes: >
      Groomed 2026-07-16 from the 2026-07-15 inbox line (same gate, second residual).
      Pure type-correctness resolution on the candidate side — the edge meanings were
      confirmed at the gate; no new relationship types.
      DONE 2026-07-19 — executed by a general-purpose subagent (sonnet), diff reviewed
      in-session. plan_curated resolves script-src READS_FROM/WRITES_TO to the owning
      ControlMJob(s) via graph INVOKES topology (not the confirmed batch); multi-job
      owners fan out (each job-run is a distinct Activity); no-owner candidates drop
      into WritePlan.unresolved_file_ops (never silent); non-DataAsset dst raises.
      rels keeps the confirmed audit count; rel_types/statements reflect the resolved
      set. Refusal + vocabulary untouched. Feed gap (no extractor emits file-ops yet)
      and the unresolved-counter surfacing are inboxed in IDEAS.md. Suite 735 green
      (+5 file-ops tests).

  # ── Epic H — drydocs-review back-flow (main, opus) ────────────────────────
  # REVERSE of the normal port: reproduce the company-authored generic SME-review/
  # HITL toolkit GENERICALLY in this public producer. Re-implement from screenshots/
  # descriptions — never copy company code. See docs/restructure/05-drydocs-review-backflow.md.
  - id: H1
    epic: drydocs-review-backflow
    title: "Reproduce graph_verify + review_labels (offline spine)"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/restructure/05-drydocs-review-backflow.md, config/classification.yaml]
    acceptance: >
      graph_verify + review_labels reproduced generically (offline spine): review_labels is a typed
      accessor over a sanitized review-labels.yaml (vendor-bmc seed, no internal sources); graph_verify
      loads YAML TC-* suites + evaluate is pure/offline with unit tests; both carry a classification entry;
      Track-1 stays green. No real SIDs / sources / Confluence coords.
    notes: >
      Done 2026-07-01. drydocs/review_labels.py + drydocs/graph_verify.py (pure load/evaluate; run_* takes a
      duck-typed GraphRunner so no Neo4j import), config/review-labels.yaml + graph-tests/vendor-bmc-smoke.yaml
      (both classification: Internal-Public), tests/unit/test_review_labels.py + test_graph_verify.py (27 tests).
      NOT wired into cli.py (entrypoint-boundary decision — see H-TODO). Full suite 214 passed.

  - id: H2
    epic: drydocs-review-backflow
    title: "Reproduce the graph_review HTML renderer"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [H1]
    inputs: [docs/restructure/05-drydocs-review-backflow.md]
    acceptance: >
      graph_review reproduced: pure HTML renderer (no Neo4j in module — CLI hands it rows), one section
      per DATA label, hidden_props stripped; offline unit test renders a fixture row set. classification set.
    notes: >
      Done 2026-07-01. drydocs/graph_review.py: render_review({label: [props]}) → self-contained HTML,
      DEFAULT_HIDDEN_PROPS + `_`-prefixed keys stripped, provenance from the review spine on each header;
      group_rows() helper. 6 unit tests. Added to review boundary group.

  - id: H3
    epic: drydocs-review-backflow
    title: "Reproduce the SME-notes harvester"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: opus
    priority: p3
    status: done
    depends_on: [H1]
    inputs: [docs/restructure/05-drydocs-review-backflow.md]
    acceptance: >
      sme_notes harvester reproduced: scans repo for owner-attributed notes, routes typed sub-tags to
      $FR/$UC/$OQ/$NOTES; SID format is generic/synthetic, no real SIDs committed. Unit test on a fixture tree.
    notes: >
      Done 2026-07-01. drydocs/sme_notes.py: SME[sid] $FR/$UC/$OQ/$NOTES grammar; harvest_text/file/tree
      (read-only, excludes data/ + non-source exts) + route(). 5 unit tests (synthetic SIDs). Boundary group.

  - id: H4
    epic: drydocs-review-backflow
    title: "Reproduce the HITL gate-page generator"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: opus
    priority: p3
    status: done
    depends_on: [H1, H2]
    inputs: [docs/restructure/04-sme-checklist-and-load-plan.md, config/taxonomy-ontology-map.yaml, config/classification.yaml]
    acceptance: >
      HITL prompt-page GENERATOR reproduced: a renderer over 04-sme-checklist-and-load-plan.md +
      taxonomy-ontology-map.yaml + classification.yaml that emits interactive checklist pages (localStorage
      persistence, "no graph write until confirmed", parent-index + per-step-child structure). Example page
      seeded from vendor-bmc, never real data. Producer already owns all three inputs + gate-log.md record.
    notes: >
      Done 2026-07-01. drydocs/gate_pages.py: GateSpec + render_gate_page → self-contained interactive HTML
      (checkbox per confirmation, localStorage persistence, progress bar, classification badge, mapping table,
      "no graph write until confirmed" banner). Example config/gate-prompts/vendor-bmc-example.yaml
      (Internal-Public). 6 unit tests. GATED/DEFERRED: rendering pages from REAL PAT/SEAL steps (Internal-
      Confidential) → gitignored twin, through the HITL gate; not built.

  - id: H5
    epic: drydocs-review-backflow
    title: "Reproduce the publishing pipeline with a pluggable publisher"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: opus
    priority: p3
    status: done
    depends_on: [H4]
    inputs: [docs/restructure/05-drydocs-review-backflow.md]
    acceptance: >
      drydocs/publishing/ pipeline reproduced (assembler/validator/preview) with the Confluence push
      ABSTRACTED behind a generic pluggable publisher interface — no toby_publish_confluence, no space coords.
      Local preview + XML/macro-allow-list validation covered by a unit test.
    notes: >
      Done 2026-07-01. drydocs/publishing/ package: assemble() (template wrap), validator (well-formed XML +
      macro allow-list), write_preview(), Publisher Protocol + NoopPublisher/LocalPublisher (Confluence push
      abstracted — no toby_publish_confluence / space coords). 10 unit tests. GATED/DEFERRED: a real
      ConfluencePublisher (space coords, auth, internal wrapper) → company gitignored twin; not built.

  - id: H6
    epic: drydocs-review-backflow
    title: "Close the module-boundary blind spot (default-deny)"
    type: task
    module: drydocs-review
    phase: 7
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [H1]
    inputs: [tests/unit/test_module_boundary.py, MODULE_MAP.md]
    acceptance: >
      Boundary guard closes the blind spot: (a) a `review` COMPONENT_GROUP added to
      test_module_boundary.py + MODULE_MAP.md rows for the landed drydocs-review modules; (b) a new
      default-deny test asserts every scanned .py resolves to exactly one bucket (core or a component
      group), failing with the list of stragglers, exempting package __init__.py. Passes on producer today
      (only drydocs/__init__.py unclassified). Track-1 stays green.
    notes: >
      Done 2026-07-01. review group = (graph_verify, review_labels); test_every_module_is_classified added
      (default-deny, flags UNCLASSIFIED + AMBIGUOUS). MODULE_MAP.md rows + note added. Boundary guard 3
      passed; Track-1 92 passed / 0 failed. graph_review (H2) / publishing (H5) get added to the review
      group when they land — the default-deny test now FORCES that.

  - id: H7
    epic: drydocs-review-backflow
    title: "Document the Canonical-COMPANY back-flow rule"
    type: chore
    module: docs
    phase: 7
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [git-readme.md, docs/port-prompt.md, .claude/skills/reconcile-port/SKILL.md]
    acceptance: >
      The back-flow reconciliation direction (Canonical-COMPANY for all drydocs-review paths — keep
      company's originals, do not overwrite with the sanitized public template) is documented in the port
      guide, the port prompt, and the reconcile-port divergence ledger.
    notes: "Done 2026-07-01 alongside this plan. The cross-boundary rule that prevents the next port from clobbering company's wired originals."

  # ── Epic I — Project board & planning infrastructure (main) — Phase 8 ─────
  # Groomed 2026-07-01 from the architecture review: the backlog is the database,
  # the board is a render of it, capture/transcription is an agent job. Model
  # discipline (daily-usage limits): opus/fable only where a decision changes the
  # schema or a boundary; sonnet for anything with an acceptance test already
  # written; haiku for lookups, renames, ritual wiring.
  - id: I1
    epic: project-board
    title: "Backlog schema v2: plan, modules, titles, validation"
    type: task
    module: docs
    phase: 8
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: []
    inputs: [docs/restructure/backlog.yaml, MODULE_MAP.md, tests/unit/test_classification.py]
    acceptance: >
      backlog.yaml declares drydocs.backlog.v2 with a plan: (phases incl. goal/status) and a
      modules: registry; every item carries title/type/module/phase; tests/unit/test_backlog.py
      validates the schema (unique ids, deps resolve + acyclic, module/phase/enums valid,
      summary + next_ready computed-consistent) and passes; full suite stays green.
    notes: >
      Done 2026-07-01 (this change). Fields chosen so a future to-do lands as
      type: requirement + module + phase — requirement-to-module traceability without
      leaving git. summary/next_ready are now test-enforced, no longer hand-trusted.

  - id: I2
    epic: project-board
    title: "Render the backlog as an HTML project board"
    type: requirement
    module: drydocs-plan
    phase: 8
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: [I1]
    inputs: [docs/restructure/backlog.yaml, drydocs/gate_pages.py, MODULE_MAP.md]
    acceptance: >
      drydocs/plan_board.py renders backlog.yaml -> docs/plan/board.html, self-contained
      (roadmap strip from plan: with computed per-phase progress, kanban columns by status,
      client-side filters by module/phase/epic/type, depends_on rendered as links, quick-capture
      box that formats a paste-ready IDEAS.md line to clipboard); module classified in
      MODULE_MAP.md + test_module_boundary.py (new `plan` component group — default-deny will
      force it); offline unit tests on a fixture backlog; the rendered board is committed.
      Entry point = thin script beside snapshot.ps1 until the cli.py entrypoint-boundary TODO
      (MODULE_MAP.md) is resolved.
    notes: >
      Done 2026-07-01. Built by a sonnet agent per the model matrix; opus review pass done.
      drydocs/plan_board.py (pure, deterministic — no build timestamps, backlog `updated:`
      shown instead) + scripts/render_board.py + tests/unit/test_plan_board.py (16 tests) +
      `plan` COMPONENT_GROUP in test_module_boundary.py + MODULE_MAP.md row. Board committed
      at docs/plan/board.html. Board replaces 02-backlog.md as the human view.

  - id: I3
    epic: project-board
    title: "Groom-backlog skill: raw notes -> backlog items"
    type: requirement
    module: docs
    phase: 8
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [I1]
    inputs: [docs/restructure/IDEAS.md, docs/restructure/backlog.yaml, docs/restructure/02-backlog.md]
    acceptance: >
      .claude/skills/groom-backlog/SKILL.md exists: takes raw notes (pasted text, IDEAS.md inbox,
      or a transcribed photo of paper notes), emits either IDEAS.md inbox lines or full v2 items
      (title/type/module/phase/acceptance), asks the user only when module/phase is genuinely
      ambiguous, moves groomed lines to the IDEAS.md audit trail, updates summary/next_ready,
      and regenerates the board once I2 has landed. test_backlog.py green after a groom run.
    notes: >
      Done 2026-07-01 (authored on opus per the model matrix; routine runs on sonnet).
      .claude/skills/groom-backlog/SKILL.md (promote/inbox/merge procedure, field guide,
      ambiguity + HITL rules, groom-run mechanics) + validate.py (standalone schema gate for
      envs without pytest — test_backlog.py wins on disagreement). Demonstration groom run:
      IDEAS inbox fragment-cleanup line -> J1 (Epic J, release-infrastructure); audit trail
      updated; validator green; board regenerated.

  - id: I4
    epic: project-board
    title: "Regenerate the board in the session-end ritual"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: [I2]
    inputs: [CLAUDE.md, knowledge/depgraph-snapshots/snapshot.ps1]
    acceptance: >
      The CLAUDE.md session ritual regenerates docs/plan/board.html at session end alongside the
      depgraph snapshot (render script wired next to snapshot.ps1), so the committed board can
      never go stale for more than one session.
    notes: >
      Done 2026-07-01. snapshot.ps1 now refreshes the board first (best-effort try/catch, before
      the depgraph dependency, so it works even where depgraph is absent); CLAUDE.md §0 backlog
      pointer updated (board = human view, groom-backlog skill named) and ritual step 3 gains the
      regen + the deterministic stale-board check (re-render, git diff --quiet board.html).
      Epic I complete — the planning loop is closed.

  # ── Epic J — Release infrastructure (main) — Phase 8 ──────────────────────
  # Groomed 2026-07-01 from the IDEAS inbox (architecture-review follow-ups) via the
  # groom-backlog skill (I3's demonstration run). Sibling inbox lines (versioning
  # reset, CI, cli regroup) stay parked pending user decisions (semver start, rename
  # window) — promote them here when decided.
  - id: J1
    epic: release-infrastructure
    title: "Sweep the iteration fragments (naming drift, superseded banners)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p2
    status: done
    depends_on: []
    inputs: [CLAUDE.md, docs/restructure/01-project-plan.md, knowledge/depgraph-snapshots/snapshot.ps1, README.md, docs/reviews/, SDLC-Docs/]
    acceptance: >
      DryDocs1 -> DryDocs in CLAUDE.md:1 and 01-project-plan.md (title, repo URL; merged-branch
      line removed); drydocs1 -> drydocs in knowledge/depgraph-snapshots/snapshot.ps1 + its README
      (existing drydocs1-*.json snapshot files left untouched as history); the stray
      "@claude - <Aura console URL>" line removed from README.md:1; a "Superseded by the Phase 0-5
      restructure plan" banner on docs/reviews/drydocs-consolidated-plan.md and the persona/SDLC
      review docs; a README in SDLC-Docs/ marking it a frozen reference corpus that fed the ADRs.
      Tests stay green.
    notes: >
      Done 2026-07-01 (haiku agent per the model matrix; opus review pass). 19 files: renames
      (CLAUDE.md, 01-project-plan.md incl. URL + merged-branch line, snapshot.ps1 + README +
      viewer.html, reference/REGISTRY.yaml — a residual the sweep caught beyond the checklist),
      README.md stray console-URL line removed, 11 docs/reviews/ superseded banners,
      SDLC-Docs/README.md created (frozen corpus). Residuals justified: historical drydocs1-*.json
      snapshots, the drydocs1-live-neo4j memory name, docs/history/. m1/m3-verify renames stay
      parked for the v1.0 window.

  - id: J2
    epic: release-infrastructure
    title: "README reconcile: :DEPENDS_ON → :WAS_INFORMED_BY + feature currency (lineage-review, SQL run-log)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [README.md, drydocs_core/ontology/relationship_vocabulary.yaml, drydocs/loaders/cypher/controlm_dependencies_derived.cypher, docs/oracle-sql-logging.md]
    acceptance: >
      README.md describes the derived job→job dependency edge as :WAS_INFORMED_BY — matching
      the loader (controlm_dependencies_derived.cypher MERGEs :WAS_INFORMED_BY) and the vocab
      (m3_was_informed_by, active, "Replaces DEPENDS_ON") — not :DEPENDS_ON; the four stale
      references (README.md:16,139,152,231) are reconciled. No loader/vocab change (README is
      the only stale side). PLUS feature currency (merged from the 2026-07-11 documentation
      tech-debt review, D3): the CLI reference gains `lineage-review`; the --use-oracle
      paragraph notes the per-run SQL log (link docs/oracle-sql-logging.md); Tests highlights
      mention the lineage suites; the ARCHITECTURE.md "Further reading" line says historical
      rationale, not current organization. Tests stay green.
    notes: >
      Groomed 2026-07-09 from the IDEAS line (2026-07-01 Control-M naming review). Verified
      2026-07-09: loader controlm_dependencies_derived.cypher MERGEs :WAS_INFORMED_BY and vocab
      m3_was_informed_by is active + "Replaces DEPENDS_ON" — README is the stale side. Naming-
      drift doc hygiene, same class as J1. MERGED 2026-07-11: README feature-currency gap
      (docs/reviews/tech-debt-documentation.md D3) — one README pass covers both. DONE
      2026-07-11: all four refs reconciled (the one remaining mention is the deliberate
      "replaces the older DEPENDS_ON name" note); feature currency added (Lineage CLI group,
      SQL run-log paragraph + guide link, lineage/sql-log test highlights, ARCHITECTURE.md
      billed historical). Adjacent same-class fix: controlm_dependencies_derived.py module
      docstring said :DEPENDS_ON — reconciled (comment-only).

  - id: J3
    epic: release-infrastructure
    title: "Adopt SemVer + cut the first release tag (v0.3.0): VERSIONING.md, CHANGELOG.md, pyproject bump"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p2
    status: done
    depends_on: []
    inputs: [pyproject.toml, docs/plan/board.html, docs/restructure/backlog.yaml]
    acceptance: >
      VERSIONING.md documents the SemVer policy (single source = pyproject.toml version,
      annotated vX.Y.Z tags mirror it, 0.x pre-1.0 bump rules, the public surface, the release
      ritual); pyproject.toml version bumped 0.1.0 → 0.3.0; CHANGELOG.md (Keep a Changelog)
      back-filled from the completed epics under [0.3.0]; an annotated git tag v0.3.0 cut at the
      release commit and pushed. Aligns with plan phase 8's release: v0.3.0.
    notes: >
      Done 2026-07-09 (user decision: v0.3.0 over v0.2.0 — matches phase 8's release: field;
      0.1.0 was the poetry-init stub, 0.2.0 skipped). Promoted from the parked IDEAS
      versioning-reset line. Sibling parked lines (cli.py regroup) remain in the inbox
      pending their own decisions; CI + unused-dep removal promoted 2026-07-09 (J5, J6).

  - id: J4
    epic: release-infrastructure
    title: "Refresh the run-drydocs skill: stale test counts, PyYAML gotcha, dropped Aura, m3-supplement rename"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [.claude/skills/run-drydocs/SKILL.md, pyproject.toml]
    acceptance: >
      .claude/skills/run-drydocs/SKILL.md is reconciled to current reality: the two
      "PyYAML not installed / 4 skipped" Gotchas are removed (PyYAML is a runtime dep since
      D2 — pyproject.toml [tool.poetry.dependencies]); the "159 pass, 4 skipped" counts
      (lines 30, 99) are refreshed to the current suite; the dropped Neo4j Aura references
      (Aura ruled out 2026-07-06) are repointed to the Docker EE / local-Neo4j path; and the
      stale apply-m3-supplement command (line 65) is reconciled to apply-ontology-supplement.
      No functional change to the skill; smoke.ps1 still runs.
    notes: >
      Groomed 2026-07-09 from the IDEAS line. Verified 2026-07-09: SKILL.md still asserts
      PyYAML uninstalled (x2), "159 pass", Aura, and apply-m3-supplement — all stale. Same
      doc-drift hygiene class as J1/J2. DONE 2026-07-11: all four staleness classes fixed
      (counts now count-free "suite green + expected sample skips"; PyYAML gotchas replaced
      by the real skip reason; Aura → local Docker EE with the port-remap gotcha; supplement
      chain reconciled to the real commands incl. apply-registry-supplement). BONUS finding
      fixed in-scope: smoke.ps1 + the direct-invocation snippet still imported pre-relocate
      drydocs.models/adapters/controlm — broken since Phase B; repointed to drydocs_core,
      smoke PASSES again (566/3).

  - id: J5
    epic: release-infrastructure
    title: "CI: GitHub Actions running the CLAUDE.md gates + the classification publish-boundary guard"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [pyproject.toml, tests/unit/, CLAUDE.md, tests/unit/test_classification.py]
    acceptance: >
      A .github/workflows/ci.yml runs on push + pull_request: checkout, set up Python
      (3.11 + 3.12) + Poetry (cached), poetry install, then the CLAUDE.md acceptance gates —
      poetry run pytest -q, python -c "import drydocs.cli", drydocs --help, and ruff check —
      plus the publish-boundary guard (test_classification.py) and test_schema.py; the
      network-dependent Neo4j steps are excluded (unit-test scope only); the workflow is green
      on main with no test/code changes required to pass.
    notes: >
      Groomed 2026-07-09 from the parked IDEAS CI line (2026-07-01 architecture review; user
      confirmed promote 2026-07-09). Turns the CLAUDE.md §6 gates into an enforced push guard;
      the classification test is the automated half of the publish boundary (PUBLISH-BOUNDARY.md). DONE
      2026-07-11: .github/workflows/ci.yml — push+PR, Python 3.11+3.12 matrix, Poetry cached,
      blocking gates (pytest -q, import drydocs.cli, drydocs --help, classification + schema
      subsets surfaced as named steps). ONE deliberate deviation from the groomed acceptance:
      ruff check runs ADVISORY (continue-on-error) — 757 pre-existing findings make a blocking
      lint gate impossible without a mass fix that would churn the whole port surface; the
      cleanup is inboxed (IDEAS 2026-07-11) and the step flips to blocking when it lands.
      Local verification: yaml parses, every blocking command green; the live green-on-main
      run triggers on this push (verify in Actions).

  - id: J6
    epic: release-infrastructure
    title: "Drop the unused streamlit dependencies (streamlit + streamlit-agraph)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [pyproject.toml, poetry.lock]
    acceptance: >
      streamlit and streamlit-agraph are removed from pyproject.toml
      [tool.poetry.dependencies] and poetry.lock is regenerated (poetry remove / poetry lock);
      a repo grep confirms neither is imported anywhere; pandas and pypdf are RETAINED (pandas
      kept deliberately for analytical work per drydocs_core/adapters/csv_adapter.py; pypdf is used
      by scripts/ingest_jpmc_reports.py); poetry run pytest -q stays green and drydocs --help works.
    notes: >
      Groomed 2026-07-09 from the parked unused-deps IDEAS line, SCOPED after verification: the
      original note also named pandas (intentional — csv_adapter.py:6-8) and pypdf (now imported
      by scripts/ingest_jpmc_reports.py:27), so only the two streamlit packages are actually dead
      (imported nowhere). Removes the heaviest unused install weight. DONE 2026-07-11:
      poetry remove streamlit streamlit-agraph (lock regenerated, transitive deps pruned);
      import scan clean; pandas + pypdf retained; suite 566/3 + drydocs --help green.

  # ── Epic K — SEAL application attribution (seal_app_ref) — Phase 9 ────────
  # Groomed 2026-07-01 from the company reconciliation answers (SME-run prompt).
  # KEY CORRECTION: the edge is SPEC-LEVEL on BOTH sides — company FR-NS-013/UC-NS-005
  # docs read ACTIVE but no loader/vocab/gate exists there either. job.APPLICATION is
  # NOT reliable SEAL identity; the feed is STG_APP_FACT semantic facts (C3/C4 variable
  # normalization: fact types SEAL/FID/APP_NAME/ALIAS). Company gate sequence adopted:
  # vocabulary registration -> match-policy gate -> loader activation -> verify thresholds.
  - id: K1
    epic: seal-attribution
    title: "Register the seal_app_ref attribution edge (planned) + mapping proposal"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, docs/RELATIONSHIP_GUIDE.md]
    acceptance: >
      m3_seal_app_ref registered in relationship_vocabulary.yaml (WAS_ASSOCIATED_WITH,
      role seal_app_ref, ControlMJob -> Application, prov:wasAssociatedWith, status
      planned) using the company-confirmed generic shape; a taxonomy-ontology-map entry
      drafted status: proposed citing the 2026-07-01 company reconciliation; test_schema.py
      drift guard green (planned entries carry no supplement/loader claims).
    notes: >
      Done 2026-07-01 (ontology-mapper agent, sonnet; opus review). m3_seal_app_ref
      registered planned (no supplement/loader claims); map entry job-seal-app-ref
      status proposed with the three gate open-questions (match policy / zero-unmatched
      surfacing / multi-hit triage); map summary proposed 2->3, counts verified vs
      computed. RELATIONSHIP_GUIDE needed no change (generic matrix only). FLAGGED for
      K2: config/source-registry.yaml has NO STG_APP_FACT / variable-normalization-stream
      source entry yet — register it (pipeline-config) before the loader.

  - id: K2
    epic: seal-attribution
    title: "SEAL attribution loader: STG_APP_FACT facts -> job WAS_ASSOCIATED_WITH edges"
    type: requirement
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [K1, K3]
    inputs: [drydocs/staging.py, drydocs/loaders/, config/source-registry.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      Gate sequence honored: the match policy (SEAL-fact precedence; one-to-one accepted,
      zero unmatched surfaced, multi-hit -> deterministic conflict triage or precedence
      resolution) is SME-confirmed via the HITL gate and logged BEFORE activation; the K1
      vocabulary entry flips planned -> active with supplement + loader recorded. Loader
      consumes STG_APP_FACT rows (fact_type SEAL), matches job by (folder_id, job_id) node
      key and Application by seal_id, writes the agreed shape (MERGE with role;
      ON CREATE first_seen_at/source/match_method; SET last_seen_at/last_run_id); runs
      AFTER jobs + SEAL reference in the chain with JobRun provenance. Verify gains a
      coverage invariant (edge count + unmatched count, no silent drops). Offline unit
      tests on a fixture batch; full suite green.
    notes: >
      Write shape + UC-NS-005 one-hop/recursive query shapes from the company
      reconciliation (2026-07-01) — document them (parameterized) beside the loader.
      Fidelity notes adopted: never key off job.APPLICATION without a declared
      reconciliation policy; keep source/match_method explicit so mixed pipelines stay
      auditable; attribution loads only after both job and application identities exist.
      2026-07-08 TYPE CONFLICT (see taxonomy-ontology-map job-seal-app-ref
      type_conflict_note): wasAssociatedWith needs :Application to be a prov:Agent, but
      the K3 reshape reclasses it prov:Entity/dprod:DataProduct — run the K3 gate FIRST;
      the match-policy gate spec (seal-attribution-match-policy) may need its edge shape
      revised to the reshaped form before ticking. depends_on gains K3.
      2026-07-10: K3 gate SIGNED OFF — the reshape IS confirmed needed (post-reclass
      wasAssociatedWith no longer type-checks against an Entity target), but the specific edge
      shape (a: prov:used vs b: local BELONGS_TO_APPLICATION) is DEFERRED to a K2 follow-up gate.
      K2 stays in_progress: still needs (1) that deferred edge-shape decision and (2) its own
      match-policy gate before the loader is built.
      2026-07-14: match-policy gate SIGNED OFF 24/24 (config/gate-log.md) — the shape question
      resolved as: current WAS_ASSOCIATED_WITH shape type-checks TODAY, the K4 reclass re-opens
      it at its own follow-up gate; the confirmed policy is shape-agnostic. Loader build unblocked.
    progress: >
      2026-07-14 — BUILT + DONE (branch feat/k2-seal-attribution-loader, same day as the
      gate): drydocs/loaders/seal_attribution.py (pure resolver — tier precedence, SEAL-alone,
      one-to-one accept, deterministic tie-break flagged for audit, TierReconcilers seam:
      APP_NAME from the loaded SEAL reference, FID/ALIAS await company-side tables; PIN-aware)
      + seal_attribution.cypher (gate §D shape exactly, MATCH-only endpoints, manual-pin guard)
      + manual_loads.py/manual_seal_attribution.cypher (§F tier 5: manifest-gated,
      never-mint-a-relationship, manually_created stamps) + controlm_app_facts.sql (ORDER BY =
      the tie-break contract) + CLI load-seal-attribution/load-manual-mappings (§E sequencing
      precondition). m3_seal_app_ref planned -> ACTIVE (supplement + loader recorded; K3/K4
      rider intact); stg-app-fact confirmed: true per its own recorded activation condition,
      as ONE unit with the audit-fields stub + LEDGER_PENDING + registry-pin test. Coverage
      invariant joined graph_verify (graph-tests/seal-attribution-coverage.yaml, 6 TCs;
      JobRun stamps matched+unmatched+pinned=eligible, edges_written/dropped_in_graph).
      37 offline tests on the synthetic fixture; suite 625/6. Acceptance met at build scope:
      the LIVE load (edges in the company graph) is Track-2 — map entry job-seal-app-ref
      stays confirmed (flips applied only when a live run writes it).

  - id: K3
    epic: seal-attribution
    title: "Run the BusinessApplication entity-reshape gate (:Application -> Entity/DataProduct; TOM roles -> qualifiedAttribution)"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: [config/gate-prompts/seal-tom-attribution-reshape.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, knowledge/upgrade-plans/docmeta-component.md]
    acceptance: >
      The seal-tom-attribution-reshape gate is SME-run and transcribed: gate-log entry;
      the :Application node class flips to prov:Entity/dprod:DataProduct (or the SME's
      edit); TOM governance roles bind via prov:qualifiedAttribution + prov:hadRole
      (Role = shared skos vocab, org:Membership stays PAT-only);
      seal_has_membership/seal_of_role/seal_held_by deprecation recorded;
      hadPrimarySource/wasAttributedTo/qualifiedAttribution vocabulary entries flip per
      the SME calls; the job-seal-app-ref map entry is re-shaped consistently (unblocks
      K2). Drift guards green.
    notes: >
      Groomed 2026-07-08 from the IDEAS reshape question. Agent-side prep already landed
      (0986d6d): gate spec + planned vocabulary registrations + map type_conflict_note.
      The remaining action is the SME gate session itself + transcription — the most
      consequential pending gate (three incompatible PROV types on one node today).
      2026-07-10: STARTED — in_progress = awaiting HITL (the E1/F1/F2/K2/P2 pattern); the
      agent-side prep is complete, only the SME gate session remains. No graph/vocab write
      until the gate is signed off and transcribed to config/gate-log.md.
      2026-07-10 (user request): RENAMED "SEAL entity-reshape" -> "BusinessApplication
      entity-reshape gate" to normalize to the ontology naming convention for
      internally-developed applications — the taxonomy concept is BusinessApplication
      (config/taxonomy/business-application.yaml, key SEALID) while the graph node label is
      still :Application (relationship_vocabulary.yaml node_classifications:174; the vocab
      already flags the mismatch at :1292 "DevTeam develops a BusinessApplication (graph
      label :Application)"). NEW gate open-question this rename raises: alongside the class
      reclass (prov:SoftwareAgent -> prov:Entity/dprod:DataProduct, gate section A), the SME
      should decide whether to normalize the node LABEL :Application -> :BusinessApplication.
      A node-label change is an ontology/node-meaning decision -> gate-bound, NOT auto-applied;
      the label stays :Application until the gate confirms. The gate spec
      (seal-tom-attribution-reshape.yaml) does not yet carry this label question — add it to
      section A before the SME session.
      2026-07-10: GATE SIGNED OFF (SME chad.wilson) — full transcription in config/gate-log.md
      (2026-07-10 entry). Outcome: A confirmed (Entity/DataProduct); B confirmed with a REVISED
      two-family role model (7 SEAL Technical Operational roles on :Application via qualified
      attribution + a TOMRole scheme; CTO shared; Product Cabinet split out -> K5); C confirmed
      (deprecate the 3 seal_* edges, keep seal_has_port); D confirmed (hadPrimarySource + docmeta
      now a required dependency); E decided FLIP arch_develops -> WAS_ATTRIBUTED_TO {role:
      developed_by}; F/K2 edge shape DEFERRED; label normalization :Application -> :BusinessApplication
      confirmed-direction. K3 = run+transcribe the gate = DONE. The mechanical application of every
      flip is K4; the Product Cabinet is K5.

  - id: K4
    epic: seal-attribution
    title: "Apply the BusinessApplication entity-reshape gate outcome (reclass + label + TOMRole attribution + deprecations + provenance + precedence)"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: done          # done 2026-07-15 pm — 8 commits on feat/k4-businessapplication-reshape (K4(1)..K4(7) + port narrative), branch pending --no-ff merge
    depends_on: [K3]
    inputs: [config/gate-log.md, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, drydocs_core/schema/, config/precedence.yaml]
    acceptance: >
      Per the 2026-07-10 gate sign-off (config/gate-log.md), each applied as its OWN logged change
      with drift guards green: (1) node_classifications#Application class flips prov:SoftwareAgent ->
      prov:Entity/dprod:DataProduct AND the label normalizes :Application -> :BusinessApplication
      across the vocabulary, loaders, supplements and tests; (2) a TOMRole skos ConceptScheme (the 7
      SEAL Technical Operational roles; CTO shared with K5's ProductRole scheme) + the
      qualifiedAttribution/hadRole supplement land (seal_qualified_attribution /
      seal_attribution_has_agent / seal_attribution_had_role planned -> active with supplement blocks);
      (3) seal_has_membership / seal_of_role / seal_held_by deprecated (RELATIONSHIP_GUIDE steps;
      seal_has_port KEPT); (4) seal_had_primary_source activated as an edge (Document nodes wait on the
      drydocs-docmeta component); (5) arch_develops flips to
      (:Application)-[:WAS_ATTRIBUTED_TO {role: developed_by}]->(:DevTeam); (6) map entries
      application-as-dataproduct / seal-tom-attribution / seal-doc-source-of-record flip proposed ->
      confirmed (carrying vocab_id + capture per F5/C7) -> applied with loaders; (7) the SEAL/PAT
      precedence proposed_additions block wired into the live order/active chain. Full suite green.
    notes: >
      Created 2026-07-10 from the K3 gate sign-off. The gate confirmed the DIRECTION; this is the
      mechanical APPLICATION, each flip its own logged change (gate section G discipline). Does NOT
      include the K2 job->app edge (deferred) or the Product Cabinet (K5). CTO is shared between the
      TOMRole (here) and ProductRole (K5) schemes — coordinate the two. PORT NARRATIVE (added
      2026-07-10 review): when the :Application -> :BusinessApplication rename applies, ALSO update
      git-readme.md's SEAL-reshape section (currently instructs the consumer "do NOT take as
      applied" — becomes wrong), add the port-prompt step, and check PORT-MANIFEST per-entry notes
      — the prose-mirror drift class from docs/reviews/tech-debt-port-boundary.md.
      BRANCH (2026-07-15 pm groom): feat/k4-businessapplication-reshape is reserved for
      this item. The remote stub pointed at 40fe038 (73 commits behind, predating K2's
      attribution edges this item re-shapes) with zero commits of its own — re-based onto
      main a683384 at the groom; work starts from there.
      DONE 2026-07-15 pm (all 7 clauses + port narrative, each its own logged commit;
      655 tests green throughout). DEVIATIONS/RESIDUALS: (a) seal-doc-source-of-record
      flipped only to CONFIRMED, not applied — applied = a loader wrote the graph, and the
      Document loader waits on docmeta (honest-lifecycle rule); (b) contact roles outside
      the fixed 7 (CBT, Backup App Owner, Risk Manager) load flagged unmapped_role=true —
      whether any subsume into the scheme is a future SME call; (c) company-side live-graph
      node-label migration is THEIRS (port-prompt step 35); (d) precedence placement:
      seal-pat wired at authority 3 above lob-product-team (identity beats catalog) — the
      open placement question resolved by the governs rationale, flag if SME disagrees.

  - id: K5
    epic: seal-attribution
    title: "Product Cabinet attribution model (product-scoped ProductRole scheme + cross-role reporting edges + DevTeam matrix)"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-20 — gate product-cabinet-attribution §F SIGNED OFF (in-session chat gate; gate-log 2026-07-20)
    depends_on: []
    inputs: [config/gate-log.md, config/taxonomy/lob-product-team.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml]
    acceptance: >
      A ProductRole skos ConceptScheme (Area Product Owner, Product Owner, Product Architect, Tech
      Partner, Data Owner, Data Certifier, Analytics Lead; CTO shared with the TOMRole scheme) is
      proposed and SME-confirmed via its OWN HITL gate, scoped to the PAT hierarchy nodes
      (:Product / :AreaProduct, NOT :Application), with Area Product Owner explicitly NOT tying to a
      business application; the cross-family reporting edges (Tech Partner manages Application Owner;
      TOM Information Owner reports to Data Owner; Data Certifier relates to the app Information Owner)
      are modeled as org:reportsTo-style edges or explicitly recorded as deferred; the
      DevTeam -> BusinessApplication matrix (M:N) is confirmed. No graph write before the gate;
      drift guards green.
    notes: >
      Split out of K3 at the 2026-07-10 gate (SME "ok to split"). Product Cabinet is PAT/Product-layer,
      distinct from the SEAL app reshape (K3/K4). CTO is the shared concept between the TOMRole and
      ProductRole schemes. Related to Epic C (catalog hierarchy) — the :Product/:AreaProduct nodes come
      from config/taxonomy/lob-product-team.yaml.
      DONE 2026-07-20 (in-session chat gate, run off the 2026-07-19 gate-prep; page rendered
      via gate_pages.py; 24 confirmed / 0 edited / 0 rejected — gate-log 2026-07-20). SME
      AMENDMENTS vs this item's original wording: (a) CTO is NOT shared — the families are
      INDEPENDENT (shared-cto dropped for now; rename history: area-product Tech Partner
      formerly named "CTO" in SEAL, SEAL's CTO now the product-level role — supersedes the
      2026-07-10 §B record); (b) scheme FIXED; tech_partner joins area_product_owner as
      :AreaProduct-only; (c) BOTH attribution forms confirmed (reified chain + collapsed
      catalog_cabinet_attributed_to — deviation from the K4 qualified-only resolution);
      (d) reporting edges recorded DEFERRED (option b — internal-side once a person-level
      cabinet extract exists); (e) DevTeam<->BusinessApplication M:N confirmed. Map entry
      confirmed; 3 vocab entries + ProductRole classification stay planned -> K6 supplement
      follow-up; no graph write; suite 735.

  - id: K6
    epic: seal-attribution
    title: "Product Cabinet supplement: seed the ProductRole scheme (fixed 7, no cto, tech_partner changeNote) and flip the catalog_cabinet_* entries planned -> active"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-20 (same session as the K5 sign-off; commit 202175e)
    depends_on: []
    inputs: [config/gate-log.md, config/gate-prompts/product-cabinet-attribution.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, drydocs_core/schema/seal_ontology_supplement.cypher, config/taxonomy-ontology-map.yaml]
    acceptance: >
      Per the 2026-07-20 K5 gate sign-off (config/gate-log.md), a supplement block
      (catalog-side, the K4 seal_ontology_supplement idiom) idempotently seeds: the
      product_roles SkosConceptScheme + EXACTLY the 7 ProductRole concepts (no cto —
      scheme FIXED, new roles require a new gate); the tech_partner skos:changeNote
      carrying the rename history verbatim; the :AreaProduct-only scope notes for
      area_product_owner + tech_partner; and the LocalRelationship terms for the two
      reified hops + the collapsed form. The three catalog_cabinet_* vocabulary entries
      flip planned -> active WITH their supplement refs (registration, not data — the K4
      idiom); the ProductRole node_classifications entry drops its PLANNED marker;
      schema_graph re-rendered; drift guards + full suite green. NO attribution loader
      and NO map flip to applied — the loader stays blocked company-side until a
      person-level PAT cabinet extract exists (honest-lifecycle rule).
    notes: >
      Groomed 2026-07-20 directly from the K5 gate sign-off (section F follow-up list;
      the C9 direct-promotion precedent). The AreaProduct data gap (no rows in the
      lob-product-team sample; catalog_has_area_product still planned) does NOT block the
      scheme seed — vocabulary and loader are independent lifecycles (gate §B). The
      cross-family reporting edges stay DEFERRED (gate §D, internal-side later); nothing
      in this item touches them.
      DONE 2026-07-20 (202175e): Product Cabinet section appended to
      catalog_ontology_supplement.cypher (ProductRole LocalClass; #cabinetQualifiedAttribution
      / #cabinetHadRole / #cabinetAttributedTo LocalRelationship terms MAPS_TO their PROV
      properties; product_roles SkosConceptScheme fixed=true + exactly 7 concepts, scope
      property on area_product_owner + tech_partner, tech_partner change_note verbatim;
      HAS_AGENT deliberately not re-declared — reuses the seal #hasAgent term). Three
      catalog_cabinet_* entries active with supplement refs; ProductRole classification
      ACTIVE; schema_graph re-rendered; map entry stays confirmed (loader blocked
      company-side). Drift guards + suite green (735). Live EE re-apply of the catalog
      supplement refreshes the container whenever it next runs (idempotent).
  - id: K7
    epic: seal-attribution
    title: "Run the Application ↔ Control-M attribution close-out gate to sign-off (seal-app-ref-edge-reshape v3): folder grain, app-code defined mapping, edge shape, steward overrides, orchestrator authorship"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p1
    status: done          # DONE 2026-08-03 — gate seal-app-ref-edge-reshape SIGNED OFF 24/24 (in-session chat gate, user as SME); gate-log entry + vocab m3_belongs_to_application planned + map app-code-defined-mapping confirmed; build items K8-K13 opened BY the gate
    depends_on: []
    inputs: [config/gate-prompts/seal-app-ref-edge-reshape.yaml, config/gate-log.md, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, drydocs/loaders/seal_attribution.py, drydocs/loaders/cypher/seal_attribution.cypher, knowledge/standards/technology/folder-naming-convention.md]
    acceptance: >
      The 24 still-unruled confirmations of
      config/gate-prompts/seal-app-ref-edge-reshape.yaml are ruled with the SME and
      transcribed to config/gate-log.md as a SIGN-OFF entry, not a third partial
      ruling: §A grain (the folder, not the job), §B authoring (the app-code DEFINED
      mapping replaces fuzzy matching as primary), §C what a tier-1 row points at,
      §D edge shape + PROV term (the surviving K3 §F deferral), §E steward overrides
      (the "manually overwritten until fixed" half of the user's own framing), §F
      migration + follow-through, the nine unruled §G confirmations, and §H itself.
      §G6-RIDER is ALREADY RULED (2026-08-01, PAT grain keying) and is NOT re-opened;
      neither is the K2 match policy, which the page demotes to a fallback tier
      unchanged. Per the page's own gate-bound clause: proposed vocabulary lands
      status planned ONLY, the taxonomy-ontology-map entry is AMENDED rather than
      duplicated, BUILD items are opened BY the gate rather than ahead of it, and
      gates.json + the enforcement matrix regenerate. NOTHING is written to the graph
      at the gate itself.
    notes: >
      Groomed 2026-08-03 on user request ("I need to close that out"). THE GAP THIS
      CLOSES IS PROCEDURAL: the gate spec has existed since 2026-07-15 and reached v3
      on 2026-07-27, but no backlog item ever owned RUNNING it — C17, S3 and J24 cite
      the prompt file and are all done, so the work had no claim channel and no
      acceptance test. Every comparable gate has an owning item (U10 runs
      code-graph-package-layer, G22 runs rua-load-shapes, O23 runs rollup-shape);
      this one had none.
      The spec's own header records the user direction that created v2 (2026-07-22):
      close out the mapping for any relationships that need to be defined or manually
      overwritten until fixed — the application to Control-M folders. v1 framed only
      the K3 §F job-edge SHAPE question; the grain correction superseded that framing
      and the shape question survives as §D.
      PUBLISH BOUNDARY: §G's evidence basis is a company-side session (screenshots)
      plus source-system report headers carrying real application ids, SIDs, names and
      team/LoB values. STRUCTURE ONLY reaches the gate log — nothing from those
      captures is transcribed anywhere in the publishable tree (PUBLISH-BOUNDARY.md,
      the sealid-in-strings lesson).
      Sibling open item in this epic: G35 (TOM-roles) is a DIFFERENT subject (role
      enumeration + cardinality) and is not a dependency in either direction. J28 owns
      the renderer defect that kept this gate off the open-gates surface.


  # ── Epic L — Documentation infrastructure (main) — Phase 10 ───────────────
  # Groomed 2026-07-08 from the deterministic-documentation design conversation.
  # THREE senses of "deterministic", unified by ONE contract: a canonical per-doc-type
  # OUTLINE (docs/design/templates/*.outline.yaml) whose stable section/requirement
  # ANCHORS are the id namespace the render pipeline emits, the traceability matrix
  # keys on, and HITL markup (digital save-button OR scanned pen notes) re-attaches to.
  # Source of truth = the .md; HTML/PDF are DERIVED (kills today's three-hand-maintained-
  # copies drift on the Control-M TDD). Sequence (updated 2026-07-08 by the user): TDD
  # (L1) -> render/feedback pipeline (L3-L7) -> Runbook (L8, the capstone that proves the
  # system generalizes to a 2nd doc type); BRD is a LATER phase (inbox-parked, WIP upstream).
  # NOT the planned drydocs-docmeta component: that INGESTS docs into the graph; Epic L
  # AUTHORS ours. They meet only at L7 (docs-as-context, through the gate).
  - id: K9
    epic: seal-attribution
    title: "Defined-mapping store: the app-code mapping domain in mapping.db, the K2_SHAPE update, and the manual-loads template rekeyed to app_code"
    type: requirement
    module: drydocs-api
    phase: 9
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: [K7]
    inputs: [drydocs_api/mappings.py, config/overrides/, drydocs/loaders/manual_loads.py, config/manual-loads/, config/gate-log.md]
    acceptance: >
      The steward-defined app-code mapping exists as a first-class store domain, reusing the
      O24 origin-flagged pattern verbatim per gate §E1: committed CSV in config/overrides/ ->
      mapping.db table -> the /mappings console domain. A row carries the app code, its tier
      (seal-born | platform | dual-coded), the target, and an origin flag (defined |
      matched-fallback | override | manual-pin). Tier-3 rows carry the DECLARED end state §B2
      requires, so a stalled migration is visible rather than silently permanent. Override rows
      may be PERMANENT in this domain (§E2) — do NOT implement the corrected-in-source flip
      here; it belongs to the domains where permanence is temporary. K2_SHAPE in
      drydocs_api/mappings.py updates to the ruled edge shape, and the manual-loads template is
      rekeyed from job identity to app_code. Store rows never write the graph (§E3). Offline
      tests on fixtures; full suite green.
    notes: >
      Opened BY the K7 gate (§F2), 2026-08-03 — the gate opens its build items rather than
      being preceded by them. This is the AUTHORING half; K8 is the loading half and depends
      on it. The store IS the source of record for this domain: there is no machine feed to
      defer to, which is exactly why §E2 ruled defined rows graph-loadable.
      Claim RELEASED 2026-08-03 (night): the remote claude.ai session that claimed it
      (3608ae5) was interrupted by an API failure before any work landed — the claim commit
      is that session's last; no K9 work was committed. Back to todo/next_ready untouched.

  - id: K8
    epic: seal-attribution
    title: "Folder-grain attribution loader: BELONGS_TO_APPLICATION onto the Batch Port, the 1:1 graph-test, and the K2 job-edge migration"
    type: requirement
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p1
    status: done
    closed: 2026-08-04
    depends_on: [K7, K9]
    inputs: [drydocs/loaders/folder_attribution.py, drydocs/loaders/cypher/folder_attribution.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, graph-tests/folder-attribution-coverage.yaml, config/gate-log.md]
    acceptance: >
      A loader writes (:ControlMFolder)-[:BELONGS_TO_APPLICATION {role: seal_app_ref, origin,
      match_method, tier, first_seen_at, last_seen_at, source}]->(:Port) at the FOLDER grain,
      derived by fanning ONE app-code row out over m3_contains_folder (§B1). Jobs inherit via
      CONTAINS_JOB; no per-job application edge is authored (§A1). m3_belongs_to_application
      flips planned -> active with supplement + loader recorded. The K2 fuzzy matcher DEMOTES
      to a fallback tier for codes with no defined row, and every fallback value carries
      origin=matched-fallback so it is DISCLOSED rather than presented as defined (§B3).
      A GRAPH-TEST asserts folder->application is 1:1 (never a constraint — Neo4j cannot
      declare relationship cardinality); a folder with two application edges FAILS. Live K2 job
      edges are migrated at the new grain preserving every property. Coverage invariant joins
      graph_verify. Offline tests on fixtures; full suite green.
    notes: >
      Opened BY the K7 gate (§F2), 2026-08-03. Target is the Batch :Port, NOT
      :BusinessApplication — supernode avoidance, §C1(b); the app node already hubs TOM roles,
      contacts, product links and orchestrator edges. :AreaProduct is a ROUTING step in the
      steward UI and NEVER an edge target (there is no direct mapping of batch technical
      components to area product). PRODUCER-SIDE the migration is a reload under
      wipe-and-rebuild (§F1); the company's real migration is theirs under guardrail 6 / T23.
      CLOSED 2026-08-04 (desktop): built as drydocs/loaders/folder_attribution.py +
      folder_attribution.cypher (MATCH-only, ONE edge MERGE, pin backstop, origin/match_method/
      tier ON CREATE) with `drydocs load-folder-attribution` replacing load-seal-attribution in
      the canonical sequence. The K2 module DEMOTED to the pure match-policy resolver (its
      writer + cypher deleted; m3_seal_app_ref active->deprecated under the gate's own
      authority clause, m3_belongs_to_application planned->active with supplement + loader).
      Resolver semantics chosen where the gate left room, each written into a test: authored
      precedence manual-pin > override > defined; an equal-precedence tie between different
      apps is a CONFLICT (1:1, never auto-picked); a DECLARED platform code's unresolved
      folders surface as platform-unresolved rather than falling back (§B3's fallback applies
      only to codes with NO defined row); fallback needs per-folder unanimity of K2 decisions
      and carries the winning tier's match_method. Manual tier-5 chain rekeyed end to end
      (SUPPORTED_SHAPE, parser, store table, manual cypher fan-out) closing K9's deliberate
      fail-closed queue; explorer specs re-bound (§A2); graph-tests suite rewritten as
      folder-attribution-coverage (8 TCs incl. the 1:1 test and a no-job-grain-residue guard).
      BONUS FINDING: the publish-boundary bare-id guard caught a REAL psgmgr folder id in the
      composite-key standard's examples (committed 2026-08-03) — replaced with a synthetic
      value in this commit. Suite 1399, ruff clean. Governed-doc staleness (TDD "K2 live"
      statements) recorded on L19, not hot-edited.

  - id: K10
    epic: seal-attribution
    title: "Port activation cutover: active_state (declared|confirmed) per port replaces the never-read active boolean"
    type: task
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: [K7]
    inputs: [drydocs/loaders/cypher/seal_applications.cypher, drydocs/loaders/cypher/folder_attribution.cypher, graph-tests/folder-attribution-coverage.yaml, config/gate-log.md]
    acceptance: >
      seal_applications.cypher stops seeding Port.active (a boolean created false and never
      written true or read anywhere producer-side) and seeds active_state instead, PER PORT:
      declared | confirmed, with declared_by/declared_at and confirmed_by/confirmed_at/
      confirmed_run_id (§G4). Confirmation is DERIVED from the BELONGS_TO_APPLICATION edge
      landing on the port — no separate trigger, which is what §C1(b) bought (§G5). The
      EventProcessing port is declared-only until an event source is onboarded, and a
      declared-but-unconfirmed port is visibly distinct from a confirmed one on every surface.
      Drift guards green.
    notes: >
      Opened BY the K7 gate (§F2), 2026-08-03. Per PORT, not per orchestrator (SME correction
      at the gate): the port answers "does this app run batch", the 1:N reg_uses_software edges
      answer "scheduled by whom" — putting the orchestrator on the port would duplicate it.
      COMPANY-SIDE this is a live-behaviour change, not a formalization: two loaders
      (controlm_app_codes.cypher and an AutoSys twin) already write the boolean by derivation.
      Their already-active ports grandfather as confirmed with the Control-M app-code link as
      provenance (§G4-RIDER), unambiguous because the initial series loads Control-M only.
      CLOSED 2026-08-04 (desktop): seal_applications.cypher seeds active_state='declared' +
      declared_by/declared_at ON CREATE only (a SEAL reload never resets a confirmation) and
      never writes 'confirmed'; BOTH edge writers (folder_attribution.cypher, the manual
      cypher) stamp the port confirmed when the edge lands — no separate trigger, §G5 —
      with coalesce-stable first-confirmation stamps (confirmed_by = the authoring steward
      via the new FolderAttributionRow.authored_by, else the loader for matched-fallback;
      confirmed_run_id = the confirming run). Suite TCs 09-11 added (state enum + declared
      stamps + no retired boolean; confirmation<->edge two-way agreement; EventProcessing
      declared-only); explorer.folder-applications.v1 surfaces port_state so
      declared-vs-confirmed is visibly distinct. Suite 1403 (+4), ruff clean. The company
      grandfathering (§G4-RIDER) stays theirs — ledger step 65.

  - id: K11
    epic: seal-attribution
    title: "Steward mapping screen: the orchestrator-first cascade, unmapped-only folder filter, and lifecycle chips as confirmation provenance"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: [K9]
    inputs: [web/src/, drydocs_api/mappings.py, config/gate-prompts/seal-app-ref-edge-reshape.yaml]
    acceptance: >
      The /mappings console gains the app-code mapping domain as a cascade: Product Line ->
      Product -> Business Application -> ORCHESTRATOR -> folder filter -> folder -> SME
      check/approval with notes. The orchestrator is picked BEFORE the folder filter and that
      choice AUTHORS the USES_SOFTWARE orchestrator edge (§G1); the SEAL-declared
      batch_orchestrator string only PREFILLS it (§G2). The Business Application picker returns
      a LIST, never a single application (§G6). "Available folders" = unmapped only, with
      naming-pattern as an OPTIONAL filter layered on top — never primary. run_as_user is
      surfaced as a sort option (SME addition at the gate). The O13 mandatory-rationale and
      lifecycle chips (draft -> submitted -> gated -> loaded) ARE the confirmed edge's
      provenance and satisfy K10's confirmation stamp — one mechanism, not two. The screen
      drafts store rows only; the loader writes (§E3/§G7).
    notes: >
      Opened BY the K7 gate (§F2), 2026-08-03. Sections A-F of the gate are Control-M-shaped;
      §G generalizes the ACT so AutoSys drops in as a sibling domain without re-opening the
      grain ruling. Company-side evidence reviewed at the gate shows the split has already been
      built twice (a Control-M app-code loader and an AutoSys twin), so this formalizes an
      established shape rather than inventing one.
      CLOSED 2026-08-04 (desktop): AppCodeCascadePane on /mappings (domain app-code-mapping)
      walks the cascade exactly as §G ruled — Product Line -> Product -> the product's
      application LIST (§G6; producer-side catalog_has_application is still planned so the
      pane says so and degrades to full application search, K13 unchanged) -> orchestrator
      BEFORE the folder filter (§G1), prefilled from the declared 'batch-port' edge with the
      §G2 "prefill never authors" note on the surface, several orchestrators shown as normal
      mid-migration (§G3) -> unmapped-only folders (spec-enforced, §G7) with the naming-pattern
      filter OPTIONAL and layered on top and run_as_user as a sort key (aggregated from job
      owners — folders carry no run-as; jobs do) -> SME approval dialog (tier per §B2,
      mandatory rationale) -> per-user tray with the lifecycle chips, 'loaded' verified against
      the committed grid + live unmapped queue. Drafts travel draftAppCode ->
      POST /mappings/app-code/draft — a route K9's pure handler NEVER HAD (found and wired
      here, with a route-presence test). THE BUILD'S BIGGEST PIECE WAS NOT THE SCREEN: §G1's
      "the choice AUTHORS the USES_SOFTWARE edge" had landed NOWHERE (K8 closed without it), so
      both attribution writers now author (:BusinessApplication)-[:USES_SOFTWARE {source:
      'app-code-mapping', origin: 'confirmed'}]->(sp) when a folder edge lands — one mechanism
      with the K10 port confirmation, stamps ON CREATE only, FOREACH-guarded with the shortfall
      stamped on the JobRun (orchestrator_edges) and warned. The orchestrator ref is resolved
      from platforms.yaml ('controlm', C12-confirmed) via a new BaseLoader extra_cypher_params
      hook — never hardcoded in Cypher. batch_port_orchestrator.cypher now stamps its edges
      origin='declared' (§G2's vocabulary made real). Four new QuerySpecs
      (mappings.catalog-cascade/orchestrators/app-orchestrators/unmapped-folders.v1), TC-12
      (confirmed-edge two-way agreement), reg_uses_software note/loader updated, component
      ledgered (64->65 unbound, like its parent route). FOUND IN PASSING, inboxed not hot-fixed:
      the job-application pane is now fully legacy (its coverage spec still reads the retired
      job-grain edge). Suite 1410 (+7), web build + oxlint clean.

  - id: K12
    epic: seal-attribution
    title: "Capture :ControlMApplication as taxonomy — the app-code classification the ontology map had to waive"
    type: task
    module: taxonomy
    phase: 9
    agent: taxonomy-importer
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: []
    inputs: [config/taxonomy/controlm.yaml, config/taxonomy-ontology-map.yaml, drydocs/loaders/cypher/controlm_folders.cypher]
    acceptance: >
      config/taxonomy/controlm.yaml gains the app-code grouping (:ControlMApplication) as PURE
      CLASSIFICATION — no meaning-bearing edges — alongside the servers/folders/jobs/conditions
      it already captures. The app-code-defined-mapping entry in config/taxonomy-ontology-map.yaml
      then replaces its capture WAIVER with the real path, and the post-cutoff capture guard
      (test_taxonomy_ontology_map.py::test_post_cutoff_mappings_declare_capture) passes on the
      path rather than on the waiver.
    notes: >
      Found at the K7 sign-off 2026-08-03: :ControlMApplication is LOADED but was never captured
      taxonomy-first — controlm.yaml predates the 2026-07-07 folder-pass extension that added
      the app code. Same F5 gap job-seal-app-ref records. The mapping's capture was waived
      explicitly rather than silently, per the guard's own "waived — <reason>" form; this item
      closes the waiver. Note what is owed is a capture of the app-code CLASSIFICATION, not of
      the defined mapping — the mapping has no machine feed and the steward store IS its source
      of record (§E2).
      CLOSED 2026-08-04 (desktop): controlm.yaml gained the applications: section (4 codes,
      pure classification, faithful to the REDUCED sample — the sample folders CSV carries no
      APPLICATION column, so codes are observed via the jobs' application column and jobless
      folders are name-DERIVED and flagged, honoring the naming standard's a-name-is-not-a-key
      warning). The map entry's capture waiver replaced with the real path; the post-cutoff
      capture guard passes on the path.

  - id: K13
    epic: seal-attribution
    title: "catalog_has_application back-flow: adopt the company's structural SUPPORT reading and the loader that already exists there"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p3
    status: done
    closed: 2026-08-04
    depends_on: [K7]
    inputs: [config/taxonomy-ontology-map.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, drydocs/loaders/cypher/products.cypher, config/gate-log.md]
    acceptance: >
      catalog_has_application ((:Product)-[:HAS_APPLICATION]->(:BusinessApplication)) is
      reconciled to the ONE reading the gate signed (§G6): a structural SUPPORT link — a Product
      is supported by 2 or more applications, front-end/back-end — NOT "a Product owns a set of
      SEAL-registered applications". The vocabulary note and the ontology-map entry both state
      the support reading; the edge is documented 1:many BY DESIGN (the source extract's
      application-id column is multi-valued). Whether the producer entry leaves status: planned
      is decided by whether a product-scoped extract is onboarded here — the C9 condition is
      unchanged by this item; only the SEMANTICS are.
    notes: >
      Opened BY the K7 gate (§G6), 2026-08-03 — explicitly a BACK-FLOW/divergence item, not new
      build. Company-side the C9 precondition is already met: a product-scoped extract feeds a
      dedicated loader and the edge is ACTIVE there. The two sides worded the semantics
      differently and the gate ruled the company reading, because it is backed by a live extract
      and loader where the producer's was an unbuilt intention. The picker in K11 means a
      different thing under each reading, which is why one had to be chosen.
      CLOSED 2026-08-04 (desktop): every producer surface now states the support reading.
      (1) The ontology map gained its FIRST catalog_has_application entry —
      product-has-application, status: confirmed (chad.wilson 2026-08-03, the K7 sign-off),
      capture waived with the C9 reason, decision_resolved carrying the §G6 ruling verbatim;
      map summary confirmed 23 -> 24. The map had NO entry for this edge at all — the
      acceptance's "the ontology-map entry" was an entry that had to be created, not edited.
      (2) relationship_vocabulary note rewritten: support reading + 1:many BY DESIGN lead;
      the old WAS_ATTRIBUTED_TO matrix hint is recorded as FALLEN (both ends are Entities
      post-K4, so no PROV row — consistent with the entry's existing prov_maps_to: ~);
      C9 history and the planned status kept verbatim. (3) catalog_ontology_supplement's
      OntologyTerm notes rewritten the same way — which also removed a FALSE claim: "Written
      by pat_product_mapping loader" had been untrue since C9 removed that write. (4) The
      data-context-extractor skill's two "product owns" lines fixed. (5) The K11 surfaces
      (cascade spec description, pane comment + notice) re-aimed from "the K13 back-flow"
      to the real remaining condition — loading waits on the C9 product-scoped extract.
      Status UNCHANGED everywhere: vocabulary stays planned, no loader, no graph write —
      the C9 condition is untouched, exactly as the acceptance requires. Renders regenerated
      (the enforcement-matrix drift guard fired on the vocabulary note first, as designed);
      suite 1410 green. Left as history: git-readme port-step prose and docs/history/*.
      Inboxed: sdlc-neo4j-schema.md still claims pat_product_mapping.cypher MERGEs
      HAS_APPLICATION (stale since C9 — that living doc belongs to the SDLC cron stream).

  - id: K14
    epic: seal-attribution
    title: "Composite-key conformance sweep: every serialized (folder_id, job_id) pair uses the ctlm_id dot form"
    type: task
    module: drydocs-web
    phase: 9
    agent: main
    model: sonnet
    priority: p3
    status: done
    closed: 2026-08-04
    depends_on: []
    inputs: [knowledge/standards/technology/composite-key-serialization.md, web/src/, drydocs_api/, drydocs/loaders/]
    acceptance: >
      Every surface that renders the ControlMJob (folder_id, job_id) node key as ONE string
      conforms to the composite-key-serialization standard (SME-ruled 2026-08-03): the ctlm_id
      dot form folder_id.job_id — no colon/pipe/underscore sibling grammars. Known stray at
      grooming: the web mapping tray's internal React list key (MappingsRoute.tsx,
      `${r.folder_id}:${r.job_id}`). The sweep covers web/src, drydocs_api and the loaders;
      each hit is either converted or recorded in the standard's stray list with why it is
      exempt (adhoc profiling SQL is pre-exempted by the standard). Suite green.
    notes: >
      Groomed 2026-08-03 from the SME's two in-chat rulings during the K9 session: (1) the dot
      composite IS the standard (the psgmgr ctlm_id convention, P2 gate §B precedent), (2) key-cell
      PAIRS join with ':' not ';' (semicolon is the SQL statement terminator). Ruling (2) was
      BUILT the same session — _parse_key flipped while zero manual CSVs were committed, so no
      migration; this item owns only the remaining VALUE-form sweep. The parked internal-side
      ctlm_id ripple (docs/next-internal-session.md item 4 — which other CM_ views carry the
      derived column) stays separate: it needs Oracle access.
      CLOSED 2026-08-04 (desktop): swept web/src, drydocs_api and drydocs/loaders. TWO strays
      converted to the dot form — the known MappingsRoute.tsx colon-joined React key (plus its
      interface comment) and a second the sweep found: the static mapping demo's slash join
      (mapping_demo.html). No other sibling grammars; the conforming surfaces
      (controlm_dependencies_recursive.sql, the derived-deps cypher) verified unchanged. The
      standard's stray list updated to record the sweep. Web build green.

  - id: K15
    epic: seal-attribution
    title: "Retire or re-bind the legacy /mappings job-application pane — its coverage grid and assign flow still read the retired job-grain edge"
    type: bug
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-05
    depends_on: []
    inputs: [web/src/, drydocs_api/query_specs.py, drydocs_api/mappings.py, config/gate-prompts/seal-app-ref-edge-reshape.yaml]
    acceptance: >
      No /mappings surface reads the retired job-grain WAS_ASSOCIATED_WITH {seal_app_ref}
      edge or drafts the retired job-grain changeset. Either the coverage grid
      (mappings.attribution-coverage.v1) is re-bound to the folder-grain
      BELONGS_TO_APPLICATION truth with jobs inheriting their folder's attribution, or the
      pane is retired and app-code-mapping becomes the default /mappings domain — the
      direction is confirmed with the user at build and recorded in the close note. The
      MappingsRoute derivedLifecycle self-verification is re-bound to the same source
      either way. Suite + web build green.
    notes: >
      Groomed 2026-08-04 (Control-M inbox groom, desktop) from the finding K11's close
      inboxed rather than hot-fixed: post-K8 the job-grain edge is retired (TC-08 asserts
      none exist), so the pane's coverage grid reads every row as "unresolved" and its
      assign flow drafts a changeset the server refuses — a live misreporting surface,
      hence type bug. Leaning at grooming: re-bind the coverage grid as the jobs VIEW of
      the K8 folder-grain truth (support still asks "which app owns this job") and retire
      the job-grain assign flow in favor of the K11 cascade, making app-code-mapping the
      default /mappings domain.
      DIRECTION CONFIRMED 2026-08-05 (user, in-session — the acceptance's "confirmed with
      the user at build"): RETIRE, not re-bind. The grooming lean was to keep a jobs view;
      the SME fact that settled it is that A FOLDER AND ITS JOBS CARRY THE SAME APP CODE,
      so a job-grain grid is N× rows carrying ONE folder-level fact — not a finer answer,
      the same answer repeated — and a grid that looks per-job invites being read as
      per-job truth, which is exactly the model K7 §A1 corrected. "Which application owns
      this job" stays answerable as a one-hop traversal (job -> CONTAINS_JOB -> folder ->
      BELONGS_TO_APPLICATION); it does not need a maintained grid.
    progress: >
      CLOSED 2026-08-05 (desktop, same session as the K-series review that found it).
      RETIRED end to end: QuerySpec mappings.attribution-coverage.v1 DELETED (it read the
      job-grain WAS_ASSOCIATED_WITH {seal_app_ref} edge K8 retired); the drydocs_api domain
      row job-application flipped available: false — KEPT in the registry rather than
      deleted, so a bookmarked ?domain=job-application renders a visibly retired tab
      instead of vanishing and reading as a bug; MappingsRoute lost the coverage grid, the
      assign dialog, the job-grain changeset tray + its localStorage key, derivedLifecycle,
      StatusChip/LifecycleChip and AssignDialog (~495 lines), and defaults to
      app-code-mapping; the CoverageRow/DEMO_COVERAGE demo fixture went with it (a demo
      fixture for a retired surface renders convincingly and describes nothing that
      exists). TWO TESTS RE-POINTED, both INVERTED rather than deleted:
      test_mappings_coverage_spec_registered -> test_mappings_coverage_spec_stays_retired
      (asserts the spec is absent AND that no surviving spec reads the job-grain edge —
      the K15 acceptance as a guard, because the failure mode is someone re-adding a
      job-grain grid when the folder grid "looks empty"); the schema-meta exemplar guard
      now ENUMERATES specs riding role='seal_app_ref' instead of naming a hardcoded pair,
      which is why it went stale here at all. Suite 1542, web build + oxlint green.

  - id: K16
    epic: seal-attribution
    title: "FID directory census: measure the demand-set pull scope on ONE application, and the registration-vs-attribution disagreement rate"
    type: task
    module: config
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: blocked                     # 2026-08-07 (LAPTOP): PRODUCER HALF DELIVERED, blocked on the company-side run. drydocs/fid_census.py + 14 tests + docs/k16-fid-census-company-prompt.md are in; doc 09's phase table and a "The numbers" table are built with every cell _pending_. The counts CANNOT be produced here — this repo holds no directory extract, by design — so the acceptance is only satisfiable company-side. Unblocks when the counts come back; K17 waits on that, not on the method
    depends_on: []
    inputs: [docs/restructure/09-fid-identity-and-scope.md, config/gate-prompts/fid-identity-and-scope.yaml, drydocs/loaders/seal_attribution.py]
    acceptance: >
      For ONE application, three counts are reported (counts ONLY — never a row dump;
      the output is Internal and lands company-side): (a) total directory rows for that
      application; (b) |demand set ∩ that application|, where the demand set is the union
      of run-as owners in the Control-M extract, distinct STG_APP_FACT fact_value rows
      with fact_type='FID' the K2 resolver currently counts unresolved, and account names
      in registered adhoc evidence; (c) the breakdown of (a) minus (b) by the directory's
      own type/status columns. PLUS the §Q0 measurement the gate cannot sign without: of
      the accounts in the demand set, how many have a directory application assignment
      that DISAGREES with the Control-M-derived folder attribution, and the by-case split
      of those disagreements into "different subjects, both correct" vs "stale directory"
      vs "wrong folder attribution" (§G5). Doc 09's phase table is updated with the real
      numbers. No pull beyond the demand set; no rows loaded.
    notes: >
      Groomed 2026-08-05 (desktop) from the 2026-08-05 IDEAS entries. THIS RUNS BEFORE
      THE GATE, not after: "about two hundred accounts per application" is the estimate
      the gate cannot rule on, and the census turns it into "N of ~200, and here is what
      the other rows are". The §Q0 disagreement breakdown is the part that decides
      whether the directory is a trustworthy ownership source with expected consumer
      divergence, or a lagging record needing remediation — a question raised only after
      the gate was drafted, which is why it sits in a separate item rather than inside
      the gate page. module: config (a source-scoping measurement, not a loader).
      Producer-side this repo carries the METHOD; the counts themselves are Internal.
      PRODUCER HALF DELIVERED 2026-08-07 (laptop), and the item is BLOCKED rather than
      done because its acceptance asks for real counts this repo structurally cannot
      produce — it holds no directory extract, which is the whole point of the
      demand-set pull. Shipped: drydocs/fid_census.py (pure — opens no file, contacts
      no database, writes nothing; every input injected), tests/unit/test_fid_census.py
      (14 tests, all synthetic), and docs/k16-fid-census-company-prompt.md following the
      2026-08-07 company-pack convention. Doc 09 gains a "The numbers" table with every
      cell marked pending, and an explicit note that an empty cell means NOT MEASURED,
      never zero.
      TWO DESIGN DECISIONS WORTH KNOWING BEFORE THE COUNTS ARRIVE.
      (1) COUNTS-ONLY IS ENFORCED BY THE RETURN TYPE, not by discipline: FidCensus holds
      int and dict[str,int] fields only, so a row dump is UNEXPRESSIBLE, and a test pins
      that structurally. That is what lets the result travel back into this repo at all —
      a later field carrying account names would leak Internal values into a
      producer-side artifact.
      (2) THE §G5 READING IS NEVER COMPUTED. The gate says the three readings are
      distinguished per case and never globally, with a human ruling each — so the census
      counts disagreements mechanically and parks every one in `unruled` until an SME
      ruling is injected. The number still sitting in `unruled` is itself reportable: the
      never-silent rule applied to the report rather than to a load.
      THE BLOCKER'S REASON CHANGED 2026-08-12 (SME evidence) — still blocked, much
      smaller ask. The FID directory is not an external system needing an export or an
      onboarding: it is `psgmgr.HR_PHONE_EXP`, functional rows `EMP_TYP_CD = 'FCT'`, in
      the replica ALREADY IN SCOPE and already the Control-M source. So the census is a
      SINGLE-SCHEMA JOIN — demand set (i) from CM_DEF_VJOB.OWNER against the directory
      in the same schema — and no new source crosses the boundary. Status stays blocked
      because the counts are Internal and this repo has no psgmgr access, which is
      unchanged; what changed is that satisfying it no longer waits on a source
      onboarding, only on someone running the query company-side.
      RUN Q6 FIRST (gate fid-identity-and-scope): whether CM_DEF_VJOB.OWNER holds the
      NAME or the ID decides the join key, and the two keys give different demand sets.
      Measuring with the wrong one produces a confident wrong number.
      module: config is kept as the WORK AREA; the code module classifies as
      drydocs-review in test_module_boundary.py (it loads nothing and configures nothing
      — it produces the counts an SME rules from, the same reasoning that puts
      source_mappings there).

  - id: K17
    epic: seal-attribution
    title: "Run the fid-identity-and-scope HITL gate to sign-off: FID identity, ownership as an as-of fact, and the K2 tier-2 unblock"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p1
    status: todo
    depends_on: [K16]
    inputs: [config/gate-prompts/fid-identity-and-scope.yaml, docs/restructure/09-fid-identity-and-scope.md, config/gate-log.md, drydocs_core/ontology/relationship_vocabulary/41-local-seal.yaml]
    acceptance: >
      Every section A-G is ticked or explicitly deferred with a reason, transcribed to
      config/gate-log.md under a slug-named heading (the J28 classifier reads the
      heading), and the gate's status flips from awaiting-sme. The six open questions
      Q0-Q6 are answered by the directory owner or recorded as still-open with the
      consequence of each stated. §G3 produces an explicit ruling on the K2 FID tier —
      it is a change to a SIGNED gate (seal-attribution-match-policy), so the outcome is
      either a recorded amendment to that gate or an explicit decision to leave the tier
      unfilled. Any vocabulary entry the gate confirms is registered status: planned (the
      K2 flips-are-follow-ups pattern) with its build groomed as a follow-up item; NO
      loader is built and NO pull is taken inside this item.
    notes: >
      Groomed 2026-08-05 (desktop). model: fable — this decides identity keys, edge
      meaning, and the correction of a signed gate. WHY IT MATTERS BEYOND ITS OWN SCOPE:
      TierReconcilers.fid (drydocs/loaders/seal_attribution.py) has been an empty dict
      since the K2 build, so the signed precedence order SEAL > FID > APP_NAME > ALIAS
      has never had a working second tier. §G, added on live SME evidence 2026-08-05, is
      why this is not a simple "fill the table" job: a run-as account is REGISTERED to
      the platform application while its jobs are ATTRIBUTED elsewhere, so filling the
      tier as originally specified would resolve a job to the account's owning
      application and CONTRADICT a confirmed app-code mapping. The tier is harmless today
      only because the table is empty.
      TWO SME INPUTS RECORDED 2026-08-07 (laptop) — DIRECTION, NOT RULINGS. Neither
      ticks a clause; both constrain what the session may say, in the D3-SME-INPUT
      idiom G22 used.
      (1) EVERY FID CARRIES AT LEAST TWO HUMAN OWNERS. Three consequences. (a) The
      rule is a GRAPH TEST, never a constraint — minimum cardinality is not
      expressible in Neo4j, and the gate log already settled this twice ("NEVER a
      uniqueness constraint - graph-TEST instead (the TOM-roles lesson)"; "Neo4j
      cannot declare relationship cardinality any more than it could declare the
      TOM-roles singleton"). (b) IT HAS NOTHING TO ATTACH TO YET: doc 09's phase list
      models account -> APPLICATION (phase 4, BELONGS_TO_APPLICATION
      {role: service_account}) and NEVER account -> PERSON, so a rule about human
      owners has no edge. That gap is this gate's to rule, and it must not be swept
      into doc 09's standing deferral of CONTACT columns (support owners, teams,
      mailboxes -> email-dl-contact-point): owner-of-record is the accountability
      fact the directory exists to assert, not a contact point. (c) The census
      already MEASURES compliance — accounts_below_owner_minimum, kept separate from
      accounts_with_no_owner_recorded so an unmeasurable estate is never reported as
      a compliant one.
      (2) THE ORACLE TABLE IS AN ALTERNATIVE SOURCE FOR THE FID — and this is a THIRD
      answer to §Q3, which asks only "is the export the only pull mechanism, or is
      there an API?". Not an export, not an API: a replica already in scope whose
      confidential surface is already assessed. The distinction the session must hold:
      Oracle supplies the FID VALUE (CM_DEF_VJOB.OWNER, which the psgmgr ledger
      records as "run-as tenant Functional ID (service identity, not a person); also
      the :run_as scope-bind (exact match)"), but NOT the FID RECORD — it carries no
      type, no status and no application assignment, which are exactly the columns
      §D2's clause (c) breaks down by and Q0 compares against. So Oracle can source
      demand set (i); it CANNOT substitute for the directory extract, and if it did
      the census would report 100% coverage while measuring nothing. The upside worth
      recording: sourcing (i) from Oracle means the only thing crossing the boundary
      is the directory extract filtered to accounts already named, which is the
      smallest confidential surface available and is the argument doc 09 makes for
      demand-driven scope in the first place.
      SME INPUT (2) IS NOW PARTLY FALSIFIED — 2026-08-12, and the correction is the
      point. It recorded that Oracle supplies the FID VALUE but NOT the FID RECORD,
      because it 'carries no type, no status and no application assignment'. That is
      true of CM_DEF_VJOB — the Control-M job table — and was generalized from it to
      'Oracle'. `psgmgr.HR_PHONE_EXP` carries ALL THREE: EMP_TYP_CD (type, 'FCT' for
      functional), EMP_STATUS_CD (status), CALC_LOB_NAME (LOB). So Oracle can source
      the RECORD as well as the value, the §D2 census runs inside one schema, and Q3 is
      answered with an option it did not offer. The half that still stands: the
      directory being reachable says nothing about whether its assignment AGREES with
      Control-M attribution — Q0/Q4 and the whole §G reading are untouched.
      THE account->PERSON GAP HAS A COLUMN NOW. Input (1)(b) said the two-human-owners
      rule 'has nothing to attach to yet' because doc 09 models account->APPLICATION
      and never account->PERSON. EMP_MGRS_ID is the FID's owner, and the SME states
      that owner RECERTIFIES ANNUALLY — so ownership arrives already as-of, with a
      stated cadence, which is exactly §B's subject. Raised as Q7. It must NOT be swept
      into doc 09's contact-column deferral: a recertifying manager is accountability,
      not a contact point, and input (1)(b) already drew that line.
      SEPARATE SURFACE, DO NOT CONFLATE: the SME also showed a FID MANAGEMENT TOOL
      (not the Oracle table) whose record carries a Seal id + seal status, a
      functional TYPE of Application|Component, support team/email/queue, and OWNER
      ROLES drawn from the SAME TOM vocabulary as the G35 register (the SEAL* role
      codes). Two consequences: the FID record and the application TOM register share
      a role vocabulary, which G70's register seeding should know; and the support
      team/email/queue columns are precisely §D3's 'OUT for now' set that belongs to
      email-dl-contact-point. The tool is a THIRD surface beside the table and the
      export — its columns are not evidence of what HR_PHONE_EXP projects.

  - id: K18
    epic: seal-attribution
    title: "Derive the K7 tier from the folder name instead of trusting the CSV, and give the store an explicit platform-declaration row kind — the two routes to the same silent fan-out"
    type: bug
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: [drydocs/loaders/folder_attribution.py, drydocs_api/mappings.py, web/src/routes/AppCodeCascadePane.tsx, knowledge/standards/technology/folder-naming-convention.md, internal/standards/technology/folder-naming-convention.md, docs/restructure/k7-folder-mapping-decisions.md]
    acceptance: >
      A platform app code can no longer be mistaken for a tier-1 code by either route.
      (a) DERIVATION: tier comes from the folder name — prefix positions 3-5 matched against
      the closed platform-code list (six codes; the list lives in the VALUES twin under
      internal/, the mechanism in the publishable twin) — so a code-level row that carries a
      SEAL is not read as an application attribution for folders that are merely HOSTED by
      that platform. A row-level test covers both a platform prefix and an application prefix.
      (b) AUTHORING: the mapping store gains an explicit platform-declaration entry kind
      (app_code + declaration + rationale) rather than the current declare-by-absence-of-app_id
      encoding. The store's app_id-required check STAYS — it is correct for tier-1 rows, and
      relaxing it would let a blank target through as an ordinary attribution.
      (c) Folders under a declared platform code that the steward has not resolved per-folder
      surface as `platform-unresolved` instead of falling through to the K2 fuzzy fallback,
      which is what K7 §B2 already rules and what the current write path cannot express.
      No graph-test relaxation: folder-attribution-coverage stays 1:1. Full suite green.
    notes: >
      Groomed 2026-08-05 (desktop) from Idea-66 + Idea-69, with Idea-63b and Idea-68 riding
      as clauses. WHY (a) IS THE BIGGER HALF and was the surprise: this looked like a steward
      capture exercise until the SME supplied the DAT SRE Framework -> APPCODE table. The
      platform list is CLOSED AT SIX CODES and positions 3-5 of the folder prefix decide the
      tier, so tier assignment is MECHANICALLY DERIVABLE — a six-row list plus a name parse,
      not a data-entry backlog. It also means this item does NOT depend on the SME's owed
      tier-1/tier-2 enumeration (Idea-28): that enumeration is needed to RESOLVE a tier-2
      code's consumers, not to recognize one.
      THE FAILURE IT PREVENTS IS SILENT AND LOOKS CORRECT. `AOC -> <platform SEAL>` is a TRUE
      statement about the platform and a false one about the consumer folders it would be
      stamped on. Both AOC (the datalake / Ab Initio platform) and DCL (the DPL launcher
      spine) are shared codes whose folders serve many consuming applications, and the only
      in-file hint of platform-ness is the word "Platform" in `descr` — the untrustworthy
      column. Nothing is mis-written TODAY only because no producer-side platform code has
      been authored yet.
      RIDER (Idea-63b): `descr` is a CORROBORATION signal with a known-imperfect base rate,
      never a validity test and never a derivation source. If this item compares column 2 to
      the head of column 3 at all, the output is a REVIEW QUEUE of disagreeing rows for a
      human — candidate stale renames, decommissioned SEALs, copy-paste — not a pass/fail gate.
      RIDER (Idea-68), cosmetic and do it here or not at all: "tier" names two things —
      the K7 row-kind (a STRING enum seal-born|platform|dual-coded) and the K2 match
      precedence (an INT, 1 SEAL .. 5 manual). The value spaces do not collide, so a `tier`
      edge property is not ambiguous in practice and this is naming hygiene rather than a
      correctness risk — but `row_kind` vs `match_tier` is worth renaming BEFORE either
      surfaces in a QuerySpec, and this item is already in both files.
      DISAGREEMENT RULE (added at claim, 2026-08-05): clause (a) gives the loader TWO tier
      signals for an authored code — the name-derived tier and the row's declared kind — and
      the acceptance did not say which wins when they disagree (e.g. a steward authors a
      tier-1 row for a code the name parse says is platform). Ruling, on the Idea-63b
      pattern and the S10 refusal precedent: a disagreement NEVER resolves silently. The
      name derivation wins for BLOCKING fan-out — a platform-prefixed code never fans out
      code-level even if its row claims tier-1 — and the disagreeing row lands in the
      review queue with both signals shown; a human re-authors or the list is corrected.
      The inverse disagreement (row declares platform, name parse says application) blocks
      nothing but queues the same way.
      DONE 2026-08-05 (desktop, 1386faf): (a) built — load_platform_codes (values twin
      internal/standards/technology/platform-codes.yaml, six codes; missing file = guard
      inert) + platform_prefix pos3-5 + the resolver guard (blocked fan-out queues a
      RowKindDisagreement, folders surface platform-unresolved; inverse queues without
      blocking); row-level tests cover a platform prefix AND an application prefix.
      (b) built — validate_app_code_row: platform code-level row now REQUIRES the
      platform's own app_id + rationale (declare-by-absence retired; legacy empty-app_id
      shape still reads as a declaration in the resolver, defensively); store schema v3.
      (c) built — declared-code folders already surfaced platform-unresolved; the guard
      now routes derived-platform codes there too. Idea-68 rider APPLIED: tier ->
      row_kind end to end (column, CSV header, wire, model, edge property, steward
      screen); K2 match tiers keep their name (no property surfaces, nothing to rename).
      Idea-63b rider NOT EXERCISED: no descr comparison was built (the rider is
      conditional; the review-queue rule stands recorded for whoever builds one).
      COMPANY-SIDE: header+edge-property renames ride the T23 migration family — their
      tier-authored rows re-author as row_kind; live r.tier edges migrate with the K8
      reload. Suite 1597 green. K19 unblocks.

  - id: K19
    epic: seal-attribution
    title: "An app-code mapping is an as-of assertion, not a fact: detect a reused 3-char code inheriting its predecessor's application"
    type: bug
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [K18]
    inputs: [drydocs_api/mappings.py, drydocs/loaders/folder_attribution.py, config/overrides/app-code-mappings.csv]
    acceptance: >
      The store can answer "is this code's mapping older than the folders it is being applied
      to?" and says so where a steward will see it. Concretely: a check that reports any
      app_code whose mapping `authored_on` PREDATES the first-seen date of folders now
      carrying that code, as a review queue with counts — never an automatic re-attribution,
      because a reissued code and a genuinely growing application are indistinguishable
      without a human. Either effective dating on the mapping row (valid_from/valid_to) or the
      detection alone, with the choice recorded in the item's close note and the reason.
      A test builds the reuse case from fixtures: one code, two eras, folders on both sides.
    notes: >
      Groomed 2026-08-05 (desktop) from Idea-67 (user, same day). THE SOURCE FACT: a Control-M
      app code is a 3-character namespace, which is scarce, so codes are retired and REISSUED
      with a different meaning — `DDC` is the documented case (created for the PySpark
      conversion, repurposed, nothing PySpark now). That makes a code -> application mapping
      an as-of assertion, the same shape as the FID -> SEAL registration ruled in
      `fid-identity-and-scope` §B1 and the third current-state-only identifier found in one
      week. `authored_on` already gives the store an as-of; what nothing prevents is a reused
      code silently inheriting its predecessor's mapping while folders authored under the OLD
      meaning keep an attribution that is now wrong.
      DEPENDS ON K18 for a practical reason, not a logical one: K18 touches the same store
      entry shape, and doing the two in one pass avoids migrating the row format twice.
      NOT IN SCOPE: deciding whether history should be preserved (a retired mapping kept
      valid_to-stamped) or overwritten. That is an ontology-flavored question about whether
      the graph records past attributions at all, and it goes to a gate if the answer is
      anything but "detect and report".
    progress: >
      2026-08-05 — DONE (desktop, claimed same day). CHOICE: detection alone, NO
      valid_from/valid_to effective dating. Reasons: (1) effective dating IS preserved
      mapping history, which this item's own scope note routes to a gate if the answer is
      anything but "detect and report" — choosing it here would pre-empt that gate; (2) it
      would be a second committed-CSV format break (v4) landing on the company right behind
      K18's v3, for a file that is header-only producer-side; (3) dating buys no automatic
      power anyway — a reissued code and a growing application stay indistinguishable
      without a human. BUILT: detect_mapping_age_suspects() (pure, folder_attribution.py)
      answers "is this row older than the folders it is applied to?" per authored ROW (each
      row is its own as-of assertion; same-day is not postdating; no date on either side =
      no age claim; per-folder rows age against their one folder). Queue surfaces as
      MappingAgeSuspect entries on the coverage (count in as_dict + stamped
      run.mapping_age_suspects + LOGGER warning), and the CLI prints the full
      mapping_age_review_queue after the coverage dict — reported, never re-attributed;
      suspect folders stay attributed, so the queue rides OUTSIDE the coverage invariant.
      Folder dates come from ControlMFolder.first_seen_at (fetch_folder_first_seen, date
      part only). Tests: the acceptance's reuse case verbatim (one code DDC, two eras,
      folders on both sides — flagged AND still attributed) + inertness cases.

  - id: K20
    epic: seal-attribution
    title: "Draft the K5-amendment gate prompt: is Tech Partner product-level (SME + role doc) or AreaProduct-level (as signed), and where does Area Tech Partner live?"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - config/gate-prompts/product-cabinet-attribution.yaml
      - docs/Product/technology_roles_and_responsibilities.md
      - drydocs_core/schema/catalog_ontology_supplement.cypher
    acceptance: >
      A gate prompt is drafted on the G35 amendment model (drafting decides nothing;
      K5 stays signed and the supplement unedited until sign-off) covering: (1) the
      attach level — the SME (2026-08-06, in-chat) and the company role doc both
      read Tech Partner as PRODUCT-level while K5 §B118 signed AreaProduct-only;
      (2) whether Area Tech Partner needs its own concept in a scheme K5 fixed at
      exactly 7; (3) the measurable consequence today — tech_partner is scoped
      exclusively to a node class with zero instances and no loader (the
      technology_risk_controls G35 §A2 failure mode reproduced), which also argues
      for a seeded-concept-reachability check; (4) the K5 change_note re-read (its
      two halves may describe one level). The adjacent SEAL-side "tech partner" ->
      "CTO" alias ruling (G35 §A6) is cited as SEPARATE and unaffected.
    notes: >
      Groomed 2026-08-07 from Idea-75. p1 — a signed concept nothing can write is
      live governance debt. G35's scope fence explicitly declines to reopen K5, so
      this cannot fold into that walk; it needs its own gate. Inputs verified at the groom: the K5 gate
      prompt is config/gate-prompts/product-cabinet-attribution.yaml and the
      supplement lives in drydocs_core/schema/.
  - id: K21
    epic: seal-attribution
    title: "LAPTOP: mine the ServiceNow replica screenshots into the TOM mapping — the working query, the view inventory, and whether we are asking the replica for the right columns"
    type: task
    module: taxonomy
    phase: 9
    agent: main
    model: fable
    priority: p2
    status: done
    depends_on: []
    inputs:
      - config/gate-prompts/seal-tom-attribution-reshape.yaml
      - config/gate-prompts/tom-roles-enumeration-and-cardinality.yaml
      - config/source-registry.yaml
      - config/taxonomy/business-application.yaml
      - reference/platforms/README.md
    acceptance: >
      THIS IS A LAPTOP ITEM by SME assignment — the evidence is a set of DBeaver
      screenshots held on the laptop, and they are INTERNAL: replica host,
      database name, schema names, SEAL application ids, configuration-item names
      and the `x_<scope>_` company scope prefix never enter this repo. Only
      mechanism is recorded here; anything value-bearing stays company-side.
      Three deliverables. (1) THE WORKING QUERY, transcribed as MECHANISM into
      the ServiceNow/TOM material: the join spine is
      `x_<scope>_cmdb_tom_main` -> `x_<scope>_cmdb_tom_roles` (role -> sys_id,
      active) -> `cmdb_ci` (parent_id) -> `core_company` -> `cmdb_ci_service_discovered`
      -> `sys_user_group` (group, and again on assignment_group) ->
      `cmdb_ci_business_app`, with the CI self-join on `inherited_from_ci`. The
      columns that matter to DryDocs are named and mapped to our vocabulary:
      `u_seal_application_id` / `u_seal_deployment_id` (the SEAL join),
      `correlation_id`, `sys_class_name`, `inheritance` / `inherited_from_ci`,
      `u_status`, `u_application_state`, `u_external_service`, and the
      role/group/team names. State which of those the TOM-role gates (G35,
      seal-tom-attribution-reshape) already assume and which are NEW evidence.
      (2) THE VIEW INVENTORY as taxonomy: the replica exposes one `V_`-prefixed
      view per ServiceNow table across several `DW_*_DATA_VIEW` schemas
      (a common/CMDB one, an ITSM one, and at least two others). Capture the
      view -> ServiceNow-table naming rule and the schema split; that rule is
      what keeps `origin: snow` unambiguous while the carrier is Snowflake, and
      it is currently asserted in a comment with nothing behind it.
      (3) THE PRECISION QUESTION, which is the point of the item rather than a
      rider: the sample query is a hand-built REPORT — it filters to one
      company, one application state set, one SEAL id and one named CI. That is
      not an ingestion contract. Decide what DryDocs should actually pull, and
      justify it against the PUBLIC ServiceNow CMDB documentation (the tables
      are stock ServiceNow, so their canonical columns, `sys_class_name`
      hierarchy and CI-relationship model are all publicly documented and can be
      read from the web without touching the estate) — the vendor doc is the
      baseline, the replica is the instance, and where they disagree the
      difference is a company extension worth naming. Specifically rule whether
      the CI-relationship tables (`cmdb_rel_ci` and the `cmdb_rel_*` family
      visible in the navigator) belong in the pull, since a CMDB without its
      relationship rows is a node list rather than a graph.
      Output is an analysis document plus, if warranted, a DRAFT dataset row for
      the registry under the `snow@[db].[schema].<servicenow_table>` grammar with
      `confirmed: false`. NO source is activated, no loader is built and no gate
      is signed inside this item — a real pull scope is a gate decision.
    notes: >
      Raised 2026-08-09 (desktop) on SME direction, from DBeaver screenshots of
      the replica the SME will query. WHY THE LAPTOP: the screenshots live
      there, and they are Internal — moving them to reach a different session is
      the wrong direction of travel.
      CONTEXT THIS ITEM DEPENDS ON, so it is not re-derived: ServiceNow metadata
      is NOT read from ServiceNow. It is read from an AWS Snowflake REPLICA,
      which is the same shape as Control-M being read from the Oracle `psgmgr`
      replica rather than from the orchestrator. So `snowflake` is the CARRIER
      and `snow` keeps the ORIGIN slot — recorded on both system entries in
      config/source-registry.yaml 2026-08-09 with guards, after the SME flagged
      SNOW/Snowflake as an IMPORTANT confusion. The v2 grammar already ruled
      this ("replica grammar: origin catalog, carrier snowflake").
      WHY THE PUBLIC-DOC LEG IS WORTH THE TIME rather than shortcutting to the
      replica: these are STOCK ServiceNow tables, so the vendor documents the
      canonical schema publicly. That gives a baseline the estate cannot supply
      — it tells us which columns are ServiceNow's and which are local
      extensions (the `u_` prefix and the `x_<scope>_` scoped app are the two
      local classes visible in the sample), and a column we invent a meaning for
      because we only ever saw it in one hand-built report is exactly the kind
      of assumption T10/T13 exist to prevent. Route the vendor lookup through
      the reference-librarian agent and register what it finds in
      reference/REGISTRY.yaml, the same way BMC and Oracle are held.
      RELATIONSHIP TO THE OPEN TOM GATES: G35 (TOM-roles enumeration, in
      progress) and seal-tom-attribution-reshape both reason about role classes
      and cardinality from the SEAL side. This item supplies the ServiceNow-side
      evidence they have been arguing without — the `tom_roles` join and its
      `active` flag are the live shape of exactly what G35 is trying to
      enumerate. Feed the findings INTO those gates; do not pre-empt them.
      EVIDENCE ADDED 2026-08-09 (SME showed the cmdb_rel family live, so clause (3)'s relationship-table question is ANSWERED IN PART before the item starts — do not re-derive these, verify them): (a) THE EDGE TABLE IS REAL AND IS AN EDGE LIST. `cmdb_rel_ci` carries `parent` / `child` / `type`, all sys_id GUIDs, with `type` an FK to `cmdb_rel_type` — which in turn carries `name`, `child_descriptor` and an `end_point` boolean. That is a FORWARD LABEL plus an INVERSE LABEL on the type row, which is the same shape as this repo's own relationship_vocabulary (`neo4j_label` + `inverse_label`). The mapping is therefore close to mechanical rather than interpretive, and the item should say so rather than treating it as open. Also present: `connection_strength` and `percent_outage`, i.e. the CMDB models impact weight on the edge, which we have no equivalent for. (b) THE REPLICA IS NOT A PURE MIRROR, and this is the sharpest correction to the 'stock ServiceNow tables' premise the item is built on. The view carries columns ServiceNow never had: a `dwintel_dl_*` snapshot family (snapshot date, load timestamp, snapshot trim) and a `delete_flag`. Those are CARRIER artifacts of the data-lake replication, not vendor schema — exactly the vendor-baseline-vs-instance difference clause (3) exists to name, now with a concrete first example. (c) `delete_flag` MAKES SOFT DELETES AN INGESTION CONTRACT QUESTION, not a detail: if rows persist in the replica after deletion in ServiceNow, then any pull that ignores the flag ingests dead CIs and dead edges as live ones. Rule it explicitly. The D7 tombstone idiom is the obvious precedent — we already distinguish removed-from-source from never-existed, and this is the same problem arriving from a different source. (d) THE SERVICENOW AUDIT ENVELOPE IS PRESENT: `sys_created_by` / `sys_created_on` / `sys_updated_by` / `sys_updated_on` map cleanly onto the four frozen envelope property names, so a `config/audit-fields.yaml` entry for any snow dataset is a real mapping rather than another stub — unlike most sources ruled at audit-envelope-phase4. `dwintel_dl_snapshot_dt` is replication time and is EXCLUDED under the standing CAPTURE_DATE rule. Note the split: the ServiceNow columns are authorship, the dwintel ones are capture, and only the first kind is envelope. (e) Local extensions visible on this one view: `u_hash`, `u_discovery_source` (the `u_` class) and a company scoped-app data-destruction column (the `x_<scope>_` class). Both classes were predicted by the item; this confirms them on a table we would actually ingest. Mechanism only — GUID values, the scope name, database and host stay company-side.
      DONE 2026-08-09 (LAPTOP, same day it was raised). Output: knowledge/upgrade-plans/servicenow-replica-evidence.md — the INSTANCE half of a pair with C10's servicenow-cmdb-analysis.md (the vendor-doc BASELINE half), cross-linked both ways. All three clauses delivered. Clause (1): the nine-table join spine and a per-column map marking which columns the TOM gates already assume and which are new. Clause (2): the V_-prefix rule now has evidence behind it (no exception observed) plus the observed view inventory by family, recorded WITH its limits — the navigator was cut at both ends, and V_SYS_USER_GROUP / V_SYS_CHOICE are known present from the query rather than from the tree. Clause (3) ruled: cmdb_rel_ci + cmdb_rel_type ARE in the pull, the other seven cmdb_rel_* views are NOT (they are ServiceNow's own governance and UI machinery for maintaining edges, not the edges), and a three-ring scope is proposed for the gate to rule. FOUR FINDINGS WORTH THE ITEM. (a) The item's own premise needed correcting: these are NOT all stock tables. The two at the centre of what G35 needs — x_<scope>_cmdb_tom_main and tom_roles — are a company SCOPED APP with no vendor baseline at all, so the public-doc leg cannot cover them and G35 is reasoning about a surface the vendor has never described. (b) The SEAL ids sit on cmdb_ci_service_discovered (CSDM Application Service), NOT on cmdb_ci_business_app — and u_seal_deployment_id sits beside u_seal_application_id. The source keys application and deployment separately where DryDocs has one concept, which meets the trigger condition C10's gate-bound candidate #1 was deferred on; that candidate is annotated RE-OPENED rather than acted on. (c) G35 §E4's premise changes: it assumed the inherited-from parent CI is estate data DryDocs does not have, and the parent is cmdb_ci in the SAME replica, so E4 is now a modelling choice rather than an availability constraint. (d) A tension for §B6 — as sampled, the TOM row names a GROUP (sys_user_group), never a person, which is recorded as a limit of one report rather than a conclusion and is open question 1. Also corrected the standing evidence note on cmdb_rel_type: the vendor baseline is parent_descriptor + child_descriptor as TWO columns with name being their "A::B" concatenation, so read the columns and do NOT split name on '::'. Registered ServiceNow in reference/REGISTRY.yaml as a source-platform with the public-docs source_url, and recorded on the snowflake entry the non-obvious fact that Snowflake is already live as the replica CARRIER (the reference/platforms README said "future placeholder"). NOTHING ACTIVATED, as the item required: no source row written to config/source-registry.yaml, no loader, no gate signed. The ring-1 dataset rows are DRAFTED IN THE DOCUMENT with confirmed: false rather than pasted into the registry — deliberate, because the registry is an enforcement surface and the pull scope is a gate decision, so the gate should approve a recommendation instead of striking rows. Six open questions carried, three of them SME-blocking. Suite green (1772 passed); the one red file, test_seal_samples.py, is the uncommitted business-application.yaml work in progress and was proven independent by stashing it.
  - id: L1
    epic: doc-infrastructure
    title: "Canonical TDD outline + completeness/traceability validator; refit the Control-M TDD"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: []
    inputs: ["SDLC-Docs/extracted/Technical Design Doc Template.doc.txt", "SDLC-Docs/extracted/Technical Design.docx.txt", docs/design/controlm-ingestion-tdd.md, docs/restructure/backlog.yaml]
    acceptance: >
      docs/design/templates/tdd.outline.yaml defines the canonical TDD outline
      (schema drydocs.doc-outline.v1: ordered sections, each a STABLE authored anchor +
      required flag + guidance), modernized from the SDLC corpus (MERGES the enterprise
      Preface→…→Traceability-Matrix shape with the agile Background→DoR/DoD→Component-
      Changes→Tests shape) and adds the DryDocs-specific sections (four-layer frame,
      classification, HITL gate); a completeness validator asserts a given TDD contains
      every required anchor and fails listing gaps, plus a traceability rule (every
      requirement id appears once in the matrix section); docs/design/
      controlm-ingestion-tdd.md is refit to the outline and passes; validator is
      test-enforced (tests/unit/test_doc_outline.py); full suite green.
    output: docs/design/templates/tdd.outline.yaml + drydocs/doc_outline.py
    notes: >
      Traceability matrix is first-class (the §6 both SDLC TDDs orbit). drydocs-docgen
      classified in test_module_boundary.py (docgen group) + MODULE_MAP.md.
    progress: >
      2026-07-08 — DONE (user sign-off; committed + folded into main).
      Outline reviewed with the user; 5 decisions applied: (1) dropped org-standard/agile
      sections; (2) required-with-`N/A`-escape; (3) traceability is `mode: loose` now
      (matrix shape only), tightens to strict once the ingestion loop surfaces real
      requirements; (4) merged `data-config` INTO `detailed-design` + a `design-data-mapping`
      column-level subsection (the ingestion-layer view); (5) numbering deferred. Result:
      tdd.outline.yaml = 12 sections / 10 required (+1 optional subsection) + loose/strict
      traceability contract; validator drydocs/doc_outline.py (anchor `<!-- anchor: id -->`,
      completeness + loose/strict traceability, CLI); tests/unit/test_doc_outline.py
      (10 tests incl. real-TDD-conforms + loose-mode) pass via direct driver (pytest absent
      on this box); controlm-ingestion-tdd.md refit (Rev 3) VALIDATES clean; boundary guard
      4/4 green. Full `poetry pytest -q` not runnable here.
  - id: L3
    epic: doc-infrastructure
    title: "Deterministic design-doc renderer (.md -> .html/.print.html) + stale-check + ritual hook"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [L1]
    inputs: [scripts/render_board.py, drydocs/plan_board.py, drydocs/gate_pages.py, docs/design/controlm-ingestion-tdd.md]
    acceptance: >
      scripts/render_design_doc.py renders a doc .md -> .html + .print.html through a
      shared print-CSS template, PURE and deterministic (no build timestamps — Rev/commit
      passed in, like the board's `updated:`), emitting each outline section's stable
      anchor as the element id; offline unit tests on a fixture doc; the .md becomes the
      single committed source (the hand-maintained .print.html is retired); a stale-check
      (re-render + `git diff --quiet`) is wired into the session ritual beside the board
      regen; full suite green.
    output: scripts/render_design_doc.py + drydocs/design_doc.py
    notes: >
      Renderer approach = B2 (user, 2026-07-08): stdlib hand-rolled markdown-SUBSET renderer,
      NO new dependency — determinism is intrinsic (no library version to drift) and matches
      the repo's hand-built-HTML pattern. Subset: headings, GFM tables, fenced code, bold,
      italic, inline code, links, blockquotes, ordered/unordered (nested) lists, hr, anchors.
    progress: >
      2026-07-08 — DONE (user sign-off; option 1 — the generated print.html is accepted as the
      single-source-derived artifact, retiring the hand-crafted one).
      drydocs/design_doc.py (render_body/render_doc/write_doc; `<!-- anchor: id -->` -> element
      id; screen + print CSS, print CSS reuses the prior hand-tuned look); scripts/
      render_design_doc.py CLI; tests/unit/test_design_doc.py (14 tests) pass via direct driver;
      render is byte-identical across re-runs (deterministic). Generated controlm-ingestion-tdd.
      {html,print.html} from the .md — the .md is now the SINGLE SOURCE (hand-maintained
      print.html RETIRED; it loses the bespoke page-1/callout/footer chrome that was hand-authored
      and never in the md). Ritual hook: snapshot.ps1 refreshes docs/design/*.md + the CLAUDE.md
      stale-render check extended. boundary 4/4; backlog green.
  - id: L4
    epic: doc-infrastructure
    title: "Deterministic PDF step (.print.html -> .pdf, pinned headless)"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [L3]
    inputs: [scripts/render_design_doc.py, PUBLISH-BOUNDARY.md, config/classification.yaml]
    acceptance: >
      scripts/doc_to_pdf.py renders .print.html -> .pdf via a PINNED headless-Chrome
      invocation with fixed flags; the PDF /CreationDate is normalized so re-runs are
      STRUCTURALLY identical (stable layout + anchor positions — NOT byte-identical, by
      design, per the user's intent); a documented regeneration command; the blank .pdf
      is build-on-demand (gitignored or Rev-boundary commit only) per PUBLISH-BOUNDARY,
      while scanned annotated PDFs are captured input; full suite green.
    notes: >
      The paper bridge: print -> pen -> scan. The blank pdf is a reproducible throwaway;
      the SCANNED pdf is the valuable artifact that L6 feeds back by anchor.
    progress: >
      2026-07-08 — DONE (user sign-off). drydocs/doc_pdf.py (browser discovery Edge/Chrome-first —
      Brave HANGS headless on this box, 2x 180s; Edge rendered in 3.5s; fixed --headless=new print
      flags; /CreationDate+/ModDate normalized) + scripts/doc_to_pdf.py + tests/unit/test_doc_pdf.py
      (4). PDF is BUILD-ON-DEMAND (option A): docs/design/*.pdf gitignored + untracked. Structurally
      reproducible, not byte-identical (trailer /ID varies, by design).
  - id: L5
    epic: doc-infrastructure
    title: "Digital HITL: per-section SAVE/annotate control keyed to anchors"
    type: requirement
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [L3]
    inputs: [drydocs/gate_pages.py, drydocs/plan_board.py, scripts/render_design_doc.py]
    acceptance: >
      the rendered .html gains a per-section annotate control with localStorage
      persistence (the gate_pages.py pattern) that EXPORTS a paste-ready, anchor-keyed
      feedback line (clipboard, the board quick-capture pattern) into
      docs/design/feedback/<doc>-rev<N>.yaml; offline unit test on the emitted feedback
      structure; full suite green.
    notes: >
      Closes the gap the user flagged — HTML reads well for humans+agents but is hard to
      mark up; this is the WIP "save button", COMPOSED from two existing primitives
      rather than invented.
    progress: >
      2026-07-08 — DONE (user sign-off). design_doc.py screen render gained a per-anchor annotate
      control (localStorage) + "Copy feedback" clipboard export in the feedback_yaml format; print
      output stays clean. docs/design/feedback/README.md documents the format. test_design_doc.py
      now 17 tests (3 new). Verified live (✎ buttons + toolbar render).
  - id: L6
    epic: doc-infrastructure
    title: "Paper HITL: print-margin anchors + scan->transcribe skill"
    type: requirement
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [L4]
    inputs: [scripts/doc_to_pdf.py, .claude/skills/groom-backlog/SKILL.md, config/classification.yaml]
    acceptance: >
      the .print.html renders each section's stable anchor in the print margin plus a
      Rev/commit footer; a .claude/skills/transcribe-doc-markup skill turns a scanned
      annotated PDF into anchor-keyed lines in docs/design/feedback/<doc>-rev<N>.yaml
      (faithful transcription shown FIRST, per the groom pattern); scans stored under
      docs/design/feedback/scans/ classification-aware (Internal → not published);
      demonstrated on a fixture scan.
    notes: >
      The pen/paper loop the user works best in; annotations re-attach to requirements by
      anchor, surviving re-renders. Done 2026-07-08 (sonnet agent): print-only margin
      anchors + front-matter-derived Rev/commit footer in design_doc.py (deterministic —
      no git/timestamp reads); transcribe-doc-markup skill emits via the SAME
      feedback_yaml() as L5 so paper/digital are shape-identical; scans dir triple-guarded
      (gitignore + classification excluded_paths + README). 11 new tests.
  - id: L7
    epic: doc-infrastructure
    title: "Ingest the traceability matrix + feedback as DryDocs context (via the gate)"
    type: requirement
    module: ontology
    phase: 10
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-20 — gate doc-traceability-feedback SIGNED OFF (21/21, SaaS product-plane reframe); vocab active + supplement + 3-pass loader load-doc-traceability; live-loaded + verified (95 sections / 41 requirements / 44 SPECIFIED_IN, 0 unlinked)
    depends_on: [L1, L5]
    inputs: [docs/design/templates/tdd.outline.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      the requirements traceability matrix (requirement -> design-section anchor ->
      component/module -> test) and the anchor-keyed feedback are modeled as DryDocs graph
      edges THROUGH the HITL gate (relationship vocab registered planned -> confirmed),
      correlating each annotation to its requirement node; NO edge is written before gate
      confirmation; the mapping is logged in config/gate-log.md.
    notes: >
      Dogfoods DryDocs — the docs about the system become queryable context in it. Ties
      to Epic E (context graph). Later phase; an ontology/edge-meaning decision => the
      gate, never auto-decided.
  - id: L8
    epic: doc-infrastructure
    title: "Runbook outline + author the DryDocs startup/refresh runbook (capstone: 2nd doc type)"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p1
    status: done          # DONE 2026-07-20 (e6bcb24) — the capstone: 2nd doc type through the outline system
    depends_on: [L1, L5, L6]
    inputs: [docs/design/templates/tdd.outline.yaml, internal/helpmeloginlocalneo4j.md, scripts/ingest.sh, knowledge/depgraph-snapshots/snapshot.ps1, README.md]
    acceptance: >
      docs/design/templates/runbook.outline.yaml defines the canonical Runbook outline
      (stable anchors: purpose/scope, prerequisites, startup, refresh/ingest, verify,
      rollback, troubleshooting, contacts/escalation) reusing the L1 doc-outline schema;
      a REAL DryDocs startup/refresh runbook is authored to it (Neo4j Docker bring-up +
      .env, the ingest-controlm chain, board + depgraph snapshot refresh, m1/m3-verify),
      rendered via L3 and exercised through BOTH feedback loops (L5 digital, L6 paper),
      and passes the completeness validator; classification set; full suite green.
    notes: >
      Resequenced 2026-07-08 (was L2) to run AFTER the full render+feedback pipeline, per
      the user: the runbook is the CAPSTONE proving the outline system generalizes to a
      second doc type end-to-end. depends_on covers the outline (L1) + both feedback loops
      (L5→L3 renderer, L6→L4 pdf); intentionally NOT gated on L7 (context ingest), which
      the runbook doesn't need — say the word to make it strictly last.
      DONE 2026-07-20 (e6bcb24): runbook.outline.yaml = front-matter + the 8 acceptance
      anchors + optional appendices; decisions recorded in its header — front-matter
      carried from the TDD outline (the L6 footer derives from it), NO traceability block
      (procedures, not requirements; verify is the runbook's proof surface — add the
      block later rather than invent a per-doc format). Exemplar
      docs/design/drydocs-startup-refresh-runbook.md (classification Internal, the
      helpmeloginlocalneo4j class; secrets .env-only) rendered via L3; both loops
      verified IN the emitted html (annotate/Copy feedback; dd-margin-tag +
      dd-print-footer "Rev 1 · commit 0acc0f8"). test_doc_outline.py sweeps
      *-runbook.md (3 new tests). Suite 738. LANDING FIRES two parked inbox triggers
      for the next groom: the runbook-mapping-demo refit and the review.outline.yaml
      consideration (both lines cite "when L8 lands").

  - id: L9
    epic: doc-infrastructure
    title: "Design-doc screen HTML renders partial in Chrome (Brave renders it correctly)"
    type: bug
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done                                     # 2026-07-21 pm — NO LONGER REPRODUCES: controlm-ingestion-tdd.html renders FULL-PAGE in Chrome 150.0.7871.129 (headless=new, 1200x2000 viewport + 1200x17000 full-height capture reaching the doc end; evidence l9-chrome-*.png, session scratchpad). Root cause attribution: the report (2026-07-08) predates the L13 single-html rework that REPLACED the L3-era screen CSS — the stylesheet the bug was filed against no longer exists. No CSS change made; L4 PDF path untouched (stays pinned Edge/Chrome headless; the Brave headless hang note stands); renderer tests green. CAVEAT recorded: verified headless — if it still reproduces in INTERACTIVE Chrome, re-open with a screenshot.
    depends_on: [L3]
    inputs: [docs/design/controlm-ingestion-tdd.html, drydocs/design_doc.py]
    acceptance: >
      The L3 screen .html renders full-page in current Chrome (root cause identified —
      likely CSS/print-view/file:// quirk, markup already validated), or the screen CSS
      is adjusted; the L4 PDF path stays pinned to Edge/Chrome headless (Brave hangs
      headless --print-to-pdf on this box, confirmed 2x 180s timeouts). Renderer tests green.
    notes: "Groomed 2026-07-08 from IDEAS (seen same day; Brave = reference viewer for now)."

  - id: L10
    epic: doc-infrastructure
    title: "Design-doc feedback widget: appendix 'SME - Feedback' panel with notes field + paste destination"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [L5]
    inputs: [drydocs/design_doc.py, docs/design/feedback/README.md, tests/unit/test_design_doc.py]
    acceptance: >
      the screen .html gains an "SME - Feedback" section below the appendix / last outline
      section, separated by a divider line, containing a SHORT static instruction block
      telling the reviewer exactly what to do with their annotations — numbered steps in
      plain language, concretely: annotate sections with the per-section pencil control;
      click "Copy feedback" (bottom toolbar); create the file
      docs/design/feedback/<doc-stem>-rev<N>.yaml (YAML — not markdown; the exact
      per-doc filename with the doc's current Rev is rendered into the instructions, ready
      to copy); paste the clipboard contents in and save/commit. Print output unchanged;
      render stays deterministic (Rev comes from front matter, no git/timestamp reads);
      test_design_doc.py extended; full suite green.
    notes: >
      Groomed 2026-07-09 from a chat note after reviewing docs/design/feedback/scans/;
      amended same day — the panel is a static HOW-TO instruction block, NOT a free-text
      notes area (user call: the anchor-keyed notes already capture content; the panel's
      job is walking the SME through create-file/copy/paste). Enhances L5 (copy button
      verified working by the user). Answers the note's open question: the export
      extension is .yaml (drydocs.design_doc.feedback_yaml), not .md.
    progress: >
      2026-07-09 — DONE. design_doc.py: _sme_feedback_panel + sme_feedback_filename
      (Rev from front matter, deterministic; placeholder rev<N> when undeclared);
      appended after the last section in screen mode only, no element ids inside so the
      annotate layer never attaches to its own instructions. Verified at runtime
      (headless-Edge DOM dump: panel present, 0 controls inside; exact filename
      controlm-ingestion-tdd-rev3.yaml rendered). 3 new tests; feedback README updated.

  - id: L11
    epic: doc-infrastructure
    title: "Design-doc feedback: per-subsection annotate controls when a section has >2 subsections"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [L5]
    inputs: [drydocs/design_doc.py, drydocs/doc_outline.py, docs/design/feedback/README.md, tests/unit/test_design_doc.py]
    acceptance: >
      when a rendered section contains MORE THAN TWO subsections (nested sub-headings
      like 1.a/1.b/1.c/1.d, or a numbered step list 1/2/3), the screen .html renders an
      annotate (pencil) control on EACH subsection, not just the section header, so the
      exported feedback keys to the specific subsection (tighter control); each
      subsection control uses a STABLE derived anchor (parent anchor + deterministic
      subsection suffix — never positional-only, so notes survive re-renders and
      subsection insertion), and that derived-anchor namespace is accepted by the
      feedback_yaml format / doc_outline anchor validation and documented in
      docs/design/feedback/README.md; sections with two or fewer subsections keep the
      single section-level control; print output unchanged; render stays deterministic;
      test_design_doc.py extended (cases: 3+ sub-headings, numbered step list, <=2
      subsections unchanged); full suite green.
    notes: >
      Groomed 2026-07-09 from a chat note (follow-on to L10, same review pass). Threshold
      is the user's ">2" (examples given: 1.a-1.d, steps 1-3); confirm the exact boundary
      (>2 vs >=2) with the user at implementation if it matters in practice. The anchor
      derivation is the design core: feedback re-attaches by anchor, so subsection anchors
      must be as stable as the authored section anchors.
    progress: >
      2026-07-09 — DONE. design_doc.py: _inject_subsection_anchors (screen-only post-pass)
      derives <parent>--<slug-of-subsection-text> ids for sub-headings one level down and
      for top-level items of numbered lists, when the group has >2 members; anchored
      subheadings count toward the threshold but keep their authored id; dedupe -2/-3
      deterministic; JS attach() gained an LI branch (button afterbegin / box beforeend).
      doc_outline.py: DERIVED_ANCHOR_SEP + feedback_anchor_valid (a derived note degrades
      to its parent section if the slug changes; "--" now reserved in authored ids).
      Verified at runtime (headless-Edge DOM dump: 29/29 ids present, controls on all
      derived h3s + 9 list items; print surface byte-identical to pre-change). 7+3 new
      tests; feedback README documents the namespace.

  - id: L12
    epic: doc-infrastructure
    title: "Document the governed-render-fidelity rule: governed surfaces publish verbatim; editorial styling only for outward-facing docs"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: haiku
    priority: p2
    status: done                                     # 2026-07-21 pm — PLACEMENT: CLAUDE.md §6 Working agreements (new bullet 'Governed renders publish VERBATIM', right after verify-before-asserting — the spot every session reads); names both surface classes (governed = docs/design/* renders + gate pages + board.html, verbatim because L5/L6 feedback re-attachment keys anchors on them; non-governed outward-facing = whitepaper/website, the only editorial-treatment surfaces). No renderer code change.
    depends_on: []
    inputs: [CLAUDE.md, docs/design/, docs/plan/board.html, docs/whitepaper/drydocs-whitepaper.md]
    acceptance: >
      A governed-render-fidelity rule is documented where agents will hit it (CLAUDE.md
      working agreements and/or the docgen-adjacent skill docs — placement recorded in
      these notes when done): governed surfaces (docs/design/* renders, gate pages,
      docs/plan/board.html) are published/shared VERBATIM from their deterministic
      renderers — never restyled, summarized, or editorially reworked — because the
      HITL loop keys anchors on them (L5 digital / L6 paper feedback re-attachment);
      editorial/design treatment (artifact-design pass, the whitepaper's visual
      identity) applies ONLY to non-governed outward-facing docs (whitepaper, website).
      The rule names both surface classes. No renderer code change.
    notes: >
      Groomed 2026-07-13 from the 2026-07-12 IDEAS line (artifact-design review,
      sub-item 1 of 3). Sub-item 2 merged into O1 (design-lens note); sub-item 3
      (whitepaper "overnight ledger" identity as the dry-docs.com visual seed)
      re-inboxed, parked until website work starts.

  - id: L13
    epic: doc-infrastructure
    title: "Collapse the design-doc dual-HTML render to ONE file: .print.html misrenders; the screen .html already print-adapts — retire the .print.html series"
    type: bug
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/design_doc.py, drydocs/doc_outline.py, scripts/render_design_doc.py, scripts/doc_to_pdf.py, docs/design/, tests/unit/test_design_doc.py, tests/unit/test_doc_pdf.py, CLAUDE.md]
    acceptance: >
      write_doc emits a SINGLE .html per design doc (the .print.html twin retired):
      on screen it keeps the current dark theme + the L5 annotate/save layer; under
      @media print it renders black-on-white (already true of today's screen html)
      AND carries the L6 print-margin anchor tags currently injected only into the
      .print.html — the paper HITL loop (print, pen, scan, transcribe-doc-markup
      re-attach) must survive the collapse unchanged, and the doc's own
      "How to read" boilerplate updates to name one file for both uses.
      scripts/doc_to_pdf.py re-points to the single html (PDF output visually
      equivalent to today's print render). All committed docs/design/*.print.html
      files are git-rm'd and the renderer never writes them again; the session
      ritual/stale-render wording (CLAUDE.md) and render/pdf/transcribe tests
      update to the one-file world. Regenerated renders committed; full suite
      green.
    notes: >
      Filed 2026-07-18 from SME chat + screenshot evidence (root-level
      drydocs-project-review-print-html-bug.png, local-only per the root-images
      gitignore): drydocs-project-review.print.html renders WRONG in the
      browser, while the plain .html renders correctly on screen (white-on-
      black) and already converts to black-on-white when printed — two surfaces
      where one suffices, and the broken one is the redundant one. SME call in
      chat: "we only need one — remove the .print.html series." Related but
      distinct: L9 (screen html partial-render in Chrome specifically) — fixing
      L13 does not close L9; check both at execution since the single file
      becomes the only surface. Design intent stays deterministic renders +
      anchor-keyed HITL (Epic L invariants), only the file count changes.
      2026-07-18 — DONE (commit 71750bd). _PRINT_CSS became an @media print block
      LAST in the merged sheet (variable block lists every theme selector, so the
      light palette beats OS-dark and the toggle); L6 margin tags injected into
      the one body BEFORE the L11 pass (printed gutter stays authored-only),
      screen-hidden by base CSS; Rev/commit footer always emitted, print-shown.
      doc_pdf re-pointed (with_suffix). PDF equivalence live-verified: 8 pages
      old vs 8 pages new on controlm-ingestion-tdd (the screen layout would
      paginate far past 8 — the print sheet drove it). 6 committed .print.html
      git-rm'd; renders regenerated; ritual/skill/README/MODULE_MAP/PORT-MANIFEST
      wording moved to one-file world; suite 708 green. L9 remains open.
  - id: L14
    epic: doc-infrastructure
    title: "Refit docs/runbook-mapping-demo.md to the runbook outline (2nd runbook exemplar)"
    type: chore
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done                                     # 2026-07-21 pm — D6 either/or DECIDED: RELOCATED to docs/design/drydocs-mapping-demo-runbook.md (joins the *-runbook.md validation sweep + the Epic L render pipeline; decision recorded in the doc's front-matter). Conforms to runbook.outline.yaml (doc_outline passes); all 8 required anchors authored; content preserved + refreshed (5-domain verify incl. the O24 seal-contact-override, stale feat/mapping-store branch ref dropped, rollback section = derived-file story); classification Internal-Public; the 2 referencing docs re-pointed + re-rendered; outline sweep 17 green.
    depends_on: [L8]
    inputs: [docs/runbook-mapping-demo.md, docs/design/templates/runbook.outline.yaml, docs/design/drydocs-startup-refresh-runbook.md, drydocs/doc_outline.py, scripts/render_design_doc.py]
    acceptance: >
      The mapping-demo runbook conforms to runbook.outline.yaml — python -m
      drydocs.doc_outline --outline docs/design/templates/runbook.outline.yaml
      --doc <the doc> passes — carrying the outline's front-matter + required
      anchors with the existing content (demo steps, steward flow) preserved
      under them. Whether it relocates to docs/design/ and renders through the
      Epic L pipeline is decided at execution (the D6 either/or idiom) and
      recorded in the doc header; classification set; doc tests + full suite
      green.
    notes: >
      Groomed 2026-07-20 from the 2026-07-18 inbox line (runbook authored
      FREE-FORM in the mapping-store session, pre-L8 by design). Trigger =
      L8 landing the runbook outline, fired 2026-07-20 (e6bcb24). Second
      exemplar beside the startup/refresh runbook — proves the outline
      generalizes beyond its authoring doc.
  - id: L15
    epic: doc-infrastructure
    title: "Canonical outline for the whole-project review doc (review.outline.yaml, 3rd doc type) + a recorded refresh cadence"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done                                     # 2026-07-21 pm — review.outline.yaml exists (drydocs.doc-outline.v1; 16 sections + 9 subsections, anchors derived from Rev 1's authored set — retrofit-free except ONE added front-matter anchor marker); drydocs-project-review.md conforms (doc_outline passes) and re-renders deterministically (1-line html delta = the anchor). CADENCE RECORDED in the outline header: Rev bump per EPIC CLOSE (epic completion is what changes the review's story — status/roadmap refresh minimum; no hot-editing facts between Revs). Exemplar-pin tests added (the third doc type through the same contract).
    depends_on: [L8]
    inputs: [docs/design/drydocs-project-review.md, docs/design/templates/tdd.outline.yaml, docs/design/templates/runbook.outline.yaml, drydocs/doc_outline.py]
    acceptance: >
      docs/design/templates/review.outline.yaml exists (schema
      drydocs.doc-outline.v1; stable anchors derived from the current review's
      sections); drydocs-project-review.md conforms (doc_outline validation
      passes) and re-renders deterministically; a refresh cadence is decided
      and recorded in the outline header (candidate: Rev bump per epic close —
      facts pinned to a commit go stale quietly); full suite green.
    notes: >
      Groomed 2026-07-20 from the 2026-07-14 inbox line; trigger = L8
      introducing the second doc type, fired 2026-07-20. Third canonical type
      after TDD (L1) and Runbook (L8); the BRD outline stays parked (upstream
      WIP, user call). p3 — completeness/traceability hygiene, nothing
      downstream blocks on it.

  - id: L16
    epic: doc-infrastructure
    title: "Refresh the startup/refresh runbook's container name + ports to match the neo4jtest recreation (neo4j-drydocs-ee/7476/7689 -> neo4jtest/7474/7687)"
    type: chore
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [docs/design/drydocs-startup-refresh-runbook.md, scripts/render_design_doc.py]
    acceptance: >
      docs/design/drydocs-startup-refresh-runbook.md's container table and
      start commands reference container `neo4jtest` on ports 7474/7687 (not
      `neo4j-drydocs-ee`/7476/7689); the doc's Rev is bumped with a change
      note per its own cadence; re-rendered via scripts/render_design_doc.py
      (deterministic — git diff clean after regen); doc_outline validation +
      full suite green.
    notes: >
      Groomed 2026-07-23 from the IDEAS.md Neo4j-container-recreation entry
      (2026-07-23): the container migration itself is already done (data
      moved to named volume neo4j-testdata, canonical container `neo4jtest`
      on default ports; local .env/agents/.env/web/.env.local/MCP config
      re-pointed 7689->7687) — this item is documentation-only catch-up
      through the governed doc pipeline (CLAUDE.md L13 idiom: one .html,
      deterministic render, no hand-restyling).
      DONE 2026-07-28: Rev 2 -> Rev 3. Container/ports fixed in prerequisite 1,
      the NEO4J_URI example, Startup step 1, Rollback step 3, Appendix A and
      Appendix B. Appendix A is now stated as a RENDER of
      config/dev-environment.yaml rather than an independently-maintained
      table — the drift happened because the doc kept its own copy of facts
      that file already owns. Render proven deterministic (rendered twice,
      byte-identical). test_doc_traceability_loader.py pins the parsed rev, so
      its expected value moved with the bump.

  - id: L17
    epic: doc-infrastructure
    title: "doc_traceability + doc_feedback load 'successfully' with every anchor link silently dropped when their cross-loader prereqs are absent — add the Q8 loud-refusal guards"
    type: bug
    module: drydocs-load
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/loaders/doc_traceability.py, drydocs/loaders/cypher/doc_traceability.cypher, drydocs/loaders/cypher/doc_feedback.cypher, drydocs/loaders/batch_port_orchestrator.py]
    acceptance: >
      DocTraceabilityLoader and DocFeedbackLoader (today bare class bodies,
      doc_traceability.py:376,383 — no prereq check) refuse loudly BEFORE
      writing when a cross-loader prerequisite set is absent, per the Q8 /
      batch_port_orchestrator precedent: doc_traceability.cypher:69 and
      doc_feedback.cypher:37 both OPTIONAL MATCH (:DocSection) written by a
      DIFFERENT loader (doc_sections.v1), and doc_feedback.cypher:49 does the
      same for (:Employee) — an absent set currently drops every anchor link
      and still reports OK. After the guard: absent prereq -> loud refusal
      (exit 2, nothing written); present prereq -> per-row unmatched anchors
      COUNTED in the load report, never silent. Tests pin both paths for both
      loaders. Full suite green.
    notes: >
      Groomed 2026-07-28 from the 2026-07-27 sweep that fixed
      batch_port_orchestrator (same defect family, instances five-through-
      seven of the "succeeds loudly, does nothing" through-line: G29, G30,
      Q8, both batch_port halves, these two). doc_feedback matters MOST: it
      IS the L5/L6 re-attachment loop, so its failure mode is SME feedback
      loading "successfully" while silently detached from the doc it
      annotates. Cleared as NOT defective by the same sweep: the prev:Chunk
      self-references (bmc_docs.cypher:94, essential_graphrag.cypher:80,
      same-loader same-run) and pat_product_mapping.cypher:129,133
      (stale-edge cleanup, not prereq joins).
      CLOSED 2026-07-28: _SectionPrereqLoader shared guard (refusal before
      _open_run, so a refused load leaves no :JobRun; probes exclude
      :SchemaMeta exemplars per the O33 write-side clause) + per-row
      unmatched-anchor reporting on both loaders; DocFeedbackLoader
      additionally refuses an AUTHORED batch against an empty :Employee
      registry (author-less batches load fine — gate C1 keeps attribution
      optional) and reports unknown authors. CLI load-doc-traceability:
      refusal -> red + exit 2; per-row misses -> yellow report. Both cypher
      headers now point at the enforcing loader methods (the batch_port
      header convention, test-pinned). 9 guard tests, no Neo4j.

  # Groomed 2026-07-08 from the 2026-07-05 IDEAS line + the 2026-07-07 property-terms
  # line. Doc 06 Phases 0-1 (Control-M envelope: audit-fields.yaml + jobs/folders
  # extraction) shipped 2026-07-07 PRE-groom (gate controlm-q1q3-phase1) — recorded
  # here for traceability, not re-itemized. M1/M2 = doc 06 Phases 2/3; M3 = Phase 4
  # (+ Phase 5 docs folded in); M4 = the property-term bindings.
  - id: M1
    epic: provenance-audit
    title: "Provenance-edge diet: WAS_GENERATED_BY only on create/change (delta detection)"
    type: task
    module: drydocs-load
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/restructure/06-provenance-source-audit-fields.md, drydocs/loaders/, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: >
      Loaders detect record change (checksum or version compare, e.g. VERSION_SERIAL) and
      write WAS_GENERATED_BY -> :JobRun only on create/actual change; full-refresh runs
      record counts on the :JobRun node instead of blanket membership edges; the
      prov_was_generated_by vocabulary note is updated via the gate; offline unit tests
      prove no-change rows write no provenance edge. Full suite green.
    notes: >
      Doc 06 Phase 2 — the fix for the full-refresh supernode (persona review Issue 3).
      Done 2026-07-08 (sonnet agent): sha256 row_checksum in to_params (volatile fields
      excluded: capture_date + batch/bookkeeping params); WAS_GENERATED_BY guarded by
      FOREACH-over-CASE on checksum change in all 4 node-writing cyphers
      (dependencies_derived never had the tail); JobRun.rows_changed derived from the
      actual edge count at _close_run; prov_was_generated_by note updated (note-only);
      graph-tests/provenance-diet.yaml live suite. 40 new tests.

  - id: M2
    epic: provenance-audit
    title: "Migrate existing graphs: delete blanket WAS_GENERATED_BY edges, backfill envelope"
    type: task
    module: drydocs-load
    phase: 11
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-21 pm — migration built + HITL-confirmed + RUN live: 0 blanket edges (sandbox reloaded post-M1; step protects older graphs + company copy), 8 raw-prop retirements, 816 created_at->first_seen_at renames (snapshot labels excluded); idempotent re-run 0 effects; m3-verify +3 Phase-3 invariants all green; 20 loader cyphers standardized; suite 844 green
    depends_on: [M1]
    inputs: [drydocs/migrations/, docs/restructure/06-provenance-source-audit-fields.md]
    acceptance: >
      A drydocs/migrations/ file (pattern: the ControlMFolder rename) batch-deletes
      blanket WAS_GENERATED_BY edges on existing sandbox graphs — destructive, so
      HITL-confirmed before running; envelope properties backfilled where recoverable;
      m3-verify updated; the raw-named folder props (last_updated/last_updated_user)
      retired in favor of the envelope pair. Migration idempotent; suite green.
    notes: "Doc 06 Phase 3. Also absorbs the first_seen_at naming standardization deferred from Phase 1."

  - id: M3
    epic: provenance-audit
    title: "Audit envelope for the remaining sources (SEAL, catalog) via per-source gates"
    type: requirement
    module: config
    phase: 11
    agent: pipeline-config
    model: sonnet
    priority: p3
    status: done   # 2026-08-04 (desktop): gate audit-envelope-phase4 SIGNED OFF 13/13 in-chat, same session as the claim
    depends_on: []
    inputs: [config/audit-fields.yaml, config/source-registry.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      The seal-extract and catalog-pat audit-fields stubs are resolved through per-source
      HITL gates (real column mappings authored in the internal twin — Internal, and
      confidential-handling, so never producer-side; producer entries flip stub -> confirmed with gate ids);
      their loaders SET the envelope; software-registry's stub gets an explicit decision
      (git history as audit trail). Doc 06 Phase 5 docs (loader README, port-prompt/
      git-readme disposition row) land with it. test_audit_fields green both sides.
    notes: >
      Doc 06 Phase 4 + 5. Gates are per-source pauses — sequence producer-first per
      port-prompt step 15. CLOSED 2026-08-04 (desktop): gate audit-envelope-phase4
      SIGNED OFF 13/13 in-chat. The acceptance was OVERTAKEN BY EVIDENCE on both of
      its premises, recorded rather than papered over. (1) "flip stub -> confirmed":
      the SME evidence REVERSED the page's B1 recommendation — the SEAL registry's
      date fields are onboarding-LIFECYCLE milestones with two-era capture (legacy
      applications lack them; three live examples shown in-session), so creation_date
      is not record authorship and NO honest mapping exists to confirm; all four
      entries are RULED stubs with the why and the revisit trigger in their notes
      (SEAL's trigger: the registry UI exposes a per-application audit download —
      a true record-audit trail exists source-side, unextracted). "Resolved through
      per-source gates" is met; "confirmed" was never available. (2) the
      internal-twin clause was discharged as not-needed (gate §A2): the field
      inventories were already committed Internal-Public as loader-field mechanism
      by the S3 §B1(c) ledger and the catalog row models. PAT pair =
      stub-until-projected (zero audit columns in the report extracts; the C17
      lesson keeps it non-permanent; valid_from/to pinned as validity-never-
      authorship); software-registry = ruled PERMANENT stub closing the
      repo-committed trio. No loader change (the B2 branch). Phase 5: loader README
      envelope section + doc 06 phase marks; the port "disposition row" is
      superseded by PORT-MANIFEST config/** canonical-producer (recorded in doc 06).
      Controlm-family stubs stay open as the Phase-4 residual. test_audit_fields
      green producer-side; company runs its own gate per the two-tier doctrine.

  - id: M4
    epic: provenance-audit
    title: "Property-level ontology terms for the audit envelope (dct:/prov:, via the gate)"
    type: task
    module: ontology
    phase: 11
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done   # 2026-08-04 (desktop): gate envelope-property-terms SIGNED OFF 10/10 in-chat, same session as M3
    depends_on: []
    inputs: [config/audit-fields.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, reference/standards/]
    acceptance: >
      The four envelope properties carry standard-term bindings — dct:creator/dct:created/
      dct:modified or PROV qualified attribution — decided via the HITL gate, including
      WHERE property-term bindings live (own registry vs a vocabulary-file section:
      relationship_vocabulary.yaml covers edges only today). SOSA explicitly ruled out in
      the record (authorship provenance, not observation). Drift guards green.
    notes: >
      Groomed 2026-07-08 from the 2026-07-07 IDEAS line (raised while answering
      'where are the SOSA mappings for created by?'). CLOSED 2026-08-04 (desktop):
      gate envelope-property-terms SIGNED OFF 10/10 in-chat (same session as M3).
      Rulings: the dct trio (creator/created/modified) + the contested updated_by
      row ruled dct:contributor with the imprecision RECORDED in the entry note
      (DCMI defines no 'modifier') over the prov qualified-attribution alternative;
      the WHERE question ruled C1 — a new property_terms section (0b) in
      relationship_vocabulary.yaml, on the node_classifications precedent that the
      file is the ontology registry, not literally a relationship list. SOSA ruled
      OUT in the record (§A2). Build: dct: registered in namespaces.py + the
      ontology.cypher header sync (documentation-grade — no dct: OntologyTerm nodes
      seeded); reference/standards/dcmi-terms/ stub + REGISTRY.yaml row + standards
      README table row; three drift guards in test_audit_fields.py (every envelope
      property bound, every CURIE expands via namespaces.expand() with a gate id,
      the four rulings pinned). Found in passing + inboxed: REGISTRY.yaml has cited
      reference/standards/dcat/README.md since before this item — the file does not
      exist.

  # ── Epic N — Source column mappings (doc 08) — Phase 11 ────────────────────
  # Groomed 2026-07-08 from the 2026-07-07 IDEAS line; plan = docs/restructure/
  # 08-source-column-mappings.md (Phases 0-1 promoted per the plan's own note;
  # profile integration / render / ODCS export stay in the plan doc until these land).
  - id: N1
    epic: source-column-mappings
    title: "Column-ledger schema + accessor + the Control-M ledger (transcription, no gate)"
    type: task
    module: config
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/restructure/08-source-column-mappings.md, drydocs/loaders/sql/, config/gate-prompts/controlm-q1q3-phase1.yaml, docs/restructure/06a-controlm-source-er-review.md]
    acceptance: >
      drydocs.source-mapping.v1 schema + typed accessor (pattern: review_labels.py) +
      unit tests; config/source-mappings/controlm-psgmgr.yaml seeded by TRANSCRIBING
      already-gated decisions (q1q3 envelope, employee_sid, MEMNAME demotion, filter
      columns, phase-1 scope sweep) + the SQL headers' scope rules — no new gate needed;
      dispositions projected/filter-only/excluded/deferred with origin source|derived;
      classification Internal-Public, mechanism-only. Full suite green.
    notes: >
      Doc 08 Phase 0. Done 2026-07-08 (sonnet agent): drydocs/source_mappings.py
      (review group per the default-deny guard; MODULE_MAP row) +
      config/source-mappings/controlm-psgmgr.yaml (5 objects, 69 projected rows, census
      pending). Honest deviation from the plan sketch, ledger matches the REAL SQL:
      IS_CURRENT_VERSION/USER_DAILY are projected (they are SELECTed, not filter-only),
      with filter roles in notes; LNKI/LNKO IS_CURRENT_VERSION targets
      staging:is_current_version (never SET on the graph); bind params + the var_scope
      CASE documented as notes, not fake column rows. 27 new tests.

  - id: N2
    epic: source-column-mappings
    title: "Column-ledger drift guards: coverage vs profile census + SQL SELECT-list parse + registry pointer"
    type: task
    module: config
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [N1]
    inputs: [config/source-mappings/, drydocs/loaders/sql/, config/source-registry.yaml, drydocs_lineage/extractors/controlm_inventory.py]
    acceptance: >
      Per doc 08 Phase 1: coverage test (every profiled column accounted for; new columns
      since last census must be dispositioned by name, never swept); SQL drift guard
      (loader SELECT ... AS list set-equals the ledger's projected columns; strict regex
      first, sqlglot only if it lies); source-registry entries gain a mapping: pointer and
      a test requires a ledger for every confirmed source. The guard ALSO covers the
      lineage extractor's CSV column contract — the header names
      drydocs_lineage/extractors/controlm_inventory.py expects must set-match the
      controlm_jobs.sql aliases (today they are duplicated as strings and a renamed alias
      fails by silent drop). Full suite green.
    notes: >
      Doc 08 Phase 1. Profile-census refresh via the db skill is doc-08 Phase 2 (not this
      item). MERGED 2026-07-11: G9 tech-debt finding #2 (extractor CSV-contract drift) —
      same guard, one more consumer of the same SELECT list. DONE 2026-07-11:
      tests/unit/test_source_mapping_drift.py — (1) SQL drift guard: strict paren-aware
      item grammar (QUAL.COL AS alias source items; expression items classify derived,
      internals deferred like WHERE parsing); per-object UNION across loader SQLs
      set-equals the ledger's projected columns (holds exactly on the real ledger —
      CM_DEF_VJOB spans jobs+variables SQLs; folders' H alias = the header-row join);
      (2) census reconciliation via new census_failures() accessor method (pending -> [],
      recorded census must balance explicit + sweep count:, so new columns disposition
      BY NAME); (3) registry test: every confirmed source has a locator.mapping ledger
      or sits on the frozen shrink-only LEDGER_PENDING list (seal/catalog/software-
      registry/bmc-docs — pre-doc-08 legacy, named debt); (4) the merged extractor
      CSV contract: CSV_CONTRACT constant AST-matched to the code's row.get() keys and
      subset-checked against controlm_jobs.sql aliases.

  # Groomed 2026-07-28 pm from the in-session "one document with loaders, order,
  # commands, source→target mapping?" scope. Answer was NO — it is split across
  # internal/repo-README.md (CLI reference + Control-M run order), the startup/refresh
  # runbook (operational chain), 04-sme-checklist-and-load-plan.md (sequential plan)
  # and config/source-mappings/*.yaml (the column ledgers). N3-N6 build it as a
  # RENDER rather than a fourth hand-written doc — hand-authoring it would create
  # exactly the drift this session fixed twice (README scan roots, provision.ps1).
  - id: N3
    epic: source-column-mappings
    title: "Declare the three joins a load map needs: loader->source_id, command->loaders, and the canonical load sequence"
    type: task
    module: drydocs-load
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/loaders/base.py, drydocs/loaders/, drydocs/cli.py, config/source-registry.yaml, scripts/ingest.sh]
    acceptance: >
      The three facts a load map is built from become machine-readable, because none of
      them are today. (1) Every BaseLoader subclass declares source_id resolving to a
      config/source-registry.yaml id — BaseLoader carries name (e.g. controlm_jobs.v1)
      and source_label ('csv'|'oracle'|'agent'|'human') but NOTHING tying a loader to its
      registered source, so loader->source->column-ledger cannot be traversed at all.
      (2) Each Typer command that runs loaders declares which ones, in order (today it is
      only in the function body). (3) ONE canonical ordered load sequence is declared
      once. Guards: every declared source_id resolves in the registry; every sequence step
      is a real registered command; every loader is either declared or explicitly exempt
      (base/abstract) — no silent omissions.
    notes: >
      Counts at groom time: 25 BaseLoader subclasses across 21 modules, 32 registered
      Typer commands, 16 registry sources. Module drydocs-load (the declarations live on
      loader classes) rather than config like N1/N2 — the ledgers are config, the joins
      are code. The guard catches an UNRESOLVABLE source_id, not a WRONG one, so assign
      each from the loader's own SQL/adapter and review in one pass. Startable now:
      N1/N2 are done.
      DONE 2026-07-29: (1) BaseLoader.source_id ClassVar declared on all 25 concrete
      loaders, assigned from each loader's own gate/adapter (batch_port_orchestrator ->
      seal-extract per its taxonomy feed; seal_attribution -> stg-app-fact; code_snapshot
      -> depgraph-snapshot); ManualSealAttributionLoader is the ONE named exemption
      (SOURCELESS_LOADERS with written reason — manifest-gated human source, not a
      registry feed; honest deviation from the "exempt = base/abstract only" wording).
      (2) refresh-reference / ingest-controlm / doc-traceability chains lifted to
      module-level constants the command bodies CONSUME (declaration == behavior);
      COMMAND_LOADERS derives from them; every dedicated command's _gate_source literal
      now reads cls.source_id; LOADER_SOURCE is a projection of the classes, never
      hand-kept. (3) CANONICAL_LOAD_SEQUENCE (14 steps, mode standing|optional|gated
      with notes). Guards: tests/unit/test_load_map_declarations.py (11 tests —
      resolution, exemption reasons, projection tie, chain/registry binding, command
      reality, reachability/no-orphans, sequence validity); the old text-scan chain-order
      test rewritten against the declarations. Suite 1155 passed / 5 skipped.
      N4 same session: scripts/render_load_map.py -> web/src/generated/load-map.json.
  - id: N4
    epic: source-column-mappings
    title: "Render the load map: one generated artifact joining sources, loaders, commands, order and column ledgers"
    type: task
    module: drydocs-docgen
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [N3]
    inputs: [config/source-registry.yaml, config/source-mappings/, drydocs/source_mappings.py, scripts/render_gates.py, scripts/render_enforcement_matrix.py]
    acceptance: >
      A scripts/render_*.py emits web/src/generated/load-map.json joining the N3
      declarations to the registry and the column ledgers, plus a committed==regenerated
      guard (the gates.json / enforcement-matrix.json precedent, and J17's lesson: wire it
      into the default board render so a groom cannot leave it stale). Ledger coverage is
      rendered in the THREE states governance already defines, never omitted: has a ledger
      (3 — controlm-psgmgr, depgraph-snapshot, design-docs), confirmed but on the frozen
      shrink-only LEDGER_PENDING list (8), unconfirmed placeholder (5). A source that is
      silently absent from the render is a defect. WIDENED 2026-07-29: the per-source row
      also joins the source's taxonomy capture (its config/taxonomy/ file, when one
      exists) and its config/taxonomy-ontology-map.yaml entries with their status
      (proposed/planned/confirmed) — so the one surface answers taxonomy-by-source,
      ontology, extract AND load, not just the load half.
    notes: >
      Deliberately reuses existing governance instead of inventing a "no ledger yet"
      concept: LEDGER_PENDING and test_every_confirmed_source_has_a_ledger_or_is_named_pending
      (N2) already carry that state, and the list is frozen shrink-only, so the render
      inherits the guarantee. Module drydocs-docgen — it is a renderer of the L3 class,
      even though its inputs are config. MERGE 2026-07-29: the user restated the ask as
      "one view of all of the taxonomy by source, ontology, extract and loads in one
      place" — the 2026-07-28 groom had scoped only loaders/order/commands/ledgers, so
      the taxonomy + ontology-map joins were added here rather than as a new item. The
      map file and taxonomy captures are already status-bearing YAML; no gate is implied
      (the render displays statuses, it never rules them).
      DONE 2026-07-29 (same session as N3): scripts/render_load_map.py emits
      web/src/generated/load-map.json — 18 sources in registry order (4 with ledgers /
      8 pending / 6 placeholders — counts grew past the groom's 3/8/5 as the registry
      did; states DERIVED from locator.mapping + confirmed, so the render tracks the
      registry by construction), each row joining ledger state, taxonomy captures
      (their own top-level source: key), taxonomy-ontology-map entries with status,
      loaders (from N3 source_id) and the commands that run them; the 14-step sequence
      + ad-hoc commands + the named sourceless exemption ride along. Wired into the
      default-paths render_board.py run (now board + gates + matrix + load-map — one
      entry point). Guard tests/unit/test_load_map_json.py: committed==regenerated,
      every registered source has a row in order, three governed ledger states only,
      every concrete loader appears exactly once, sequence mirrors the declaration.
      BONUS FINDING the surface caught on day one: 4 map entries cite source ids that
      are NOT registry entries (controlm-variable-normalization, seal-pat-scrape,
      design-doc-outline, outlook-dl) — rendered under
      map_entries_without_registry_source, never dropped; registering them (or
      re-pointing the entries) is follow-up grooming. Suite 1160 passed / 5 skipped.
  - id: N5
    epic: source-column-mappings
    title: "The human load-map surface: a readable page generated from load-map.json, wired into the session ritual"
    type: task
    module: drydocs-docgen
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [N4]
    inputs: [web/src/generated/load-map.json, scripts/render_design_doc.py, knowledge/depgraph-snapshots/snapshot.ps1]
    acceptance: >
      A readable rendered page (screen + print, PDF-able for the SME loop) generated from
      load-map.json and refreshed by the session ritual alongside the board and design
      docs; re-render leaves a clean tree. It does NOT live under docs/design/ as a
      generated .md: that tree's contract is hand-authored .md as the single source of
      truth, validated against the outline templates, so a generated file there would
      break both the contract and test_doc_outline. Either a separate home or an explicit
      generated-doc carve-out — decide and record which.
    notes: >
      The placement question is the real content of this item; the rendering is
      mechanical. Answers the original ask (one surface with loaders, order, commands and
      source->target mapping) and supersedes reading three docs plus three YAML ledgers.
      DONE 2026-07-29 (same session as N3/N4). PLACEMENT DECIDED: docs/plan/load-map.html
      — docs/plan/ is the committed generated-surface home (board.html is the precedent);
      docs/design/ rejected because that tree's contract is hand-authored .md validated
      against outline templates (a generated file there breaks the contract and
      test_doc_outline). Recorded in the renderer docstring. Screen + @media print in the
      board.html house style, generated from the same build_load_map() data as the json
      (one joiner, two surfaces); rides the default-paths render_board.py run, so the
      session ritual refreshes it with the board (CLAUDE.md ritual text updated);
      committed==regenerated guard added to test_load_map_json.py; page verified in
      headless Edge (screenshot in session record). Faithful-render note: the 4th
      "ledger" is snowflake-data-catalog, whose registry locator.mapping points at its
      plan .md — displayed as committed, not reinterpreted.
  - id: N6
    epic: source-column-mappings
    title: "Retire the two disagreeing load sequences: ingest.sh and the startup runbook both derive from the declared one"
    type: chore
    module: docs
    phase: 11
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-04 (laptop). THE RULING FIRST, since the item existed to make one: ingest.sh's shorter list is DELIBERATE, and the four standing steps it skips now carry written reasons in cli.SCHEDULED_INGEST_EXCLUSIONS — refresh-reference is a weekly chain on a different cadence (declared on the command itself), load-software-registry and load-bmc-docs are repo-triggered corpora that move with the repo rather than the batch estate, and docs-verify would FAIL that path by design (it loads no doc corpora, and under `set -e` a non-zero exit would abort a Control-M ingest over a reconciliation never meant to hold there). The runbook's omission was NOT deliberate: docs-verify is standing and a cold start is exactly when the corpora load, so Appendix B gains it. MECHANISM: CANONICAL_LOAD_SEQUENCE steps widened from 3-tuples to a named LoadStep carrying `profiles`, plus LOAD_PROFILES (scheduled-ingest = ingest.sh, cold-start = Appendix B) and load_profile(). scripts/ingest.sh now READS its profile at run time and has no list left to drift — verified by stubbing the CLI: it reproduces the previous 7 steps in the previous order, and argument forwarding still reaches ingest-controlm alone. Appendix B is prose and can derive nothing, so tests/unit/test_load_sequence_surfaces.py (11 tests, the deliverable half of the acceptance) holds it to the declaration. THE GUARD GAP the item also named is closed by DIRECTION rather than by patching: the old completeness check walks COMMAND_LOADERS and so only ever reaches loader-backed verbs; the new one starts at the operator surfaces and works inward, so a non-loader verb an operator really runs (bootstrap-schema-graph, the exhibit) cannot go undeclared. Negative-tested with the anchor asserted first (the 2026-08-04 silent-no-op lesson): dropping docs-verify from Appendix B fails, and a literal verb grown back into ingest.sh fails. TWO THINGS FOUND ALONG THE WAY, both fixed here. (1) test_runbook_currency.py::_cli_verbs had NEVER run successfully on this machine — `text=True` decodes with cp1252 and Typer's rich box char `┐` is 0x90, so stdout came back None; and the rows start with `│`, not `|`, so the pattern matched nothing, which would have made every documented verb look unregistered. Now reads app.registered_commands: no render to parse. (2) drydocs-load-runbook.md (V3, hours old) told readers to run a one-liner that the widening broke, and still said N6 had not landed — Rev 2. GOVERNED RENDER: startup-refresh runbook Rev 10 with a change note, its pinned rev in test_doc_traceability_loader.py bumped 9 -> 10. Full suite 1551 passed / 7 skipped, ruff clean, all renders deterministic.
    depends_on: [N3]
    inputs: [scripts/ingest.sh, docs/design/drydocs-startup-refresh-runbook.md, drydocs/cli.py]
    acceptance: >
      scripts/ingest.sh and the startup/refresh runbook stop carrying independent
      sequences and derive from the one declared at N3 (cli.CANONICAL_LOAD_SEQUENCE) —
      a guard proves they agree, and the guard is as much the deliverable as the
      derivation is.
      STATE RE-MEASURED 2026-08-04. The original text described a pre-L21 world and was
      wrong twice over: the runbook no longer "expands the supplements into three
      separate commands" (L21 / Rev 5 collapsed that to apply-supplements), and its
      ingest.sh enumeration omitted bootstrap-schema-graph, which ingest.sh does run.
      Measured today:
        DECLARATION (standing): check, bootstrap, bootstrap-schema-graph,
          apply-supplements, refresh-reference, ingest-controlm, load-software-registry,
          load-bmc-docs, m1-verify, m3-verify, docs-verify
        INGEST.SH: the first four, then ingest-controlm, m1-verify, m3-verify — omits
          refresh-reference, load-software-registry, load-bmc-docs, docs-verify
        RUNBOOK APPENDIX B: the declaration's standing set plus load-doc-traceability
          (legitimate — it is `optional` in the declaration), MINUS docs-verify
      The disagreement is therefore narrow and specific rather than broad: the runbook is
      one standing step short, and ingest.sh omits four. Whether ingest.sh's omissions are
      DELIBERATE — a scheduled Control-M ingest arguably should not run the corpora
      loaders — is the ruling this item must make and RECORD. Nothing states it today, so
      a deliberate subset is indistinguishable from drift, which is the actual defect
      rather than the step counts.
      Also in scope, because it is why the drift went unseen: the existing guard
      (tests/unit/test_load_map_declarations.py) checks that every DECLARED step is a real
      command and that every LOADER-BACKED command is sequenced — but the completeness
      half never reaches non-loader verbs, so bootstrap-schema-graph sat missing from the
      declaration while both operator surfaces ran it (found and fixed 2026-08-04).
    notes: >
      p3 and last because it is the only phase that changes behaviour rather than adding
      a surface, and it touches a GOVERNED render (the runbook — Rev bump + change note +
      pinned rev in test_doc_traceability_loader.py). Without it the load map is accurate
      the day it ships and drifts the first time either sequence is edited, which is the
      failure mode N3-N5 exist to end.
      SEQUENCING NOTE (2026-07-29, at the N3-N5 close): N3 is done, so this is next_ready
      and the declared sequence it derives from EXISTS (cli.CANONICAL_LOAD_SEQUENCE, on
      the human surface at docs/plan/load-map.html). BUNDLING NOTE SPENT (2026-08-04):
      L21 is DONE and the startup-refresh runbook has since reached Rev 8, so this item
      no longer rides another edit — it needs its own Rev bump and change note on that
      governed render. The original instruction was to bundle with L21 so the L5/L6 loop
      reviewed one Rev rather than two; that window has closed.

  # --- 2026-07-30 groom: source-registry v2 (user directive "the flat id does not
  # --- work") + the N4 render's day-one finding. N7 is the DESIGN SESSION bundling
  # --- the four coupled registry decisions the J21 close note parked together.
  - id: N7
    epic: source-column-mappings
    title: "Source-registry schema v2 design session: split source SYSTEM from extracted DATASET, ruling the per-side loader overlay, URN identity handle, and reconcile same-id guard together (HITL gate)"
    type: task
    module: config
    phase: 11
    agent: main
    model: fable
    priority: p1
    status: done
    depends_on: []
    inputs: [config/source-registry.yaml, drydocs_core/source_registry.py, config/taxonomy-ontology-map.yaml, PORT-MANIFEST.yaml, git-readme.md, internal/registry-redesign/REGISTRY-PLAN.md]
    acceptance: >
      A design doc + gate prompt exist covering the four coupled decisions as ONE
      design, and an SME gate session rules each (active, or explicitly deferred
      with reason): (1) two-level identity — source SYSTEM (connection/locator/
      classification carrier) vs extracted DATASET (gate/crosswalk/feeds_taxonomy/
      ontology-domain carrier, each with its OWN confirmed state), loaders binding
      to the DATASET, datasets keyed or tagged by ontology domain
      (DPROD | ORG | SWO | data-platform | code); (2) the per-side loader->source_id
      config overlay that wins over class defaults (the company T19 deferral's
      producer follow-up — adopt or rule against); (3) the URN cross-repo identity
      handle; (4) the reconcile same-id/changed-meaning guard. No registry schema,
      mapping, or loader-binding change lands before sign-off (J21's hardening of
      the CURRENT shape is the interim guarantee). The catalog-pat != pat-catalog
      id collision is recorded in the producer divergence ledger REGARDLESS of the
      ruling. Output feeds the company T19 gate review rather than racing it.
    notes: >
      Groomed 2026-07-30 from the user directive (chat, captured same day): the flat
      id conflates the source system with the extracted dataset — PAT yields both
      DPROD datasets and ORG datasets; Control-M yields multiple extraction domains —
      which is exactly how catalog-pat vs pat-catalog became same-string-different-
      meaning across repos (T19) and why feeds_taxonomy lists keep widening instead
      of splitting. Absorbs the 2026-07-29 N7-overlay inbox candidate (this id was
      reserved for it; the directive bundled it into the one design session).
      Fable: registry schema v2 is a schema/boundary decision. Company blockers
      informing the design: 8 company loaders gate on pat-catalog while the producer
      class says catalog-pat (value-level guard gap — a wrong-but-resolving source_id
      passes), and ~13 company-only loaders carry no source_id. Direction, NOT
      decided — everything routes through the gate. 2026-07-31 the user expanded
      the design input (chat + saved samples): THREE distinctions — software we
      use/trace (software-registry.yaml, not a source) vs ingested data sources
      (system + dataset two-level) vs REPLICA datasets whose id combines
      origin@carrier:artifact (SEAL loaded into psgmgr = seal@psgmgr:CM_ESCALATION_DB);
      documents keep their own ledger; dataset rows adopt the FCDO Data Authority
      SOR/ADS vocabulary as a field; classification simplifies to 3 levels for now
      (External / Internal-Public / Internal). Plan of record + target-column
      worksheet + SNOW-CMDB / DataHub / SDLC-metamodel samples:
      internal/registry-redesign/ (REGISTRY-PLAN.md; Phase 0 re-home done, Phase 1
      classification collapse may land pre-gate, Phases 2-3 = this item's session
      and gate). CLOSED 2026-07-31: gate session RUN IN-CHAT same day — D1-D4 and
      Q1-Q6 ALL RULED (gate-log entry source-registry-v2; prompt
      config/gate-prompts/source-registry-v2.yaml authored from the session record).
      SME amendments: D1 seal_id = standing placeholder field (real value
      internal-twin only); Q1 grammar = {origin}@{db}.{schema}.{table} — the
      qualified locator REPLACES the informal nickname, all dots after @, lowercase,
      born-here datasets keep {system}:{artifact}. Q2 URN env always prod; Q3
      derived: true + authority omitted (FCDO SOC checked, does not fit); Q4 snow
      registers now as a SaaS system; Q5 design-docs pipeline-side only; Q6
      signed-off gates TRANSFER across renames. T19 naming note recorded in the
      port-prompt divergence ledger: replacement name = pat:product-catalog +
      pat:people-report (matches neither legacy string). RESIDUAL explicitly
      deferred: the 18-row migration table confirms per-row at the N9 BUILD
      (previously signed rows transfer per Q6; rest land confirmed: false).
      Divergence-ledger recording done; output feeds company T19, not a port.
  - id: N8
    epic: source-column-mappings
    title: "Resolve the 4 taxonomy-ontology-map entries citing unregistered source ids (N4 render day-one finding): register, re-point, or exempt — ruled per entry"
    type: chore
    module: config
    phase: 11
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [config/taxonomy-ontology-map.yaml, config/source-registry.yaml, scripts/render_load_map.py, web/src/generated/load-map.json]
    acceptance: >
      After the fix, the regenerated load-map.json's map_entries_without_registry_source
      is empty, or contains ONLY entries carrying an explicit recorded exemption the
      render displays (the SOURCELESS_LOADERS written-reason idiom). Each of the four
      is ruled per entry, never swept: job-seal-app-ref -> controlm-variable-normalization,
      seal-doc-source-of-record -> seal-pat-scrape, doc-traceability-feedback ->
      design-doc-outline (each: register the feed — lightweight registry stub or
      add-source-object walkthrough — OR re-point taxonomy.source at the registered
      feed it actually rides); dl-contact-point -> outlook-dl (expected outcome:
      explicit exemption — no feed by design per the DL gate's store-as-source
      discussion). Full suite green; board/load-map re-rendered clean.
    notes: >
      Groomed 2026-07-30 from the 2026-07-29 [chore] inbox line. COORDINATE with N7:
      anything registered here is current-shape (v1) and gets re-mapped by the
      redesign; J21's pk/binding guards cover the interim, so closing the render
      defect now is safe. Independent of N7 by design — the defect is live on the
      committed load-map surface today.
    progress: 'DONE 2026-08-06 (desktop): all four ruled per entry, exactly the acceptance''s
      prescriptions. job-seal-app-ref RE-POINTED to controlm@[db].drydocs_stg.stg_app_fact (its
      own source_evidence already named STG_APP_FACT as the ridden feed; the normalization
      stream is that dataset''s producer, not a source id). doc-traceability-feedback
      RE-POINTED to repo:design-docs (whose registry entry names this very mapping as its
      gate_spec). seal-doc-source-of-record: seal-pat-scrape REGISTERED as a lightweight
      doc-source-registry stub (Internal / web / T2 sme-confirm / dddocs / confirmed: false -
      the docmeta component gate stays the activation gate; the stub only closes the
      forward-reference). dl-contact-point: source_exemption RECORDED on the map entry (no
      feed by design - Outlook-only definition, store-as-source posture) and the render now
      DISPLAYS exemptions: map_entries_without_registry_source rows carry the reason, the
      html splits ruled-exempt from drift, and the warn box only shows unruled rows.
      Regenerated load-map: the defect list holds ONLY the exempt dl-contact-point row; the
      three re-pointed mappings now ride their sources'' ontology_mappings (35 sources, +1 =
      the stub). Suite 1636 passed / 5 skipped; board/load-map re-rendered clean.'
  - id: N9
    epic: source-column-mappings
    title: "Source-registry schema v2 BUILD: system/dataset rows, qualified replica ids, overlay seam, retired-id refusal, URN render — migrating all 18 entries with per-row confirmation"
    type: task
    module: config
    phase: 11
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: [internal/registry-redesign/REGISTRY-PLAN.md, config/gate-prompts/source-registry-v2.yaml, config/gate-log.md, config/source-registry.yaml, drydocs_core/source_registry.py, config/doc-source-registry.yaml, scripts/render_load_map.py]
    acceptance: >
      Implements the N7 gate rulings exactly (gate-log source-registry-v2,
      2026-07-31) — no re-deciding: (1) SourceRegistry schema v2 with SYSTEM rows
      (id/name/seal_id PLACEHOLDER (real values internal-twin only)/cmdb_ci/
      layer(BDAT four-value)/classification/locator/sdlc) and DATASET rows
      (id/system/origin/artifact+kind/taxonomy_category/asset_type/authority
      SOR|ADS/feeds_taxonomy scoped per dataset/adapter/crosswalk/confirmed/urn);
      loaders bind to dataset ids. (2) Id grammar: replica/derived =
      {origin}@{db}.{schema}.{table} lowercase with [db].[schema] placeholders
      committed (real values internal twin); born-here = {system}:{artifact};
      derived stores carry derived: true with authority omitted. (3) The per-side
      loader->source_id config overlay winning over class defaults, guarded to
      resolve to registered dataset ids (extends the J21 agreement guard).
      (4) replaces: back-pointers + retired-id refusal list — from_yaml AND the
      overlay guard refuse every legacy flat id (catalog-pat, controlm-psgmgr,
      seal-extract, ... — the full v1 id set retires). (5) URN derived render
      urn:drydocs:dataset:({carrier-or-origin},{artifact},prod), lowercase, never
      hand-maintained. (6) Migration of all 18 rows per the plan table with
      PER-ROW confirmation (the N7 residual): previously signed gate rows
      transfer confirmed per Q6 with ONE gate-log amendment entry mapping
      old->new ids; everything else lands confirmed: false. Catalog family
      lands as pat:product-catalog + pat:people-report (the T19 naming ruling).
      snow system row (SaaS) + snow:cmdb-ci-classes dataset added. Doc twins
      (bmc-docs, essential-graphrag, fcdo-frameworks) drop to
      doc-source-registry only; design-docs stays pipeline-side only.
      (7) Renders (load-map/board/matrix/software-registry surfaces) + guards
      updated; full suite green.
    notes: >
      Groomed 2026-07-31 at the N7 close — the build the gate authorized. Sonnet:
      every decision is ruled and written (gate-log + plan RULED section); this is
      execution against a written acceptance. The per-row confirmation sweep is
      record-keeping against the plan table, not new gating — anything that turns
      out to CHANGE meaning (not just rename) stops and goes back to the SME (the
      D4 boundary). Company side: never port this — T19 rules their adoption.
      CLOSED 2026-07-31 same session (desktop): registry v2 SHIPPED — 15 systems
      (incl. snow SaaS per Q4) / 28 datasets / 17-id retired refusal list (both
      T19 legacy strings). SourceRegistry v2: doc-ledger union (twins dropped,
      confirmed moved to doc rows), retired refusal on parse+lookup+overlay,
      derived URN (hand-carried urn: refused), loader-source-overlay.yaml D2
      seam (shipped empty, guarded). 14 loader modules re-bound to dataset ids
      (pat_product_mapping + pat_team_roles -> pat:people-report); CLI gates
      overlay-aware (_gate_loader), ingest-controlm gates every stage dataset.
      Ledger renamed psgmgr.yaml (carrier scope); audit-fields re-keyed + 8 new
      stubs; taxonomy captures + ~30 ontology-map entries re-pointed (N8's 4
      unmatched preserved verbatim). Per-row sweep findings: cm_avg_run gate
      actually SIGNED 2026-07-14 (P2) — v1 umbrella note was stale ("AWAITING
      SME"), corrected; new rows seal@[db].psgmgr.cm_escalation_db + cm_hist_vw
      registered unconfirmed. Q6 amendment entry appended to gate-log
      (old->new map). Load-map v2 (systems + datasets + retired tables);
      enforcement matrix +1 surface (17). Suite 1204 passed / 5 skipped.
      No meaning changes arose — nothing went back to the SME.

  - id: N10
    epic: source-column-mappings
    title: "Draft the registry wired/ready gate prompt: split pipeline-wiring readiness out of the semantic confirmed flag"
    type: task
    module: config
    phase: 11
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - config/source-registry.yaml
      - docs/port-prompt.md
    acceptance: >
      A gate prompt is drafted (G27/W1/U11 precedent — drafting decides nothing)
      proposing a separate wired/ready field on registry entries so semantic
      confirmation (gate-signed, transfers per Q6) and pipeline-wiring readiness stop
      sharing one flag; the prompt cites the a14a8028 fix session's Q1-B ruling and
      the cm_hosts standing divergence (semantically confirmed yet false
      company-side because their P3 host stage is unwired) as the motivating case;
      the registry schema changes ONLY when the gate signs.
    notes: >
      Groomed 2026-08-07 from Idea-81. Fable: registry schema/meaning boundary.
      Epic N because the registry v2 schema is N7/N9's artifact; the company
      suggestion is endorsed at Q1-B but a schema field is gate territory.
  - id: N11
    epic: source-column-mappings
    title: "The load-sequence completeness guard is one-directional: a non-loader verb can go missing from the declaration and no test notices"
    type: bug
    module: docs
    phase: 11
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - tests/unit/test_load_map_declarations.py
      - drydocs/cli.py
      - scripts/ingest.sh
    acceptance: >
      A guard catches a command that the operator surfaces run but the declaration omits
      — the case that actually happened: bootstrap-schema-graph was in scripts/ingest.sh
      AND the startup runbook's Appendix B and missing ONLY from
      cli.CANONICAL_LOAD_SEQUENCE, so the generated load map published 15 steps while both
      real paths ran 16, and no test could see it because the completeness check only
      reaches LOADER-backed commands. The new guard reasons from the operator surfaces (or
      their declared derivation), so a schema-level verb absent from the declaration fails
      it; the existing loader-side checks are left alone.
    notes: >
      Groomed 2026-08-07 (pm) from the still-open half of Idea-51. N6 closed the
      divergence itself on 2026-08-04 — ingest.sh's shorter list was ruled DELIBERATE and
      the skipped steps now carry written reasons — but N6 did not close why nothing
      caught the omission, which is this item. Epic N and module docs follow N6, its
      parent.
  # ── Epic O — Web console / graph visualization (main) — Phase 12 ───────────
  # Groomed 2026-07-09 (user decision: promote to a module + phase — the web/ front-end
  # design pass is in flight on branches). NEW module drydocs-web (JS/TS under web/, not a
  # Python package, so absent from MODULE_MAP.md/test_module_boundary.py). This is the
  # browser surface over the graph — distinct from every Python component.
  - id: O1
    epic: web-console
    title: "Web console design pass: wireframes, a real C4/graph rendering, and the browser↔Neo4j access decision"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: fable
    priority: p2
    status: done
    depends_on: []
    inputs: [web/, docs/decisions/]
    acceptance: >
      The web/ front-end leaves its throwaway-test-page state: wireframes committed; a
      decision recorded (ADR or design doc) on the browser↔Neo4j access path — bolt-from-
      browser vs a thin API — with rationale; the console renders a REAL C4 / graph view of
      the knowledge graph (NVL or an agraph-successor in web/, against the local EE DB)
      rather than the test page; drydocs-web's build/run is documented. No secrets in web/
      (publish boundary).
    notes: >
      Groomed 2026-07-09 from the 2026-07-03 IDEAS note (updated 2026-07-09 with the active
      branches). Marked in_progress: a design pass is actively in flight (branches
      feature/ui-dark-landing-myapps + origin/feat/web-console-design-pass, untracked UI-WIP/).
      Plan change (user-approved): created module drydocs-web + phase 12. The architecture is
      still open — the bolt-vs-thin-API call is the crux of this item.
      MERGED 2026-07-13 (groom, IDEAS 2026-07-12 artifact-design review sub-item 2): the
      design pass applies the artifact-design skill's "UI, not a document" checklist + its
      AI-default-looks list as the review lens over the UI-WIP/ mockups. RE-TIERED
      opus -> fable on this groom touch (the G3 groom-touch policy; the bolt-vs-thin-API
      call is a boundary decision, the matrix top slot).
    progress: >
      2026-07-14 — DESIGN-LANGUAGE PORT LANDED (branch feat/web-login-mock, on top of the
      O2 mock login): the dd270b2 dark mockup is now the React app — Landing (hero/features/
      tower cards), TowerDrill (cypher/graph/schema/preview/matrix, all SYNTHESIZED via
      data/towers.ts), dark token system (index.css, single-theme by design), IBM Plex,
      GraphSvg renderer, My Apps rollup graph; role-gated tower drilling (user = own tower
      only). web/ has left its throwaway-test-page state. Verified 27/27 headless-Edge CDP
      drive + screenshots. REMAINING for O1: the browser<->Neo4j access-path ADR (the crux;
      forced by any real login — mock deliberately doesn't decide it), a REAL C4/graph view
      against the live EE DB (NVL or successor) instead of the synthesized SVGs, wireframes
      committed, build/run docs.
      2026-07-14 (later) — ADR 0005 on main, status PROPOSED (docs/decisions/0005): the
      session's sanitized-evidence draft was REWRITTEN by the SME and landed via the other
      machine (b5235f4) — thin API is the DEPLOYMENT shape; bolt-from-browser survives only
      as a dev-mode adapter (dev flag + admin) behind ONE GraphAccess seam in web/src/lib/;
      no VITE_NEO4J_PASSWORD ever committed. Main's version is canonical (branch draft
      superseded at the 2026-07-14 reconcile merge). The acceptance's "decision recorded"
      ticks when 0005 flips ACCEPTED; on accept, groom the thin-API component + GraphAccess
      seam refit + live graph view into Epic O. NOTE: the landed design pass predates the
      seam — no new bolt usage (pages are synthesized), but the seam refit is the first
      code consequence of acceptance.
      2026-07-14 (evening) — ADR 0005 ACCEPTED (O3 done): the "decision recorded"
      acceptance clause is SATISFIED. O1's remainder = O6 (live C4/graph view through
      the seam, via O4+O5); wireframes committed (UI-WIP, e5608ff); build/run docs ride O5.
      DONE 2026-07-15 — O6 landed, closing the umbrella: every acceptance clause now
      holds — throwaway state left (O1 design pass), wireframes committed (UI-WIP),
      decision recorded (ADR 0005 ACCEPTED, thin API; O3), REAL C4/graph view against
      the local EE DB (O6: agraph-successor = d3-force + in-repo SVG in web/, NVL
      decision recorded on O6), build/run documented (web/README Graph-view section +
      O5's api run docs), no secrets in web/ (publish-boundary greps clean at O2/O4/O6).

  - id: O2
    epic: web-console
    title: "Mock persona sign-in (user + admin) gating the web console: My Apps user view; admin-only Cypher sandbox, governance placeholder, env toggle"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [web/src/, UI-WIP/drydocs-landing-dark.html, UI-WIP/DryDocs_UI_Development_Specs.md, UI-WIP/icons.md]
    acceptance: >
      Client-side mock auth in web/ (no new npm deps, no real security): with no session the
      app shows only a persona sign-in screen carrying a MOCK/SYNTHESIZED tag; signing in as
      the synthetic user persona (jdoe4821) lands on a read-only, SYNTHESIZED-labeled My Apps
      view (synthetic apps with SNOW permission badges rolling up to a CTO tower) and cannot
      reach the Cypher console even by editing the URL hash; signing in as the synthetic admin
      persona reaches the existing Cypher sandbox (both bolt and ADK flows unchanged), a
      Posture & Governance placeholder, and a cosmetic Prod|UAT|Dev env toggle; the session
      persists across reload via localStorage and sign-out clears it; the ADK userId is the
      persona id (no more hardcoded local-dev); a persistent banner labels the whole thing
      MOCK pending the O1 access-path ADR; npm run lint and npm run build pass; no real
      SIDs/secrets in web/ (publish boundary).
    notes: >
      Groomed 2026-07-13 from the web-login ask (planned session, user-confirmed: mock
      fidelity / scoped-user-full-admin gating / new branch off main). Scope guard:
      login/shell/role-gating plus a minimal My Apps view ONLY — not the UI-WIP design-pass
      port (that stays O1). Deliberately NOT depends_on O1: the mock must neither wait on
      nor pre-empt the bolt-vs-thin-API access-path ADR — real authn/authz arrives with that
      decision. Hash-based view switching (no react-router) and no vitest (pure-TS auth
      module gated by tsc + runtime drive) — both revisitable post-O1. Model sonnet per the
      matrix (written acceptance; not a boundary decision).
      DONE 2026-07-13 (same day, branch feat/web-login-mock, commits B-E): lib/auth.ts
      (localStorage session, role re-derived from PERSONAS — forged/garbage blobs degrade
      to sign-in) + lib/views.ts (single canSee registry; unauthorized deep links normalize
      to the role default) + data/myApps.ts (SYNTHESIZED, mirrors the dd270b2 mockup) +
      SignIn/Shell/MyApps/Governance components + App orchestrator; ADK userId = persona id.
      Verified: build+lint green; headless-Edge CDP drive 27/27 (sign-in-only fresh state,
      user hash-guard fallback, reload persistence, sign-out, admin all-views + env toggle,
      ADK request URL carries asmith7734, bolt wiring fires, corrupt-blob degradation);
      publish-boundary grep clean (synthetics only). Live-DB rows check env-gated (Docker
      down) — bolt flow moved verbatim in commit C, wiring proven by connection attempt.

  # (O3–O6 groomed 2026-07-14 from ADR 0005's Action items, at the design-pass merge.)
  - id: O3
    epic: web-console
    title: "Ratify ADR 0005 (browser↔Neo4j access path): SME accept/edit/reject + status flip"
    type: task
    module: docs
    phase: 12
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs: [docs/decisions/0005-browser-neo4j-access-path.md, docs/decisions/README.md, docs/restructure/backlog.yaml]
    acceptance: >
      The SME reviews ADR 0005 and its status flips PROPOSED -> ACCEPTED (or the SME's
      edits land first and then it flips; a rejection instead reopens the O1 crux with the
      rejection reason recorded in the ADR). The decisions README row drops its PROPOSED
      marker; O1's "decision recorded" acceptance clause is satisfied and noted in O1
      progress. No code in this item.
    notes: >
      Groomed 2026-07-14 from ADR 0005 Action item 1. in_progress = awaiting the SME
      (the E1/P2 idiom): the ADR is authored by the SME (rewrite b5235f4) and delivered;
      only the ratification act remains. Gates O4/O5 — the decision must be ACCEPTED
      before code implements it.
      DONE 2026-07-14 (same day): SME (chad.wilson) accepted 0005 AS WRITTEN, in-session;
      status flipped ACCEPTED (Action item 1 ticked), README row already PROPOSED-free,
      O1 "decision recorded" clause satisfied (noted in O1 progress). O4 + O5 now
      next_ready.

  - id: O4
    epic: web-console
    title: "GraphAccess seam refit in web/src/lib: bolt becomes a dev-mode adapter; raw Cypher gated behind admin + dev flag"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [O3]
    inputs: [docs/decisions/0005-browser-neo4j-access-path.md, web/src/lib/neo4j.ts, web/src/components/CypherConsole.tsx, web/README.md]
    acceptance: >
      Per ADR 0005 Action items 2/4/5: one GraphAccess interface in web/src/lib/ is the
      only way console code reads the graph — view components never import neo4j-driver
      directly; neo4j.ts is refitted as the bolt adapter behind it, activating ONLY on an
      explicit dev flag + admin role; an api adapter stub exists against the O5 endpoint
      shape (may throw not-implemented until O5 lands, but the seam + a bolt/api
      conformance test are real); the CypherConsole raw-Cypher panel is gated behind
      admin + dev flag; a repo check confirms no VITE_NEO4J_PASSWORD in any committed env
      file and web/README.md documents the dev-mode credential rule (localhost form-entry
      only, never committed/CI-injected). npm run build + lint green; the CDP drive's
      login/role checks still pass.
    notes: >
      Groomed 2026-07-14 from ADR 0005 Action items 2, 4, 5 (the ADR assigns the refit to
      O1's code stream; this item carries it so O1 stays the umbrella). The seam
      conformance test is the ADR's "two adapters kept honest" consequence.
      DONE 2026-07-14 (same day, branch feat/o4-graphaccess-seam, c553a32): graph.ts
      (interface + boltAllowed = DEV && admin — production bundles compile the bolt path
      out by construction), neo4j.ts refitted (only createBoltAccess exported; driver
      internal), graphApi.ts fail-loud stub, CypherConsole panel gated + env password
      seeding removed, .env.example + README carry the never-commit rule. Conformance =
      `satisfies GraphAccess` in both adapters (tsc-enforced; no vitest per the O2
      precedent — noted deviation from the groomed wording, mechanism equivalent) + the
      runtime drives. VERIFIED: dev build panel PRESENT/password empty, PRODUCTION
      PREVIEW panel ABSENT + explanation shown (3/3+3/3), 27/27 login/role regression,
      zero VITE_NEO4J_PASSWORD assignments.

  - id: O5
    epic: web-console
    title: "Thin-API component scaffold: read endpoints, per-view DB routing, gated raw-Cypher, session-auth stub"
    type: requirement
    module: drydocs-api
    phase: 12
    agent: main
    model: fable
    priority: p2
    status: done
    depends_on: [O3]
    inputs: [docs/decisions/0005-browser-neo4j-access-path.md, docs/decisions/0002-component-database-topology.md, agents/common/neo4j_tool.py, drydocs_core/, MODULE_MAP.md]
    acceptance: >
      A new monorepo component (working name drydocs-api; final name at scaffold, per the
      ADR 0002 components-on-core pattern) exists: imports drydocs_core only; its own
      COMPONENT_GROUP in tests/unit/test_module_boundary.py + MODULE_MAP row; server-side
      Neo4j driver with credentials from server env (never a browser); READ-ONLY enforced
      at the endpoint layer (generalizing the agents/common/neo4j_tool.py write-token
      guard) with per-view database routing (drydocs vs drydocs_all) decided server-side;
      named query endpoints shaped for the console's views (towers, drill-down, C4/graph
      payload) + a raw-Cypher endpoint gated to admin; a session-auth stub replaces the
      browser as role authority for the api path (synthetic personas server-side;
      enterprise OIDC remains a company-side twin per the ADR Evidence). Offline unit
      tests (pure handlers + duck-typed graph runner, the graph_verify idiom); build/run
      documented; full suite green.
    notes: >
      Groomed 2026-07-14 from ADR 0005 Action item 3 (the ADR explicitly defers this item
      to the groom-backlog flow). Model fable per the component-boundary precedent (G3/G4
      re-tiers): new COMPONENT_GROUP + MODULE_MAP boundary work. NOT an ADK api_server
      extension — shared guard pattern only. New module drydocs-api registered this groom.
      DONE 2026-07-14 (same day, branch feat/o5-drydocs-api, d7c21f3): drydocs_api/ —
      guard (code-regions write-token check; READ routing = 2nd layer), routing
      (fail-closed per-view DB map), queries (overview-counts, folder-census +
      dependency-chain from the live-proven IDEAS capture, c4-graph for O6), auth stub
      (personas drift-guarded against web auth.ts by a TS-parsing test; opaque tokens,
      role server-side), pure handlers + optional-group FastAPI wiring (poetry
      --with api; CORS for the console origins). REAL wiring smoke caught + fixed a
      PEP-563/FastAPI body-detection bug the pure tests couldn't see. 22 offline tests +
      TestClient smoke; suite 655/3; boundary green with the new `api` group; lock
      regenerated. LIVE-DB exercise deferred to O6 (Docker down today; LiveRunner is
      env-config only).

  - id: O6
    epic: web-console
    title: "Live C4/graph view against the local EE DB through the GraphAccess seam (api adapter primary)"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [O4, O5]
    inputs: [web/src/, docs/decisions/0005-browser-neo4j-access-path.md, UI-WIP/wf-console-01.html, UI-WIP/design-review.md]
    acceptance: >
      The console renders a REAL C4/graph view of the knowledge graph against the local
      EE DB — data through the GraphAccess seam with the api adapter as the default path
      (dev-mode bolt allowed for the bench, per ADR 0005) — replacing the synthesized
      drill-down SVGs as the live surface (the synthesized pages stay as the no-backend
      demo); the rendering-library decision (NVL vs successor) is made and recorded in
      the item notes or a design doc; payload shaping is server-side (O5's C4 endpoint),
      not duplicated in the browser; the wf-console-01 wireframe's graph-before-Cypher
      ordering finding is honored. Build + lint green; runtime-verified against the live
      DB with DOM/screenshot evidence.
    notes: >
      Groomed 2026-07-14 — the remaining O1 build; O1 closes when O3 (decision recorded)
      and this item land (wireframes committed 2026-07-14 via UI-WIP; build/run docs ride
      O5). Library choice is a tech pick, not a boundary decision — record it, escalate
      only if it grows one.
      RENDERING-LIBRARY DECISION (2026-07-15, recorded per acceptance): d3-force (ISC,
      ~90KB, layout-only, deterministic — fixed-seed LCG random source) + the in-repo SVG
      idiom (GraphSvg's visual language, dark tokens, DOM-assertable). NVL deferred, NOT
      blocked: @neo4j-nvl/react 1.2.0 verified React-19-compatible (peer 18||^19), but it
      is proprietary-licensed + ~1.25MB WebGL canvas — overkill for the server-capped
      200-edge payload and opaque to DOM-level CDP assertions. REVISIT TRIGGER: the V5
      explorer's C4 zoom ladder or >200-node scenes.
    progress: >
      DONE 2026-07-15 (branch feat/o6-live-graph-view, 298fc55): Graph nav view (both
      roles, ordered BEFORE Console — the wf-console-01 graph-before-Cypher finding; NO
      Cypher affordance in the view) renders real WAS_INFORMED_BY edges from the live EE
      drydocs db. Seam grew runNamed(queryId) -> NamedResult carrying the server-routed
      database (shown in the LIVE tag); graphApi.ts is the REAL adapter (persona login ->
      bearer, 401 retry, fails loud); bolt.runNamed throws (no browser-side query-registry
      fork — payload shaping stays server-side in drydocs_api/queries.py per acceptance).
      VERIFIED: build+lint green, pytest 652 passed, headless-Edge CDP drive 16/16 against
      the full live stack — DOM node/edge counts equal the API payload (9 jobs/8 edges),
      live job names asserted in the SVG DOM, O2/O4 role regressions green; screenshots
      captured. Environment notes: first live EE bootstrap exposed the APOC comment-';'
      landmine (fixed on main, 8478204) and the sample-data m3-verify nit (inboxed);
      synthesized tower pages remain the no-backend demo.

  - id: O7
    epic: web-console
    title: "Ship the two live-proven support queries as named drydocs-api endpoints"
    type: task
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # closed 2026-07-15 — already SHIPPED by O5 (see notes)
    depends_on: [O5]
    inputs: [drydocs_api/queries.py, docs/restructure/IDEAS.md]
    acceptance: >
      Two read endpoints land in drydocs-api with unit tests: (1) dependency-chain finder —
      two job_names in, undirected shortestPath over WAS_INFORMED_BY (bounded hops), returns
      hop job names + per-edge via_condition + hop count; (2) folder-scoped dependency
      census — sched_table filter in, returns dependency-edge count / distinct successors /
      cross-folder edge count. Mechanism-only fixtures; guarded like the existing read
      endpoints.
    notes: >
      Groomed 2026-07-15 from the 2026-07-14 inbox note (both queries proven single-digit-ms
      on the internal graph; the ~69% cross-folder-dependency stat is a whitepaper headline
      candidate). Also candidates for controlm-runbook-automation — reference, don't
      duplicate.
      CLOSED same day as groomed: the acceptance was ALREADY MET by the O5 scaffold —
      drydocs_api/queries.py ships folder-census + dependency-chain as named, param-validated
      view queries (its module docstring cites the same IDEAS capture), covered by
      test_drydocs_api.py. The inbox note predated the O5 build by hours and was stale when
      groomed. Item kept (ids are stable) as the audit-trail record; no new code.

  # O8–O11: the site-plan build-out (UI-WIP/site-plan.md, groomed 2026-07-17 from
  # the IDEAS UI DECISION line — single-track ReUI, Salt dropped). Module choice:
  # the EXISTING drydocs-web / drydocs-api registry entries, not a new drydocs-ui
  # module (site-plan §6's suggested id superseded by this groom).
  - id: O8
    epic: web-console
    title: "Site shell rebuild on the locked UI stack: ReUI/Tailwind zone shell, system-default theming, 9-module routing skeleton"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [O6]
    inputs: [UI-WIP/site-plan.md, UI-WIP/layout-anatomy-checklist.md, UI-WIP/design-review.md, web/src/]
    acceptance: >
      web/ runs on the locked single-track stack (Vite + React + Tailwind + ReUI free —
      Salt dropped): the zone shell from layout-anatomy-checklist.md (aside / header /
      page-owned toolbar / content / right-sidebar slot, one typed layout config driving
      every zone); theming = dark + light token sheets behind a pre-paint boot script
      defaulting to the system scheme with a 3-state System/Dark/Light toggle (dark
      tokens carried from drydocs-landing-dark.html, --faint contrast fix applied);
      IBM Plex self-hosted; all 9 module routes from site-plan §3 exist as real
      deep-linkable routes (back-button safe; focus moves to the view h2 on route
      change); view components still reach the graph only through the O4 GraphAccess
      seam.
    notes: >
      site-plan.md P0. Evolves the existing web/ console (O2/O6 surfaces survive as
      routes or are consciously retired in the shell PR — no silent deletion).
      2026-07-18 — DONE (agent worktree 423000f, merged --no-ff as feat/o8-web-shell,
      merge 408e8fc; build re-verified on main post-merge). Zone shell in
      web/src/layout/ driven by one typed shellConfig.ts; dark tokens carried from
      drydocs-landing-dark.html with the --faint contrast fix (#5C6B7E -> #71809A);
      pre-paint boot script + System/Dark/Light toggle; IBM Plex via @fontsource;
      all 9 site-plan §3 routes real + deep-linkable, focus-to-h2 on route change;
      GraphAccess seam untouched. Kept: GraphExplorer (/explorer/live), TowerDrill
      (/explorer/tower/:key), MyApps (/ownership), CypherConsole (/console).
      Consciously retired (reasons in the branch commit): Landing.tsx,
      Governance.tsx, old components/Shell.tsx, hash-router half of views.ts.
      NOTE — layout-anatomy-checklist.md named in inputs was NEVER COMMITTED
      (Cowork artifact); zones built from the acceptance + site-plan + wf-*.md.
      Disclosed gaps: no mobile off-canvas aside (desktop-first call), light
      tokens derived not contrast-verified (site-plan follow-up), no browser
      screenshot (no extension in the worktree env — verified via tsc/build/
      lint + curl route checks).

  - id: O9
    epic: web-console
    title: "Radial-hub landing + Explorer module page on the shared subpage template (graph pane over data frames, linked selection)"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 ("create a launchable site" session) — see notes for the two disclosed deviations
    depends_on: [O8]
    inputs: [UI-WIP/site-plan.md, UI-WIP/Gemini_Generated_Landing-Favorite.png, UI-WIP/Gemini-generated-subpage.png, UI-WIP/codeflow-ui-reference.png, UI-WIP/DryDocs_UI_Development_Specs.md, UI-WIP/gemini-wire-frame.md, UI-WIP/icons.md, web/src/]
    acceptance: >
      Landing renders the radial hub (red brand core, one spoke per module, spoke = the
      same route as the aside-nav item; SVG rebuild, not the raster mock) with a generic
      Explore CTA; the shared module-subpage template exists ONCE and Explorer
      instantiates it: page-owned toolbar (breadcrumbs + page actions), React Flow graph
      pane over ReUI data-frame tabs with a resizable divider, right node-inspector
      panel keyed by node type; graph and grid share one selection store (node click
      filters rows, row select highlights the node); demo tower content keeps its
      SYNTHESIZED/illustrative tags; screenshot parity with the mock in BOTH dark and
      light.
    notes: >
      site-plan.md P1. Towers are Explorer demo content, not modules.
      2026-07-19 groom merge: UI-WIP/codeflow-ui-reference.png (classification
      External; screenshot from the MIT-licensed codeflow repo, source cited in the
      IDEAS audit trail) kept as the worked example of exactly this subpage shape —
      left explorer+stats rail, center graph pane with view-mode tabs, right
      per-selection detail panel with a blast-radius card, linked selection, dark
      tokens. Cite, don't imitate branding.
      2026-07-21 groom merge (UI extension): DryDocs_UI_Development_Specs.md = the
      demo tower CONTENT spec (4 SYNTHESIZED pages — Global Enterprise / Cards /
      Home Lending / Auto — with table shapes + node palettes for the Explorer
      demo data); gemini-wire-frame.md = expanded-landing options (4 benefit
      cards, onboarding checklist, posture panel -> feeds the spoke health
      glyphs); icons.md glyphs are their own item (O22).
      DONE 2026-07-21: landing already had the SVG radial hub + generic CTA from
      O8; this pass added the onboarding checklist (5 steps, localStorage) and
      the 4 gemini-wire-frame benefit cards. Explorer now truly instantiates the
      template: React Flow (@xyflow/react) tower graph with tower chips, custom
      token-styled nodes (KIND_TOKEN map — kind->token, NOT the stored demo
      hex), edges labeled with rel names; four data frames (Applications/Jobs/
      Conditions/Servers) from the SYNTHESIZED tower set; ONE lifted selection
      store — node click filters rows (explicit clear chip) + breadcrumb gains
      the selection, row click highlights/re-frames the node (cross-tower jump
      verified), right inspector keyed by node kind with a connections list.
      TowerDemoGrid placeholder consciously retired (chips + drill/live links
      replace it). All verified live in the preview, both themes (RF pane bg +
      node borders flip token values; JS-verified). DEVIATIONS: (1) data grid is
      the hand-authored ReUI-pattern stand-in, consistent with the O8 Tabs/
      ResizableSplit convention (real ReUI copy-in still open); (2) acceptance's
      "screenshot parity" verified structurally + via computed styles because
      the Browser pane's screenshot capture times out this session — dark AND
      light checked, pixels not captured. QuerySpec binding is O11 by design.

  - id: O10
    epic: web-console
    title: "Lineage module page: React Flow source-to-target DAG over lineage data frames"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9]
    inputs: [UI-WIP/site-plan.md, UI-WIP/Gemini-generated-subpage-2.png, web/src/]
    acceptance: >
      /lineage instantiates the shared template: a React Flow source-to-target DAG
      (mock subpage-2 pattern) with node/edge styles reading the theme tokens in both
      schemes; its data frames (hops, data assets, schema definition) served through
      the GraphAccess seam; /lineage/asset/:assetId deep links resolve; selection
      linking works as in Explorer.
    notes: >
      site-plan.md P2, canvas half.
      DONE 2026-07-21 (Epic-O finish session): LineageRoute on the shared
      template — LineageGraphPane renders the mechanism-only data-series chain
      (FW job -> landing pair -> ingest -> RAW -> transform -> REFINED ->
      provision -> mart) as a React Flow DAG, kind colors via theme tokens
      (Explorer's KIND_TOKEN vocabulary), ILLUSTRATIVE badge; three NEW
      ddlineage QuerySpecs (lineage.hops.v1 / data-assets.v1 /
      schema-definition.v1 — the schema spec says honestly it carries
      node-schema only until the G17 MAC feed) bound via SpecGrid with the
      SYNTHESIZED demo frames as linked-selection fallbacks; Row-level preview
      stays the template's honest empty state (needs source platforms, not
      ddlineage). /lineage/asset/:assetId resolves to a selection (crumb +
      node glow). Verified: suite green (+lineage spec test), build+lint
      green, headless-Edge smoke — live spec ran against empty ddlineage and
      fell back honestly; deep-link screenshot shows crumb + highlighted node.

  - id: O11
    epic: web-console
    title: "QuerySpec registry + two-path Neo4j data-frame export (client view export; streamed full export with provenance manifest)"
    type: task
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (launchable-site session, follows O9) — see notes
    depends_on: [O9]
    inputs: [UI-WIP/site-plan.md, drydocs_api/, web/src/, PUBLISH-BOUNDARY.md, config/classification.yaml]
    acceptance: >
      Every data frame binds to a versioned QuerySpec (id, database, parameterized
      read-only cypher, columns, classification) and the UI renders only registry
      results; the toolbar/grid [Export] offers (a) client-side CSV/JSON of the current
      grid state and (b) a drydocs-api export endpoint that re-runs the spec
      server-side and STREAMS csv/jsonl (driver streaming — not apoc.export, which
      writes on the DB server) with a provenance manifest (spec id, cypher hash,
      params, database, executed_at, row_count, classification, trust tiers present);
      internal / internal-confidential exports carry the banner + filename prefix;
      anything readable from ddcontext is watermarked SYNTHESIZED; a "Copy as Cypher"
      action exposes the spec's query + params; unit tests cover manifest content and
      the classification rules.
    notes: >
      site-plan.md P2, export half. The plan §4 "until O3 resolves, the ADK tool layer
      IS the API" caveat is OBSOLETE — ADR 0005 is ratified and drydocs-api shipped
      (O5), so export endpoints land there directly; the ADK agent layer is a later,
      separate concern (site-plan corrected this groom).
      DONE 2026-07-21: drydocs_api/query_specs.py (registry — versioned ids,
      spec-owned database from the reviewed set, read-only-guard-validated
      cypher, classification vocabulary; invariants asserted at import) +
      drydocs_api/exports.py (run_spec with the ddcontext/ddall grid-visible
      trust_watermark column; export_spec streaming csv/jsonl generators;
      ExportLedger registers the manifest only when the stream COMPLETES —
      served at GET /exports/{id}/manifest as the sidecar; banner row +
      INTERNAL__/INTERNAL-CONFIDENTIAL__ filename prefixes) + LiveRunner.stream
      (session.run driver streaming, READ pinned, session owned by the
      generator — NOT apoc.export, NOT buffered execute_query). Web:
      GraphAccess seam gains runSpec/exportSpec (bolt adapter fails loud);
      SpecGrid binds all four Explorer tabs to their specs with both export
      paths + client-built view manifest + Copy-as-Cypher + classification
      banner; O9 demo frames survive as the VISIBLE fallback when the api is
      down or the spec returns zero rows (empty local graph) — never silent.
      Tests: test_query_specs.py (15 — registry invariants, watermark, banner/
      prefix rules, csv/jsonl stream content, manifest fields incl. cypher
      sha256 + exported_by, ledger bounds); suite 787 passed. Live-verified:
      uvicorn against neo4jtest — INTERNAL__ csv with banner + manifest,
      ddcontext jsonl with SYNTHESIZED tier; browser end-to-end (auth → spec
      run → zero-row fallback notice). Local DBs are empty so the LIVE grid
      path is covered by tests + the server round-trip, not yet by eyeballed
      populated frames. Toolbar [Export] stays disabled — grid-level export
      per frame is the shipped surface (acceptance's "toolbar/grid" either/or).
      SME CORRECTION same day (user review of the field mapping): jobs +
      conditions bumped v2 — folder resolved through the :ControlMFolder node
      (f.sched_table, not the denormalized folder_id join key), jobs gains
      data_center via the folder's SCHEDULED_ON :ControlMServer (the reified
      DATA_CENTER field); NEW explorer.folder-applications.v1 = the
      ControlMFolder -> BusinessApplication crosswalk over the gated edges
      (CONTAINS_JOB + WAS_ASSOCIATED_WITH {seal_app_ref}), bound to a new
      Folders tab (reviewed fifth Explorer tab — site-plan §3 listed four).
      SECOND SME CORRECTION same day: explorer.controlm-app-codes.v1 + App
      codes tab — the two-pattern code model (dedicated code = direct 1:1 vs
      shared platform code = many apps), pattern DERIVED from observed
      cardinality over gated edges only; the authoritative code->application
      edge is inboxed as a gate candidate (IDEAS 2026-07-21, feeds K2 tiers +
      the O13 code->application mapping-domain hook). SME: these frames = the
      power-user/SME mapping surface needed soonest -> O13 screen next.

  - id: O12
    epic: web-console
    title: "Admin configuration page: read-only view of the config layer with a GENERATED enforcement matrix (config file -> code consumer -> guard test -> gate)"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O8]
    inputs: [UI-WIP/wf-admin-config-01.md, UI-WIP/wf-admin-config-01.html, config/, tests/unit/, web/src/]
    acceptance: >
      /admin/config exists behind the admin persona (O2 gating) and instantiates the
      shared subpage template: graph pane renders the selected surface's traceability
      chain (config file -> code consumer(s) -> guard test(s) -> gate reference);
      data-frame tabs show (a) the enforcement matrix, (b) the selected surface's file
      content read-only (rendered verbatim - secrets are .env-only so config files
      carry env-var REFERENCES, never values; the matrix lists each surface's expected
      env keys by name), (c) the gate log. The matrix is GENERATED - a script scans
      config/ + tests/unit/ and emits enforcement-matrix.json - and a unit test fails
      when a config surface has no matrix row or a row references a missing file/test
      (the matrix cannot drift from the repo). Guard-test freshness shows as
      LAST-RUN metadata from the most recent CI artifact (no live CI polling).
      Status column distinguishes enforced / unguarded / gate-pending; the
      code-resident launcher registry renders as the visible unguarded-config example.
      NO edit controls anywhere - each surface links to its repo path + gate flow
      (config-as-code stays behind the gates).
    notes: >
      Groomed 2026-07-17 from the 2026-07-16 launcher-registry IDEAS line's "admin
      screen in the web console (Epic O candidate)" end-state - its stated trigger
      ("groom when the web console picks up admin surfaces") fired with the site-plan
      build-out. The config-file launcher-registry migration itself stays inboxed
      (drydocs-core work, separate groom). Wireframes are the item spec.
      2026-07-17 user decisions folded in: CI = last-run metadata; secrets .env-only
      (redaction question dissolved).
      2026-07-20 (L7 SaaS reframe, user direction) - this page is ALSO the home of the
      SME/tenant configuration surfaces that reframe planned: (a) per-source doc-connector
      config (origin registration + per-source requirement_id pattern, replacing the
      global loose/strict mode); (b) display-label/terminology config (the
      generic-terminology-research Salesforce pattern - stable internal labels,
      per-tenant display names); (c) the Jira-board mapping - each BusinessApplication
      or DevTeam MAY have a Jira board, keyed off the Jira PROJECT NAME (the join key
      the future Jira connector uses against the ownership graph; gate
      doc-traceability-feedback section D). When those config files land in config/,
      the generated enforcement matrix picks them up as surfaces automatically -
      read-only here like everything else, edits stay gate-bound.
      DONE 2026-07-21 (Epic-O finish session): scripts/render_enforcement_matrix.py
      scans a 14-surface registry against the repo (missing file/consumer/test =
      generation fails closed; every top-level config/ entry must be covered)
      and emits web/src/generated/enforcement-matrix.json with per-surface
      status (enforced / unguarded / gate-pending w/ pending-entry count from
      status: proposed|planned|placeholder markers), env-var references
      (explicit ${VAR} / os.environ / env: forms only), embedded verbatim file
      content (single-file surfaces; dirs list files), and the gate log.
      tests/unit/test_enforcement_matrix.py (4 tests) = the drift guard:
      regeneration equality, config/ completeness, referenced-path existence,
      and launcher-registry-is-the-unguarded-example. /admin/config renders the
      artifact on the shared template: React Flow traceability chain (config
      file -> consumers -> guard tests -> gate; a red NO-GUARD-TEST node for
      unguarded surfaces), matrix tab with the honest no-CI-artifact note
      (last-run metadata only — never polls), verbatim surface-detail tab,
      gate-log tab, right-sidebar inspector with copy-repo-path + gate-flow
      links, zero edit controls. Crosswalks surface honestly renders unguarded
      too (gate-signed, no drift test). Verified: suite 796 green; build+lint
      green; headless-Edge smoke as admin (screenshot) + steward-redirect
      proof. The L7 SaaS-reframe surfaces (doc-connector config, display-label
      config, Jira-board mapping) auto-join the matrix when their files land
      in config/ — the completeness test forces a registry row then.

  - id: O13
    epic: web-console
    title: "Manual mapping stewardship screen (power-user): draft job->application / FID / ALIAS entries into a gate-bound manual-loads changeset - never a direct graph write"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O8]
    inputs: [UI-WIP/wf-mapping-01.md, UI-WIP/wf-mapping-01.html, config/manual-loads/, drydocs/loaders/manual_loads.py, config/gate-prompts/, web/src/]
    acceptance: >
      /mappings exists behind a new steward (power-user) persona extending the O2 mock
      auth (admin also sees it; steward does NOT see /admin/config or the Cypher
      sandbox). A registry-driven domain strip lists the reconciler-consumed manual
      tables (K2 tier model: tier-5 manual job->application CSV in config/manual-loads/,
      FID tier-2, ALIAS tier-4). The attribution-coverage grid shows every job's current
      resolution (tier + evidence) with unresolved and conflict rows as work queues;
      assigning a target BusinessApplication (searchable by seal_id/name, rationale
      REQUIRED) drafts entries (source key, target, rationale, author, timestamp) into
      a per-user changeset tray; the assign dialog states the precedence honesty rule
      (manual = tier 5, never overrides SEAL evidence). Submit emits a
      config/manual-loads/ change ARTIFACT (drydocs-api draft endpoint or file-download
      fallback) that travels git + gate review + merge, applied by the NEXT LOAD RUN -
      the UI performs zero graph writes (the loader remains the only writer). Entry
      lifecycle draft/submitted/gated/loaded is visible, with loaded self-verified by
      the grid's tier flipping to manual after the load. Dropdowns/options read
      mapping.db via drydocs-api (the mapping-store plan §6 acceptance rider — M2
      landed 2026-07-18).
    notes: >
      Groomed 2026-07-17 from chat (power-user mapping screen). "Code->application"
      joins the domain strip when that mapping table exists as a reconciler input -
      same mechanics, registry entry only. Open items in wf-mapping-01.md: draft
      endpoint shape (PR-creating vs artifact download - decide at build per GHE
      posture); conflict-row escalation semantics.
    progress: >
      2026-07-18 — the API + store half LANDED pre-O8 (branch feat/mapping-store,
      807e050; mapping-store plan M0-M4 executed): drydocs_core/mapping_store.py
      (SQLite materialization, deterministic dumps) + drydocs_api /mappings/*
      (steward/admin gate, grid/options reads, POST changeset -> CSV+manifest
      ARTIFACT, zero server writes) + static/mapping_demo.html at /demo (the
      wf-mapping-01 live-data twin) + test_mapping_store.py / test_mapping_api.py.
      The draft-endpoint open item resolved conservatively: artifact download, no
      server-side git. REMAINING for O13: the real /mappings React screen on the
      O8 shell per the acceptance (coverage grid with tier/evidence, work queues,
      lifecycle visibility). Documented: docs/runbook-mapping-demo.md +
      docs/design/drydocs-web-console-tdd.md.
      DONE 2026-07-21 (Epic-O finish session): /mappings React screen on the O8
      shell — steward+admin route guard (steward still has NO /console; the
      server enforces 403 below steward anyway); registry-driven domain strip
      from GET /mappings/domains (available rows + not-built tier-2/4
      placeholders); coverage grid binds the NEW spec
      mappings.attribution-coverage.v1 (status resolved/unresolved/conflict +
      match_method tier evidence over the gated K2 edge; unresolved/conflict as
      one-click work queues); assign dialog searches explorer.applications.v1
      (free-text seal_id fallback when the list is unavailable), rationale
      REQUIRED, the K2 precedence-honesty rule stated up front + an
      already-resolves-at-higher-tier warning; per-user localStorage changeset
      tray; submit POSTs /mappings/changeset and downloads the CSV +
      manifest-snippet ARTIFACT (zero graph writes — the loader stays the only
      writer); lifecycle chips draft/submitted/gated(bookkeeping)/loaded, with
      loaded DERIVED live from the coverage grid flipping the job's tier to
      manual. graphApi refactor: shared createApiClient token client (ONE auth
      policy for GraphAccess + the mappings client). DEV-only ?as=<personaId>
      sign-in affordance for headless verification (the boltAllowed
      construction — baked out of prod bundles). Verified: suite 791 green;
      web build + lint green; headless-Edge smoke against the live api
      (mapping.db vocabulary footer live: applied 8 / confirmed 22 /
      proposed 3; SYNTHESIZED demo coverage fallback shows honestly on the
      empty graph; screenshots captured steward persona).

  - id: O14
    epic: web-console
    title: "Mapping-store staleness guard: detect source-hash drift in var/mapping.db and rebuild, so /mappings never serves stale grids"
    type: chore
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-18 same session it was groomed (main fable, post-merge)
    depends_on: []
    inputs: [drydocs_core/mapping_store.py, drydocs_api/mappings.py, tests/unit/test_mapping_store.py, knowledge/upgrade-plans/mapping-store-plan-2026-07-17.md]
    acceptance: >
      MappingStore detects that var/mapping.db was built from sources whose hash no
      longer matches the committed YAML/CSV (the source-hash meta row already stored
      at build) and EITHER rebuilds automatically on next connect OR refuses with a
      clear rebuild instruction — either/or decided at execution. A unit test edits
      a source, hits the store, and proves the served result reflects the edit (or
      the refusal fires). The plan M2 bullet's "DB rebuilt post-merge (CI/ritual)"
      is either wired or explicitly recorded as superseded by this guard. Full
      suite green.
    notes: >
      Groomed 2026-07-18 as the mapping-store plan residual: the store auto-builds
      only when the FILE IS ABSENT — a stale file serves stale grids until deleted
      (the docs/runbook-mapping-demo.md workaround). Derived artifact, no gate.
      DONE 2026-07-18 same session: source_hashes() + is_current() in
      drydocs_core.mapping_store (expected-meta computed WITHOUT building; exact
      dict compare so added/removed manifest CSVs count as drift; missing/corrupt/
      foreign file -> False); MappingStore._connect rebuilds when not current —
      the either/or decided as AUTO-REBUILD over refusal (the derived-artifact
      doctrine; corrupt files heal the same way). Tests: core drift test edits a
      fixture source and proves the rebuild serves the edit; api test tampers the
      meta and proves a read rebuilds + restores current hashes. Plan M2's
      CI/ritual rebuild bullet recorded SUPERSEDED in the plan header; runbook
      troubleshooting updated. Suite 693 passed / 6 expected skips.

  # O15–O22: the site-plan P3 extension (groomed 2026-07-21 from chat + the new
  # UI-WIP corpus: DryDocs_UI_Development_Specs.md, gemini-wire-frame.md, icons.md,
  # layout-anatomy-checklist.md now on disk). Covers every remaining console
  # surface buildable WITHOUT a gate; the chain terminates at O20 — the HITL gate
  # deciding the UI write-surface boundary. Until O20 signs off, every console
  # surface is read-only / artifact-only (the O13 doctrine).
  - id: O15
    epic: web-console
    title: "Ownership module page: SEAL->PAT rollup graph rendering the K4 qualified-attribution shape over team/attribution data frames"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9, O11]
    inputs: [UI-WIP/site-plan.md, web/src/, drydocs/loaders/cypher/seal_applications.cypher, drydocs/loaders/cypher/seal_contacts.cypher, drydocs_core/schema/seal_ontology_supplement.cypher]
    acceptance: >
      /ownership evolves the kept MyApps view into the shared subpage template:
      graph pane renders the ownership rollup (CatalogLOB -> Product -> AreaProduct ->
      DevTeam -> BusinessApplication) using the K4 qualified-attribution shape —
      Attribution nodes keyed attribution_id with their TOMRole, and
      unmapped_role=true attributions visibly flagged, never hidden; data-frame
      tabs (Teams / Attributions / Escalation routing) bind to QuerySpecs, with
      honest empty states when a source (e.g. escalation routing) isn't loaded in
      the target DB; selection linking works as in Explorer; both themes pass.
    notes: >
      site-plan.md P3 wave 1 (module 3). Deliberately sequenced after the K4
      constraint fix (4abe1aa) — the page reads the exact MERGE keys the
      constraints now back. MyApps at /ownership survived O8; this is its
      template-instantiation upgrade, not a new surface.
      DONE 2026-07-21 (Epic-O finish session): OwnershipRoute on the shared
      template — OwnershipGraphPane renders the acceptance rollup chain
      (CatalogLOB -> Product -> AreaProduct -> DevTeam -> BusinessApplication)
      in the K4 qualified-attribution shape: Attribution nodes keyed
      attribution_id, HAD_ROLE -> TOMRole, and an unmapped_role=true example
      VISIBLY red-flagged (never hidden); MyApps kept as the pane's toggled
      alternate view (evolution, not teardown). Three NEW QuerySpecs:
      ownership.teams.v1 (DEVELOPS counts), ownership.attributions.v1
      (internal-confidential; holder by SID only, unmapped rows float to top),
      ownership.escalation-routing.v1 (ServiceNowGroup — zero rows is the
      honest producer state, company-side source). Registry tab renamed
      Memberships -> Attributions (tracks the K4 model). Selection links
      graph <-> frames as in Explorer. Verified: spec tests green, build+lint
      green, headless-Edge smoke (rollup + unmapped flag + live spec fallback).

  - id: O16
    epic: web-console
    title: "Loads module page: loader->JobRun timeline over run/reject/coverage data frames"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9, O11]
    inputs: [UI-WIP/site-plan.md, web/src/, drydocs/loaders/]
    acceptance: >
      /loads instantiates the shared template with the ReUI Timeline as this
      module's canvas (site-plan §3 row 8): loader -> :JobRun provenance rendered
      as a run timeline; data-frame tabs (Runs / Rejects / Drift-coverage) bind
      to QuerySpecs over the BaseLoader :JobRun envelope; /loads/run/:runId deep
      links resolve; empty states honest on databases with no runs; both themes.
    notes: >
      site-plan.md P3 wave 1 (module 8).
      DONE 2026-07-21 (Epic-O finish session): LoadsRoute on the shared
      template — LoadsTimeline (dot-and-rail vertical timeline in the ReUI
      idiom, status theme-tokened, per-run counts incl. rejected/
      marked-removed/reactivated) fed by NEW spec loads.runs.v1; sibling specs
      loads.rejects.v1 (rows_rejected > 0 — never silent drops) and
      loads.drift-coverage.v1 (the D7 mark counters). /loads/run/:runId
      preselects the run (crumb + item highlight). LIVE-VERIFIED beyond the
      demo path: the local drydocs DB carries real :JobRun envelopes (bmc_docs
      + software_registry loads, 2026-07-09) and the smoke screenshot shows
      the timeline + Runs grid rendering them with the LIVE badge and full
      export toolbar — the first module page verified against real graph rows.
      Demo runs remain the honest fallback for empty DBs. Build+lint+spec
      tests green.

  - id: O17
    epic: web-console
    title: "Runbooks + Remediation module pages: provisioning-chain and finding->fix-batch flows on the shared template"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9, O11]
    inputs: [UI-WIP/site-plan.md, web/src/, drydocs_remediation/, .claude/skills/controlm-runbook-automation/SKILL.md]
    acceptance: >
      /runbooks and /remediation both instantiate the shared template. Runbooks:
      graph pane = the data-series provisioning chain (FileWatcher -> RAW -> ING ->
      LD), frames Series / Generated runbooks / Metadata completeness. Remediation:
      graph pane = finding -> fix-batch flow, frames Findings / Fix batches / Jira
      handoffs, reading drydocs_remediation outputs through drydocs-api QuerySpecs.
      Mechanism-only fixtures where the live data is company-side, tagged
      SYNTHESIZED/illustrative like the Explorer towers; both themes.
    notes: >
      site-plan.md P3 wave 1 (modules 4+5), combined: both back onto the
      remediation/runbook pipeline domain and are pure template instantiations.
      DONE 2026-07-21 (Epic-O finish session): both pages on the shared
      template via the NEW shared MiniDag + LinkedDemoFrame components
      (extracted here — 4th copy of the boilerplate was the signal).
      Runbooks: FW -> RAW -> ING -> LD provisioning-chain pane; NEW specs
      runbooks.series.v1 (ddlineage, zero rows honest pre-gate) +
      runbooks.metadata-completeness.v1 (drydocs description-field coverage,
      missing rows float to top — the fix-in-batches queue); the
      Generated-runbooks frame lists the repo's REAL runbook artifacts
      (files, not graph rows — no spec by design). Remediation: finding ->
      fix-batch -> package -> Jira SoD flow; ONE CONSCIOUS DEVIATION from the
      acceptance letter, recorded: all remediation frames are SYNTHESIZED
      fixtures with no QuerySpec binding, because drydocs_remediation writes
      ZERO graph (its invariant) and inventing :Finding/:FixBatch labels in a
      spec would smuggle an ontology decision past the HITL gate — specs bind
      if/when a remediation graph shape is gate-confirmed (TDD §6/§7). Route
      wiring rode the concurrent session's 3bca511 (shared App.tsx). Suite +
      build + lint green; both pages headless-Edge smoked.

  - id: O18
    epic: web-console
    title: "Docs module page: Document->Chunk corpus map with trust/provenance tiers visible per row"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9, O11]
    inputs: [UI-WIP/site-plan.md, web/src/, drydocs/loaders/cypher/bmc_docs.cypher, drydocs/loaders/cypher/essential_graphrag.cypher]
    acceptance: >
      /docs instantiates the shared template over the bmc-docs lexical corpus:
      Document -> Chunk map in the graph pane; data-frame tabs (Documents / Chunks /
      Trust-provenance) with the trust tier (VERBATIM / GROUNDED / SYNTHESIZED)
      visible as a column, never hidden; QuerySpecs against the docs corpus
      database; /docs/document/:docId deep links resolve; both themes.
    notes: >
      site-plan.md P3 wave 2 (module 6). Corpus exists (document_id/chunk_id
      constraints, bmc-docs load gate-accepted 2026-07-08).
      DONE 2026-07-21 (Epic-O finish session): DocsRoute on the shared
      template — MiniDag Document->Chunk map (FIRST_CHUNK/NEXT_CHUNK/
      PART_OF/DESCRIBES shape); three NEW specs docs.documents.v1 /
      docs.chunks.v1 / docs.trust-provenance.v1 with the trust tier
      (VERBATIM/GROUNDED/SYNTHESIZED) as a visible column, never hidden
      (effective tier = chunk override else document default);
      /docs/document/:docId deep links resolve. LIVE-VERIFIED against the
      real corpus: the smoke screenshot shows all 27 bmc-docs documents
      (GROUNDED tiers, External classification, chunk counts) with the full
      export toolbar. Suite + build + lint green.

  - id: O19
    epic: web-console
    title: "Gates module page (READ-ONLY): gate dependency graph + gate-log frames from a generated gates.json — zero action controls"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-21 (Epic-O finish session) — see notes
    depends_on: [O9]
    inputs: [UI-WIP/site-plan.md, web/src/, config/gate-log.md, config/gate-prompts/, docs/restructure/backlog.yaml]
    acceptance: >
      /gates instantiates the shared template READ-ONLY: graph pane renders the
      gate dependency graph (gates -> the backlog items / vocabulary entries they
      unblock); data-frame tabs Open gates / Signed off / Gate log. Data is a
      GENERATED gates.json (a script scans config/gate-log.md + config/gate-prompts/
      + backlog.yaml — the O12 enforcement-matrix pattern) with a unit test that
      fails when a gate-log entry or gate-prompt file has no row (no drift). ZERO
      action controls — no sign-off, no edits; each gate links to its repo
      artifacts. Write-shaped gate actions are explicitly out of scope: they
      belong to the O20 gate decision.
    notes: >
      site-plan.md P3 wave 2 (module 7). The read-only render is ungated;
      acting on gates from the UI is exactly what O20 decides.
      DONE 2026-07-21 (Epic-O finish session): scripts/render_gates.py scans
      config/gate-log.md (one row per ## entry, status derived from the
      heading VERBATIM: signed-off/deferred/pending/recorded — the page never
      reinterprets the record) + config/gate-prompts/ (prompt files with no
      log mention = status open) + backlog.yaml (items mentioning a gate slug
      = the "references" edges) -> web/src/generated/gates.json (25 gates, 5
      open/deferred/pending at generation). tests/unit/test_gates_json.py =
      the drift guard (regeneration equality; every log entry + prompt file
      has a row). GatesRoute renders READ-ONLY on the shared template: gate
      dependency MiniDag for the selected row, Open gates / Signed off /
      Gate log frames, zero action controls, artifacts as repo-path text.
      Suite + build + lint green; headless-Edge smoked (deferred SOSA gate
      visible in the open queue).

  - id: O20
    epic: web-console
    title: "HITL gate session: the UI write-surface boundary — which console actions may ever write, and through what mechanism"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: fable
    priority: p3
    status: done          # SIGNED OFF 2026-07-21 pm (in-chat guided session, 4/4 as recommended) — gate-log transcribed
    depends_on: [O13, O19]
    progress: >
      2026-07-21 (Epic-O finish session) — GATE-PREPPED, awaiting the SME:
      config/gate-prompts/ui-write-surface.yaml authored (doctrine
      reaffirmation + four candidate affordances C1 gate-sign-off-drafting /
      C2 SME notes / C3 steward PR-creation / C4 admin edits, each against
      the mechanism tiers M1 artifact-only / M2 non-graph drafts / M3 direct
      write-NEVER; conservative recommendations recorded as recommendations,
      every ruling open). Page rendered to var/gate-ui-write-surface.html
      (10 confirmations). gates.json regenerated — the new gate correctly
      appears as `open` on /gates (the O19 auto-pickup proving itself).
      Declining every extension is a valid sign-off; the item completes when
      the session runs and transcribes to config/gate-log.md.
      SME REVIEW SAME DAY (pre-session): scope clarified console-wide (title
      + summary now explicit — NOT just the /gates page); file-identity
      footer added to the gate-page renderer (path + copy button, +test);
      then AMENDED with in-session direction (gate pages necessary; SaaS
      super-user page expected = new C5; M2 origin-flagged store ratified;
      override lists -> source-corrections reports; O24 promoted).
      DONE 2026-07-21 pm — SIGNED OFF via the in-chat guided walk-through
      (AskUserQuestion, 4/4 as recommended; transcribed to config/gate-log.md
      same day): SME-1 doctrine + C4-never + C5-expectation confirmed as
      written; SME-2 C1 = M1 artifact-drafting (groomed O25); SME-3 C2 = M2
      origin-flagged non-graph store (first instance O24); SME-4 C3 = defer
      server-side git (M2 + report is the path). M3 direct graph write
      REFUSED standing. The Epic O phase-12 chain is COMPLETE end to end.
    inputs: [docs/restructure/03-hitl-sme-flow.md, config/gate-log.md, UI-WIP/site-plan.md, docs/restructure/backlog.yaml]
    acceptance: >
      A HITL gate session (docs/restructure/03-hitl-sme-flow.md) decides the UI
      write-surface boundary and records it in config/gate-log.md: which
      write-shaped console actions, if any, are ever permitted (gate sign-off from
      /gates, SME annotations/notes, steward drafting beyond the O13
      artifact-download) and the mechanism each must use (gate-bound change
      artifact per the O13 doctrine vs a direct API write). The standing rule
      "the UI performs zero graph writes" is the default the gate either
      re-affirms or consciously amends PER SURFACE. Until signed off, every
      console surface stays read-only / artifact-only, and any future UI item
      proposing a write must cite this gate id.
    notes: >
      THE TERMINUS of the 2026-07-21 UI extension groom — everything before it is
      buildable ungated; nothing write-shaped proceeds past it. Also the home for
      the O12-noted L7 SaaS-reframe config surfaces IF any of them ever grows an
      edit control (today they are read-only by design).

  - id: O21
    epic: web-console
    title: "Commit the UI-WIP design corpus: LFS the 19 MB .fig first, classify each file against the publish boundary"
    type: chore
    module: docs
    phase: 12
    agent: main
    model: haiku
    priority: p3
    status: done          # DONE 2026-07-21 same session groomed — one conscious deviation, see notes
    depends_on: []
    inputs: [UI-WIP/, PUBLISH-BOUNDARY.md, config/classification.yaml, .gitattributes]
    acceptance: >
      start-react-free-reference.fig (19 MB) is tracked via git-lfs BEFORE add;
      every untracked UI-WIP file is checked against PUBLISH-BOUNDARY.md and
      committed with a sensible classification (AI-generated mocks + design prose
      = internal-public ceiling; codeflow-ui-reference.png stays External with
      its citation; anything that turns out to show internal data — check
      dd-verify-top.png — moves to internal/ instead of committing here);
      layout-anatomy-checklist.md lands (closing the O8 "named input never
      committed" note); git status is clean for UI-WIP afterward.
    notes: >
      site-plan.md §6 LFS follow-up, now actionable since the corpus feeds O15+
      inputs. DONE 2026-07-21: all 17 files individually reviewed. 12 PNGs = AI
      mocks with explicitly illustrative/anonymized data (some carry AI-invented
      "Trusted by" real-company logo strips — reference material, never publish
      as marketing) + dd-verify-top.png = a screenshot of the already-committed
      controlm-ingestion-tdd render (PRARAG fixture name already public); 5 md
      files = clean design prose. All committed at the internal-public ceiling.
      DEVIATION from the acceptance letter: the .fig was NOT LFS-committed — it
      is the Keenthemes vendor template's own Figma source; committing would
      REDISTRIBUTE a third-party asset on a sometimes-published repo (license
      unverifiable offline), so it is gitignored with rationale (the vendor-PDF
      rule) per the original either/or in the UI-workstream memory note. Our own
      structure-only extraction (layout-anatomy-checklist.md) IS committed.

  - id: O22
    epic: web-console
    title: "Icon set: export the 7 hub glyphs from icons.md as recolorable SVG components consumed by the radial hub + aside nav"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-21 same session groomed
    depends_on: [O8]
    inputs: [UI-WIP/icons.md, web/src/]
    acceptance: >
      The 7 glyphs in icons.md (brand mark, Tower Auto, Infrastructure, Home
      Lending, Data Lineage, Credit Cards + the reused infrastructure variant)
      exist as standalone recolorable SVG assets/components under web/src —
      stroke + palette via the O8 theme tokens, no hard-coded hex — and the
      aside-nav module items (and the radial hub, once O9 lands) consume them;
      the icons.md status checklist is updated; both themes verified (glow layer
      gets the light-mode solid-outline equivalent per site-plan §2).
    notes: >
      Groomed from icons.md's open checklist. Coordinate with O9: whichever lands
      first, the other consumes the shared assets (dep is O8 so icon work can run
      in parallel with the landing build).
      DONE 2026-07-21: web/src/components/icons/HubGlyphs.tsx — 7 glyphs as
      inline-SVG components (120x120, ~2.2 stroke per icons.md), token-only
      (palette mapping icons.md->tokens.css in the header; zero hex in rendered
      SVGs, JS-verified); accent layers use currentColor so the dark-only
      .glyph-accent drop-shadow glows each accent's own hue; light = filter:none
      solid outline (site-plan §2). TowerIcon + BrandMark rewired to consume
      (their old hard-coded hex removed; 'shared' tower -> Infrastructure glyph
      per the icons.md reuse rule); HUB_GLYPHS export = the 7-slot hub
      composition ready for O9. icons.md checklist updated (fonts line
      superseded by the IBM Plex decision). Verified: npm build green; both
      themes checked via computed styles in the live preview (screenshot capture
      timed out in the pane — DOM/style verification instead, disclosed).

  - id: O23
    epic: web-console
    title: "HITL gate session: rollup-shape review — define the canonical rollup shape per UI scenario against the vocabulary"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [O15]
    inputs: [drydocs_core/schema/schema_graph.cypher, web/src/ownership/demoOwnership.ts, web/src/ownership/assetSearch.ts, docs/restructure/03-hitl-sme-flow.md, config/gate-log.md]
    acceptance: >
      A HITL gate session defines THE rollup shape for each UI scenario that
      renders one (ownership K4 rollup, asset-path source→target, mappings
      attribution views, lineage rollups as they land): for each scenario the
      gate records the exact node kinds, relationship types, directions, and
      qualifier properties (role, status active/planned) drawn from the
      schema_graph vocabulary — no UI may render a relationship type or
      direction that is not in the vocabulary, and planned-status edges must
      be visibly marked as planned. Decisions land in config/gate-log.md; every
      demo/mock dataset in web/src is then reconciled to the ruled shapes (the
      2026-07-21 drift found in demoOwnership.ts — HAS_PRODUCT skipping
      ProductLine, ALIGNED/DEVELOPS invented types, SUPPORTS direction — is
      the motivating example; a hotfix aligned the mock same day, but the
      per-scenario ruling is the gate's job, not the hotfix's).
    notes: >
      User-requested 2026-07-21: "Rollup Shape review, it needs to be defined
      for each scenario with HITL gate." The standing rule this gate encodes:
      mock data may invent VALUES, never RELATIONSHIP SEMANTICS — labels,
      node kinds, edge types, and directions always reflect current schema
      state so the demo teaches the true shape.

  # ── Epic P — Runtime topology & maintenance windows — Phase 13 ─────────────
  # Born 2026-07-09: CM_HOSTS + CM_AVG_RUN onboarded via the add-source-object
  # walkthrough; gate controlm-hosts-topology SIGNED OFF 18/18 same day
  # (config/gate-log.md); gate controlm-avg-run-supplement prepped, awaiting SME.
  - id: P1
    epic: runtime-topology
    title: "Run the CM_HOSTS + CM_AVG_RUN profiling probes internally; record conclusions + the DC scope call"
    type: task
    module: config
    phase: 13
    agent: main
    model: haiku
    priority: p1
    status: done          # DONE COMPANY-SIDE — learned from the 2026-07-20 bundle-port readout (company PORT-REPORT-bd7952f.md: "Restored P1 + P4 to done ... company's completion status + notes win"); resolves the 07-18 "concurrent Epic P session" observation — it was the company machine. Producer never runs these probes (internal-only by design); status mirrors the company SoR.
    depends_on: []
    inputs: [drydocs/loaders/sql/adhoc/profile_cm_hosts.sql, drydocs/loaders/sql/adhoc/profile_cm_avg_run.sql]
    acceptance: >
      Conclusions (counts/domains/match rates only — never result rows) recorded for
      cm_hosts P1–P5 and cm_avg_run P0–P8 (P4s scoped smoke test BEFORE the estate-wide
      join, per the user's performance flag) in the two gate specs' provenance blocks +
      the ledger via fields; the 22-DC-vs-production scope call appended to
      config/gate-log.md as the controlm-hosts-topology residual resolution.
    notes: "User-run on the internal network (no producer-side psgmgr access); agent transcribes."

  - id: P2
    epic: runtime-topology
    title: "CM_AVG_RUN supplement gate session (controlm-avg-run-supplement) — SME review"
    type: task
    module: ontology
    phase: 13
    agent: main
    model: opus
    priority: p1
    status: done          # SIGNED OFF 2026-07-14, 1 SME edit (§B ctlm_id join upgrade); map entry confirmed; loader still blocked on P1 probes
    depends_on: []
    inputs: [config/gate-prompts/controlm-avg-run-supplement.yaml, config/gate-log.md]
    acceptance: >
      SME records Confirm/edit/reject per section (A landing shape, B weak-key join
      policy, C normalization rules, D refresh, E use case) in config/gate-log.md;
      map entry job-runtime-stats-supplement flips proposed→confirmed only then.
    notes: "in_progress = awaiting HITL (the E1/F1/F2/K2 pattern); gate page rendered + delivered 2026-07-09."

  - id: P3
    epic: runtime-topology
    title: "Hosts loader + derived RUNS_ON resolution pass (group wins; coverage-reported)"
    type: task
    module: drydocs-load
    phase: 13
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: [P1]
    inputs: [drydocs/loaders/sql/controlm_hosts.sql, config/gate-prompts/controlm-hosts-topology.yaml, config/gate-log.md]
    acceptance: >
      Loader MERGEs :ControlMHostGroup {data_center,name} + :ExecutionHost {nodeid} +
      CONTAINS_HOST {participation_type,last_capture_date} (+ DEFINED_ON only per the
      P1 DC-key verification); the derived pass (runs AFTER jobs+hosts) writes RUNS_ON
      {role: host_group|agent_host, derived:true} per the gate's group-wins rule;
      UNMATCHED/NULL reported as coverage, never guessed; vocabulary m3_runs_on_host_group
      / m3_host_group_contains_host (+ agent_host) flip planned→active with supplement
      blocks (test_schema green); map entries flip confirmed→applied with applied_on
      (test_taxonomy_ontology_map green).
    notes: >
      CLOSED 2026-07-27. Built: ControlMHostsLoader (controlm_hosts.v1, sweep
      on ControlMHostGroup) + controlm_hosts.cypher; RunsOnResolutionPass
      (runs_on_resolution.v1) as a graph-side derived pass — coverage
      dataclass with the reconciles() partition invariant, counts stamped on
      its :JobRun (seal_attribution precedent), P4 both-match census and the
      multi-DC group-name ambiguity count reported alongside; ingest-controlm
      gained the controlm_hosts node stage + the resolution stage in the
      relationships phase; constraints controlmhostgroup_key
      (data_center,name) NODE KEY + executionhost_nodeid (EXPECTED 47→49);
      supplement class anchors (ControlMHostGroup, ExecutionHost) + RUNS_ON /
      CONTAINS_HOST LocalRelationship terms; sample
      controlm_hosts__sample.csv exercises all three outcomes. Live sample
      run (desktop, neo4jtest, drydocs DB — retro-tagged 2026-08-04 at J18;
      sample-reproducible: re-ingesting the bundled samples reproduces these
      counts, proven at the P5 close): 9 host_group / 7 agent_host /
      1 UNMATCHED / 0 collisions, reconciles=true, 16 edges. DEFINED_ON deliberately NOT written —
      m3_host_group_defined_on stays planned on the open gate-log residuals
      (DC value-domain probe untranscribed producer-side + the 22-DC scope
      call); ledger CM_HOSTS retargeted staging:* → graph targets.

  - id: P4
    epic: runtime-topology
    title: "CM_AVG_RUN property-supplement loader: normalized timings + day-of-week medians + folder windows"
    type: task
    module: drydocs-load
    phase: 13
    agent: main
    model: sonnet
    priority: p1
    status: done          # DONE COMPANY-SIDE (same 2026-07-20 bundle-port readout as P1 — their probes + supplement loader shipped). Producer mechanism back-port, if ever wanted, is a reconcile-port/backflow concern, not a re-open.
    depends_on: [P1, P2]
    inputs: [drydocs/loaders/sql/controlm_avg_run.sql, config/gate-prompts/controlm-avg-run-supplement.yaml]
    acceptance: >
      Supplement pass MATCHes existing :ControlMJob on (parent_table, job_name) — never
      MERGEs; index on ControlMJob(parent_table, job_name) created BEFORE the estate-wide
      run; >24h clock normalized in Python (avg_start_time + start_next_day); FileWatcher
      rows flagged via APPL_TYPE and excluded from blended stats; day-of-week medians
      derived from SAMPLES_*; ControlMFolder.window_start/window_end rolled up with the
      DC-default-time fallback (knowledge/standards/technology/data-center-naming-convention.md);
      unmatched stats rows = coverage metric; map entry flips confirmed→applied.

  - id: P5
    epic: runtime-topology
    title: "Maintenance-window query: best patch window for a host or host group"
    type: requirement
    module: drydocs-load
    phase: 13
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [P3, P4]
    inputs: [config/gate-prompts/controlm-hosts-topology.yaml]
    acceptance: >
      Given a host (nodeid) or group name, a committed query/utility returns every job
      that can land on it (2-hop group + 1-hop hard-coded) with normalized folder windows
      and the computed quiet window(s); window math is critical-path based, never a path
      sum (the standing TDQ-ETA rule); demonstrated against sample data with the
      NODE_GROUP↔RUNS_ON cross-validation reported as a metadata-findings list
      (remediation feeder).
    notes: >
      CLOSED 2026-07-28 (worktree p5-main) — EPIC P COMPLETE 5/5. Built:
      drydocs/loaders/patch_window.py + committed READ-ONLY
      cypher/patch_window.cypher (named sections, every anchor O33-guarded; the
      writing pass remains runs_on_resolution) + `drydocs patch-window
      --host|--group [--json]`. Busy time = interval UNION on the wrapping 24h
      clock — the critical-path extent, never a sum (test_union_never_a_sum
      pins two chained 2h jobs overlapping 1h = 3h busy, not 4h). Placement
      order: job avg_start_time/avg_run_time -> folder window_start/window_end
      -> NO_TIMING_DATA finding; runtime outliers >= 24h (the gate's P3c junk
      class) are excluded AND flagged so one bad stat can never silently
      declare the whole clock busy. Findings vocabulary: no_timing_data /
      unparseable_timing / runtime_outlier / hardcoded_bypass /
      intent_without_edge / stale_edge / multi_dc_group. LIVE (desktop,
      neo4jtest, drydocs DB — machine named per the VERIFIED-LIVE inbox
      lesson): bundled samples re-ingested reproducing the P3 close-note
      coverage exactly (9 host_group / 7 agent_host / 1 UNMATCHED); group
      grp-auto = 4 pinned jobs, each doubling as a hardcoded_bypass finding;
      host host-hldm-02 = 12 jobs across BOTH paths (3 direct + 9 via the
      host-hldm-01 group). The P4 timing supplement is company-side only (note
      15043cd), so live rows place 0 jobs — the report says so loudly
      (whole-day quiet + CAVEAT + per-job findings = the remediation feeder in
      action); the math clauses are pinned by 19 unit tests. Full unit suite
      green (1070 pre-existing + 19 new); import + CLI help clean.

  # (J7/J8 groomed 2026-07-09 — tech-debt remediation tails, docs/reviews/*.md)
  - id: J7
    epic: release-infrastructure
    title: "Per-entry port reconciler guards: status no-downgrade + gate-log append-only + version-string rule"
    type: chore
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [PORT-MANIFEST.yaml, tests/unit/test_taxonomy_ontology_map.py, config/gate-log.md]
    acceptance: >
      Executable checks behind the PORT-MANIFEST per-entry entry_rules, REUSING the map
      guard's loaders (no second YAML parser): (1) a fixture-driven test proving a
      vocabulary/map merge that downgrades active/confirmed→planned/proposed FAILS;
      (2) gate-log append-only check (existing entries are a prefix of the merged file);
      (3) the pyproject version-string rule asserted in the manifest test. Rules
      runnable consumer-side during reconcile-port (documented in that skill).
    notes: >
      docs/reviews/tech-debt-port-boundary.md Phase 2; sequenced AFTER the map guard
      (landed ede0b94) by design. DONE 2026-07-11: tests/unit/test_port_reconcile_guards.py
      — executable entry rules (vocab active no-downgrade + map confirmed/applied
      no-downgrade + no dropped entries + gate-log byte-prefix append-only), fixture-proven
      both directions, plus env-gated LIVE comparison (RECONCILE_BEFORE_DIR=<snapshot dir>)
      verified pass AND fail against a doctored snapshot; version-string rule asserted in
      test_port_manifest.py; consumer-side usage documented in the reconcile-port skill.

  - id: J8
    epic: release-infrastructure
    title: "Skip-guard policy test: tests reading gitignored assets must skip, not fail"
    type: chore
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [tests/unit/, .gitignore, PORT-MANIFEST.yaml]
    acceptance: >
      A policy test scans tests/unit/ for references to drydocs/data/ or internal-local/
      and fails if the referencing test lacks a skipif/importorskip guard — the exact
      failure a prior port hit (skip guard lost, FileNotFoundError instead of skip).
    notes: >
      docs/reviews/tech-debt-port-boundary.md Phase 4 (Class E). DONE 2026-07-11:
      tests/unit/test_skip_guard_policy.py — file-level guard requirement over concrete
      drydocs/data/ + internal-local/ references (single-literal AND segment-chained
      construction both matched; glob literals like the port-manifest "drydocs/data/**"
      row excluded as pattern rows, not filesystem reads); checker self-test pins the
      port-failure shape (unguarded -> flagged, guarded/glob/clean -> not).

  - id: J9
    epic: release-infrastructure
    title: "End-to-end CSV -> Neo4j load integration test via testcontainers (opt-in, Docker-gated)"
    type: task
    module: drydocs-load
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [tests/, drydocs/loaders/, drydocs/data/samples/, pyproject.toml]
    acceptance: >
      An integration-marked pytest module spins a testcontainers[neo4j] instance, runs
      bootstrap + the M3 sample chain (bundled CSVs, no Oracle) end-to-end, and asserts
      the m3-verify invariants against the loaded graph — covering the Cypher-execution
      path (Neo4jClient.run/run_script + loader cypher) the unit suite never executes.
      Auto-SKIPS (never fails) when Docker is unavailable; excluded from the default
      `pytest tests/unit -q` run so its timing is unchanged; how-to-run documented
      (README or run-drydocs SKILL). Full unit suite stays green.
    notes: >
      Promoted 2026-07-11 under the "groom remaining non-HITL items" directive — parked
      since the 2026-07-01 review; testcontainers[neo4j] verified an unused dev dep
      2026-07-09. No HITL surface: sample CSVs + existing loaders only, no new edge
      meanings. DONE 2026-07-11 (same day): tests/integration/test_e2e_load.py —
      testcontainers Neo4j (community 5.26 + APOC), the REAL CLI chain (check/bootstrap/
      apply-ontology-supplement/ingest-controlm) via CliRunner, the m3-verify core
      invariants asserted directly + m3-verify smoke; integration marker deselected by
      default (pyproject addopts), auto-skips without Docker; run documented in README +
      run-drydocs SKILL. VERIFIED LIVE (3 passed, ~25s warm) — and the run CAUGHT TWO
      REAL BUGS, both fixed with regression pins: (1) BaseLoader's run()/runMany dispatch
      counted semicolons inside comments — the doc-06 audit-envelope comment in
      controlm_folders.cypher routed a single-statement template to apoc.cypher.runMany,
      which sheared it mid-comment (every folders load on stock APOC failed);
      _code_semicolons() now counts code semicolons only. (2) m3-verify's dupe check
      grouped (job_id, version_serial) — stale vs the (folder_id, job_id) NODE KEY;
      the sample's legitimate DLY/CYC promoted pair (job_id 22 in two folders) proved
      it; check now groups the composite.
      CORRECTED 2026-07-27: the fixture's Community image was wrong and the suite had
      rotted to 3 errors — constraints.cypher has declared `port_unique ... IS NODE KEY`
      since the 2026-05-05 initial commit, and NODE KEY is Enterprise-only, so `bootstrap`
      could not complete. (Community was never a supported target: ADR 0002 commits the
      project to EE and records Community single-DB only as a rejected alternative.) The
      fixture now reads its image from config/dev-environment.yaml — the declared single
      source of truth for local infra, so it cannot drift again — and sets
      NEO4J_ACCEPT_LICENSE_AGREEMENT=eval, matching the local neo4jtest container.
      Re-verified live 2026-07-27: 3 passed (~38s), full integration suite 6 passed.
      How the 2026-07-11 "verified live on community" run above ever passed is unresolved;
      the likeliest cause is that bootstrap's error handling changed underneath it (the D5
      client-side split gave each statement its own auto-commit transaction, where the
      earlier apoc.cypher.runMany path could swallow the constraint failure).

  - id: J10
    epic: release-infrastructure
    title: "Ruff cleanup: fix the pre-existing lint findings in stages, then flip the CI ruff step to blocking"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: [J5]
    inputs: [pyproject.toml, .github/workflows/ci.yml]
    acceptance: >
      AMENDED 2026-07-19 (formatter-convergence decision; the plan doc
      docs/ruff-format-convergence.md is the authority and carries the company-side
      instructions): staged producer-side execution using the committed
      commit-subject contract "J10 stage 0..5" — (0) pin ruff = "0.5.7" EXACT
      (caret removed) + settle [tool.ruff]/[tool.ruff.lint]/[tool.ruff.format] +
      add the numbered company-side step to docs/port-prompt.md (this commit
      PORTS); (1) ruff check --fix; (2) --unsafe-fixes hand-reviewed; (3)
      ruff format . + own SHA into .git-blame-ignore-revs with
      blame.ignoreRevsFile configured (stages 1-3 marked DO-NOT-PORT — company
      regenerates per the plan doc); (4) remaining findings per-rule in batches,
      keepers via explicit pyproject per-file-ignores with reason comments (ports
      normally); (5) ci.yml ruff step drops continue-on-error AND gains
      `ruff format --check` (ports, company-gated). End state: `poetry run ruff
      check .` exits 0 AND `poetry run ruff format --check .` exits 0 AND CI
      blocks both. Full suite green at every stage.
    notes: >
      DONE 2026-08-01 (laptop) — ALL SIX PRODUCER COMMITS ON main. End state met:
      `ruff check .` exits 0, `ruff format --check .` exits 0 over 266 files, CI
      blocks on both, suite 1270 passed / 7 skipped unchanged at every stage.
      Ran on branch fix/j10-ruff-convergence, --no-ff merged (a rebase would have
      invalidated the stage-3 SHA recorded in .git-blame-ignore-revs).
      COMPANY SIDE IS NOT STARTED and is not blocked by anything here — the
      instructions in the plan doc are live; stages 1-3 regenerate there, 0/4/5 port.
      .
      Rulings (user, 2026-08-01): timing GO; E501 ignored-with-reason as recommended;
      keeper set decided per-ORIGIN, which the 2026-07-19 sizing had not anticipated —
      177 of the 362 post-format findings (49%) were VENDORED Anthropic skill scripts
      (.claude/skills/{docx,pptx,xlsx,pdf,skill-creator}, three near-identical copies
      of the same office validators) and are now an extend-exclude, not a per-file-
      ignore; .claude/skills/groom-backlog is ours and stays in scope. 30 findings in
      our own code were FIXED rather than ignored.
      .
      Three things execution taught that the plan doc did not know, now recorded there:
      (1) ruff's F841 unsafe fix leaves DEAD CODE — it strips the assignment target but
      keeps the call, six times across three files; the diagnosis was right every time,
      the fix was not. (2) B017 was a REAL TEST DEFECT: pytest.raises(Exception) in
      test_plan_board.py passes on a typo'd attribute name too, so it never proved what
      its name claimed; narrowed to FrozenInstanceError. (3) RUF003 was hiding a latent
      ENCODING BUG — "â†’" in test_query_specs.py, a double-encoded "->"; a blanket
      RUF00x ignore would have preserved it. Tree sweep found no others.
      Also: E402's premise in the plan doc was WRONG (it assumed sys.path setup in
      scripts/; all 6 hits are pytest.importorskip guards in two test modules), and
      B008 had grown 14 -> 26 while CI was advisory.
      .
      The J16 fall-through guard EARNED ITS KEEP: .git-blame-ignore-revs (new in stage
      3) had no PORT-MANIFEST disposition and test_port_reconcile_guards.py failed
      exactly as designed. Now an explicit never-port row — a SHA is the one thing two
      disjoint histories can never share.
      .
      STAGE 0 DONE 2026-07-31 (`f18c88e`) + B023 RULED AND FIXED (`b3b90dc`).
      Stage 0 = ruff pinned "0.5.7" EXACT, [tool.ruff.format] written out, and
      `*.py text eol=lf` — the last is NOT optional: `line-ending = "lf"` without it
      makes `ruff format --check` unsatisfiable on a Windows checkout while passing on
      the Linux runner. B023's two hits were ONE site (benchmark_p0.py:206), not a live
      bug (same-iteration call), fixed with `marker=marker` because the failure mode
      under any later refactor is silently scoring every question against the last
      marker. STAGES 1-5 ARE NOW UNBLOCKED — the port boundary settled at
      PORT-REPORT-57914bf4 (merged 2026-08-01). Remaining gate is the user's two open
      calls (E501 residual policy; keeper set B008/RUF001-3/E402) — see the plan doc's
      "Open calls". Live baseline re-measured at 3882db5: 1,017 findings, 404 safe +
      42 unsafe, 284 of 328 files would reformat.
      Groomed 2026-07-13 from the 2026-07-11 IDEAS line (found executing J5 — the CI
      workflow runs ruff ADVISORY because gating would have required this mass fix
      inside a CI chore). TIMING (user flag preserved): the diff touches every Python
      file — execute during a port lull, coordinated with the company-port cadence
      (git-readme.md), never right before a port. Module `docs` chosen per the J5
      precedent (release/CI infrastructure work area); the fixes span every Python
      package, so no single component module fits.
      AMENDED 2026-07-19 (main session, user-approved approach): adopt ruff format
      INDEPENDENTLY on both sides — mechanical commits regenerate company-side
      instead of porting (disjoint histories make this clean; the port pipe uses a
      git bundle because the repo stays private — see git-readme.md's bundle note).
      Live baseline re-measured 2026-07-19: 838 findings (was ~757; CI advisory =
      debt grows), 358 safe-fix + 20 unsafe; ruff format trial (reverted): 203/237
      files, E501 337→206 — residual is prose in strings/comments the formatter
      never wraps. TIMING flag SOFTENED but start stays user-gated: port exposure
      is now two regenerable commits + the boundary protocol. Confirm at start
      (plan doc "Open calls"): timing; E501 residual policy (recommendation:
      ignore-with-reason); keeper set (B008 Typer defaults, RUF001/2/3 prose
      unicode, E402 scripts) — and B023 x2 reviewed individually as potential
      real bugs.

  - id: C7
    epic: ontology-mapping
    title: "Structured map fields at the next gate: vocab_id required, capture taxonomy-or-waived"
    type: chore
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [config/taxonomy-ontology-map.yaml, tests/unit/test_taxonomy_ontology_map.py, .claude/skills/add-source-object/SKILL.md]
    acceptance: >
      test_taxonomy_ontology_map extended: every mapping confirmed after 2026-07-10 must
      carry vocab_id (or explicit ~ with a reason note) AND a capture field naming its
      config/taxonomy/ file or waived-with-reason — the taxonomy-first erosion fix
      (audit F5; the platforms.yaml retrofit precedent); add-source-object step 3 and the
      ontology-mapper agent instructions updated to fill both.
    notes: >
      docs/reviews/tech-debt-taxonomy-ontology-map.md Phase 3.
      DONE 2026-07-19 (general-purpose subagent, reviewed in-session): two new guards in
      test_taxonomy_ontology_map.py (cutoff 2026-07-10 exclusive; vocab_id entry-level or
      ~ + vocab_id_reason; capture = existing config/taxonomy/ file or "waived — <reason>");
      both post-cutoff entries (job-seal-app-ref, job-runtime-stats-supplement) backfilled
      with honest F5 waivers — retrofitting real captures stays taxonomy-importer work.
      SKILL step 3 + ontology-mapper Method step 6 updated. Suite 721 green.

  # (J11/J12/C8 groomed 2026-07-15 — weekly inbox groom.)
  - id: J11
    epic: release-infrastructure
    title: "Move the resolved controlm-loader-flow baseline doc to docs/history/ and repoint inbound links"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [docs/controlm-loader-flow.md, README.md, internal/remediation/governance/command-line-and-variables-standard.md]
    acceptance: >
      docs/controlm-loader-flow.md is git-mv'd to docs/history/ keeping its 2026-07-15
      resolution banner (the M1-Fix-README precedent); the two inbound references are
      repointed (README.md + internal/remediation/governance/command-line-and-variables-
      standard.md — verified by grep 2026-07-15; the depgraph snapshot JSONs refresh
      themselves); a repo grep shows no stale docs/controlm-loader-flow.md path outside
      history/audit prose; full suite green.
    notes: >
      Groomed 2026-07-15 from the same-day IDEAS line (captured at the controlm docs
      status-refresh sweep e3e7bec: all §4 corrections resolved, resolution banner added —
      the doc carries no live content; the TDD + staging-flow §3a are the current
      descriptions). Inbound-linker correction during grooming: grep says README.md + the
      internal governance doc, NOT CHECKPOINT/reviews as the inbox line guessed.
      DONE 2026-07-19 (main session, direct): git-mv'd with banner intact; both inbound
      references repointed (README tree line + Further-reading line; governance doc link
      also depth-corrected — it was ../../ from a 3-deep dir, broken since authoring);
      the two banner-internal relative links gained ../ so they survive the move; a row
      added to docs/history/README.md's index table (M1-Fix-README precedent). Stale-path
      grep clean outside backlog/board/depgraph-snapshot prose. NOTE: J10 was next in
      queue order but is user-gated on port-lull timing (notes' preserved TIMING flag) —
      skipped, stays todo/ready.

  - id: J12
    epic: release-infrastructure
    title: "Fix the session-ritual render invocations: bare python -> poetry run python (CLAUDE.md + snapshot.ps1)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [CLAUDE.md, knowledge/depgraph-snapshots/snapshot.ps1]
    acceptance: >
      The CLAUDE.md session-ritual step 3 render commands (render_board.py +
      render_design_doc.py — lines 54-55 today) and snapshot.ps1's two `& python`
      invocations (lines 28/42) run via `poetry run python` (or an equivalently
      venv-robust invocation) so they work without an activated venv; snapshot.ps1
      executes end-to-end on this machine; a grep confirms no bare `python scripts/`
      ritual invocation remains in either file.
    notes: >
      Groomed 2026-07-15 from the 2026-07-14 IDEAS line. Re-verified live this session:
      bare `python scripts/render_design_doc.py` fails outside the venv
      (ModuleNotFoundError: drydocs); `poetry run python` succeeds. EXECUTION CAUTION:
      CLAUDE.md carried uncommitted user edits in the working tree at groom time —
      coordinate before committing so the fix doesn't sweep unrelated changes.
      DONE 2026-07-19 (main session, direct): all four render invocations now
      `poetry run python scripts/...` (CLAUDE.md ritual step 3 x2; snapshot.ps1
      lines 28/42). The groom-time caution was moot — tree was clean at execution.
      snapshot.ps1 verified end-to-end on this machine (board + 6 design docs +
      depgraph snapshot wrote with meta header; renders came out byte-identical to
      the committed ones; test snapshot JSON deleted as an artifact). snapshot.ps1's
      third `& python` (line 81, depgraph CLI) deliberately untouched — it runs in
      the separate depgraph repo where DryDocs' poetry env does not apply, and the
      acceptance scopes to the two render invocations. Acceptance grep clean: no
      bare `python scripts/` remains in either file.

  - id: C8
    epic: ontology-mapping
    title: "Resolve schema_graph.cypher staleness: regenerate from the vocabulary + drift guard, or mark point-in-time"
    type: chore
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs_core/schema/schema_graph.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, git-readme.md, docs/port-prompt.md]
    acceptance: >
      EITHER schema_graph.cypher is regenerated from relationship_vocabulary.yaml (the
      absent docs_* edges, m3_runs_on_*, seal_requires_scheduler, and the ACTIVE
      m3_seal_app_ref all present) with a test-enforced drift guard so it cannot silently
      stale again, OR it carries an explicit point-in-time banner (generated 2026-06-09)
      and every reference implying "regenerate when the vocabulary changes"
      (git-readme.md, docs/port-prompt.md, the file's own header) is reconciled to the
      chosen stance; which branch was taken is recorded in these notes. Presentation/
      hygiene only — no edge meaning changes, no gate needed (the vocabulary is the
      confirmed source; this file is a derived view). Full suite green.
    notes: >
      Groomed 2026-07-15 from the 2026-07-14 IDEAS line (found during the K2 build:
      generated 2026-06-09, the vocabulary has moved on, no drift guard covers it).
      The either/or is deliberately in the acceptance — decide at execution.
      DONE 2026-07-19 (general-purpose subagent, reviewed in-session): REGENERATE branch
      taken — file is pure derived view, nothing hand-authored. New deterministic renderer
      drydocs_core/ontology/schema_graph.py + scripts/render_schema_graph.py (render_board
      idiom) + byte-match drift guard tests/unit/test_schema_graph.py (7 tests). Inclusion
      rule: active+planned render (status on edge), deprecated/removed excluded; pipe-union
      endpoints fan out; wildcard from_node uses fixed exemplars. All acceptance edges
      present incl. ACTIVE m3_seal_app_ref. Not executed by bootstrap (manual Browser apply
      only — verified). git-readme/port-prompt refs left: historical port records, not
      policy claims (reconcile clause belongs to the untaken banner branch). Suite 728.

  - id: C9
    epic: ontology-mapping
    title: "Reconcile pat_product_mapping with the PAT model: teams map to BusinessApplications (team report), area-product alignment volatile + relationship-typed — retire or re-gate the deprecated DevTeam->Product write"
    type: task
    module: drydocs-load
    phase: 2
    agent: main
    model: fable
    priority: p1
    status: done
    depends_on: [C5]
    inputs: [drydocs/loaders/cypher/pat_product_mapping.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, docs/RELATIONSHIP_GUIDE.md, config/gate-log.md, config/source-registry.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      pat_product_mapping.cypher no longer writes edges whose vocabulary entry is
      deprecated (catalog_supports) or merely planned (catalog_has_application)
      without a gate decision making each ACTIVE or removing the write; existing
      DevTeam->SUPPORTS->Product edges are swept from live graphs if the
      deprecation stands. The PAT reality is modeled through the gate:
      DevTeam->BusinessApplication mapping from the PAT team report (source
      registered in config/source-registry.yaml with a locator; real SEAL ids and
      rosters stay in the internal/ twin per the publish boundary); area-product
      alignment stays volatile-aware (relationship type dedicated|aligned|flex on
      the edge, changes tracked rather than frozen); supporting vs sponsoring
      distinguished. Whether DevTeam->Product SUPPORTS survives as an
      independently-asserted PAT fact (PAT's own Supporting Product column — the
      C5 rule's exception path) or stays deprecated is THE gate question, decided
      by the SME and recorded in gate-log + vocabulary. Full suite green.
    notes: >
      Filed 2026-07-18 from the C5 gate session (gate same-row-derived-edges §c):
      the C5 audit found pat_product_mapping.cypher still writing the
      2026-06-21-deprecated catalog_supports edge on every load. SME context
      in-session with PAT screenshots (Internal-Confidential — held OUT of the
      repo): dev teams map to ONE OR MORE business applications via the PAT team
      report (the stable mapping to load); team->area-product alignment changes
      over time and is hard to pin; PAT models Supporting Product and Supporting
      Area Product as SEPARATE columns with a relationship type plus distinct
      sponsoring fields — so the deprecated edge may be independently asserted,
      not a same-row join-restatement. Model fable: re-gates SUPPORTS semantics
      AND onboards a source. p1 because every load re-creates a deprecated edge.
      2026-07-18 — DONE (gate pat-reconcile SIGNED OFF, same day). The SME's
      ladder reconciled: seal_ids feed the ACTIVE arch_develops (loader finally
      wired — no second label); catalog_supports re-activated FALLBACK-ONLY
      (no area product on the row); HAS_APPLICATION write removed (entry stays
      planned for a product-scoped extract); sponsoring extended to area
      products; per-team volatile-alignment sweep each load; anchor apps
      source:pat + m1-verify ports check scoped to source:SEAL; new m1-verify
      join-restatement guard. migrate_pat_alignment_c9.cypher applied live.
      Synthetic 4-row sample covers every ladder branch; refresh-reference
      chain now exercises the loader; live shape verified exact. Suite 718.

  - id: C17
    epic: ontology-mapping
    title: "PAT grain keying: the Product LINE grain is name-only at source while :ProductLine is id-keyed — decide picker-1 resolution, and reconcile the unqualified area_product_id + the third (name-only) sponsoring form"
    type: task
    module: drydocs-load
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: done   # 2026-08-01 (desktop) — §a/§b/§c ruled + built; gate as a whole still unsigned
    depends_on: [C9]
    inputs: [drydocs/loaders/catalog.py, drydocs/loaders/cypher/pat_product_mapping.cypher, drydocs/loaders/cypher/products.cypher, config/gate-prompts/seal-app-ref-edge-reshape.yaml, config/source-registry.yaml, config/gate-log.md]
    acceptance: >
      The keying asymmetry is resolved in writing, not assumed. (a) Picker-1
      resolution is ruled at the seal-app-ref-edge-reshape §G6-RIDER: either a
      product-line-scoped extract supplies product_line_id, or the resolver maps
      name -> id against the loaded :ProductLine set and REPORTS ambiguity
      (counts always reported, never silent) — a name must not become the
      de-facto key. (b) It is recorded which source column feeds
      area_product_id (Supporting vs Sponsoring), given the team report splits
      them and the member report does not — qualification is report-specific.
      (c) The third sponsoring form (Sponsoring Product Line, name-only) is
      either modeled or explicitly ruled out of scope, closing C9 §d's two-form
      coverage. (d) Whatever is decided lands in the row model + cypher + a test,
      and the gate-log carries the outcome. Full suite green.
    notes: >
      CLOSED 2026-08-01 — the item's premise was WRONG, and the SME fact that
      corrected it is what closed it. C17 read the PAT team report's name-only
      Product Line column as a property of the SOURCE; the user supplied the
      decisive fact in-chat — LoB, Sub-LoB, Product Line, Product and Area
      Product EACH carry a numeric id at source — so it is a property of the
      REPORT. §a therefore rules option (a) outright (a product-line-scoped
      extract supplies the id); name -> id resolution is NOT the mechanism, only
      a bounded migration path that must report ambiguity counts. Corroborated
      independently by internal/fcdo-reference ("native IDs at each level") and
      the company's own 5-field pat_lob_sublob_productline.csv. §b RECORDED:
      area_product_id is the SUPPORTING area product, sponsored_area_product_id
      the Sponsoring one; the two are co-populated, so cypher §3a/§3b firing
      independently is correct — the field name stays unqualified because the
      qualification is report-specific (the member report has one unqualified
      column), and the description carries the join. §c RULED OUT OF SCOPE:
      Sponsoring Product Line stays unmodelled while it is name-only, since
      modelling it means keying a :ProductLine on a name.
      THE FACT EXPOSED A LIVE DEFECT the rider never asked about: our node keys
      are strings, pydantic v2 does not coerce int -> str, and every catalog id
      field was a bare str — so a numerically typed read of the real feed
      rejects EVERY catalog row, not some (verified against the shipped models
      before the fix). attribution.py already carried this coercion for the
      Control-M keys; the catalog loaders never got it. _catalog_id refuses a
      fractional id rather than rounding, and normalizes integral floats (a
      nullable numeric column via pandas arrives float64, where a blind str()
      keys '12345.0' and MERGEs a duplicate beside the real node).
      COROLLARY APPLIED: having ruled the join is by id, the id join must not
      fail silently either. products.cypher hard-MATCHed the parent AFTER the
      Product MERGE, leaving an unparented Product with orphan:false from ON
      CREATE — a flag no code path could set true. Now OPTIONAL MATCH + a
      per-run flag + orphan_parent_product_line_id. The identical shape in
      product_lines/area_products was left alone and INBOXED: changing four
      loaders on one item's authority is a drive-by.
      NOT DECIDED: the Sub-LoB grain the same fact reveals (we model none) is an
      ontology decision belonging to the parked company-catalog back-flow item.
      Gate-log carries a PARTIAL RULING entry — the seal-app-ref-edge-reshape
      gate as a whole remains unsigned. Suite 1296.
      Filed 2026-07-27 from an SME review of the source report structure
      (captures Internal-Confidential — held OUT of the repo; structure only).
      The Team Details Report carries ID+Name for Product, Supporting Area
      Product, Sponsoring Area Product and Sponsoring Product, but Product Line
      and Sponsoring Product Line are NAME-ONLY. :ProductLine is keyed on
      product_line_id (required, min_length=1) and products.cypher MATCHes
      parent_product_line_id — so this extract cannot join product lines by key.
      That is precisely the FIRST dropdown of the §G6 mapping cascade, which
      makes it a build dependency for the steward screen, not a modeling nicety.
      The two sponsoring ID columns appear CO-POPULATED rather than exclusive —
      cypher §3a/§3b already fire independently, so the loader is right; confirm
      the model intends both. Depends on C9 because it re-opens that gate's §d
      coverage question with the source structure now visible.

  - id: C18
    epic: ontology-mapping
    title: "Delete the stale shadow catalog row models in drydocs_core/models/catalog.py (unused, already drifted past C9 §d) — one definition, imported like every other loader"
    type: chore
    module: drydocs-core
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done   # 2026-07-27 (CLI session) — shadow deleted + the wider no-shadow guard; also step 1 of the "did the gate land?" plan
    depends_on: []
    inputs: [drydocs_core/models/catalog.py, drydocs/loaders/catalog.py, tests/unit/test_pat_product_mapping.py, docs/adr/]
    acceptance: >
      Exactly ONE definition of each catalog row model survives, and the loaders
      import it the way every other loader does (from drydocs_core.models) or the
      shadow is deleted outright — no duplicate class names across the two
      modules. A test pins that pat_product_mapping's row model carries
      sponsored_area_product_id (the C9 §d field) and the ';' -> ',' seal_ids
      normalizer, so a future move cannot silently lose either. Full suite green;
      import drydocs.cli and drydocs --help both OK.
    notes: >
      Filed 2026-07-27. All 8 catalog row classes exist twice — in
      drydocs_core/models/catalog.py and again in drydocs/loaders/catalog.py —
      and NOTHING imports the drydocs_core copies. The shadow has already
      drifted past a signed gate: its PatProductMappingRow is missing
      sponsored_area_product_id (C9 §d, 2026-07-18) and lacks the ';' -> ','
      seal_ids normalizer the real PAT report needs. Because model_config is
      extra="ignore", switching to it would drop that column SILENTLY at
      validation and leave pat_product_mapping.cypher §3b permanently dead with
      no error — the "succeeds loudly, does nothing" family again, this time
      pre-armed rather than live. p2 not p3 because the ADR 0002-A-1 migration
      direction is TOWARD drydocs_core, so a Phase-C move makes the stale copy
      win by default. Every other loader (controlm_*, seal_*, manual_loads, cli)
      already imports from drydocs_core.models — the catalog loaders are the only
      ones defining models locally.
      2026-07-27 — DONE. drydocs_core/models/catalog.py DELETED (its 5 re-exports
      removed from models/__init__.py with the reasoning kept inline); nothing
      imported it, so no runtime change. Deleted rather than re-synced: two
      definitions is the defect, not which one is stale. Scoped deliberately
      SMALL — the live models stay in drydocs/loaders/catalog.py and the
      "catalog loaders are the odd ones out" inconsistency is left for the
      Phase-C move (ADR 0002-A-1), which should MOVE them, not copy.
      Generalized per the SME's step-1 direction: new
      tests/unit/test_no_shadow_definitions.py fails on ANY top-level class name
      defined in two production modules (drydocs/ + drydocs_core/, public names
      only — test-local fakes and private names are legitimately per-module),
      with an EMPTY allowlist that requires a written reason per entry, plus a
      path pin so the specific file cannot come back unnoticed. The C9 §d field
      and the ';'->',' normalizer were ALREADY pinned by
      test_pat_product_mapping.py — those pins tested the live copy, which is
      exactly why the shadow drifted invisibly; the guard closes that, no new
      pin needed. Suite 979 (+3), ruff-neutral, drydocs --help OK.

  - id: C19
    epic: ontology-mapping
    title: "ontology.cypher describes an SWO load step that cannot run — the referenced swo_sdlc_ontology.cypher was never created; fix the comment, record the subset question"
    type: chore
    module: ontology
    phase: 2
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs_core/schema/ontology.cypher, config/gate-prompts/self-documentation-code-graph.yaml]
    acceptance: >
      The comment above the 13 seeded SWO anchor terms (ontology.cypher:109
      today) no longer describes loading a file that does not exist —
      `ontology/reference/swo_sdlc_ontology.cypher` is referenced nowhere in
      the tracked tree (grep-verified), and the rewritten comment states
      reality: the anchors are the seeded set; a wider ~250-term SDLC subset
      WOULD HAVE TO BE BUILT (it is not a load step), and whether it is worth
      building is an OPEN question recorded in the comment, not silently
      dropped. Suite green; apply-supplements unaffected (comment-only edit).
    notes: >
      Found drafting the G33 gate spec (2026-07-27 inbox): §A4 scoped "the
      wider set" out, and this finding retroactively justifies it — the M0
      framing dates the dangling reference to the earliest phase. Context has
      moved since the sibling §E0 zero-consumers finding: G33 shipped the
      FIRST SWO consumer (IS_ENCODED_IN -> the Python term), so the anchors
      are no longer decoration. Whether the SDLC subset is built at all stays
      a user/SME call — this chore only stops the comment lying to readers.
      CLOSED 2026-07-28 (same day): comment rewritten — the 13 anchors ARE
      the seeded set, no wider load step exists; provenance recovered from
      docs/history/M0-README.md (the ~250-term file lived only in the
      pre-repo SAVEPOINT and was never ported — a port that never happened,
      not a file that vanished); the open build-it-at-all question recorded
      in the comment itself, plus the first-consumer state (G33 Python
      binding live; Shell/SQL await G22 rider R1). The stale "Streamlit
      pages" claim went with it. Remaining grep hits for the old path are
      correct by design: gate-log.md:1014 + gate §A4 QUOTE the finding on
      append-only audit surfaces, and docs/history/* accurately describe the
      savepoint plan. Comment-only edit; regenerating gates.json picked up
      C19's gate-prompt input (unblocks list) — drift introduced at the
      groom, caught by test_gates_json. Suite green.

  # ── Epic Q — Document ingestion & doc-graph benchmarks (docmeta) — Phase 14 ─
  # Born 2026-07-16: the Essential GraphRAG ebook groomed on user-decided intents,
  # with the phase-14 plan change user-approved in-session (the phase-12/13 idiom).
  # The docmeta component plan (knowledge/upgrade-plans/docmeta-component.md) grooms
  # its P1+ phases HERE once the P0 WRITTEN verdict + the docmeta ADR land.
  - id: Q1
    epic: docmeta
    title: "Mine the Essential GraphRAG ebook for applicable patterns; feed the docmeta P0 benchmark verdict"
    type: task
    module: reference
    phase: 14
    agent: reference-librarian
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-16 same day (reference-librarian on sonnet, dispatched by main)
    depends_on: []
    inputs: ["Essential-GraphRAG.pdf (repo root — local-only, gitignored)", reference/research/README.md, knowledge/upgrade-plans/docmeta-component.md]
    acceptance: >
      A notes file under reference/research/ (link-cited, classification External, no
      copyrighted full text pasted — the research README house rule) inventories the
      book's applicable patterns at chapter/section level: lexical-graph construction,
      retrieval strategies (traversal / hybrid / vector), agentic traversal, and
      evaluation method — each mapped to its DryDocs consumer (which docmeta P0
      benchmark arm it informs; whether it confirms or corrects the neo4j-skills
      graphrag approach). The docmeta plan's P0 section gains a pointer to the notes,
      and the user's open question ("are there more examples of how to do it
      properly?") gets an explicit answer with the list.
    notes: >
      Groomed 2026-07-16 from the same-day Essential-GraphRAG [source] inbox line after
      the user decided housing + intent. Registration EXECUTED at the groom: research
      README seed-table row (Manning link verified 2026-07-16) + the root-level /*.pdf
      gitignore blanket rule (root-images precedent; tracked UI-WIP/*.pdf unaffected).
      Book: Bratanič & Hane, Manning 2025 (Neo4j-sponsored ebook), 179 pp.
      DONE 2026-07-16: reference/research/essential-graphrag-notes.md written by
      reference-librarian (full-text pypdf extract read chapter-by-chapter; no
      copyrighted passages pasted) — 8-chapter pattern inventory with page cites,
      consumer-mapping table (P0 arm / confirms-corrects / Q2 relevance), and the
      SME question answered: YES, 8 worked patterns beyond current bmc-docs practice
      (notes §3). Headline finding: ch.8's RAGAS + Cypher-as-ground-truth design fills
      the P0 acceptance's previously unnamed accuracy methodology. Pointer added to
      the docmeta plan's P0 row by main (the plan is outside the librarian's write
      surface). Chunking + HITL entity-resolution stances independently CONFIRM the
      bmc-docs deterministic lexical load; communities/GDS stay correctly deferred.

  - id: Q2
    epic: docmeta
    title: "Experiment: load the Essential GraphRAG ebook as a Document→Chunk lexical graph; test agent traversal over it"
    type: task
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-16 same day (main session; loaded live + traversal session run)
    depends_on: []
    inputs: ["Essential-GraphRAG.pdf (repo root — local-only, gitignored)", drydocs/loaders/bmc_docs.py, drydocs/loaders/cypher/bmc_docs.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, config/source-registry.yaml]
    acceptance: >
      The ebook is text-extracted (pypdf — the ingest_jpmc_reports.py precedent) and
      chunked with the DETERMINISTIC lexical pattern the bmc-docs gate accepted
      (section-based, seq-0 preamble; no LLM/embeddings in the load), then loaded under
      a NEWLY registered source entry (config/source-registry.yaml, classification
      External; the confirmed flip cites this groom's user decision or a mini-gate note
      in config/gate-log.md — decided at execution) REUSING ONLY active docs_*
      vocabulary (docs_chunk_part_of / docs_first_chunk / docs_next_chunk;
      docs_describes only if a registry SoftwareProduct target genuinely applies) —
      zero new relationship types; any new edge meaning aborts to the HITL gate.
      Target DB decided at execution — drydocs (co-locate, the bmc-docs precedent) vs
      ddcontext (the user's "our own unstructured context graph" phrasing) — with the
      choice + rationale recorded in these notes. Then the experiment: an
      agent-traversal test session answers >=5 retrieval questions by graph traversal
      over the loaded chunks (MCP read tools or an ADK agent), with transcript +
      findings recorded as docmeta P0 benchmark evidence (the traversal arm) and
      pointed to from the docmeta plan. Full suite green.
    notes: >
      Groomed 2026-07-16 (same inbox line as Q1; user intent: "an experiment to load
      into our own unstructured context graph for testing agent traversal").
      Deliberately vocabulary-reusing — the bmc-docs-lexical-load gate (2026-07-08)
      activated the lexical model, so no gate is needed unless a new edge meaning
      appears. The one methodological delta from bmc_docs (which chunked .md by H2) is
      PDF sectioning — extraction choices belong in the run notes.
      DONE 2026-07-16. TARGET DB DECIDED: ddcontext — experiment/reference content
      stays out of the ground-truth drydocs DB; matches the user's "our own
      unstructured context graph" phrasing + the jpmc-reports ddcontext precedent
      (drydocs co-location rejected: the book is not operational ground truth and
      has no SoftwareProduct DESCRIBES target). CONFIRMED FLIP: registry-note
      citation of the Q2 groom user decision (covering gate bmc-docs-lexical-load;
      no gate-log entry — no new edge meaning). Built: EssentialGraphragAdapter/
      Loader (pdf-lexical-v1: 8 chapter openers via "This chapter covers", monotonic
      section scan immune to glued running heads, appendix A.1-A.4, front/back
      matter) + essential_graphrag.cypher (bmc_docs minus DESCRIBES, + chapter/
      section/page_start) + CLI load-essential-graphrag (--database, default
      ddcontext) + registry/audit-fields/LEDGER_PENDING entries + 8 unit tests
      (synthetic pages + skip-guarded real-PDF test). Loaded live: 43 chunks,
      42-edge NEXT_CHUNK chain, 0 rejects, idempotent re-run rows_changed=0.
      Traversal session: 7/7 questions via MCP read-cypher with USE ddcontext
      (structure/sequence/aggregation/provenance exact; content scans bounded at
      exact-substring — the vector arm's case; front-matter pollution lesson).
      Evidence: docs/reviews/essential-graphrag-traversal-experiment.md, pointed
      from the docmeta plan P0 row. Suite 667 passed / 6 skipped.

  - id: Q3
    epic: docmeta
    title: "Write the docmeta P0 benchmark verdict: traversal vs manifest-routed markdown vs vector RAG, with a build/shrink recommendation"
    type: task
    module: docs
    phase: 14
    agent: main
    model: fable
    priority: p1
    status: done          # DONE 2026-07-16 same day (main fable session; benchmark run live, verdict = BUILD)
    depends_on: [Q1, Q2]
    inputs: [knowledge/upgrade-plans/docmeta-component.md, reference/research/essential-graphrag-notes.md, docs/reviews/essential-graphrag-traversal-experiment.md, external/orchestration/bmc-controlm/SOURCE-MANIFEST.md]
    acceptance: >
      The docmeta plan's P0 acceptance is met by a WRITTEN comparison document
      (knowledge/upgrade-plans/docmeta-p0-verdict.md, linked from the plan's P0 row):
      a FIXED support-question set (>=10 questions spanning the ch.8-style stage
      classes: structure/aggregation, exact lookup, paraphrase/conceptual, multi-doc,
      provenance, out-of-scope) is run over the LIVE bmc-docs corpus for every arm
      that can run in this environment — graph traversal (Cypher over Document→Chunk)
      and manifest-routed markdown at minimum, plus a full-text-index arm as the
      no-embedding lexical component of the vector arm; the embedding-proper vector
      arm may be assessed analytically ONLY with the blocker named (no committed
      embedding/LLM key — the open LLM-key-strategy inbox question). Per-question
      per-arm scoring covers context recall (ground-truth marker presence), token
      cost (retrieved chars), and latency; the document closes with an explicit
      build / shrink-to-registry-only recommendation and names what the docmeta ADR
      (P1) should adopt. Any throwaway index created for the benchmark is dropped or
      its retention recorded. Full suite green.
    notes: >
      Groomed 2026-07-16 on the user's "start the P0 written verdict" order — the
      phase-14 capstone the phase goal names; model fable per the matrix (the
      build-vs-shrink recommendation is a component-boundary decision, the G3/O5
      precedent). The verdict + the docmeta ADR are the recorded gate for promoting
      docmeta P1-P3 to this epic (IDEAS docmeta line + the plan's own instruction).
      DONE 2026-07-16: knowledge/upgrade-plans/docmeta-p0-verdict.md — VERDICT =
      BUILD. Benchmark run live: the current EE container was found WITHOUT the
      docs corpus (fresh O6 bootstrap), so the P0 spike's own load step ran first
      (software-registry 9 rows, bmc-docs 374 chunks / 0 rejects into drydocs);
      12 fixed questions x 3 live arms (handwritten-Cypher traversal, throwaway
      fulltext index — created + DROPPED, DB left as found, manifest-routed file
      reading), vector-proper assessed analytically (blocker named: the open
      LLM-key-strategy question). Adjudicated: traversal 12/12 recall (~16.4k
      chars total, median 75ms; only arm answering aggregation/provenance/version
      classes; paraphrase needs term-mapping — naive CONTAINS 0-row runs recorded);
      fulltext 7/12 (ranking dilution, paraphrase gap, confident noise on
      out-of-scope); manifest 10/12+2-partial at ~449.5k chars (~27x traversal).
      Two scorer artifacts corrected in adjudication + disclosed. ADR-adoption
      list + limitations written. Plan P0 row updated; IDEAS docmeta line updated:
      P1-P3 now groomable. Suite green.

  # (Q4–Q6 groomed 2026-07-18 — the docmeta plan's P1–P3, promoted on the recorded
  # trigger: Q3's P0 verdict = BUILD. P4–P7 stay plan-tracked until these land.)
  - id: Q4
    epic: docmeta
    title: "Docmeta gate session + the docmeta ADR: component name (docmeta vs deepdoc fold-in), drydocs_docs DB, planned doc-graph vocabulary entries"
    type: task
    module: ontology
    phase: 14
    agent: main
    model: fable
    priority: p2
    status: done          # DONE 2026-07-18 — gate SIGNED OFF in-session, ADR 0006 ACCEPTED
    depends_on: [Q3]
    inputs: [knowledge/upgrade-plans/docmeta-component.md, knowledge/upgrade-plans/docmeta-p0-verdict.md, drydocs_core/ontology/relationship_vocabulary.yaml, docs/decisions/, config/gate-log.md]
    acceptance: >
      The docmeta ADR is authored (next free number — the plan's reserved "0004" was
      taken by the software-registry ADR) adopting what the P0 verdict names, and is
      SME-accepted with the status flip recorded; the gate session decides component
      name (docmeta vs drydocs-deepdoc fold-in), the drydocs_docs DB target, and the
      curation-ladder->gate mapping; the plan's doc-graph relationship entries
      (HAS_DOCUMENT, HAS_CHUNK, DESCRIBES, GOVERNED_BY) are reconciled against the
      already-active docs_* vocabulary (no double registration) and any new entries
      land status: planned ONLY — nothing flips confirmed outside the gate;
      config/gate-log.md carries the session record.
    notes: >
      Groomed 2026-07-18 — docmeta plan P1. Model fable per the matrix: component
      boundary + ontology decisions (the G3/O5 precedent). The bmc-docs gate
      (2026-07-08) already activated docs_chunk_part_of / docs_first_chunk /
      docs_next_chunk / docs_describes — the gate reconciles, not re-registers.
      DONE 2026-07-18 same day: ADR 0006 authored PROPOSED, SME reviewed
      in-session and ACCEPTED AS WRITTEN on all four recommended options —
      (a) separate drydocs-docmeta component (deepdoc consumes); (b) NEW dddocs
      DB (drydocs_docs renamed to the live dd* convention; bmc-docs corpus
      re-targets at P4 via idempotent reload); (c) vocabulary reconciled:
      HAS_CHUNK superseded by the active lexical shape, docs_has_document +
      docs_governed_by registered planned (+ DocSource node classification),
      chunk-level DESCRIBES deferred to its own gate at extraction design;
      (d) curation ladder -> gate mapping + the P0-verdict adoption list.
      Gate-log entry appended; decisions README row added; plan §1.1 annotated.
      Nothing flipped active — both new entries are K2-pattern flips-are-
      follow-ups at their P4+ loader builds.

  - id: Q5
    epic: docmeta
    title: "Doc-source registry ledger: config/doc-source-registry.yaml + guard test; backfill every ingested corpus; house or evict root-level stray PDFs"
    type: task
    module: config
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [Q4]
    inputs: [knowledge/upgrade-plans/docmeta-component.md, config/source-registry.yaml, config/classification.yaml, reference/research/README.md]
    acceptance: >
      config/doc-source-registry.yaml exists with a tests/unit/test_doc_registry.py
      guard enforcing classification + connector + curation tier on EVERY entry; all
      currently ingested doc corpora (bmc-docs, essential-graphrag, jpmc reports)
      are backfilled; remaining root-level stray PDFs are registered, housed, or
      gitignored (the Q1 /*.pdf precedent) — zero unregistered corpora. Full suite
      green.
    notes: >
      Groomed 2026-07-18 — docmeta plan P2, the ledger house pattern (machine-
      readable config + a test that fails on drift). Sequenced after the Q4 gate so
      the registry schema matches what the ADR adopts.
      2026-07-18 — DONE (commit 3e424f5, same-day as the gate). Ledger built on
      ADR 0006 semantics: target_db dddocs|ddcontext only, curation FIXED per
      tier (§4 ladder — the guard derives it, entries can't soften their gate),
      External => source_url+captured_at. Backfilled bmc-docs (dddocs target,
      P4 reload note), essential-graphrag + jpmc-reports (ddcontext; jpmc was
      the unregistered pre-docmeta ingestion — captured_at from e419ec7).
      Guard adds an INGESTED_CORPORA pin: a loaded-but-unregistered corpus
      fails CI. Root strays: only the gitignored+now-registered ebook PDF;
      tracked PDFs are the two UI-WIP wireframes (purposeful home). Suite 712.

  - id: Q6
    epic: docmeta
    title: "Port A: bkup scrapers -> the producer docmeta component (cleaner/tokenizer/manifest/registry models; web + filedrop connectors; component boundary row)"
    type: task
    module: drydocs-docmeta
    phase: 14
    agent: main
    model: fable
    priority: p2
    status: done          # DONE 2026-08-04 (LAPTOP) — drydocs_docmeta/ landed with both generic connectors, the Q12 refusal built in
    depends_on: [Q4, Q5]
    inputs: [knowledge/upgrade-plans/docmeta-component.md, MODULE_MAP.md, tests/unit/test_module_boundary.py, git-readme.md]
    acceptance: >
      The docmeta component package exists under the name the Q4 gate decided (the
      modules-registry comment updated if it differs from the drydocs-docmeta
      working name): cleaner/tokenizer/manifest/registry-models ported from the bkup
      scrapers per the plan's §5 port table, with web + filedrop connectors; its own
      COMPONENT_GROUP in tests/unit/test_module_boundary.py + MODULE_MAP row;
      imports drydocs_core only (components never import each other); Track-1
      portable tests cover parse/clean/hash offline (no network); boundary guard +
      full suite green. The web connector does NOT ship without Q12's page-count
      refusal — an unguarded bulk scraper is not an acceptable intermediate state
      (see Q12 for why: the trees in scope enumerate 2k-11k pages).
      CONNECTOR SHAPE (merged 2026-08-02, from the company-side answer to tracker
      T21): the two generic connectors are ACQUISITION ONLY —
      `fetch(source) -> list[RawPage]`, with cleaning and hashing downstream — over
      a `Connector` protocol plus a `RawPage` model and an unavailable-source error;
      `web` speaks public http(s) through stdlib urllib with an INJECTABLE TRANSPORT
      and an SSRF scheme allow-list; `filedrop` reads a file or a directory of
      md/txt/html through pathlib. The transport injection and the allow-list are
      NON-NEGOTIABLE, not preferences: the first is what makes the Track-1 offline
      tests real rather than network-dependent, and the second is the same guardrail
      Q12 exists to enforce. Reproduction is producer-authored against that protocol
      shape — never a copy of the other tree's files.
    notes: >
      Groomed 2026-07-18 — docmeta plan P3. Module drydocs-docmeta registered at
      this groom as a WORKING NAME (the drydocs-api precedent) — final at the Q4
      gate; this item renames the registry entry if the gate folds it into
      drydocs-deepdoc. Model fable per the component-boundary precedent (O5/G3).
      MERGED 2026-08-02 (weekly groom), from the [source->Q6] inbox line: the
      fetcher already exists company-side, and the split lands exactly where this
      item needs it — web and filedrop have ZERO internal dependencies, while the
      confluence connector and the separate scrapers CLI are internal-realm and stay
      there. Their own base.py designates web+filedrop as the "wired and
      offline-testable" generic pair, i.e. the boundary this port would have had to
      draw was drawn by the authors first, which is the strongest available signal
      that a reproduction is clean. Merged into the acceptance rather than opened as
      an item because it specifies work Q6 already owns. Unblocks three things at
      once: Q6 itself, R7 (released unbuilt 2026-08-01 for exactly this missing
      fetcher) and Q12 behind it. Direction rule stands — back-flow is SANITIZED
      REPRODUCTION, never a copy.
      .
      DONE 2026-08-04 (LAPTOP). PRODUCER-AUTHORED against the shape the T21 answer
      describes — the bkup repo is not mounted on this machine and the company tree
      was never read, so every line here was written from the plan's §5 table and
      the recorded connector contract. The T21 port row is discharged.
      .
      PATH: drydocs_docmeta/, NOT the plan's drydocs/docmeta/. That path predates
      the Phase B relocate; every component created since (remediation, lineage,
      deepdoc, api) is a top-level package while `drydocs` is the component
      REMAINDER. Recorded as an amendment note on ADR 0006 rather than a body edit
      (the ADR 0002 banner precedent) — the confirmed NAME and the separate-component
      ruling are untouched, only the physical path.
      .
      BUILT: connectors/base.py (the `Connector` protocol, `RawPage`, `FetchSource`,
      `SourceUnavailableError`) + web.py + filedrop.py + cleaner.py + tokenizer.py +
      manifest.py + registry.py + policy.py; `docmeta` COMPONENT_GROUP, MODULE_MAP
      row, PORT-MANIFEST row, enforcement-matrix surface row, pyproject package.
      46 offline tests — no network, no credentials, no data root (Track-1).
      .
      THE TWO NON-NEGOTIABLES ARE TESTED AS BEHAVIOUR, not as presence. The
      injectable transport is what makes that possible: `test_web_refuses_above_the_
      ceiling_without_fetching_anything` asserts the TRANSPORT WAS NEVER CALLED, not
      merely that an exception was raised — a refusal that still fetched would pass
      the weaker test. Same for the SSRF allow-list: a run containing one file:// URL
      fetches NOTHING, including the good URLs before it, because a run that got 300
      pages in before hitting it has already done the thing the list exists to stop.
      .
      Q12's clause is satisfied by construction rather than by a later item: the
      ceiling is enforced against the fully resolved location list BEFORE the first
      request, in the connector itself. See Q12 for the config half.
      .
      THREE DELIBERATE DEPARTURES from the plan's sketch, all recorded in the plan:
      (a) no T4 connector STUBS — a stub that raises is indistinguishable at a call
      site from a connector that is merely misconfigured, and this epic has spent
      three items on things that succeed loudly and do nothing; the producer ships
      the protocol and the company implements against it. (b) freshness.py is not a
      module yet: its primitive IS manifest.diff(), because the digest and the
      comparison over digests are one fact; what P5 adds is cadence and a queue.
      (c) The bkup curation vocabulary and the last_synced_from authority ladder live
      in registry.py as CODE vocabularies mapped onto the HITL gate positions, not as
      new ledger fields — they describe a DOCUMENT's state, and the ledger declares
      SOURCES. Adding columns to the YAML would have been a schema change nobody
      asked for.
      .
      NO GRAPH WRITE. Acquisition only; the load path is P4. The component writes
      nothing outside DRYDOCS_DATA_ROOT.

  # ── appended at the 2026-07-26 groom run (docs-residency design session) ───
  # Eight items from the 2026-07-26 chat that followed G28/G29/G30. The session
  # theme was ONE failure pattern found three times — "succeeds loudly, does
  # nothing": G29 (a supplement that runs and seeds no terms), G30 (a spec that
  # reads a database nothing writes), and Q8 below (an OPTIONAL MATCH whose
  # target class is in another database). Q8 is the ready-now bug; G32 is the
  # decision everything else waits on.

  - id: Q7
    epic: docmeta
    title: "Reconcile the doc-source registry against what is actually loaded: per-corpus declared / loaded / stale / missing status"
    type: task
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [config/doc-source-registry.yaml, drydocs/cli.py, drydocs_core/schema/provisioning/01_databases.cypher, tests/unit/test_doc_registry.py]
    acceptance: >
      A `drydocs docs-verify` (name at implementer's discretion) reports one row per
      config/doc-source-registry.yaml entry: the declared target_db, whether that
      corpus is present in that database, its loaded Document/Chunk counts, and a
      status of loaded | missing | stale | wrong-db. `stale` = the registry's
      recorded sha256/captured_at no longer matches what the graph carries (the
      ADR 0006 §4 gate-preserving freshness rule); `wrong-db` = the corpus is
      present in a database OTHER than its declared target_db. Exits non-zero on
      any wrong-db row. Covers every corpus kind the registry admits — scraped web
      pages, filedrop PDFs, .msg/email extracts — because the check is registry-
      driven, not per-loader. A unit test drives it against a fake client.
    notes: >
      DONE 2026-08-01 (laptop, concurrent with a desktop R-epic session — claimed in
      a module that session was not touching). `drydocs docs-verify` +
      drydocs/docs_verify.py (pure; `run(database, cypher, params)` is the only I/O
      seam) + 16 offline tests. Suite 1330.
      .
      THE DESIGN PROBLEM THE ITEM DID NOT KNOW ABOUT: the registry is corpus-keyed
      and THE GRAPH IS NOT. Only the Q13 vendor-docs loader writes `corpus_id`;
      bmc_docs identifies pages by repo-relative `path` and essential_graphrag writes
      one Document whose `doc_id` IS the corpus id. A "registry-driven, not
      per-loader" check (the acceptance's own words) is therefore impossible unless
      the registry says HOW to find each corpus — so every entry now carries
      `graph_locator: {match: corpus_id|doc_id|path_prefix|none, value: ...}`,
      guarded by a new test_doc_registry case. Teaching the verifier three loader
      shapes instead would have made it a per-loader registry that rots silently.
      .
      SIX STATUSES, NOT THE FOUR NAMED — two live situations cannot be described by
      loaded|missing|stale|wrong-db, and calling either "missing" would be a FALSE
      diagnosis: (a) `db-absent` — bmc-docs and bmc-controlm-utilities declare
      target_db `dddocs`, which schema/provisioning/01_databases.cypher HAS NEVER
      CREATED (it makes drydocs, ddlineage, ddcontext, ddall). That is not a load
      failure, it is G32's open topology ruling, so it reports and does not fail.
      (b) `unshaped` — jpmc-reports loaded as :DataAsset slices, never on the
      Document->Chunk spine; its documents are not missing, they were never
      documents. Only wrong-db exits non-zero, per the acceptance.
      .
      LIVE RUN — laptop, container `neo4jtest` (localhost:7687), 2026-08-01:
      2 db-absent (both dddocs entries) · 1 loaded (essential-graphrag in ddcontext,
      1 Document / 43 Chunks) · 2 unshaped. Exit 0. NOTE THE MACHINE: bmc-docs
      reports "not found in any existing database either" HERE, which is a statement
      about this laptop's graph, not about the corpus — the two machines hold
      separate graphs and git syncs neither.
      .
      Two accuracy defects in the first cut, both caught by reading the live output
      rather than the tests: `unshaped` said "no graph_locator declared" for entries
      that declared `match: none` on purpose (a ruling read as a silence), and
      `db-absent` short-circuited before probing, so it never reported WHERE the
      corpus actually is — the more useful half. Both fixed; the second is now the
      difference between "your target is unprovisioned" and "...and it is sitting in
      drydocs meanwhile".
      .
      Freshness is never silently reported as fresh: the older loaders write no
      captured_at, and the row says "uncheckable" rather than implying a comparison
      that did not happen.
      .
      User-requested 2026-07-26 ("we'll need to have a registry comparison or
      status to check if it's loaded into the db... scraped pages - pdf - etc").
      The gap is real: doc-source-registry.yaml DECLARES corpora and
      test_doc_registry.py enforces the declaration's shape, but nothing anywhere
      checks that a declared corpus was ever actually loaded, or that it landed in
      the database it declared. The `wrong-db` case is the one worth the non-zero
      exit — it is the G30 failure class at corpus granularity.
      MODULE CHOICE: drydocs-load, not drydocs-docmeta, because the verb lives in
      cli.py beside m1-verify/m3-verify and must work BEFORE the docmeta component
      exists (Q6). Re-home it to docmeta if that component takes over corpus state.
      Startable now against the corpora that exist today (bmc-docs, essential-
      graphrag, jpmc-reports); the multi-DB cases arrive with G32.

  - id: Q8
    epic: docmeta
    title: "BUG: the bmc-docs DESCRIBES edge is silently dropped when SoftwareProduct is absent — a whole-corpus miss is indistinguishable from success"
    type: bug
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p1
    status: done   # 2026-07-27 (CLI session) — loader-side prereq refusal + per-run missing-edge report; 6 new tests
    depends_on: []
    inputs: [drydocs/loaders/cypher/bmc_docs.cypher, drydocs/loaders/bmc_docs.py, tests/unit/test_bmc_docs.py]
    acceptance: >
      A bmc-docs load in which ZERO :SoftwareProduct nodes are reachable fails
      loudly instead of reporting success with no DESCRIBES edges — either a hard
      error or a summary count the caller asserts on. Per-ROW misses (a single
      misspelled subject_product_id) must still be tolerated as they are today,
      so the fix distinguishes "this one row found no product" from "the product
      registry is not in this database at all". A test proves both halves: one row
      with a bad product id still loads its Document/Chunks and reports the miss;
      an empty-registry graph fails.
    notes: >
      MEASURED 2026-07-26, drydocs/loaders/cypher/bmc_docs.cypher:97 —
      `OPTIONAL MATCH (sp:SoftwareProduct {product_id: row.subject_product_id})`
      guarded by `FOREACH (_ IN CASE WHEN sp IS NOT NULL ...)`. The template's own
      header already warns that a misspelled id silently drops the edge for that
      row; the same mechanism fires for an entire absent database and nobody
      noticed. Harmless TODAY (both the corpus and the software registry live in
      `drydocs`), which is exactly why it is worth fixing before anything moves:
      ADR 0006 already plans to re-target bmc-docs to `dddocs` at the P4 loader
      build while load-software-registry keeps writing `drydocs`, and a
      relationship cannot span databases — so that move would silently drop EVERY
      DESCRIBES edge and take the vendor→product routing spine with it. Third
      instance this session of "succeeds loudly, does nothing" (see G29, G30).
      Ready now and independent of the G32 topology ruling.

  - id: Q9
    epic: docmeta
    title: "Re-file the Essential GraphRAG corpus as VENDOR documentation for Neo4j: dddocs + a DESCRIBES product hook, amending ADR 0006 §2"
    type: task
    module: config
    phase: 14
    agent: pipeline-config
    model: sonnet
    priority: p2
    status: in_progress   # 2026-08-19 (laptop) — batch-claimed with its two siblings (the G60/G83/G84 precedent; all three unblocked by G31 the same evening), pushed before work per the C19 rule
    depends_on: [G31]
    inputs: [config/doc-source-registry.yaml, docs/decisions/0006-docmeta-component-and-doc-graph.md, config/gate-log.md, drydocs/loaders/cypher/essential_graphrag.cypher]
    acceptance: >
      config/doc-source-registry.yaml#essential-graphrag carries target_db: dddocs,
      a taxonomy_path under the graph-platform/neo4j branch, and its
      `taxonomy_path: ~` + "deliberately no DESCRIBES / product taxonomy hook" note
      replaced with the vendor rationale; the loader template's DELIBERATELY-NO-
      DESCRIBES block is replaced with the bmc-docs product hook (which requires Q8
      to be done first, or the hook drops silently); a config/gate-log.md entry
      records the ADR 0006 §2 amendment with the SME's reasoning; ADR 0006 gains a
      short amendment note rather than a body rewrite (the ADR 0002 banner
      precedent). test_doc_registry.py stays green.
    notes: >
      SME decision in session 2026-07-26, reasoning recorded verbatim in intent:
      the book is Neo4j-SPONSORED and teaches implementation ON their graph — "it's
      not written to load into a competitor like TigerGraph" — so it is vendor
      documentation wearing a book jacket, tier T1, same as bmc-docs. The existing
      registry entry already agrees on every OTHER axis (tier: T1, curation: none,
      trust_default: GROUNDED) and differs only in target_db, which it inherited
      from the jpmc-reports ddcontext precedent rather than from the tier model.
      The filing error was by FORMAT (a PDF book) rather than by SUBJECT (Neo4j the
      product) — the same class of error as the ddcontext placement itself.
      This OVERTURNS a signed-off decision (ADR 0006 §2, SME 2026-07-18), so it
      goes through the gate as an amendment, never by edit — the discipline G30
      established. depends_on G31 because dddocs must exist and carry a
      SoftwareProduct proxy before the hook can bind.

  - id: Q10
    epic: docmeta
    title: "Ingest the failure/activity email corpus (.msg + Copilot JSON) with an SME surface for assigning an email to a folder or process"
    type: requirement
    module: drydocs-docmeta
    phase: 14
    agent: main
    model: fable
    priority: p2
    status: in_progress   # 2026-08-19 (laptop) — batch-claimed with its two siblings (the G60/G83/G84 precedent; all three unblocked by G31 the same evening), pushed before work per the C19 rule
    depends_on: [G31]
    inputs: [config/doc-source-registry.yaml, docs/restructure/03-hitl-sme-flow.md, config/gate-prompts/, drydocs_core/ontology/relationship_vocabulary/]
    acceptance: >
      A doc-source-registry entry declares the email corpus with its classification,
      connector: email, tier, curation ladder, target_db and retention note; the
      corpus loads as the ACTIVE lexical shape (Document → Chunk) with the source
      .msg/JSON pair referenced by path, never copied into the repo; and an email
      whose folder/process is NOT extractable is loadable WITHOUT an assignment,
      landing unassigned rather than being rejected or guessed. The assignment edge
      (email Document → ControlMFolder / process) is NEW relationship semantics and
      goes through the HITL gate: registered `status: planned` in
      relationship_vocabulary.yaml with a gate-prompt spec, and no loader writes it
      until the gate confirms. The SME assignment surface itself may be a later
      slice — this item's floor is "ingested, unassigned, and gate-registered".
    notes: >
      User source 2026-07-26: "we get emails that reference a failure or activity
      and I've extracted those as json files and save the original msg format...
      it may require an SME assigning it to a particular folder or process if it's
      not extractable from the email."
      RETENTION, and it is the reason the source pair matters: Outlook purges at
      6-18 months, but the extracts are deliberately preserved on a file server
      until the PROCESS OR PROJECT retires — so after ~18 months the file server
      holds the only copy and becomes a system of record with a backup obligation,
      not a cache. Purge is therefore project/process-scoped (a property-scoped
      delete), NOT a database drop — which is why retention did NOT end up being an
      argument for a separate database, contrary to the initial guess in that
      session. Same instinct as the "always keep the original requirement" rule.
      CLASSIFICATION: production-failure email almost certainly Internal-
      Confidential (real names, real folder names, incident detail). The .msg/JSON
      originals live OUTSIDE the repo — the gitignored-PDF precedent — and only
      mechanism-level references are committed.
      Distinct from the 2026-07-22 email-DL contact-point idea already in IDEAS:
      that one is about DL membership as an ontology mapping; this is the email
      BODY as a document corpus.
      SIBLING CORPUS NOW EXISTS — READ BEFORE BUILDING (2026-08-05, user
      flagged the relation): `adhoc-sme-email` was registered in
      config/doc-source-registry.yaml for a one-off SME evidence email, with
      connector: email and citation-only shape. This item's acceptance says "a
      doc-source-registry entry declares THE email corpus", singular, so the
      first decision here is EXTEND vs ADD — not a discovery to make mid-build.
      Four properties differ (shape: citation-only vs the ACTIVE Document→Chunk
      spine; cadence: one-off vs recurring feed; retention: only Q10's extracts
      carry the post-purge backup obligation; assignment: only Q10 needs the
      email→folder/process edge), which is why they are two entries today. If
      the SME folds them, the shape difference becomes a per-Document property
      — cheap now, expensive once either has loaded.

  - id: Q11
    epic: docmeta
    title: "Model document supersession and currency so 2-3 stale versions of the same internal process page are distinguishable by more than a date"
    type: requirement
    module: ontology
    phase: 14
    agent: ontology-mapper
    model: fable
    priority: p3
    status: in_progress   # 2026-08-19 (laptop) — batch-claimed with its two siblings (the G60/G83/G84 precedent; all three unblocked by G31 the same evening), pushed before work per the C19 rule
    depends_on: [G31]
    inputs: [docs/RELATIONSHIP_GUIDE.md, drydocs_core/ontology/relationship_vocabulary/, config/doc-source-registry.yaml]
    acceptance: >
      A SUPERSEDES (or gate-chosen spelling) Document→Document relationship plus a
      currency signal on :Document is registered `status: planned` in
      relationship_vocabulary.yaml with a decision-matrix entry and a gate-prompt
      spec; a retrieval over a multi-version corpus can return ONLY the current
      version without relying on captured_at ordering alone. No loader writes the
      edge until the HITL gate confirms it.
    notes: >
      User problem statement 2026-07-26: "internally i've seen 2 or 3 different
      versions with the same referenced person/team/process, but is stale and the
      only indication is date."
      The point that decided this is its own item rather than a rider on the
      database question: QUARANTINING DOES NOT DISAMBIGUATE. Putting all three
      versions in an unstructured-content database leaves all three versions
      sitting together, still ambiguous — the separation protects OTHER corpora
      from them, it does not tell you which one is current. That is a modeling
      problem and needs modeling, whatever the topology ends up being. Hence
      depends_on G31 only for where the nodes live, not for the semantics.

  - id: Q12
    epic: docmeta
    title: "Doc-capture scale guardrail: the web connector refuses a bulk scrape above a threshold instead of silently fetching thousands of vendor pages"
    type: task
    module: drydocs-docmeta
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-08-04 (LAPTOP) — the refusal shipped 2026-07-31 in scripts/; this close moved its numbers into config and gave the component's connector the same rule
    depends_on: [Q6]
    inputs: [config/doc-source-registry.yaml, knowledge/upgrade-plans/docmeta-component.md]
    acceptance: >
      The web-capture entrypoint resolves its FULL target page list from the
      publisher's own manifest (e.g. the Author-it `toc.json`) BEFORE issuing any
      content request, and prints tree, page count, and estimated bytes + wall-clock
      at the configured delay. It REFUSES to proceed (non-zero exit, count named in
      the message) when the resolved count exceeds a configured threshold unless an
      explicit opt-in flag is passed; the threshold and the per-request delay are
      config values, never hardcoded literals. A `--dry-run` prints the same plan
      and exits 0 having fetched no content. Unit tests drive refusal-above and
      pass-below against a fixture manifest with NO network (the Track-1 portable
      rule). No capture script may land before this guard exists — Q6's acceptance
      carries the same clause.
    notes: >
      User-requested 2026-07-31 after a live feasibility probe of the BMC help
      trees: "stop or warn the user if they try to scan that many with the script."
      The numbers are the argument, and they were measured, not estimated —
      documents.bmc.com publishes a machine-readable `toc.json` per tree, so the
      page count is knowable BEFORE fetching anything, which is exactly what makes
      a pre-flight refusal cheap and a post-hoc abort unnecessary:
      /supportu/INC/ (INCONTROL mainframe) = 10,912 pages ~210 MB raw;
      /supportu/9.0.20/ (distributed, complete) = 3,860 pages ~74 MB;
      /supportu/9.0.21/ (distributed, trimmed — no Utilities book) = 1,965 pages
      ~38 MB. Sampled pages averaged ~19 KB raw / ~6.9 KB extracted text.
      Two distinct harms the guard prevents, both real: hammering a third-party
      vendor site (robots.txt ALLOWS these paths, so politeness is entirely on us —
      there is no server-side backstop), and committing tens of MB of vendor
      copyright into a repo that is private-but-sometimes-published. A THIRD hazard
      motivated the request and belongs in the warning text: the operator can pick
      the wrong tree entirely and not notice. /supportu/INC/ is the INCONTROL
      MAINFRAME family (Control-D, Control-O, Control-M/Tape, JCL Verify) — its
      "Control-M" subtree at 40498.htm is Control-M **for z/OS** administration
      (CTMPLEX/Sysplex, started tasks, user exits, allocated datasets), NOT the
      distributed estate DryDocs models. So the pre-flight summary must name the
      TREE and its top-level books, not just a number — a count alone would not
      have caught this.
      .
      DONE 2026-08-04 (LAPTOP), and it closed in two halves rather than one build.
      THE REFUSAL ITSELF ALREADY EXISTED: scripts/external_vendor_scrape.py shipped
      it 2026-07-31 (e549f07) with --dry-run, the manifest-resolved page list, the
      book-by-book pre-flight naming the tree, and 14 offline tests — built ahead of
      this item's dependency because the capture needed it that day. What was NOT
      met was the acceptance's other clause, and it is the one that matters for a
      guardrail: "the threshold and the per-request delay are config values, never
      hardcoded literals". They were module constants (DEFAULT_MAX_PAGES = 600,
      DEFAULT_DELAY_SECONDS = 1.0).
      .
      WHY THAT CLAUSE IS NOT PEDANTRY, and the reason this closed WITH Q6 rather
      than before it: Q6 gave the repo a SECOND door to the same third party — the
      component's web connector. Two doors with two literals is two different
      notions of "too many pages", decided by which entrypoint the operator
      happened to use. So the numbers moved to config/doc-capture.yaml (schema
      drydocs.doc-capture.v1: max_pages, delay_seconds, timeout, retries,
      user_agent, and the SSRF scheme allow-list), both consumers read it, and a
      test asserts BOTH files reference CapturePolicy so a future third door
      cannot quietly re-hardcode. The refusal type moved with it — one rule, one
      exception class, raised from one place.
      .
      The scraper's own fetch() now also enforces the scheme allow-list, which it
      never did: it would have followed a file:// URL in a tree definition.
      .
      Registered as an enforcement-matrix surface (config/doc-capture.yaml ->
      scripts/external_vendor_scrape.py + drydocs_docmeta/connectors/web.py), so
      the config-surface guard covers it like every other governed config file.

  - id: Q13
    epic: docmeta
    title: "STANDALONE repeatable vendor-docs ingestion + loader: capture -> convert -> load a NAVIGABLE spine (TOC hierarchy + page_role + abstract), taxonomy only"
    type: task
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-08-04 (LAPTOP) — pipeline built 2026-07-31 (9713118), three defects found and fixed at the close verification
    depends_on: []
    inputs: [knowledge/upgrade-plans/vendor-docs-agent-navigation.md, config/doc-source-registry.yaml, drydocs/loaders/bmc_docs.py, scripts/external_vendor_scrape.py]
    acceptance: >
      A STANDALONE, REPEATABLE pipeline — its own converter, loader, cypher and
      `drydocs` verb, NOT a widening of load-bmc-docs (user ruling 2026-07-31:
      "unique enough to use a stand alone, but repeatable ingestion and loader").
      It reads the capture manifest as its contract, so capture / convert / load
      are three re-runnable stages and re-running any of them is idempotent
      (MERGE on doc_id; a second full run reports zero net change). The
      converter strips the Author-it/ExtJS navigation chrome and emits body
      markdown; the loader writes :Document with doc_id, title, breadcrumb,
      source_url, sha256, captured_at, a deterministic `abstract` (first
      paragraph) and a derived `page_role`; the toc.json hierarchy loads as
      IN_SECTION / SUBSECTION_OF; chunks keep the established
      (:Chunk)-[:PART_OF]->(:Document) + NEXT_CHUNK shape. A TOC node bearing a
      fragment (`page` + `anchor` in the manifest) attaches to the EXISTING
      document as a section — it must NOT mint a second :Document. `page_role`
      derivation is an explicit title rule, NEVER an LLM, and the run REPORTS
      its unclassified count rather than defaulting pages silently (the G16
      values-decide pattern). Every node carries doc_version and
      version_verified=false. NO meaning edges — no :ControlMUtility, no
      DOCUMENTS, no SEE_ALSO (Q14's gate). Counts reported per role.
    notes: >
      Layer 1 of knowledge/upgrade-plans/vendor-docs-agent-navigation.md, and the
      half that needs NO ruling because it only transcribes the publisher's own
      structure — the taxonomy-before-ontology rule in CLAUDE.md §1. Worth doing
      alone: hierarchy + role + abstract already beats similarity-search-only,
      which is all the corpus supports today.
      STANDALONE, decided 2026-07-31: load-bmc-docs cannot be stretched to cover
      this and should not be. It consumes hand-written markdown, splits on H2,
      and infers a provenance TIER from heading text — a heuristic that exists
      only because that corpus is our lossy paraphrase. This corpus is VERBATIM
      throughout, so there is no tier to infer, and its real structure (TOC
      hierarchy, page_role, anchors) has nowhere to go in that loader's model.
      Two loaders with honest contracts beat one with a mode flag.
      REPEATABLE means the capture manifest is the contract between stages:
      scripts/external_vendor_scrape.py already emits it (sha256 per document,
      page/anchor per TOC node), so a recapture after a version bump re-runs the
      same convert+load with no code change.
      MODULE: drydocs-load beside bmc_docs.py, not drydocs-docmeta — the Q7
      precedent (it must work BEFORE the docmeta component exists at Q6).
      The version properties are load-bearing, not bookkeeping: the capture is
      9.0.20 (BMC ships NO Utilities book in the trimmed 9.0.21 publication)
      while the estate runs 9.0.21.300, so the graph must make "the 9.0.20 docs
      say X, unverified for your estate" the only answer it can produce. The
      config-side half of that already exists — see Q16.
      .
      DONE 2026-08-04 (LAPTOP). The pipeline itself landed 2026-07-31 in 9713118
      — converter, loader, cypher, both CLI verbs, 41 offline tests — and the
      status was never flipped, so the repo went on advertising Q13 in
      next_ready for four days. This close is the VERIFICATION that flip should
      have carried, and it found three defects. None of them would fail a test
      or a load; all three are the "succeeds loudly, does nothing" reporting
      class this epic hit at G29, G30 and Q8.
      .
      (1) THE LOADER COULD NOT REPORT A CHANGE. vendor_docs.cypher had no
      WAS_GENERATED_BY provenance tail, and BaseLoader derives rows_changed by
      counting the edges a run attached — so it reported rows_changed=0 on a
      FIRST load exactly as on a no-op re-run, which makes this item's own
      idempotence evidence ("a second full run reports zero net change")
      unfalsifiable. `to_params` had been computing row_checksum all along and
      nothing read it. Fixed with the delta-only tail (bmc_docs /
      controlm_folders idiom), placed ABOVE the SUBSECTION_OF block because that
      block's UNWIND of an empty list drops every row at TOC depth <= 1 from the
      rest of the statement — the tail-ordering trap bmc_docs.cypher documents,
      and a test now pins the ordering.
      .
      (2) A CAPTURE IS NOT A CORPUS, and the loader wrote the wrong one. It set
      corpus_id to the CAPTURE id (bmc-controlm-9.0.20-utilities) while this
      item's own input, config/doc-source-registry.yaml, declares
      `graph_locator: {match: corpus_id, value: bmc-controlm-utilities}` — so
      Q7's `docs-verify` would have reported a fully loaded corpus as MISSING.
      A false negative in the one check built to stop false claims. It survived
      because nothing has ever run both halves together. Now every row carries
      BOTH: corpus_id resolved REGISTRY-DRIVEN (the entry naming this capture in
      its `manifest:` path — never a string transform of the capture id, and an
      unregistered capture refuses per docmeta invariant 1), and capture_id for
      which fetch produced the row. A test asserts the loaded id equals what the
      registry's locator searches for, so the two can no longer drift apart.
      .
      (3) doc_id WAS THE BARE TOPIC STEM ("16200"), globally unique across every
      capture. Author-it reuses topic ids across publications, so loading the
      already-registered 9.0.21 Parameters tree would have MERGEd onto the
      9.0.20 Utilities documents and overwritten doc_version — silently
      destroying the one distinction this corpus exists to carry. doc_id and
      section_id are now capture-scoped. "MERGE on doc_id" stays literally true;
      it is doc_id that got an honest identity. Free to change because nothing
      is loaded anywhere yet — the build-order argument G31 makes.
      .
      Also closed the literal acceptance gap: doc_version + version_verified now
      ride :Chunk and :DocSection, not only :Document, so a chunk that surfaces
      alone in a retrieval result still carries the version caveat (Q15 returns
      chunk-grain rows; this is what lets those rows cite honestly).
      .
      NO LIVE RUN — ON EITHER MACHINE, and this is the honest state rather than
      an untested one (J18). Two independent blocks: the registry entry is
      `confirmed: false`, so `_gate_loader` refuses the write until an SME gate
      signs the doc-graph shape; and the 1016-page capture lives in the
      DESKTOP's data root (DRYDOCS_DATA_ROOT is machine-local and git syncs
      none of it), so this laptop has no payload to convert. Evidence here is
      offline and reproducible anywhere: 60 tests over a fixture capture,
      including two full loads through a recording client proving the checksums
      are identical across runs — which is precisely the condition under which
      the new delta-only tail fires zero times. The graph-side count stays
      unproven until a machine holding the capture runs it post-gate.
      .
      Registry entries for the two remaining trees: the 9.0.21 Parameters tree
      in scripts/external_vendor_scrape.py has NO doc-source-registry entry, so
      convert refuses it by design rather than inventing a corpus id.

  - id: Q14
    epic: docmeta
    title: "Draft the vendor-docs entity-spine gate prompt: is a documented utility a first-class node, and which edge carries it"
    type: task
    module: ontology
    phase: 14
    agent: ontology-mapper
    model: fable
    priority: p2
    status: todo
    depends_on: [Q13]
    inputs: [knowledge/upgrade-plans/vendor-docs-agent-navigation.md, drydocs_core/ontology/relationship_vocabulary/, config/gate-prompts/, docs/RELATIONSHIP_GUIDE.md]
    acceptance: >
      A gate prompt in config/gate-prompts/ puts four questions to the SME with
      evidence drawn from the LOADED layer-1 spine (real counts, real titles):
      (a) is a documented utility a new :ControlMUtility label, or an instance of
      the existing software-registry model — inventing a label for convenience is
      what CLAUDE.md §1 warns against; (b) DOCUMENTS as a new edge vs reusing the
      registered :Document->:SoftwareProduct DESCRIBES; (c) whether a vendor's own
      in-page cross-link is an assertion we carry as SEE_ALSO or merely
      presentation; (d) the layer-3 estate join (job CMD_LINE -> the page
      describing its utility) recorded as BLOCKED on G32 with the reason stated.
      Every proposed term is registered `status: planned` in
      relationship_vocabulary.yaml with a decision-matrix entry; NOTHING is
      flipped and no loader writes an edge. The SIGN-OFF is a separate SME
      session — this item only drafts (the G27 precedent).
    notes: >
      Layer 2 of the plan. Depends on Q13 so the gate argues from loaded evidence
      rather than from a proposal — the pattern that made the K2/P2 gates
      decidable in one sitting.
      Layer 3 is deliberately NOT its own item: Q8's close note established that
      a relationship cannot span Neo4j databases, and ADR 0006 re-targets doc
      corpora to dddocs while the software registry keeps writing drydocs, so the
      estate join would silently drop every edge. G32 owns that ruling; this
      item's job is to record the dependency honestly, not to route around it.

  - id: Q15
    epic: docmeta
    title: "Agent navigation QuerySpecs over the doc spine: name-addressable lookup, section browse, role siblings, search as FALLBACK"
    type: task
    module: drydocs-api
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Q13]
    inputs: [knowledge/upgrade-plans/vendor-docs-agent-navigation.md, drydocs_api/, web/src/ask/, web/src/routes/AskRoute.tsx]
    acceptance: >
      Four registered QuerySpecs — docs.utility-lookup.v1 (exact name -> docs
      grouped by page_role), docs.section-browse.v1 (section -> siblings, one
      hop), docs.role-siblings.v1 (doc -> same-subject docs of another role), and
      docs.search.v1 (full-text/vector, explicitly the FALLBACK for "I don't know
      the name"). All four: bounded expansion with no unbounded variable-length
      traversal; every row carries source_url + doc_version + trust so the R5
      citation surface renders provenance; chunk text is NOT returned by default
      — the agent asks for it as a deliberate second step. Proven against the
      loaded corpus, and reachable from /ask without touching /raw-cypher.
    notes: >
      Section 4 of the plan. The point is that an agent usually arrives already
      knowing the noun ("how do I use exportdefjob?"), and a vector search for a
      known exact identifier is both slower and less reliable than an indexed
      exact match — so similarity search is demoted to the fallback path rather
      than being the only door in. Chunk-free by default because agent context is
      the scarce resource: triage on abstract + page_role, spend context on text
      only after choosing.
      Specs over raw Cypher per the R4/R5 design (ephemeral specs + citations
      carrying trust and classification) — this keeps doc answers citable and
      keeps agents off /raw-cypher.

  - id: Q16
    epic: docmeta
    title: "Software -> documentation coverage: every product's docs, their residency, their load state and their blockers - including version drift"
    type: task
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-08-07 (LAPTOP): PARTIAL CLOSE, stated as such - clause (a)/(a2)/(a3)/(c)/(d)/(e) SHIPPED; clause (b), the pointer reaching the GRAPH, is NOT done and is blocked behind Q14 (the term) which is behind G32 (the residency ruling). Read the close note before treating this `done` as "the whole item shipped". Claimed and PUSHED before work earlier the same day, with the acceptance WIDENED at the claim - see notes
    depends_on: [Q13]
    inputs: [config/taxonomy/software-registry.yaml, config/doc-source-registry.yaml, config/source-registry.yaml, drydocs/docs_verify.py, tests/unit/test_software_registry.py, drydocs/cli.py]
    acceptance: >
      (a) A COVERAGE REPORT — its own verb, `drydocs docs-coverage` — prints one
      row per software-registry product, WHETHER OR NOT it carries a
      `documentation:` block. A product with no documentation at all is the row
      this report exists to print: exactly one product of thirteen carries that
      block today, so the original documentation-block-only scope could not
      answer "what documentation do we hold for Airflow". Each row states the
      product's coverage against a ladder whose first match governs (no-corpus |
      unregistered-corpus | ungated | cross-db-blocked | no-locator | not-loaded
      | product-node-absent | loaded-no-edge | traversable-until-move |
      traversable | not-probed), a `blockers` list naming EVERY wall rather than
      only the governing one, and the currency columns: runtime `versions`,
      `docs_version`, every runtime version NOT in `current_for`, and a currency
      of current | drifted | unverified | no-docs.
      `cross-db-blocked` sits ABOVE every load state in the ladder, so a corpus
      that cannot reach an edge is never reported as merely `not-loaded`.
      Exits non-zero on drifted and on unregistered-corpus so a version bump or
      a broken pointer cannot pass unnoticed in CI — and DELIBERATELY NOT on
      no-corpus or cross-db-blocked, which are true statements about the world
      that G32 owns; failing CI on an open design ruling trains people to
      ignore the verb (the test_software_registry.py known-state-pin precedent).
      (a2) Two further sections, so nothing is counted only from the product
      side: one row per doc-source-registry corpus that no product names, and
      one row per source-registry SYSTEM with no product row — which is where
      Snowflake appears, being absent from the software registry entirely.
      (a3) The report must work with the database SWITCHED OFF: the cross-DB
      determination is arithmetic on two YAML fields, so a pure declaration
      layer answers "is an edge even POSSIBLE" without Neo4j. Graph probing is
      an optional injected second layer, and every graph-derived field is None
      (`not-probed`) when it does not run — never 0, which would be a false
      claim of absence.
      (b) The pointer reaches the GRAPH so an agent can traverse from the product
      it is asked about to the documentation describing it, carrying
      docs_version + the currency status — via the term Q14's gate confirms
      (DESCRIBES is already registered for :Document -> :SoftwareProduct; this
      may need nothing new, which is the cheapest possible answer and should be
      checked FIRST). Nothing is written until that gate signs.
      (c) `current_for` is only ever set by a human confirming a capture against
      a runtime version — never derived by parsing version strings.
      (d) The two never-silent tails are reported rather than dropped: a corpus
      no product declares, and a DESCRIBES edge that exists in the graph with no
      declaration behind it. The second is a live case, not hypothetical — see
      the notes.
      (e) The report projects a CLOSED field set — ids, enum states, integer
      counts, declared classification labels, repo-relative paths already
      committed in publishable config. It never emits `source:` / `notes:` /
      `source_url:` free text from either registry, because three corpora are
      classification Internal and their prose describes internal systems. Those
      entries are still counted and named by id — dropping them would itself be
      a silent default — but their prose never reaches the output.
    notes: >
      User requirement 2026-07-31: "we do need a pointer to it from the software
      registry which should contain external-vendor version number, so that when
      our ingestion process moves to the next major version, we know we need to
      update the docs."
      THE CONFIG HALF IS ALREADY BUILT (same session): the `controlm` product in
      software-registry.yaml carries `documentation: {corpus, docs_version,
      current_for}` beside its existing `versions:`, and two guards in
      test_software_registry.py enforce that the corpus id resolves to a real
      doc-source-registry entry and that the drift is COMPUTABLE (both sides
      present and typed). The mechanism is deliberately declaration-based:
      when the estate moves to a new version it lands in `versions:` and is
      absent from `current_for`, so the gap falls out with no version-string
      heuristics — because "does 9.0.20 documentation still describe 9.0.21.300?"
      is a judgement, not a string comparison.
      The drift-pinning guard asserts the KNOWN state rather than failing on it:
      drift is a true statement about the world today (9.0.20 docs, 9.0.21.300
      runtime), not a code defect, so a red suite would be dishonest. What the
      test prevents is the drift changing SILENTLY. The operational alarm is
      this item's report.
      Home chosen beside `versions:` on the product rather than only on the doc
      corpus so the comparison is LOCAL — the two numbers a human must compare
      sit on adjacent lines.
      ACCEPTANCE WIDENED 2026-08-07 (laptop) at the claim, from the user's
      question "when we have a tool like Apache Airflow or Snowflake and the
      internal runs AWS MWAA with its own reference documentation, what connects
      them all with a Cypher query?". Answering it exposed that (a) was scoped to
      products CARRYING a `documentation:` block — exactly ONE of thirteen — so
      the report could not have printed the Airflow row, which is the row the
      question was about. Widened rather than groomed as a second item: same
      join, same inputs, same dependency, and two half-reports over one join
      would grow two status vocabularies that drift.
      WHAT THE ANSWER TURNED OUT TO BE, recorded because it is the report's
      subject matter. ONE edge connects them: (:Document)-[:DESCRIBES
      {target_version}]->(:SoftwareProduct), status active, written by
      bmc_docs.cypher. There is NO Document->Vendor edge; the vendor hop rides
      MADE_BY. It resolves for exactly one product — 27 Documents, all controlm.
      Airflow is a product node with zero docs; SNOWFLAKE IS NOT IN THE SOFTWARE
      REGISTRY AT ALL.
      THE FOUR BREAKS, none of which is a bug: (1) the product->corpus pointer is
      YAML-only — software_registry.py's rows() never reads the `documentation:`
      block, which is this item's own (b); (2) the doc-source registry has NO
      graph representation — :DocSource is not a live label and HAS_DOCUMENT is
      planned with loader ~; (3) the 1016-page vendor-docs corpus is staged and
      gate-blocked, and vendor_docs.cypher DELIBERATELY writes no DESCRIBES edge
      because a relationship cannot span Neo4j databases (the Q8 finding) while
      the registry writes drydocs and ADR 0006 re-targets corpora to dddocs;
      (4) therefore NO corpus can currently declare its way to a traversable
      edge — target_db is constrained to {dddocs, ddcontext} by the doc-registry
      guard and the registry writes drydocs. G32 owns that residency ruling.
      THE FINDING THAT MAKES THE REPORT WORTH MORE THAN A QUERY: the one working
      traversal is UNREPRODUCIBLE FROM THE LEDGER. Those 27 edges come from a
      corpus no product declares, attributed by a hardcoded Python constant
      (bmc_docs.py SUBJECT_PRODUCT_ID = "controlm"), resident in a database it
      did not declare (bmc-docs declares dddocs, lives in drydocs). It works
      BECAUSE OF the residency accident G32 exists to end — so acceptance (d)
      requires reporting it, and the report must NOT infer the declaration from
      taxonomy_path, since no file maps a taxonomy path to a product id.
      (b) IS UNCHANGED AND STILL BLOCKED behind Q14's gate, which is behind G32.
      The widening touches (a) only.
      .
      CLOSED 2026-08-07 (laptop) as a PARTIAL close, said out loud rather than
      implied. WHAT SHIPPED: `drydocs docs-coverage` + drydocs/docs_coverage.py
      (555 lines) + 25 tests, commit b297268 — the eleven-state first-match
      ladder, the `blockers` list naming every wall, the currency columns, the
      two tail sections (a2), the database-off declaration layer with
      `not-probed` rather than 0 (a3), the human-only `current_for` (c), both
      never-silent tails (d) and the closed field set (e). Non-zero on drifted
      and on unregistered-corpus only, per the acceptance. docs_coverage is
      classified `load` in test_module_boundary (default-deny) and listed in
      MODULE_MAP.md. Two follow-on commits from the same session: 0ddf880 emits
      the doc-governance + product-documentation fields into
      web/src/generated/software-registry.json and load-map.json, and 9b4cf59
      builds the /software console page over them — that page is groomed as its
      OWN item, O56, because no item had ever claimed it.
      WHAT DID NOT SHIP, and is the reason this close is qualified: clause (b).
      No DESCRIBES edge is written from a registry declaration, because the term
      is Q14's gate to confirm and the residency that makes any such edge
      possible is G32's ruling to make — target_db is constrained to
      {dddocs, ddcontext} while the software registry writes drydocs, so today
      NO corpus can declare its way to a traversable edge. Writing one anyway
      would be exactly the "invent a relationship during import" failure
      CLAUDE.md §1 forbids. (b) therefore needs a SUCCESSOR ITEM once G32 rules;
      the nearest live piece of it — replacing bmc_docs.py's hardcoded
      SUBJECT_PRODUCT_ID with a `describes_product:` registry field — is parked
      in the IDEAS.md inbox as Idea-88 rather than groomed, because it touches a
      loader and a gated corpus behind those same two rulings.
      WHERE (b) IS NOW TRACKED, added 2026-08-07 on the user's ruling so it is not
      traceable only by this prose: G32's notes carry a CLOSE CHECKLIST whose
      first entry is exactly this clause — when that ruling signs, it must either
      mint (b)'s successor item or record (b) as declined. That puts the pickup on
      the item that unblocks it rather than on a reader happening to re-open this
      close note.
      VENUE (J18): the report is sample-reproducible — `drydocs docs-coverage`
      runs against the committed YAML with the database OFF and reproduces every
      declaration-layer row; the graph-probed columns in this session's run were
      taken on the laptop, `neo4jtest`, `drydocs` DB.

  - id: G14
    epic: component-topology
    title: "Lineage extractor file-ops pass: CMD_LINE move/gzip parsing emits READS_FROM/WRITES_TO candidates (activates G13's dormant resolution)"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-20 (main session, same day as the pull)
    depends_on: [G12, G13]
    inputs: [drydocs_lineage/extractors/controlm_inventory.py, drydocs_core/controlm/commands.py, drydocs_lineage/writer.py, tests/unit/test_lineage_inventory.py, config/gate-log.md]
    acceptance: >
      The inventory extractor emits READS_FROM / WRITES_TO candidates from CMD_LINE
      file-operation parsing (the unix move/copy/gzip wrapper forms named in the
      2026-07-15 lineage-vocabulary gate caveat — "unix file operations (move, gzip)
      with no ETL engine involved"), dst as DataAsset; endpoints exactly per the gate
      EDIT (ETLProcess | ControlMJob -> DataAsset), no new relationship types. G13's
      owning-job resolution activates on the feed: a unit test proves a job whose
      CMD_LINE moves a file plans job->asset edges in plan_curated output.
      Unparseable/skipped file-ops are counted, never dropped silently (the coverage
      house rule), and WritePlan.unresolved_file_ops is surfaced on a human review
      surface (lineage-review page or plan report) so the drop count no longer sits
      unread. Refusal semantics unchanged — m3_reads_from / m3_writes_to stay planned
      until the curated-load gate. Full suite green.
    notes: >
      Groomed 2026-07-19 from two G13-subagent inbox findings: the missing-feed line
      (only INVOKES/TRIGGERS emitted today, so G13's resolution logic is built +
      tested but dormant) promoted as this item; the sibling
      surface-unresolved_file_ops line FOLDED into this acceptance rather than
      itemized separately — the feed is what makes the counter worth reading. Edge
      meanings were confirmed at the 2026-07-15/16 gates; pure candidate-side
      extraction, no ontology decision remains, hence sonnet.
      DONE 2026-07-20: core parser gains gzip/gunzip -> COMPRESS (deterministic
      .gz-twin derivation; -c stream forms keep tgt None) beside the existing
      MOVE/COPY; the extractor's new file-ops pass emits READS_FROM/WRITES_TO
      with the JOB as the Activity (the gate EDIT's file-ops case — CMD_LINE
      ops have no Script hop, so G13's resolution passes them through to the
      ControlMJob MATCH shape), operands -> local_file DataAssets verbatim.
      Skips counted by reason (file_ops_skipped_non_dataflow / no_operand;
      DELETE/MKDIR/TRANSFORM/OTHER = job mechanics, not flow). Surfacing:
      writer.unresolved_file_op_candidates() (mirrors the plan-time drop) +
      a fifth lineage-review assertion row, so the counter is read pre-plan;
      the CLI's coverage line carries the new counters for free. Activation
      proven by test_cmdline_file_op_feed_plans_job_to_asset_edges; live
      lineage-review smoke rendered 4 candidates + 3 assets. Vocab untouched
      (all four m3_* stay planned); suite 761 green.

  - id: G15
    epic: component-topology
    title: "Lineage extractor: capture the DPL launcher argument contract (key params as properties; --pipeline-id spelling + launcher-shell variants)"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 — all 5 acceptance points tested, suite 766 green
    depends_on: [G14]
    inputs: [drydocs_core/controlm/commands.py, drydocs_lineage/extractors/controlm_inventory.py, drydocs_lineage/model.py, tests/unit/test_lineage_inventory.py]
    acceptance: >
      The DPL invocation parse captures the launcher's key parameters, verified by
      unit tests on synthetic CMD_LINEs modeled on the observed prod grammar
      (SME screenshots 2026-07-21): (a) the on-prem shell launcher spellings
      (dpl_spark_processor and its _dynamic variant) classify as DPL alongside the
      registered dt-launcher.sh / dt-pipelines-launcher jar; (b) BOTH GUID
      spellings key ETLProcess identity — the observed single-dash `-pipeline`
      form and the argument-contract `--pipeline-id` form (the GUID is the ONLY
      literal on otherwise fully-variable CMD_LINEs, hardcoded for ~99% of jobs);
      (c) an UNRESOLVED original whose executable is a folder variable
      (%%PY_LAUNCH / %%SCRIPT_PATH prefix) still yields DPL classification +
      GUID identity via the -pipeline literal, without crashing on the %%VAR
      tokens; (d) the mode flags -i / -t / -py are captured as a launch-mode
      PROPERTY and all three STAY kind=dpl — never a separate invocation_type;
      (e) the remaining observed params land as ETLProcess/Invocation PROPERTIES
      — -env, -appName, -alias, -seal, -fid, -img, -dataflow, -conf, -compute,
      plus the argument-contract set (dataset id, aws flag, user jar path, hdfs
      location, token/manifest paths, queue name) — properties never identity
      (the standing G12 rule), with -seal noted as a direct SEAL-attribution
      source. No new relationship types; m3_* statuses untouched. Full suite green.
    notes: >
      From the 2026-07-21 DPL runtime-trace session, upgraded same day with real
      prod CMD_LINE samples (mechanism-only — values stay company-side). Observed:
      ingestion trigger, AWS transformation, and Teradata provisioning all use the
      SAME dt-launcher.sh (modes -i/-t; -img container image is a secondary phase
      discriminator); the template's separate ingestion-launcher jar was NOT
      observed in any sampled CMD_LINE and stays an open inbox question. Excluded
      as runtime values, never definition-level properties: -bd/-od (%%ODATE
      partition values), -proId (per-run GUID threaded via cross-job
      %%\\JOB\VAR references), -timeout/-sleep. Classifier normally runs on
      RESOLVED lines (C3: Python owns var resolution) — acceptance (c) is the
      fallback. SME plan: company-side the pipeline id is also extracted from the
      job-definition table as a job_detail VIEW — this item keeps producer
      CMD_LINE parsing in agreement with that view's extraction rule. Explicit
      NON-goals: DataAsset edges from dataset-id / MAC dataset-flow metadata
      (separate enrichment-feed inbox entry), and the Pre/Post-execution-command
      file-op surface (G14-shaped, separate inbox entry). Pure candidate-side
      parsing, no ontology decision, hence sonnet.

  - id: G16
    epic: component-topology
    title: "Variable-standard engine alignment: ETL_* canonical facts + alias rollups with value contracts; ICDW Informatica launcher rule"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 — all 6 acceptance points tested, suite 774 green
    depends_on: []
    inputs: [drydocs_core/controlm/variables.py, drydocs_core/controlm/commands.py, internal/remediation/governance/command-line-and-variables-standard.md, config/gate-log.md, tests/unit/test_variable_classifier.py, tests/unit/test_command_parser.py]
    acceptance: >
      Per the 2026-07-21 cmdline-nfr-vetting gate (SME-4, all 7 deltas adopted):
      (a) FACT_REGISTRY gains the ETL_* canonical names (LAUNCHER_SCRIPT_PATH,
      ETL_PLATFORM, ETL_ARTIFACT_URI, ETL_ARTIFACT_KIND, ETL_PLATFORM_FLAGS,
      ETL_ARTIFACT_SHA) plus the production alias rollups from the 2,384-var gap
      analysis (JAR_PATH, USER_JAR, PY_LAUNCH, SCRIPT_PATH, JAR_LOC, JAR_NAME,
      MULTI_FILE_JAR, CONTAINER_IMAGE, IMAGE_NAME, ACCELERATOR_PATH and kin);
      (b) the IMAGE -> ARTIFACT_URI clean break lands (IMAGE -> IMAGE removed)
      per the v2 decision log; (c) aliases suggest, VALUES decide — each
      artifact/launcher canonical carries a value contract (launcher-registry
      hit / URI shape / SHA-digest shape), a name-value mismatch classifies by
      VALUE and surfaces a WARN, and a variable whose value hits the launcher
      registry is a launcher ref regardless of its name (the JAR_PATH ->
      dt-launcher.sh case is a test); (d) SHA digests classify as ETL_ARTIFACT_SHA,
      never ARTIFACT_URI; (e) LAUNCHER_REGISTRY gains the ICDW_etl_run_interface.ksh
      Informatica rule (v2 templates 6.4/6.5) with a test; (f) legacy behavior is
      non-destructive — aliased legacy names still materialize facts (WARN), only
      canonical names are WARN-free. Full suite green.
    notes: >
      Groomed 2026-07-21 at the cmdline-nfr-vetting gate sign-off (SME-4) — the
      engine-alignment half of the adopted variable-standard deltas; the vocab half
      (m7_uses_artifact registration + m3 note amendments) landed with the gate
      itself. The digested v2 standard (internal/remediation/governance/
      command-line-and-variables-standard.md) is the ratification source; ETL_*
      wins over the gap-analysis CTM_* spelling. Platform-axis note: %%ETL_PLATFORM
      = execution technology (enum extended with emr, snowflake reserved);
      ETLProcess.kind stays a separate launcher-derived axis — do NOT map one onto
      the other. Mode flags stay CMD_LINE literals (only -py rides
      ETL_PLATFORM_FLAGS) — the launch-mode property itself is G15's.
      ecosystem_execution_engine.sh and the template ingestion-launcher jar stay
      OUT (await CMD_LINE samples — inbox). No new relationship types; the m7
      build (payload migration to USES_ARTIFACT, script_role stamping) is its own
      follow-up item once the writer endpoint work lands. Deterministic
      registry/parser work with a written acceptance test, hence sonnet.

  - id: J13
    epic: release-infrastructure
    title: "Publish-ceiling drift: sanitize or re-classify the real identifiers found in publishable-tier files (2026-07-19 mirror grep)"
    type: bug
    module: config
    phase: 8
    agent: main
    model: fable
    priority: p1
    status: done
    depends_on: []
    inputs: [config/classification.yaml, PUBLISH-BOUNDARY.md, knowledge/standards/technology/folder-naming-convention.md, knowledge/standards/technology/description-field-metadata-plan.md, config/taxonomy/controlm.yaml, drydocs_core/models/controlm.py, docs/design/controlm-ingestion-tdd.md]
    acceptance: >
      USER-GATED START — the user first confirms which flagged identifiers are real
      vs already-synthetic; the confirmed term list is recorded internal/-side only,
      never in publishable tiers. Then every file the 2026-07-19 aborted-mirror grep
      flagged (the two knowledge/standards/technology docs;
      config/taxonomy/controlm.yaml; drydocs/data/samples/*.csv;
      drydocs_core/models/controlm.py; docs/design/controlm-ingestion-tdd.md;
      docs/history/LoadPlanV*.md) is either SANITIZED with synthetic replacements
      (the pat_product_mapping precedent) or RE-CLASSIFIED Internal and excluded
      from the publish set, with the decision recorded per file;
      classification.yaml's "Internal-Public ceiling for tracked files" note is
      corrected to match reality; a repeatable pre-publish grep over the publishable
      tiers (term list internal/-side) returns zero hits; full suite green.
    notes: >
      Groomed 2026-07-19 from the same-day [bug] inbox line. The sanitized-mirror
      attempt was ABORTED at PUBLISH-BOUNDARY enforcement step 3 — nothing was
      published; the aborted export in the session scratchpad holds the grep-hit
      evidence. Module=config (classification.yaml is the enforcement surface),
      phase 8 beside J5's publish-boundary CI guard; fable per the matrix —
      publish-boundary decisions. p1 because it blocks ANY future public artifact,
      but not p0 while the repo stays private. The backlog pull loop must SKIP this
      item until the user supplies the term-list confirmation (the J10
      confirm-at-start idiom).
      MERGED IN 2026-08-05 (desktop, Idea-38 — the parked "internal platform vocabulary
      in the sample corpus" ruling): that entry is the SAME decision this item's
      user-gated start asks for, so it rides here rather than opening a second one. It
      widens the term list the user must rule on beyond the SEALIDs J14/J15 already swept,
      to FOUR value classes deliberately left untouched by the 2026-07-27 sweep:
      (1) platform tokens (`HLDM`, `PRARAG`, `svc.hldm`, `/opt/scripts/hldm/`,
      `host-hldm-01`) — RULED 2026-08-11, SME: AUTHORED FIXTURE NAMES, NO SWEEP OWED. Class (1)
      is CLOSED; the recorded ruling lives in internal/standards/technology/folder-naming-convention.md
      beside the J15 realness table, because that table replaced the NUMERIC segments inside these
      folder names and reading it alone suggests the surrounding tokens were left by oversight. They
      were not — the ids were the real part and they are gone. A sweep of the tokens was proposed the
      same day and stopped by the ruling: `PRARAG` sits in ~36 files including the bundled sample
      corpus, the lineage fixtures and FIVE tests that assert on it literally, so removing an authored
      name would have rewritten the corpus and broken the parser's own pins. The remaining THREE
      classes are still user-gated; (2) datacenter codes — and note
      `knowledge/standards/technology/data-center-naming-convention.md` carries real DC
      codes AND a real app code, same class and same sibling directory, out of J14's scope
      by accident of scoping rather than by decision; (3) schema/table/column identifiers
      (`psgmgr` / `cm_escalation_db` / `EJOBNAME` / `ECOMPONENT`), generalized out of the
      two J14 files but still present in 6+ tracked files — the controlm-db skill, gate
      prompts, the taxonomy-ontology map, the remediation TDD; (4) synthetic-sample product
      NAMES that ECHO real ones ("Home Lending Servicing" in lob-product-team.yaml, paired
      only with synthetic ids) — the subtlest class, because the ids are clean and the name
      is not. THE QUESTION IS THE SAME FOR ALL FOUR and is the user's: is platform
      vocabulary publishable MECHANISM, like the naming grammar, or a value class to
      synthesize? Once ruled, J15's value-shape guard test grows a rule per class, which is
      what turns the ruling into something that cannot drift back.
      ALL FOUR CLASSES NOW RULED — 2026-08-11, recorded in config/gate-log.md
      ("RECORD: J13 publish-ceiling value classes 2, 3, 4"). (1) platform tokens: AUTHORED,
      no sweep. (2) data-center codes: SWEPT — SME ruled the position-1 environment letter
      swaps to a non-production value across 19 tracked files; real inventory moved to
      internal/standards/technology/data-center-inventory.md; the sanitize rule is now in
      config/classification.yaml and Scan E in tests/unit/test_publish_boundary_values.py
      enforces it. (3) schema/table/column ids: NO SWEEP OWED — already covered by the
      SIGNED N9 source-registry-v2 id grammar, which redacts the database and publishes
      schema.table; cm_escalation_db reads as a database because of its suffix but is a
      TABLE inside psgmgr. (4) sample product/LOB names: ruled publishable (assistant's
      call under the SME's "no preference") — ids are the identity-bearing half, are
      in-block, and are guarded; names are not policed.
      TWO THINGS THIS PASS FOUND THAT THE ITEM DID NOT KNOW ABOUT. (a) A FIFTH data center
      (P045) in a test fixture and the web demo data — the guard's SHAPE scan caught it,
      the token-list sweep could not have. Third instance of the J15 lesson: enumerate the
      shape, never the values. (b) Real per-data-center VOLUMETRICS in
      controlm_staging_ddl.sql — a distinct class, NOT covered by the SME's identifier
      ruling. The per-DC split was pulled to the internal twin and the totals kept (the
      sizing rationale rests on totals); FLAGGED for an SME ruling, and reversible.
      The J15 realness table's parked "string-vocab ruling" on the real job name is
      DISCHARGED by class 2 — that name was swept in this pass.

  # ── appended at the 2026-07-21 groom run (weekly-style inbox groom) ───────
  - id: G17
    epic: component-topology
    title: "DPL Metadata-As-Code ingest seam: dataset-flow READS/WRITES enrichment, pipeline-JSON kind discriminator, SEAL attribution facts"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G15]
    inputs: [drydocs_lineage/, drydocs_core/controlm/commands.py, tests/unit/test_lineage_inventory.py, config/gate-log.md]
    acceptance: >
      A MAC (Metadata-As-Code) ingest seam exists in the lineage component, driven
      by SYNTHETIC fixtures modeled on the traced 6-JSON-per-pipeline shape
      (mechanism-only — real values stay company-side; publish boundary clean):
      (a) the JSON set is keyed by the launcher --pipeline-id GUID and joins to the
      ETLProcess identity G15 captures — an unmatched GUID is counted, never
      silently dropped; (b) dataset_flow.json input/output datasets (GUID + version
      + zone) become dataset-level READS_FROM / WRITES_TO CANDIDATES for that
      ETLProcess, endpoints exactly per the 2026-07-15 gate EDIT (ETLProcess |
      ControlMJob -> DataAsset), no new relationship types, all m3_* vocabulary
      statuses untouched (refusal semantics unchanged until the curated-load gate);
      (c) pipeline.json pipelineType/subType land as ETLProcess PROPERTIES and
      drive a recorded kind-derivation rule where MAC metadata exists (provisioning
      subtype = DB load with no transform — the G12 constant kind='etl' stops being
      stamped blind there; jobs without MAC keep today's default), with any
      enum-semantics question surfaced as a gate rider, never auto-decided;
      (d) the pipeline.json owner SEAL id is captured as an attribution
      FACT/property beside G15's -seal note — never an attribution edge (Epic K
      owns those). Full suite green.
    notes: >
      Groomed 2026-07-21 from two inbox lines merged: the same-day DPL
      runtime-trace [source] line (company-side agent doc, mechanism only here;
      its own "groom candidates" named this extractor + the kind wiring) and the
      2026-07-19 "ETLProcess kind discriminator needs a real signal" [idea],
      whose trigger FIRED — pipeline.json subType is the discriminating signal
      G12 lacked. The DPL ingestion-leg inbox line's open item (b) (DataAsset
      zone/glue-table shapes for the enrichment feed) rides this item.
      Candidate-side parsing on gate-confirmed endpoint shapes, hence sonnet.

  # ── rua-extract ingest phase (groomed 2026-07-21 from the company-side
  #    collector run + PuTTY readout; mechanism-only — real host/user values
  #    stay company-side / internal-local) ──────────────────────────────────
  - id: G18
    epic: component-topology
    title: "rua collector v2 parity: script-file capture (-n name glob) back-ported from the observed company-side run"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-07-23: -n glob capture -> scripts.tsv (+sha256) + tree-mirrored copies (COPY_SCRIPTS gate, SCRIPT_COPY_MAX_BYTES cap), profiles.tsv sha256, COLLECTOR_VERSION v2 (v1 stays ingestible), summary line, README; smoke-tested in Git Bash (globs/ignores/cap/repeatable -n/no -n/v1 mode)
    depends_on: []
    inputs: [drydocs_lineage/collect/rua_inventory.sh, drydocs_lineage/collect/rua_inventory.conf, drydocs_lineage/collect/README.md]
    acceptance: >
      rua_inventory.sh gains the script-capture feature observed in the
      2026-07-20 company-side run (screenshots -> reproduce generically, the
      drydocs-review back-flow discipline): a -n <glob> flag (repeatable or
      space-separated) captures FILES matching the name glob under the scan
      roots into scripts.tsv (path, owner, group, perms, size, mtime, sha256)
      plus copies under scripts/ in the bundle (copy behavior config-gated
      like COPY_PROFILES; size cap configurable so a stray large file never
      bloats the bundle); profiles.tsv gains the same sha256 column (content
      hash = the version discriminator and the anchor for G24's all-refs
      repo blob sweep; v1 captures no hashes — verified 2026-07-21); the run
      summary prints a "scripts: N captured" line;
      COLLECTOR_VERSION bumps to rua-inventory/v2 with v1 bundles explicitly
      still ingestible (the extractor treats scripts.tsv as optional);
      README documents the flag and the bundle addition. POSIX sh / ksh
      compatible, still strictly read-only outside -o.
    notes: >
      The company copy already runs this (observed: -n '*.py' -> 35 scripts
      captured beside 17 directories); producer v1 lacks it. Script CONTENT
      is what feeds G21's code-operations pass, so this lands first. Exact
      company implementation unseen — reproduce from observed CLI + summary
      behavior, don't guess internals.

  - id: G19
    epic: component-topology
    title: "Out-of-repo landing zone for rua bundles: DRYDOCS_DATA_ROOT + source registration"
    type: task
    module: config
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-07-22: drydocs_core/data_root.py (one shared resolver + rua subfolders), rua-inventory registered internal-confidential confirmed:false (G22 = activation), collect/README "Where bundles live", test_data_root.py guard sweep
    depends_on: []
    inputs: [drydocs_core/adapters/sql_run_log.py, config/source-registry.yaml, config/classification.yaml, drydocs_lineage/collect/README.md, PUBLISH-BOUNDARY.md]
    acceptance: >
      A data-root convention exists mirroring the sql_run_log logs idiom
      (DEFAULT_LOGDIR = ~/logs/DryDocs, env-overridable): default
      ~/data/DryDocs, override via DRYDOCS_DATA_ROOT, resolved by ONE shared
      helper in drydocs_core (unit-tested), with per-source subfolders —
      rua/incoming/ for tarballs, rua/extracted/<bundle>/ for unpacked
      bundles. The rua source is registered in config/source-registry.yaml
      with classification internal-confidential (bundles hold real
      hostnames, uids, home paths, profile + script copies) and the
      registration records that DATA NEVER ENTERS THE REPO — only the
      pointer does (the internal-local hand-carry note covers transport).
      collect/README gains a "where bundles live" section; a guard test
      asserts the repo tree contains no rua_*.tar.gz / extracted bundle
      (publish-boundary safety net, same spirit as the root-image rule).
    notes: >
      User call 2026-07-21: bundle output is not the structured seal/pat
      csv shape, can be big, and belongs outside the project like the logs.
      ~/data/DryDocs (not internal-local/) because internal-local is the
      hand-carry WORKING area inside the tree — big binary bundles get a
      real out-of-tree home; internal-local keeps only pointers/notes.

  - id: G20
    epic: component-topology
    title: "rua_inventory extractor: bundle -> staging records with the meta.txt provenance envelope"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-07-23: extractors/rua_inventory.py — tarball (filter="data") or dir; meta.txt envelope stamped rua_* on every record + full meta kept for G23; directories/ownership -> rua_path assets, profiles/scripts -> rua_profile/rua_script artifacts (sha256 + rua_copy pointers); v1+v2 fixtures; malformed/missing/cross-host counted; NODES only
    depends_on: [G18, G19]
    inputs: [drydocs_lineage/extractors/controlm_inventory.py, drydocs_lineage/model.py, drydocs_lineage/writer.py, tests/unit/test_lineage_inventory.py]
    acceptance: >
      drydocs_lineage/extractors/rua_inventory.py reads a bundle (tarball or
      extracted dir) from the G19 landing zone and emits staging records in
      the controlm_inventory idiom: meta.txt parsed into a provenance
      envelope carried on every record (schema version, collected_at/by,
      user, uid, groups, shell, host, fqdn, os/kernel, scan roots — the
      "metadata as nodes" raw material); directories.tsv -> DataAsset
      candidates; profiles.tsv + profiles/ -> profile artifacts;
      scripts.tsv + scripts/ -> script artifacts (optional, v1 bundles
      accepted); ownership_dirs.tsv consumed when present. v1 AND v2
      bundles both parse (fixture per version, synthetic values only);
      malformed/missing sections are counted, never silently dropped (the
      coverage house rule). NO graph writes and NO new relationship types —
      candidates only, refusal semantics unchanged. Full suite green.
    notes: >
      The extractor the collect/README has promised since the ADR 0002-C
      re-home. Join keys downstream: (host, user) -> ControlMJob run_as +
      node_target (Epic P hosts-topology), script path -> CMD_LINE /
      launcher references. Pure candidate-side parsing, hence sonnet.

  - id: G21
    epic: component-topology
    title: "rua code-operations pass: captured scripts + profiles through the software-ontology mappers (launcher registry, ETL_* facts, file-ops)"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G20]
    inputs: [drydocs_core/controlm/commands.py, drydocs_core/controlm/variables.py, drydocs_lineage/extractors/rua_inventory.py, config/gate-log.md]
    acceptance: >
      A code-operations pass runs over the bundle's captured script and
      profile CONTENT and maps what it finds onto the existing software
      ontology surfaces — reusing, not duplicating, the G14/G15/G16
      machinery: (a) launcher/interpreter invocations found inside scripts
      classify via LAUNCHER_REGISTRY (DPL GUID identity per G15 where
      present); (b) variable assignments classify via FACT_REGISTRY ETL_*
      canonicals with G16 value contracts (values decide, WARN stream);
      (c) file operations (move/copy/gzip and kin) emit READS_FROM /
      WRITES_TO candidates with endpoints exactly per the 2026-07-15 gate
      EDIT — no new relationship types, all m3_*/m7 statuses untouched;
      (d) profile parsing captures PATH mutations and sourced/invoked
      scripts as script-to-script DEPENDENCY CANDIDATES (flagged
      needs-vocabulary — the edge meaning is G22's, never auto-decided);
      (e) every skipped/unparseable construct is counted by reason.
      Unit tests on synthetic script/profile fixtures; full suite green.
    notes: >
      "Extract the code operations as they map to the software ontology"
      (user, 2026-07-21). Static analysis of shell/python text is
      best-effort by design — coverage counters make the miss rate visible
      instead of implying completeness. Candidate-side only, hence sonnet.
      CLOSED 2026-07-27: drydocs_lineage/extractors/rua_code_ops.py —
      RuaCodeOpsExtractor runs on the SAME LineageGraph after the G20
      inventory pass, reading each staged artifact's rua_copy content.
      Scripts: INVOKES candidates via the shared parse_command /
      LAUNCHER_REGISTRY (DPL keeps GUID identity + G15 property flags,
      reusing controlm_inventory's _stable_invocation_key/_dpl_properties);
      assignments via classify_variable (FACT_REGISTRY + G16 value
      contracts — mismatch/alias WARN counters + a facts candidate list);
      dataflow file ops → READS_FROM/WRITES_TO from the script node (G14
      idiom, gate-EDIT endpoints). Profiles stage NO rels: PATH mutations +
      sourced/invoked scripts land as dependency_candidates flagged
      needs_vocabulary (G22's edge meaning). Coverage counts every skip by
      reason (no-copy, unreadable, comment/blank/continuation, non-dataflow,
      profile-op-skipped, unparsed). 5 fixture tests; suite 949 green.

  - id: G22
    epic: component-topology
    title: "HITL gate session: rua load shapes — ownership + profile-dependency edge meanings, planned-tier activation scope"
    type: requirement
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: done                        # 2026-08-07 (LAPTOP): gate rua-load-shapes SIGNED OFF, 28/28 (config/gate-log.md). Walked A→J across two days, each section transcribed to the page and recorded as it closed (nine RECORD entries + the sign-off). Outcomes: 3 activate (A3/A4/A5), 1 declined (A2), 1 HELD behind K17 (A1), 1 already active before the session; C1–C3 mint new meanings at status: planned; D1–D4 settle identity, occurrence grain, the host join and the envelope. Sign-off also settled its own three preconditions: the bitbucket + two dpl audit-fields entries (all ruled stub, three different reasons), the G55 lifecycle question (RULED deprecated, reversing this session's own `removed` recommendation — K7 had already answered it 2026-08-03), and the four source-registry confirmed flips. NOT unblocked by this: A1 and C1's build both still wait on K17. Follow-ups: G55 (vocabulary), G23 (curated load + the D2 extractor fix), G56, G57.
    depends_on: [G21, G24, G25]
    inputs: [docs/restructure/03-hitl-sme-flow.md, config/gate-log.md, drydocs_core/schema/schema_graph.cypher, docs/RELATIONSHIP_GUIDE.md, config/precedence.yaml, drydocs_core/schema/provisioning/02_proxy_constraints.cypher]
    acceptance: >
      A HITL gate session rules the graph shape for rua data before ANY
      load: (a) which existing planned vocabulary the load may activate
      for these facts (candidates: AppUser -DELEGATES_TO-> ExecutionHost,
      ControlMJob -RUNS_ON-> ExecutionHost, ControlMJob -INVOKES-> Script,
      m7 USES_ARTIFACT, m3 READS_FROM/WRITES_TO); (b) the NEW edge
      meanings the bundle implies and the vocabulary lacks — run-as user
      owns home/directory DataAssets (WAS_ATTRIBUTED_TO {role} per the
      arch_owns_code precedent, or a new type), and profile
      sources/invokes script (G21's flagged dependency candidates) —
      proposed via ontology-mapper as status: planned first, per the
      RELATIONSHIP_GUIDE registry flow; (c) the SCRIPT IDENTITY + DEDUP
      contract: a DryDocs URN for the logical script (extending the D1
      urn:drydocs:dataasset pattern — the gate rules the URN segments,
      e.g. urn:drydocs:script:{...}) so the SAME file arriving from the
      server extract (G20), the code repo (G24), and CMD_LINE/variable
      references (G21/G16 SCRIPT_PATH facts) MERGES to ONE node — never
      duplicate labels/nodes — while every source occurrence is recorded
      WITH its origin (server-extract | code-repo) and full path
      (host+path vs repo+branch+commit+path); plus identity keys for the
      other nodes (ExecutionHost by fqdn?, AppUser by (user, uid)?) and
      how the meta.txt provenance envelope lands (properties vs the
      provenance-audit-fields envelope plan); (d) the SOURCE-PRECEDENCE
      ruling recorded in config/precedence.yaml terms: the server extract
      is the truth for "latest code that actually runs"; the code repo is
      the truth for intent/history ONLY where the team maintains a clean
      branch (a per-team/per-repo trust flag, not a global assumption) —
      and the usage axis stays SEPARATE: present-on-server does not mean
      used, and G21/G16 reference facts decide "what calls it"; a script
      with no references is FLAGGED unreferenced, never auto-judged
      unused; (e) classification handling on load — which properties are
      internal-confidential and must never reach a publishable export
      (ties to the O11 spec classification vocabulary); (f) GUID-vs-URN
      identity for registry-known pipelines/datasets (G25): where an
      asset is DPL-managed the registry GUID IS the business key per
      ADR 0001 and the gate rules where it sits (URN segment vs
      property), with the urn:drydocs:dataasset pattern as the fallback
      for everything else — and the CONTENT HASH rides on
      source-occurrence records as the version discriminator (same URN +
      different hashes = drift, queryable); versions do NOT become their
      own nodes unless this gate rules otherwise; (g) the THREE-SIGNAL
      usage ruling: referenced (G21/G16 facts), registry-active (G25
      flag), and present-on-server (G20) stay SEPARATE axes,
      corroborated never conflated — active=false-but-referenced and
      active=true-but-unreferenced are findings, not errors. Decisions
      in config/gate-log.md; anything declined stays candidate-only.
    notes: >
      THE TERMINUS of this groom — G18-G21 + G24 are all candidate-side
      and buildable ungated; nothing writes the graph before this session.
      fable per the matrix (ontology decisions). The 2026-07-21 SME-chat
      evidence motivating (c)/(d), mechanism-only: a production script
      referenced by a job-definition folder VARIABLE was not findable in
      the code repo — the team's code-fetch tooling keys on pipeline-id,
      which this job family lacks — so repo completeness can never be
      assumed and the server extract is the reliable "what runs" source.
      PREMISE CORRECTION 2026-07-27 (user): the planning around this gate
      assumed a psgmgr CM_DEF_VJOB_DETAIL-style table (split by job type:
      file_watcher, cmd_line, ...) would exist company-side. IT WAS NEVER
      BUILT. The G39 temporary staging store (graph-sourced cmd_line) +
      the G40 Python parse stand in until it is — the gate's CMD_LINE-fact
      clauses (a)/(g) read from that seam in the interim. The folder/job
      VARIABLES pull stays deferred exactly as originally sequenced;
      neither G39 nor G40 touches it, and neither writes the graph — this
      gate remains the terminus.
      TWO RIDERS merged from the inbox 2026-07-28, for this session's
      agenda: (R1, Script->SWO) when clause (b)/(c) lands :Script nodes,
      propose (:Script)-[:IS_ENCODED_IN]->(SwoClass) derived from the path
      extension — the G33 §E1(b) precedent applied verbatim to the second
      software-artifact class (.sh -> SWO_0000124 Shell, .sql ->
      SWO_0000126 SQL, binding two of the three seeded-but-unused terms;
      the adapter mechanism exists, EXTENSION_LANGUAGE_IRI in
      code_snapshot.py). Boundary already ruled in that discussion:
      run_as/J.OWNER is Agent territory (PROV/ORG), NEVER an SWO binding;
      unreferenced Scripts are the dead-script detection FEATURE per
      clause (d)'s flagged-never-auto-judged rule. (R2, m3_invokes
      endpoint) clause (a) activates m3_invokes — rule whether its to_node
      broadens Script -> Script|ETLProcess: the abioncloud wrapper-payload
      expansion means INVOKES sometimes lands directly on an :ETLProcess
      with no Script hop (G12 implements exactly that; 2026-07-19 subagent
      finding). Vocabulary-shape decisions, so they ride THIS gate, never
      an auto-edit.
      PROMPT DRAFTED 2026-07-28 (config/gate-prompts/rua-load-shapes.yaml):
      clauses a-g as sections A/C-H, riders R1/R2 as C3/B, the G39/G40
      interim-seam premise carried in the header + I3. Session-ready —
      sign-off is the SME's, in its own session; item stays todo until then.

  - id: G23
    epic: component-topology
    title: "rua curated load: bundle facts + provenance metadata land as nodes per the G22 rulings"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G22, G55]
    inputs: [drydocs_lineage/writer.py, drydocs_core/schema/constraints.cypher, config/gate-log.md, tests/]
    acceptance: >
      The lineage writer loads a rua bundle into the graph EXACTLY per the
      G22 gate rulings: the ruled node set (server / run-as user / script +
      profile artifacts / directory DataAssets) with the ruled identity
      keys and constraints, the ruled edges only (activated or newly
      planned->active per the gate — nothing beyond it), and the meta.txt
      provenance envelope attached in the ruled form. Idempotent re-load of
      the same bundle (MERGE on identity keys); a two-source fixture — the
      SAME logical script arriving as a server-extract occurrence AND a
      code-repo occurrence — proves single-node merge on the G22 script
      URN with BOTH source records (origin + path) retained; a
      testcontainers e2e proves a synthetic v2 bundle round-trips
      collector-shape -> extractor -> load -> cypher readback. Coverage
      counters from G20/G21/G24 surface in the load report. Full suite
      green.
    notes: >
      Sequenced after the gate on purpose (K2 -> K4 loader precedent:
      gate first, build same day after sign-off). Synthetic fixtures only
      in-repo; real bundles stay in the G19 landing zone.
      MERGED 2026-08-06 (gate rua-load-shapes D2, an as-built defect the
      clause exposed): rua_inventory._stage_artifact currently DROPS the
      second arrival of a staged id — it increments cross_host_collisions
      and returns, so the second host's origin/sha256/owner/perms/mtime
      and envelope never land. The two-source fixture in this item's own
      acceptance CANNOT pass until that is fixed, so the extractor change
      is a precondition of this item rather than new scope: occurrences
      must ACCUMULATE as reified records (D2's ruled grain), not
      first-write-wins. Note also that code_repo.py keeps its occurrences
      as a newline-packed string property, fine for staging but ruled out
      as graph shape by ADR 0001 (a string is not queryable) — the load
      reads the parsed records, never the packed form.
      DEPENDS_ON G55 ADDED 2026-08-07 (laptop, G22 close-out) — a REAL ordering
      constraint, not bookkeeping, and verified in code rather than assumed:
      drydocs_lineage/writer.py::write_curated calls vocabulary_status() and
      RAISES GateBoundVocabularyError unless the entries it needs are `active`.
      G55 is what flips them. So this item's own acceptance ("the lineage writer
      loads a rua bundle into the graph") cannot pass until G55 lands — starting
      here first walks straight into the guard. G55 first, then this.
      DONE 2026-08-07 (desktop, same session as G55): BUILT per the signed
      rulings and nothing beyond them. The D2 extractor precondition fixed
      first — ProcessNode.occurrences added, _stage_artifact ACCUMULATES
      (second-host record kept, cross_host_collisions still counts as the
      D3 ambiguity signal, identical re-stages dedup). plan_rua/write_rua in
      writer.py: :Script MERGEd on the D1 normalized-absolute-path key
      (mechanical only; malformed counted, never loaded; URN = render),
      profiles as script_role=profile (C2), rua_path directory DataAssets
      (owner = PLAIN property, C1 stays K17-blocked), :SourceOccurrence per
      record on deterministic occurrenceId anchored OCCURRENCE_OF, repo
      occurrences joined on CONTENT HASH (G24 blob sha, never path), C3
      IS_ENCODED_IN from the shared adapter (moved to
      drydocs_core/ontology/swo_adapter.py — the lineage import boundary
      forbids drydocs.loaders; code_snapshot re-imports it), D3 host
      resolution read-only vs ExecutionHost.nodeid (qualified fqdn only,
      misses counted), storage_scope=unknown until G56 with multi-host
      scripts flagged identity_unconfirmed_across_hosts, D4 stub honored
      (rua_* capture props only, no audit envelope). ONE REGISTRATION MADE
      HERE, flagged for SME review: g22_occurrence_of (OCCURRENCE_OF,
      SourceOccurrence->Script, prov:specializationOf seeded in
      ontology.cypher) — the D2 binding is verbatim in the SIGNED class
      supplement but G55 minted no edge entry; the loader cannot land the
      ruled grain without it, so it was registered active at this build as
      transcription of the signed ruling (I1 fix-2 reading), NOT a new
      meaning. Coverage counters ride RuaLoadReport verbatim. Two-source
      fixture + 15 unit tests + testcontainers e2e (collector-shape ->
      extract -> write_rua -> readback on a real EE 'drydocs' DB,
      idempotent re-load proven) ALL GREEN; e2e note: this desktop needs
      TESTCONTAINERS_RYUK_DISABLED=true (pre-existing ryuk port-mapping
      failure, hits the J9 tests identically). Suite 1689/3/5 — the 3 are
      the Idea-83 rich-ANSI env failures, untouched.


  - id: G39
    epic: component-topology
    title: "Temporary CMD_LINE job-detail staging store — the seam that stands in for the unbuilt psgmgr CM_DEF_VJOB_DETAIL table"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/loaders/cypher/controlm_jobs.cypher, drydocs_core/data_root.py, drydocs_core/mapping_store.py, config/source-registry.yaml]
    acceptance: >
      A CLI verb (working name `drydocs export-cmdline-staging`) materializes
      the TEMPORARY job-detail staging store: one row per loaded job carrying
      the identity keys (folder_id, job_id, job_name, data_center where
      present), the job/task type discriminator (file_watcher vs cmd_line
      etc.), and the VERBATIM cmd_line — sourced FROM THE GRAPH (j.cmd_line
      is already loaded, controlm_jobs.cypher), with the equivalent psgmgr
      CM_DEF_VJOB SQL projection documented beside it as the company-side
      source for the same shape. The store lives under the G19 data root
      (DRYDOCS_DATA_ROOT — real command lines are Internal; NEVER in-repo),
      is deterministic (same graph -> same rows), reports its counts (the
      never-silent rule), and carries an explicit RETIREMENT note: this
      store is deleted the day a real psgmgr CM_DEF_VJOB_DETAIL-style table
      exists. EXPLICITLY OUT OF SCOPE, by design not omission: folder/job
      VARIABLE columns — the variables pull stays deferred exactly as the
      original G18->G22 plan sequenced it.
    notes: >
      PREMISE CORRECTION (user, 2026-07-27): the G18->G22 rua/cmdline plan
      assumed a company-side psgmgr detail table (CM_DEF_VJOB split by job
      type) would exist by now. It was never built — this seam stands in
      until it is, and the graph is a legitimate interim source precisely
      because the jobs loader already lands cmd_line verbatim. SQLite via
      the mapping-store precedent (M2) or a flat file both satisfy the
      acceptance; pick at build. Groomed from user chat notes 2026-07-27
      (audit trail in IDEAS.md). G37 deliberately NOT allocated — skipped
      in the sequence (G38 exists), possibly reserved by the concurrent
      session; ids are cheap, collisions are not.
      CLOSED 2026-07-27 (same day): drydocs/cmdline_staging.py (SQLite per
      the mapping-store precedent) + CLI `drydocs export-cmdline-staging`.
      Store = <DRYDOCS_DATA_ROOT>/cmdline-staging/job_detail.db; job_detail
      keyed (folder_id, job_id) with job_name, data_center (= graph
      instance_name), task_type discriminator, active, VERBATIM cmd_line;
      meta carries schema_version + the RETIREMENT note + the psgmgr
      CM_DEF_VJOB projection (PSGMGR_PROJECTION_SQL, documented beside the
      store per acceptance) + the variables-out-of-scope declaration.
      Deterministic (node-key order, no wall-clock values); counts via
      ExportReport + LoaderRunLog; loud yellow on 0 jobs. Classified into
      the load bucket (MODULE_MAP + boundary guard). 12 fixture tests;
      suite 1019 green.
      CAVEAT AMENDED 2026-07-28 (user; store schema v2): "variables out of
      scope" was overstated. Precisely: variable COLUMNS / the variables
      pull stay deferred (G18->G22 sequencing unchanged), but RESOLVED
      command lines are in scope as an INPUT — the user has an internal
      (company-side) %%VAR resolution query; job_detail gained the nullable
      cmd_line_resolved landing column for it. This module never resolves;
      cmd_line stays verbatim beside the resolved value (O24 source-vs-
      derived discipline); re-export rebuilds the store, so resolution
      re-runs after each export (documented loudly — temporary plumbing,
      not a system of record).

  - id: G40
    epic: component-topology
    title: "Parse the staged command lines in Python into structured job-detail columns (launcher / payload / args), loaded back into the staging store"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G39]
    inputs: [drydocs_core/controlm/commands.py, config/launcher-registry (G26), docs/restructure/backlog.yaml]
    acceptance: >
      Every cmd_line row in the G39 store decomposes through the EXISTING
      machinery — the G26 launcher registry + the G15 DPL launcher arg
      contract (both GUID spellings, %%VAR-launcher fallback, launch_mode +
      props) — into structured detail columns: launcher, launch_mode,
      payload/script_path, artifact kind, and the argument map. The G16
      values-decide contract holds (aliases suggest, VALUES decide — never
      the variable name alone); unparseable/partial rows go to the WARN
      stream and are COUNTED, never silently dropped; a coverage line
      (parsed / partial / unparsed) prints on every run. Output lands as
      the detail columns/table beside the G39 rows. NO graph writes — the
      SCRIPT_PATH/artifact facts this produces feed the m7_uses_artifact
      and G22 rulings later; G22 stays the terminus gate for anything
      entering Neo4j.
    notes: >
      The company-side end state — loading this parser's output into the
      real psgmgr detail table once it is built — is a company-side
      follow-up (their tracker / DD-series), recorded here so the intent
      is not lost; producer ships the parser + the temporary landing.
      Groomed from the same 2026-07-27 user chat notes as G39.
      CLOSED 2026-07-27 (same day): parse_job_detail in
      drydocs/cmdline_staging.py + CLI `drydocs parse-cmdline-staging`.
      Reuses the shared core parser end to end; to satisfy MODULE_MAP
      (components import core only) the G15 DPL arg contract was PROMOTED
      from drydocs_lineage.extractors.controlm_inventory into
      drydocs_core.controlm.commands.dpl_properties — lineage now imports
      it back from core, ONE contract for both consumers. Detail columns:
      invocation_type/classifier_rule (G26), launcher, launch_mode (own
      column, popped from props), script_path, config_path, artifact_kind,
      pipeline_guid (both spellings; the %%VAR-launcher fallback pinned by
      test), props_json/args_json, raw_command; per-job parse_quality
      verdict (parsed/partial/unparsed/no_cmd_line — every row bucketed,
      never dropped) + WARN stream counted + coverage line on every run.
      NO graph writes (pinned by test: read seam is one MATCH; parse takes
      no client). G22 stays the terminus.
      AMENDED 2026-07-28 (with the G39 caveat fix): the parser now prefers
      cmd_line_resolved when the internal resolution query has populated
      it, else the verbatim cmd_line (the core parser is designed for
      resolved values — resolution improves coverage); parse_quality
      gained parsed_from (resolved|raw) and the coverage line reports
      the from-resolved count.

  - id: G24
    epic: component-topology
    title: "Code-repo source seam + server/repo corroboration: same staging shape, origin=code-repo, drift report"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G20]
    inputs: [drydocs_lineage/extractors/rua_inventory.py, drydocs_lineage/model.py, config/source-registry.yaml, tests/unit/test_lineage_inventory.py]
    acceptance: >
      A code-repo source seam emits script-artifact staging records in the
      SAME shape as G20's server-extract records, differing only in the
      provenance origin: origin=code-repo with repo + branch + commit +
      path (vs origin=server-extract with host + path + mtime). Driven by
      SYNTHETIC fixtures modeled on the observed shapes (a repo file
      listing/manifest and job-definition XML carrying script paths via
      folder VARIABLEs — mechanism-only; real Bitbucket access stays
      company-side behind this seam). A corroboration report joins the two
      origins on CONTENT HASH (compute the git blob hash of each server
      file and sweep ALL refs in one pass — git rev-list --all --objects —
      never branch guesswork; path-tail matching only as fallback until
      the G22 URN is ruled) into verdict buckets — found-at-refs (WITH
      the matching refs + commit dates: names the de-facto current
      branch mechanically), behind (only older commits match = running
      code ahead of everything committed), never-committed (the observed
      failure mode: production code absent from the repo), and repo-only
      — per-team hash drift IS the "clean branch" signal. The trust flag
      is a nullable per-repo trusted_ref field on the source-registry
      entry (the human-blessed intent line; null = server-extract-only
      truth — covers the stale-main case: main exists but dead, real
      code scattered across feature branches; the sweep names the
      candidate ref, a human blesses it), never inferred. NO graph
      writes, no new relationship types; unmatched records counted,
      never dropped. Full suite green.
    notes: >
      Groomed 2026-07-21 from the SME-chat evidence (mechanism-only): a
      script running in production was not findable in the code repo —
      code-fetch tooling keys on pipeline-id, which that job family
      lacks. The repo is a valid source ONLY where the team keeps a clean
      branch (the trust flag); production servers always have the latest
      RUNNING code — but "what calls it / is it used" stays the reference
      graph's question (G21/G16 facts), never this seam's. Amended same
      day from the stale-repo use case (team migrated to GitHub in sync,
      then the manager + best-practice devs left; main untouched >1yr,
      latest likely scattered in feature branches): content-hash blob
      sweep replaces path-only matching, trusted_ref replaces the
      boolean flag. Feeds the G22 gate (identity/dedup + precedence
      rulings). Candidate-side, hence sonnet.
      CLOSED 2026-07-27: drydocs_lineage/extractors/code_repo.py —
      CodeRepoExtractor stages repo_script records from the repo_objects.tsv
      manifest contract (rev-list --all sweep + ref tips; live-tip rows vs
      empty-ref historical rows), same LineageGraph shape as G20 with
      origin=code-repo (G20 artifacts now stamp origin=server-extract);
      corroborate() joins on git blob sha1 of the carried-back copies into
      found_at_refs (refs + commit dates; candidate_ref = most recent tip,
      mechanical) / behind (history-only match) / never_committed (with
      path-tail hints as the weak fallback) / repo_only; uncomputable and
      malformed counted. trusted_ref added as a nullable HUMAN-BLESSED field
      on the new code-repo source-registry entry (Internal, confirmed:false)
      and only checked, never inferred. Honest limit recorded: content
      hashing cannot tell edited-after-checkout from never-committed — both
      bucket to never_committed with hints. No rels, no graph writes.
      6 fixture tests; suite 955 green. G22 (gate) now unblocked.

  - id: G25
    epic: component-topology
    title: "DPL registry ingestion source: per-SEAL Swagger JSON (pipeline/dataset GUID, version, active flag) + GUID cross-check"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-07-23: extractors/dpl_registry.py (assumed contract: <seal>/pipeline_id.json + dataset_id.json, bare or wrapped lists; seal from field else folder; active normalized, unknown counted) + cross_check report (observed-not-registered / registered-not-observed / clone-lag third column per the SME caveat) + dpl_registry_dir() + dpl-registry source registration (internal-confidential, confirmed:false, G22 f/g); synthetic fixtures; NO graph writes
    depends_on: [G19]
    inputs: [config/source-registry.yaml, config/classification.yaml, drydocs_lineage/, drydocs_core/controlm/commands.py, tests/unit/test_lineage_inventory.py]
    acceptance: >
      The DPL taxonomy registry becomes a registered ingestion source:
      per-SEAL Swagger JSON exports of the registry DB (pipeline GUID /
      dataset GUID, version, active|inactive flag, keyed by SEAL) are
      ingested TAXONOMY-FIRST (pure classification, no meaning edges)
      into staging records (guid, kind=pipeline|dataset, version,
      active, seal, provenance envelope). Downloads land in the G19
      out-of-tree zone (dpl-registry/ subfolder); the source-registry
      entry carries classification internal-confidential (real SEALs +
      GUIDs + lifecycle state) and records that data never enters the
      repo; fixtures are SYNTHETIC. A cross-check report joins registry
      GUIDs against the GUIDs G15 observes in launcher CMD_LINEs:
      observed-not-registered and registered-not-observed are each
      counted and listed, never dropped — the registry is also the truth
      table for which job families carry pipeline ids at all (the
      code-fetch tooling gap from the SME chat). The active flag is
      staged as a THIRD usage signal (beside referenced and
      present-on-server) — staged only; any conflation with "used" is a
      G22 ruling. NO graph writes. Full suite green.
    notes: >
      Groomed 2026-07-21 from the user's registry disclosure: the DPL
      taxonomy lives in a database showing version + active/inactive
      flag by SEAL, exportable per-SEAL as Swagger JSON. Independent of
      the collector track (only needs the G19 landing zone), so it can
      run in parallel. Complements G17 — G17 reads each pipeline's own
      MAC JSON set, G25 reads the registry ABOVE the pipelines; the
      shared join key is the same GUID G15 extracts. Feeds G22 clauses
      (f)/(g). Candidate-side, hence sonnet.

  - id: C11
    epic: ontology-mapping
    title: "Capture the company-confirmed AisCapability/AiTool platform taxonomy as the seed answer for the platforms gate"
    type: task
    module: taxonomy
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done                                     # 2026-07-21 pm — see the DONE paragraph in acceptance; VS Code session
    depends_on: []
    inputs: [config/taxonomy/platforms.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      USER-GATED START — the user supplies the company-side confirmed
      platforms.yaml shape via the screenshot/describe channel (mechanism-only;
      no company values committed). Then config/taxonomy/platforms.yaml is
      upgraded from status: placeholder to a captured taxonomy carrying the
      confirmed AisCapability/AiTool classes as the PROPOSED seed answer to its
      recorded open questions; the C6 requires-scheduler map entry's provisional
      to_node and the SchedulerKind-deprecation inbox line reference the captured
      shape as their gate input. THE HARD RULE: pure classification — nothing
      flips active, no vocabulary status changes, no graph writes; the
      platforms/SchedulerKind HITL gate decides. Sensitivity classification set
      on the source; drift guards + full suite green.
      DONE 2026-07-21 pm: user supplied the company session report (screenshot
      channel) — richer than expected: company CONFIRMED + BUILT the taxonomy
      at its 2026-06-29 AIS gate (platforms_supplement.cypher; real labels
      AisCapability skos:Concept / AisTool prov:SoftwareAgent — with the s;
      capability areas orchestration/etl/file-transfer; 7 tools incl. 2
      internal MFT products, names held company-side; USES_TOOL app edge +
      IN_CAPABILITY roll-up; SchedulerKind deprecated there same day).
      Captured sanitized in platforms.yaml#company_confirmed (status:
      captured, confirmed: false); Q1-Q5 open questions updated (Q2/Q3
      corroborated, Q1 granularity conflict + Q4 software-registry overlap =
      the remaining gate questions); C6 map entry + IDEAS line re-pointed.
      Nothing flipped active.
    notes: >
      Groomed 2026-07-21 from the AIS-taxonomy back-flow inbox line (flagged in
      the 66acea8 port report 2026-07-10, unactioned since). The company's
      platforms.yaml is AHEAD with a confirmed AisCapability/AiTool taxonomy
      while the producer still holds the deprecated :SchedulerKind placeholder —
      bring the evolved model across so the gate session decides from it instead
      of re-deriving. The backlog pull loop SKIPS this item until the user
      supplies the material (the J13/J10 confirm-at-start idiom). The sibling
      SchedulerKind-deprecation inbox line stays parked on that same gate.

# Roll-up the dispatcher can glance at. COMPUTED views — test_backlog.py enforces
# that these match the items exactly; update them with any status change.
  - id: O24
    epic: web-console
    title: "SEAL-contacts override list: steward mapping domain for operate-manager (L1/L2) corrections — DB-table store with a source-vs-user origin flag + a source-corrections report for the AOs"
    type: task
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done                                     # 2026-07-21 pm (`b241661`): committed override list config/overrides/seal-contact-overrides.csv → seal_contact_override table in mapping.db (fail-closed ingestion, source-hash tracked); v_seal_contact_grid = origin-flagged ADJACENT rows (source first, never merged); domain seal-contact-override live on /mappings (SealOverridePane + ?domain= deep-link, DEMO fallback); POST /mappings/overrides/draft returns the COMPLETE updated file (commit-by-replace, authored_by server-stamped); GET /mappings/overrides/report = AO-facing source-corrections markdown; spec mappings.seal-contact-roles.v1 pairs LIVE K4 attributions as source rows; enforcement-matrix surface 15; +13 tests, suite 815 green, endpoints + pane live-smoked. Graph-precedence stayed OUT (open gate question, as ruled).
    depends_on: [O13]
    inputs: [drydocs_api/mappings.py, drydocs_core/mapping_store.py, web/src/routes/MappingsRoute.tsx, drydocs/loaders/cypher/seal_contacts.cypher, drydocs_api/query_specs.py, config/gate-log.md]
    acceptance: >
      A new steward mapping domain (seal-contact-override) joins the /mappings
      domain strip via the registry (the O13 mechanics): its grid shows the
      current SEAL operate-manager attributions (L1/L2) with an ORIGIN flag on
      every row — source (SEAL) vs user override — always visible, never
      hidden; stewards draft override entries (app seal_id + role/level +
      corrected holder SID + REQUIRED rationale) that persist in the
      mapping-store DATABASE TABLE (the file-to-table enhancement: the
      committed override list is the source the derived var/mapping.db
      materializes from, so it stays rebuildable); overrides NEVER write the
      graph and NEVER silently replace the SEAL value — where both exist the
      surfaces show them side by side; a SOURCE-CORRECTIONS REPORT export
      (artifact + manifest) lists every override with the SEAL current value,
      the corrected value, author and rationale, formatted for the
      application owners who hold the AO privilege to fix SEAL itself.
      Whether overrides ever influence graph-side attribution (a precedence
      tier) is explicitly OUT of scope — recorded as a gate question for the
      ui-write-surface / K-family gates, not decided here. Tests cover the
      origin flag, the override-store round-trip, and the report content.
      Full suite green.
    notes: >
      Promoted 2026-07-21 pm directly from SME direction in the O20 review
      chat (the C9 direct-promotion precedent): SEAL Contacts L1/L2 Operate
      Manager data is known-inaccurate; fixing SEAL requires the application
      owner (AO privilege) to update it manually, so DryDocs carries the
      mapping FROM SEAL plus a user override list, flagged by origin, feeding
      a corrections report — fix-the-source doctrine, not a graph write. The
      same direction is amended into config/gate-prompts/ui-write-surface.yaml
      (M2 non-graph-store ratified as the standing pattern; formal sign-off
      still at the O20 session). Module drydocs-api (domain registry + store
      + report endpoint); the /mappings screen half rides the existing O13
      surface.

  - id: O25
    epic: web-console
    title: "Gate-page M1 drafting affordance: a Draft-gate-log-entry button on rendered gate pages assembling the sign-off snippet from ticked confirmations"
    type: task
    module: drydocs-review
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done                                     # 2026-07-21 pm (`632def3`): Draft-gate-log-entry + Copy-snippet + Download-.md on every rendered gate page; client-side ONLY (M1 exactly — ticks browser-local, zero server writes); house-format heading with in-browser date, per-section [x]/[ ] lines + counts, all-ticked→SIGNED OFF else PENDING; drift-proofed — Python prebuilds every emittable line (JS selects/joins only) and draft_gate_log_entry() is the pinned reference (+4 tests: all-ticked, partial, empty-spec, page-embedding); count guards hold; suite 819 green; O20 page re-rendered + smoked.
    depends_on: [O20]
    inputs: [drydocs/gate_pages.py, tests/unit/test_gate_pages.py, config/gate-log.md, config/gate-prompts/ui-write-surface.yaml]
    acceptance: >
      Rendered gate pages (render_gate_page) gain the M1 affordance the O20
      gate signed off (SME-2, 2026-07-21): a Draft-gate-log-entry control that
      assembles a config/gate-log.md entry SNIPPET client-side from the page's
      ticked confirmations (heading in the house format, per-section
      ticked/unticked summary, date placeholder) and offers copy-to-clipboard
      and/or file download — the SME reviews, edits, and commits it; the page
      writes NOTHING server-side (ticks stay browser-local; M1 exactly, per
      the gate ruling — an M2 upgrade would be its own decision). Unit tests
      cover the snippet content (all-ticked and partially-ticked shapes);
      existing checkbox-count and format guards stay green. Full suite green.
    notes: >
      Groomed 2026-07-21 pm at the O20 sign-off (gate-log SME-2): gate pages
      are NECESSARY for building and confirming (no upfront spec), so the
      drafting affordance is first-class. Pure renderer work in
      drydocs/gate_pages.py (module drydocs-review — the H4 gate-page
      generator's home); zero api surface.

  - id: C12
    epic: ontology-mapping
    title: "Run the platforms-taxonomy HITL gate: retire SchedulerKind into the software-registry model (confirm the 2026-07-21 rulings; B4/B5 residual), then groom the build follow-ups"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: done          # DONE 2026-07-21 pm — gate RUN in-chat same session as the pre-rulings: 3/3 as recommended (A+B1–B3 as written; B4 existing typing covers; B5 airflow row stays); gate-log entry appended; platforms.yaml confirmed: true; follow-ups groomed C13/C14; gates.json + matrix regenerated
    depends_on: [C6, C11]
    inputs: [config/gate-prompts/platforms-taxonomy.yaml, config/taxonomy/platforms.yaml, config/taxonomy/software-registry.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, config/gate-log.md]
    acceptance: >
      USER-GATED START — the SME convenes the gate session (the pull loop
      skips this item until then; the J13/C11 idiom). The rendered
      platforms-taxonomy gate page is walked in-session: section A plus the
      2026-07-21 pm rulings (B1 USES_TOOL retired -> the ACTIVE USES_SOFTWARE
      edge with source: 'batch-port'; B2 no capability node layer — role:
      orchestrator carries the classification; B3 merge into :SoftwareProduct;
      Q6 Ais* removed both sides, acronym preserved only in
      software-registry.yaml#acronyms) are confirmed AS WRITTEN or amended;
      B4 (typing note) and B5 (airflow row) are ruled. Sign-off transcribed to
      config/gate-log.md; gates.json + enforcement matrix regenerated;
      platforms.yaml flips confirmed: true. The build follow-ups are GROOMED
      as items, not built here: SchedulerKind seed/constraint retirement, the
      USES_SOFTWARE batch-port loader migration, seal_requires_scheduler +
      requires-scheduler map-entry retirement, and the Ais* straggler sweep
      (README / NODE_QUICK_REFERENCE / LoadPlanV3 refs). Nothing flips active
      inside this item; full suite green.
    notes: >
      Groomed 2026-07-21 pm from the SchedulerKind-deprecation inbox line
      (parked since 2026-07-09; its groom-condition fully fired same day: C11
      captured the company shape in the morning, the SME ruled the reshape
      in-chat in the afternoon — Ais* removed both sides, registry model wins,
      gate prompt reshaped to confirm-as-written). Model fable per the matrix:
      the session ratifies ontology/class decisions. The K5 gate-RUN item is
      the precedent for itemizing a gate session.

  - id: O26
    epic: web-console
    title: "App-to-app path runbook view: Runbooks-page tab with source/target application pickers, shortest-path TECHNICAL/DATA lanes, and a generated runbook grid citing path nodes"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo         # SME HOLD 2026-07-22: runbook template shape goes through a HITL template session BEFORE this view is built — deps are done but do NOT pull this item until that session rules (see summary groom note)
    depends_on: [O11, O17]
    inputs: [UI-WIP/wf-runbook-path-01.md, UI-WIP/wf-runbook-path-01.html, web/src/routes/RunbooksRoute.tsx, drydocs_api/query_specs.py, web/src/runbooks/demoRunbooks.ts]
    acceptance: >
      The /runbooks page gains an App-path tab implementing the
      wf-runbook-path-01 wireframe: source/target BusinessApplication pickers
      -> a shortest-path query registered as QuerySpec runbooks.app-path.v1
      (the O11 registry, provenance manifest included) -> the path rendered in
      two lanes (TECHNICAL and DATA) -> a GENERATED runbook grid in which
      every step cites the path node it derives from. Lane partition derives
      from LABEL SETS ONLY — the layer/c4_level node-property idea stays a
      flagged open gate question (no new node properties, no ontology writes,
      no new edge types). Works against the demo dataset without Neo4j (the
      O9 pattern); tests cover the spec registration and the lane partition;
      full suite green.
    notes: >
      Groomed 2026-07-21 pm from the wireframe inbox line (wireframe committed
      with the Epic O landings, dc89191). Mechanism-only twin of the internal
      search-for-file-name use case — the root screenshot stays uncommitted;
      the file-name-contains anchor rides the same tab. O17 (/runbooks) and
      O11 (QuerySpec registry) are done, so the seat exists.

  - id: G26
    epic: component-topology
    title: "Launcher registry becomes a config file: move LAUNCHER_REGISTRY out of commands.py into a schema-guarded config so teams add wrappers without a code release"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/controlm/commands.py, config/taxonomy/software-registry.yaml, scripts/render_enforcement_matrix.py, tests/unit/test_command_parser.py]
    acceptance: >
      LAUNCHER_REGISTRY rules live in a config file (the config/ pattern, e.g.
      config/launcher-registry.yaml) with a schema-guard unit test; commands.py
      LOADS the registry instead of defining it, with no behavior change (the
      existing parser/classifier suite stays green untouched); every
      classifier_rule id referenced by software-registry.yaml
      invocation_patterns rows still resolves (a test asserts the id set —
      those rows pin rule ids for gate review and must not silently break);
      the O12 enforcement matrix flips the launcher-registry surface from the
      code-resident unguarded-config example to a guarded config surface
      (matrix regenerated, drift test green). End state: adding a wrapper rule
      is a config edit plus a test run, no Python change.
    notes: >
      Groomed 2026-07-21 pm from the 2026-07-16 SME requirement line (captured
      at the cmdline-lineage-review mini-gate); its admin-screen end state
      landed as O12 (done, cd76c91), which renders this registry as the
      visible unguarded-config example — this item is the migration that
      retires that example. Epic G / phase 6 per the G15/G16 precedent (the
      same drydocs_core/controlm engine).
      CLOSED 2026-07-27: config/launcher-registry.yaml (schema
      drydocs.launcher-registry.v1; ordered rules, ignore_case flag,
      named_launcher marks the G16 value-contract subset) + commands.py
      load_launcher_registry() (loud ValueError on schema mismatch/empty —
      a silently-empty registry would classify everything UNKNOWN);
      tests/unit/test_launcher_registry.py guards shape, id uniqueness,
      module/file parity, and the invocation_patterns rule-id set; matrix
      surface flipped to guarded (test updated to assert the inverse it
      used to pin). BONUS CATCH on first run: the proposed
      abinitio-dtlaunch-wrapper pattern row referenced the extinct
      abinitio.dtlaunch_accelerator rule AND contradicted the 2026-07-16
      DPL-is-not-AbInitio ruling — row removed with an audit comment (DPL
      joins the gate spec candidates-without-a-product open question).
      Parser suite green untouched (no behavior change); suite 960.

  - id: C13
    epic: ontology-mapping
    title: "SchedulerKind retirement build: deprecate the ontology.cypher seeds + scheduler_kind constraint, retire seal_requires_scheduler and the requires-scheduler map entry, sweep the Ais* stragglers"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 pm — seeds retired audit-kept (commented; constraint stays for old graphs); seal_requires_scheduler deprecated + superseded_by: reg_uses_software; map entry rejected/superseded with all 3 OQs resolved; sweep: config/taxonomy/README, internal/repo-README, LoadPlanV3 banner, cli verify check retired; schema_graph regenerated (REQUIRES_SCHEDULER excluded); suite 831 green
    depends_on: [C12]
    inputs: [drydocs_core/schema/ontology.cypher, drydocs_core/schema/constraints.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, config/taxonomy-ontology-map.yaml, config/gate-log.md, knowledge/ontology/NODE_QUICK_REFERENCE.md]
    acceptance: >
      Per the C12 sign-off (gate-log 2026-07-21): the :SchedulerKind seeds in
      ontology.cypher and the scheduler_kind constraint are retired the
      audit-kept way (deprecated, not silently deleted — the company 06-29
      precedent); vocab entry seal_requires_scheduler flips planned ->
      retired/superseded with superseded_by: reg_uses_software, and the
      requires-scheduler map entry records the same closure;
      reg_uses_software's note keeps the source: 'batch-port' convention as
      the landing (no typing change — B4). The Ais* straggler sweep lands:
      README / NODE_QUICK_REFERENCE / LoadPlanV3-era references to
      REQUIRES_SCHEDULER-> SchedulerKind or the Ais* classes updated to the
      registry model (the acronym survives ONLY in
      software-registry.yaml#acronyms). Drift guards (test_schema,
      gates/matrix) + full suite green. No graph writes in this item.
    notes: >
      Groomed 2026-07-21 pm at the C12 sign-off (section-C consequence; the
      K2 flips-are-follow-ups pattern). Everything here executes decisions
      already ruled — no new SME calls; anything ambiguous found mid-sweep
      goes back to the inbox, not auto-decided.

  - id: C14
    epic: ontology-mapping
    title: "Batch-port orchestrator migration: load USES_SOFTWARE {source: 'batch-port'} edges from the SEAL batch-port orchestrator strings to registry SoftwareProducts"
    type: task
    module: drydocs-load
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-21 pm — batch_port_orchestrator loader + cypher (MATCH-only endpoints, MERGE keyed {source:'batch-port'}); crosswalk from platforms.yaml seed rows; unmapped = flagged + reported, never guessed; registry cypher hardened to key {source:'registry'} (live shared pair DRYDOCS-SELF->controlm); live-verified on the EE container (3 batch-port + 4 registry edges, idempotent re-runs both writers); 9 new tests; suite 840 green
    depends_on: [C12]
    inputs: [drydocs/loaders/software_registry.py, drydocs/loaders/cypher/software_registry.cypher, config/taxonomy/platforms.yaml, config/taxonomy/business-application.yaml, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: >
      Per the C12 sign-off: a loader pass migrates the SEAL batch-port
      orchestrator string to app-level edges
      (:BusinessApplication)-[:USES_SOFTWARE {source: 'batch-port'}]->
      (:SoftwareProduct) via the platforms.yaml seed rows'
      software_registry_ref links (controlm/autosys/airflow); the source
      property distinguishes these declared-orchestrator edges from
      DRYDOCS-SELF stack rows and future 'controlm-cmdline' detections on the
      same edge type; idempotent MERGE (re-run safe); an unmapped orchestrator
      string is REPORTED, never guessed (the coverage-policy pattern). Tests
      cover the mapping, the source stamping, and the unmapped path; live
      verify against the EE container; full suite green.
    notes: >
      Groomed 2026-07-21 pm at the C12 sign-off. Zero new edge or node types —
      reg_uses_software is active and B4 confirmed its typing; this is purely
      a new source value on an existing edge. Module drydocs-load (loader
      work); ontology epic keeps the gate lineage visible.

  # ── appended at the 2026-07-22 groom (Backstage catalog assessment T1–T8;
  #    source = UI-WIP/backstage-catalog-assessment.md — the shallow clone was
  #    deleted the same day, the assessment doc is the durable record) ────────
  - id: G27
    epic: component-topology
    title: "ETLProcess kind-enum rider gate brief: extend the kind enum vs keep it closed with a governed discriminator (Backstage closed-kind/open-type precedent)"
    type: task
    module: ontology
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: done          # DONE 2026-07-22 same session — config/gate-prompts/etlprocess-kind-enum.yaml drafted (A context / B1 admit-the-value RECOMMENDED vs B2 hold-the-3-set / C consequences / D sign-off); gates.json + enforcement-matrix.json regenerated (28 gates); suite 864 green. The GATE SESSION itself is still awaiting the SME.
    depends_on: []
    inputs: [UI-WIP/backstage-catalog-assessment.md, config/gate-prompts/, drydocs_lineage/writer.py, drydocs_core/ontology/relationship_vocabulary.yaml, config/gate-log.md, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      A drydocs.gate-prompt.v1 spec exists
      (config/gate-prompts/etlprocess-kind-enum.yaml) for the kind-enum rider
      G17(c) surfaced: whether the :ETLProcess kind value set
      (etl|utility|notification) EXTENDS per MAC pipeline.json subType values
      (provisioning = DB load with no transform) or stays a small closed set
      with subtype carried as a separately governed property. The brief frames
      both options with a recommendation and cites the Backstage precedent
      verified 2026-07-22 (kinds few and closed + spec.type open-but-governed;
      explicit anti-catch-all-"Other" guidance) as external prior art, plus the
      G12/G17 history. The DECISION stays gate-bound: no enum, vocabulary, or
      writer change lands from this item — build follow-ups are groomed at
      sign-off (the K2 flips-are-follow-ups pattern).
    notes: >
      Groomed 2026-07-22 from assessment T1; started the same session (user
      pull). Fable per the matrix — the brief frames an ontology/schema
      decision. The rider originates from G17(c) acceptance ("any
      enum-semantics question surfaced as a gate rider, never auto-decided").

  - id: O27
    epic: web-console
    title: "QuerySpec authoring conventions: consume gated edges not raw staging columns; canonical external ref grammar; no graph element ids on external surfaces"
    type: chore
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # closed 2026-08-01 (desktop session)
    depends_on: []
    inputs: [drydocs_api/query_specs.py, UI-WIP/backstage-catalog-assessment.md, web/src/]
    acceptance: >
      A conventions section (drydocs_api/ docstring or a sibling doc,
      discoverable from query_specs.py) records three rules with rationale:
      (a) specs read gate-confirmed materialized edges and properties, never
      re-derive meaning from raw staged columns (precedence resolution lives in
      the loaders — the Backstage consume-the-relation-not-the-spec-field
      rule); (b) a canonical external ref grammar kind:namespace/name
      (namespace = data center where names collide across the 4 DCs) is
      defined for deep links and export manifests; (c) graph-internal element
      ids / uids never appear in URLs, manifests, or exports (documented as
      unstable). Existing registry specs are audited against (a) with
      violations fixed or flagged; a registry invariant or unit test enforces
      (c) where mechanical.
    notes: >
      Groomed 2026-07-22 from assessment T2+T3 (one conventions home: two
      consumption rules + the ref grammar). p3 — largely codifies what O11
      already does.
      CLOSED 2026-08-01. drydocs_api/AUTHORING.md holds the three rules with
      rationale, linked from query_specs.py's module docstring. The audit
      found the registry ALREADY compliant on all three — which was the point
      worth writing down: compliance was a property of careful authoring, not
      of the system, so nothing would have caught the first violation.
      Three things the work changed relative to how the item was framed:
      (1) The ref-grammar namespace is NOT the data center. O27 was groomed
      "namespace = data center where names collide across the 4 DCs"; checked
      against drydocs_core/schema/constraints.cypher that holds for exactly
      ONE node kind (ControlMHostGroup's (data_center, name) NODE KEY).
      ControlMJob and Condition — the two kinds most likely to appear in a
      deep link — are FOLDER-scoped ((folder_id, job_id) and (folder_id,
      name)). The rule shipped as "namespace = the declared uniqueness scope",
      which yields the data center where the key says so and the folder where
      it does not, and self-maintains because a new loader's NODE KEY already
      states its namespace. The doc's table is held to the constraint file by
      test_authoring_doc_namespace_table_matches_declared_keys — otherwise it
      would be a record checkable only against itself.
      (2) Rule (a) needed its NON-violations stated, or it would push authors
      into worse Cypher. explorer.conditions.v2's property join looks like a
      violation and is not: folder_id is a declared NODE KEY component, so the
      join resolves identity rather than re-deriving a relationship. Flagged
      not fixed — no folder->condition edge exists (conditions attach to JOBS
      via REQUIRES_IN_CONDITION/EMITS_OUT_CONDITION), and materialising one is
      an ontology decision for the gate, never a read-spec edit.
      (3) The element-id guard went where the RISK is, not where the item
      pointed. The acceptance said "a registry invariant"; the registry is
      hand-authored and reviewed and had zero violations, while the ephemeral
      path (R4) carries LLM-authored Cypher into the same export/manifest
      code. Guarding only the registry would have put the check on the path
      that cannot fail. Both paths now call one guard.ensure_no_element_ids —
      placed in guard.py beside ensure_read_only, which also earns it
      _code_regions' comment/string stripping (RETURN 'id(' as data cannot
      false-positive; the ADR 0003 lesson) and its lower-casing, which is what
      makes the check case-insensitive across ElementId( / ID( spellings. The
      text2cypher prompt carries the rule too: the prompt prevents, the guard
      catches. Zero first-party elementId uses exist repo-wide, so all of it
      is regression-only.
      Suite 1277 passed / 5 skipped (was 1272); ruff check + format clean.

  - id: C15
    epic: ontology-mapping
    title: "relationship_vocabulary.yaml gains a display-only inverse_label per entry (SUPPORTS -> 'supported by') for endpoint-correct edge phrasing"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs_core/ontology/relationship_vocabulary.yaml, tests/unit/test_schema.py, docs/RELATIONSHIP_GUIDE.md, UI-WIP/backstage-catalog-assessment.md]
    acceptance: >
      Every relationship_vocabulary.yaml entry carries inverse_label — the
      target-side display phrasing (e.g. SUPPORTS -> "supported by") — with
      presence enforced by the vocabulary schema test; RELATIONSHIP_GUIDE.md
      documents the field as PRESENTATIONAL ONLY (no direction, type, status,
      or semantics change; storage stays one directed edge; explicitly not a
      gate matter, recorded as such); full suite green. Consumption (the node
      inspector phrasing incoming edges from the target side) rides the next
      inspector touch in Epic O, not this item.
    notes: >
      Groomed 2026-07-22 from assessment T4 (Backstage inverse-pair naming,
      ownedBy/ownerOf). Labels-not-semantics, hence sonnet and no gate.
      CLOSED 2026-07-28: all 78 entries carry inverse_label (read as
      "<target> <inverse_label> <source>"); presence enforced by
      test_vocabulary_every_entry_has_inverse_label; field documented in the
      vocab header + a RELATIONSHIP_GUIDE "Maintaining" subsection recording
      PRESENTATIONAL ONLY / not-a-gate-matter explicitly; enforcement matrix
      regenerated (it keys on the vocab file). Inspector consumption rides
      the next Epic O touch per the acceptance. Suite 1082 green.

  - id: C16
    epic: ontology-mapping
    title: "Description-field metadata standard gains key-prefix governance (reserved core prefix; system-owned prefixes; unprefixed = team-local)"
    type: chore
    module: docs
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [knowledge/standards/technology/description-field-metadata-plan.md, UI-WIP/backstage-catalog-assessment.md]
    acceptance: >
      The description-field metadata plan's template phase includes a
      key-governance section modeled on annotation-naming governance: a
      reserved drydocs prefix for keys the template/graph owns; keys owned by
      external systems carry that system's prefix (the scim / jira / seal
      families); a bare unprefixed key is team-local and never load-bearing
      for ingestion; the plan's validation phase states it will enforce these
      rules. Doc-only — no loader or template build lands from this item.
    notes: >
      Groomed 2026-07-22 from assessment T7 (Backstage annotation/label key
      governance). Chore on the standards doc; the 3-phase plan itself stays
      PLANNED and un-groomed until activated. Epic ontology-mapping = closest
      governance home (naming-standard vocabulary), module docs — recorded
      here per the ask-only-when-ambiguous rule.
      CLOSED 2026-07-28: Phase 1 gains the key-prefix governance subsection
      (drydocs. reserved core; scim./jira./seal. + observed snow./mfts.
      system families; unprefixed = team-local, never load-bearing) with a
      grandfathering/migration note for the observed unprefixed inventory;
      Phase 2 states the enforcement rules (unregistered drydocs.*, bad
      system-value format, loader keying on unprefixed = findings; team-local
      keys informational only). Doc-only per acceptance — no loader or
      template build; the plan stays PLANNED until ratified.

  - id: O28
    epic: web-console
    title: "Node status envelope for the inspector + hub health glyphs: per-source namespaced status items, derived never hand-authored (status.items pattern)"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # closed 2026-08-01 (desktop session)
    depends_on: []
    inputs: [UI-WIP/backstage-catalog-assessment.md, UI-WIP/site-plan.md, web/src/, drydocs_api/]
    acceptance: >
      A documented node-status envelope — a list of per-source items
      {type (source-namespaced), level (info|warning|error), message, error?},
      always DERIVED by a producing system, never hand-authored — is adopted
      as the contract for the inspector sidebar's status section and the
      landing hub's spoke health glyphs; at least ONE real producer is wired
      end-to-end (first candidate: the loader per-run WARN/reject stream
      attaching to its :JobRun / loaded nodes) through a QuerySpec returning
      that shape; the envelope doc records the Backstage status.items
      precedent and the rule that new sources add namespaced types, never new
      shapes.
    notes: >
      Groomed 2026-07-22 from assessment T5. Requirement (future capability) —
      the layer-4 context-graph surface in the UI; p3 until a module needs
      live health.
      CLOSED 2026-08-01. Contract in knowledge/standards/node-status-envelope.md
      (Backstage status.items precedent recorded, plus the two rules: always
      DERIVED by a producing system, and new sources add NAMESPACED TYPES never
      new shapes). Producer = BaseLoader._close_run via status_items_for(), a
      PURE function of the run summary — which is what makes "always derived"
      a testable property rather than a promise (8 tests, no database). Wire =
      loads.status-items.v1, live end-to-end as the /loads Status frame:
      loader derives -> rides the :JobRun -> spec unwinds -> SpecGrid renders.
      UI adoption via one shared component (StatusItems + HealthGlyph) used by
      BOTH the Explorer inspector's status section and the landing hub's spoke
      glyphs, so a new producer lights both with no UI change.
      Four design decisions worth keeping:
      (1) A PROPERTY on :JobRun, not a node + relationship. A relationship type
      is an ontology decision that goes through the HITL gate (CLAUDE.md §1),
      and a derived health signal has no ontological meaning to gate — modelling
      it as an edge would have made every future producer's rollout gate-bound
      for no semantic gain. This is why O28 could close without a gate session.
      (2) JSON strings, not parallel lists. Neo4j cannot hold a map inside a
      list property; parallel lists (status_types/status_levels/...) would put
      the envelope's integrity in the ALIGNMENT of three lists, where one
      mismatched append silently mislabels an item's severity. One string per
      item cannot desynchronise.
      (3) NO all-clear item. A clean run stores an empty list, so "healthy" (a
      producer ran, nothing to report) stays distinguishable from "unknown"
      (no :JobRun at all). Collapsing them is how a dashboard ends up green
      because nothing is watching — the hub renders every spoke except loads
      as UNKNOWN today, deliberately, rather than a false green.
      (4) The demo inspector is fed through the SAME parser as the live path
      (demoStatus.ts authors JSON strings, parseStatusItems reads them), so a
      shape change breaks the demo surface loudly instead of leaving it
      rendering a format nothing produces.
      Scope note: the Explorer inspector's items are demo-derived because that
      canvas is the SYNTHESIZED demo graph — wiring it to live data needs a
      demo->live selection bridge that does not exist. That is graph plumbing,
      not contract work; the acceptance's "at least ONE real producer wired
      end-to-end" is met by the /loads Status frame, which is fully live.
      The O42 component-ledger guard fired on the new component and was
      answered rather than silenced: StatusItems is registered UNBOUND (26/63)
      because it serves two modules, so binding it to either would misstate it.
      Suite 1285 passed / 5 skipped; ruff clean; tsc + vite build clean.

  # Groomed 2026-07-23 from UI-WIP/two-track-ui-plan.md Track 1 (T1-5/T1-6/T1-7/
  # T1-8) — the "UI acceleration session" groom candidates, misfiled below the
  # audit trail in IDEAS.md and folded back into the inbox groom.
  - id: O29
    epic: web-console
    title: "Trust-tier / edge-provenance legend surfaced live in-UI on the lineage and docs graph canvases"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-07 (desktop, same session as the claim). BUILT: web/src/components/TrustLegend.tsx — the adopt of context-graph's declared/observed legend pattern (ui-architecture-analysis.md "surface that in the live UI too, not only in the export manifest"). Tier palette REUSED from underhood/benchmarkData.ts (VERBATIM --green / GROUNDED --blue-br / SYNTHESIZED --yellow — no new colors), edge-provenance key = declared solid / observed dashed, all colors via theme CSS variables. Wiring: LineageGraphPane renders it directly; MiniDag gained an opt-in `legend` prop and ONLY DocsRoute passes it (Gates/Remediation/Runbooks consumers unchanged — they opt in when their content mixes trust). Rendered inside the ReactFlow canvas as a bottom-left Panel (!ml-12 clears the zoom controls — the first screenshot caught the overlap). VERIFIED LIVE (headless-Edge, dev server + ?as= affordance): DOM dumps show the legend on /lineage and /docs; BOTH THEMES live-verified — dark via the OS scheme, light via a throwaway public/ probe page seeding drydocs.theme.v1 before redirect (deleted after; data-theme-mode="light" confirmed in the dump); screenshots in the session record. Ledger: TrustLegend registered in ui-components.yaml (deliberately UNBOUND — serves two modules) and the coverage pin moved 65->66 with the O29 line in its history. Suite 1698/3/5 (the 3 = the standing Idea-83 rich-ANSI env failures, untouched); web build + oxlint green (only-export-components warning = the same pre-existing class as rightSidebarContext/HubGlyphs).
    depends_on: [O10, O18]
    inputs: [web/src/, UI-WIP/two-track-ui-plan.md, internal/context-graph-analysis/ui-architecture-analysis.md]
    acceptance: >
      A legend component (declared vs observed / trust-tier color key) renders
      on both the /lineage and /docs graph-pane canvases, reusing the existing
      tier vocabulary (VERBATIM/GROUNDED/SYNTHESIZED, the KIND_TOKEN palette)
      rather than inventing new colors; tokens read the theme CSS variables so
      it renders correctly in both dark and light mode; visible on the canvas
      itself, not only in the export manifest; suite + build + lint green.
    notes: >
      Groomed 2026-07-23 from UI-WIP/two-track-ui-plan.md T1-5 (adopt of
      context-graph's declared/observed legend pattern, per
      internal/context-graph-analysis/ui-architecture-analysis.md). Sonnet-
      able — reuses existing tier vocab and canvases (O10/O18 both done), no
      new ontology or edge semantics.

  - id: O30
    epic: web-console
    title: "Retire App.css legacy mockup classes into the Tailwind/token idiom (SignIn, MyApps, GraphExplorer, TowerDrill, CypherConsole)"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-07 (desktop, same session as the claim). App.css is now a COMMENT-ONLY STUB recording the migration; all five surfaces (SignIn / MyApps / GraphExplorer / TowerDrill / CypherConsole) style inline through the Tailwind/token idiom. Mechanics worth keeping: the shared SVG text styles moved to GraphSvg.tsx as exported GNODE/GSUB/GEDGE consts (GraphExplorer reuses them); the cypher-syntax spans (.kw/.lbl/.rel/.str) live inside data/towers.ts HTML literals, so they are styled FROM TowerDrill's <pre> via Tailwind arbitrary-variant descendant selectors ([&_.kw]:text-...) rather than a stylesheet; the dynamic role/badge variants became lookup records (ROLE_BADGE/SNOW_BADGE). Mockup dark hardcodes (#c8d2de, #0a111b, syntax colors) kept VERBATIM for pixel parity — O32 owns the light redesign. DELIBERATE delta beyond the five: App.css's global table/th/td rules used to cascade forced 11.5px mono cells and dark-hardcoded text over every self-styled table in the console (Gates/Mappings/Admin/etc.) — removal moves those tables to their authored Tailwind intent, which also un-breaks their light mode (the hardcodes were dark-only); recorded in the stub. Dead rules (.hero/.features/.tower*, last referent = the Idea-77 orphan HeroArt) died with the file. VERIFIED (headless-Edge, dev server, ?as= affordance): screenshots of all five — signin, console (admin), tower drill (cypher colors + all three table treatments), explorer (error-panel state; the live graph pane needs the API, but its text classes are the GraphSvg ones verified in the tower shot), and MyApps via a temporary toggle-default flip on the ownership pane (reverted, git-verified clean). Build + oxlint green; UI ledger guards green (no inventory change — no files added/removed).
    depends_on: []
    inputs: [web/src/App.css, web/src/components/SignIn.tsx, web/src/components/MyApps.tsx, web/src/components/GraphExplorer.tsx, web/src/components/TowerDrill.tsx, web/src/components/CypherConsole.tsx]
    acceptance: >
      web/src/App.css is deleted (or reduced to a comment-only stub recording
      the migration) and the five named surfaces (SignIn / MyApps /
      GraphExplorer / TowerDrill / CypherConsole) style entirely through the
      Tailwind/token classes already used elsewhere in web/src/; no visual
      regression on any of the five (before/after screenshot or headless-Edge
      smoke in both themes); build + lint green.
    notes: >
      Groomed 2026-07-23 from UI-WIP/two-track-ui-plan.md T1-7. Pure
      hygiene/consistency chore — no new component behavior.

  - id: O31
    epic: web-console
    title: "Regenerate web/src/underhood/benchmarkData.ts from the docmeta evaluation-harness output instead of the hand-carried P0-verdict record"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: [web/src/underhood/benchmarkData.ts, knowledge/upgrade-plans/docmeta-p0-verdict.md, UI-WIP/two-track-ui-plan.md]
    acceptance: >
      benchmarkData.ts is produced by a documented regeneration step (a
      script, or a recorded manual procedure in the file header/a sibling
      README) that reads a real evaluation-harness run rather than the
      hand-carried P0-verdict numbers; the /under-the-hood scoreboard renders
      unchanged in shape from the regenerated data; build + lint green.
    notes: >
      Groomed 2026-07-23 from UI-WIP/two-track-ui-plan.md T1-8. No standalone
      "docmeta evaluation harness" backlog item exists yet (checked: R8
      "Answer evaluation + cap tuning" evaluates the Q&A console's OWN runs,
      not a reusable docmeta benchmark harness — a different surface) — per
      the groom instruction the dependency is recorded here as prose rather
      than a fabricated id; depends_on is left [] and this item is in
      practice blocked on that harness existing until one is groomed and this
      item's depends_on is updated to point at it.

  - id: O32
    epic: web-console
    title: "Light-mode design pass across the console (dark stays canonical; light is currently derived, not designed)"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-07 (desktop, same session as the claim). THE FINDING THAT REFRAMED THE ITEM: the light sheet already sat ON the DL-7 bridge values — --bg #f4f6f9 (~runtime monitor #F4F6FA) and --panel2 #eef2f7 (= its #EEF2F7 header tint) landed with the 2026-07-28 DL quick-wins, and the named surfaces are fully token-routed (hex census: zero hardcodes outside the Idea-77 orphan HeroArt). So the pass became what it should be: a surface-by-surface light-mode review — hub, shell, module template via the lineage AND docs canvas pages (incl. the O29 legend), /under-the-hood — against the DL-7 targets. ONE REAL DEFECT found and fixed, and it was a CASCADE bug, not a token: index.css's element defaults (a/code/table/button/inputs) sat UN-LAYERED and out-cascaded every layered Tailwind utility — the same root-cause class as O36's margin reset, one comment block apart — so `a {color: --blue-br}` ate every authored link color; the Overview CTA rendered navy-on-blue in light (unreadable) and wrong-blue-on-blue in dark. Fixed by moving those defaults into @layer base (utilities now win, as authored). HONEST DARK DELTA: dark is otherwise pixel-unchanged, but the CTA is now white-on-blue there too — that is the authored text-white applying for the first time, a defect fix, not a redesign. tokens.css header records the pass (light is no longer "derived, not designed"); the tower-drill mockup pages alone keep dark hardcodes (recorded in the App.css retirement stub, O30). two-track plan rows T1-5/6/7 marked DONE. VERIFIED headless-Edge both themes (light via a parameterized probe page seeding drydocs.theme.v1, deleted after): light hub/lineage/docs/underhood + re-shot hub after the fix, dark hub for the unchanged check; screenshots in the session record. Build + oxlint green; suite 1698/3/5 (the 3 = standing Idea-83).
    depends_on: []
    inputs: [web/src/, UI-WIP/site-plan.md, UI-WIP/two-track-ui-plan.md]
    acceptance: >
      Every themed surface in web/src/ (shell/header, landing hub, the shared
      module template, the lineage/docs graph canvases, /under-the-hood) gets
      an intentional light-mode design pass rather than the automatic
      dark-token inversion in place today; verified via a light-mode
      headless-Edge smoke across at least the shell, one module page, and the
      lineage/docs canvases; dark mode remains the default/canonical theme
      and is visually unchanged; build + lint green in both themes.
    notes: >
      Groomed 2026-07-23 from UI-WIP/two-track-ui-plan.md T1-6 and the
      2026-07-23 UI-acceleration session's open follow-up ("light verified
      functional today, but derived, not designed"). Not previously tracked
      as its own item (checked backlog.yaml — only a passing site-plan §2
      mention of the equivalent solid-outline treatment).
      DL-7 merge (2026-07-28, internal/datalens-reference/continuity.md): the
      light pass should land on (or consciously near) the runtime monitor's
      observed values — bg ~#F4F6FA, tinted table headers #EEF2F7, threshold
      green/--status-fail semantics per DL-2 — light mode is the bridge
      surface between the console and the SRE runtime view, and theme
      polarity is the harshest transition between the two tools.

  # ── Epic R — Agentic Q&A console (ADR 0007; phase 15) ──────────────────────
  # Groomed 2026-07-23 from the llm-graph-builder vs knowledge-graph-of-thoughts
  # comparative analysis (dossier linked in ADR 0007). Tiered read-only agent:
  # QuerySpec router → schema-grounded text2cypher → bounded graph-of-thoughts
  # loop. O20 (UI zero graph writes) stands throughout the epic.

  - id: R1
    epic: agentic-qa
    title: "SME gate session: review ADR 0007 (agentic Q&A architecture) and rule the three open axes — context-graph escalation residency, :AgentRun target DB, LLM key strategy"
    type: task
    module: docs
    phase: 15
    agent: main
    model: fable
    priority: p1
    status: done          # gate session 2026-07-23 — ADR 0007 ACCEPTED as written; rulings in config/gate-log.md
    depends_on: []
    inputs: [docs/decisions/0007-agentic-qa-architecture.md, docs/restructure/IDEAS.md, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      ADR 0007 status flipped to ACCEPTED (or SME edits recorded and the ADR
      re-drafted) in a gate session; the three axes it leaves open are ruled
      and recorded in the ADR body — (a) context-graph escalation residency
      (in-process only vs ddcontext SYNTHESIZED envelope with session tag +
      TTL sweep), (b) :AgentRun envelope target DB (proposal ddcontext; never
      drydocs), (c) LLM key strategy for the ADK agents (Gemini vs Anthropic
      via LiteLLM) — closing the 2026-07-03 IDEAS.md key question into the
      ruling.
    notes: >
      Decision item → fable per the model matrix (residency + key strategy
      change component boundaries). Reference clones live in the sandbox
      beside this repo (llm-graph-builder, knowledge-graph-of-thoughts).
      DONE 2026-07-23 — accepted as written; rulings: (a) in-process-only
      task-graph residency (ddcontext persistence deferred, new gate if ever),
      (b) :AgentRun -> ddcontext via a dedicated writer boundary, (c) LLM keys
      environment-split — local/producer Anthropic, company Azure OpenAI,
      Gemini NOT the default (supersedes the 2026-07-03 IDEAS assumption);
      provider adapter + usage-extractor seam must cover both from day one.

  - id: R2
    epic: agentic-qa
    title: "graph_qa ADK app: Tier-0 QuerySpec router + Tier-1 schema-grounded text2cypher with server-enforced READ mode, fix loop, and the response envelope"
    type: requirement
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p1
    status: done          # live-verified 2026-07-23 (real model + real graph + api_server transport)
    depends_on: [R1]
    inputs: [agents/, drydocs_api/query_specs.py, drydocs_core/ontology/relationship_vocabulary.yaml, docs/decisions/0007-agentic-qa-architecture.md]
    notes: >
      2026-07-23 build landed: agents/graph_qa (pipeline, envelope, providers,
      schema_context, ADK agent, README=envelope contract), common/graph_read
      (READ-access-mode executor = the server-side write boundary; row cap 100,
      tx timeout 15s), common/specs_catalog (imports the drydocs_api registry —
      zero agent-owned named Cypher). Verified: 8 unit tests (fakes; Tier-0
      verbatim, prompt grounding+bounds, fix cap 2, pre-flight, contract,
      usage extractor) + 3 live integration tests (server rejects CREATE(x)
      and CALL db.createLabel probes that evade the token guards) + agents-venv
      structural smoke over live drydocs rows. DONE 2026-07-23 after the
      user keyed agents/.env: live two-question smoke (Tier-0 spec verbatim,
      7 rows; Tier-1 text2cypher after a 0-row spec fall-through, correct
      sched_table aggregation) + full transport smoke through adk api_server
      (/run returned the envelope; session memory events counted). Live
      findings fixed in-item: (1) 0-row spec now falls through to Tier-1
      (insufficient-context rule); (2) per-label properties added to the
      schema prompt (graph_schema_detailed); (3) prompt section budgets —
      whole-prompt truncation had silently dropped schema+examples (regression
      test added). Router force-fit tuning -> R8 ledger work. litellm gotcha:
      install wheels only (--only-binary), sdist needs Rust on Windows.
    acceptance: >
      The envelope contract is documented and returned per question: answer +
      steps[] (each with cypher, database, rows, ms, fix_retries) + sources
      (trust tier) + metrics (tokens prompt/completion/total, context size in
      tokens + chunks/rows, memory size in session events + tokens, timing
      breakdown). A question matching a registered spec routes to it and shows
      the spec Cypher verbatim (Tier 0). Tier-1 prompts are assembled from the
      ontology vocabulary + live graph_schema + few-shot spec examples — never
      whole-graph state. A write-shaped query is rejected server-side by READ
      access mode (a test proves the token guard alone is NOT the boundary);
      fix loop capped at 2; row caps + query timeout applied. The agent
      consumes the QuerySpec registry — it defines no named Cypher of its own.

  - id: R3
    epic: agentic-qa
    title: "Agent run telemetry: per-LLM-call JSONL ledger in DRYDOCS_LOGDIR + :AgentRun envelope node + console.agent-runs.v1 QuerySpec"
    type: requirement
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p1
    status: done          # 2026-07-30, desktop session (claimed + built same day)
    depends_on: [R2]
    inputs: [drydocs_core/run_log.py, drydocs_api/query_specs.py, docs/decisions/0007-agentic-qa-architecture.md]
    acceptance: >
      Every LLM call appends one JSONL line (run_id, step, model,
      prompt_tokens, completion_tokens, cost_est, duration_ms, iteration) to
      DRYDOCS_LOGDIR — never the repo. One :AgentRun node per question
      (kind 'qa', mirroring :JobRun) carries question sha256 + length ONLY
      (full text solely in the local ledger), tier, iterations, llm_calls,
      token totals, context/memory sizes, cypher count, fix retries, specs
      used, dbs touched, timings, staleness flags — written to the DB the R1
      ruling names (never drydocs) via a dedicated writer boundary. A
      console.agent-runs.v1 QuerySpec returns runs for the admin view.
      The envelope also RESERVES a hashed caller-identity slot (user_id
      sha256 + length, mirroring the question-text rule) — ADK 2.0 run_async
      takes user_id per call; full identity never lands in the graph.
    notes: >
      MERGED 2026-07-28 (groom): the caller-identity slot from the
      agent-runtime target-state review (ADR 0007 revisit check PASSED;
      detail in internal/agent-platform/, never public).
      2026-07-30 build: agents/common/llm_ledger.py (LlmLedger — one JSONL
      line per LLM call with step router/text2cypher/fix/answer + one run
      line per question; day-file qa.graph_qa.<date>.jsonl via
      drydocs_core.run_log.resolve_log_dir; the run line is the ONLY home of
      full question text; owns the model->price map, unknown model = None
      never a guess) + agents/common/agent_run_writer.py (the dedicated
      WRITE boundary — agent_run_props pure/offline-testable, kind='qa'
      mirroring :JobRun, MERGE on run_id into ddcontext; refuses 'drydocs'
      from BOTH env and argument) + pipeline ledger hook (cost accumulates
      into metrics.cost_est_usd; telemetry failure never kills an answer) +
      envelope user_id_sha256/user_id_chars filled from ADK ctx.user_id +
      console.agent-runs.v1 spec on ddcontext (watermarked by the standard
      rule). stale_sources=0 honest until R7. 8 new unit tests
      (test_agent_telemetry.py). Live :AgentRun write rides the next agent
      session against the EE container.

  - id: R4
    epic: agentic-qa
    title: "Ephemeral session-scoped QuerySpecs in drydocs_api: agent-executed Cypher becomes re-runnable/exportable through the existing /specs paths"
    type: requirement
    module: drydocs-api
    phase: 15
    agent: main
    model: sonnet
    priority: p1
    status: done          # 2026-07-30, desktop session (claimed + built same day)
    depends_on: [R2]
    inputs: [drydocs_api/query_specs.py, drydocs_api/app.py, drydocs_api/exports.py, drydocs_api/sessions.py]
    acceptance: >
      The agent registers each executed Cypher server-side and receives a
      hash-addressed explore_ref; /specs/{ref}/run and /specs/{ref}/export
      work for the owning session only, re-validated read-only, TTL-bounded;
      the export manifest carries cypher_sha256 + provenance exactly like
      permanent specs; the browser never submits raw Cypher and the
      /raw-cypher admin+dev gate is byte-for-byte unchanged (ADR 0005).
    notes: >
      2026-07-30 build: drydocs_api/ephemeral_specs.py (EphemeralSpecStore —
      ref = eph.<sha256(db+cypher+params)[:16]>, keyed (owner_token, ref),
      TTL 30min + capacity 500, params FROZEN at registration so run-time
      params fail closed) + pure register_ephemeral handler. Registration is
      a trusted-caller surface: POST /specs/ephemeral requires the
      DRYDOCS_AGENT_REG_KEY header (env unset = endpoint disabled), so a
      browser bearer token can never register Cypher — the design choice
      that keeps /raw-cypher (untouched in the diff) the only interactive
      Cypher surface. Classification = internal-confidential fail-closed
      ceiling (banner + filename prefix follow). run/export resolve eph.
      refs through the SAME exports.py code path (one manifest builder);
      foreign/expired/unknown all map to the same 404. Agent side: pipeline
      register_cypher hook fills StepRecord.explore_ref (failure degrades to
      null, never kills an answer) + stdlib urllib client
      agents/common/ephemeral_client.make_register; live wiring waits on R5
      forwarding the console session token to the agent. 12 new unit tests
      (test_ephemeral_specs.py incl. FastAPI end-to-end) + 1 pipeline test.

  - id: R5
    epic: agentic-qa
    title: "Ask spoke in the web console: chat panel with streamed agent steps, answer with citations + trust watermark, per-step Cypher panel, metrics chip"
    type: requirement
    module: drydocs-web
    phase: 15
    agent: main
    model: sonnet
    priority: p1
    status: done          # 2026-07-30, desktop session (claimed + built same day; third of the R4->R3->R5 run)
    depends_on: [R2, R4]
    inputs: [web/src/, web/src/lib/adk.ts, web/src/lib/graphApi.ts, docs/decisions/0007-agentic-qa-architecture.md]
    acceptance: >
      A non-admin persona asks a free-text question and sees: streamed agent
      steps while it runs; the answer with source citations carrying trust
      tier + classification; a "How I got this" panel showing every executed
      Cypher with database, rows, ms (and fix history when repaired);
      Open-in-Explorer / Export per step via the ephemeral explore_ref through
      the GraphAccess api adapter; a metrics chip (time, tokens, iterations).
      Zero graph writes from the UI path (cites O20). The admin console bench
      panel remains as-is for dev use.
    notes: >
      2026-07-30 build. Agent side: pipeline on_step observer (steps stream
      the moment they land; observer failure never kills an answer) +
      graph_qa/control.py (part 0 = question, later part = drydocs_control
      JSON with api_token/api_url — the R4 owner-token handshake, in-band so
      the shape is repo-owned on both ends and never reaches the LLM) +
      agent.py bridges the sync pipeline onto the ADK event stream via
      thread+queue, yielding {"kind":"step"} events then the envelope;
      per-request pipeline binds make_register to the ASKING session's
      token. Web side: /ask route for EVERY persona ('ask' module registry
      entry, retrieval:agent), adk.ts grows parts + /run_sse SSE parsing
      (buffered /run fallback), graphApi ApiClient.getToken + shared-client
      createApiAccess (one session for token AND runSpec — the ownership
      requirement), ask/askApi.ts envelope types + event parsing, AskRoute
      renders streamed steps, answer w/ SYNTHESIZED watermark + source
      citation chips (trust tier + classification via GET /specs; agent
      Cypher = internal-confidential ceiling), "How I got this" per-step
      panel w/ Cypher + fix history, Open-in-Explorer/Export = the SAME
      SpecGrid pointed at the eph. ref (run + both exports + manifest),
      metrics chip. VERIFIED on this machine: 1187 unit tests, tsc+vite
      build, oxlint clean; wire-level live e2e (real drydocs-api + real
      Neo4j + stub ADK because no agents venv here): non-admin login ->
      control handshake -> SSE steps arrive incrementally -> explore_ref ->
      runSpec 10 live rows -> export w/ cypher_sha256 manifest -> foreign
      session 404; headless-Edge render check of /ask?as=jdoe4821. Full
      LLM-in-the-loop smoke rides the agents-venv machine next session.

  - id: R6
    epic: agentic-qa
    title: "Tier-2 bounded graph-of-thoughts loop: enhance/solve controller with hard caps and the task-scoped context graph per the R1 residency ruling"
    type: requirement
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p2
    status: done   # 2026-08-01 (desktop)
    depends_on: [R2, R3]
    inputs: [docs/decisions/0007-agentic-qa-architecture.md, agents/graph_qa/]
    acceptance: >
      The loop engages only when Tier-1 context is insufficient; hard caps
      enforced (iterations <=2, next-step vote x3, fix loop <=2, per-question
      token budget) with a forced-solve fallback so every run terminates with
      an answer or an explicit "cannot answer"; the enhance branch writes ONLY
      the task-scoped context graph named by the R1 ruling (never drydocs /
      ddlineage — a test proves it); per-iteration snapshots are captured and
      renderable in the existing d3 graph pane; the R3 ledger records
      per-iteration cost so caps are tunable from data.
    notes: >
      CLOSED 2026-08-01. tier2.py is a PURE controller taking llm/retrieve
      callables rather than the pipeline, so the whole loop is testable with
      fakes and the private coupling stays in pipeline.py; retrieval REUSES
      Tier 1, which is why the <=2 fix loop and the read-only pre-flight are
      inherited rather than reimplemented — there is exactly one path that runs
      Cypher, and a test pins the executed sequence.
      Two bounding decisions the acceptance did not settle, both stated in the
      module docstring: (1) a tie or unparseable vote counts as SOLVE — the
      failure mode of voting is not a wrong answer, it is a loop that will not
      stop, so ambiguity resolves toward terminating; (2) the token budget
      bounds EXPLORATION, not the whole run — once spent the loop stops
      enhancing, but evidence already bought still gets one final answer call,
      deliberately over budget and recorded as budget.exhausted. Refusing that
      last call would spend the budget gathering evidence and then discard it
      unanswered. With NO evidence and no budget the run makes zero calls.
      The R1 residency proof is STRUCTURAL, not behavioural: task_graph.py has
      no driver, session, database name or Cypher in it, and the test reads the
      source and fails if any appears. A comment saying "do not persist this"
      is not a control; a module with nothing to persist with is.
      Also fixed a real gap the acceptance named only in passing — the ledger
      recorded iteration=1 for EVERY call, so per-iteration cost was
      unavailable and the caps were untunable from data. The loop now drives
      the iteration counter and a test pins round 2's calls to round 2.
      Every cap reports whether it BIT (forced_solve, budget.exhausted), and
      the budget is reported on runs it never touched — a limit you can only
      see once it has bitten is one nobody can tune before it does.
      Web: rather than a second d3 copy, the deterministic placement engine
      moved to lib/forceLayout.ts and GraphExplorer now consumes it; the new
      ask/TaskGraphPane keeps its OWN visual language on purpose, because a
      viewer who mistakes a synthesized sub-question for a real graph node has
      been actively misled. Snapshots are cumulative per phase (start /
      iteration N / final) so any frame renders alone. Suite 1315; tsc + build
      clean; O42 ledger 27/64 (TaskGraphPane bound to ask/).

  - id: R7
    epic: agentic-qa
    title: "Stale-source detection + rescrape queue: freshness fields on :Document, staleness check at answer time, queued full re-chunk refresh, rescrape-and-re-answer in the Ask spoke"
    type: requirement
    module: drydocs-docmeta
    phase: 15
    agent: main
    model: sonnet
    priority: p2
    status: todo   # 2026-08-01 claim RELEASED unbuilt — see notes: no fetcher exists (Q6), so the auto-run rescrape cannot be built
    depends_on: [R5, Q6]   # Q6 added 2026-08-01: it owns the web + filedrop connectors this item's rescrape needs
    inputs: [config/doc-source-registry.yaml, drydocs/loaders/bmc_docs.py, drydocs/loaders/essential_graphrag.py, docs/decisions/0006-docmeta-component-and-doc-graph.md]
    acceptance: >
      :Document nodes carry source_url, source_sha256, fetched_at, refresh
      policy, and a status lifecycle; answers flag citations whose source is
      stale per policy; rescrape is a QUEUED refresh artifact — auto-runnable
      for SYNTHESIZED corpora, re-queues HITL curation for CONFIRMED (the
      doc-source-registry sha256 rule, now code-enforced, never a silent
      overwrite); a changed sha triggers FULL re-chunk (a test proves
      position-resume is not used when content changed); "rescrape &
      re-answer" round-trips in the Ask spoke with both envelopes kept for
      comparison.
    notes: >
      CLAIMED THEN RELEASED 2026-08-01 (desktop) — the claim is withdrawn, not
      abandoned mid-build: nothing was written, so the item is exactly as it
      was, plus this finding. A pre-build survey found a HARD dependency the
      item never recorded. "Rescrape is a QUEUED refresh artifact — AUTO-RUNNABLE
      for SYNTHESIZED corpora" needs something that can actually refetch, and
      the producer has NO fetcher: the web + filedrop connectors are Q6's
      deliverable ("Port A: bkup scrapers -> the producer docmeta component"),
      Q6 is todo and parked on physical media confirmed absent on both
      machines, and its web connector is additionally gated on Q12's
      page-count refusal. Building the queue anyway would ship a queue nothing
      can drain and an "auto-runnable" path that cannot run — worse than not
      building it, because the surface would claim a capability that does not
      exist. Q6 therefore added to depends_on.
      What is NOT blocked, and why a SPLIT is the obvious alternative for the
      user to rule rather than for an agent to assume: clauses (a) the
      :Document freshness fields + status lifecycle, (b) staleness flagging on
      citations, and (d) full re-chunk on a changed sha are all buildable today
      against the existing loaders — only the auto-run rescrape and the Ask
      spoke's round-trip need the fetcher. Splitting freshness-now from
      rescrape-later would unblock roughly three quarters of this item.
      One further gap the survey turned up, cheap to fix inside whichever
      shape wins: `agents/graph_qa/pipeline.py` hardcodes `"chunks": 0` in two
      places with the comment "doc-chunk retrieval arrives with R7 corpora
      wiring", so the agent currently has no Document-sourced SourceRecord at
      all. Clause (b) is still reachable without new retrieval architecture —
      the O18 docs QuerySpecs already return doc_ids, so a routed docs spec's
      rows can be joined to Document freshness and stamped onto the existing
      SourceRecord.fetched_at/stale slots — but the item's inputs name neither
      the pipeline nor the specs, so whoever builds it should widen them.

  - id: R8
    epic: agentic-qa
    title: "Answer evaluation + cap tuning: on-demand answer metrics over stored question/context/answer, agent-runs admin view, promotion feed of recurring Tier-1 Cypher into gate-reviewed permanent specs"
    type: requirement
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: [R3, R5]
    inputs: [drydocs_api/query_specs.py, docs/design/graph-retrieval-benchmark-explainer.md]
    acceptance: >
      Evaluation (faithfulness / answer-relevancy style) runs on demand from
      the stored question/context/answer triple — never inline in the answer
      path; an admin view lists runs from console.agent-runs.v1 with cost and
      latency; the promotion feed ranks recurring Tier-1 Cypher by frequency
      and emits spec CANDIDATES for review — gate-bound, never auto-registered
      as permanent specs; loop caps re-tuned from ledger data with the change
      recorded in notes.
    notes: >
      Ties into the /under-the-hood retrieval-benchmark narrative (Q3
      verdict) — the evaluation harness should reuse its question set where
      possible.

  # ── Epic S — Pre-UI structure remediation (main; ADRs 0008/0009/0010) ──────
  # Source: docs/reviews/architecture-structure-review-2026-07-25.md — a scored
  # /architecture + /tech-debt pass over the tree, run because the console is being
  # built and the API contract + graph property names harden once routes and
  # fixtures depend on them.
  #
  # Grouped as its own epic rather than folded into G (component-topology) because
  # these share ONE review document, ONE phased plan (its §5), and THREE ADRs whose
  # acceptance gates the build items — the board should show that sequencing as a
  # unit. Each item still carries its correct existing plan phase, so the roadmap
  # strip is unaffected.
  #
  # Findings deliberately WITHOUT an item:
  #   F4  / F9 / F10-part  DONE 2026-07-25 (432ea43 boundary-guard fix, bbf29cf gitignore)
  #   F5  drydocs/ 4-component flat namespace — deferred to Phase C by ADR 0002-a-1;
  #       the review's §6 "what NOT to do" says explicitly not to reopen it mid-UI-build
  #   F15 two test roots — tests/ (pytest) and graph-tests/ (YAML acceptance runner)
  #       are two different mechanisms, not duplication; scored lowest, recorded only
  #   F11 depgraph-snapshot retention — a user call about audit history, re-inboxed
  - id: S1
    epic: structure-remediation
    title: "Rule on ADRs 0008 (orchestration parent) / 0009 (config substrate) / 0010 (app_id + id_authority)"
    type: task
    module: docs
    phase: 6
    agent: main
    model: fable
    priority: p1
    status: done
    depends_on: []
    inputs: [docs/reviews/architecture-structure-review-2026-07-25.md, docs/decisions/0008-orchestration-module-boundary.md, docs/decisions/0009-configuration-substrate.md, docs/decisions/0010-internal-source-term-abstraction.md, docs/decisions/README.md]
    acceptance: >
      Each of the three ADRs carries a Status other than Proposed — Accepted,
      Accepted-with-amendments, or Rejected — with the deciding session and date
      recorded in the ADR itself and in docs/decisions/README.md; an amended ADR is
      EDITED to the ruling, not annotated around it. ADR 0010 additionally names the
      HITL gate that owns its property-term binding (S3 runs it): this item authorizes
      the SHAPE, the gate authorizes the graph write. No code moves here — a rejection
      is a valid outcome and closes S2/S4/S5/S6/S7 with it.
    notes: >
      RULED 2026-08-01 (laptop; user ruling "move S1 to accepted"). All three ADRs now carry a
      status and a deciding date, and docs/decisions/README.md matches.
      0008 ACCEPTED as proposed (orchestration parent, no rename) -> unblocks S2.
      0009 ACCEPTED as proposed (git text stays source of truth permanently; mapping.db stays
      derived/gitignored) -> unblocks S4.
      0010 ACCEPTED WITH AMENDMENTS -> unblocks S3.
      .
      WHY 0010 COULD NOT BE ACCEPTED AS WRITTEN, and what changed: its own body carried a
      "PARTIALLY SUPERSEDED - do not build from this ADR as written" banner saying an
      amendment was OWED AT THIS ITEM. The business-application-identity gate had already
      SIGNED OFF 2026-07-27 (22/22, 0 edits) and overruled the ADR's central mechanism, so
      ratifying the text verbatim would have written a WITHDRAWN property (`id_authority`)
      into the decision record as accepted. S1's own acceptance requires an amended ADR to be
      EDITED to the ruling, not annotated around it - so the body was edited: title, Decision
      block, the six rules, the migration table, Option B/C dispositions, Consequences and
      Action items. The banner is replaced by an ACCEPTED-WITH-AMENDMENTS record that keeps
      WHY each thing changed.
      .
      The gate's substance, now in the ADR: the rule is TWO-PART - (i) identity takes a
      neutral name, (ii) evidence/provenance vocabulary KEEPS the source's own term, so
      ATTRIBUTION_TIERS 'SEAL' and match_method 'seal' STAY. This reverses v1 action item 3,
      which told the implementer to reword match_method. `id_authority` withdrawn (one
      permanent registry - the authority is declared once in the taxonomy file plus a
      seal-extract column ledger, which B6 records as DECLARATIVE, not a runtime mapping).
      `app_urn` deferred with a named trigger. Cutover is 8 key-bearing sites (4 MERGE, 4
      MATCH) in ONE atomic change, because a Neo4j uniqueness constraint IGNORES NULLS and a
      partial flip silently DOUBLES the canonical node. :Port follows now, and DROP its
      constraint first - CREATE CONSTRAINT ... IF NOT EXISTS matches on NAME and would no-op.
      .
      Also corrected while editing: the ADR cited config/taxonomy/business-application.yaml as
      the layer that "already got this right", quoting `identifier: SEALID`. Checked the live
      file - it reads `identifier: "Application ID"`, corrected at the gate sign-off, and NOT
      to `app_id`. That is the two-part rule in miniature: the taxonomy entry is evidence so it
      keeps the source's header, while the graph property is identity so it is neutral.
      .
      The sequencing inverted relative to the plan: S1 was created 2026-07-25 to rule the SHAPE
      before its gate authorized the graph write, and the gate ran first (07-27). For 0010 this
      item therefore RECORDED a decision rather than making one. Worth noting as a pattern -
      a decision item ahead of its gate only holds if the gate actually waits.
      .
      Created 2026-07-25 groom from the pre-UI structure review. The R1 precedent:
      a decision item ahead of its build items, so nothing gets groomed into a done
      deal. The review's own recommendations are: 0008 no rename, add a parent;
      0009 keep git YAML as source of truth, widen the derived SQLite read model;
      0010 app_id + id_authority instead of seal_id. S8/S9 deliberately do NOT depend
      on this item — neither needs an ADR ruling to start.

  - id: S2
    epic: structure-remediation
    title: "Create drydocs_core/orchestration/ as the parent of controlm/, with the vendor-neutral shell/paths/crosswalk surface beside it (ADR 0008, F1)"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: fable
    priority: p1
    status: done
    depends_on: [S1]
    inputs: [drydocs_core/controlm/, docs/decisions/0008-orchestration-module-boundary.md, config/crosswalks/, MODULE_MAP.md, tests/unit/test_module_boundary.py]
    acceptance: >
      drydocs_core/orchestration/ exists holding __init__.py, shell.py, paths.py and
      crosswalk.py, with controlm/ moved beneath it by git mv and its content otherwise
      unchanged — except the two mixed modules split along the line the ADR draws:
      commands.py into shell.py (argv/statement parsing, launcher registry) plus
      controlm/fields.py (PRECMD/POSTCMD/CMD_LINE routing), and paths.py into the
      neutral canonicalization half and the Control-M {ODATE}/FileWatcher token half.
      crosswalk.py reads config/crosswalks/*.yaml as the gate-confirmed native→baseline
      map. Every importer updated, MODULE_MAP.md carries the new core rows,
      test_module_boundary.py green (core still imports no component), full suite green.
      NOTHING in the graph changes — no label, no relationship vocabulary entry, no
      Cypher; :ControlMJob/:ControlMFolder/:ControlMServer stay exactly as they are
      (ADR 0003 rule 4).
    notes: >
      The review measured this before recommending it: ~1,100 of controlm/'s 1,725 lines
      are irreducibly Control-M (AutoEdit %%NAME|VALUE syntax, variable simulation,
      folder-name convention), so RENAMING the directory to orchestration/ would put a
      neutral name on the most vendor-coupled code in the repo — the exact failure ADR
      0004 exists to prevent. The real gap is that controlm/ has no parent and no
      sibling slot, so an AutoSys or Airflow module has nowhere to land. Additive:
      one git mv, one new package, two file splits. Do the CORE move before the console
      ships (the API's query specs are where an orchestrator assumption would first
      become a public contract); the drydocs/loaders/controlm_* + sql/ + cypher/
      reshuffle is internal and follows in S5's phase (review §5 Phase 3, F1b).
    progress: >
      DONE 2026-08-02 (laptop). One git mv, one new package, two file splits, one new
      module, 25 importers repointed. Suite 1352 / 7 skipped, ruff + format clean, no
      graph change of any kind.

      THE ADR WAS WRONG ABOUT ONE THING AND THE BUILD SAYS SO. ADR 0008 classified
      commands.py as "Mixed" and scheduled its "PRECMD/POSTCMD/CMD_LINE and
      EMBEDDED_SHELL/UCM field routing" out into controlm/fields.py. Measured here: that
      routing IS NOT IN THAT FILE. Those field names appear only in the module docstring
      and two explanatory comments; every executable line was already vendor-neutral. The
      real routing lives in controlm/variables.py (SHELL_VAR_NAMES + the EMBEDDED_SHELL
      classification — already vendor-side, which is where rule 1 wants it) and in
      drydocs/staging.py (component-side, load-cadence, ADR 0002-a §6). Neither moved:
      S2's acceptance freezes controlm/ apart from the two named splits. So commands.py
      went to orchestration/shell.py WHOLE, and fields.py is deliberately thin — the
      field vocabulary, a parse_field router for a caller holding a field NAME, and the
      UCM container-override extraction, which is the one genuinely Control-M-shaped
      function commands.py did carry. Inventing routing code to fill a planned module
      would have been worse than recording that the plan over-estimated the split.
      ADR action item 2 now carries this correction.

      THE paths.py SPLIT NEEDED A SEAM, NOT A CUT. Neutral and vendor were entangled:
      build_file_ref calls the canonicalizer, the date-token reader and looks_like_path,
      and all three carry Control-M vocabulary (?-runs -> {TS16}/{Q<n>}, {ODATE}, the
      unresolved-%% exclusion, the FILEWATCH role rule). Cutting the file in half would
      have either duplicated the assembly or left the neutral half importing the vendor —
      the exact direction rule 1 forbids. So the neutral module owns the FileRef shape and
      the assembly and gains PathDialect; controlm/paths.py declares the Control-M dialect
      and re-exports the neutral functions bound to it. Every existing caller gets byte-
      identical behaviour, and a second orchestrator supplies a dialect instead of forking
      the module — which is the reusability ADR 0008 was actually buying. NEUTRAL is a
      real usable dialect (identity canonicalization, no date tokens), not a stub.
      Ordering preserved deliberately: dialect role rules are checked BEFORE the neutral
      ones, because FILEWATCH was first in the pre-split list and has to keep beating the
      generic OUT/LOG hits on names like FW_OUT_DIR.

      crosswalk.py IS THE ITEM'S REAL VALUE. config/crosswalks/*.yaml had been
      gate-confirmed since 2026-07-14 with NO runtime reader, so `fidelity: no-equivalent`
      was a warning in prose that nothing could enforce — an unmapped concept could be
      rendered as the nearest Control-M label and the graph would assert something no gate
      ruled. resolve() now RAISES NoEquivalentError with no nearest-match fallback, tells
      the caller when a mapping is `approximate` rather than letting it read as exact,
      REFUSES a `status: proposed` crosswalk at runtime (a proposed crosswalk is a gate's
      input, not a mapping), and rejects an unknown fidelity value because a new tier is a
      gate decision, not a typo. 16 tests over both confirmed crosswalks.

      THE GUARD IS NARROWER THAN THE ADR ASKED, ON PURPOSE. Action item 4 said "nothing
      outside orchestration/controlm/ imports Control-M-specific modules" — taken
      literally that fails on its first run, because drydocs/staging.py, drydocs/cli.py and
      the lineage extractors all import the Control-M parser and are RIGHT to: they ingest
      Control-M. The enforceable invariant is the WITHIN-CORE direction — the neutral level
      must never import the vendor — plus a shape guard that fails if drydocs_core/controlm/
      reappears at the top level. That pair is what makes a second vendor possible; the
      literal reading would only have made the suite red.

      Three exception names hit N818. Renamed rather than per-file-ignored: the config
      layer already uses the Error suffix (UnknownSpecError, UnknownDomainError are exact
      analogues), and adding a fourth ignore for brand-new code with zero external call
      sites is the shape of reasoning the B023 commit warned about. The existing N818
      ignores rest on 55 call sites and port-collision cost; neither applies here.

      NOT DONE, and named: ADR action item 5 (folder-vs-module naming — web/ <-> drydocs-web,
      agents/ <-> drydocs-agents, libs/) is a DECISION, not a chore. One of its two options
      rewrites `module:` on live backlog items, which is governance and outside this
      acceptance. Item 6 (the drydocs/loaders/ mirror) is phase 3 and now unblocked.
      Port sequencing (item 7): step 53b added, and it flags that
      scripts/render_enforcement_matrix.py is a 3-way merge company-side — they carry 4
      company-only SURFACES rows, so the launcher-registry consumer change and the new
      orchestration-crosswalks row must be taken INTO their file, not over it.

  - id: S3
    epic: structure-remediation
    title: "Rename the canonical BusinessApplication key seal_id -> app_id across 8 loader sites + the :Port NODE KEY, and write the missing seal-extract column ledger (ADR 0010 F2; gate SIGNED OFF 2026-07-27)"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: fable
    priority: p1
    status: done
    depends_on: [S1]
    inputs: [docs/decisions/0010-internal-source-term-abstraction.md, config/taxonomy/business-application.yaml, drydocs_core/schema/, config/taxonomy-ontology-map.yaml, drydocs_api/query_specs.py, "web/src/data/mappingsDemo.ts"]
    acceptance: >
      GATE SIGNED OFF 2026-07-27 (config/gate-log.md; 22/22 confirmed, 0 edits) — the map
      entry business-application-identity is `confirmed` and this item is now BUILD work.
      REWRITTEN at sign-off: the previous acceptance described the WITHDRAWN v1 shape
      (app_id + id_authority), a property §B0 deleted — building to it would have written
      exactly what the gate removed.
      Phase 1: loaders write app_id ALONGSIDE seal_id from the same row value across all
      FOUR MERGE sites in ONE atomic change (seal_applications.cypher:19,
      manual_seal_attribution.cypher:32, pat_product_mapping.cypher:54,
      software_registry.cypher:52) plus the four key-bearing MATCH sites
      (batch_port_orchestrator:25, manual_seal_attribution:41, seal_attribution:32,
      seal_contacts:27) — never loader-by-loader, because a Neo4j uniqueness constraint
      IGNORES NULLS and a partial cutover SILENTLY DOUBLES the canonical node (§C2).
      A new uniqueness constraint on app_id is added under a NEW name. The :Port NODE KEY
      flips here too (§D1(a)): constraints.cypher:44 + seal_applications.cypher:96,100.
      CRITICAL — `CREATE CONSTRAINT <name> IF NOT EXISTS` matches on the NAME, so
      redefining port_unique under the same name SUCCEEDS AND DOES NOTHING, leaving the
      old (parent_seal_id, kind) definition live: DROP it first or take a new name.
      A graph-tests/ rule asserts app_id = seal_id on every :BusinessApplication (§C3).
      Existing graphs are REBUILT, not migrated (§C4, wipe-and-rebuild doctrine).
      Phase 2 (has the deadline — before more console routes land): drydocs_api and web
      emit app_id ONLY (singular, not a "pair"); web/src/data/mappingsDemo.ts loses
      app_seal_id and its stray match_method 'seal_var' becomes the real 'seal'.
      SCOPED OUT per §B2(ii): ATTRIBUTION_TIERS 'SEAL' and match_method 'seal' STAY —
      they record what Control-M CMDLINEs and docs literally say, and belong to the
      signed seal-attribution-match-policy gate.
      Also in scope: write config/source-mappings/seal-extract.yaml (§B1(c)) with the
      column row `Application ID -> BusinessApplication.app_id` — the source's real field
      name, NOT "SEALID" (§B5). app_urn is NOT written (§B3, deferred with a trigger);
      id_authority is NOT written (§B0, withdrawn). Retiring seal_id is NOT in scope
      (§G3, a separate later gate). Module/file renames are phase 3+ (§G2).
      ADR 0010 must be amended at S1 — its Option C and rules 2/3 assume the withdrawn shape.
      Full suite green; graph-verify coverage unchanged.
    notes: >
      Why this one is urgent despite scoring 7th on the review's formula: effort is
      high today and rises every week the console adds a route that reads seal_id.
      It has already crossed into drydocs_api/query_specs.py and the console demo
      fixture. The formula measures the fix, not the cost of delay.
      The argument is the repo's own rule turned on itself: ADR 0003 rule 1 says
      source-system fields stay verbatim on SOURCE-LABELED nodes, and
      :BusinessApplication is the canonical node, not a source-labeled one — so a
      vendor-named key on it is precisely the case that rule excludes.
      config/taxonomy/business-application.yaml already gets this right at the taxonomy
      layer (concept: BusinessApplication, source_of_record: SEAL, identifier: SEALID);
      the graph and API layers did not inherit it. PAT needs NO equivalent work — it
      never leaked into code (review §4.2, and §6 rule 4 says don't touch it).
      Sequencing note: the still-open seal-app-ref-edge-reshape v2 gate also touches
      :BusinessApplication. RESOLVED at §A4 by CARVE-OUT rather than by co-scheduling:
      everything the identity gate ruled is SEALED except §C1 (the 8-site inventory,
      stale if the reshape re-targets seal_attribution) and §D2 (the attribution_id
      shape, judged against today's grain). Those two — and only those — may be
      re-opened by the reshape. Port sequencing (§G4): this lands BEFORE any other wide
      structural port, because the company side has seal_id in code the producer cannot
      see and every additional structural port compounds the conflict.
    progress: >
      DONE 2026-08-01 (laptop). Phases 1 + 2 + the ledger, in one commit — the acceptance's
      "never loader-by-loader" applies to the whole cutover, not just the MERGEs.

      PHASE 1. The MERGE key is now app_id at all four MERGE sites and all four MATCH sites
      read it; seal_id is written from the SAME row value as a deprecated alias (§G3 owns its
      retirement). Two of the eight line numbers in the acceptance had DRIFTED since the gate:
      pat_product_mapping is :64 not :54, batch_port_orchestrator is :42 not :25. The SET of
      sites was right, which is what mattered — recorded because the next reader will otherwise
      trust the numbers. Constraints: new businessapplication_app_id (NEW name, as ruled);
      businessapplication_seal RETAINED; port_unique DROPped and recreated as port_app_key
      (parent_app_id, kind) — both a drop AND a rename, so an operator reading SHOW CONSTRAINTS
      sees the change rather than having to diff definitions.

      :Port got NO dual-write, unlike :BusinessApplication. Deliberate and asymmetric: seal_id's
      alias exists to protect readers, and the gate MEASURED that parent_seal_id has none
      (zero references in drydocs_api/, graph-tests/, web/src/; cli.py binds ports through
      HAS_PORT). A NODE KEY property also cannot be null. Writing a dead alias no test protects
      would have been cargo-culting the shape.

      THE TRAP IS REAL — PROVEN, NOT QUOTED. Live-verified on a scratch database (created and
      dropped): re-declaring port_app_key with a DIFFERENT key under the SAME name SUCCEEDED
      and left the original definition live. That is the gate's §D1 warning reproduced, so the
      DROP above is load-bearing, not belt-and-braces. Same run proved the whole file applies
      (61 statements, 52 constraints, the three expected present, port_unique gone), the loader
      shape yields app_id == seal_id with a matching parent_app_id, app_id uniqueness raises
      ConstraintError on a duplicate, and all five new graph-test cases return zero rows.

      PHASE 2. query_specs.py (6 specs) and mappings.py emit app_id; web/src follows
      (mappingsDemo.ts, mappingsApi.ts, MappingsRoute.tsx, assetSearch.ts). Column LABELS became
      "Application ID" — the source's own header, which is neutral, so rule 1 and the evidence
      rule agree here. tsc + vite build + oxlint clean. The tray localStorage key went v1 -> v2
      because TrayEntry.seal_id became app_id: a v1 entry read back under v2 would render a
      draft with no target application and submit it that way.

      THE BOUNDARY S3 DREW, and why it is not scope-shaving: §E1 rules "a route, a QuerySpec, a
      column header, a demo fixture" — the WIRE. It does not rule the committed FILE formats, and
      those cannot follow yet: manual_mappings.py parses `target_key=seal_id=<value>` BY NAME and
      REFUSES a row without it, so emitting app_id would stop every committed
      config/manual-loads/*.csv (and the TEMPLATE) from loading; OVERRIDE_HEADER IS the header of
      config/overrides/seal-contact-overrides.csv, which mapping_store.py ingests by name. Both
      are aliased at the SELECT / request-field boundary instead, commented at all four sites,
      and handed to §G3's retirement gate (recorded in ADR 0010 action item 6). The same reasoning
      keeps the pydantic row-model field names (ManualMappingRow.seal_id, SealAttributionRow.seal_id)
      as they are — they name what the source/file column is called.

      SCOPED OUT AND VERIFIED SO: ATTRIBUTION_TIERS 'SEAL' and match_method 'seal' are untouched
      (§B2(ii)). The one fixture change went the OTHER way — mappingsDemo.ts carried
      match_method: 'seal_var', a value the graph never writes, corrected to the real 'seal'.
      That is fixing an invented string, not renaming evidence.

      THE LEDGER (§B1(c)): config/source-mappings/seal-extract.yaml, 87 + 5 column rows
      TRANSCRIBED from models/seal.py and the two loaders' Cypher (generated from the source, not
      hand-typed), census: pending, both objects sweeping the tail. Registered at the dataset's
      locator.mapping and removed from test_source_mapping_drift.py's LEDGER_PENDING (8 -> 7).
      Two honesty notes written INTO the file: (i) only ONE column name is an SME-confirmed source
      header — `Application ID`, the gate's own row; every other name is the loader's field name,
      which is DryDocs' belief about the export, and that gap is exactly what let `SEALID` live
      here for months, so it is labelled rather than smoothed over; (ii) models/seal.py documents
      the row as PSGMGR.DECO_SEAL_APP_INFO while the registry calls the dataset a csv report — a
      replica view and a Reports export are different objects with different headers. Recorded
      UNRESOLVED (it decides what the Phase-2 census profiles), not guessed at.

      Two guards fixed as collateral, both real: test_constraint_count matched the bare string
      "CREATE CONSTRAINT" anywhere in the file, so documenting the DDL trap in a comment inflated
      the count AND false-failed test_constraints_are_idempotent; both now anchor at line start,
      matching the parser the bootstrap presence guard already uses. Inboxed for a wider sweep.

      Suite 1330 passed / 7 skipped. Renders regenerated (the load map caught the ledger count
      going 10 -> 11 on its own). Doc residual inboxed for L19: two governed design docs assert
      things this change made false, and both are rev-bump surfaces, so S3 did not edit them.

  - id: S4
    epic: structure-remediation
    title: "Give the console a durable draft substrate: a draft table in mapping.db as write-ahead buffer, promoted by emitting a YAML/CSV diff (ADR 0009, F3)"
    type: task
    module: drydocs-core
    phase: 12
    agent: main
    model: sonnet
    priority: p1
    status: done   # closed 2026-08-04 same session (laptop). `draft` table in mapping.db (schema v2, bumped so a pre-S4 file reads stale and REBUILDS rather than failing at first write); build() carries draft rows ACROSS a rebuild — the one exception to the destructive rebuild, because any source edit makes the store stale, so discarding pending work would defeat the buffer exactly when it is doing its job. Both write endpoints moved off commit-by-replace: draft_override and draft_app_code_mapping write ROWS and return a receipt; promote_draft emits an ADDITIVE unified diff (committed text carried through verbatim, drafted rows appended) with GET /mappings/drafts and POST /mappings/drafts/{id}/promote wired. WIDER THAN THE ACCEPTANCE'S LETTER, deliberately: the acceptance names the override endpoint, but the K9 app-code endpoint is the same mechanism on a sibling file and both share promote_draft — leaving one on commit-by-replace would have left two write models in one module, which is the confusion ADR 0009 exists to remove. Console updated (its ONLY consumer): mappingsApi.ts loses OverrideArtifact/AppCodeArtifact for DraftReceipt/PromotedDiff, both panes draft-then-promote and download a .patch. Deleting var/mapping.db still discards unpromoted drafts — the deletable-without-data-loss contract holds for everything DERIVED, and that limit is written into the DDL comment and the ADR rather than glossed. Tests +12: rebuild survival, two concurrent sessions not colliding, diff applies via an INDEPENDENT unified-diff applier AND via real `git apply`, promotion is idempotent-closed, pre-S4/foreign DB carry-over tolerated, draft excluded from the deterministic dumps, and an AST guard that drydocs_api calls no filesystem write primitive at all (proven red on a probe). ADR 0009 action item 2 marked done. Suite 1527 green, ruff clean, tsc --noEmit + oxlint clean.
    depends_on: [S1]
    inputs: [drydocs_core/mapping_store.py, scripts/build_mapping_db.py, drydocs_api/mappings.py, docs/decisions/0009-configuration-substrate.md]
    acceptance: >
      mapping.db carries a `draft` table built and rebuilt by scripts/build_mapping_db.py
      with the rest of the derived database (still gitignored, still deletable);
      the override-draft endpoint writes a ROW instead of returning a whole replacement
      file; a promote path emits the YAML/CSV diff for a branch, and a test proves the
      emitted diff applies cleanly to the committed file and round-trips to the same
      parsed content. Git remains the only commit target — a test asserts no endpoint
      writes a tracked file directly. Concurrent drafts from two sessions both survive
      (the property commit-by-replace cannot offer).
    notes: >
      The review found the config/DB architecture already CORRECT for the read path —
      mapping_store.py materializes committed YAML/CSV into var/mapping.db as a derived,
      rebuildable read model with a round-trip parity test. That is a materialized view
      over a git source of truth, which is the right answer. The unsolved half is the
      WRITE path: POST /mappings/overrides/draft today returns the complete updated file,
      which is correct for a small override list and does not survive a second concurrent
      editor. This is the one piece to design BEFORE the console grows write surfaces.
      ADR 0009 §"propose in the DB, land in git".

  - id: S5
    epic: structure-remediation
    title: "Split the two monolith config YAMLs by domain behind a deterministic concatenating loader (F7/F8)"
    type: chore
    module: ontology
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [S1]
    inputs: [config/taxonomy-ontology-map.yaml, drydocs_core/ontology/relationship_vocabulary.yaml, PORT-MANIFEST.yaml]
    acceptance: >
      config/taxonomy-ontology-map.yaml (1,013 lines) and
      drydocs_core/ontology/relationship_vocabulary.yaml (2,111 lines) each become a
      directory of per-domain files (controlm / catalog / seal / registry / docs, or the
      domains that actually exist), read by ONE loader that concatenates in deterministic
      sorted-filename order and fails loudly on a duplicate key. A test proves the
      concatenated result parses equal to the pre-split file's content. Every existing
      consumer and test passes unchanged. PORT-MANIFEST.yaml records the split so the
      company side applies the same shape rather than resolving it as a conflict.
    notes: >
      The problem is granularity, not size or format: one file serving many domains means
      two concurrent sessions collide, and a cross-repo port conflict lands as one big
      hunk instead of one small one. Splitting shrinks the conflict to the domain that
      changed. Module recorded as `ontology` (not `config`) because the registry defines
      that work area as "drydocs_core/schema/, relationship vocabulary, mappings" — which
      names both files — even though one of them lives under config/.

      DONE 2026-08-04 (d84d86b): both files are per-domain fragment
      DIRECTORIES read by drydocs_core/yaml_fragments.py (sorted-filename
      concatenation; loud on duplicate top-level keys and entry ids; single
      files stay first-class). Split-time proof: 83 + 38 entries deep-equal
      to the pre-split parse; only entry ORDER changed (domain-grouped),
      schema_graph.cypher + matrix + load-map regenerated same commit.
      PORT-MANIFEST rows now glob the directories and record the split;
      reconcile-port snapshots the MERGED documents. Suite 1515.
  - id: S6
    epic: structure-remediation
    title: "JSON Schema for each config family, so a malformed entry fails in the editor rather than in pytest (F14)"
    type: task
    module: config
    phase: 6
    agent: pipeline-config
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-07 (desktop, same session as the claim). BUILT: config/schemas/ with six draft-2020-12 schemas — source-registry (v2 two-level shape, urn-refusal expressed as `not: required: [urn]` mirroring from_yaml), precedence, classification, source-mapping (the doc-08 disposition enum projected|filter-only|excluded|deferred; default_disposition is an OBJECT), taxonomy-ontology-map and relationship-vocabulary (both FRAGMENT-AWARE: a fragment is a keyed mapping, a BARE LIST of entries — the 20-50 map files and 41-48 vocab domain files — or the comment-only header, i.e. null). Guard: tests/unit/test_config_schemas.py (16 tests) — every live family file validates; six deliberately-malformed fixtures fail PATH-PRECISE (json_path names the offending key, e.g. $.objects[0].columns[0].disposition); a subprocess test proves validation with ONLY yaml+jsonschema, cwd outside the repo, drydocs_core never in sys.modules; the module polices itself core-free. Split of labor per the notes: schemas own SHAPE, tests keep SEMANTICS — test_classification's publishable-bool loop swapped for a pointer at the schema (the three-level id pin STAYS, it is policy not shape). jsonschema added as a dev-group dependency. Registered as enforcement-matrix surface `config-schemas` (the matrix's own completeness check caught the missing row — the guard working as designed); matrix regenerated. Field realities the schemas encode from the census, not guesses: `updated` arrives as a YAML date object; vocab role/inverse/prov_maps_to are nullable; vocab_id may be a LIST (one map entry activating several registry rows). Suite 1714/3/5 (the 3 = standing Idea-83); ruff clean.
    depends_on: [S1]
    inputs: [config/source-registry.yaml, config/precedence.yaml, config/classification.yaml, config/source-mappings/, config/taxonomy-ontology-map.yaml, drydocs_core/ontology/relationship_vocabulary.yaml]
    acceptance: >
      Each config family has a JSON Schema under config/schemas/; the existing Python
      guards VALIDATE AGAINST IT rather than re-implementing shape checks in assertions;
      a deliberately malformed fixture fails with a path-precise message naming the
      offending key. A test proves the schemas are usable without importing
      drydocs_core, so the console's admin surface and the agent tier can validate
      without a Python dependency on the package.
    notes: >
      Complements rather than replaces the test guards: the tests own semantics
      (classification present, vocab id resolves, precedence order valid), the schema
      owns shape. Easier after S5 splits the monoliths, but not dependent on it.

  - id: S7
    epic: structure-remediation
    title: "Record the folder-name vs module-name rule once in MODULE_MAP.md (F12)"
    type: chore
    module: docs
    phase: 6
    agent: main
    model: haiku
    priority: p2
    status: done   # closed 2026-08-07 (desktop, same session as the claim). MODULE_MAP.md gained '§Naming: folder name vs module name' right under the Invariant: module names name COMPONENTS (guard-enforced boundaries), directories name CODE ROOTS, and they must coincide ONLY for first-party Python packages — test_module_boundary.py classifies by package root, so that is the one condition forcing a rename; non-Python surfaces (web/=drydocs-web, agents/=drydocs-agents) keep ecosystem-conventional names because no guard keys on them and their identity lives in the ledgers. The UI-WIP/-vs-web/ confusion is answered in the section (S9 owns the re-home). Both backlog modules: comments now cite the section instead of re-explaining (the web comment's 'absent from MODULE_MAP.md' claim was ALSO stale — O42 added the Non-Python surfaces table — fixed by the re-point). No directory renamed. Guards green (backlog + module-boundary).
    depends_on: [S1]
    inputs: [MODULE_MAP.md, docs/restructure/backlog.yaml, docs/decisions/0008-orchestration-module-boundary.md]
    acceptance: >
      MODULE_MAP.md states the rule once — why web/ is the module drydocs-web and
      agents/ is drydocs-agents, and under what condition a directory must instead match
      its module name — and the backlog `modules:` comments point at that rule instead of
      re-explaining it. No directory is renamed by this item.
    notes: >
      Cosmetic but pre-UI: a new contributor reading the root listing cannot currently
      tell which of UI-WIP/ and web/ is the real console (S9 fixes the other half of
      that confusion). Cheapest item in the epic; grouped here because ADR 0008 is where
      the naming question was raised.

  - id: S8
    epic: structure-remediation
    title: "Split cli.py into a thin composition root plus per-domain command modules, with non-milestone verb names (F6 + the parked cli.py regroup note)"
    type: chore
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: [drydocs/cli.py, tests/unit/test_module_boundary.py, internal/repo-README.md]
    acceptance: >
      drydocs/cli.py is a thin root registering per-domain Typer sub-apps
      (schema / ingest / verify / variables / docs / plan), each in its own module;
      `drydocs --help` lists the same commands under the same names as before;
      the milestone-named verbs m1-verify / m3-verify gain verify-reference /
      verify-controlm aliases with the old names deprecated but still working;
      `python -c "import drydocs.cli"` clean; cli.py remains the composition root and
      remains exempt under ENTRYPOINT_MODULES (not a new exemption); full suite green.
    notes: >
      MERGE at the 2026-07-25 groom: the review's F6 (cli.py, 1,519 lines, 27 commands)
      and the long-parked IDEAS line "cli.py regroup: split the 937-line flat command
      list into domain subcommand groups — NOT milestone names" are the same work. The
      file has grown 582 lines since that note was written, which is the argument for
      doing it. Deliberately NOT dependent on S1 — no ADR ruling is needed to reorganize
      a CLI. Deprecation aliases were the original note's condition; keep them.

  - id: S9
    epic: structure-remediation
    title: "Repo-root docs hygiene: UI-WIP/ into docs/design/ui-exploration/ and the loose docs/*.md into their subdirectory homes (F10/F13)"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: ["UI-WIP/", "docs/", PORT-MANIFEST.yaml, docs/port-prompt.md, docs/plan/board.html]
    acceptance: >
      UI-WIP/ lives at docs/design/ui-exploration/ and the loose docs/controlm-*.md /
      docs/port-*.md files are grouped into subdirectory homes. All 31 tracked references
      updated — including PORT-MANIFEST.yaml, the two gate-prompt specs, drydocs_api/app.py,
      backlog.yaml (45 hits) and the governed docs/design/* renders, which are REGENERATED
      from their .md sources, never hand-edited. board.html regenerated.
      `git grep -l "UI-WIP"` returns only deliberate historical mentions (the port archive
      and the IDEAS audit trail). Full suite green. Done on a branch with a port-prompt
      step added — never directly on main.
    notes: >
      Scored Effort=1 in the review and CORRECTED to 3-4 the same day when the attempt
      measured the reference sweep: the score had priced the git mv and ignored the 31
      tracked files. Because the sweep crosses governed renders and the cross-repo port
      machinery, this is branch work under docs/port-prompt.md, not a tidy-up commit —
      which is exactly why it is p3 and carries no dependency: it can wait, and it can
      start whenever someone has a clean branch. The root-level scratch-screenshot half
      of F10 was already closed 2026-07-25 (bbf29cf).

  - id: S10
    epic: structure-remediation
    title: "The four :BusinessApplication MERGE loaders refuse to run against a pre-cutover graph, instead of minting the silent twin S3 argued about but never guarded"
    type: bug
    module: drydocs-load
    phase: 9
    agent: main
    model: sonnet
    priority: p1
    status: done          # DONE 2026-08-05 (desktop) — drydocs/loaders/app_identity.py + 4 loaders + 15 tests; the batch_port stale-key nit fixed in passing
    depends_on: []
    inputs: [drydocs/loaders/seal_applications.py, drydocs/loaders/manual_loads.py, drydocs/loaders/catalog.py, drydocs/loaders/software_registry.py, drydocs/loaders/base.py, graph-tests/business-application-identity.yaml, docs/port-prompt.md]
    acceptance: >
      Every loader that MERGEs `:BusinessApplication {app_id}` — seal_applications.v1,
      manual_seal_attribution.v1, pat_product_mapping.v1, software_registry.v1 — refuses
      up front when the target database holds any `:BusinessApplication` with a null
      `app_id`, i.e. a node a pre-S3 loader created. The refusal follows the established
      idiom (`batch_port_orchestrator._assert_endpoint_registries_present`): it runs BEFORE
      `super()._load`, so a refused run writes nothing at all — not even the :JobRun — and
      the message names the remediation (backfill `app_id = seal_id` on the pre-cutover
      nodes, or rebuild, and count first) rather than only the symptom. The count excludes
      `:SchemaMeta` exemplars, which carry the real label with no key and would otherwise
      red every load (the `_count_real_nodes` precedent). Unit tests cover both directions
      per loader against a fake client — refuses on a dirty graph, proceeds on a clean one —
      plus one test asserting the refusal happens before any write. Full suite green.
    notes: >
      WHY THIS IS A BUG AND NOT AN ENHANCEMENT: it fired. The company ran
      `drydocs load seal_applications` against a graph that took the S3 CODE but never the
      S3 re-key and got `Neo.ClientError.Schema.ConstraintValidationFailed` mid-load — after
      partial commits, because batches commit per flush. S3's own §C2 states the mechanism
      exactly ("a uniqueness constraint IGNORES NULLS, so a loader still MERGEing on seal_id
      would silently create a SECOND node rather than fail") and seal_applications.cypher:23
      repeats it in a comment, but nothing enforces it at load time.
      WHAT ALREADY EXISTS AND WHY IT IS NOT ENOUGH: graph-tests/business-application-identity
      TC-01 detects exactly this condition — but it is a POST-HOC verification run, so it
      reports the damage after the loader has done it. A detector that only runs when someone
      remembers to run graph-verify is not the guard the null-tolerance argument implies.
      SCOPED OUT: the four MATCH-only sites (batch_port_orchestrator, seal_attribution,
      seal_contacts, folder_attribution) do not mint twins — a MATCH miss writes nothing.
      They have the opposite failure (silent no-op), which their own registry-presence
      refusals already cover. Also out of scope: retiring the seal_id alias (§G3, a separate
      later gate) and any migration/backfill verb — this item REFUSES, it does not repair.
      Worth fixing in passing if trivial: `batch_port_orchestrator._assert_endpoint_registries_present`
      still counts on the pre-S3 key (`_count_real_nodes("BusinessApplication", "seal_id")`),
      which works only while the alias is dual-written.
      COMPANY-SIDE READ: this is producer payload that lands with the next port; it does not
      substitute for the T23 migration, which is live-state repair on their graph.
      BUILT 2026-08-05 (desktop). `drydocs/loaders/app_identity.py` carries
      `PreCutoverApplicationGuard`, mixed in BEFORE BaseLoader on all four MERGE-site loaders
      so its `_load` reaches the MRO first — the refusal lands ahead of `_preflight_indexes`
      as well as `_open_run`, which a test pins: a graph this broken should not be reported
      as an index problem. 15 tests in `tests/unit/test_app_identity_guard.py`, three of them
      parametrized across all four loaders (refuses dirty / proceeds clean / writes nothing),
      plus a message test (count + backfill + rebuild + the TC-03 pointer) and a COVERAGE test
      that re-derives the MERGE-site list from the .cypher files rather than trusting the
      enumeration — a fifth MERGE site added without the guard reds that test instead of
      passing by omission. Suite 1575 passed / 5 skipped.
      FIXED IN PASSING, as the item's own note anticipated: `batch_port_orchestrator`'s
      endpoint probe counted on the deprecated `seal_id`. It works today only because the
      alias is dual-written; it would have started reporting an empty registry against a good
      graph the day §G3 retires it. Now counts on `app_id`.

  - id: S11
    epic: structure-remediation
    title: "Extract drydocs_plan/, drydocs_docgen/ and drydocs_port/ — the three declared components that never got a package"
    type: chore
    module: drydocs-docgen
    phase: 6
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: [S8]
    inputs:
      - drydocs/plan_board.py
      - drydocs/plan_roadmap.py
      - drydocs/plan_ideas.py
      - drydocs/doc_outline.py
      - drydocs/design_doc.py
      - drydocs/doc_pdf.py
      - drydocs/port_preflight.py
      - tests/unit/test_module_boundary.py
      - MODULE_MAP.md
      - pyproject.toml
      - scripts/
      - PORT-MANIFEST.yaml
      - docs/port-prompt.md
    acceptance: >
      Three first-party packages exist and the seven modules no longer sit at drydocs/ root:
      drydocs_plan/ (plan_board, plan_roadmap), drydocs_docgen/ (doc_outline, design_doc,
      doc_pdf, plan_ideas — plan_ideas stays with docgen, the 2026-08-05 ruling stands) and
      drydocs_port/ (port_preflight). test_module_boundary.py classifies all three BY PACKAGE
      ROOT and the hand-maintained "plan" / "docgen" / "port" module-name tuples are DELETED,
      not re-pointed — that deletion is the acceptance signal that the physical tree now
      carries the classification the test was carrying by hand. pyproject packages updated;
      the five scripts/render_*.py wrappers, scripts/doc_to_pdf.py and the six unit test
      modules import the new paths; `drydocs --help` lists the same commands under the same
      names. Zero behaviour change is PROVEN, not asserted: docs/plan/board.html, ideas.html,
      roadmap.html and docs/design/*.html regenerate byte-identical from the moved code.
      MODULE_MAP.md's "Module (today)" table gains three package rows and drops the per-file
      drydocs/ rows they replace. Full suite green. Lands as ONE frozen unit and is ported as
      ONE unit (GN2's discipline — a wide move landing in pieces leaves the two repos
      disagreeing about import paths for as long as the pieces take).
    notes: >
      Raised 2026-08-18 from a module-ownership read of drydocs/. drydocs-plan, drydocs-docgen
      and drydocs-port are DECLARED in `modules:` and enforced as logical groups by
      test_module_boundary.py, but they are the only components with no physical directory —
      every other one was extracted (drydocs_api, drydocs_lineage, drydocs_deepdoc,
      drydocs_docmeta, drydocs_remediation). G2 extracted core and stopped, so drydocs/ still
      carries five components' worth of loose modules and the boundary test compensates with a
      curated list of module names.
      NOT into drydocs_core, and the reason is what core is FOR: models, adapters, neo4j
      lifecycle, config/precedence/source_registry, ontology, schema DDL, orchestration — the
      DOMAIN core, whose value is that importing it carries no opinion about presentation. An
      HTML renderer and a headless-Chromium shell-out would spend exactly that guarantee.
      Sibling packages follow the precedent every other component already set.
      Cheap by construction: design_doc and doc_pdf are pure stdlib; doc_outline and plan_board
      are yaml plus one drydocs_core import; plan_roadmap and plan_ideas import siblings. No
      graph access, no loader coupling, no component imports. Dependency direction after the
      move is drydocs_plan -> drydocs_docgen -> stdlib/core: one edge, pointing the right way.
      The sweep is ~11 tracked importers, not S9's 31 — but score the sweep before committing
      to an effort number, which is the lesson S9 recorded the hard way.
      depends_on S8 so cli.py is a thin composition root with per-domain sub-apps BEFORE the
      files move; the other order makes S8 split a CLI across three packages instead of one.
      Not a hard blocker on correctness — if S8 stalls, this can be re-scored and run first.
      port_preflight rides along ON PURPOSE: identical position (own group, own declared
      component, stdlib + subprocess + one repo_paths import), and extracting two of the three
      guarantees a repeat visit. Cut it from scope only if the SME rules drydocs-port folds
      into another component.
      S7 is what makes this a rename rather than a preference: module names name COMPONENTS,
      directories name CODE ROOTS, and they must coincide for first-party Python packages
      precisely because test_module_boundary.py classifies by package root.
      REJECTED ALTERNATIVE: one drydocs_docs/ package holding all three groups. Fewer
      top-level entries, but it invents a fourth component name that the ledgers, runbooks,
      guards and backlog `modules:` list do not know, so every one of them would need updating
      for no structural gain. Follow the declared components.

  - id: S12
    epic: structure-remediation
    title: "Environment-drift guard: the suite fails loudly when the interpreter's packages disagree with poetry.lock"
    type: chore
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - tests/conftest.py
      - poetry.lock
      - poetry.toml
      - internal/repo-README.md
      - .github/workflows/ci.yml
      - pyproject.toml
    acceptance: >
      A session-scoped guard in tests/conftest.py reads poetry.lock, and for every locked
      package that is ALSO installed in the running interpreter, asserts the installed version
      equals the locked one. Packages absent from the interpreter are skipped, not failed —
      the lock carries groups and extras a given install legitimately does not select. On
      drift the guard fails the session ONCE, before any test runs, naming sys.prefix, the
      count, and the first few offenders as `name locked X, installed Y`; it must also name
      the usual cause (an inherited VIRTUAL_ENV taking precedence over the in-project venv)
      and the remedy, because the failure the guard replaces was misread as twenty broken
      tests. Guard on DRIFT, never on the venv PATH: a path assertion would fail Docker, tox
      and system-python runs that are correctly provisioned, and would miss a wrong-version
      install inside the right directory.
      Proven both ways, and the fixture for the negative case is already on this machine:
      DryDocs/.venv reports zero drift across 65 installed locked packages and the suite stays
      green, while the shared sandbox venv reports 16 — including click (locked 8.1.8,
      installed 8.3.2) and ruff (locked 0.5.7, installed 0.15.11). A unit test drives the
      comparison function over synthetic lock/installed pairs so the negative case is
      reproducible off this machine. CI green on both matrix versions — CI installs from the
      lock, so it has no drift by construction.
    notes: >
      Raised 2026-08-18, the follow-up the S11 session's venv note (internal/repo-README.md,
      PR #3) deliberately left out. The note tells a reader what to do; nothing MAKES the
      wrong environment announce itself.
      What it costs today: poetry honours an already-activated VIRTUAL_ENV ahead of the
      in-project venv poetry.toml pins, so a shell with another environment active runs the
      suite against that one. Under click >= 8.3 typer 0.12's secondary flags raise TypeError
      and about twenty CLI tests go red — tests that are not broken. That cost a full
      investigation cycle during the S11 work, and it is self-concealing: the suite looks
      broken, CI looks green, and the two disagree for a reason nothing on screen explains.
      The click case is the CHEAP one. The same venv carries ruff 0.15.11 against the exact
      0.5.7 pin that pyproject calls load-bearing (J10 stage 0, authority
      docs/ruff-format-convergence.md) because formatter output varies across versions. A
      `ruff format` run from the wrong environment reformats the tree to a different
      convention and lands a diff CI then blocks — the failure J10 exists to prevent, and the
      one that ran RED for 100+ consecutive runs between 2026-08-05 and 08-12 without anyone
      looking. That is the real argument for p2 rather than the p3 its S8/S9 siblings carry:
      this guard protects a gate that has already failed silently once.
      Placement is tests/conftest.py, not tests/unit/ — integration runs deserve the same
      guard, and the file already owns session-scoped concerns (the shared Neo4j driver
      teardown).
      REJECTED ALTERNATIVE: assert sys.prefix equals the repo .venv. Simpler to write and
      wrong — it fails correctly-provisioned runs that are not in-project, and passes a
      mismatched install that happens to sit in the right folder. Drift is the defect; the
      path is only its most common cause, and belongs in the failure MESSAGE rather than the
      assertion.
      RETRACTED 2026-08-18, and the retraction is EVIDENCE FOR THIS ITEM. These notes
      previously carried a second finding -- "`poetry run pytest` needs PYTHONPATH=. locally
      or tests/unit/conftest.py cannot import drydocs_core" -- filed as a separate defect
      wanting its own backlog item. It is not a defect and there is no second item. Re-run
      cleanly: with VIRTUAL_ENV and PYTHONPATH both unset, `poetry env info --path` resolves
      to DryDocs/.venv and `poetry run pytest tests/unit` is 2176 passed / 5 skipped, with
      `import drydocs_core` succeeding on the bare interpreter -- the package is installed in
      the project venv, so no PYTHONPATH was ever required. The original ModuleNotFoundError
      came from poetry resolving to the INHERITED sandbox venv, which has no DryDocs install:
      the same root cause this item guards, wearing a different mask. Kept rather than
      deleted because it is the sharpest argument the item has -- the wrong environment did
      not merely produce twenty false failures, it manufactured a phantom SECOND defect that
      was written down and nearly scheduled. A guard that names sys.prefix on drift would
      have closed the question in one line instead.

  - id: G28
    epic: component-topology
    title: "Close the multi-database naming drift: code and docs still name drydocs_context / drydocs_all, which provisioning never creates"
    type: bug
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-26 — naming drift closed + guard added; the ddlineage residency clause SPLIT OUT to G30
    depends_on: []
    inputs: [drydocs_core/schema/provisioning/01_databases.cypher, drydocs_deepdoc/__init__.py, drydocs_api/routing.py, drydocs_lineage/writer.py, drydocs_api/query_specs.py, MODULE_MAP.md, docs/decisions/0002-component-database-topology.md]
    acceptance: >
      Every database name in code, tests and docs matches the DEPLOYED topology —
      drydocs / ddlineage / ddcontext / ddall, created by
      provisioning/01_databases.cypher (plus dddocs, planned per ADR 0006). A new test
      asserts no source file names a superseded database and no module-level DATABASE
      constant points at one provisioning does not create, so this cannot drift back.
      NOTE (2026-07-26): the clause about whether drydocs_lineage.writer should target
      ddlineage was SPLIT OUT to G30 — see notes.
    notes: >
      Found 2026-07-25 while writing the executive overview against the live gated
      convention. Measured, not recalled - drydocs_deepdoc.DATABASE = "drydocs_context"
      and tests/unit/test_lineage_deepdoc_scaffold.py PINS that value, so the suite
      PROTECTED a database name provisioning never creates; deepdoc would fail or
      silently create a stray DB on its first live write. NOT a trust-boundary hole:
      drydocs_lineage.writer refuses on an allowlist (database != "drydocs" raises
      TrustBoundaryError), so the stale name in its test fixture was harmless — any
      wrong name triggers the guard.
      DONE 2026-07-26. The "one naming ruling" the original acceptance asked for turned
      out NOT to be needed: the dd* convention was already RULED and recorded — ADR 0006
      §1 states the deployed topology outright ("the plan's working name predates the
      deploy") and the gate-log carries the rename. So this was pure drift cleanup
      against an existing decision, not a decision. ADR 0002 keeps its original names in
      the body (an accepted decision record is not rewritten) with a superseded-names
      banner added at the top pointing at ADR 0006 and the provisioning DDL.
      SCOPE SPLIT, recorded honestly: the acceptance ALSO bundled "does
      drydocs_lineage.writer target ddlineage" — that is a data-RESIDENCY decision, not
      a naming fix, and bundling them was an error in my own grooming. Split to G30
      rather than quietly satisfied or quietly dropped.
      Guard: tests/unit/test_database_names.py (4 tests) — verified load-bearing by
      restoring the old constant and watching two of them fail. Its first draft used a
      generic drydocs_*|dd* regex and flagged the CSS colour #ddd and the column
      drydocs_application_id; the shipped version hunts the three explicitly-named
      superseded databases instead, with an escape hatch only for lines that SAY the
      name is superseded. Also fixed two pre-existing ruff errors in
      drydocs_deepdoc/investigate.py (import sort, en-dash) while in the file.

  - id: G30
    epic: component-topology
    title: "Decide where curated lineage lands: the writer pins drydocs while four query specs read the provisioned-but-never-written ddlineage"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_lineage/writer.py, drydocs_api/query_specs.py, drydocs_core/schema/provisioning/01_databases.cypher, docs/decisions/0002-component-database-topology.md, docs/decisions/0002-c-depgraph-lineage-rehome.md]
    acceptance: >
      One ruling recorded in writing, then made true in code: either (a)
      drydocs_lineage.writer targets ddlineage — which means its TrustBoundaryError
      allowlist, DATABASE constant and the 0002-C §5 ground-truth-only tests all move
      with it — or (b) the four query_specs rows that declare database="ddlineage" are
      repointed at drydocs, and ddlineage is documented as provisioned-for-later rather
      than live. Whichever wins, a test asserts the writer's target and the specs' target
      AGREE, so a spec can never again read a database nothing writes.
    notes: >
      Split out of G28 at its close (2026-07-26) — G28 bundled this with the naming
      cleanup and they are different kinds of decision: G28 was mechanical drift against
      an already-ruled convention, this one changes WHERE curated lineage data lives.
      Measured: drydocs_lineage/writer.py line 70 pins DATABASE = "drydocs" and refuses
      any other database (TrustBoundaryError, an allowlist — so this is a correctness
      and emptiness problem, not a safety one); drydocs_api/query_specs.py declares
      database="ddlineage" on four specs (lines 417, 540, 565, 589). Those four specs
      therefore query a database that is provisioned by 01_databases.cypher and written
      by nothing. Not urgent — the lineage writer is gate-bound anyway (its vocabulary
      is status: planned, so write_curated refuses a live load until the HITL gate flips
      it), so nothing is silently producing wrong answers today; it would bite on the
      first live curated write. Ties to the lineage live-load gate parked in IDEAS.
      DONE 2026-07-26 — RULED (b): the four specs repoint to `drydocs`; ddlineage stays
      provisioned + composite-aliased but documented as provisioned-for-later. The ruling
      is written up in ADR 0002 as a "Residency clarification" — NOT an amendment, because
      D1/D2 already say drydocs-lineage writes ground truth to `drydocs` and were never
      amended when G1 provisioning added a fourth database two days later. Each half of
      the codebase had followed a different source and both cited one, which is why
      neither looked wrong alone. Three grounds: (1) the stated reason for the DB boundary
      is the TRUST axis, and curated lineage is VERBATIM/GROUNDED — the same tier as the
      main load, so splitting it serves nothing; (2) 0002-C §5 already carries a ticked,
      tested "writes ground truth only" gate; (3) decisive — write_curated MATCHes
      :ControlMJob nodes the M3 load owns in `drydocs` and deliberately never MERGEs them
      (a lineage-created stub would break m3-verify's "every job has a folder"). A
      transaction cannot span databases, so from ddlineage those MATCHes would match
      nothing and every job-endpoint edge would vanish SILENTLY — an unmatched MATCH
      yields zero rows, not an error. Option (a) is therefore a design (a :ControlMJob
      proxy spine in ddlineage), not a constant flip. Deferring is also the cheap
      direction: ddlineage is empty, so choosing it later costs a design, not a migration.
      Named trigger to revisit, as an ADR amendment through the gate: the docmeta gate
      (ADR 0006 §b) adopted `dddocs` on the same component-per-database pattern and
      re-targets the bmc-docs corpus out of `drydocs` — if that proves out and lineage
      grows its own proxy spine, reopen. Guard: two tests in test_database_names.py —
      no query spec may read a database nothing writes (SPEC_DATABASES also dropped
      ddlineage, so pointing a spec there is now a deliberate edit), and the writer's
      target must equal the four specs' target. Verified load-bearing: restoring one spec
      to ddlineage fails 3 tests. Also corrected the user-visible UI copy that named
      ddlineage (module registry `backsOnto`, the Lineage SYNTHESIZED banner, route
      comments) — the honest reason those frames are empty is the vocabulary gate, not
      the database. Suite 928 -> 930.

  - id: G29
    epic: component-topology
    title: "Consolidate the supplement verbs into one data-driven apply-supplements, with the legacy verbs kept as delegating aliases"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/cli.py, drydocs_core/run_log.py, drydocs_core/schema/, .claude/skills/run-drydocs/SKILL.md, internal/repo-README.md]
    acceptance: >
      One `apply-supplements` verb applies a data-driven ordered list (base → seal →
      catalog → registry, with SOSA staying opt-in behind a flag); every legacy verb
      still works as a delegating alias; the run asserts the post-apply OntologyTerm
      count and fails if it does not move as expected; the run writes a
      load.supplement.<stamp>.log envelope through drydocs_core/run_log.py — today NO
      supplement or bootstrap verb writes a log file at all (execute_file is
      debug-and-console only); the run-drydocs skill and internal/repo-README.md command
      chains are updated; the e2e test switches to the new verb.
    notes: >
      Shape A, designed and user-reviewed 2026-07-24 with the riders agreed in that
      review (count assertion, run-log envelope, skill + README chain update, e2e
      switch); groundwork already landed the same day (9f5ebe1 stale refs + supplement
      header charter + the missing org#member anchor; 76be07c catalog docstring).
      Shape C — the registration-vs-instance-seed re-slice — is deliberately NOT here:
      it is gate-worthy, not a refactor, and stays in IDEAS.
      Rider: this rewrites the verb set the startup-refresh runbook's Appendix B lists,
      which is where the unlocated "apply-catalog says apply ontology" typo report most
      likely points — fold that check into the runbook update rather than hot-editing a
      doc that is mid-SME-review.
      DONE 2026-07-26. The registry is drydocs_core/schema/supplements.py (core, not CLI:
      the order is a schema fact, the verb is a caller); `apply-supplements` walks it with
      --only NAME (repeatable, registry order wins over flag order) and --with-sosa; all
      five legacy verbs are generated as delegating aliases from the same registry, so
      they inherit the verification and the log rather than duplicating four lines each.
      The acceptance said "asserts the post-apply OntologyTerm count and fails if it does
      not move as expected" — implemented as PRESENCE, not delta, and that is the honest
      reading: supplements are idempotent, so a re-run legitimately moves the total by
      zero and a delta assertion would fail every second run. Instead declared_terms()
      parses the :OntologyTerm IRIs each .cypher MERGEs and the run asserts every one is
      present afterwards; the total is still reported before/after per file. The parser
      reads code only — drydocs_core.cypher_split gained strip_comments() so a
      commented-out MERGE (how a term is retired) is not turned into a term the graph is
      then required to hold forever.
      Found while doing it: the run-drydocs skill listed the chain in the WRONG ORDER
      (catalog before seal, which breaks catalog's reuse of seal's :Attribution class +
      #hasAgent term) — the order living in prose in four places is exactly why it is now
      data. New tests: tests/unit/test_supplements.py (19) + 4 strip_comments cases in
      test_cypher_split.py; suite 905 -> 928. Verified load-bearing: neutering the
      presence check fails test_a_supplement_that_seeds_nothing_fails_the_command.
      Runbook NOT touched — it is mid-SME-review (L5/L6 anchors); the three things it now
      needs are parked in IDEAS for the post-review revision.

  # ── appended at the 2026-07-26 groom run (docs-residency design session) ───
  # The topology half of that session. G32 is the ruling; G31 is the prerequisite
  # every corpus move depends on; G33 is independent and startable now.

  - id: G31
    epic: component-topology
    title: "Extend the proxy-node spine beyond DataAsset + ControlMJob before any corpus moves database"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p1
    status: done   # 2026-08-18 (laptop): RE-SCOPED AND DELIVERED — the acceptance's cross-database premise died at the G32 fold; the surviving substance (the D1 business-key spine) shipped in full. Four of the six named keys ALREADY existed (the 07-26 measurement went stale between then and now); dataasset_id moved HOME to constraints.cypher (52->53); 02_proxy_constraints.cypher tombstoned (its charter, not its keys, retired); provision.ps1's ddcontext/ddall invocations + the federated smoke removed (G102 had missed the provisioner); the acceptance's guard reshaped for one database as test_business_key_spine.py — every loader MATCH-only join target must carry a constrained key, first run classified ONE exemption (patch_window's node_id FILTER) and confirmed all six spine keys home
    depends_on: [G32]
    inputs: [drydocs_core/schema/provisioning/02_proxy_constraints.cypher, drydocs_core/schema/provisioning/README.md, drydocs_core/schema/constraints.cypher, docs/decisions/0002-component-database-topology.md, tests/unit/test_database_names.py]
    acceptance: >
      02_proxy_constraints.cypher declares a constrained business key for every class
      that must be joinable ACROSS the database boundaries G32 rules — at minimum
      SoftwareProduct.product_id, Vendor.vendor_id, Document.doc_id and
      ControlMFolder.folder_id alongside the existing DataAsset.assetId and
      ControlMJob(folder_id, job_id) — its header names every database it must be run
      against (it currently names only drydocs and ddcontext, omitting ddlineage), and
      provision.ps1 runs it against each. A test asserts the proxy set covers every
      class that appears on BOTH sides of a cross-database relationship in the
      shipped loaders, so adding such a relationship without a proxy key fails.
    notes: >
      MEASURED 2026-07-26: 02_proxy_constraints.cypher — the file whose entire job is
      making cross-database joins possible — declares exactly TWO keys
      (DataAsset.assetId, ControlMJob NODE KEY) and its header names only drydocs and
      ddcontext. No SoftwareProduct, no Vendor, no Document, no ControlMFolder,
      no Employee. ADR 0006's existing plan to re-target the bmc-docs corpus to dddocs
      already depends on a SoftwareProduct proxy that nobody wrote down; see Q8 for
      what that omission does at runtime (silently drops every DESCRIBES edge).
      BUILD ORDER IS THE POINT: spine first, corpora second. The reverse order is how
      you get a green load that wrote nothing — the pattern this session hit three
      times. Q9/Q10/Q11 all depend on this item for exactly that reason.

  - id: G32
    epic: component-topology
    title: "Rule the document/content database topology and restate the ddcontext charter: reference vs unstructured content vs derived findings"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: fable
    priority: p1
    status: done   # 2026-08-18 (laptop): gate document-content-topology SIGNED 32/32 — the count FOLDS TO ONE (retrieval grounds + the §F origin precedent); B-DURABLE forced; deepdoc chartered a graph-seeded retriever; Q9 origin ruled GENERAL; ADRs 0002/0006/0011 amended; apply staged as G102 (guards first) + Q20
    depends_on: []
    inputs: [docs/decisions/0002-component-database-topology.md, docs/decisions/0006-docmeta-component-and-doc-graph.md, config/doc-source-registry.yaml, config/gate-prompts/, drydocs_api/exports.py, drydocs_core/schema/provisioning/01_databases.cypher]
    acceptance: >
      One ruling through the HITL gate, recorded as an amendment to ADR 0002 and
      ADR 0006 (amendment notes, not body rewrites — the ADR 0002 banner precedent),
      fixing: (a) how many document/content databases exist and what each holds;
      (b) the ddcontext charter, which today says "isolated uncertain / SYNTHESIZED"
      and cannot honestly describe a VERBATIM-captured email — if unconfirmed content
      lands there the charter restates to UNCONFIRMED (authority not established,
      whether captured verbatim or inferred) and drydocs_api/exports.py's
      WATERMARK_VALUE follows, since it stamps "SYNTHESIZED — unverified" keyed on
      database name; (c) the target_db column of every doc-source-registry entry;
      (d) an explicit, recorded acceptance of what a boundary COSTS — see notes;
      (e) MERGED 2026-07-27: the drydocs-deepdoc CHARTER, which has drifted
      between ADR 0002 (reactive command-line dependency analysis — what the
      scaffold implements) and ADR 0006 §1 (a consumer of docmeta's corpus,
      citing Document/Chunk nodes) — rule whether deepdoc is a parser-driven
      investigator, a corpus-driven retriever, or both, against the user's
      stated intent: profile a Confluence space, then search it SEEDED from
      what the grounded graph already knows, creating no relationship unless
      the subject is already in the DryDocs graph (the ADR 0002 D1 proxy-node
      pattern, so the mechanism exists).
      01_databases.cypher and provisioning/README.md then match the ruling, and
      tests/unit/test_database_names.py's read-vs-write guard covers the new set.
    notes: >
      Design session 2026-07-26 (after G30). SME direction: THREE databases —
      structured / unstructured-content / derived — for separation of loads and
      wipe blast-radius during testing, both of which hold independently of
      retention. Retention turned out NOT to be an argument either way (see Q10).
      THE COST, to be accepted explicitly rather than discovered later. `ddall`
      federates reads but a relationship can NEVER span databases — a composite
      query is a join on returned values, not an edge walk (see the shape in
      provisioning/smoke_drydocs_all.cypher: chained `CALL { USE ddall.x }` blocks).
      So known-shape questions still work, restated as explicit joins, and every
      QuerySpec is fine. What is lost across the boundary: shortestPath and
      variable-length paths, all GDS (centrality, community, similarity), and a
      single vector index over reference AND content chunks — which becomes two
      index queries plus a client-side rerank, notable because the Essential
      GraphRAG corpus was ingested to do exactly that kind of retrieval. Also
      harder: text2cypher (Epic R / ADR 0007) must generate correct USE clauses and
      business-key joins rather than a plain MATCH. If those matter, the mitigation
      is a single-database analytical projection, and that should be decided here
      rather than improvised later.
      Blocks G31 (the spine cannot be sized until the database set is known) and
      through it Q9/Q10/Q11.
      Clause (e) merged at the 2026-07-27 groom from the 2026-07-26 deepdoc-scope
      inbox entry (user question; NOT covered by the database ruling alone).
      Rider: the graph-seeded-retrieval shape is the SECOND application of the
      same idea (first: the 2026-07-23 HR-hierarchy graph-seeded resolution) —
      name it as a reusable pattern in the ruling instead of re-deriving it.
      Until (e) is ruled, do not build the deepdoc scaffold against either
      stale charter.
      CLOSE CHECKLIST — WORK THESE BEFORE FLIPPING THIS ITEM done. Added
      2026-08-07 (laptop) on the user's ruling, because each one is a real
      consequence of this ruling that has NO item of its own and would otherwise
      be found only by re-reading prose.
      (1) Q16 CLAUSE (b) HAS NO SUCCESSOR ITEM. Q16 closed `done` on 2026-08-07
      as an explicit PARTIAL: its clause (a) shipped (drydocs docs-coverage), but
      clause (b) — the product->documentation pointer reaching the GRAPH so an
      agent can traverse from a product to the docs describing it — did NOT, and
      is blocked behind Q14 (the term) which is blocked behind THIS item. Its
      only forward trace today is prose in Q16's close note pointing at Idea-88.
      So: when this signs, either mint the successor item or record in the
      sign-off that (b) is declined — do not leave it traceable only by prose.
      The ruling decides which: if doc corpora land in the registry's database,
      (b) becomes buildable and DESCRIBES may need nothing new (Q16's own
      "cheapest possible answer, check FIRST"); if the boundary hardens, (b) is
      re-scoped as a business-key join and its acceptance changes shape.
      (2) THE `not queried` LABELS GO STALE THE DAY THIS SIGNS. Two surfaces
      render "dddocs is not provisioned (G32)" from the declaration —
      web/src/software/softwareModel.ts (inGraphLabel) and the docs-coverage
      detail lines. tests/unit/test_load_map_json.py::
      test_the_not_queryable_target_db_set_is_pinned pins that set to exactly
      {dddocs} and WILL FAIL when this ruling lands, which is deliberate: it is
      the alarm that forces both surfaces to be revisited rather than quietly
      lying. Treat that failure as a task, not a break.
      (3) THE ONE WORKING TRAVERSAL DIES OR SURVIVES BY THIS RULING. The 27 live
      (:Document)-[:DESCRIBES]->(:SoftwareProduct) edges exist ONLY because
      bmc-docs is loaded in `drydocs` while declaring target_db: dddocs — the
      residency accident this item exists to end. vendor_docs.cypher already
      refuses to write that edge for exactly this reason. Say explicitly in the
      ruling what happens to those 27 edges at the move; docs-coverage reports
      the state as `traversable-until-move` precisely so it is not mistaken for
      a durable capability.

  - id: G33
    epic: self-documentation
    title: "Load the DryDocs code-dependency snapshot into the graph under a Project root, using the seeded-but-unused software ontology"
    type: requirement
    module: drydocs-load
    phase: 16
    agent: main
    model: fable
    priority: p2
    status: done
    closed: 2026-07-27
    close_note: >
      BUILT the same day the gate signed. `drydocs load-code-snapshot`
      (code_snapshot.v1: adapter + cypher + CodeModuleRow + 2 constraints +
      the ontology_supplement U-section + source-registry/audit-fields/doc-08
      ledger entries; u1_* terms flipped active). Loaded LIVE into `drydocs`
      (desktop, neo4jtest — retro-tagged 2026-08-04 at J18: this untagged
      claim is the one a laptop-side audit nearly filed as a defect, reading
      the laptop's graph) and verified: 1 :Project (git provenance on the node), 183 :CodeModule
      across the six scan-root property values, 183 HAS_MODULE, 98 IMPORTS,
      183 IS_ENCODED_IN -> the seeded SwoClass Python (first SWO use);
      idempotent re-run byte-identical counts; m1-verify carries the §F1
      co-labeling graph-test (0). TWO live catches beyond the build notes:
      (a) the machine's .env targeted the EE home db `neo4j` — realigned to
      `drydocs` per dev-environment.yaml and today's stray writes cleaned from
      the home db (pre-existing strays there left for the user); (b) the
      date-only-name stale-pick — selection now parses the filename timestamp
      (see the acceptance build note). 21 unit tests; full suite green.
    depends_on: []
    inputs: [knowledge/depgraph-snapshots/, knowledge/depgraph-snapshots/snapshot.ps1, drydocs_core/schema/ontology.cypher, drydocs_core/ontology/relationship_vocabulary.yaml, docs/RELATIONSHIP_GUIDE.md]
    acceptance: >
      A `drydocs load-code-snapshot` verb reads the newest
      knowledge/depgraph-snapshots/*.json (schema depgraph-machine-first/v1) and
      MERGEs it idempotently into `drydocs`, so it survives the wipe-and-rebuild
      cycle like every other loader and is re-runnable from committed files with
      zero data loss (the ADR 0002 D3 stateless test). Everything hangs off an
      explicit (:Project {project_id:'drydocs'}) root so "is this our own code?" is
      one hop, and modules carry a label that CANNOT collide with the software
      registry's :SoftwareProduct nodes. The new node classes and relationships
      plus their binding to the SWO terms are registered `status: planned` and go
      through the HITL gate; no loader writes them until it confirms. snapshot.ps1
      stays DECOUPLED — the session ritual must keep working with no database running.
      GATE SIGNED OFF 2026-07-27 (config/gate-prompts/self-documentation-code-graph.yaml,
      36/36, 0 edits) — THIS ITEM IS NOW CLEARED TO BUILD and the acceptance is
      tightened to what was ruled. FIVE registrations already exist at
      `status: planned`, not the four this text originally named: node classes
      :Project and :CodeModule, edges HAS_MODULE, IMPORTS and IS_ENCODED_IN (the
      third added by the §E1(b) SWO ruling). Build to these rulings:
      (1) ONE :Project root; the snapshot's SIX scan roots are a `project` PROPERTY
      on :CodeModule, never sibling nodes (§B1a). (2) :CodeModule is keyed on
      `file_id`, the repo-relative path — the only non-colliding key (§C1a/§C4).
      (3) `circular` loads as a property (§C3). (4) edges carry the M2
      pull-provenance fields first_seen_at / last_seen_at / last_run_id like every
      other loader (§D3). (5) IS_ENCODED_IN targets the already-seeded SwoClass
      Python, value derived from node.extension (§E1b). (6) the :CodeModule /
      :SoftwareProduct exclusion is a GRAPH-TEST, not a constraint — Neo4j cannot
      declare label mutual-exclusion (§F1). (7) the loader DISCRIMINATES ON
      meta.tree and REFUSES tree-mode input (§G1a) — BUILD NOTE 2026-07-27,
      post-sign-off review finding: the tree-mode files carry NO `meta` key AT
      ALL (verified: top-level keys are schema/projects/nodes/edges/stats/rels),
      so there is no `tree: true` to refuse on — and a naive `*.json` name-sort
      picks tree-this-version.json as "newest" ('t' > 'd'). A literal negative
      check (`meta.get('tree')` truthy → refuse) would therefore ACCEPT the tree
      files. Implement as a POSITIVE assertion: select `drydocs-*.json` only,
      require `meta` PRESENT and `meta.tree == false`, refuse loudly on anything
      else. The §G1(a) ruling stands; this fixes only its literal reading.
      SECOND selection subtlety, found LIVE at the first build run: a date-only
      name (drydocs-20260727.json, a one-off) sorts ordinally AFTER every timed
      name of the same day ('.' > '-'), so raw string sort picked the stale
      12:42 capture over the 17:33 one. "Newest by filename timestamp" (§H2)
      must PARSE the drydocs-YYYYMMDD[-HHMM] timestamp, date-only keyed as 0000.
      (8) `abs_path` is DROPPED;
      load rel_path / file_id only (§H4). (9) a refusal must be LOUD — see the
      "succeeds loudly, does nothing" family. (10) register the source in
      config/source-registry.yaml with classification Internal-Public (§H6).
    notes: >
      User source 2026-07-26: load the DryDocs project code snapshot under the
      software ontology, "which may prevent missing code fixes", plus "a way to
      distinguish it's for drydocs project and not the products metadata".
      Cheap because both halves already exist and neither is used: ontology.cypher
      seeds 7 SwoClass + 6 SwoProperty terms (Python, Shell, SQL, "is encoded in",
      "implements", "has specified data input/output", "uses platform") with ZERO
      consumers anywhere in the tree, and snapshot.ps1 already writes a code
      dependency graph every session that never enters Neo4j — it is JSON plus a
      viewer.html.
      *** THREE PREMISES IN THIS NOTE WERE WRONG AND ARE CORRECTED HERE (gate
      self-documentation-code-graph, 2026-07-27). All three came from describing
      the artifact without opening it, and finding them was the gate's main value:
      (i) it said the depgraph schema "already carries a `projects` key (=1), so
      the Project root matches the producer's own shape rather than inventing one".
      IT CARRIES SIX — drydocs, drydocs_core, drydocs_deepdoc, drydocs_lineage,
      drydocs_remediation, tests — and 142 of the 183 nodes declare a project that
      is NOT 'drydocs'. The single root was a modelling CHOICE presented as a
      read-off; §B1 made it a choice and ruled (a).
      (ii) it said "664 nodes / 86 edges at the latest". That figure belongs to
      tree-this-version.json, a one-off TREE-mode capture (540 files + 124 dirs,
      projects: ['drydocs1'], a CONTAINS relationship). The rolling dependency
      snapshots are 183 files / 98 edges — the real load is about a QUARTER the
      size this note implied.
      (iii) both shapes declare the SAME schema string depgraph-machine-first/v1,
      so a loader keying on it alone mis-reads one; §G1 ruled discrimination on
      meta.tree. ***
      TARGET IS `drydocs`, NOT dddocs, and the stated purpose is what decides it:
      "prevent missing code fixes" means traversing from a DryDocs module to the
      loader to the Control-M job it loads. That is a path query, so they must
      share a database — and code is not reference material ABOUT a system, it IS
      one, deterministically derived from source we control with no authority
      ambiguity. Same character as folders and jobs. The Project root is what keeps
      it from polluting product metadata.
      RE-PHASED 2026-07-26, same day, on the user's call. Groomed into phase 6 /
      component-topology with the marginal fit flagged rather than hidden; the user
      ratified it as a NEW CAPABILITY — "similar to a major version change of the
      snapshot ritual" — so it moved to the new phase 16 (self-documentation) and
      the new epic of the same name. The framing that earned the phase: the
      depgraph ritual's output stops being a JSON file a human reads and becomes a
      queryable :Project subgraph, which is a different KIND of thing, not a bigger
      version of the same thing.
      ID KEPT AS G33 DELIBERATELY. It no longer matches its epic letter, which is
      the lesser of two evils: ids are stable references (groom rule, git-readme
      §backlog id allocation) and G33 is already named in commit a37043a, which
      cannot be amended. Epic U is therefore the first epic whose opening item
      carries another letter's id — historical, not a pattern to copy. New items in
      this epic take U1, U2, … (T is NOT free: it is the port-turn series, e.g.
      docs/port-T12-*.md).
      Load only the LATEST snapshot as current state; the ~70 dated files (not ~20)
      are a time series available later if code evolution is ever wanted — §A5
      scoped that out.
      "Do not build it" is DISCHARGED as of 2026-07-27: it meant "not before the
      gate", and the gate is signed. Build to the ten rulings in `acceptance`.
      WHAT THIS ITEM STILL DOES NOT DELIVER, stated so it is not judged against a
      promise it never made (§H5): the motivating query — traverse from a DryDocs
      module to the loader to the Control-M job it loads — needs an edge NOTHING in
      a dependency snapshot knows, because that mapping lives in the loaders' Cypher,
      not in their imports. This item lands the code subgraph and the shared database
      that make the join possible; the join itself is a separate Epic U item.

  # ── Epic U — Self-documentation (phase 16; opened by G33, which keeps its G id) ──
  # U1-U3 = the three phases of docs/reviews/code-graph-review-plan.md (groomed
  # 2026-07-27 from the IDEAS hook, same session as the G33 build). U4/U5 = the
  # plan's two OPTIONAL skill-edit follow-ups, gated on their phase proving the
  # queries earn their keep. All read-only analysis — findings route through
  # IDEAS, never direct backlog/graph edits; none is HITL.
  - id: U1
    epic: self-documentation
    title: "Run the python-architect structural review over the live code graph (code-graph review plan, Phase 1)"
    type: task
    module: docs
    phase: 16
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/reviews/code-graph-review-plan.md, knowledge/depgraph-snapshots/]
    acceptance: >
      docs/reviews/persona-python-architect-2026-07.md exists and answers all six
      seed units (A1 layering, A2 circular cross-check, A3 fan-in ranking, A4 the
      24 orphan candidates each dispositioned entry-point/dead/blind-spot by a
      human read, A5 test-coverage shape, A6 cross-root coupling vs MODULE_MAP),
      recording the plan's probed baselines next to current values so drift is
      visible. Every finding carries the query that found it AND the confirming
      file read; findings are ranked by the tech-debt skill's
      (Impact+Risk)x(6-Effort) score; each actionable one lands as a tagged
      IDEAS inbox line. Zero backlog.yaml or graph edits.
    notes: >
      Fresh-graph precondition: snapshot.ps1 -> drydocs load-code-snapshot
      (idempotent). Model opus, not sonnet: A4/A6 dispositions are judgment
      reads, not checklist work; not fable — no schema/boundary decision rides
      on it. IMPORTS caveat (gate D2) applies to every conclusion: never read
      an edge as "breaks if removed".
      CLOSED 2026-07-28 (run 1, all six units): headline = the snapshot
      records ZERO cross-root IMPORTS edges and no function-level imports,
      and drydocs_api is not a scan root — A1/A5/A6 vacuous, A4's 24
      orphans = 24 false positives (each dispositioned with grep evidence);
      A2 healthy (scanner + live graph agree, 0 cycles); A3 base.py 18->19,
      lineage/model.py 9 = new second hotspot. Findings ranked; scanner-fix
      IDEAS line filed — U4 should wait on that fix or the query pack
      enshrines vacuous queries. Zero graph/backlog edits from the review.
  - id: U2
    epic: self-documentation
    title: "Project-manager audit: does backlog.yaml tell the truth about done and todo, checked against the code graph (Phase 2)"
    type: task
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/reviews/code-graph-review-plan.md, docs/restructure/backlog.yaml]
    acceptance: >
      docs/reviews/persona-project-manager-2026-07.md contains the drift table
      (item -> claim -> graph verdict) covering all four units: (1) every item
      closed in the last ~30 days whose close_note names files verified against
      :CodeModule file_ids; (2) the modules: registry censused against the six
      project values + top-level file_id prefixes both directions; (3) every
      next_ready item's inputs: paths confirmed to exist; (4) the per-epic code
      footprint view. Every mismatch has a tagged IDEAS line; ZERO direct
      backlog.yaml edits (the groom owns those).
    notes: >
      Sonnet: mechanical cross-checks with a written pass/fail acceptance.
      D7 makes deletions visible (removed_from_source_at) — use it in unit 2.
      CLOSED 2026-07-28 (run 1): the backlog is telling the truth — 80
      recent done items / 91 path claims, ZERO false done-claims (8
      mechanical flags all dispositioned: matcher artifacts, recorded
      relocations, or files whose ABSENCE was the acceptance); 30/30
      next_ready inputs alive; D7 removed count 0. Census gap: drydocs_api
      is a real package with no scan root — folded into the U1 scanner
      IDEAS line (one fix, three symptoms). No epic footprint contradicts
      its charter.
  - id: U3
    epic: self-documentation
    title: "Technical-writer doc-status board: join the docs subgraph to the code graph (Phase 3)"
    type: task
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/reviews/code-graph-review-plan.md, docs/design/]
    acceptance: >
      Precondition executed and shown: drydocs load-doc-traceability against
      `drydocs` (the DesignDoc/Requirement/Component subgraph was EMPTY at plan
      time). docs/reviews/persona-tech-writer-2026-07.md contains the doc-status
      board (doc -> last commit -> cited components alive? -> fresh/stale/
      orphaned) for every docs/design/*.md, the Component.ref <-> file_id join
      results both directions (stale citations + heavily-imported modules no doc
      cites), the staleness ranking vs Project.git_commit, and the sdlc-*.md
      SDEP-section regeneration check. Mismatches -> tagged IDEAS lines.
    notes: >
      Independent of U1/U2 — runnable in parallel. The code<->Control-M join
      (gate H5) does NOT exist; the plan forbids faking it with string matching.
      CLOSED 2026-07-28 (run 1): precondition run shown — sections 148 +
      matrix 51 loaded; the feedback pass REFUSED via the L17 :Employee
      guard (first live firing — recorded as evidence, the honest producer
      state). Board: 5 docs cite pre-squash commits that dangle off main,
      startup-refresh edited same day still cites 07-20, mapping-demo has
      no cite; ZERO dead component citations both directions; 8/56
      Component refs sheared by the comma-split convention; base.py +
      lineage/model.py cited by no component; sdlc-*.md par-DEP stale in 3
      rows. Four IDEAS lines filed.
  - id: U4
    epic: self-documentation
    title: "OPTIONAL: add a 'DryDocs graph evidence' section to the tech-debt skill (query pack + offline fallback)"
    type: chore
    module: docs
    phase: 16
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: [U1, U6]
    inputs: [.claude/skills/tech-debt/SKILL.md, docs/reviews/code-graph-review-plan.md]
    acceptance: >
      .claude/skills/tech-debt/SKILL.md gains a DryDocs-specific graph-evidence
      section carrying the Phase-1 query pack (A1-A6) with one-line "what debt
      category this measures" notes, AND an explicit no-database fallback
      (snapshot JSON + viewer.html) so the skill never hard-depends on Neo4j —
      the same decoupling rule as the session ritual (gate H3). Skill loads and
      reads coherently offline.
    notes: >
      Gated on U1 proving the queries earn their keep (the plan's own condition).
      Haiku: transcription of already-written queries + a caveat, no judgment.
      RE-SEQUENCED 2026-07-28 groom: also depends on U6 — U1's verdict was
      that A1/A5/A6 are vacuous until the scanner fix lands, so transcribing
      the query pack now would enshrine vacuous queries.
      CLOSED 2026-07-28 (same day as U6): section added with the A1-A6 pack
      NOT transcribed verbatim — hardened with the O33 NOT :SchemaMeta
      guards, the D2 IMPORTS caveat, the scripts/-unscanned caveat, and
      post-U6 baselines (205/370). All 7 probes (A2 = two) executed live
      against `drydocs`: A1=0, cycles=0, A4 dead-code candidates=0, A5=26
      no-direct-test-importer modules (real future audit signal), A6=12
      cross-root pairs. Offline fallback documented (snapshot JSON fields +
      viewer.html; exemplars absent from JSON so no guard needed there).
  - id: U5
    epic: self-documentation
    title: "OPTIONAL: add an optional graph cross-check subsection to the groom-backlog skill"
    type: chore
    module: docs
    phase: 16
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: [U2]
    inputs: [.claude/skills/groom-backlog/SKILL.md, docs/reviews/code-graph-review-plan.md]
    acceptance: >
      The groom-backlog skill gains a short "graph cross-check (optional, needs
      Neo4j)" subsection in its mechanics: (a) sanity-check an item's module:
      against the graph census, (b) verify close_note file claims before
      flipping done — both marked EXPLICITLY optional so a groom with no
      database running is never blocked. The skill's validator/test flow is
      unchanged.
    notes: >
      Gated on U2 (same earn-their-keep condition). The groom itself stays
      offline-first — grooming is yaml transcription; the graph only validates.
      CLOSED 2026-07-28 (executed inside the epic-close-out groom run — the
      last open Epic U item, Epic U now 6/6): SKILL.md gains the "Graph
      cross-check (optional, needs Neo4j)" section with exactly the two checks
      (module-census sanity, close-note file-claim spot-check), both explicitly
      optional/skip-silently, offline flow and validator untouched.
  - id: U6
    epic: self-documentation
    title: "Fix the depgraph scanner's three blind spots so the code graph tells the truth: cross-root IMPORTS edges, function-level imports, and the missing drydocs_api scan root"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [knowledge/depgraph-snapshots/snapshot.ps1, docs/reviews/persona-python-architect-2026-07.md, docs/reviews/persona-project-manager-2026-07.md]
    acceptance: >
      A regenerated snapshot loaded via drydocs load-code-snapshot yields all
      of: (a) IMPORTS edges that CROSS scan roots — the probe "drydocs/ modules
      importing drydocs_core/" returns rows and its module count matches a
      fresh grep ground truth (24 files as of 2026-07-28); (b) function-level
      imports recorded — the known case drydocs_core/neo4j_client.py ->
      drydocs_core/cypher_split.py has an edge; (c) drydocs_api/ modules
      present as :CodeModule with project drydocs_api (snapshot.ps1 target
      list + its Phase-B comment updated); (d) the U1 orphan query re-run
      shows none of the 24 dispositioned false positives as importer-less
      unless the U1 report classed it a genuine entry point; (e) scanner-side
      regression tests covering (a)+(b) live in the depgraph repo's own suite.
    notes: >
      Groomed 2026-07-28 from the U1/U2 "one fix, three symptoms" IDEAS line,
      confirmed live by the graph-navigation benchmark (graph said 0
      cross-root drydocs->drydocs_core imports vs grep's true 24). The work
      splits across two repos: (a)+(b) are import-resolution changes in the
      EXTERNAL depgraph tool (local at C:/coding/projects/depgraph, own git
      history — invoked by snapshot.ps1 as python -m depgraph.cli scan);
      (c) is a one-line target-list edit in snapshot.ps1. Module graph-infra
      chosen over docs: the snapshot pipeline feeds the graph, it is not
      process prose. Unblocks U4 (re-sequenced onto this item) and makes
      graph-first agent navigation safe (see the companion IDEAS line, still
      inboxed). Verify with tests/integration + the live drydocs db; the
      IMPORTS caveat (gate D2 — never read an edge as breaks-if-removed)
      still applies after the fix.
      CLOSED 2026-07-28 same day (built + verified live): root cause was ONE
      defect — scan() ran each root in isolation, so absolute imports naming
      a sibling root (cross-root) OR the file's own package dir (the
      function-level neo4j_client case) never resolved; ast.walk was never
      the problem. Fix = shared-namespace extract_many in the depgraph repo
      (python_imports.py + scan(); 7 regression tests, committed on its
      feat/controlm-lineage branch) + drydocs_api added to snapshot.ps1
      targets. Live acceptance all four clauses: (a) 24 modules / 49 edges
      drydocs->drydocs_core, matching grep module-for-module; (b)
      neo4j_client->cypher_split edge present; (c) 11 drydocs_api
      :CodeModule; (d) orphan re-run = ONLY tests/* files (pytest entry
      points) — all 24 U1 false positives now have importers. Snapshot
      drydocs-20260728-0848.json: 205 modules, 370 IMPORTS (was 194/105),
      still 0 cycles; layering clean (zero drydocs_core->upward edges) —
      A1 is now answerable from the graph.

  - id: G34
    epic: ontology-mapping
    title: "Scaffold the business glossary NOW so the internal port cannot collide on a name or a backlog slot: reserve CatalogBusinessTerm + its edges as status: planned, schema public / definitions internal"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs: [docs/patterns/data-catalog/enterprise-data-catalog-ontology.md, drydocs_core/ontology/relationship_vocabulary/, docs/RELATIONSHIP_GUIDE.md, config/taxonomy-ontology-map/, reference/standards/, config/gate-log.md]
    acceptance: >
      RESERVATION ONLY — this item deliberately does NOT define terms or build a
      glossary. It claims the names and the slot so a later internal port cannot
      collide, then stops. Specifically: (a) `CatalogBusinessTerm` keyed by `term_id`
      is registered as a `proposed` taxonomy-ontology-map entry, taking the shape the
      data-catalog pattern already defines rather than inventing one; (b) its three
      edges — CatalogElement IS_REPRESENTED_AS, CatalogValidValue HAS_ALLOWED_VALUES,
      CatalogBusinessTerm DEFINED_FOR — are registered in
      drydocs_core/ontology/relationship_vocabulary.yaml at `status: planned`, the
      documented pre-gate step (SOSA/SSN precedent, the only other planned entry);
      (c) the classification split is written down: the glossary SCHEMA (label, key,
      edge terms, YAML shape) is Internal-Public and ports to the company repo, the
      term DEFINITIONS are Internal and live in internal/, and industry-standard terms
      bind to SKOS, already a declared reference standard in reference/standards/;
      (d) the file locations for both halves exist with a README stating the split.
      NO term is defined, no loader writes anything, and the HITL gate that would
      confirm the edges is NOT run here. Suite green.
    notes: >
      Raised at the business-application-identity gate §F2 (SIGNED OFF 2026-07-27).
      SME reason, verbatim in substance: the glossary will for the most part be
      internal unless there are industry-standard acronyms or names, and doing the
      scaffolding producer-side now means the internal ports do not collide later
      with either a name or a backlog item. That is a PORT-SEQUENCING argument, not
      an ontology one — which is why reservation is the whole deliverable.
      WHY IT IS AN ITEM RATHER THAN AN ACT OF THAT GATE: the identity gate's §A1
      states it "creates no relationship-vocabulary term". Registering the edges
      inside it would have falsified A1 and re-opened a ruled scope item, so the
      registration was pushed here, where `status: planned` needs no gate of its own.
      Substance this feeds: F2's finding that the durable home for what an acronym
      MEANS is a CatalogBusinessTerm-shaped glossary, the same conclusion the
      reopened gate-log Q6 acronym question circles, plus the Unity Catalog
      takeaway that a controlled vocabulary needs an enforcement point or it rots.
      The identity gate is itself the evidence: "SEAL" was found carrying THREE
      distinct meanings across three homes (the issuing authority, the source id,
      and an ecosystem referencing convention used in Control-M CMDLINEs), and
      nothing in the repo recorded that they were different things.
      MERGED IN 2026-08-05 (desktop, Idea-35 — the acronym-catalog line): that entry is
      CONTENT for this scaffold, not a second home, and it names the shape the scaffold
      should be able to hold — which is worth knowing while building the empty version.
      Its requirement: key by acronym, carry MANY senses per key, and tag each sense with
      its scope (`area`, `business-domain`, `technical-domain`, `industry` — what an
      outsider would assume). Plus, wherever a misreading has actually happened, an explicit
      **does-NOT-mean** note; that note, not the expansion, is the protective sentence.
      AIS is the worked example and the reason: "as-is" (a standard modeling idiom) vs
      "Application Integration Streaming" (an org platform family), with our own `:AisTool`
      label having meant NEITHER. Today `software-registry.yaml#acronyms` is a one-key
      section with no scope dimension, so it can record expansions but not COLLISIONS — and
      collisions are the failure mode that bites. This is a SKOS job (prefLabel / altLabel /
      definition / scopeNote, senses as concepts in a scheme); SKOS is declared in
      reference/standards/README.md but has NO fetched local copy yet, unlike
      prov-o / w3c-org / dprod-ekgf / sosa-ssn — fetching it belongs with the content pass.
      SCAFFOLD-ONLY STILL HOLDS: this item defines no terms, so nothing above changes its
      acceptance. The content pass stays parked on its own trigger, the gate-log Q6 ruling
      (which decides whether `#acronyms` survives at all), and lands INSIDE this scaffold.

  - id: G35
    epic: seal-attribution
    title: "Open the TOM-roles gate: the live registry shows 9 distinct accountable role classes against the signed 7, and cardinality is a per-role-class attribute rather than a global rule"
    type: requirement
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p1
    status: done   # SIGNED OFF 2026-08-11 at a live SME walk (gate-log: two RECORD entries + the GATE entry). Prior: CLAIMED 2026-08-10 (LAPTOP, pushed before work) — the release note's intended puller. Drafting half only: the gate-prompt spec is brought to a walkable state carrying the K21 evidence; the SME walks it afterwards and nothing loads until sign-off. Prior: RELEASED 2026-08-10 (SME direction) for the LAPTOP to pull. Was in_progress under this repo's awaiting-HITL convention (the E1/F1/F2/K2 pattern) -- NOT an abandoned claim, and no work is being discarded. Released because the DRAFTING half is pullable: the deliverable is a gate-prompt spec, and the SME walks it afterwards (the same drafting-only rule the 2026-08-10 sweep used for G62-G65). It goes to the laptop specifically because that machine holds the K21 evidence -- the DBeaver screenshots are gitignored and live only there -- and clauses (d) roster disagreement and (e) inheritance are exactly what that evidence speaks to. Whoever pulls it: claim in_progress and PUSH before starting, per the pull rule.
    depends_on: []
    inputs: [config/gate-log.md, config/taxonomy/business-application.yaml, drydocs/loaders/cypher/seal_contacts.cypher, drydocs/loaders/cypher/seal_applications.cypher, config/gate-prompts/seal-tom-attribution-reshape.yaml, knowledge/upgrade-plans/servicenow-replica-evidence.md, knowledge/upgrade-plans/servicenow-cmdb-analysis.md]
    acceptance: >
      A gate-prompt spec is authored and walked with the SME. It must rule: (a) the
      ROLE ENUMERATION — the signed 2026-07-10 §B list of 7 is contradicted by the
      live registry, which shows 9 distinct accountable role classes including L1
      Operate manager, L2 Operate manager and Operate Manager as THREE separate
      concepts rather than one with a level attribute; (b) CARDINALITY as a
      PER-ROLE-CLASS attribute, not a global singleton rule — the same application
      legitimately carries four different people in one role class (Backup
      Information Owner) while other classes are effectively singleton; (c) that the
      singleton constraint, where it applies, is expressed as a GRAPH-TEST and not a
      database constraint, because Neo4j cannot express "at most one per (app, role)"
      declaratively; (d) the ROSTER-DISAGREEMENT question — SEAL's own Application
      Contacts view and the ServiceNow TOM Accountable view return different counts
      for the same application, and nothing in the model records which surface an
      attribution came from; (e) whether INHERITANCE is modelled at all — the
      ServiceNow view carries Inherited / Overridden / (empty) plus an "Inherited
      from CI" parent, and DryDocs today models attribution as a flat assertion with
      no way to say an assignment was inherited rather than directly asserted.
      config/taxonomy/business-application.yaml's `roles:` list is reconciled to
      whatever is ruled. Nothing loads until sign-off.
    notes: >
      Carried over from the business-application-identity gate (SIGNED OFF
      2026-07-27) and deliberately NOT folded into S3: that gate's §A1 declares the
      signed seal-tom-attribution-reshape ruling (2026-07-10) UNCHANGED, so an item
      that re-opens its §B role enumeration cannot live inside it.
      Evidence is SME screenshots of the live registry, reviewed 2026-07-27 —
      Internal-Confidential, so no values are recorded here or in any committed
      file; only the SHAPE of the finding. Note the taxonomy file's current `roles:`
      list is itself divergent (it carries Chief Business Technologist and Risk
      Manager, which the live surfaces do not show under those names).
      Adjacent, already ruled and NOT reopened by this: §D2 of the identity gate
      pinned attribution_id's 4-part shape precisely BECAUSE of (b) — one person
      across three accountable roles collides if the role segment is dropped, and
      four people in one role class collide if the sid is dropped. Whatever this
      gate rules about role classes must keep that key shape valid.
      DRAFTING HALF DELIVERED 2026-08-10 (LAPTOP). STATUS STAYS in_progress ON PURPOSE — under the
      awaiting-HITL convention it was released from, because the acceptance says the spec is
      "authored AND WALKED with the SME" and only the authoring is done. Nothing is signed, nothing
      loads, and business-application.yaml's roles: list is deliberately NOT reconciled, since §H4
      reconciles it to whatever §A rules and §A is still open. WHAT THE PASS DID: brought the K21
      ServiceNow evidence onto the page, which is the reason this item was routed to the laptop.
      Clauses (d) and (e) were the two the release note named, and both were written from
      DESCRIPTION rather than evidence — (e) turned out to be wrong. §E4 reasoned the inherited-from
      parent is estate data DryDocs does not have and treated the modelled-node option as blocked on
      acquiring it; the parent is the AREA PRODUCT and :AreaProduct is a K6-signed node class we
      already hold, so the option was never blocked. The false premise is kept above its correction
      per the §A4 precedent, and §E5b states the K6 boundary explicitly so an inheritance edge to
      :AreaProduct is not later misread as merging the two role families K6 declared independent.
      Also landed: §D5/§D5b (the ServiceNow side specified end to end — and the two rosters differ in
      KIND of holder, not only in count), §B6b/§B6c (the TOM row names a GROUP, so §B6's
      always-a-person invariant needs its scope ruled before a loader relies on it; the PROV shape
      survives either way since prov:agent admits an Organization), §G0d-§G0f (G13/G14/G15 gain a
      named subject — the Deployment Module — which §G15 explicitly asked for, plus what that does
      NOT authorize and a correction to a reading that would have ruled §G15 by dissolving its
      subject), §F6/§F6b (the vocabulary-as-data target already running on the ServiceNow side,
      carrying a role RETIREMENT flag DryDocs has no equivalent for, which bears on §A2/§G5/§G12),
      §E1c (the TOM tables are a company scoped app with NO vendor baseline, so every TOM fact is an
      SME or instance fact), mapping rows 5-6, one new open question and one new risk. THE NEW OPEN
      QUESTION SHOULD BE SETTLED BEFORE THE WALK, not during it: is the TOM row's subject a Business
      Application CI or a Deployment Module CI? One GROUP BY cmdb_ci.sys_class_name answers it, and
      §G0e's fork turns on it. THE NEW RISK is filed rather than hidden: all of this evidence comes
      through a replica whose completeness is unconfirmed (K21 open question 9). Every ruling here is
      about SHAPE, which survives a partial copy; counts do not — including §C4's completeness
      report, which is a count by construction. Suite green at 1811 passed.

  - id: G70
    epic: seal-attribution
    title: "The TOM role vocabulary reshape — ONE unit: new declared vocabulary file + reader + guard, both registers, Scope/Type/required/retired, and every downstream surface amended together"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p1
    status: todo
    depends_on: [G35]
    inputs:
      - config/gate-log.md
      - config/gate-prompts/tom-roles-enumeration-and-cardinality.yaml
      - config/taxonomy/business-application.yaml
      - drydocs_core/schema/seal_ontology_supplement.cypher
      - drydocs_core/models/seal.py
      - drydocs/loaders/cypher/seal_contacts.cypher
      - drydocs/loaders/cypher/seal_applications.cypher
      - drydocs_core/ontology/relationship_vocabulary/41-local-seal.yaml
      - config/taxonomy-ontology-map/20-mappings-seal.yaml
      - knowledge/upgrade-plans/servicenow-replica-evidence.md
    acceptance: >
      LANDS AS ONE COMMIT SERIES, NOT PIECEMEAL — §H3 of the signed gate is explicit that these
      surfaces are mutually inconsistent if they land apart, and §A8 measured why: the vocabulary is
      hardcoded across four surfaces in three languages and the only YAML copy is read by no code.
      (1) NEW FILE under config/taxonomy/ holds the declared role vocabulary, per §F3, carrying BOTH
      registers (§A: the ServiceNow TOM catalog of 100+ types AND the SEAL extract's 13, with the
      mapping between them), plus per-class `scope` (Individual|Group), `type`
      (Accountable|Operational|Approval|Assignment), `required` (from the §G register) and a
      RETIREMENT state — §F6b: the ServiceNow side carries an `active` flag, so classes retire
      rather than being deleted, and DryDocs has no equivalent today. It gets a READER and a drift
      guard; a vocabulary nobody is forced to update rots into fiction (the ui-components precedent).
      (2) config/taxonomy/business-application.yaml's `roles:` list is DELETED, not reinterpreted —
      §F3 named leaving both as the one outcome to refuse. Note `roles:` is DERIVED from the
      memberships block, so the membership blocks move with it.
      (3) THE REGISTER IS SEEDED FROM THE SIGNED §G: REQUIRED = application_owner,
      primary_information_owner, backup_information_owner, cto, technology_risk_controls,
      design_authority, backup_application_owner. OPTIONAL = the three Operate Manager classes,
      chief_business_technologist, deployment_owner, deployment_information_owner,
      application_module_owner, and site_reliability_engineer.
      SEED BOTH SRE ROWS (SME 2026-08-11): the catalog carries Site Reliability Engineer at
      INDIVIDUAL scope AND an SRE/DevOps incident-resolver team at GROUP scope, and which one an
      application uses is a PER-TEAM IMPLEMENTATION CHOICE. Seed both, each with its own scope
      from the catalog, so a team that reorganises moves DATA rather than VOCABULARY. A register
      that picked one would need re-ruling every time a team changed shape.
      AND READ THE REGISTER MARKER RATHER THAN RECONSTRUCTING IT: the catalog's own description
      text labels the SEAL-sourced rows ('... from SEAL'), which IS the two-register mapping §A
      asked for. Read it and verify; do not derive it. Note `type` is weakly populated (mostly
      'other'), so it is not a reliable classifier on its own, and most of the 83 rows are out of
      scope — MARK those rather than dropping them, so the file's coverage stays legible.
      SRE IS A SPECIAL CASE — AMENDED 2026-08-11 after the sign-off, see the gate-log amendment.
      It is OPTIONAL *and DERIVED*: an SRE team serves 1:many applications (roughly 20-60), so it
      is a shared function pointing at many apps rather than a per-application holding, and its
      presence is READ FROM THE GROUP NAMING CONVENTION's function segment rather than asserted.
      Seed it with a `derived: true` marker so G71's completeness report can EXCLUDE it — a
      derived many-to-one fact in a per-application required-contact report generates a finding
      on every application whose SRE team simply was not asserted. AND DO NOT PATTERN-MATCH THE
      ROLE NAME: in the sampled data an SRE-named role resolves to a SUPPORT-function group (the
      team reuses one), so the role label disagrees with the group and the GROUP is right.
      (4) THE OPERATE MANAGER SPLIT IS APPLIED: operate_manager becomes THREE concepts (L1, L2,
      bare); the `level` property and c7.levels retire with it. The bare class carries its ruled
      definition — responsibility scope (change, problem and incident resolution), not a level.
      (5) RISK MANAGER CROSSWALKS TO technology_risk_controls (§A2 ruled: one class, two names) —
      and STOPS THERE. The SEAL-risk ↔ ServiceNow-group mapping is NOT known and no group
      resolution is designed for it (signed residual 1).
      (6) SealRole IS RETIRED AS THE ADMISSION GATE (§A3/§F4): an unrecognised name LOADS flagged
      undeclared instead of raising ValidationError and killing the row. _ROLE_CANONICAL survives as
      an alias table. The four classes today's code rejects outright must load after this.
      (7) EVERY DOWNSTREAM SURFACE AMENDED IN THE SAME UNIT: the supplement's scheme seed, both
      loader crosswalks, the map entry's role_vocabulary text, the vocabulary fragment,
      NODE_QUICK_REFERENCE.md, and the seal-extract field ledger's "kept verbatim" claim (§A4b — the
      raw source string must survive ALONGSIDE the canonical one, not instead of it).
      (8) MIGRATION IS EXPLICIT (§H5): seal_contacts.cypher builds attribution_id from the SOURCE
      role name while seal_applications.cypher uses literal concept ids, so any renaming re-keys the
      contact-side Attributions and orphans the old nodes. Either carry a migration step or rebuild
      from source — say which. The attribution_id 4-part shape itself is UNTOUCHED (identity gate §D2).
      Suite green; no graph write beyond the supplement re-run.
    notes: >
      Raised 2026-08-11 at the G35 sign-off. THE ONE-UNIT RULE IS THE POINT — §A8 showed the
      vocabulary living in four hardcoded places, and §A1b showed the one YAML copy drifting twice
      inside a single gate with the full suite passing both times, because no code reads it.
      G35 §A ruled BOTH REGISTERS, so this file is not a list of 13 names: it holds the ServiceNow
      catalog's 100+ types with their Scope and Type, the SEAL extract's names, and the mapping.
      Only ~28 types were observed instantiated on one sampled application, so the file should
      distinguish DECLARED from OBSERVED rather than implying every catalog entry is in use.
      MERGED 2026-08-11 from Idea-102 (does not change the signed section G register, which is
      why it lands here and not in the acceptance): the SME confirmed the deployment grain --
      ONE APPLICATION, MANY DEPLOYMENTS, with the identifier reading app_id(seal_id):deployment_id
      -- and supplied the CI topology behind it (Business Application [Instantiates] Deployment
      Module, inverse [Instance of], [Contained by] area product). The consequence for this file
      is that section G's G13 deployment_owner, G14 deployment_information_owner and G15
      application_module_owner plausibly share ONE SUBJECT, the Deployment Module CI, because
      "deployment" and "module" are the same thing here. Record the shared subject rather than
      carrying three unrelated register lines, and note that attribution itself does NOT move --
      everything DryDocs maps stays off the application. The CI class capture is K22; this entry
      exists so the register does not have to rediscover it.
  - id: G71
    epic: seal-attribution
    title: "The required-contact completeness report + the presence graph-test — the first surface that can say an application is missing an accountable owner"
    type: task
    module: drydocs-review
    phase: 9
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G70]
    inputs:
      - config/gate-log.md
      - config/gate-prompts/tom-roles-enumeration-and-cardinality.yaml
    acceptance: >
      A per-application completeness report and the graph-test behind it, per the signed §B5/§C4/§C6.
      THE CHECK COUNTS TO ONE AND STOPS: a REQUIRED class with zero holders on a covered application
      is REPORTED, never refused at write time — §C1 closed the constraint route on platform grounds
      (Neo4j cannot declare a count over a traversal in either direction), and §C3 refused the
      tempting re-key because multiple holders are the NORMAL shape.
      THE DENOMINATOR IS RULED (§C6): only applications a feed actually covered. An application no
      feed mentioned is a CAPTURE gap and is reported SEPARATELY — merging the two produces a noisy
      check, and the first response to a noisy check is to weaken it.
      IT JOINS THE EXISTING REVIEW SURFACE rather than opening a new one (§C5): ownership.attributions.v1
      already orders unmapped rows first, so an operator reads ONE page for "what is wrong with this
      application's contacts".
      TWO CAVEATS THE REPORT MUST CARRY IN ITS OWN OUTPUT, both from the sign-off. (a) The Operate
      Manager assignments are MID-CORRECTION AT SOURCE (the SME is rescoping the team to platforms it
      owns), so findings there are real defects being fixed elsewhere — the report should say so
      rather than imply DryDocs discovered them. (b) The check cannot distinguish a genuine 24h
      coverage gap from a satisfied one, because Operate Manager multiplicity is GEOGRAPHY and
      nothing records which holder covers which region (signed residual 4).
    notes: >
      Raised 2026-08-11 at the G35 sign-off. Has no counterpart today: nothing checks per-application
      completeness at all, which is why a missing required contact is currently invisible while an
      unreadable role name kills its row — the exact inversion §B5 ruled against.
  - id: G72
    epic: seal-attribution
    title: "The three-valued surface discriminator + the precedence order — make a roster disagreement a readable fact"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G70]
    inputs:
      - config/gate-log.md
      - config/precedence.yaml
      - knowledge/upgrade-plans/servicenow-replica-evidence.md
    acceptance: >
      Per the signed §D3/§D4. THE SURFACES ARE THREE, not the two §D1 described: hand-verified
      crosswalk, SEAL contact extract, ServiceNow TOM. Every Attribution records which asserted it —
      today Attribution.source is the literal string 'SEAL' at both loader sites regardless, so a
      count difference is unattributable.
      TRUST RIDES ON EXISTING VOCABULARY, NOT A NEW ONE: the company crosswalk already carries
      `verified` / `cert_status` / `cert_next_date`. Reuse rather than mint — the same discipline that
      reads cmdb_rel_type's descriptor columns instead of splitting `name`.
      PRECEDENCE IS RULED AND GOES INTO config/precedence.yaml: hand-verified > ServiceNow TOM >
      SEAL extract. §A's both-registers ruling made this MANDATORY rather than deferrable — two
      ingested surfaces that disagree with no rule is the one outcome §D4 refuses.
      CHECK BEFORE BUILDING (§E1g): ServiceNow's Direct rows carry `u_source` naming the feed that
      produced them, which may already be the discriminator this item would otherwise invent.
      THE CONSUMER IS O44, the four-column reconciliation alluvial — a disagreement renders as a
      ribbon that STOPS, so the discriminator must distinguish "this surface did not assert it" from
      "this surface was never read". A diagram cannot draw a difference the data does not carry.
    notes: >
      Raised 2026-08-11 at the G35 sign-off. NOTE THE THIRD SURFACE IS COMPANY-SIDE ONLY: the
      hand-verified crosswalk belongs to gate snow-hpsm-queue-to-group, which the producer repo does
      not hold (RELAY-6, and §9 of the evidence doc). So the discriminator must be able to NAME a
      surface this repo cannot itself load.
  - id: G73
    epic: seal-attribution
    title: "Inheritance: assertion mode + a modelled ancestor edge, authored rows only — and the ancestor-CI widening that K21 §7.4 needs"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G70]
    inputs:
      - config/gate-log.md
      - knowledge/upgrade-plans/servicenow-replica-evidence.md
      - drydocs_core/ontology/relationship_vocabulary/41-local-seal.yaml
    acceptance: >
      Per the signed §E. THE LOAD STORES AUTHORED ROWS ONLY: of the three Inheritance states, Direct
      (blank) and Overridden are AUTHORED; Inherited rows are DERIVED COPIES carrying two lineage
      pointers. Inheritance is computed, not materialised — storing a derived fact as though asserted
      is how a graph starts disagreeing with its source.
      THE ANCESTOR IS A MODELLED EDGE, not a property (§E4 ruled), so "why does this application show
      a different owner than its parent" is TRAVERSABLE — which is what §D2 says an operator cannot
      do today. Both lineage references are load-bearing: the ancestor CI and the ancestor ROW.
      IT IS ONE MECHANISM UP AN N-LEVEL CHAIN, not a two-level relation (§E1e): "copy from the
      nearest ancestor holding the role", applied repeatedly. A model built for a fixed
      application→deployment relation cannot represent the observed pass-through case, where a role
      originates two rungs up and passes straight through the application untouched.
      OVERRIDES PROPAGATE: a child inherits the ancestor's CURRENT value even when that value is
      itself an Override, so the effective holder can differ from the top of the chain.
      AND IT AMENDS THE PULL SCOPE — this is the item that carries it. Reconstructing an inherited
      holder requires ANCESTOR CIs, which sit ABOVE the ~200 applications DryDocs supports, including
      CIs owned by teams it does not. K21 §7.4 is amended to take ancestor CIs FOR THEIR TOM ROWS
      ONLY, and the amendment is written there rather than left implicit.
    notes: >
      Raised 2026-08-11 at the G35 sign-off. This is the first place the ~200-application boundary and
      the inheritance model actually conflict, and the gate crossed it deliberately rather than
      quietly. The alternative — materialise inherited rows and flag them derived — stays inside the
      boundary and was considered and rejected: it duplicates, and it drifts when an ancestor changes.
  - id: G74
    epic: seal-attribution
    title: "The :Employee spine — hierarchy placement (§B7's deferral has no owner) AND the creation policy, where the runbook forbids exactly what seal_applications.cypher does"
    type: task
    module: ontology
    phase: 9
    agent: main
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - config/gate-log.md
      - drydocs_core/ontology/relationship_vocabulary/10-node-classifications.yaml
      - drydocs/loaders/cypher/seal_applications.cypher
    acceptance: >
      TWO CLAUSES, one spine. Per the signed §B7. The TOM load links an Attribution to a PERSON and stops; placing that person
      in the reporting structure is separate work on a separate cadence. THE DEFERRAL CURRENTLY POINTS
      AT NOTHING, which is what this item fixes: :Employee is a node class with NO Employee-to-Employee
      edge anywhere in the relationship vocabulary — no REPORTS_TO, no manager edge — and until this
      item there was no backlog entry that would create one.
      Scope: propose the edge and its provenance, route it through docs/RELATIONSHIP_GUIDE.md and the
      relationship-vocabulary registry as status: planned, and identify the SOURCE that could populate
      it. DO NOT BUILD A LOADER — the source question is the real one, and the producer-side HR
      hierarchy direction (two-scope HR supplement, two-pass loader, REPORTS_TO current-state sweep)
      is company-gated work that this item must not pre-empt.
      A CLOSER SOURCE MAY EXIST: group-scoped TOM roles resolve to sys_user_group MEMBERS today, which
      is a people-graph of a different shape — adjacency rather than hierarchy. Say whether that is a
      route or a distraction.
      SECOND CLAUSE, ADDED 2026-08-11 (SME, from a desktop session) — RULE THE :Employee CREATION
      POLICY, BECAUSE THE DOC AND THE CODE CONTRADICT EACH OTHER AND BOTH ARE DEFENSIBLE. The runbook's
      "spine-and-enrich" rule, in BOTH v2 and the published v3, states that :Employee is created ONLY
      by load-employee-roster and that SEAL attaches enrich-only via OPTIONAL MATCH — "a SID not in the
      roster gets no edge, never a stub". seal_applications.cypher does the OPPOSITE: it MERGEs
      (e:Employee {employee_id: <owner/CTO/info-owner SID>}) ON CREATE SET e.source = 'SEAL', minting
      placeholders.
      DIRECTION LANDED 2026-08-18 (SME in-chat, gate-log RECORD same date): STUB-AND-ENRICH WINS.
      "The HR database has ~300k; my expectation is that it will be the stub and supplemented with
      HR data later." The loader behavior is the policy; the runbook text is what must change. What
      this item still owes: harmonize the runbook (v2 + published v3), rule the stub property idiom
      (what marks awaiting-enrichment beyond `source`), and clause 1 (REPORTS_TO + its source) in
      full. pat_team_roles.cypher was flipped to the MERGE-stub idiom in the RECORD's commit.
      MEASURED STATE (desktop graph, 2026-08-11): 11,464 :Employee nodes, ALL source='SEAL', every one
      minted by the seal_applications load. They carry employee_id + full_name and NO REPORTS_TO — there
      is no HR spine yet. 11,445 are HAS_AGENT targets of ownership attributions; 19 are unattached
      (likely CTO/info-owner SIDs whose attribution did not land, or delete leftovers).
      THE MECHANISM IS SOUND AND SELF-RECONCILING, which is why this is a ruling and not a bug:
      load-employee-roster MERGEs on the SAME key and unconditionally flips source SEAL -> HR
      (authoritative), so a SID in both is upgraded and gains the REPORTS_TO spine, a roster-only SID
      is created fresh as HR, and a SID in SEAL but in no loaded HR subtree STAYS source='SEAL'. That
      residual is a COVERAGE REPORT, not debris — count the source='SEAL' survivors after a roster load
      and you have the owners HR did not cover. The placeholders are intentional and keyed to reconcile.
      RULE ONE OF TWO, and record which: (a) FIX THE DOC to describe the placeholder pattern, keeping
      the code's behaviour — it preserves the ownership SID and name that would otherwise be LOST
      whenever the roster is not loaded, and the source flag keeps them cleanly reconcilable; or
      (b) FIX THE LOADER to OPTIONAL MATCH, if roster-only really is the intent — at the cost of
      dropping ownership identity when the roster is absent.
      NOT A MID-LOAD DECISION and not urgent: the graph is safe to run load-employee-roster against as
      it stands. What is NOT acceptable is leaving a design doc that forbids what the loader does, since
      the next reader trusts whichever they happen to open.
      THE RULING HAS A COMPANY-SIDE HALF, VERIFIED PRODUCER-SIDE 2026-08-11 — DO NOT RULE IT HERE ALONE.
      Only ONE of the three surfaces is producer-side: drydocs/loaders/cypher/seal_applications.cypher
      DOES mint the placeholders, at lines 145 / 169 / 192, for app_owner_sid, chief_tech_officer_sid
      and info_owner_sid respectively — checkable from here. The OTHER TWO ARE NOT. There is no
      employee-roster loader producer-side (no load-employee-roster verb, no roster cypher; the
      port-prompt already records tests/unit/test_employee_roster.py as company-only, T21), and the
      "spine-and-enrich" runbook prose that FORBIDS the stub appears in no producer runbook under
      docs/design/. So the producer holds the code that mints, and the company holds both the doc that
      forbids it AND the loader that reconciles it.
      CONSEQUENCE: the producer can rule what ITS loader should do, but it cannot fix a doc it does not
      have, and it cannot verify the reconciliation half. Route this ruling through the port rather than
      landing it one-sided — the same asymmetry class as RELAY-6, and it is the second time this session
      that a producer-side item turned out to overlap company-only material.
    notes: >
      Raised 2026-08-11 at the G35 sign-off, on the SME's instruction that the deferral needed an
      owner. Deliberately p3 and dependency-free: it blocks nothing and nothing blocks it, and it
      exists so a correct deferral stops being an omission with better wording.
      THE SECOND CLAUSE ARRIVED THE SAME DAY and belongs here rather than in its own item because it is
      the SAME SPINE: clause one asks what creates the Employee-to-Employee edge, clause two asks what
      is allowed to create the :Employee NODE. Ruling the second without the first would settle the
      node policy while leaving the hierarchy homeless, and ruling the first without the second would
      build a spine on top of an unruled creation policy. The doc-vs-code contradiction was found by a
      desktop session reading the loader against the runbook, and it is the kind that survives because
      each surface is internally consistent — only reading both together exposes it.
      MERGED IN 2026-08-12 (groom, desktop) — Idea-73, "where does the employee hierarchy come from,
      and does it live producer-side at all", filed 2026-08-05 and open since. It is this item's clause
      one asked from the other end, so it rides here rather than opening a second owner. Three things it
      adds that the acceptance should carry through the ruling: (1) THERE IS A SECOND WAITING CONSUMER
      besides §B7 — O44's first column is an ownership-reconciliation view whose MANAGER FILTER is its
      whole point, so two committed directions currently defer to a structure that does not exist;
      (2) pat:people-report is NOT the source — it carries TEAMS, not reporting lines, so the closest
      producer-side people data cannot populate a hierarchy (G82's projection does not change that);
      (3) the 2026-07-23 producer-session HR-hierarchy direction — single :Employee spine, two-scope HR
      supplement, two-pass loader, REPORTS_TO current-state sweep — was written FOR THE COMPANY
      hr-emp-hierarchy gate, which is the likeliest reason nothing landed here. So the honest outcomes
      include "the producer records explicitly that both halves are company-side": a deferral pointing
      at nothing is an omission with better wording, and saying so counts as ruling it.

  - id: G82
    epic: seal-attribution
    title: "The PAT team-report PROJECTION is missing — DevTeamsLoader and PatProductMappingLoader are real, tested and have never run on real data"
    type: task
    module: drydocs-load
    phase: 9
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs/loaders/catalog.py
      - drydocs/loaders/cypher/dev_teams.cypher
      - drydocs/loaders/cypher/pat_product_mapping.cypher
      - config/source-registry.yaml
      - drydocs/data/samples/dev_teams__sample.csv
    acceptance: >
      BUILD THE RAW -> PROJECTED EXTRACTION FOR THE PAT TEAM REPORT, which is the one link missing
      between a real source file and two loaders that are otherwise complete.
      THE STATE, verified producer-side 2026-08-11 rather than assumed. Both loaders EXIST and are
      wired: DevTeamsLoader (drydocs/loaders/catalog.py, source_id `pat:product-catalog`, cypher
      dev_teams.cypher) and PatProductMappingLoader (same file, source_id `pat:people-report`, cypher
      pat_product_mapping.cypher, guarded by PreCutoverApplicationGuard). Both SOURCES are registered
      and `pat:people-report` is already `confirmed: true` (C9 gate, 2026-07-18). A sample fixture
      exists (drydocs/data/samples/dev_teams__sample.csv), so sample-mode runs.
      WHAT DOES NOT EXIST, ON EITHER SIDE: the script that projects the raw PAT team report CSV into
      the two files the loaders actually read. It is referenced by the loader docstrings company-side
      and was never built. CONSEQUENCE, and it is the finding rather than the task: THE DEV-TEAM LOAD
      HAS NEVER RUN AGAINST REAL DATA. Everything green about it today is green against a fixture.
      CORROBORATION ALREADY IN THE REPO: `pat:people-report` carries `locator.mapping: ~` — no field
      ledger. A confirmed source with no column mapping is exactly the fingerprint of a feed nothing
      has ever read for real, and it went unnoticed because the loaders pass on the sample.
      DELIVERABLES. (1) Read both row models (DevTeamRow, PatProductMappingRow) for the exact columns
      required. (2) Read the raw report header. (3) Write the projection — a script, or emit the two
      CSVs directly; the choice is whether the projection is a repeatable step or a one-off. (4) Fill
      `locator.mapping` for `pat:people-report` from what the projection actually reads, so the ledger
      stops being null. (5) Run the load against real data and report what changed versus the fixture.
      NO REAL VALUES IN THE REPO: the raw report and the projected CSVs are Internal and stay
      company-side; only the projection MECHANISM and the column ledger land here.
    notes: >
      Raised 2026-08-11 (SME: "another loader that I thought was built is not complete"). THE LOADER IS
      NOT THE INCOMPLETE PART - that is the useful distinction. The loaders, their cypher, their row
      models, their source registrations and their tests are all real; the INPUT PIPELINE is missing,
      one step upstream of everything that gets tested.
      WHY IT SURVIVED, and it generalises beyond this item: a sample fixture makes a loader test green
      independently of whether any real file can reach it. So "the loader is built and tested" and "the
      load has never run" are both true at once, and nothing in the suite can tell them apart. Worth
      asking, once this is fixed, which OTHER confirmed sources carry `locator.mapping: ~` - that null
      is the cheapest available signal for the same condition elsewhere.
      SCOPE NOTE: this is producer-side buildable. Unlike G74 and RELAY-6, nothing here is
      company-only - both loaders, both sources and the fixture are in this tree. Only the DATA is
      company-side, which is the normal split rather than an asymmetry.

  # ── appended at the 2026-07-27 groom run (weekly inbox groom) ──────────────
  # Sources: the two 2026-07-27 publish-boundary inbox entries (→ J14/J15), the
  # 2026-07-26 schema/spec findings from the identity-gate review session
  # (→ G36/O33/G38), plus one merge (deepdoc charter drift → G32 clause e).
  # The catalog-gate drift entry stays inboxed on its company-sign-off trigger
  # (its coordinate-first constraint lifted — the laptop session landed 07-27).
  - id: J14
    epic: release-infrastructure
    title: "Split the two knowledge/standards/technology docs that carry real SEALIDs — mechanism stays public, registry tables and real values move to internal/ (blocks any public push)"
    type: task
    module: docs
    phase: 8
    agent: main
    model: fable
    priority: p0
    status: done
    depends_on: []
    inputs: [knowledge/standards/technology/description-field-metadata-plan.md, knowledge/standards/technology/folder-naming-convention.md, PUBLISH-BOUNDARY.md, config/classification.yaml]
    acceptance: >
      Both files are split per option (b) of the 2026-07-27 inbox entry: the
      MECHANISM halves (the naming grammar, the positional-code semantics, the
      escalation-table join SHAPE) stay under knowledge/ as Internal-Public; the
      app-code -> SEAL registry rows, real application names, the SEALID
      format/width disclosure, and the real Oracle table/column/value details
      move to internal/ twins, each half cross-referencing the other (the
      established `ccb-` twin convention). After the split, a grep of the
      publishable tree for each moved value returns hits ONLY under internal/;
      neither public half claims anything no longer true once the values are
      gone (the "document that no longer says anything true" failure is the
      reason sanitize-in-place was rejected). Suite green, including
      tests/unit/test_classification.py.
    notes: >
      Found 2026-07-27 while sweeping the SEALID publish-boundary breach — the
      other 13 files were sanitized in that sweep; these two were deliberately
      left because the real mappings ARE the knowledge, making this a
      RELOCATE-vs-SANITIZE decision, not a find-and-replace. Options considered:
      (a) move both files wholesale with a stub left behind, (c) reclassify the
      whole knowledge/standards/technology/ directory as Internal. (b) chosen
      because it is the shape the repo already uses elsewhere; the diff is the
      user's review surface if they prefer (a) or (c).
      DELIBERATELY OUT OF SCOPE: the adjacent internal-platform-vocabulary
      question (HLDM/PRARAG tokens etc. in the sample corpus) — a different
      value class, not ruled on, re-inboxed as its own IDEAS question.
      Model fable per the matrix: this changes what the publish boundary
      contains, a boundary decision, even though the mechanics are file moves.
      CLOSED 2026-07-27 (same-day build). Twins at
      internal/standards/technology/ (Internal-Confidential, incl. the
      real<->synthetic id key); public halves keep the grammar/plan with
      sanitized examples drawn from the sample corpus's own conventions
      (70001-70099 block, PRARAG-HLDM-70002-... shapes). Moved-value grep clean
      outside internal/ with two recorded exceptions, both left for the
      residual identifier-class ruling, not silently: (1) the escalation-table
      schema identifiers were GENERALIZED out of these two files but remain in
      6+ other tracked files (skills, gate prompts, taxonomy-ontology map,
      remediation TDD) — an identifier class, not a data value, same residual
      class as the platform vocabulary; (2) the phrase "Home Lending Servicing"
      persists in the declared-synthetic sample taxonomy paired ONLY with
      synthetic ids (a product-name class, predates J14). New out-of-scope
      finds re-inboxed 2026-07-27: real person name + internal DLs in
      docs/Product/product-overview.md and two test fixtures; two sample
      folder ids outside the synthetic block in config/taxonomy/controlm.yaml.
  - id: J15
    epic: release-infrastructure
    title: "Boundary guard test — grep the publishable tree for the VALUE shapes real ids take and fail on any id outside the reserved synthetic block (70001-70099)"
    type: task
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p1
    status: done
    closed: 2026-07-27
    close_note: >
      BUILT: tests/unit/test_publish_boundary_values.py — three scans over the
      git-tracked publishable tree (bare 5-6 digit ids in taxonomy/samples/
      knowledge; folder-name-parser numeric segments EVERYWHERE — the historical
      miss class; %%SEAL-paired values in tests), block 70001-70099 or
      allowlist-with-reason, no real values embedded in the test. Building it
      forced 14 resweeps (values now internal-side only): the two flagged
      folder-name ids (unknown realness, treated real), a %%SEAL value + a FID
      triplet + an RFID + a job-name segment CONFIRMED real against the
      untracked production extract, five authored pre-block strays, and the
      PAT sample's out-of-block seal_ids. Real↔synthetic key rows recorded in
      the internal/standards twin. Recorded ruling (SME may flip): 6-digit
      Control-M surrogate folder TABLE KEYS stay, allowlisted.
    depends_on: [J14]
    inputs: [tests/unit/test_classification.py, config/classification.yaml, PUBLISH-BOUNDARY.md, drydocs/data/samples/, config/taxonomy/, drydocs_core/controlm/folder_name.py]
    acceptance: >
      A unit test in the classification/boundary family scans the publishable
      tree (everything outside internal/ and gitignored paths) for the shapes
      real identifiers take — bare 5-6 digit ids in taxonomy/sample/knowledge
      files, and the numeric segment of any string the folder-name parser
      decomposes — and FAILS when a value falls outside the reserved synthetic
      block (70001-70099), unless the specific file+value is allowlisted with a
      recorded reason. It must catch the historical miss: a SEALID embedded
      inside a Control-M folder-name string (PRARAG-HLDM-<id>-...) that no grep
      for the field name `seal_id` can see. Runs in the default suite with no
      live services; green on the tree as left by J14.
    notes: >
      Lesson entry 2026-07-27: two sweeps (9d59f53 and the 2026-07-27 one)
      failed the same way — they searched for the FIELD, not the VALUE, so real
      ids survived for months inside folder-name strings. Same failure family
      as the "succeeds loudly, does nothing" session through-line, and the same
      conclusion as the identity gate's §F2 / the Unity Catalog takeaway: a
      convention violated twice needs an enforcement point or it rots.
      depends_on J14 because the test is red today on the two files J14
      relocates — landing it first would just document the breach in CI.

  - id: J16
    epic: release-infrastructure
    title: "PORT-MANIFEST fall-through guard — no tracked path may silently resolve to `default: clean-add`; the inverse question nobody has asked"
    type: task
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [PORT-MANIFEST.yaml, tests/unit/test_port_reconcile_guards.py, git-readme.md]
    acceptance: >
      A guard in tests/unit/test_port_reconcile_guards.py walks the tracked
      tree, resolves every path FIRST-MATCH against PORT-MANIFEST.yaml's rows,
      and FAILS on any path that falls through to `default:` unless the path
      (or a covering pattern) is on a written allowlist with a reason per
      entry — the test_no_shadow_definitions shape (C18) applied to port
      dispositions. Making it green forces the backlog of judgment calls into
      rows: whatever falls through today gets an explicit row or an allowlist
      entry, including the standing DECISION that git-readme.md is deliberately
      uncovered (the WHY guide for the cross-repo model, both sides benefit) —
      currently written down nowhere but the idea inbox. Runs in the default
      suite, no live services, no consumer-side env vars required.
    notes: >
      Groomed 2026-07-28 from the 2026-07-27 inbox. The motivating incidents:
      depgraph snapshots and docs/port-*.md were both silently on clean-add —
      the manifest was instructing the consumer to commit the producer's port
      INSTRUCTIONS as payload — found by accident one at a time, and a prose
      "do NOT clean-add" workaround in step 43 stood in for the missing row
      for a week. Both rows are fixed (378f4ba, bf3fd34); this item is the
      inverse question: which paths match NO row? Until it exists every port
      re-derives the same judgment calls and the manifest keeps disagreeing
      with practice.
      DONE 2026-07-28. Measured answer: 398 of 1415 tracked paths (28%) took
      `default:` with nothing written down. FOUR became real rows, and the
      first is the finding that justifies the item on its own —
      docs/restructure/backlog.yaml HAD NO ROW, so the manifest was telling
      the consumer that a blind checkout of the producer's plan was an
      acceptable resolution for a file whose ids overlap both sides; it is now
      per-entry with an entry_rule modelled on the taxonomy-ontology-map row.
      Also new rows: docs/plan/board.html (derived — regenerate, never take
      the producer render), docs/design/** and libs/oracle_kerberos/**
      (evaluate + warning notes; the second records that the oracle_adapter.py
      port-freeze does NOT extend there). Everything else became a
      `default_ok:` entry — BEHAVIOR-PRESERVING by construction, recording why
      the default is right rather than changing it — including the git-readme.md
      standing decision, which now has its own regression test. Guard proven to
      bite against an uncovered probe file, not just in principle.
  - id: G36
    epic: component-topology
    title: "Drop the three never-consulted :BusinessApplication indexes (risk_level/status/name) and decide coverage for the one property that IS a predicate, manually_created"
    type: chore
    module: ontology
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [S3]
    inputs: [drydocs_core/schema/constraints.cypher, drydocs/loaders/manual_loads.py, drydocs_api/query_specs.py]
    acceptance: >
      constraints.cypher no longer declares businessapplication_risk,
      businessapplication_status, or businessapplication_name (lines 37-39
      today); a grep confirms no QuerySpec, loader, or web/src read binds on
      those properties (today zero do — they appear only in SET clauses and
      RETURN projections, where an index cannot help). A recorded decision (in
      the commit body) rules whether manual_loads.py:113's
      `{manually_created: true}` predicate — the one real predicate with no
      index — gets one; if yes it exists under a NEW name (the
      CREATE-IF-NOT-EXISTS name-match trap). Bootstrap schema re-run against
      the live container succeeds; suite green.
    notes: >
      User finding 2026-07-26 reading constraints.cypher during the
      business-application-identity gate review: every bind on
      :BusinessApplication goes through the key property, already covered by
      the UNIQUE constraint's backing index, and explorer.applications.v1 is an
      unfiltered label scan no name/status index can accelerate. A declared
      index READS AS A CLAIM the property is queried, so it mis-maps access
      patterns for the next reader; at registry cardinality a label scan is
      sub-millisecond. Original intent unrecoverable — git log -L reaches only
      the 2026-07-20 squash root.
      depends_on S3 because S3 rewrites the SAME file (key flip seal_id ->
      app_id + new uniqueness constraint + :Port NODE KEY): one bootstrap
      re-run instead of two, and no drive-by edit racing the atomic cutover.
      DONE 2026-08-04 (ea66764): three indexes removed AND explicitly DROPped
      (the port_unique belt-and-braces precedent — removing the CREATE alone
      leaves live DBs carrying the stale claim). Predicate census in the
      commit body; the one name-bind is the single-node SchemaMeta bootstrap
      MERGE. RECORDED DECISION: manually_created gets NO index — both
      consumers are offline reporting paths at registry cardinality; same
      ruling for batch_orchestrator_last_run_id, the other non-key predicate
      the sweep surfaced. Live re-run verified (desktop, neo4jtest, drydocs
      DB): bootstrap succeeded, SHOW INDEXES returns only the two
      constraint-backed indexes. Suite 1501.
  - id: G38
    epic: component-topology
    title: "Prove the ddall federated read path with one real cross-database QuerySpec — or record that the composite is unnecessary and retire the unused watermark machinery"
    type: task
    module: drydocs-api
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [G32]
    inputs: [drydocs_api/query_specs.py, drydocs_api/exports.py, drydocs_core/schema/provisioning/smoke_drydocs_all.cypher, tests/unit/test_database_names.py]
    acceptance: >
      Either: one QuerySpec answers a real support question by reading `ddall`
      across at least two member databases (chained `CALL { USE ddall.x }`
      value-join per the smoke-file shape), carries the trust_watermark column
      end to end, and is covered by a test — OR, if G32's ruling makes the
      composite unnecessary, that is recorded and the consumer-less machinery
      (WATERMARKED_DATABASES, the export banner path for ddall) is retired.
      Either outcome closes the item; leaving the machinery built-but-unread
      does not.
    notes: >
      Measured at the G30 close: 22 of 23 QuerySpecs read drydocs, 1 reads
      ddcontext, ZERO read the composite — the federated watermark machinery is
      fully built and tested against a surface no spec uses. Same shape as the
      G30 finding (infrastructure with no consumer), and it gets more expensive
      to leave alone if G32 adds a third data database. Written AFTER G32 so it
      targets the ruled database set; it is also the honest end-to-end test of
      G32's acceptance clause (d), the recorded cost of the database boundary.
  - id: O33
    epic: web-console
    title: "SchemaMeta exemplar nodes contaminate unfiltered QuerySpecs — the applications frame would list the :BusinessApplication exemplar as an app with a null id; add exclusion predicates and audit all ~20 exemplars"
    type: bug
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/schema/schema_graph.cypher, drydocs_api/query_specs.py]
    acceptance: >
      Every QuerySpec whose MATCH binds a label that schema_graph.cypher also
      stamps on a :SchemaMeta exemplar (schema_graph.cypher:39+ MERGEs
      `:SchemaMeta:<RealLabel>` pairs — ~20 of them) excludes the exemplar,
      preferring the rename-proof label predicate (`WHERE NOT n:SchemaMeta`)
      over a non-null-key predicate. The audit covers ALL exemplar labels
      against ALL specs, not just explorer.applications.v1 — AND (merged
      2026-07-28) prereq/guard queries in LOADERS: any loader guard that
      counts a label the meta-graph stamps must exclude exemplars the way Q8
      shipped (`WHERE NOT sp:SchemaMeta AND sp.product_id IS NOT NULL`),
      else a keyless exemplar waves an empty registry through. A test proves
      it: with the meta-graph applied to a test database, no spec returns a
      :SchemaMeta node and no loader guard is satisfied by exemplars alone.
      The meta-graph itself is KEPT — it is applied manually by design and
      removable with `MATCH (n:SchemaMeta) DETACH DELETE n`.
    notes: >
      Found 2026-07-26 alongside the G36 index note. LATENT, not live: the file
      header says "Applied MANUALLY only — never part of drydocs bootstrap",
      but whenever it IS applied, the exemplar carries the real label with no
      key property, and the UNIQUE constraint does not stop it because Neo4j
      uniqueness ignores nulls (the same null-blindness behind the identity
      gate's §C2 partial-cutover hazard). Key-predicate wording would change
      under S3's seal_id -> app_id flip — another reason the label predicate is
      the primary fix.
      MERGED 2026-07-28 (inbox line, Q8 build finding): the contamination
      defeats WRITE-side guards too, hence the loader-guard audit clause
      above. Root-fix option to weigh during the audit: give the exemplars
      their key properties (a keyless exemplar is indistinguishable from a
      real node with an unset key) — if taken, record it in the commit body;
      the label predicate stays the belt either way.
      CLOSED 2026-07-28: 22 of 23 specs gained the label predicate (only
      ownership.escalation-routing binds no stamped label); the audit found
      the meta-graph also MERGEs property-qualified exemplar EDGES
      ({role:'seal_app_ref'}), so pattern specs were exposed too — worse
      than the single-node case the item described. Root fix REJECTED with
      reasoning: fake keys would satisfy the key-based loader guards (the
      exact Q8/L17 family) — keyless + label predicate is strictly safer.
      Residual write path fixed: runs_on_resolution's name-keyed group
      MATCH could edge a job with node_id='ControlMHostGroup' to the
      exemplar (cypher + both census queries guarded). Enforcement:
      test_schema_meta_exclusion.py audits ALL specs against labels parsed
      from the committed meta-graph (regeneration re-audits automatically);
      test_meta_graph_exclusion.py (integration, Docker-gated) proves it
      live — meta-graph applied to a real EE container: every spec returns
      ZERO rows, no guard satisfied, the bait job edges nowhere. Note for
      Windows runs: TESTCONTAINERS_RYUK_DISABLED=true needed (Reaper port
      mapping fails on this Docker Desktop).

  - id: O34
    epic: web-console
    title: "web/ dependency hygiene: clear the 3 high-severity npm advisories (bundle code-splitting explicitly NOT included — design call)"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [web/package.json, web/package-lock.json]
    acceptance: >
      `npm audit` in web/ reports ZERO high-severity advisories; `npm run
      build` passes; the lockfile diff is reviewed and any major-version bump
      is named in the commit body (audit-fix moves versions — that is the
      verification pass the fix was deferred for). EXPLICITLY OUT OF SCOPE:
      the 1,485 kB main bundle (390 kB gzipped, past Vite's 500 kB warning) —
      code-splitting is a design call for the UI workstream, recorded here so
      it is not lost, not smuggled into a dependency chore.
    notes: >
      Groomed 2026-07-28 from the 2026-07-27 inbox (found while removing the
      never-committed check/no-op junk deps; sibling of J1 unused-deps). The
      advisories predate that cleanup. If `npm audit fix` cannot clear a
      high-severity advisory without a breaking major bump, say so and stop —
      that escalates to the UI workstream, not a forced upgrade.
      CLOSED 2026-07-28 via the stop clause: postcss advisory cleared
      (8.5.16 -> 8.5.23; nanoid 3.3.15 -> 3.3.16 rode along — patches only,
      no major moved), `npm run build` passes (bundle unchanged, still the
      out-of-scope 1,487 kB). The react-router advisory
      (GHSA-qwww-vcr4-c8h2) CANNOT clear without the v7 -> v8 major:
      v8 absorbs react-router-dom, whose latest (7.18.1) sits inside the
      vulnerable range, so audit fix is a no-op — escalated to the UI
      workstream via the IDEAS inbox (2026-07-28 chore line; likely moot
      in practice, the console is a Vite SPA with no RSC actions, but the
      audit stays red until ruled).

  - id: G41
    epic: component-topology
    title: "AWS Glue base-table inventory seam: stage the dataset POPULATION (per-zone glue db/table placements) onto the GUID-keyed dpl_dataset DataAssets, path-keyed fallback for GUID-less rows — nodes only, no edges, no graph writes"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: [G17]
    inputs: [drydocs_lineage/extractors/dpl_mac.py, drydocs_lineage/model.py]
    acceptance: >
      A GlueTableInventoryExtractor consumes the three observed sheet variants
      (GUID-bearing inventory / GUID-less consumption inventory / normalized
      appId load sheet) through one header-synonym contract; rows whose
      tableDatasetIdSource is datasetGuid land per-zone glue placements
      (glue_database_/glue_table_/glue_path_<zone>) as PROPERTIES on the SAME
      dpl_dataset DataAsset the G17 MAC seam stages (identity = GUID alone);
      GUID-less rows key on the canonical lowercase db.table path as
      glue_table DataAssets, joining an already-staged GUID placement when
      the path matches; appId lands as a seal property FACT (never an edge);
      a coverage dataclass counts every skip/mismatch by reason; the
      extractor adds ZERO relationships and performs NO graph writes; unit
      tests pin all of it on synthetic fixtures (70001-70099 SEAL block).
    notes: >
      Built + closed 2026-07-28 from the user's three sheet screenshots
      (real sheets are Internal-Confidential; screenshots moved to
      internal-local/, mechanism only in-repo). Load-bearing observation
      captured in the module docstring: a *_RAW_TRUSTED table appears in
      BOTH the RAW__ and TRUSTED__ databases with the SAME dataset GUID —
      one dataset, two zone placements (measures the FILE->CONFORMED ~
      RAW->TRUSTED note); REFINED placements carry their own GUID. The GUID
      is the join key that makes G17's READS_FROM/WRITES_TO candidates land
      on these same nodes — relationships come from the join, never from
      this loader; SEAL attribution edges stay Epic K; path-vs-GUID identity
      unification rides G22 clause f; every graph write stays behind G22.
      databaseName grammar: <ZONE>__<LOB>__<segments>__<name> on double
      underscore; tableName numeric prefix is a cross-check against appId
      only (legacy 1_* family proves it is not always a SEAL).

  # ---- 2026-07-28 pm groom (epic close-out run): Snowflake data-catalog seam
  # (G42-G44, from docs/generic-snowflake-data-catalog-ingestion-plan.md) +
  # fresh 07-28 inbox findings (L18/L19/D8/J17). U5 executed in-run. ----
  - id: G42
    epic: component-topology
    title: "Snowflake data-catalog seam: register the snowflake-data-catalog source + taxonomy-first extractor for the dataset/distribution curated views — staging only, no graph writes"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/generic-snowflake-data-catalog-ingestion-plan.md, drydocs_lineage/extractors/dpl_registry.py, drydocs_lineage/extractors/glue_tables.py, config/source-registry.yaml]
    acceptance: >
      Source snowflake-data-catalog is registered in config/source-registry.yaml
      (Internal-Confidential, kind registry, confirmed: false with its gate as
      the activation condition, locator = DRYDOCS_DATA_ROOT/catalog/ + the plan
      doc as mapping ledger). A SnowflakeCatalogExtractor
      (drydocs_lineage/extractors/snowflake_catalog.py) stages
      CatalogDatasetRecord + CatalogDistributionRecord per plan section 4 phase
      1: three physical shapes (table/file/s3) discriminated, the NOT
      INSTRUMENTED sentinel nulled AND counted, union duplicates
      latest-per-GUID with dupes counted, origin in {dpl, authority2, catalog}
      (never joining foreign ids into DPL GUID space), candidate_asset_urn
      staged as a fact column only; coverage counts every skip by reason;
      synthetic fixtures define the assumed field contract; ZERO graph writes;
      tests/unit/test_data_root.py sweep extended to catalog/ artifacts; unit
      tests green.
    notes: >
      Groomed 2026-07-28 from the same-day [source] inbox line (plan doc =
      docs/generic-snowflake-data-catalog-ingestion-plan.md, from the SME
      screenshot walkthrough; screenshots live in DRYDOCS_DATA_ROOT/catalog/
      screenshots/, out of tree). The fourth registry seam beside G17/G25/G41,
      same taxonomy-first discipline. Real system/view names stay company-side
      per the twin convention — producer side uses the generic names.
      BUILT + CLOSED 2026-07-28: source snowflake-data-catalog registered
      (Internal-Confidential, kind registry, confirmed: false with the
      snowflake-data-catalog gate as activation, locator = catalog_dir() +
      the plan doc as mapping ledger); catalog_dir() added to
      drydocs_core/data_root.py (catalog/ + catalog/screenshots/);
      SnowflakeCatalogExtractor stages CatalogDatasetRecord +
      CatalogDistributionRecord from CSV view exports (header decides the
      view; JSON deferred until a real sample pins its shape). Three shapes
      discriminated by name-bearing key columns, zero/multiple populated ->
      shape "" with shape_unresolved/shape_ambiguous counters; NOT
      INSTRUMENTED nulled by the cell reader AND counted eagerly per row (so
      an occurrence in an unread column still counts); union dupes
      latest-per-GUID on both views (both observed ts formats; unparseable
      counted, loses ties); origin = dpl | authority2 (any non-DPL
      authority, verbatim name kept for the census) | catalog, foreign refs
      never join DPL GUID space; candidate_asset_urn fact-column only,
      schema-less table rows keep the G41 canonical db namespace for the
      G43 join; dpl_refs() helper pre-wires G43. 13 synthetic-fixture tests
      (tmp_path only, app id 70001); test_data_root.py gains catalog_dir
      convention + the *_DATASETS_V/*_DISTRIBUTIONS_V export sweep; the
      extract API is graph-free by construction (no LineageGraph parameter
      exists). Suite 1121 passed / 5 skipped after the matrix re-render
      (the new source entry surfaces in enforcement-matrix.json).
  - id: G43
    epic: component-topology
    title: "Data-catalog cross-check reports: catalog vs DPL-registry GUIDs, distribution URNs vs Glue placements, producing-app ids vs SEAL applications, registration-source + instrumentation census"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done          # done 2026-08-12 (desktop), same session as the claim
    depends_on: [G42]
    inputs: [drydocs_lineage/extractors/dpl_registry.py, drydocs_lineage/extractors/glue_tables.py]
    acceptance: >
      Reports per plan section 4 phase 2, every bucket counted AND listed,
      never silent: (1) catalog DPL-origin dataset GUIDs vs the G25 registry
      (both-sides / catalog-only / registry-only); (2) distribution
      candidate_asset_urn vs G41 glue placements on the canonical lowercase
      db.table path; (3) producedby_app_id vs SEAL BusinessApplication ids —
      an attribution CENSUS, fact only, no edges (Epic K owns attribution);
      (4) registration-source census (dpl/authority2/null) + instrumentation
      coverage (NOT INSTRUMENTED + empty-shape counts). Proven on synthetic
      fixtures in tests; real exports stay in the landing zone.
    notes: >
      The puzzle-pieces deliverable while lineage completes — measured lists
      instead of guesses (the G25 cross-check precedent). No graph writes.
  - id: G44
    epic: component-topology
    title: "Data-catalog gate prompt + proposed ontology entries: the dcat dataset/distribution ruling (one node or two), second-authority unification, attribution-edge family, contact-point fold-in"
    type: task
    module: ontology
    phase: 6
    agent: ontology-mapper
    model: fable
    priority: p2
    status: done          # done 2026-08-12 (desktop), same session as the claim
    depends_on: [G43]
    inputs: [docs/generic-snowflake-data-catalog-ingestion-plan.md, config/gate-prompts/email-dl-contact-point.yaml, drydocs_core/ontology/relationship_vocabulary/]
    acceptance: >
      config/gate-prompts/snowflake-data-catalog.yaml drafts the plan section 5
      rulings as confirmations to be signed, never pre-decided: (a) logical
      dataset + physical distribution = one DataAsset with placement properties
      (G41 pattern) vs Dataset/Distribution nodes with HAS_DISTRIBUTION
      (dcat:distribution); (b) authority2/null-origin unification with DPL
      GUIDs default-NO pending ruling; (c) the producedby attribution edge
      family + precedence vs STG_APP_FACT; (d) contact email folds into the
      email-dl-contact-point gate, no parallel concept; (e) lifecycle/event
      views explicitly deferred to layer 4; (f) publisher org mapping =
      property now. Any taxonomy-ontology-map entries land status: proposed
      only. NOTHING loads before sign-off; the loader item stays ungroomed
      until the gate rules.
    notes: >
      fable per the matrix — the draft frames an ontology boundary (the J14
      precedent: the boundary decision rides in the item). Cross-check numbers
      from G43 are the gate's evidence pack, hence the dependency.
  - id: L18
    epic: doc-infrastructure
    title: "Component-cell comma-split shears refs holding commas/semicolons inside parentheticals — 8 of 56 Component.ref values truncate, corrupting (origin, ref) node identity"
    type: bug
    module: drydocs-load
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs/loaders/doc_traceability.py]
    acceptance: >
      _split_cell no longer shears refs whose commas/semicolons sit inside
      parentheticals (extend it with the same _PAREN_QUALIFIER_RE strip the
      section cells already use, or an equivalent explicitly-recorded authoring
      convention); the 8 known sheared Component.ref values load whole; a
      regression test pins a parenthetical-comma ref; full suite green.
    notes: >
      Groomed 2026-07-28 from the same-day U3-census [bug] inbox line. Sits
      beside L17 in the doc-loader defect family.
      CLOSED 2026-07-28: _split_cell is now depth-0 (paren-aware) for BOTH
      separators (component ',' and test ';'); the parenthetical stays part
      of the ref — stripping would re-key every benign qualifier ref. Census
      reconciliation, measured against today's tree: exactly ONE committed
      shear existed (FR-CMI-007's K2 loader ref -> 2 corrupt values); the
      census's "8 of 56" counted damaged-LOOKING values broadly (balanced
      prose-fragment refs included — an authoring-convention matter, not a
      loader defect). Post-fix probe: zero unbalanced refs across all docs;
      2 regression tests incl. a conformance guard over the real matrices
      (unbalanced parens now fail before any load). Suite 1081 green.
  - id: L19
    epic: doc-infrastructure
    title: "Post-census doc-drift sweep: re-cite pre-squash commit hashes, fix sdlc DEP tables to the post-G2 tree, cite the two fan-in hotspot contracts in the traceability matrix"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2                                     # raised from p3 2026-08-09: second filing, drift worsened (see notes)
    status: todo
    depends_on: []
    inputs: [docs/design/, docs/reviews/sdlc-oracle-ingestion.md, docs/reviews/sdlc-neo4j-schema.md]
    acceptance: >
      (a) The five design docs citing pre-squash hashes (project-tdd,
      project-review, remediation-tdd, web-console-tdd, benchmark-explainer)
      plus the startup-refresh runbook re-cite hashes reachable from main on a
      fresh clone, and the mapping-demo runbook gains a commit citation; (b)
      the sdlc-*.md DEP tables match the post-G2 tree (neo4j_client at its
      drydocs_core/ path; the relationship-vocabulary row is TWO moves behind —
      the G2 re-home AND the 2026-08-06 S5 split into the fragment DIRECTORY
      drydocs_core/ontology/relationship_vocabulary/, so regenerate §DEP from
      the tree rather than patching rows; OQ-NS-3
      closed - APOC is load-bearing via run_script + the J9 container); (c)
      drydocs/loaders/base.py and drydocs_lineage/model.py are each cited by
      at least one traceability component cell; (d) the two statements the S3
      identity cutover made FALSE are corrected on their rev-bump surfaces —
      drydocs-web-console-tdd.md's "grid columns surfaced to the browser are the
      table columns verbatim" (S3 broke verbatim ON PURPOSE: the job-application and
      seal-contact-override grids now SELECT ... AS app_id per that gate's §E1, while
      the committed CSV keeps its own header), and controlm-ingestion-tdd.md's
      `Application.seal_id`, which is stale on BOTH halves — the label became
      :BusinessApplication at ADR 0003 / K4 and the property became app_id at S3.
      drydocs-mapping-store-tdd.md:154-155 is deliberately NOT touched: it describes
      the SQLite manual_mapping / seal_contact_override COLUMNS, which S3 chose not
      to rename. (e) sdlc-neo4j-schema.md's loader-inventory row for
      pat_product_mapping.cypher stops claiming it "MERGE HAS_APPLICATION
      (Product→Application)" — that write was removed by C9 on 2026-07-18 and the
      loader file itself says so in a comment; the row is then restated to the K13
      support reading, so the correction lands once rather than twice. (f) Every
      design-doc reference to the two files the S5 fragment split retired —
      `config/taxonomy-ontology-map.yaml` and
      `drydocs_core/ontology/relationship_vocabulary.yaml` — re-points at the
      fragment DIRECTORY, including the traceability-matrix Component cell that
      names one of them; a design doc may not cite a path that no longer exists.
      Any touched design-doc .md is re-rendered and the stale-render check passes.
    notes: >
      Groomed 2026-07-28: three same-day U3-census [doc] inbox lines
      consolidated into one sweep (citation sweep + sdlc DEP drift + fan-in
      hotspot citation gap) — one pass, one review.
      Clause (d) MERGED 2026-08-02 (weekly groom) from the [chore->L19] inbox line.
      S3 deliberately did not hot-edit either doc: both are rev-bump surfaces and
      web-console-tdd is "Status: DESCRIPTIVE ... as of Rev 1, 2026-07-18, authored
      at commit 807e050" — a snapshot pinned to a commit, so it is not strictly
      wrong. It is still read as the live contract, which is the reason it belongs
      in a sweep rather than in nothing.
      RIDER 2026-08-04 (K8 close, same class as clause (d)): the K8 grain flip
      made the TDD's K2 statements stale on their rev-bump surfaces —
      controlm-ingestion-tdd.md/.html say the K2 loader is "live"
      (`load-seal-attribution`, job-grain WAS_ASSOCIATED_WITH, FR-CMI-007's
      component cell) where the ruled state is now the K8 folder-grain
      `load-folder-attribution` / BELONGS_TO_APPLICATION with the K2 policy
      demoted to the disclosed fallback. The NON-GOVERNED half was edited
      directly at the K8 session (2026-08-04): the status tables in
      docs/controlm-staging-ingestion-flow.md and
      docs/controlm-c3-normalization-status.md now state the folder-grain
      shape — what remains for this item is the GOVERNED
      controlm-ingestion-tdd.md/.html rev-bump surfaces only.
      docs/history/controlm-loader-flow.md is HISTORY — leave it.
      Clause (e) MERGED 2026-08-04 (weekly groom) from the [doc] inbox line K13's close
      note raised. VERIFIED at the groom: sdlc-neo4j-schema.md:553 still carries the
      HAS_APPLICATION claim, while pat_product_mapping.cypher:39 reads "Product→
      HAS_APPLICATION is NO LONGER written here (C9)". The inbox line's own instruction
      is honored — that living doc belongs to the drydocs-sdlc-docs cron stream, so it is
      fixed in a sweep that owns the file rather than piecemeal, which is precisely what
      L19 already is. No new item opened.
      SECOND FILING 2026-08-09 (weekly groom, from Idea-94 + Idea-95a/b — persona
      Run 2's tech-writer pass): the sweep has NOT run and the drift got worse, so
      priority is raised p3 -> p2 and the item is re-stated with Run 2's numbers.
      All five pre-squash cites are unchanged (807e050, ac2ea2e, 97ee81c, 24d6a4b,
      0e036ff); drydocs-startup-refresh-runbook reached Rev 10 — SEVEN rev bumps
      since Run 1 — still citing squash-day a135a6d; drydocs-mapping-store-runbook
      took two bumps on 22d1a39; drydocs-mapping-demo-runbook still has NO commit:
      at all; drydocs-project-tdd was edited 2026-08-06 on a dangling cite. The
      pattern is behavioral — rev bumps happen, cite refreshes do not — which is why
      Idea-94's mechanism half was promoted SEPARATELY as L27 (a guard that fails on
      an unreachable commit:); this item stays the one-time sweep, and L27 is what
      keeps it swept. Clause (c) also gained evidence: drydocs_lineage/model.py's
      fan-in has grown 9 -> 24 and it is the G22-reshape fan-out surface, while
      base.py DID get its cite — the Run-1 line half-landed. Clause (b) is
      aggravated by the S5 split (stated in the clause) and sdlc-neo4j-schema.md is
      still taking additive edits (a C23 note landed 2026-08-03) with no
      verification sweep, which is the argument for regenerating §DEP rather than
      patching it. The docmeta half of Idea-95 (drydocs_docmeta has neither design
      doc nor runbook) is NOT here — it went to V11, where runbook coverage lives.
  - id: D8
    epic: config-loaders
    title: "Bootstrap constraint-count guard: count SHOW CONSTRAINTS against constraints.cypher declarations and refuse loudly on mismatch (the supplement-chain guard pattern)"
    type: task
    module: drydocs-core
    phase: 3
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/neo4j_client.py, drydocs_core/schema/constraints.cypher]
    acceptance: >
      drydocs bootstrap counts applied constraints (SHOW CONSTRAINTS) against
      what constraints.cypher declares and FAILS LOUDLY on a mismatch, exactly
      the guard family _apply_supplement_chain already implements for
      supplements; the success message reports the count, never a bare
      "Constraints applied."; a test proves the mismatch path refuses.
    notes: >
      Groomed 2026-07-28 from the same-day [bug] inbox line: apoc.cypher.runMany
      silently no-ops DDL, so pre-D5 bootstraps printed success while creating
      nothing (initial commit -> D5, reproduced in a throwaway container). The
      post-D5 split is sound (51/51) — this item is the missing structural
      check, the same "succeeds loudly, does nothing" family as G29/G30/Q8/L17.
      CLOSED 2026-07-28: NAMES carry the check, not counts (foreign/provisioning
      constraints never false-fail; a partial landing names its missing).
      declared_constraint_names in drydocs_core/schema/constraints.py (refuses
      anonymous + duplicate declarations), Neo4jClient.constraint_names, guard
      wired into bootstrap before the ontology seed; 7 tests in
      test_bootstrap_guard.py incl. the lands-nothing refusal; live-verified
      on the desktop container: "Constraints applied (51/51 declared present)."
  - id: D9
    epic: config-loaders
    title: "Precedence: express the internal refines chain (vendor <- platform team <- support group) instead of one flat internal tier"
    type: task
    module: config
    phase: 3
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - config/precedence.yaml
      - knowledge/standards/technology/folder-naming-convention.md
    acceptance: >
      A proposal lands for how config/precedence.yaml expresses two internal levels
      (the DAT SRE platform standard refining the BMC baseline, the HLT support-group
      standard refining DAT) so that where the two internal standards disagree —
      folder grammar, position-6 meaning, whether the app code carries the SEAL —
      the file records which wins; the ordering decision itself routes through the
      HITL gate (docs/restructure/03-hitl-sme-flow.md) and precedence.yaml changes
      only when confirmed; the position-6 "frequency = legacy" claim in the
      publishable folder-naming standard is verified against both source standards
      and corrected or annotated per-standard if still wrong.
    notes: >
      Groomed 2026-08-07 from Idea-64 (SME framing: Vendor -> Company/Platform team
      -> Lower support group; both internal levels sit at precedence tier 2 today).
      Fable: conflict-resolution semantics. The refines: frontmatter chain in
      knowledge/standards is the input evidence.
  - id: D10
    epic: config-loaders
    title: "Draft the precedence gate prompt: when the Control-M XML export and the psgmgr replica disagree about an object, which one is the definition source of record?"
    type: task
    module: config
    phase: 3
    agent: main
    model: fable
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - config/precedence.yaml
      - docs/restructure/03-hitl-sme-flow.md
      - docs/port-prompt.md
    acceptance: >
      A gate prompt is drafted (drafting decides nothing; config/precedence.yaml is
      unchanged until sign-off) that puts ONE question to the SME per object class —
      which source wins when the XML export and the Oracle replica disagree — with a
      candidate answer stated for them to accept or reject ("the XML export is the
      definition source of record; the replica remains the runtime/stats feed") rather
      than an open essay question. The prompt names the two riders the ruling must carry:
      a named OWNER and a SUNSET for the dual-definition path (the dual-ingestion rule of
      thumb), and the consequence for T16 — if XML becomes the standing feed, the
      CM_DEF_VJOB_DETAIL retirement note gains a second path.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-43, which says plainly that this is HITL and never
      a groom's call — so the ITEM is the prompt, not the ruling. Guardrail 3 of the
      XML-fed cmd-line resolution work (G46/G47/G48): those builds fill a nullable derived
      column and decide no source-of-truth question, so they are NOT blocked on this.
      Epic D and module config because precedence.yaml is the artifact; sibling of D9,
      which asks the adjacent question about two INTERNAL tiers.
  - id: J17
    epic: release-infrastructure
    title: "gates.json joins the stale-render ritual: a groom that edits backlog item text can no longer silently drift web/src/generated/gates.json"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [scripts/render_gates.py, scripts/render_board.py, knowledge/depgraph-snapshots/snapshot.ps1, CLAUDE.md]
    acceptance: >
      Either render_gates.py is added to snapshot.ps1 AND the CLAUDE.md session
      ritual/stale-render lines name gates.json, or the gates render folds into
      render_board.py so one command refreshes both; after the change, a
      text-only backlog item edit followed by the documented ritual leaves
      tests/unit/test_gates_json.py green with no manual extra step.
    notes: >
      Groomed 2026-07-28 from the same-day [chore] inbox line — the drift bit
      twice that day. Haiku-tier ritual wiring; no semantics.
      CLOSED 2026-07-28: acceptance option 2 — a default-paths render_board.py
      run now also runs render_gates (explicit --backlog/--out runs stay
      board-only, so tests/previews don't rewrite gates.json); snapshot.ps1
      inherits the fold via its existing render_board call; CLAUDE.md
      stale-render line names gates.json. Verified: text-edited backlog +
      one render -> gates.json in sync, test_gates_json green.
      DONE 2026-08-12 (desktop). drydocs_lineage/extractors/catalog_crosscheck.py — four
      READ-ONLY reports, registered in extractors/__init__.py. No graph writes, no edges:
      attribution is a census and Epic K still owns what it means.
      THE SHAPE EVERY REPORT SHARES, and the part worth reading: a row that cannot
      participate in the join is held OUT of the set arithmetic rather than folded into
      either side. A DPL-origin dataset with no registration_source_ref, a distribution
      with no URN, an unparseable URN — each gets its OWN counted list. Folding them into
      catalog_only would report gaps that do not exist; dropping them would hide real
      defects. That is the G25 counted-AND-listed rule extended one step.
      The DPL join key comes from CatalogExtract.dpl_refs(), which G42 published for
      exactly this consumer, rather than being re-derived here -- the rule (the catalog
      guid is a CATALOG id; the DPL guid lives in registration_source_ref) is not obvious
      and must have one home, and joining on the wrong one returns a clean-looking zero
      overlap.
      ONE DEFECT FOUND IN THIS ITEM'S OWN WORK BY THE J26 DISCIPLINE, worth carrying: the
      URN parser split from the RIGHT, and the module comment justified it as protecting a
      dotted db.schema namespace. That reasoning was WRONG -- a dot is not the separator,
      so left- and right-indexing agree on any well-formed URN, and the injected
      left-indexed split left the suite green. The real case is a COLON inside
      platform/namespace, where BOTH readings are wrong (last-two drops db, fields-4-5
      drops the table) and neither is detectably so. Now refused as ambiguous and bucketed
      as unparsed. The test that was supposed to cover it could not tell the two
      implementations apart -- a guard for a property nobody had tested.
      23 tests on synthetic fixtures (no Neo4j, no network, re-runs anywhere per J18); six
      injected defects each proven to red the suite. Suite 2133 passed / 8 skipped.
      DONE 2026-08-12 (desktop). config/gate-prompts/snowflake-data-catalog.yaml —
      drydocs.gate-prompt.v1, Internal-Public, mechanism-only (<CATALOG>/AUTHORITY2
      placeholders per the plan). Renders OPEN (77 gates, 16 open) because no gate-log
      entry is about it yet, which is the correct state for a drafted, unsigned page.
      Seven sections, 24 confirmations: A one-node-or-two, B cross-authority unification,
      C the attribution family + SEAL precedence, D the contact-point fold-in, E the
      layer-4 deferral, F publisher-as-property, G the standing scope/classification/
      terminus clauses. Every clause is written as a confirmation TO BE SIGNED — where the
      plan carries a default (B default-NO, F property-now) the clause says so and asks the
      SME to confirm or replace it, rather than presenting it as settled.
      NO taxonomy-ontology-map entries were added, and that is the ruling rather than an
      omission: clause A decides one node or two, so HAS_DISTRIBUTION cannot be proposed
      before it. A4 asks explicitly whether that edge enters as status: proposed at this
      gate or only once a loader is groomed. Proposing it now would pre-decide A, which the
      acceptance forbids.
      WHAT G43 BOUGHT THIS PAGE: an evidence pack. Each clause names the report that
      answers it, and clause B4 carries the trap — the unjoinable bucket is NOT evidence,
      because reading a no-ref dataset as a miss inflates the apparent gap the default-NO
      would be weighed against. That connection only exists because G43 measured first.
      The plan's §Phase-3 forward reference to this exact path (line 166) now resolves; it
      had been dangling since the plan was written.
      Suite 2133 passed / 8 skipped; ruff clean; gates.json + board re-rendered.



  # --- 2026-07-28 pm post-UI-merge groom: instrument hardening (U7/U8), the SME
  # --- landing revision + DataLens/DSI continuity (O35-O41, B5), the agent query
  # --- surface (R9), process fixes (J18/J19), doc-loader guard (L20), runbook
  # --- Rev 4 (L21). DL-7 was a merge, executed on-branch (bc61408) pre-groom.
  - id: U7
    epic: self-documentation
    title: "snapshot.ps1 records and probes its own instrument: depgraph {commit, branch, dirty} in the meta header + a capability probe so a regressed scanner cannot write a silent snapshot"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [knowledge/depgraph-snapshots/snapshot.ps1, config/dev-environment.yaml, knowledge/depgraph-snapshots/README.md]
    acceptance: >
      snapshot.ps1 emits a depgraph {commit, branch, dirty} block inside the
      meta header (gathered the way the DryDocs git block already is) AND
      refuses to scan when the checked-out depgraph lacks the capability the
      run needs — a capability probe, not a version string: multi-root
      resolution for a normal scan, --tree support when -Tree is passed. The
      expected instrument revision is recorded in config/dev-environment.yaml.
      A snapshot taken with a regressed scanner can no longer be written
      silently (the 105-edge near-commit of 2026-07-28).
    notes: >
      Groomed 2026-07-28 pm (post-UI-merge pass) from the laptop's
      instrument-unpinned [bug] line (fd2834d). The sibling-repo fork merge
      (depgraph feat/controlm-lineage + feat/p0-vector -> main) stays inboxed
      — different repo, user's call. Pairs with U8.
      LANDED 2026-07-28 (laptop): probe_instrument.py reports capabilities as JSON
      (behavioural, injectable importer, 16 unit tests incl. a live check that FAILS
      when this machine's instrument has regressed); snapshot.ps1 refuses with exit 1
      and writes nothing — proven against the real -Tree gap on feat/controlm-lineage;
      meta.depgraph records commit/branch/dirty/version/capabilities. The fork merge
      stays inboxed (different repo, user's call).
  - id: U8
    epic: self-documentation
    title: "Committed depgraph snapshots carry no machine-absolute paths — strip/relativize abs_path so cross-machine and worktree snapshots stop producing 100%-false node diffs"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [knowledge/depgraph-snapshots/snapshot.ps1, drydocs/loaders/code_snapshot.py, knowledge/depgraph-snapshots/README.md]
    acceptance: >
      Committed snapshot JSON carries no machine-absolute paths — snapshot.ps1
      post-processes abs_path out (or relativizes to the repo root) the same
      way it already injects the meta header; two snapshots of identical code
      taken from different checkout locations differ only in their meta block;
      the G33 code_snapshot loader stays green (it already drops abs_path at
      load); the README documents the change as an instrument-change boundary
      marker so the diff jump is not read as growth.
    notes: >
      Groomed 2026-07-28 pm from the abs_path [bug] line — twice
      ritual-blocking that day (laptop cross-machine diff; P5 worktree
      deferral). Closing this unblocks session-end snapshots from worktrees.
      LANDED 2026-07-28 (laptop): abs_path stripped textually before write (formatting
      preserved, exactly like the meta injection), with a loud refusal if any survive
      and a JSON-validity check. Proven cross-machine — the laptop snapshot and the
      desktop's committed drydocs-20260728.json now differ by exactly ONE node (the
      test file genuinely added) where the same pair would have differed by all 209.
  - id: O35
    epic: web-console
    title: "WF-LND landing declutter per SME feedback FB-01/FB-02: HeroArt demoted to a mark, one product-name render, h1 = value prop, navigation = modules + business-tower pick-lists"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [UI-WIP/wireframes/, UI-WIP/sme-ui-launch-guide.md, web/src/modules/registry.ts, web/src/]
    acceptance: >
      The landing implements the WF-LND wireframes (UI-WIP/wireframes/, element
      keys in out/KEYS.md): HeroArt demoted to a small decorative mark, the
      product name renders exactly once (nav wordmark), the h1 carries the
      value proposition, and navigation is two explicit pick-lists — modules
      from modules/registry.ts and business towers scoping Explorer/Lineage.
      Element keys still map per the L5/L6 feedback re-attachment idiom; both
      themes screenshot-checked; npm build + lint green.
    notes: >
      Groomed 2026-07-28 pm from the SME FB-01/FB-02 [idea] line — direct SME
      feedback (hub too busy, name unreadable), wireframes + key legend
      shipped on the branch same day. p2 because it is customer feedback.
  - id: O36
    epic: web-console
    title: "Loads timeline rail dot clips the first character of loader names — padding fix in LoadsTimeline"
    type: bug
    module: drydocs-web
    phase: 12
    agent: main
    model: haiku
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [web/src/loads/LoadsTimeline.tsx]
    acceptance: >
      Loader names in the loads timeline render their first character in both
      themes (the rail dot no longer overlaps the label); screenshot check
      attached; build green.
    notes: >
      Groomed 2026-07-28 pm from the branch-screenshot [bug] line
      ("eal_attribution" / "ntrolm_folders"); pre-existing dot-and-rail
      layout, not from the DL quick wins.
  - id: O37
    epic: web-console
    title: "DL-5 radius tokens + soften pass: --radius-sm/--radius-md, cards to md, no pill buttons or gradient headers"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [web/src/styles/tokens.css, web/src/index.css, web/src/App.css, internal/datalens-reference/continuity.md]
    acceptance: >
      --radius-sm/--radius-md tokens exist and every hard-coded border-radius
      in web/src resolves through them; cards/panels lift to --radius-md while
      inputs/small controls stay --radius-sm; before/after screenshots in both
      themes attached to the review; pill buttons and gradient headers
      explicitly NOT adopted (the continuity doc's ruled-out list); build +
      lint green.
    notes: >
      Groomed 2026-07-28 pm from continuity.md DL-5. Coordinate with O30 —
      both touch every surface's chrome; whichever lands second re-verifies
      the other's surfaces.
  - id: O38
    epic: web-console
    title: "DL-6 entity-rendering convention: identifiers as mono IdChips colored by the shared status vocabulary; medallion stage names ride the same convention (DL-11a)"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [web/src/, internal/datalens-reference/continuity.md, UI-WIP/site-plan.md]
    acceptance: >
      A documented identifier-rendering convention (site-plan or a UI-WIP
      note) plus an IdChip component: Control-M/product identifiers render in
      Plex Mono inside a subtle chip colored by the shared status vocabulary;
      adopted on the node inspector and at least one graph/list surface with
      no bespoke identifier styling left on the touched surfaces; medallion
      stage names (RAW/TRUSTED/REFINED/SNOWFLAKE — DL-11a) render as flat
      mono badges through the same convention; both themes verified; build +
      lint green.
    notes: >
      Groomed 2026-07-28 pm from continuity.md DL-6 + the DSI addendum's
      DL-11(a) fold. DL-3 (its dependency) shipped on the branch, so this is
      dependency-free. The graph-model half of DL-11 is B5.
  - id: O39
    epic: web-console
    title: "DL-8 cross-tool deep-link seam, mechanism only: config/env-driven runtime-view URL-template slot on entity surfaces; company binding stays company-side"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: [O38]
    inputs: [web/src/, UI-WIP/two-track-ui-plan.md, internal/datalens-reference/continuity.md]
    acceptance: >
      An optional config/env-driven "runtime view" URL-template slot renders
      an external-link affordance on entity surfaces (inspector + IdChips)
      when set and renders nothing when unset; the template is documented in
      .env.example; no company URL or hostname appears anywhere in the repo;
      build + lint green. Company-side binding is a DD-series item in the
      company tracker, never here (the two-track seam rule).
    notes: >
      Groomed 2026-07-28 pm from continuity.md DL-8. Depends on O38 (IdChips
      are the link surface).
  - id: O40
    epic: web-console
    title: "DL-10 StatTiles click-to-filter: optional onSelect + active tile scopes the adjacent list (DSI's KPI-card→filter idiom)"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [web/src/components/StatTiles.tsx, web/src/loads/, internal/dsi-reference/]
    acceptance: >
      StatTiles accepts an optional onSelect + active-tile id; the active
      state is token-only and keyboard-operable (aria-pressed, focus-visible);
      adopted on one live surface where tiles filter the content below (first
      candidate: loads timeline, tile = status filters runs); both themes
      verified; build + lint green.
    notes: >
      Groomed 2026-07-28 pm from the DSI addendum DL-10.
  - id: O41
    epic: web-console
    title: "DL-12 one status-vocabulary → StatusChip token mapping, documented and conformed (Ready/Failed/Processing/Pending/Not Scheduled)"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: haiku
    priority: p3
    status: done          # DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
    depends_on: []
    inputs: [web/src/components/ui/StatusChip.tsx, UI-WIP/site-plan.md, internal/datalens-reference/continuity.md]
    acceptance: >
      One ecosystem-status → StatusChip token mapping table is documented
      (site-plan or a UI-WIP note): Ready→green, Failed→status-fail-soft,
      Processing→teal, Pending→yellow, Not Scheduled→muted; StatusChip call
      sites conform and no surface invents a divergent status→token pairing
      (grep-checked).
    notes: >
      Groomed 2026-07-28 pm from the DSI addendum DL-12.

  - id: O42
    epic: web-console
    title: "The 62 first-party React components are in NO machine-readable inventory — extend the code graph to the web/ tree, or declare it out of scope on the record"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [web/src/, MODULE_MAP.md, knowledge/depgraph-snapshots/README.md, config/dev-environment.yaml]
    acceptance: >
      Either (a) the code-graph snapshot covers the web/ tree so the .tsx
      modules and their import edges appear alongside the Python ones — with
      MODULE_MAP.md gaining a drydocs-web section and the snapshot README
      recording the widened scan — or (b) a recorded decision that first-party
      front-end code stays out of the code graph, with the REASON and the
      inventory that serves instead. Either way the outcome is written down;
      what is not acceptable is the current silent gap. If (a), the depgraph
      instrument needs a TS/JS resolver, so size that before committing —
      the U6 multi-root work is the precedent for what "extend the scanner"
      actually costs.
    notes: >
      Found 2026-07-31 while answering "which React components are part of the
      UI": the answer had to be assembled with `find`, because nothing
      inventories them. Verified, not assumed —
      (1) the latest snapshot's scan list is Python roots only: drydocs,
          drydocs_core, drydocs_api, drydocs_remediation, drydocs_lineage,
          drydocs_deepdoc, tests;
      (2) MODULE_MAP.md has no web/tsx entry, and the modules registry says so
          explicitly — drydocs-web is "not a Python package, so absent from
          MODULE_MAP.md/test_module_boundary.py";
      (3) the software registry is the THIRD-PARTY ledger, so first-party
          components correctly do not belong there (that file's own scope note:
          the catalog long tail stays out).
      So all three plausible homes decline it, each for a defensible reason, and
      the result is that a 62-component surface — the whole of Epic O — has no
      inventory any tool can read. That is exactly the class of defect Epic U
      was created to find in the Python tree; the front end simply was never in
      the scan.
      Option (b) is a legitimate answer, not a cop-out: the front end may not
      earn a graph. But it must be a decision rather than an omission.
      PARTIALLY ANSWERED 2026-07-31, same session (user directive: "create
      another sub-ledger for only our UI that links from the first"):
      config/taxonomy/ui-components.yaml now inventories all 62 components by
      area, joined to the software registry's `react` product through a
      RECIPROCAL pointer (ledger -> framework/stack, product -> components), the
      same shape as `documentation:` on the controlm product. A bidirectional
      drift guard (tests/unit/test_ui_components.py) fails when a component is
      added without being registered OR when an entry names a missing file —
      PROVEN by adding a probe component and watching the suite go red. The
      inventory half of this item is therefore CLOSED.
      WHAT REMAINS, and why this item stays open: the ledger records WHAT EXISTS
      AND WHERE, not what imports what. There are still no import edges for the
      front end, so questions the Python code graph answers routinely — blast
      radius, orphan detection, coupling — remain unanswerable for web/. That
      needs a real TS resolver in the depgraph instrument; size it against the
      U6 multi-root work before committing.
      Worth recording as evidence for why the guard was built: the ledger was
      generated from a `find` whose output truncated at 60 of 62 files, so the
      first hand-read of the tree silently missed ThinkingLogicPanel and
      TokenMemoryChart. A machine-checked inventory catches exactly that.
    progress: 'DONE 2026-08-06 (desktop): option (a) built whole. The depgraph instrument gained
      a ts-imports extractor (sibling commit a56d2fc, default-on; 4 new tests, 44 pass there):
      first-party .ts/.tsx/.js/.jsx edges, relative-specifier bundler-mode resolution (exact,
      extension candidates, index files, the ./x.js-means-x.ts idiom), bare specifiers stay
      third-party, asset imports drop, line-anchored regex so commented-out imports are not
      edges; no tsconfig-paths support by design (web/ declares no alias) - add when a consumer
      carries one. Sizing verdict the item asked for: ~130 lines + tests, one session, far
      under the U6 resolver rework. DryDocs side: probe_instrument gained the ts_imports
      behavioural capability (default_extractors membership - what scan() actually consults),
      snapshot.ps1 meta carries it, dev-environment pin bumped 773fb1e -> a56d2fc, snapshots
      README gained the fourth instrument-change marker (549 -> 780 edges is the front end
      becoming visible, not growth), MODULE_MAP.md gained the Non-Python surfaces section
      (drydocs-web + drydocs-agents), u1_imports note WIDENED (same bare-pair meaning, TS
      import-type caveat as the D2 analog). Verified live (desktop, neo4jtest, drydocs DB):
      snapshot drydocs-20260806-1659 caps {multi_root,tree,ts_imports} all true, 226 TS edges
      in the committed artifact AND in the graph (total IMPORTS 549 -> 780); first real
      finding: web/src/components/HeroArt.tsx is an orphan (only comments cite it; the
      OverviewRoute comment says it was demoted) - inboxed to IDEAS. Suite 1636/5.'
  - id: O43
    epic: web-console
    title: "Seed the remaining 9 UI test suites from verified behaviour, and decide whether a UI test runner is worth introducing"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs: [config/taxonomy/ui-tests.yaml, config/taxonomy/ui-components.yaml, web/package.json]
    acceptance: >
      (a) Each of the 9 declared-but-empty suites (explorer, lineage, ownership,
      runbooks, remediation, docs, gates, ask, underhood) gains at least one
      case, and every case cites a `source` — a close note, screenshot check or
      SME observation recording behaviour someone actually watched work. A case
      with no source is not acceptable: that is how a checklist becomes fiction.
      Seed a suite when its module is next worked on rather than in one sitting.
      (b) A recorded decision on whether the console gets an automated runner
      (Vitest/Playwright) or stays manual. If automated, cases gain
      `automated_by` and `execution` moves per case, never per file; if manual,
      the reason is written down. The coverage pin in
      tests/unit/test_ui_tests_ledger.py updates with each seeding.
    notes: >
      User directive 2026-07-31: the Process/Testing domains of the uploaded
      Miller process model were admitted for INTERNAL use — "if this framework
      works internally use it… it may help me when I'm testing the UI" — with
      the standing reason that the source projects lack good test cases, which
      is why Jira is not yet a feed.
      BUILT ALREADY (same session): config/taxonomy/ui-tests.yaml with the
      TestSuite -> Feature join realized as suite.module ->
      web/src/modules/registry.ts, 2 of 11 suites seeded from verified close
      notes (O36/O35/O37 shell, O40/O41 loads), and a guard that exercises the
      real payoff — component -> module -> suite -> cases — rather than only
      checking the file parses.
      KNOWN LIMITATION, pinned by a test rather than left to be discovered: the
      36 unbound components (shell, ui primitives, shared) resolve to NO cases.
      StatTiles is the sharp example — TC-LOADS-01 exercises it, but it lives in
      the shared folder so no evidence-based module binding exists, and the
      lookup returns empty. Closing that needs import edges, i.e. O42's TS
      resolver. Until then "no cases" means "unknown", not "none".
      The Process half of the model (CheckIn/Release -> affected features) is
      deliberately NOT adopted yet: its value depends on that same complete
      component->module mapping, so it would answer confidently and wrongly.

  # ── appended at the 2026-08-05 groom (SME direction, in-chat during G35) ────
  - id: O44
    epic: web-console
    title: "Ownership reconciliation ALLUVIAL diagram (a Sankey variant): four ordered columns — employee hierarchy | SEAL/TOM contacts | ServiceNow queues + technicians | PAT structure — with ribbons where an entity appears in both, and a terminating ribbon where it does not"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [G35]
    inputs: [web/src/routes/OwnershipRoute.tsx, web/src/ownership/OwnershipGraphPane.tsx, web/src/routes/AppCodeCascadePane.tsx, drydocs_api/query_specs.py, config/gate-prompts/tom-roles-enumeration-and-cardinality.yaml, config/source-registry.yaml]
    acceptance: >
      THE DIAGRAM TYPE IS NAMED, and it is not decoration: this is an ALLUVIAL
      diagram — the Sankey family variant for CATEGORICAL data across several
      ordered dimensions, where the same entities are re-partitioned column by
      column and ribbons carry membership from one partition to the next. (SME
      2026-08-05, correcting this item's first draft, which described the shape
      and never named it.) Sankey is the family; alluvial is the member, and the
      distinction decides the data contract — a general Sankey moves a QUANTITY
      between distinct nodes, an alluvial moves the SAME ENTITIES between
      categorizations. Build to the alluvial contract: nodes = (column, category),
      links = (source category, target category, the entity set that is in both).
      A console pane rendering FOUR ordered columns over the same applications and
      people, each column a different system's view of who owns what, with a
      ribbon drawn ONLY where the entity is actually present in both adjacent
      columns' lists (the SME framing).
      THE TERMINATING RIBBON IS THE PRODUCT. An entity that appears in one column
      and not the next does not fade out quietly — a flow that stops IS the
      finding, and it must be visually distinct from a flow that continues. That
      is what makes this the reconciliation surface for G35 §D: a SEAL contact with
      no ServiceNow counterpart is a roster disagreement made legible, and a
      diagram that only draws matches would hide precisely the case it exists for. (1) EMPLOYEE HIERARCHY, filterable by manager.
      (2) SEAL/TOM CONTACTS, filterable by SEAL id, LOB, product line, product and
      contact role. (3) SERVICENOW: the queues/assignment groups matching the
      applications in column 1's list, showing their technician roles.
      (4) PAT STRUCTURE with its SEALs.
      SHIP IN PHASES, and the phase boundary is a DATA boundary, not a UI one —
      columns 2 and 4 are buildable today, columns 1 and 3 are not (see notes).
      Phase 1 acceptance is columns 2+4 with the cross-column relationship drawn
      and the two missing columns shown as EXPLICITLY EMPTY WITH A REASON, never
      omitted: a four-column pane silently rendering two columns is the failure
      this acceptance exists to prevent. Every column names its source system and
      the freshness of its load. Full suite green.
    notes: >
      SME direction 2026-08-05, in-chat during the G35 gate draft. This is the
      surface where G35 §D's roster disagreement becomes VISIBLE rather than
      inferred — §D3 proposes recording which surface an attribution came from,
      and this pane is what consumes it, which is why it depends_on G35 rather
      than merely citing it.
      DATA STATE, measured 2026-08-05 (this is the whole reason it ships in
      phases):
      * COLUMN 2 (SEAL/TOM) — EXISTS. Attribution/TOMRole/Employee load today, and
        every requested filter is backed by a real node: CatalogLOB, ProductLine,
        Product, BusinessApplication, and the contact role itself. The K11 steward
        cascade already implements Product Line -> Product -> Business Application,
        so the filter chain is a reuse, not a build.
      * COLUMN 4 (PAT) — EXISTS. Product/AreaProduct/DevTeam load via
        pat_product_mapping.cypher + area_products.cypher, and pat:people-report
        carries team-scoped seal_ids. Caveat recorded at C17: the people report
        projects Sponsoring Product Line as a NAME ONLY, so that grain stays
        unmodelled and the column cannot filter on it.
      * COLUMN 1 (EMPLOYEE HIERARCHY) — DOES NOT EXIST. :Employee is a node class
        (prov:Agent) with NO Employee-to-Employee edge anywhere in the relationship
        vocabulary: no REPORTS_TO, no manager edge, no source feeding one, and no
        backlog item that would create one. The manager filter is the column's
        whole point and there is nothing to filter on. G35 §B7 defers hierarchy
        placement to "a later pass" on SME direction; this item is the first
        consumer that makes the missing owner concrete.
      * COLUMN 3 (SERVICENOW) — NOT INGESTED. snow:cmdb-ci-classes is registered
        (Q4 ruling, 2026-07-31) but confirmed: false, adapter: ~, and it is a
        header-only capture of CMDB CLASSES — not queues, not assignment groups,
        not people. The "SEAL technician roles" the SME describes appear in the
        repo only as prose: the runbook skill's template-spec cites AO/RE Snow
        queues with a Technician role, and internal/remediation names a SNOW
        technician group + CTASK. That is enough to know the concept is real and
        not enough to build a column.
      RIBBON WEIGHT IS AN OPEN DESIGN QUESTION, flagged rather than assumed: in a
      Sankey the ribbon THICKNESS encodes a quantity, and this diagram has more
      than one candidate — count of people, count of applications, or unweighted
      equal-width membership bands. Pick before building; retrofitting a weight
      changes the layout engine's input, not just a style.
      NO LIBRARY IN THE STACK DOES THIS. web/package.json carries @xyflow/react
      (node-and-edge graphs, not flows) and d3-force (force layout). An alluvial
      needs d3-sankey or a hand-rolled SVG with a crossing-minimisation pass —
      readable column ordering is most of the work in this diagram, and it is the
      part a naive implementation gets wrong. A new dependency also lands in the
      software registry, which is the third-party ledger, so it is a registration
      as well as an install.
      NOT SPLIT INTO SEPARATE ITEMS on purpose: the two missing columns are
      SOURCE questions (does DryDocs ingest the HR hierarchy and the SNOW queue
      export, producer-side or company-side?) and that is a user decision, not an
      agent's. Both are inboxed against this item rather than groomed into
      speculative work. If the answer is company-side for either, this item's
      phase 1 IS its final scope and the acceptance says so.
  # ── appended at the 2026-08-06 groom (Idea-78 — the SME Context-Intake page plan,
  #    UI-WIP/sme-intake-page-plan.md; user direction in-session. The console surface
  #    for the Q10 unstructured email corpus: seven page sections, NO graph writes
  #    anywhere — corpus load stays Q10's (blocked G32→G31), the assignment edge is
  #    gate-registered planned per Q10's own acceptance.) ──────────────────────
  - id: O45
    epic: web-console
    title: "Context-type vocabulary: config/taxonomy/context-types.yaml + guard + generated artifact for the intake dropdown"
    type: task
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [UI-WIP/sme-intake-page-plan.md, config/taxonomy/, tests/unit/]
    acceptance: >
      config/taxonomy/context-types.yaml exists (schema drydocs.context-types.v1)
      seeded with job-failure, missed-data-load, missed-file, data-issue — each
      entry id + label + description + status (active|retired; values are never
      deleted, only retired). A unit guard (tests/unit/test_context_types.py)
      pins the schema, id uniqueness, and the status enum. The list is served to
      the console as a generated artifact with a drift test (the gates.json
      pattern) — the page never hardcodes the vocabulary. Pure classification:
      adding a value is a config edit + SME nod, never a gate.
    notes: >
      Slice 1 of the intake plan (UI-WIP/sme-intake-page-plan.md §2). Filed
      phase 1 / module taxonomy because it IS a layer-1 capture (the consumer
      being a console dropdown does not change the layer); the epic stays
      web-console so the intake slices read as one group. The dropdown carries
      an Other/not-listed option whose free text lands in the intake note —
      vocabulary-growth signal for the admin queue, not a hidden new value.
      .
      CLOSED 2026-08-06 (desktop, a4108fc): built exactly as accepted —
      context-types.yaml (v1, 4 active seeds), scripts/render_context_types.py
      emits web/src/generated/context-types.json riding the default
      render_board.py run, tests/unit/test_context_types.py = 7 guards (schema
      pin, field completeness, id uniqueness, status enum, seed-value
      permanence — retire never delete, drift, active-only dropdown list).
      Unit suite 1643 passed at the close.
  - id: O46
    epic: web-console
    title: "Intake API + origin-flagged intake store: multipart upload to the data root, digests, classification stamp, draft records"
    type: task
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [UI-WIP/sme-intake-page-plan.md, drydocs_api/, drydocs_core/mapping_store.py, config/classification.yaml]
    acceptance: >
      A drydocs_api multipart endpoint streams .msg/.json/.txt evidence to
      DRYDOCS_DATA_ROOT/context-intake/<intake_id>/ — never the repo tree; the
      .msg/.json Copilot pair auto-links by basename; every file gets a sha256
      at upload and a re-upload of changed bytes re-queues review rather than
      silently replacing. Every intake record is stamped classification:
      Internal at creation (no unlabeled default). Records land in an
      origin-flagged store (origin: sme-intake, the O24 shape) with the draft →
      … → admin-accepted status machine from the plan; parse-preview
      (.msg subject/sent_at/from best-effort, .json keys) is display-only and a
      parse failure is a warning, not a rejection. THREAD REUSE → INGEST THE
      DIFF (user direction 2026-08-06): ingest detects thread identity
      (conversation headers where present, normalized subject, quoted-content
      overlap against stored evidence) and, on a match, links the new intake to
      the prior one(s) and computes the DELTA — the new content above the
      quoted tail — as the review payload; the original file still lands WHOLE
      (evidence is never edited; a trimmed copy of evidence is no longer
      evidence). Detection flags, never auto-decides: the SME's add-value
      ruling is O47's surface. The API returns a legal-transitions map per
      record so the UI never encodes the status machine a second time. NO
      graph write anywhere; unit-tested without Neo4j.
    notes: >
      Slice 2 (plan §3 + §7's store half). The corpus REGISTRY entry is
      deliberately NOT this item: extend-vs-add against adhoc-sme-email is
      Q10's first decision, pre-analyzed in Q10's notes. .txt evidence class is
      an open question in the plan — accept the file, tag kind: note, revisit.
      STORAGE RULED 2026-08-06 (plan "File storage" section): local data root
      for dev, company-side backed-up Linux share is the deployment plan of
      record, object storage a later candidate — so storage stays behind ONE
      configured base path + the digest identity; records carry the relative
      key only, no filesystem assumption above the seam (local -> share ->
      object store must be a config change, never a code change). Demo/test
      fixtures: SYNTHETIC evidence in tests/fixtures/intake/ referencing only
      reserved-block ids + sample names — one corpus for tests and demos (the
      plan's Demo script section).
      .
      CLOSED 2026-08-06 (desktop, fd29f3a): drydocs_api/intake.py (store +
      framework-free handlers) + thin FastAPI shell + context_intake_dir
      landing zone. Every acceptance clause landed: origin/classification
      stamps, sha256 + relative-key seam, whole-file rule, pair auto-link,
      changed-bytes re-queue, display-only preview (warning never rejection),
      thread-delta detection (headers/subject/overlap, flags never decides,
      SME decision surfaced via thread-decision), server-owned status machine
      with per-role legal-transitions map, admin-accepted parked (loaded
      unreachable), zero graph writes (pinned). 31 guards in
      tests/unit/test_intake_api.py; the ADR 0009 write-primitive guard in
      test_mapping_api.py carries the argued evidence-landing exemption
      (write_bytes in intake.py only). Synthetic fixtures inline in the test
      (reserved-block SEAL 70002 + sample job names); a shared
      tests/fixtures/intake/ corpus lands with the O47/O50 demo work.
      Full suite 1674 passed; TestClient multipart smoke green.
  - id: O47
    epic: web-console
    title: "Intake page shell: /intake route, sme persona, area cascade (PAT + SEAL) and context-type + upload sections"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-10 (desktop, 04:07 session). /intake renders in the Shell behind canAccessIntake (sme persona via ?as=sme, or steward/admin — SME is a PERSONA, not a fourth role tier; API personas.py mirrors it, roster-drift guard green). Section 1: cascade from intake.area-tree.v1 (NOTE: acceptance spelled area_tree; the registry id convention is kebab-case, enforced at import, and wins) with Unknown first-class at every level and the area-product level honestly empty until the extract lands; SEAL search from explorer.applications.v1, back-fill from catalog-cascade rows, conflicts FLAG never block. Section 2: context types from the O45 generated artifact + Other/not-listed free text into the note. Section 3: drag-drop multipart upload to the O46 endpoint (new intakeApi.ts — authedPost pins JSON so upload builds its own fetch on the shared bearer), pair_key grouping = one row two format chips, thread continuation with inline delta diff + Adds-value/No-new-value + prior decisions fetched per thread_of id. IntakeStepper adapted from LoadsTimeline (ordered stages, ui-conventions tokens, actions from the API legal-transitions map — no client workflow). Sections 4-8 disabled placeholders naming O48/O49/O49/O50/O51. Ledger route 21 + component 16, coverage pin 68->70 unbound (intake is not a registry module — deep-link page, like mappings). Build + lint green; suite 1836/5. NOTE the towerKey/towerId naming: the roster field is towerKey; the plan says towerId; field name wins, comment at the persona.
    depends_on: [O45, O46]
    inputs: [UI-WIP/sme-intake-page-plan.md, web/src/routes/, web/src/lib/auth.ts, drydocs_api/query_specs.py, UI-WIP/ui-conventions.md]
    acceptance: >
      Route /intake renders in the Shell; the mock-auth roster gains an sme
      persona (?as=sme) whose towerId scopes the area default, and steward/admin
      can open the page. Sections 1-3 work end to end against O45/O46: cascading
      Product Line → Product → Area Product from a new intake.area_tree.v1
      QuerySpec (O33-guarded), searchable SEAL select that back-fills PAT levels
      where the mapping exists and FLAGS (never blocks) a conflict, and
      "unknown" selectable at every level; context-type dropdown from the O45
      generated artifact; drag-drop upload wired to the O46 endpoint with the
      paired-evidence row display. When O46 flags a thread continuation, the
      upload row shows "continues the thread of intake <id>" with an INLINE
      DIFF (new content highlighted against the prior evidence) and the SME
      decides: Adds-value (the delta proceeds as this intake's content) or
      No-new-value (linkage + decision recorded, intake STOPS — nothing
      proposed; a third bounce of the same thread shows both prior decisions).
      The status machine renders through an IntakeStepper adapted from
      LoadsTimeline (StatusChip tokens; action buttons from the API's
      legal-transitions map — no client-side workflow library, decided
      2026-08-06). Identifiers render as IdChip, statuses per the
      ui-conventions token map. Sections 4-7 render as disabled
      placeholders naming their slice — never silently absent.
    notes: >
      Slice 3 (plan §1-§3). The area selector is a HINT channel by design —
      Q10 rules that unattributable email lands unassigned, never guessed; the
      open question "can the hint override an extractable assignment" is
      answered hint-only unless the SME rules otherwise at O49.
  - id: O48
    epic: web-console
    title: "Review-for-ontology pass: extraction over uploaded evidence → proposed-bindings panel the SME confirms (the FCDO-style review)"
    type: requirement
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [O47]
    inputs: [UI-WIP/sme-intake-page-plan.md, drydocs_api/, drydocs_core/ontology/relationship_vocabulary/, config/taxonomy/context-types.yaml]
    acceptance: >
      One button runs a server-side extraction pass over the intake's evidence
      and returns proposed bindings: recognized entities (Control-M folder/job
      names, dataset identifiers, SEAL ids, file paths, DL addresses) each with
      the ontology CLASS it would bind to and a match tier (exact id / name /
      fuzzy), plus the extractor's context-type guess shown against the SME's
      pick (SME wins). Every row is confirm/retype/discard-editable; the output
      is a candidate-binding set stored on the intake record with status:
      proposed per row — a STORE write, never a graph write, and no new
      relationship semantics: classes come from the confirmed vocabulary only.
      What the SME confirmed is exactly what the admin queue later renders
      (governed-surface discipline).
    notes: >
      Slice 4 (plan §4). The interaction is the FCDO tool's review pass:
      machine proposes, human disposes. Whether this panel generalizes into a
      shared component with the gate pages is an open question deferred until
      this first build proves the shape.
  - id: O49
    epic: web-console
    title: "Related-nodes QuerySpec panel + agent first-pass correlation with accept / modify / stay-unassigned"
    type: requirement
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [O48]
    inputs: [UI-WIP/sme-intake-page-plan.md, drydocs_api/query_specs.py, agents/, web/src/components/]
    acceptance: >
      A parameterized read-only QuerySpec (intake.related_nodes.v1, O33-guarded
      — never free-text Cypher from the browser) resolves the confirmed
      entities + area hints against the structured graph and renders candidate
      related nodes with a compact neighborhood preview (the MiniDag reuse
      pattern) and the actual Cypher + parameters visible read-only under a
      disclosure. "Correlate" runs the ADK graph_qa wiring over evidence +
      bindings + related nodes and returns RANKED candidate assignments (email
      → ControlMFolder / process) each with a confidence band and an evidence
      chain; SME action per candidate is Accept / Modify (scoped re-pick) /
      Reject-all (stays unassigned — always legal). Agent unavailable → skip
      chip, manual assignment still works, the page never blocks. The accepted
      assignment is a CANDIDATE on the intake record; the assignment edge
      itself stays gate-registered planned (Q10) and nothing here writes the
      graph.
    notes: >
      Slice 5 (plan §5-§6). Mock-able behind the same interface — R11 (agents
      live smoke) helps but is deliberately not a dependency. Module filed
      drydocs-api (the QuerySpec + correlation endpoint are the deliverable);
      the panel halves ride drydocs-web, recorded here rather than split into
      a second item.
  - id: O50
    epic: web-console
    title: "Admin review queue for intake records: diff view, accept / return with note, and the waiting-on-gate park"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [O46, O47]
    inputs: [UI-WIP/sme-intake-page-plan.md, web/src/routes/AdminConfigRoute.tsx, UI-WIP/ui-conventions.md]
    acceptance: >
      ?as=admin sees the intake queue rail: sme-confirmed records with a
      diff-style display of what the SME confirmed vs what the extractor
      proposed, and two actions — accept (→ admin-accepted) or return with a
      required note (→ admin-returned, back to the SME's drafts). The full
      status machine renders with the ui-conventions StatusChip vocabulary.
      THE LOAD BOUNDARY IS HONEST: admin-accepted records PARK with a
      "waiting on gate" chip naming what they wait on (corpus load = Q10 ←
      G31 ← G32; assignment edge = Q10's HITL gate) — acceptance here never
      triggers a load, and no code path from this surface writes the graph.
    notes: >
      Slice 6 (plan §7). Ends at admin-accepted by design; the load half
      belongs to Q10 and stays there.
  - id: O51
    epic: web-console
    title: "Reviewer-quality signals + admin block: auto-accept / too-fast / admin-return rates over a rolling window, limits flag, admin blocks"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [O46, O50]
    inputs: [UI-WIP/sme-intake-page-plan.md, config/, web/src/routes/AdminConfigRoute.tsx]
    acceptance: >
      Per-SME quality signals derive from intake records the store already
      holds — no new data entry: submissions in a rolling window, auto-accept
      rate (agent candidates accepted with zero modification), too-fast rate
      (confirm faster than a floor scaled to evidence size), and admin-return
      rate. Limits live in config/review-quality.yaml (admin-editable via the
      AdminConfig surface, versioned, unit-guarded). Crossing a limit FLAGS the
      SME on the admin queue's quality rail with the metric that tripped
      (Meter + StatusChip); the ADMIN — never the machine — can block the
      persona from submitting: drafts survive, the block records who/when/why,
      and unblock requires a note. No auto-block exists in any code path. The
      admin-only per-user panel (submissions, auto-accept rate, return rate,
      median review time) uses the StatTiles/Meter patterns. The store keeps
      PER-DECISION detail (accepted/modified fields, durations), not just
      rollups — the future confirmation-weight trust model needs it.
    notes: >
      Slice 7 (plan §8). User requirement 2026-08-06: recalled as a backlog
      item ranking auto-acceptance from a QA-testing session — searched
      backlog/IDEAS/gates/git history, never captured (closest neighbors: R8
      answer-evaluation, FB-04 agent test), so the intake plan §8 + this item
      are now the requirement's home. Measure-then-decide posture throughout:
      thresholds are tripwires, the human is the judge.
  - id: O52
    epic: web-console
    title: "ownership.attributions.v1 returns an always-null holder_sid column (e.sid is never written; every site keys employee_id)"
    type: bug
    module: drydocs-api
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - drydocs_api/query_specs.py
      - drydocs/loaders/cypher/seal_contacts.cypher
    acceptance: >
      The QuerySpec ends employee_id-based (e.employee_id AS holder_sid, matching
      mappings.seal-contact-roles.v1); a regression guard keeps the two specs'
      holder_sid expressions agreeing so the typo class cannot recur silently; the
      K4 attribution review surface's Holder SID column populates on a loaded graph.
    notes: >
      Groomed 2026-08-07 from Idea-71. The larger class (a QuerySpec naming a
      property no loader writes stays green in every unit test) is the J26
      promise-vs-assertion family — recorded here, scoped out; the one-word fix plus
      the pairwise guard is this item.
      DONE 2026-08-09 (desktop): one-word fix + TWO guards. `ownership.attributions.v1` now returns `e.employee_id AS holder_sid`, matching `mappings.seal-contact-roles.v1`. Verified against the loader rather than assumed: `seal_contacts.cypher:31` MERGEs `(:Employee {employee_id: row.employee_sid})`, and a tree-wide grep found NO writer of a `sid` property anywhere in drydocs/loaders/cypher/ or drydocs_core/schema/ — so the column was always null on any loaded graph, exactly as the item said. Root cause named for the record: the SOURCE column is `employee_sid` and the GRAPH property is `employee_id`; the spec author carried the source spelling across the MERGE. Guards: (1) pairwise — both holder_sid-bearing specs must carry the identical expression, so a future edit to one announces itself; (2) registry-wide — no spec may read a `<alias>.sid` property at all, which is the rule rather than today's instance. Both guards were PROVEN to fail on the reintroduced defect before being trusted (the J26 promise-vs-assertion discipline). Suite: test_query_specs 24->26 passed; full unit 1759 passed / 5 skipped with the one standing desktop bootstrap-guard failure (pre-existing, unrelated, reproduces without this change). NOT changed, recorded instead: the ownership spec's OPTIONAL MATCH leaves `(e)` unlabeled where the sibling uses `(e:Employee)` — harmless (HAS_AGENT has one endpoint class) and outside the item's stated one-word scope.
  - id: O53
    epic: web-console
    title: "HeroArt is the code graph's first front-end orphan: remove it or wire it in (default remove)"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: haiku
    priority: p3
    status: done   # closed 2026-08-10 (desktop). SME ruled: adopt the experiment ALPHA (files-track) code set — verified-before-delete on record, fable 6v4 over BETA. Applied on the POST-O47 tree, not verbatim (the diffs were cut pre-O47): HeroArt.tsx deleted, index.css hero-net keyframe+rule replaced with the deletion stub, ui-components row out with component 16->15, and the coverage pin 70->69 WITH its changelog line — the clause BOTH experiment runs missed, fixed at adoption. Orphan claim verified by grep BEFORE the delete (only OverviewRoute's demotion comment) and re-verified after. Build green, lint = the 3 pre-existing warnings, suite 1836/5. The acceptance's depgraph orphan-query check rides the next snapshot: HeroArt leaves the graph as a soft-delete tombstone, and vite.config.ts stays the sole TS orphan.
    depends_on: []
    inputs:
      - web/src/components/HeroArt.tsx
      - web/src/index.css
      - config/taxonomy/ui-components.yaml
    acceptance: >
      web/src/components/HeroArt.tsx is either imported by a live route again or
      DELETED together with its index.css hero-net rule and its ui-components.yaml
      row (coverage pin moved with the history-line convention); the orphan query
      over the TS import edges then returns only vite.config.ts; build + lint green.
    notes: >
      Groomed 2026-08-07 from Idea-77. Default is delete — OverviewRoute's own
      comment demoted the radial hub, only a css comment still cites the file, and
      O30's App.css retirement already recorded its remaining rules as dead.
  - id: O54
    epic: web-console
    title: "The enforcement matrix cannot show a guarded code-resident surface as enforced — and the canonical load sequence is exactly that surface"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - scripts/render_enforcement_matrix.py
      - drydocs/cli.py
      - web/src/generated/enforcement-matrix.json
    acceptance: >
      A surface row for the canonical load sequence (cli.CANONICAL_LOAD_SEQUENCE with
      LOAD_PROFILES and SCHEDULED_INGEST_EXCLUSIONS) is in the generated matrix, and it
      renders `enforced` rather than `unguarded`: today the renderer forces
      status="unguarded" for ANY row flagged code_resident regardless of its guards, which
      is now simply wrong — N3/N6 gave that surface real guard tests. The status logic
      separates "config living in code" (a fact about WHERE it lives) from "nothing guards
      it" (a fact about whether it is tested), the page's KPI wording is updated to match,
      and the render stays deterministic with its drift test green.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-55. The blocker the entry named — "the registry
      needs to accept a module path" — was checked at the groom and is NOT the blocker:
      the renderer only requires the declared path to exist, and drydocs/cli.py does. The
      real blocker is the unconditional code_resident -> unguarded branch in
      render_enforcement_matrix.py. Module drydocs-web and epic O because the matrix is
      the O12 admin surface's generated artifact; the row's SUBJECT living in
      drydocs/cli.py is the whole point of the row.
  - id: O55
    epic: web-console
    title: "The react-router high advisory needs a v7-to-v8 migration or a recorded waiver — npm audit stays red until one of them happens"
    type: chore
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - web/package.json
      - web/package-lock.json
      - web/src/
    acceptance: >
      `npm audit` in web/ reports ZERO high-severity advisories, reached one of two ways
      with the choice named in the commit body: (a) the migration — router imports
      rewritten against react-router@8 (v8 absorbs react-router-dom, whose latest 7.18.1
      sits inside the vulnerable 7.12.0-8.2.0 range, which is why `npm audit fix` is a
      no-op), `npm run build` passing and every route still reachable; or (b) an explicit
      recorded waiver naming why GHSA-qwww-vcr4-c8h2 cannot apply to this console (the
      RSC-mode CSRF path — the console is a Vite SPA with no RSC actions). A red audit is
      never the resting state.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-40, escalated out of O34 by that item's own stop
      clause (the postcss/nanoid patches landed there; this one needs a major bump). Both
      branches are legitimate — the entry's own reading is "likely moot in practice" — so
      the acceptance refuses to force the migration and forbids only leaving the audit red
      AND undecided. Pairs with the bundle code-splitting design call O34 also parked.
  - id: O56
    epic: web-console
    title: "The /software console page: vendor to product to corpus to graph on one screen, declared state and loaded state as separate columns"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-08-07 (laptop) at commit 9b4cf59 — BUILT BEFORE ANY ITEM CLAIMED IT, groomed here so the ledger carries the surface. See notes
    depends_on: [Q16]
    inputs:
      - web/src/routes/SoftwareRoute.tsx
      - web/src/software/softwareModel.ts
      - web/src/software/VendorIcon.tsx
      - web/src/generated/software-registry.json
      - web/src/generated/load-map.json
      - drydocs_api/query_specs.py
      - web/src/modules/registry.ts
    acceptance: >
      A `/software` route, registered in web/src/modules/registry.ts with
      `access: 'sme'` (steward + admin) and route-guarded in App.tsx, renders four
      tabs — Products, Vendors, Documentation coverage, Corpora — over the
      generated software-registry.json plus the `doc-corpus` rows of load-map.json,
      with exactly ONE live graph read (the registered QuerySpec
      `software.doc-coverage.v1`) for the in-graph column. It is a JOIN surface, not
      a second corpus browser: /docs already browses corpora, and the cells worth
      rendering are the intersections (a product whose declared corpus can never
      produce an edge; a product with no docs at all; corpora no product names).
      FOUR HONESTY RULES hold, each of which a naive build gets wrong: (1) gate
      state and edge state are INDEPENDENT columns, never one chip — a corpus can
      be both gate-blocked and permanently edge-less, and one "blocked" chip would
      falsely imply that signing the gate produces coverage; (2) a deliberately
      withheld edge renders as a RULING (teal, citing G32), never as a red gap;
      (3) a database the page cannot read never shows "0 documents" — an
      unprovisioned dddocs reads "not queried", because 0 is a false claim of
      absence; (4) an undeclared link is REPORTED, never inferred — the page does
      not derive bmc-docs -> controlm from taxonomy_path, since no file maps a
      taxonomy path to a product id. Ledger rows land with the code
      (ui-components.yaml, ui-tests.yaml and their coverage pins); `npm run build`
      (tsc -b) and oxlint are clean and the full suite is green.
    notes: >
      Groomed 2026-08-07 as ALREADY DONE — the retro-groom case. software-registry.json
      and load-map.json were generated, drift-guarded and committed with ZERO consumers,
      five vendor icons sat in web/public/vendor-icons/ referenced by nothing, and no
      backlog item had ever claimed a page for either: the gap was invisible rather than
      deferred, which is precisely the state an item exists to prevent. Filed `done` with
      its close evidence rather than left unrecorded, so the surface has an id that O57,
      O58 and any later /software work can cite.
      depends_on Q16 because the page consumes the generated fields Q16's session emitted
      (commit 0ddf880) — notably target_db reaching the JSON, without which honesty rule
      (3) is unimplementable.
      ModuleIcon gained a `software` case; noted at the build that the switch has no
      `default`, so a missing case vanishes silently with no compiler error — a latent
      trap for the next module, not a defect of this one.
  - id: O57
    epic: web-console
    title: "A console page for the load map: the pipeline sources, systems, retired ids and load sequence in load-map.json that no web/ surface reads"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - web/src/generated/load-map.json
      - scripts/render_load_map.py
      - docs/plan/load-map.html
      - web/src/modules/registry.ts
      - config/taxonomy/ui-components.yaml
      - config/taxonomy/ui-tests.yaml
    acceptance: >
      A console route renders the part of web/src/generated/load-map.json that no
      web/ code reads today: the 28 non-doc-corpus `sources` rows (kinds table,
      view, file, report, api, query — with home, system, origin, authority,
      classification, confirmed, ledger, taxonomy_captures, ontology_mappings and
      loaders), the 15 `systems`, the 17 `retired` ids and the 17 `sequence` steps
      with their `ad_hoc_commands`. The two defect lists the JSON already carries —
      `sourceless_loaders` and `map_entries_without_registry_source` — RENDER AS
      DEFECTS rather than being dropped, since a surface that silently omits the
      known-broken rows is worse than no surface. The page reads the committed
      JSON only and never re-derives from the registries, so it cannot disagree
      with docs/plan/load-map.html; it does NOT replace that page, which is N5's
      print/PDF surface for the SME loop and keeps its own audience. Registered in
      web/src/modules/registry.ts with an access designation and a stated reason,
      ui-components.yaml + ui-tests.yaml rows with their coverage pins moved, and a
      test asserting the rendered row counts against the JSON so a new registry
      entry cannot silently fall off the page. `npm run build` and oxlint clean.
    notes: >
      Groomed 2026-08-07 from the Q16 session. THE GAP, stated precisely: N4 generated
      load-map.json for web/ and N5 then chose docs/plan/load-map.html — a rendered page,
      not a console route — so the JSON's console consumer was never scoped by any item.
      The /software page (O56) is the first web/ code to import load-map.json at all, and
      it deliberately filters to the 7 `doc-corpus` rows, which leaves the other 28
      sources plus systems, retired and sequence unconsumed. Module drydocs-web / epic
      web-console / phase 12 by the O54 precedent: file the item by the SURFACE it
      delivers, not by where the data it reads is produced.
      Deliberately NOT specified here: which module id and access level it takes, and
      whether it is a new nav module or a tab under /loads. Both are page-design calls
      that belong with the person building it, and the acceptance forces the decision to
      be recorded rather than defaulted.
  - id: O58
    epic: web-console
    title: "A console surface for docs-verify: which declared corpora are actually loaded and where - the answer a single-database QuerySpec cannot give"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs/docs_verify.py
      - drydocs/cli.py
      - drydocs_api/query_specs.py
      - drydocs_api/app.py
      - config/doc-source-registry.yaml
      - docs/decisions/0005-browser-neo4j-access-path.md
    acceptance: >
      A console surface answers "which declared doc corpora are actually loaded, and
      in which database" with the same six states drydocs/docs_verify.py computes —
      loaded | missing | stale | wrong-db | unshaped | db-absent — and renders
      wrong-db as the alarm the CLI's non-zero exit means. THE TRANSPORT IS THIS
      ITEM'S REAL CONTENT, and it must be chosen and written down, not defaulted:
      the reconciliation is inherently MULTI-database (it sweeps drydocs, dddocs and
      ddcontext precisely so a corpus sitting in a database it did not declare is
      visible, a result unobtainable from any single one of them), while a
      registered QuerySpec carries exactly one `database:` field by construction and
      SPEC_DATABASES does not even admit dddocs — so this cannot be a plain
      spec-backed page. The item names ONE of: (a) a server-side sweep endpoint in
      drydocs_api reusing docs_verify.verify(), whose `run(database, cypher, params)`
      is already its only I/O seam, making the reuse exact; (b) N registered specs,
      one per swept database, joined client-side with the join rule written down;
      or (c) a generated JSON artifact refreshed by the session ritual, the /software
      precedent. Whichever is chosen, the commit body records the choice AND its
      cost against ADR 0005 — option (a) adds a non-spec read path to an API whose
      standing rule is that /raw-cypher is the only interactive Cypher surface, and
      that is a boundary change, not a detail. Two things are never invented: a
      database that was not queried renders "not queried", never 0 (the O56 honesty
      rule); and a guard asserts the surface's state set against docs_verify.py's
      constants, so a seventh status cannot appear in the CLI and silently miss the
      page.
    notes: >
      Groomed 2026-08-07 from the Q16 session. Q7 shipped the registry-vs-graph
      reconciliation as a CLI verb only (drydocs docs-verify, 2026-08-01) and no item
      ever claimed a surface for it — the same invisible-rather-than-deferred gap O56
      records for /software.
      Model fable, not sonnet, and the reason is specific: the transport choice can
      change the drydocs_api read-path boundary. Options (b) and (c) leave it intact;
      option (a) does not. A groom cannot make that call for the builder, so the
      acceptance forces it to be made explicitly and costed.
      Module drydocs-web / epic web-console by the O54 precedent (file by the surface
      delivered) even though the hard work may land in drydocs_api — recorded here so the
      choice is visible rather than looking like a mis-bin.
      NOT gate-bound and NOT blocked on G32: this surface REPORTS the residency situation
      G32 owns (db-absent is a true statement about the world today, exactly as
      docs_verify.py already treats it) rather than resolving it. It should be readable
      before the ruling, which is when it is most useful.
  - id: B5
    epic: taxonomy-capture
    title: "Capture the medallion stage vocabulary (RAW → TRUSTED → REFINED → PROVISIONED) as pure classification in config/taxonomy/ — the estate's canonical stage set, SME-confirmed via the gate"
    type: requirement
    module: taxonomy
    phase: 1
    agent: taxonomy-importer
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: [config/taxonomy/, internal/datalens-reference/continuity.md, internal/dsi-reference/]
    acceptance: >
      The medallion stage vocabulary is captured as pure classification in
      config/taxonomy/ (the Epic B idiom — no meaning edges), registered per
      the taxonomy capture rules, with the canonical stage set SME-confirmed
      via the gate before any dataset/job stage-attribution edge is declared;
      any such edge enters relationship_vocabulary.yaml as status: planned
      only.
    notes: >
      Groomed 2026-07-28 pm from the DSI addendum DL-11(b) — both neighbor
      tools (DataLens pipeline cards, DSI stage KPI row) expose the same
      stage vocabulary; likely intersects the Snowflake data-catalog seam
      (G42-G44) and the DPL S3 zone prefixes. UI half is O38.
      RE-REFERENCED 2026-08-09 (desktop, SME direction with vendor-doc evidence in hand): the terminal stage is PROVISIONED, not SNOWFLAKE — title corrected, and the same correction applied to docs/restructure/roadmap.yaml, which carried the old set. THE ERROR CLASS IS ONE THE SME ALREADY CALLED ONCE, one level down: at C25 the ruling was that Snowflake is a TARGET DB PLATFORM and the 'Snowflake ETL' label names the loads-into-Snowflake JOB FAMILY, not a software product. Same mistake here — a consumption TARGET was promoted into a STAGE slot, so the vocabulary asserted that every dataset's final stage is one particular warehouse. PROVISIONED is the stage; Snowflake is one of several platforms a Provision pipeline can write to (the internal DPL documentation's consumption zone lists relational and MPP targets alongside it). THE REPO ALREADY KNEW, which is why this is a correction rather than a discovery: the 2026-07 DPL ingestion-leg trace in IDEAS.md states 'PROVISIONING DB-load lands the consumption target (Provisioned ≠ an S3 prefix)' — B5's title simply never caught up, and roadmap.yaml inherited the stale set. That is also the substantive point for the capture: RAW/TRUSTED/REFINED are S3 zone PREFIXES while PROVISIONED is not a prefix at all, so the four names are not four of a kind and a flat enum would misrepresent the last one. NEW FINDING the capture must settle, from the same evidence: there appear to be TWO zone vocabularies, not one — an on-prem set (conformed / semantic / analytic) and an off-prem S3 set (trusted / refined), converging on a shared provision stage. So 'the estate's canonical stage set' may not be a single list, and the gate should rule whether they are one vocabulary with platform-scoped names or two that map onto each other. SOURCE BOUNDARY: the evidence is company-internal documentation; only the stage/zone MECHANISM is recorded here — the site, its URLs, and every identifier on it stay company-side (PUBLISH-BOUNDARY.md).
  - id: R9
    epic: agentic-qa
    title: "Read-only agent query command over the O33-guarded QuerySpecs: one deterministic, testable graph-navigation surface for agents (MCP recorded as the later option)"
    type: task
    module: drydocs-api
    phase: 15
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs: [drydocs_api/query_specs.py, tests/unit/test_module_boundary.py, docs/reviews/persona-python-architect-2026-07.md]
    acceptance: >
      A read-only, deterministic query command over the O33-guarded
      QuerySpecs, placed so the module boundary holds (drydocs_api owns the
      specs; test_module_boundary stays green): each spec addressable by name
      with typed params, stable JSON output, refuses unknown specs/params,
      zero write paths; unit tests drive it against a fake client; a short
      agent-facing usage note documents it as the graph-navigation surface.
      MCP (mcp-neo4j-cypher) explicitly out of scope, recorded as the richer
      later option.
    notes: >
      Groomed 2026-07-28 pm from the live-benchmarked [idea] line (5
      questions ~410ms/~1.2k chars vs grep's noisy equivalents; safe now that
      U6 fixed the cross-root blind spot). CLI-vs-MCP resolved toward CLI by
      the line's own analysis: MCP adds config + write-risk surface.
  - id: J18
    epic: release-infrastructure
    title: "Live-verification claims name their machine/container/database — convention written down, recent close notes retro-tagged, sample-reproducible evidence preferred"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done   # closed 2026-08-04 same session (desktop, J-series batch): convention written as backlog-header pull-rule step 5 + a CLAUDE.md §6 working agreement (venue = machine/container/database; sample-reproducible evidence preferred over machine-pinned); P3 and G33 close notes retro-tagged (desktop, neo4jtest, drydocs DB) — G33's tag names it as the claim the laptop audit nearly filed as a defect; P5 needed no edit, it was tagged at close and is cited as the worked example.
    depends_on: []
    inputs: [CLAUDE.md, docs/restructure/backlog.yaml]
    acceptance: >
      The live-verification convention is written down where claims are made
      (backlog header comment + a CLAUDE.md working-agreements line): a
      VERIFIED LIVE claim names the machine/container/database it ran on, and
      sample-reproducible evidence ("ingest-controlm reproduces X") is
      preferred over machine-pinned claims; the P3/P5/G33 close notes are
      retro-tagged as the worked examples.
    notes: >
      Groomed 2026-07-28 pm from the two-graphs [idea] line — an audit read
      the laptop's graph and nearly recorded G33 (desktop-built) as a defect.
  - id: J19
    epic: release-infrastructure
    title: "Pull rule states the claim is pushed BEFORE work starts — the pushed-claim discipline becomes the written rule, not session lore"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [CLAUDE.md, docs/restructure/backlog.yaml]
    acceptance: >
      CLAUDE.md's pull rule and the backlog.yaml header comment both say the
      in_progress claim is COMMITTED AND PUSHED before work starts, not written
      at close, and name why (two sessions independently built C19 ten minutes
      apart on 2026-07-28).
    notes: >
      Groomed 2026-07-28 pm from the duplicate-C19 [idea] line.
      2026-07-30 EXECUTION RECORD — acceptance and inputs were REVISED DURING execution
      (bff98f5), so the closed item is not graded against the text it was groomed with;
      recording that here because the diff alone reads as a clean pass. Two changes, both
      substantive: (a) the original clause "and the groom-backlog skill where it restates
      it" was VACUOUS — that skill covers grooming, not pulling, and never restated the
      pull rule; it was deliberately left untouched rather than have a pull-rule section
      invented in it to satisfy the wording. (b) the real third edit site was this file's
      own "HOW AN AGENT PULLS WORK" header comment — the block agents actually read on
      pull, whose step 2 still said "(commit that change)"; it was absent from inputs.
      FOLLOW-UP in the same session: the sub-agent's edits stated only the abstract
      mechanism and dropped the named incident, so the retained "names why" clause was
      NOT met at bff98f5 — both sites now name the 2026-07-28 duplicate-C19 build
      explicitly. Naming the incident is the point of the item's own title (rule, not
      lore): an abstract sync-layer note reads as theoretical, the incident does not.
  - id: L20
    epic: doc-infrastructure
    title: "feedback/ stray-file guard: files matching no expected -rev<N>.yaml pattern are reported as findings, not silently ignored"
    type: task
    module: drydocs-load
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs/loaders/doc_traceability.py, docs/design/feedback/]
    acceptance: >
      The feedback adapter (or a companion check in the same loader family)
      reports files under docs/design/feedback/ that match no expected
      -rev<N>.yaml pattern as findings instead of silently ignoring them —
      the misnamed Copy-feedback export dead end (resolved 2026-07-28)
      becomes detectable; a test pins a stray-file fixture producing a
      finding; suite green.
    notes: >
      Groomed 2026-07-28 pm from the resolved misnamed-export [chore] line's
      latent gap: the L5 Copy-feedback path trusts the human to name the
      file, and a wrong name is invisible today. Sits in the L17/L18
      loud-refusal family.
      DONE 2026-08-04 (c075ee2): DesignDocFeedbackAdapter.stray_files()
      censuses top-level files that are neither <doc>-rev<N>.yaml nor
      README.md (subdirs like scans/ excluded by shape); DocFeedbackLoader
      reports the census after load — stray_feedback_files + WARNING naming
      each file, duck-typed for fake adapters. Tests pin the stray fixture at
      adapter and loader level, plus a standing assert that the committed
      feedback/ tree is stray-free. Suite 1505.
  - id: L21
    epic: doc-infrastructure
    title: "Startup-refresh runbook Rev 4: collapse the per-file supplement verbs to the single apply-supplements chain (G29) — also retires Appendix B's missing registry-supplement gap"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [docs/design/drydocs-startup-refresh-runbook.md, scripts/render_design_doc.py]
    acceptance: >
      Runbook Rev 4: the per-file supplement verb block (step 4 + Appendix B)
      collapses to the single verified `poetry run drydocs apply-supplements`
      chain (G29), which also retires Appendix B's long-standing missing
      apply-registry-supplement gap; the Refresh/ingest demonstrable-content
      step names `drydocs load-doc-traceability` (shipped by L7 after Rev 2 was
      signed); rendered HTML regenerated and the stale-render check passes.
    notes: >
      Groomed 2026-07-28 pm from the held 2026-07-26 [doc] line — the hold
      (SME review in flight) lifted when the review closed; edit 3 of its
      trio (container facts) already landed via L16 Rev 3, this is the
      remaining two as one revision.
      MERGED IN 2026-07-31 (weekly groom): the 2026-07-20 [doc] inbox line —
      mention `drydocs load-doc-traceability` in the Refresh/ingest step. It
      was parked explicitly to "ride the next feedback loop rather than bump a
      fresh Rev for one line", and this IS that revision, so it becomes a
      clause of L21's acceptance rather than an item of its own.

  # --- 2026-07-28 evening groom (post-rebase): the rua listing fallback (G45),
  # --- the K4-comment scope fix (C20), the render-entry-point fold part 2
  # --- (J20), and the agent-runtime alignment chores (R10). R3 gained the
  # --- caller-identity merge in place.
  - id: G45
    epic: component-topology
    title: "rua_inventory stages the metadata-only scripts.csv listing instead of silently dropping every script — presence-dispatch with scripts.tsv precedence (back-flow, mechanism-only)"
    type: bug
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_lineage/extractors/rua_inventory.py, tests/unit/test_lineage_rua.py, config/gate-prompts/rua-load-shapes.yaml]
    acceptance: >
      _stage_scripts dispatches by presence: scripts.tsv (v2, richer) WINS
      when both files exist; else scripts.csv
      (path|script|permission|date|size, pipe-delimited) stages METADATA-ONLY
      — abs_path joined from path+script, no sha256 so hash_missing counts,
      no body copy so script_copies_missing counts (a LISTING is a fact,
      never implied content); neither file present files
      sections_optional_absent. Reader refactor keeps one delimited-read
      path (the company _read_delimited shape or equivalent). Synthetic
      fixtures pin BOTH the csv fallback (rows stage, counters assert) and
      the tsv-wins precedence; zero real values anywhere
      (mechanism-not-instance); staging-only — no loader, no graph write
      (G22 stays the terminus); full suite green.
    notes: >
      Groomed 2026-07-28 evening from the back-flow [bug] line: company
      session fixed theirs same day (561 scripts across two production
      bundles were ALL silently dropped pre-fix; staging-only, terminus
      held). Producer parity keeps bundles cross-ingestible ahead of the T14
      collector convergence; the v1-tag-wearing-v2-payload collector caveat
      rides T14, not this item. Gate-page premise 2 + provenance already
      carry the hash-absent consequence for G22.
      CLOSED 2026-07-28 evening: _stage_scripts is a presence dispatch
      (_stage_scripts_tsv unchanged v2 path WINS; _stage_scripts_csv NEW —
      joined abs path = the rua_script identity, permission/date/size mapped
      to perms/mtime/size, hash_missing + script_copies_missing counted per
      row; neither file -> optional-absent exactly as before, v1 test still
      green); _read_tsv is now a thin wrapper over _read_delimited(sep).
      3 synthetic-fixture tests (fallback stages + counters; tsv-wins with
      the csv-only row NOT staged; malformed rows counted). Staging-only —
      no loader, no graph write, G22 stays the terminus. Suite 1108 green.
  - id: G46
    epic: component-topology
    title: "Public command-line resolution API on the shared Control-M resolver: resolve a verbatim CMD_LINE against a job's variable scope chain, with substitution provenance"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/controlm/resolver.py, drydocs_core/controlm/variables.py, drydocs/cmdline_staging.py]
    acceptance: >
      drydocs_core/controlm/resolver.py exposes resolve_command_line(layers,
      cmd_line) -> ResolvedCommandLine: builds the scope environment through
      the SAME machinery resolve_layers uses (guardrail 1 — one resolver,
      never a second substitution engine) and resolves the command text
      through the SAME fixed-point pass, returning resolved text +
      provenance (names substituted with their binding scope, unresolved
      user names, external %%\\ refs kept verbatim, canonical {ODATE}-class
      residue distinguished from failures, depth). Verbatim input is never
      mutated (guardrail 2 — derived beside, never replacement). Tests
      cover: a launcher line resolving through folder+job scopes, the
      %%VAR-launcher case (resolution turns an unparseable launcher ref
      into a parseable path), runtime tokens as expected residue, an
      unresolved name staying visible and reported, and the concat
      delimiter inside a command argument. No graph writes.
    notes: >
      CLAIMED in_progress 2026-07-29 (pushed-claim discipline, with G47).
      Groomed 2026-07-29 from the XML-fed cmd-line resolution idea (user
      direction 2026-07-28 pm). The C3 Phase B resolver already implements
      AutoEdit semantics for variable DEFINITIONS; this adds the public
      command-text entry point both paths (Oracle staging, XML export)
      call, so the G39 store's cmd_line_resolved column gets ONE producer.
      CLOSED 2026-07-29: resolve_layers refactored over a shared
      _resolve_scope_chain (returns the final env + each name's LAST
      binding scope — the walk exists ONCE); _resolve_value_traced adds
      the substituted-name trace under the unchanged _resolve_value
      wrapper; resolve_command_line(layers, cmd_line) ->
      ResolvedCommandLine {raw untouched, resolved, is_fully_resolved,
      depth, substituted (name, scope) pairs, unresolved, external_refs,
      canonical_tokens, env variants}. 7 new tests incl. the
      %%VAR-launcher round-trip (raw hides the launcher; the resolved
      text parses to executable_path via parse_command) and
      winning-scope attribution on rebinding. Exported from
      drydocs_core.controlm. 42 resolver+staging tests green.
  - id: G47
    epic: component-topology
    title: "Control-M XML export ingestion seam: register controlm-xml-export + taxonomy-first extractor staging folder/job definitions AND variables from the 9.0.21.300 XML config SoR"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_lineage/extractors/dpl_registry.py, drydocs_core/controlm/resolver.py, config/source-registry.yaml, docs/restructure/IDEAS.md]
    acceptance: >
      Source controlm-xml-export is registered in config/source-registry.yaml
      (Internal, kind orchestration, orchestrator bmc-controlm, confirmed:
      false; notes name the OPEN precedence ruling vs controlm-psgmgr as the
      HITL question — guardrail 3, never auto-decided here). A
      ControlMXmlDefsExtractor (drydocs_lineage/extractors/controlm_xml.py)
      stages, taxonomy-first with ZERO graph writes: folders (name,
      data_center), jobs (folder, name, task_type, cmd_line VERBATIM,
      node/application facts as present), and ORDERED variable definitions
      at folder and job scope (definition order preserved — the resolver's
      sequential-assignment contract depends on it). ASSUMED XML contract
      defined by SYNTHETIC fixtures (the dpl_mac discipline; the company's
      real 9.0.21.300 export validates or amends it — XML schema docs are a
      known gap). Coverage counts every skip by reason: files invalid,
      jobs without names, variables without names, unknown elements
      tolerated-and-counted. Real exports live under
      DRYDOCS_DATA_ROOT/controlm-xml/ (landing-zone registration; no
      filename-fingerprint sweep possible for generic .xml — reason
      documented). Unit tests green.
    notes: >
      Groomed 2026-07-29 from the XML-fed cmd-line resolution idea. The
      company XML loader exists company-side (run-log tee + _ts()
      normalizer hardening rounds); this producer seam is the sanitized
      mechanism twin that makes the resolution step testable and portable.
      Guardrail 1: the extractor STAGES ordered defs only — substitution
      happens exclusively in drydocs_core.controlm.resolver.
      CLOSED 2026-07-29 (claimed same day): controlm-xml-export registered
      (Internal, orchestrator bmc-controlm, confirmed: false behind the
      precedence question); controlm_xml_dir() landing zone (no-sweep
      limitation documented in the helper + the registry entry);
      ControlMXmlDefsExtractor stages XmlFolderRecord / XmlJobRecord
      (cmd_line VERBATIM) / XmlVariableRecord (per-container document
      ordinals) with older-format TABLE/TABLE_NAME synonyms, SUB_FOLDER
      nesting, and every skip counted (no-name folders/jobs/variables,
      duplicate jobs first-wins, no-cmd_line staged-and-counted,
      non-consumed elements tolerated-and-counted).
      XmlDefsExtract.scope_layers(job) emits exactly the resolver's
      layers shape — the guardrail-1 handoff is proven end to end in
      tests (XML fixture -> scope_layers -> resolve_command_line ->
      resolved text, incl. subfolder-overrides-folder attribution). 8
      synthetic-fixture tests; extract API graph-free by construction.
  - id: G48
    epic: component-topology
    title: "resolve-cmdline-staging: populate the G39 store's cmd_line_resolved from XML-staged variables via the shared resolver (store schema v3 adds folder_name + resolution provenance)"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G46, G47]
    inputs: [drydocs/cmdline_staging.py, drydocs_lineage/extractors/controlm_xml.py, drydocs_core/controlm/resolver.py]
    acceptance: >
      A CLI verb `drydocs resolve-cmdline-staging` joins XML-staged
      folder/job variables to job_detail rows on (data_center, folder_name,
      job_name) and populates cmd_line_resolved via
      resolve_command_line (G46) — cmd_line stays VERBATIM beside it
      (guardrail 2), and NEW provenance columns record resolution_source,
      substituted names, and unresolved residue per job. Store schema bumps
      to v3: job_detail gains folder_name (graph j.parent_table rides the
      G39 export; a v2 file is refused by resolve/parse — re-export).
      Coverage counted by reason, never silent: jobs matched/resolved,
      no-xml-match, ambiguous-match (two XML jobs hit one store row —
      skipped, counted), nothing-to-substitute, unresolved-residue. G40
      parse benefits unchanged (parsed_from already records resolved|raw).
      NO graph writes; the store stays under the G19 data root; the
      RETIREMENT and re-export-clears caveats stay intact. The
      psgmgr-vs-XML precedence ruling stays OPEN (guardrail 3): this verb
      fills a nullable derived column and decides no source-of-truth
      question.
    notes: >
      CLAIMED in_progress 2026-07-29 (deps G46/G47 closed same session;
      pushed-claim discipline).
      Groomed 2026-07-29 from the XML-fed cmd-line resolution idea. This
      deliberately amends G39's "variables pull stays deferred" sequencing
      as its own item (never folded silently) and touches T16: if XML
      becomes the standing feed, the CM_DEF_VJOB_DETAIL retirement note
      gains a second path — recorded here, ruled at the precedence
      question.
      CLOSED 2026-07-29 same session: store v3 (job_detail.folder_name from
      graph j.parent_table — psgmgr projection gains J.PARENT_TABLE too;
      resolution_quality provenance table: verdict/resolution_source/
      matched_via/substituted/unresolved/canonical-tokens JSON; older
      stores refused by resolve AND parse with a re-export hint).
      resolve_job_detail takes the XML extract DUCK-TYPED (the
      module-boundary seam: cli.py, the exempt composition root, wires
      drydocs_lineage's extractor in; cmdline_staging imports only core)
      and resolves the STORE-verbatim cmd_line via resolve_command_line —
      the XML job's own CMDLINE is compared, counted on disagreement
      (cmd_line_mismatch = measured precedence evidence), never
      substituted. Verdicts: resolved/residue/nothing_to_substitute/
      no_xml_match/ambiguous_match (subfolder twins skipped)/no_cmd_line +
      dc_fallback and no_folder_name counters; deterministic re-runs
      (column + quality table cleared first); re-export clears both. CLI
      verb resolve-cmdline-staging (run-log v1, run BEFORE parse). 8 new
      tests incl. the full chain resolve-then-parse (parsed_from =
      resolved for the %%VAR-launcher job). Suite 1144 passed / 5 skipped.
      NOT DONE HERE by design: the psgmgr-vs-XML precedence ruling
      (guardrail 3, IDEAS question) and any graph write (G22 terminus).
  - id: C20
    epic: ontology-mapping
    title: "constraints.cypher's 'deprecated by K4 — kept for old graphs' comment is under-scoped: role/membership keys are live catalog writes; re-scope the comment to what K4 actually deprecated"
    type: chore
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [drydocs_core/schema/constraints.cypher, config/gate-log.md, drydocs_core/schema/catalog_ontology_supplement.cypher, drydocs/loaders/cypher/pat_team_roles.cypher]
    acceptance: >
      The comment heading role_name / role_id / membership_id in
      constraints.cypher names the ACTUAL K4 scope (verified against the K4
      gate-log entry): the reified-membership pattern is deprecated in the
      SEAL attribution loaders specifically, while :Role seeds
      (catalog_ontology_supplement) and :Membership writes
      (pat_team_roles.cypher) remain live catalog paths; the void phrase
      "kept for old graphs" is gone (the C13 precedent — graphs rebuild from
      bootstrap, never migrate); test_schema stays green with
      EXPECTED_CONSTRAINTS unchanged (comment-only edit).
    notes: >
      Groomed 2026-07-28 evening from the same-day [question] line (noticed
      during the AIS back-flow check, 87ba693; deliberately not hot-fixed
      there). Same comment-accuracy family as C19.
      CLOSED 2026-07-28 evening: comment re-scoped against the K4 record
      (the adjacent K4 block itself says "in the SEAL loaders" — gate
      2026-07-10 §B/§C) and the verified live writers
      (catalog_ontology_supplement :Role seeds; pat_team_roles.cypher:24
      :Membership MERGE on membership_id); "kept for old graphs" dropped per
      the C13 rebuild-not-migrate precedent. Comment-only:
      EXPECTED_CONSTRAINTS unchanged at 51, schema tests green.
  - id: J20
    epic: release-infrastructure
    title: "One render entry point, part 2: enforcement-matrix.json joins the default-paths board render (the J17 fold applied to the second generated surface)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [scripts/render_board.py, scripts/render_enforcement_matrix.py, CLAUDE.md]
    acceptance: >
      A default-paths scripts/render_board.py run also refreshes
      web/src/generated/enforcement-matrix.json (explicit --backlog/--out
      runs stay board-only, the J17 rule); after the change, adding a
      gate-prompt file or editing a matrix source followed by the documented
      ritual leaves tests/unit/test_enforcement_matrix.py green with no
      separately remembered step; the CLAUDE.md stale-render line names the
      matrix alongside gates.json.
    notes: >
      Groomed 2026-07-28 evening from the same-day [chore] line: the
      stale-render check caught exactly this drift live (49667dd regenerated
      gates.json but not the matrix; swept in a90fdad). Same defect shape
      J17 closed for gates.json.
      CLOSED 2026-07-28 evening: default-paths render_board.py now runs
      render_gates + render_enforcement_matrix (explicit-path runs stay
      board-only); docstring + CLAUDE.md stale-render line name all three
      surfaces. Verified: one command wrote board + gates + matrix, all
      guards green, matrix regen produced zero diff (was in sync).
  - id: R10
    epic: agentic-qa
    title: "Agent-runtime alignment chores from the passed ADR 0007 revisit check: pin google-adk to >=2,<3 and date-stamp the revisit clause"
    type: chore
    module: drydocs-agents
    phase: 15
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [agents/requirements.txt, docs/decisions/0007-agentic-qa-architecture.md]
    acceptance: >
      agents/requirements.txt pins google-adk>=2,<3 (currently unpinned
      while ADK 2.0 breaking changes land upstream — an unpinned install can
      drift under the app shape); ADR 0007's Revisit-if section carries a
      dated note that the trigger fired 2026-07-28 and PASSED (convergence,
      not divergence), citing the internal review by path only
      (internal/agent-platform/smartsdk-3-adr0007-compatibility.md).
    notes: >
      Groomed 2026-07-28 evening from the agent-runtime target-state [idea]
      line's follow-ups; the third follow-up (R3 caller-identity slot) was
      MERGED into R3's acceptance in the same groom.
      CLOSED 2026-07-28 evening: requirements.txt pins google-adk>=2,<3 with
      the R10 rationale comment; ADR 0007's Revisit-if section carries the
      dated PASSED sub-bullet citing the internal review by path, trigger
      kept live for the next major. (agents/ has its own venv — the pin
      takes effect at that venv's next pip install, by design.)

  # --- 2026-07-30 groom (user go-ahead on the registry-review observations):
  # --- the source-registry pk check + binding-agreement guard, one item.
  - id: J21
    epic: release-infrastructure
    title: "Source-registry pk check: duplicate ids refuse loudly, and the one-off loader: field is guarded against the N3 class binding"
    type: task
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [drydocs_core/source_registry.py, config/source-registry.yaml, tests/unit/test_source_registry.py]
    acceptance: >
      (1) SourceRegistry.from_yaml REFUSES a duplicate source id with a clear error
      naming the id — today it silently last-one-wins overwrites, so a duplicated
      entry with different confirmed values would decide the D3 gate by file
      position; a fixture test pins the refusal and a schema test pins uniqueness
      on the shipped registry (the test_backlog / test_taxonomy_ontology_map house
      idiom — source-registry was the only per-entry registry without it).
      (2) Registry entries carrying a loader: field are guarded to AGREE with the
      named loader module's class-level source_id (the N3 binding) — no second
      unguarded home for the loader<->source join; today stg-app-fact is the one
      such entry. (3) The stale autosys-export / airflow-mwaa notes ("Placeholder…
      Activate (confirmed: true) only after…") are corrected to the recorded
      2026-07-14 crosswalk-gate sign-off facts. Full suite green.
    notes: >
      Groomed + claimed 2026-07-30 from the 2026-07-29 registry review (chat):
      the pk check answers the user's "should we add something like a pk check";
      scope deliberately EXCLUDES the two gate-adjacent companions — the
      cross-repo identity handle (URN field) and the reconcile-guard same-id/
      changed-meaning check — which stay inboxed with the N7 overlay candidate
      until the company's T19 gate rules. BUILT + CLOSED same day (087a685):
      DuplicateSourceIdError refusal + shipped-registry uniqueness pin +
      loader-field/N3-binding agreement guard + both stale placeholder notes
      corrected to the 2026-07-14 sign-off facts; suite green 1163. The user's
      same-day ruling "the id field as is does not work" (system vs extracted
      dataset per ontology domain) is captured as the registry-redesign IDEAS
      entry — design session + HITL, bundled with N7/URN/reconcile-guard.

  # ── Epic V — SME runbooks per module (main) — Phase 10 ─────────────────────
  # Groomed 2026-07-29 (user directive, chat): "create a SME-Runbook for each
  # module." Runbooks are the Epic L governed-doc class (runbook.outline.yaml,
  # test_doc_outline auto-sweep, deterministic render, L5/L6 feedback loop) —
  # this epic makes coverage PER MODULE a rule rather than an accident. Five
  # runbooks already exist (startup-refresh, web-console, mapping-store,
  # mapping-demo, lineage-mac, cmdline-resolution) but they are chain- or
  # system-scoped; V1 maps them onto the modules registry and the per-module
  # items fill the gaps. Pending-list idiom reused from N2's LEDGER_PENDING
  # (frozen, shrink-only) so the guard ships day one without lying.
  - id: V1
    epic: sme-runbooks
    title: "SME-runbook coverage rule: every module in the registry maps to a governed runbook or sits on a frozen shrink-only RUNBOOK_PENDING list"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop). tests/unit/test_runbook_coverage.py maps all 18 registry modules to exactly one of COVERED / EXEMPT / RUNBOOK_PENDING, with a second map dispositioning every docs/design/*-runbook.md — because 'operates part of a module' and 'IS the module runbook' are different claims and conflating them is how a module looks covered while most of it is not. COVERAGE IS A SELF-CLAIM: the test reads a `- **Module:** <name>` front-matter line and nothing else, so a rename cannot silently break the map; the runbook outline template now teaches the convention. DISPOSITIONS RULED (the six runbooks, not five — the acceptance miscounted): web-console SATISFIES drydocs-web (the only COVERED module today); mapping-store is single-ARTIFACT scoped (it scopes HTTP out itself) so drydocs-core stays pending for V2; mapping-demo covers the /demo page only, so drydocs-api stays pending and V8 rules extend-vs-author; lineage-mac and cmdline-resolution are CHAIN-scoped so V6 indexes them rather than being discharged by them; startup-refresh is SYSTEM-level across modules, so V3 must rule the overlap as its own title demands. EXEMPT = the 6 work areas by rule + drydocs-plan (no package) and drydocs-deepdoc (86 lines, no CLI verb, no importer). THE 'IF STILL PLACEHOLDER' RE-CHECK PAID: drydocs-docmeta was listed as a placeholder in this item's OWN acceptance and has been BUILT since (Q6, ADR 0006) — moved to RUNBOOK_PENDING and the stale `# planned` registry comment corrected. RUNBOOK_PENDING is frozen SHRINK-ONLY per N2's LEDGER_PENDING idiom: gaining a runbook forces removal. Guard proven RED on a probe (a partial runbook claiming a module fails three ways at once) then green. Suite 1535 (+6).
    depends_on: []
    inputs: [docs/restructure/backlog.yaml, docs/design/, docs/design/templates/runbook.outline.yaml, tests/unit/test_doc_outline.py]
    acceptance: >
      A unit test maps every entry in the backlog modules: registry to exactly one
      of three states, none silently absent: (1) COVERED — a governed
      docs/design/*-runbook.md names the module in its front matter; (2) EXEMPT —
      an explicit list with a reason per entry (work areas by rule:
      reference/taxonomy/ontology/config/graph-infra/docs are captures and
      registries, not operable components; placeholder modules with no code to
      operate: drydocs-plan, drydocs-docmeta, and drydocs-deepdoc if still
      placeholder); (3) RUNBOOK_PENDING — real modules awaiting authorship,
      frozen shrink-only per the N2 LEDGER_PENDING idiom. The map also
      dispositions the five existing runbooks (web-console -> drydocs-web;
      mapping-store/mapping-demo -> decide; lineage-mac + cmdline-resolution =
      chain-scoped, decide whether they satisfy drydocs-lineage or index under
      it; startup-refresh = system-level, decide whether it counts for
      drydocs-load). Suite green.
    notes: >
      Groomed 2026-07-29 from the user directive "create a SME-Runbook for each
      module". The mapping decisions here gate V2-V10's scope (extend an
      existing runbook vs author a new one), which is why they depend on this.
      Module docs (the rule is about the doc corpus), phase 10 with Epic L —
      no plan change.
  - id: V2
    epic: sme-runbooks
    title: "SME runbook: drydocs-core — config/env roots, schema provisioning, vocabulary registry, run logs"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop). docs/design/drydocs-core-runbook.md, outline-conformant, rendered, stale-render-clean; drydocs-core off RUNBOOK_PENDING. Covers the four surfaces the acceptance names: Neo4jSettings + the env roots (NAMES only — the doc's own rule), provisioning and when to re-apply, the relationship-vocabulary registry, and the run-log family. ORGANISED AROUND SILENCE, which is the honest shape for a library: core has no process and almost no exceptions — its failure modes are a loader that writes nothing, a MATCH that finds nothing, a log written where nobody looked — so each section leads with the silence it prevents. Highest-value entries are the ones that have actually bitten: NEO4J_DATABASE unset writes to the EE home db where no query surface looks; a same-name constraint re-declare succeeds and does nothing, so a definition change needs an explicit DROP first; DRYDOCS_LOGDIR falls back to SPIDERP_LOGDIR so 'missing' logs are usually the legacy name; apply-supplements VERIFIES declared IRIs and the per-file verbs are how `registry` went unseeded for weeks. Both shipped one-liners were RUN before commit (config resolution, supplement order) and the config one prints no secret — password is SecretStr. Appendix tables carry the re-derive command plus the rule that the code wins, the treatment adopted after the mapping-store inventory drifted a day unnoticed. V1's guard again refused the file until it was dispositioned and the module left the shrink-only list. mapping-store's disposition reason updated: it is NOT superseded — both are correct at their own scope, which is what the two-map split exists to record. Suite 1537 green.
    depends_on: [V1]
    inputs: [drydocs_core/, docs/design/templates/runbook.outline.yaml, drydocs_core/schema/provisioning/]
    acceptance: >
      docs/design/drydocs-core-runbook.md conforming to runbook.outline.yaml
      (test_doc_outline auto-sweep), rendered .html committed and
      stale-render-clean, drydocs-core removed from RUNBOOK_PENDING. Covers the
      module's operate surface: Neo4jSettings/env-root configuration (names
      only, no secrets), manual DB provisioning + when to re-run it, the
      relationship-vocabulary registry checks, and the run-log family
      (DRYDOCS_LOGDIR chain) as the review surface.
    notes: "Groomed 2026-07-29 (Epic V). SME-facing: operate + verify, not API docs."
  - id: V3
    epic: sme-runbooks
    title: "SME runbook: drydocs-load — loaders, CLI chain, snapshots; rule the overlap with the system-level startup-refresh runbook"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop). RULED AUTHOR-DISTINCT: declaring the startup/refresh runbook this module's runbook would have needed it NARROWED (it owns Docker and provisioning, which are not drydocs-load) or this module WIDENED to own the container (which is not either) — neither is honest, so the two are distinct and cross-reference. The division in one line: startup/refresh answers 'run the chain', docs/design/drydocs-load-runbook.md answers 'run, read and repair ONE loader'. THE N6 CONSTRAINT WAS THE DESIGN DRIVER and the item's own note demanded it: this runbook restates NO sequence. Two hand-maintained copies already exist (ingest.sh, Appendix B) and N6 exists to retire them; a third would make that job worse. It points at cli.CANONICAL_LOAD_SEQUENCE and ships the one-liner that prints it (run before commit). The justification is not fastidiousness — it is an incident from the SAME SESSION: V2's runbook copied the topology database list inline and X1 retired ddlineage hours later, so that copy was stale before the day ended (corrected to a read of dev-environment.yaml at this item's claim). Covers what the cold start does not: the run envelope and status items, rejected rows, idempotency, the soft-delete/sweep-removed split with the warning that a source OUTAGE looks identical to a deletion, snapshots, docs-verify as a cheap NEO4J_DATABASE check. Troubleshooting carries the live constraint-violation shape T23 is hitting company-side and the open C24 sparse-refresh caveat. Every claim verified against code/CLI before writing. drydocs-load off RUNBOOK_PENDING (6 remain). Suite 1537 green.
    depends_on: [V1]
    inputs: [drydocs/, docs/design/drydocs-startup-refresh-runbook.md, docs/design/templates/runbook.outline.yaml]
    acceptance: >
      Per V1's disposition: EITHER the startup-refresh runbook is declared the
      drydocs-load runbook (then this item brings it current with module
      framing + a coverage note, Rev bump per the governed-render rules) OR a
      distinct docs/design/drydocs-load-runbook.md is authored (outline-
      conformant, rendered, stale-render-clean) and the two cross-reference
      without duplicating the load sequence — N3's declared canonical sequence
      is the single source once it lands. drydocs-load off RUNBOOK_PENDING.
    notes: >
      Groomed 2026-07-29 (Epic V). Coordinate with N6 (the two disagreeing
      load sequences) — this runbook must not become a third one.
  - id: V4
    epic: sme-runbooks
    title: "SME runbook: drydocs-review — gate pages, graph verify, review labels, publishing flow"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [drydocs/graph_review.py, drydocs/graph_verify.py, drydocs/review_labels.py, drydocs/gate_pages.py, drydocs/publishing/, docs/design/templates/runbook.outline.yaml]
    acceptance: >
      docs/design/drydocs-review-runbook.md (outline-conformant, rendered,
      stale-render-clean), drydocs-review off RUNBOOK_PENDING. Covers: generating
      a gate page from a gate prompt, the no-write-until-confirmed contract,
      graph_verify/review_labels runs and where their output lands, and the
      Confluence publishing path (mechanism only — base URL as config).
    notes: "Groomed 2026-07-29 (Epic V)."
  - id: V5
    epic: sme-runbooks
    title: "SME runbook: drydocs-docgen — design-doc render chain, outline validation, the L5/L6 feedback loop"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [scripts/render_design_doc.py, scripts/render_board.py, docs/design/templates/, docs/design/feedback/]
    acceptance: >
      docs/design/drydocs-docgen-runbook.md (outline-conformant, rendered,
      stale-render-clean), drydocs-docgen off RUNBOOK_PENDING. Covers: the
      md-is-source / html-is-render contract, the default-paths board render
      and everything that rides it (gates.json, enforcement matrix — J17/J20),
      the stale-render check, and both feedback halves (L5 save-button export,
      L6 scan + transcribe-doc-markup) back to anchor-keyed YAML.
    notes: "Groomed 2026-07-29 (Epic V). This one documents the machinery the other runbooks are rendered BY — author it early."
  - id: V6
    epic: sme-runbooks
    title: "SME runbook: drydocs-lineage — module-wide operate surface; index or absorb the chain-scoped lineage-mac and cmdline-resolution runbooks"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [drydocs_lineage/, docs/design/drydocs-lineage-mac-runbook.md, docs/design/drydocs-cmdline-resolution-runbook.md]
    acceptance: >
      Per V1's disposition: a module-level drydocs-lineage runbook exists that
      either absorbs or explicitly indexes the two chain-scoped runbooks
      (lineage-mac, cmdline-resolution) so an SME landing on the module finds
      one entry point; outline-conformant, rendered, stale-render-clean;
      drydocs-lineage off RUNBOOK_PENDING. No procedure duplicated across the
      three — chain runbooks keep their depth, the module runbook routes.
    notes: "Groomed 2026-07-29 (Epic V)."
  - id: V7
    epic: sme-runbooks
    title: "SME runbook: drydocs-remediation — detector, transform engine, corroboration, Jira handoff"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [drydocs_remediation/, docs/design/templates/runbook.outline.yaml]
    acceptance: >
      docs/design/drydocs-remediation-runbook.md (outline-conformant, rendered,
      stale-render-clean), drydocs-remediation off RUNBOOK_PENDING. Covers: the
      two-tier fix model, running the R1 detector, the transform engine's
      before/after artifacts, corroboration, and the SoD boundary (we analyze,
      dev implements) at the Jira handoff. Mechanism-only — real instances
      stay internal/.
    notes: "Groomed 2026-07-29 (Epic V)."
  - id: V8
    epic: sme-runbooks
    title: "SME runbook: drydocs-api — server run + mappings API; rule the overlap with the mapping-store runbook"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop). RULED AUTHOR-DISTINCT, not extend: the mapping-store runbook scopes HTTP serving OUT in its own words, so widening it would have meant reversing its stated scope, and the mapping-demo runbook covers /demo — one route of twenty-two. Either extension produces a document whose title no longer describes it, so docs/design/drydocs-api-runbook.md is new, outline-conformant, rendered, stale-render-clean. Covers the three things the acceptance names: starting the thin API (the optional [tool.poetry.group.api] install and the --factory flag are both real first-run failures), the read-vs-write surfaces (every route classified by whether it needs Neo4j; the S4 draft buffer is the ONE write and its survives-rebuild / dies-on-delete asymmetry is stated), and loader-stays-the-only-graph-writer promoted to THE operating rule with its three enforcement layers listed under Verify — because an operator's job is to confirm they are still in place, not to assume it. Every factual claim re-derived from the code before writing, and the route table ships with the one-liner that regenerates it plus the rule that the code wins on disagreement. V1's guard did its job unprompted: adding the file failed FOUR tests at once (undispositioned runbook, drydocs-api still on the shrink-only pending list, two state checks) — the coverage rule catching its own successor item, which is the behaviour V1 was built for. drydocs-api off RUNBOOK_PENDING; the mapping-demo disposition reason updated from 'V8 rules whether...' to the ruling. Suite 1537 green.
    depends_on: [V1]
    inputs: [drydocs_api/, docs/design/drydocs-mapping-store-runbook.md]
    acceptance: >
      Per V1's disposition: EITHER the mapping-store runbook is extended to be
      the drydocs-api module runbook (Rev bump) OR a distinct
      drydocs-api-runbook.md is authored; outline-conformant, rendered,
      stale-render-clean; drydocs-api off RUNBOOK_PENDING. Covers: starting the
      thin API, its read-only vs store-write surfaces, and the
      loader-stays-the-only-graph-writer contract as the operating rule.
    notes: "Groomed 2026-07-29 (Epic V)."
  - id: V9
    epic: sme-runbooks
    title: "SME runbook: drydocs-agents — ADK service venv, run, console wiring, agent smoke checks"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [agents/, docs/design/templates/runbook.outline.yaml]
    acceptance: >
      docs/design/drydocs-agents-runbook.md (outline-conformant, rendered,
      stale-render-clean), drydocs-agents off RUNBOOK_PENDING. Covers: the
      module's own venv (pinned google-adk per R10), starting the service,
      wiring the console to it (VITE_ADK_URL, names only), the read-only
      contract, and a smoke check per registered app with expected output
      shape — including the ADK-unreachable fallback behaviour the console
      surfaces (SYNTHESIZED demo trace + banner).
    notes: "Groomed 2026-07-29 (Epic V)."
  - id: V10
    epic: sme-runbooks
    title: "SME runbook: drydocs-web — audit the existing web-console runbook current (post O35-O41, FB-03/FB-04 access model)"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs: [docs/design/drydocs-web-console-runbook.md, web/]
    acceptance: >
      The web-console runbook is audited against the shipped state and Rev-bumped
      where stale: category-first landing (O35), the FB-03 page-role/access
      designations, the standalone agent-test page's build/ship path (FB-04
      re-ruling), and current build/verify commands; rendered, stale-render-clean;
      drydocs-web marked COVERED in V1's map.
    notes: >
      Groomed 2026-07-29 (Epic V). COORDINATE: a parallel session is actively
      working the standalone agent-test page — audit only after its stream
      lands; do not describe in-flight state.

  # --- 2026-07-30 weekly groom: the R5 follow-up trio (R11-R13), the J16 guard's
  # --- tracked-only blind spot (J22), and two time-parked infra chores whose
  # --- triggers fired today (G49 MCP verify — port review done; G50 rollback
  # --- container — the neo4jtest probation week is up).
  - id: J22
    epic: release-infrastructure
    title: "J16 manifest-coverage guard also sweeps untracked-but-not-ignored paths: a new file must fail the suite BEFORE commit, not after"
    type: bug
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done          # claimed + closed 2026-07-30 (desktop session, pushed claim before work)
    depends_on: []
    inputs: [tests/unit/test_port_reconcile_guards.py, PORT-MANIFEST.yaml]
    acceptance: >
      test_no_tracked_path_falls_through_silently (or a sibling) widens its walk
      from `git ls-files` to ALSO include `git ls-files --others --exclude-standard`,
      so a new not-yet-added file with no manifest disposition fails the PRE-commit
      run — same fall-through semantics, wider walk. A test pins the behaviour
      (fixture or monkeypatched file list). Verified against false positives on
      legitimately transient untracked files (e.g. scratch at repo root): either
      they resolve via existing manifest rows/allowlist or a scoped rule is added
      WITH a written reason — no silent widening of the allowlist. Full suite green.
    notes: >
      Groomed 2026-07-30 from the 2026-07-29 [bug] inbox line. Live incident: the
      N5 session ran the suite green pre-commit, committed docs/plan/load-map.html,
      and the very NEXT full run failed the guard — the defect window is "on main
      until someone runs the suite again", which inverts the guard's purpose.
      CLOSED 2026-07-30 (desktop): the guard's walk is now tracked + `git ls-files
      --others --exclude-standard`; orphan computation extracted pure
      (fall_through_orphans) and pinned by test_untracked_new_file_is_caught_before_add
      (dispositioned path clean / new undispositioned sibling caught / allowlisted
      transient clean). Untracked orphans are tagged [untracked] in the failure
      message with the third legitimate resolution named (.gitignore — local-only
      by intent); no allowlist entries added or widened. Live-proven both ways: a
      probe file under docs/plan/ failed the guard BEFORE git add and the clean
      tree passes (zero untracked orphans today = no false positives). Suite 1188.
  - id: J23
    epic: release-infrastructure
    title: "Classification collapses to three levels (External / Internal-Public / Internal): drop the Internal-Confidential tier everywhere, publish boundary unchanged"
    type: chore
    module: config
    phase: 8
    agent: main
    model: sonnet
    priority: p1
    status: done          # claimed + closed 2026-07-31 (desktop session, pushed claim before work)
    depends_on: []
    inputs: [config/classification.yaml, config/source-registry.yaml, config/doc-source-registry.yaml, tests/unit/test_classification.py, tests/unit/test_doc_registry.py, PUBLISH-BOUNDARY.md, internal/registry-redesign/REGISTRY-PLAN.md]
    acceptance: >
      config/classification.yaml defines exactly three levels (External,
      Internal-Public, Internal) with the publish rule unchanged (publishable =
      External + Internal-Public); no entry in config/source-registry.yaml or
      config/doc-source-registry.yaml carries Internal-Confidential (relabels
      to Internal, keeping a confidential-handling comment where rosters/SIDs/
      SEAL values warrant it); test_classification.py + test_doc_registry.py
      updated (CURATION_BY_TIER T4 becomes sme-confirm — the +confidential
      rider retires with the level); the internal/ capture READMEs
      (fcdo-reference, registry-redesign) restate Internal; PUBLISH-BOUNDARY.md
      records the collapse and that a fourth tier can return if a real handling
      difference ever materializes. Full suite green.
    notes: >
      Groomed 2026-07-31 from the same-day inbox chore — registry-plan Phase 1
      (internal/registry-redesign/REGISTRY-PLAN.md §Classification). The
      DECISION is a recorded user ruling 2026-07-31 ("for now leave as External
      or Internal Public, or Internal") — not a gate question: the tier bought
      handling ceremony, not a different publish behavior. Deliberately allowed
      to land ahead of the N7 schema-v2 gate — it removes machinery, adds none.
      ADDED RATIONALE (user, 2026-07-31 pm): Internal-Confidential actively
      PREVENTS storing relevant information — its pointer-never-the-data
      discipline pushes real values (SEAL ids, object names, report
      coordinates) out of the repo entirely, so the internal/ twins stay
      thinner than the work needs. After content moves under internal/, its
      classification changes to Internal and the real values may live there
      in-tree — the private repo + publish boundary is the protection, not
      the extra tier. Per-source out-of-tree rules (rua bundles, view
      exports) survive where SIZE or PII justifies them — noted per source,
      no longer tier-forced. CLOSED 2026-07-31 (desktop): classification.yaml
      = exactly 3 levels (Internal's description absorbs the confidential
      material + the handling-note rule); 7 source-registry + 1 doc-registry
      entries relabeled Internal with confidential-handling comments kept;
      3 taxonomy captures (business-application, oracle-schemas,
      lob-product-team) relabeled; test_classification level set +
      test_doc_registry CURATION_BY_TIER T4=sme-confirm updated; CLAUDE.md §3
      + PUBLISH-BOUNDARY.md + config/README.md tables collapsed to 3 rows
      with the tier-can-return note; fcdo-reference README/ALIGNMENT-PLAN
      headers restated Internal; live code/test docstrings reworded
      (9 files); gate-log/gate-prompts/whitepaper/port archives left as
      HISTORY (records of what was signed, not live guidance). Renders
      refreshed (load-map + enforcement matrix pick up the new labels).
      Suite 1188.

  - id: J24
    epic: release-infrastructure
    title: "Retired Internal-Confidential residual sweep: clear the tier from FORWARD-LOOKING specs (UI type union, skill routing tables, gate-prompt template) and guard against regression"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-08-01 (desktop) — see the CLOSE block appended to notes
    depends_on: [J23]
    inputs: [UI-WIP/site-plan.md, UI-WIP/wf-admin-config-01.md, .claude/skills/data-context-extractor/references/platforms.md, .claude/skills/controlm-runbook-automation/references/fix-package.md, config/gate-prompts/bmc-docs-example.yaml, tests/unit/test_classification.py, drydocs_api/query_specs.py, drydocs_api/exports.py, drydocs_api/ephemeral_specs.py, tests/unit/test_query_specs.py]
    acceptance: >
      No surface that would ASSIGN or IMPLEMENT a classification still offers
      Internal-Confidential as a value: UI-WIP/site-plan.md's classification
      union type drops the fourth member (and its banner rule + the matching
      wf-admin-config-01.md rule read against the 3-level vocabulary), the two
      skill reference tables that route material by tier
      (data-context-extractor/references/platforms.md,
      controlm-runbook-automation/references/fix-package.md) relabel to
      Internal keeping the confidential-handling note, and the
      bmc-docs-example.yaml gate-prompt TEMPLATE stops modelling the retired
      value. The LIVE implementation goes with them: drydocs_api's
      CLASSIFICATIONS vocabulary drops the retired member, the two specs still
      carrying it (ownership.teams.v1, ownership.attributions.v1) relabel to
      internal, and exports.py's filename-prefix map + banner tuple stop
      emitting an `INTERNAL-CONFIDENTIAL__` file for a tier that no longer
      exists. ephemeral_specs.py's EPHEMERAL_CLASSIFICATION is a FAIL-CLOSED
      CEILING, not a label — it must be re-pointed at the most restrictive
      SURVIVING level (internal), never simply deleted, and its docstring
      restated so the ceiling's intent survives the rename.
      HISTORY is left untouched and named as such in the close note:
      config/gate-log.md, signed-off gate prompts, `done` backlog close-notes,
      SDLC-Docs/extracted/ corpus, IDEAS.md audit trail. test_classification.py
      gains a regression guard that fails if the retired token reappears in an
      explicit list of live-vocabulary surfaces (the guard names its own scope,
      so history files can never be swept in by accident). Full suite green.
    notes: >
      NOTED 2026-08-01 (C17 session, judgement call left to this item): the
      history carve-out says "signed-off gate prompts", and an UNSIGNED prompt
      sits in neither bucket — seal-app-ref-edge-reshape.yaml's header prose
      still calls its evidence captures Internal-Confidential. It is a
      description of sensitivity rather than a spec that assigns a tier, so it
      was left alone rather than swept in during an unrelated item; rule it
      either way when this item runs, and say which.
      Groomed 2026-07-31 (weekly groom) as the residual of J23, which closed
      the same day. J23 deliberately scoped itself to config + the boundary
      docs + the two tests, and correctly left history alone — but these files
      are neither: they are specs an agent or a future build READS to decide
      what tier something gets, so they would re-introduce the fourth value
      into real code (site-plan's union is a TypeScript enum in waiting). Not
      a gate: J23's ruling is already recorded (user, 2026-07-31) and this
      applies it, deciding nothing new. The backlog's own live carrier (M3's
      acceptance) was fixed inline at this groom, so it is deliberately NOT in
      this item's scope. .gitignore's two explanatory comments are cheap to
      reword and may ride along; they assign nothing, so they are not
      acceptance-bearing.
      WIDENED 2026-08-01 (found while auditing the registry for O27, NOT a new
      decision — it applies the same recorded J23 ruling). The drydocs_api
      surfaces were missed TWICE: J23 scoped itself by an inputs list that
      omitted them, and this item, groomed expressly to catch J23's leftovers,
      inherited the same omission — its acceptance sentence already claimed
      "no surface that would ASSIGN or IMPLEMENT a classification", which the
      API both does. Worth stating plainly because it corrects this item's own
      premise: the note below calls these "specs in waiting" and cites
      site-plan's union as "a TypeScript enum in waiting", but drydocs_api is
      not waiting — two registered specs carry the retired tier TODAY and a
      real export of either writes a file prefixed INTERNAL-CONFIDENTIAL__ and
      a banner naming a level config/classification.yaml no longer defines.
      That makes this a live user-visible inconsistency, not dormant text.
      Module stays `docs` (the bulk is still docs/skills/templates) but the
      closer should expect a live Python slice + test, not a docs-only pass.
      Root cause worth carrying: the API held a SECOND, independent copy of the
      classification vocabulary that nothing checks against config — so the
      regression guard is most valuable if it asserts the two agree, rather
      than just scanning for a token.
      CLOSED 2026-08-01 (desktop). Live code first, because that is where the
      inconsistency was user-visible: drydocs_api CLASSIFICATIONS dropped the
      retired member, the two specs (ownership.teams.v1, ownership.attributions.v1)
      relabelled to `internal` — teams.v1's description gained the confidential-
      handling note it had been carrying implicitly in its tier — and exports.py's
      prefix map + banner tuple stopped emitting INTERNAL-CONFIDENTIAL__ for a
      level config no longer defines. A THIRD and FOURTH copy of the vocabulary
      turned up that neither J23 nor this item's inputs listed: web/src/explorer/
      SpecGrid.tsx's isInternal test and web/src/routes/AskRoute.tsx's hardcoded
      client-side mirror of the ephemeral ceiling — the browser was labelling
      agent-generated sources with a tier the server had stopped stamping.
      The ephemeral ceiling was RE-POINTED, not deleted, per the acceptance:
      EPHEMERAL_CLASSIFICATION = 'internal' with the docstring restated so the
      RULE ("the most restrictive level the vocabulary currently defines") is what
      survives, not the value — plus an explicit instruction to re-point in the
      same commit if a fourth tier ever returns. tests/unit/test_ephemeral_specs.py
      keeps pinning the export literals on purpose (they prove the ceiling still
      produces a restricted artifact), so moving it stays a deliberate edit.
      THE UNSIGNED-PROMPT JUDGEMENT CALL, RULED — and the C17 note that raised it
      was working from incomplete evidence, which is why the ruling goes the other
      way: seal-app-ref-edge-reshape.yaml is not only descriptive. Its `Registry
      ref:` line ASSIGNS a tier to a source-registry entry that does not exist yet
      ("classification Internal-Confidential company-side"), so a build following
      it would write a dead value. Swept. The same sweep then had to cover the two
      OTHER unsigned prompts nobody had listed — email-dl-contact-point.yaml and
      rua-load-shapes.yaml (both `open`, gate page rendered, session not yet run) —
      and rua-load-shapes needed care rather than replacement: its clause F1 asks
      the SME to rule WHICH PROPERTIES are internal-confidential, a question whose
      premise (two tiers to sort properties between) J23 removed. The clause was
      NOT re-decided; it carries an explicit "PREMISE CHANGED SINCE DRAFTING
      (flagged at J24, NOT ruled)" note telling the session to re-frame the output
      as a confidential-handling note plus an export rule. Signed and recorded
      prompts were left alone.
      Scope rule used, stated so the next sweep can reuse it: every occurrence in
      config/ that is not gate-log.md and not a SIGNED prompt. That is crisper than
      the inputs list and it caught the three config/taxonomy header comments and
      two taxonomy-ontology-map lines still naming the retired tier one screen above
      the `classification:` fields J23 had already corrected. .gitignore's two
      comments rode along as predicted. DELIBERATELY NOT SWEPT and left as history:
      config/gate-log.md, the five signed/recorded gate prompts, `done` close-notes
      in this file, SDLC-Docs/extracted/, IDEAS.md's audit trail, docs/design/*
      governed renders, docs/whitepaper, knowledge/, internal/, and the prose plan
      docs — none assign a tier, and rewriting them would falsify the record.
      GUARD (4 tests): the vocabularies must AGREE (api vs config — the root cause,
      and the only one of the four that survives the NEXT vocabulary change); every
      registered spec carries a live level; the ephemeral ceiling names an
      unpublishable level; and a token scan over an EXPLICIT 13-file list of live
      surfaces. The scan names its own scope on purpose — a repo-wide version would
      either fail forever or create pressure to rewrite history to make it pass, so
      an explicit list can only be too SMALL, which shows up as a missed sweep.
      It earned its keep immediately, catching two of my own replacement comments
      whose line-wrap put the J23 marker on the previous line. Suite 1336 green,
      ruff check + format clean, tsc + vite build clean.
      NOT DONE, deliberately: .gitignore still names the real org in an adjacent
      comment (pre-existing, and half-redacting one line while the next keeps the
      internal domain would be worse than leaving it) — inboxed separately.

  - id: J25
    epic: release-infrastructure
    title: "Author the paste-ready L7 ratification gate-log snippet producer-side (discharges cross-repo tracker T11)"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [docs/port-prompt.md, config/gate-log.md]
    acceptance: >
      A copy-paste-ready ratification entry for the company gate-log exists as a
      committed fenced block reachable from docs/port-prompt.md (beside the §6
      Tier-A doctrine or in the T11 tracker row), carrying all four elements
      that doctrine requires — subject (6 doc_* edges + 6 doc node classes
      active, doc-traceability-feedback confirmed), the producer gate reference
      (gate 0252d29 via PORT-REPORT-6fd3270), the checks the port performed,
      and a sign-off line — with the company-side fill-ins marked as
      placeholders rather than invented. The T11 tracker row points at it
      instead of at a past chat session.
    notes: >
      Groomed 2026-07-31 (weekly groom) from the 2026-07-24 inbox chore. The
      gap is durability, not content: port-prompt.md records that the snippet
      was "provided in the producer session 2026-07-21" and §6 describes the
      four required elements, but the block itself was never committed — a
      repo-wide search for 0252d29 / PORT-REPORT-6fd3270 finds only prose
      references. So the company gate pack cites an artifact that no company
      session can actually open, and T11 has read `pending` since 2026-07-21.
      Producer-side authoring only: the entry LANDS in the company gate-log at
      their next port, and their sign-off remains theirs (producer sign-off is
      never company sign-off).

  - id: R11
    epic: agentic-qa
    title: "Ask spoke LLM-in-the-loop smoke on the agents-venv machine: two live questions through the real graph_qa streaming path"
    type: chore
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs: [agents/, web/src/ask/, web/src/routes/AskRoute.tsx, docs/decisions/0007-agentic-qa-architecture.md]
    acceptance: >
      On the machine where agents/.env exists (the R2 live-smoke precedent), with
      real drydocs-api + Neo4j + the real ADK service: two free-text questions
      asked through the /ask route as a non-admin persona. Verified live: step
      events STREAM during the run (arrive before the answer envelope, not
      buffered), the answer renders citations with trust tier + classification,
      at least one step's explore_ref opens in Explorer via the ephemeral spec,
      and the metrics chip populates (time, tokens, iterations). Close note names
      machine, container, and database (the J18 convention).
    notes: >
      Groomed 2026-07-30 from the R5 close's own follow-up (inboxed at the R5
      commit): desktop e2e was wire-level via stub ADK because that machine has
      no agents venv — the LLM-in-the-loop half of the path is the one thing R5
      could not verify.
  - id: R12
    epic: agentic-qa
    title: "Promote the R5 stub-ADK harness into a committed fixture: live-verify the web Ask wiring with no LLM key"
    type: task
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: [web/src/lib/adk.ts, web/src/ask/, web/src/routes/AskRoute.tsx, drydocs_api/query_specs.py]
    acceptance: >
      The stub-ADK harness (FastAPI serving /run_sse SSE with control-part
      handling and REAL ephemeral-spec registration against drydocs-api) is a
      committed, reusable fixture — placement decided and recorded (tests/
      integration vs a dev-fixture script) — with a documented invocation. A
      repeatable check (integration test, or a documented manual flow if live
      services are required) drives the Ask-spoke wiring against it end to end
      with NO LLM key. Full suite green; the stub never ships in web/dist.
    notes: >
      Groomed 2026-07-30 from the R5 follow-up [idea]: the R5 session's scratchpad
      stub proved the wiring live without an agents venv — worth owning instead of
      re-inventing per session. Re-author from the R5 session record if the
      scratchpad copy is gone; the design (SSE shape + control part + registration)
      is documented in the R5 close note.
  - id: R13
    epic: agentic-qa
    title: "ADK session growth from step events: check whether ADK 2.x offers a partial/non-persisted event mode for the step stream"
    type: task
    module: drydocs-agents
    phase: 15
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs: [agents/, docs/decisions/0007-agentic-qa-architecture.md]
    acceptance: >
      A written finding (ADR 0007 notes or agents/ docs): does google-adk 2.x
      support yielding events that are NOT persisted into session history
      (partial=True or equivalent) suitable for the per-step stream? If YES:
      either applied — long Ask sessions stop inflating memory_events/memory_chars
      (shown flat via the R3 metrics across a multi-question session) — or a
      follow-up item groomed with the applied-change scope. If NO: a dated note
      confirming the R3-telemetry-as-tripwire design stands (ADR 0007's
      DB-backed-session-store-before-soak clause), nothing else changed.
    notes: >
      Groomed 2026-07-30 from the R5 follow-up [question]: agent.py yields one
      event per pipeline step and ADK persists yielded events, so step streaming
      trades session-memory growth for UX — deliberate, telemetered, but worth
      one look at the 2.x event API before any soak.
  - id: R14
    epic: agentic-qa
    title: "agents/common shows up in the ADK app list, though it is a shared-tools package and not an app"
    type: chore
    module: drydocs-agents
    phase: 15
    agent: main
    model: haiku
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - agents/common/
      - agents/README.md
    acceptance: >
      ADK app discovery (the /list-apps surface) lists only the real agent apps —
      graph_qa, core_ingest, controlm_fix, graph_query — and not the shared-tools package
      `common`, by whichever of hiding or restructuring the ADK supports WITHOUT moving
      the shared tooling out of import reach of the apps that use it (tests/conftest.py
      closes common.neo4j_tool's driver singleton by module name, so the import path may
      not silently change); agents/README.md states the convention so the next shared
      package is not created as an app by accident.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-1, the OLDEST entry in the inbox (2026-07-03) —
      cosmetic, deferred at four grooms, and small enough that carrying it costs more than
      doing it. Haiku with a hard constraint: the import path is load-bearing for the
      session-teardown hook, so "restructure" may not mean "move it somewhere the apps
      cannot import".
  - id: G49
    epic: component-topology
    title: "Verify the neo4j-drydocs MCP server actually works post-APOC-fix, and rule the GDS disposition (available vs earns a seat)"
    type: chore
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-10 (desktop, container neo4jtest, database drydocs — J18). FINDING: the server was ABSENT from every config scope (user ~/.claude.json held only graphacademy) — 'verify it works' was unfalsifiable until re-registered. Re-registered user-scope (stdio, neo4j/mcp:latest — note: the image is neo4j/mcp, the item's mcp/neo4j spelling was reversed; env-passthrough form so the password lives in the config, host.docker.internal URI so the container reaches the host bolt). Live MCP stdio smoke: tools [get-schema, list-gds-procedures, read-cypher, write-cypher]; read-cypher returned total 1447 / live 1434 :CodeModule — exactly the 2026-08-10 load. Port re-confirmed from docker port neo4jtest: 7474+7687 both mapped. GDS: 423 procs live (item said 471 — version drift, recounted), nothing calls them: AVAILABLE, UNUSED; Epic R benchmark remains the seat candidate, and the MCP server exposes list-gds-procedures when it does.
    depends_on: []
    inputs: [config/dev-environment.yaml, drydocs_core/schema/provisioning/]
    acceptance: >
      The neo4j-drydocs MCP server (user-scope ~/.claude.json, stdio,
      mcp/neo4j:latest, NEO4J_DATABASE=drydocs) connects and returns a live query
      against the freshly reloaded graph — a nonzero :CodeModule count consistent
      with the latest load is the marker. The port is re-confirmed from
      `docker port neo4jtest` (the container was RECREATED — never trust the
      config value). Second, in the same pass: a recorded disposition for GDS
      (471 procs, newly present, nothing calls it) — either an inbox/backlog line
      seating it (Epic R graph-retrieval benchmark is the candidate) or an
      explicit "available, unused" note. Close note names machine + container +
      database (J18 convention).
    notes: >
      Groomed 2026-07-30 from the 2026-07-28 parked [chore] — parked "until after
      the port review", which completed (PORT-REPORT-e60822fc; port-prompt rolled
      2026-07-30). Why it needs verifying at all: APOC was silently absent from
      neo4jtest for weeks (fixed 33cfc68 — plugins now a mounted volume), so the
      server CANNOT have worked in that window and has never been checked since.
      Machine-local (this desktop) — the config lives outside the repo.
  - id: G50
    epic: component-topology
    title: "Delete the rollback container neo4j-drydocs-ee + its anonymous volumes; verify-then-prune the four orphan neo4j volumes"
    type: chore
    module: graph-infra
    phase: 6
    agent: main
    model: haiku
    priority: p3
    status: done   # closed 2026-08-10 (desktop — J18). PREMISE DRIFTED, RESOLVED ON EVIDENCE: neo4j-drydocs-ee did NOT exist (docker inspect: No such object — removed at some earlier point) and none of the four NAMED volumes (neo4j_data/neo4j_logs/neo4j2_data/neo4j2_logs) existed either. What did: 5 anonymous volumes + neo4j-testdata. Attachment-verified EVERY one (docker ps -a --filter volume=): neo4j-testdata -> neo4jtest /data KEEP; 451ffc86 -> neo4jtest /logs KEEP; four dangling attached to NOTHING (20a8acc6+55bc4913+e93d8d98 created 07-07 = the ee era; e059635e 07-16 15:29 = the logs volume two minutes before neo4jtest current 15:31) — all four removed, with a per-volume re-check immediately before each rm. neo4jtest untouched (Up, both mounts intact). Out of scope, noted to IDEAS: stopped container MCP (exited 5wk, no volumes).
    depends_on: []
    inputs: [config/dev-environment.yaml]
    acceptance: >
      Container neo4j-drydocs-ee (stopped, restart=no) and its two anonymous
      volumes are removed. The four orphan volumes (neo4j_data, neo4j_logs,
      neo4j2_data, neo4j2_logs) are each VERIFIED attached to nothing before
      any prune — pruned, or kept with a written reason. Close note carries the
      docker ps -a / docker volume ls evidence and names the machine (J18
      convention). neo4jtest is untouched.
    notes: >
      Groomed 2026-07-30 from the 2026-07-23 [chore]: the user decision was
      "delete once neo4jtest has survived a week of normal use" — the week was
      up ~2026-07-30 and neo4jtest has been the live container throughout
      (including the APOC fix + full reload). Destructive but pre-decided;
      the only judgment left is the orphan-volume verification. The password
      chore merged into this line 2026-07-28 retires with the container.
      Machine-local (this desktop).
  - id: G52
    epic: component-topology
    title: "Laptop Docker cleanup: the superseded 5.26 images, the eleven orphan neo4j volumes, and the retired neo4j-drydocs-ee data volume"
    type: chore
    module: graph-infra
    phase: 6
    agent: main
    model: haiku
    priority: p3
    status: done
    depends_on: []
    inputs: [config/dev-environment.yaml, docs/design/drydocs-startup-refresh-runbook.md]
    acceptance: >
      Every artifact VERIFIED unreferenced before removal (contents inspected, not
      just link counts); neo4jtest and its three live volumes untouched; the graph
      re-checked online afterwards. Close note carries the docker evidence and names
      the machine (J18).
    notes: >
      DONE 2026-08-04 on the LAPTOP. This is NOT G50 — G50 is the desktop's
      equivalent and stays open; the two machines hold independent Docker state.
      Removed: (1) images neo4j:5.26-enterprise, neo4j:5.26, neo4j:5.26-ubi10,
      ~3.3GB, none referenced — dev-environment.yaml pins 2026.05.0-enterprise and
      BOTH integration tests read that tag out of it, so nothing could have used
      them; (2) container awesome_williams (Exited 1 — a Docker Desktop run with no
      NEO4J_ACCEPT_LICENSE_AGREEMENT) with its two empty anonymous volumes;
      (3) eleven zero-link volumes, ~3.5GB. Verification was by MOUNTING each, not
      by trusting Links=0: neo4j_data / neo4j2_data / 6004d1bb / 7e41bea3 held only
      default neo4j+system stores dated 2026-06-29, and six small ones were log
      directories. The eleventh, anonymous e335b5d8 (1.353GB), held the real
      topology — drydocs/ddlineage/ddcontext dated 2026-07-02 — i.e. the DATA
      VOLUME of the retired neo4j-drydocs-ee, the rollback copy the runbook
      described. It outlived its container (the container itself is long gone from
      this machine). Flagged to the user as materially different from scratch and
      deleted on their explicit ruling; it was a month stale and strictly poorer
      than neo4j-testdata (predates ddschema entirely).
      Consequence, and why the runbook moved with it: NO rollback copy now exists on
      the laptop, so Appendix A's rollback paragraph was false here — runbook Rev 8.
      Final state: 3 images / 1 container / 3 volumes, 0B reclaimable; drydocs 2218
      nodes and all seven databases online after the prune (laptop, neo4jtest).

  - id: G53
    epic: component-topology
    title: "Contingency plan: fold every database into one if multi-DB is ever unavailable"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: opus
    priority: p3
    status: done   # closed 2026-08-10 (desktop, 04:07 session). ADR 0011 (its own record — 0002 stays ACCEPTED, nothing re-opened), status PLANNED, execution SME-gated. All four clauses answered with mechanisms: (1) trust boundary = :Uncertain label namespace + mandatory reliability property (existence-constraint where available, S10-pattern writer refusal on true Community) + three guards incl. a structural WHERE NOT n:Uncertain generated into the spec registry — and the loss stated plainly: physics -> discipline, with the residual double-fault window named; (2) ddschema exemplars fold by moving REAL labels into describes_label property (NODE KEYs never see them; dropping the keys rejected in advance on the T23 crash class); (3) ddall consumer enumeration = full grep sweep (query_specs SPEC/WATERMARKED sets, the two ddcontext literals at 833/871, exports.py:297, routing.py review rule, cli.py verbs, deepdoc DATABASE, schema_graph.py, docs_coverage) each with its re-key — invariant: trust signal moves from WHERE a query runs to WHAT it matches; (4) dev-environment.yaml databases map = the single switch (one map serves both topologies) with two named preconditions (the two spec literals through the map; a topology guard for the half-folded case). No provisioning/loader/schema change landed.
    depends_on: []
    inputs:
      - docs/decisions/0002-component-database-topology.md
      - drydocs_core/schema/provisioning/01_databases.cypher
      - drydocs_core/schema/schema_graph.cypher
      - config/dev-environment.yaml
    acceptance: >
      A written consolidation plan (an ADR 0002 amendment or its own ADR) that answers each
      of these with a concrete mechanism rather than an intention: (1) HOW THE TRUST BOUNDARY
      SURVIVES — ADR 0002 D1 rejected the single-DB option because it commingles trust, so
      name the label namespace, the trust property, and the guard tests that would replace the
      transaction domain, and state plainly what protection is lost (the ADR's own words:
      multi-DB moves the guarantee "from discipline to physics", and one DB moves it back);
      (2) HOW ddschema's EXEMPLAR NODES COEXIST WITH drydocs' NODE KEYs — exemplars carry real
      labels WITHOUT the properties those keys require, which is the stated reason ddschema is
      its own database (G51), so this is a hard constraint conflict, not a naming collision;
      (3) WHAT REPLACES ddall — an enumeration of every QuerySpec, loader, and console route
      that names a non-default database or federates through the composite; (4) THE CONFIG
      CHANGE POINT — dev-environment.yaml's db-name map, and whether one map can serve both
      topologies so the fallback is a config switch rather than a code fork. Plan only: no
      provisioning, loader, or schema changes land from this item.
    notes: >
      Worst-case contingency, groomed 2026-08-04 on the user's direction after the laptop's
      Neo4j Enterprise 30-day eval trial expired (2026-08-01). THE EXPIRY IS NOT THE TRIGGER
      and is explicitly out of scope — it is a local-testing license matter, the server keeps
      running, and writes were verified working after it (laptop, neo4jtest, drydocs DB: 2218
      nodes, an Enterprise-only existence constraint created then dropped, six user databases
      online). What this plans for is Enterprise becoming genuinely unavailable — a company-side
      edition constraint or a cost ruling — because Community allows exactly ONE user database.
      That would collapse drydocs + ddlineage + ddcontext + ddschema into one store and remove
      the ddall composite entirely.
      ADR 0002 already priced this option and rejected it: "Community single DB + reliability
      property + :Candidate label namespace" is a listed D1 alternative, rejected because it
      commingles trust and "one bad query promotes noise". So this item does NOT re-open the
      decision — ADR 0002 stays accepted. It writes down the fallback we would otherwise have to
      improvise under duress, while there is time to think about it properly. The honest output
      may well be "consolidation costs us X and here is the sequence"; a plan that concludes the
      trust guarantee cannot be preserved in one database is a valid and useful result, provided
      it says so explicitly instead of papering over it.

  - id: G54
    epic: component-topology
    title: "provision.ps1 cannot run as documented on a Docker-only host: it shells out to cypher-shell on the host PATH"
    type: bug
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop) via resolution (a), the preferred one. provision.ps1 detects whether cypher-shell is on the host PATH and, when it is not, falls back to docker cp + docker exec -f, reading the container name from config/dev-environment.yaml (neo4j.container) rather than hardcoding it; -Container overrides, -ForceDockerExec takes the path unconditionally, and the run announces which transport it chose. ONE THING THE ACCEPTANCE DID NOT ANTICIPATE and it would have bitten: inside the container the server is always on bolt 7687, so the HOST port mapping in dev-environment.yaml must NOT be passed through — the fallback pins bolt://localhost:7687 for the exec path and says so, which is the difference between working and failing on any container whose ports were remapped (the retired 7476/7689 one). VERIFIED end to end on the acceptance's own terms (laptop, neo4jtest, 2026.05.0 EE): a plain `provision.ps1 -Password ...` with NO flags, on a host with no cypher-shell, auto-detected, provisioned all five databases + the composite and passed the federated smoke (2218/0/46), exit 0; a bogus -Container fails fast naming the step. J29 respected and now GUARDED, not remembered: two new tests assert the script detects the PATH, resolves the container from config, executes the COPIED file with -f, and contains no Get-Content|docker exec pipe in executable code (comment lines exempt — the script documents the broken form, and a guard that reds on its own warning is useless). Both proven red on a probe first; the FIRST probe silently no-op'd on a wrong indent and passed, which is why the second asserts its anchor. The measured cause is recorded in the script: PS 5.1's [Console]::OutputEncoding carries a 3-byte preamble written ahead of the first line on stdin redirection; -Raw fails identically and neither $OutputEncoding nor a mid-session reassignment fixes it. CORRECTED SAME DAY, after a company host ran the piped form successfully: the BOM injection is HOST-DEPENDENT, not a property of PS 5.1. It happens where the console output encoding carries a preamble (chcp 65001 -> [Console]::OutputEncoding.GetPreamble().Length == 3) and not on a default ANSI-codepage console. The guard and the docker cp choice both STAND — a provisioning script must not work only on the console its author happened to have — but the stated reason was over-general, and a rule stated too broadly gets ignored the first time someone watches it not apply. Script comment, README and test docstring all rewritten to the narrower claim, with the one-liner to check your own host. Header rewritten: the old REQUIRES claimed the Docker image satisfied 'cypher-shell on PATH' (true of the IMAGE, not the PATH), and it now carries the OTHER-ENVIRONMENTS note that the script is portable while the docker run block must be re-authored per side. Suite 1537 (+2).
    depends_on: []
    inputs:
      - drydocs_core/schema/provisioning/provision.ps1
      - config/dev-environment.yaml
      - tests/unit/test_dev_environment.py
      - tests/unit/test_file_encoding.py
    acceptance: >
      A host running the neo4jtest container with NO local Neo4j install can provision the
      topology by following the script's own header. Exactly one of two resolutions:
      (a) the script becomes exec-aware — detect that cypher-shell is absent from the host
      PATH and fall back to `docker cp` of the .cypher file plus
      `docker exec … cypher-shell -f`, reading the container name from
      config/dev-environment.yaml rather than hardcoding it; or (b) the header's REQUIRES
      block stops presenting host cypher-shell as satisfied by "bundled in the Neo4j Docker
      image" — it is bundled INSIDE the image, which is not the host PATH — and documents
      the docker-exec invocation as the supported Docker-host form. (a) is preferred, since
      the header already owns the `docker run` commands and therefore already assumes Docker
      on this path. Either way the fix respects and states the J29 trap: do NOT pipe file
      content into `docker exec`; copy the file in and use `-f`, because a PowerShell 5.1
      pipe injects a UTF-8 BOM that cypher-shell then rejects.
    notes: >
      Reported 2026-08-04 from the desktop M3 reload, where the documented invocation failed
      and the session worked around it with docker cp + docker exec cypher-shell -f.
      VERIFIED at the groom (laptop, static read — no database needed): the REQUIRES block
      at provision.ps1:6 says "cypher-shell on PATH (bundled in the Neo4j Docker image and
      tarball)" while the runner at :57 invokes bare `cypher-shell -a $Uri … -f`. On a
      Windows host whose only Neo4j is the container, that binary is not on PATH and the
      script cannot complete.
      The BOM half of the original inbox report was WRONG and is already closed: J29's
      byte-scan proved every tracked .cypher and .sql is clean, and the BOM came from the
      PS 5.1 pipe rather than from the repo. That is exactly why the trap belongs in this
      item's header text — the workaround shape and the encoding standard are one lesson,
      and J29's own close note says this gap "stays inboxed separately".
      p2 rather than p1: both machines already hold working topologies, so nothing is
      blocked today. What is broken is the documented path for a fresh host.
      WORKAROUND DOCUMENTED 2026-08-04 (does NOT close this item — the script itself is
      unchanged): the provisioning README now carries the explicit per-file docker cp +
      docker exec -f block, verified end to end (laptop, neo4jtest, 2026.05.0 EE — six
      cypher-shell calls exit 0, smoke federates 2218/0/46). It also records the J29 pipe
      trap with the MEASURED cause rather than the inferred one: PS 5.1's
      [Console]::OutputEncoding is UTF-8 WITH a 3-byte preamble that is written ahead of
      the first line on stdin redirection, and neither `Get-Content -Raw`, nor
      $OutputEncoding (already preamble-free), nor reassigning [Console]::OutputEncoding
      mid-session fixes it. That measurement belongs in this item's fix.

  - id: G55
    epic: component-topology
    title: "Apply the G22 vocabulary consequences: the ruled flips, deprecations and supplement blocks — the flips-are-follow-ups half of the gate"
    type: task
    module: ontology
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G22]
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/40-local-controlm.yaml
      - drydocs_core/ontology/relationship_vocabulary/43-local-architecture.yaml
      - drydocs_core/ontology/relationship_vocabulary/10-node-classifications.yaml
      - drydocs_core/ontology/relationship_vocabulary/00-header.yaml
      - drydocs_core/schema/ontology_supplement.cypher
      - config/gate-log.md
      - config/gate-prompts/rua-load-shapes.yaml
      - tests/unit/test_supplements.py
      - tests/unit/test_lineage_writer.py
      - tests/unit/test_schema_graph.py
    acceptance: >
      Every vocabulary consequence G22 ruled is APPLIED, and nothing beyond it.
      Concretely, per §I1: (a) each entry the gate ACTIVATED goes planned →
      active WITH its supplement block written (an entry cannot be active
      without one — that is what active MEANS in 00-header.yaml's lifecycle);
      (b) each NEW meaning the gate accepted lands as a status: planned entry
      drafted via ontology-mapper through the RELATIONSHIP_GUIDE registry flow,
      never active at this build; (c) each entry the gate DECLINED is retired
      with its status set and a note naming the ruling, the date, and what
      replaces it. Known member of (c) at groom time, already ruled 2026-08-06:
      m3_runs_on_etl_host is DECLINED as redundant — ETL placement is the same
      fact as job placement — and retires pointing at m3_runs_on_agent_host /
      m3_runs_on_host_group, with ontology_supplement.cypher's documented role
      enum trimmed from (host_group | agent_host | etl_host) to the two that
      remain. B2 also rides here: m3_invokes.to_node widens to Script|ETLProcess
      with the endpoint recorded per edge (B3's verbatim evidence string is what
      makes the endpoint choice re-checkable). A test asserts no entry is active
      without a supplement, and no entry the gate declined is left at planned —
      the state that would silently read as still-a-candidate. NO LOADER IS
      BUILT here; that is G23.
    notes: >
      Groomed 2026-08-06 (laptop) at the G22 session, on the user's instruction
      to groom the K2/G27 flips-are-follow-ups pattern into an item. WHY IT IS
      SEPARATE FROM G23: G23 is the curated LOAD (nodes, keys, constraints,
      provenance envelope); this is the VOCABULARY REGISTRY reaching the state
      the gate ruled. Doing them together is how a flip gets made silently as a
      loader detail — the K2 precedent (its FID tier was signed, unbuilt, and
      wrong for a month) and G27 both exist because the flip and the build were
      not distinguished.
      DEPENDS ON G22 BEING SIGNED, not merely opened: the rulings applied here
      must be final, and §J sign-off is what makes them so. The three partial
      rulings of 2026-08-06 (A1 HOLD, A2 DECLINED, H3 CONFIRMED) are recorded in
      config/gate-log.md and will not change, but the rest of the page can still
      move what this item has to apply.
      LIFECYCLE QUESTION — RESOLVED 2026-08-07 AT THE G22 §J SIGN-OFF, and the
      answer is NOT the one this note recommended. Use `deprecated`. The question
      ("no status describes declared, never built, ruled unnecessary") was
      already answered by a SIGNED gate: four entries retired never-built sit at
      deprecated with loader ~ — m3_seal_app_ref, seal_requires_scheduler, and
      arch_contains_batch + arch_contains_folder, the last two retired at K7
      (seal-app-ref-edge-reshape §C2, SIGNED OFF 2026-08-03) with notes reading
      verbatim "never gated, never loaded". NOTHING in the registry is `removed`.
      WHY `removed` WAS WRONG, not merely second-best: it means code deleted, so
      it would license deleting the entry AND the note naming the ruling, which
      is the whole point of retiring at a gate; it would also split the retired
      set across two statuses that mean the same thing. The real defect was the
      one-line GLOSS in 00-header.yaml, amended at the sign-off. A fifth status
      would additionally have failed tests/unit/test_lineage_writer.py, which
      pins the vocabulary to the four values.
      SO FOR (c): m3_runs_on_etl_host lands `deprecated` with a note naming the
      2026-08-06 A2 ruling, its date, and the m3_runs_on_agent_host /
      m3_runs_on_host_group replacements — matching the K7 wording pattern.
      INPUTS CORRECTED 2026-08-06 (gate rua-load-shapes I1): the list named
      40-local-controlm.yaml only, which carries m3_invokes (B2's widening) but
      none of the rest this gate ruled. Added 43-local-architecture.yaml — C1's
      WAS_ATTRIBUTED_TO {role: directory_owner} belongs beside its own
      arch_owns_code precedent there, C2's SOURCES belongs there, and C3's
      IS_ENCODED_IN already lives there — and 10-node-classifications.yaml,
      where D2's occurrence NODE class belongs (the same file already holds the
      :Script and :AppUser labels). An inputs list is what a sub-agent reads, so
      an unlisted fragment is an unedited fragment.
      NOTE FOR (b), also from I1: D2 ruled a NODE class, not only an edge, and
      the supplement discipline is identical — 10-node-classifications.yaml's
      header requires every dd:* class to be MERGEd as :OntologyTerm:LocalClass
      in a supplement, so the occurrence class needs label + class + prov_type
      + its supplement block exactly as an activated relationship does.
      SECOND INPUTS GAP, found at the §J sign-off and the same class as I1's:
      tests/unit/test_lineage_writer.py is the TERMINUS IN CODE.
      test_live_load_is_gate_bound_against_the_real_registry asserts that a live
      load RAISES GateBoundVocabularyError on m3_invokes, and its own docstring
      says the test "flips to the execution contract (and gets updated
      deliberately, not silently)" once the gate activates those entries. The
      A3/A5 flips here are exactly that event, so the guard's expectation
      INVERTS at this item — retire it deliberately; do not "fix" the failure by
      deleting the raises-check, which would remove the terminus that has been
      holding the line since the vocabulary was declared. tests/unit/
      test_schema_graph.py rides along: deprecating m3_runs_on_etl_host drops it
      from RENDERED_STATUSES ("active", "planned"), so it must leave that test's
      acceptance-edge set and schema_graph.cypher must be regenerated — the same
      move the docstring already records for seal_requires_scheduler (C12) and
      m3_seal_app_ref (K8).
      DONE 2026-08-07 (desktop, same session as the claim). Applied: (a)
      m3_invokes (to_node widened Script|ETLProcess per B2, B3 verbatim-evidence
      noted) + m7_uses_artifact + m3_reads_from/m3_writes_to (A5 restriction
      restated in both notes) all planned→active with supplement blocks written
      (ontology_supplement.cypher, INVOKES/USES_ARTIFACT/READS_FROM/WRITES_TO +
      MAPS_TO anchors); (b) new planned entries arch_owns_directory (C1,
      build-blocked on K17 in its note), arch_sources (C2, SOURCES prov ~),
      SourceOccurrence node class (D2, prov:specializationOf binding in note,
      LocalClass block written per the 10-header rule) + u1_is_encoded_in
      from_node widened CodeModule|Script (C3; .ksh adapter data was already in
      code_snapshot.py from the session); (c) m3_runs_on_etl_host deprecated
      (K7 wording pattern, replacements named) + role enum trimmed to
      (host_group | agent_host) in the supplement; m3_delegates_to HELD note
      added (A1/K17). Tests: lineage-writer terminus inverted DELIBERATELY —
      raises-check retired per-label (m3_triggers still refuses, new companion
      test), schema_graph acceptance/exclusion sets updated + regenerated,
      enforcement matrix regenerated, and the two acceptance invariants added
      to test_yaml_fragments (no-active-without-supplement with the documented
      CORE_SEEDED exemption; G22 dispositions terminal). NO loader — G23,
      which this close moves into next_ready.

  - id: G56
    epic: component-topology
    title: "Collector captures the mount table, so shared-vs-local storage is DERIVED instead of declared (bundle schema v3)"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_lineage/collect/rua_inventory.sh
      - drydocs_lineage/collect/rua_inventory.conf
      - drydocs_lineage/collect/README.md
      - drydocs_lineage/extractors/rua_inventory.py
      - tests/unit/test_lineage_inventory.py
      - config/gate-prompts/rua-load-shapes.yaml
    acceptance: >
      A bundle answers "is this path on shared storage?" without asking a human.
      The collector emits an OPTIONAL mounts section capturing actual mount
      state — `findmnt -rn -o SOURCE,TARGET,FSTYPE,OPTIONS` with `/proc/mounts`
      as the no-findmnt fallback, both read-only and runnable as the service
      account with no privilege change. NOT lsblk and NOT /etc/fstab alone, and
      the script says why in a comment: `server:/path` is an NFS mount spec and
      NFS is not a block device, so it never appears in lsblk; and fstab is
      configured INTENT, not actual state (autofs/systemd/manual mounts are
      mounted without appearing there, stale lines appear without being
      mounted). COLLECTOR_VERSION bumps to v3. The extractor parses the section
      when present and files it under the EXISTING `sections_optional_absent`
      contract when absent, so every v1 and v2 bundle stays fully ingestible —
      the same discipline scripts.tsv already follows. Each staged path resolves
      against the LONGEST matching mount target to yield `mount_root` +
      `fstype`, and `storage_scope` derives from fstype — nfs/nfs4/cifs shared,
      xfs/ext4 single-host unless gfs2/ocfs2 clustered, anything unrecognised
      `unknown` and COUNTED, never guessed. Synthetic fixtures only (real mount
      specs carry live server names); tests cover a shared-NFS bundle, a
      local-only bundle, a mounts-absent v2 bundle, and an unrecognised fstype.
    notes: >
      Groomed 2026-08-06 (laptop) at the G22 session, from SME direction. THE
      PROBLEM IT SOLVES: a deployment path may be shared — the SME's case is
      roughly 20 VSI hosts against one shared filesystem — and then the same
      path on N hosts is ONE FILE SEEN N TIMES, not N deployments. Today that is
      not derivable from any bundle we hold: the collector captures no mount
      table at all, the envelope carries scan_roots but never which filesystem a
      root sits on, and the ownership sweep's -xdev stays on one filesystem
      without recording which.
      THE CORRECTION THAT SHAPES THE ACCEPTANCE, made at the session: "on the
      SAN" does not mean shared. Twenty hosts each with their OWN LUN from one
      array is twenty separate filesystems and twenty files that genuinely
      drift; twenty hosts mounting ONE NFS export is one file seen twenty times.
      Sharing follows from FSTYPE, never from the array — which is why the type
      column is load-bearing and why lsblk alone would actively mislead.
      WHY NOW IS THE CHEAP MOMENT (SME, same session): nothing has been loaded
      yet and the company-side twin collector is NOT present, so a schema bump
      costs no convergence debt. The earlier T14 caveat is WITHDRAWN on that
      correction.
      WHAT THIS DOES NOT REMOVE: bundles already collected stay scope-unknown,
      so the G22 D-amendment rule stands either way — `unknown` never defaults
      to independent, it SUPPRESSES the corroboration claim. This item reduces
      how often that state occurs; it does not retire it.
      NOT gate-bound: capturing a fact is an instrument change, not a decision
      about what an edge MEANS. It was deliberately NOT edited at the gate
      because the collector runs on production hosts.

  - id: G57
    epic: component-topology
    title: "Rename the rua_* family to bkup_* across the collector, extractor, staged kinds and envelope properties"
    type: chore
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - drydocs_lineage/collect/rua_inventory.sh
      - drydocs_lineage/extractors/rua_inventory.py
      - drydocs_lineage/extractors/rua_code_ops.py
      - drydocs_core/data_root.py
      - config/source-registry.yaml
      - config/gate-prompts/rua-load-shapes.yaml
      - config/gate-log.md
      - MODULE_MAP.md
    acceptance: >
      The `rua` prefix is gone from names the repo AUTHORS, and the rename is
      recorded rather than silent. Scope measured at groom: 24 tracked files
      carry the token in code, config and docs — the collector script + conf +
      README, the two extractors, `rua_extracted_dir`, the `RuaCoverage` /
      `RuaInventoryExtractor` classes, the staged kinds
      (`rua_path`/`rua_profile`/`rua_script`), the `rua_*` envelope property
      family, the source-registry row, dev-environment, the runbooks and the
      backlog/IDEAS prose. TWO THINGS ARE DELIBERATELY NOT RENAMED: (a) history
      in `config/gate-log.md` — a log entry records what was decided on a date
      and rewriting it falsifies the record; (b) the SIGNED-GATE IDENTITY
      question follows the source-registry v2 §Q6 precedent — a rename is
      identity refactoring, not meaning change, so gates TRANSFER with ONE
      gate-log amendment entry mapping old→new, and nothing is re-gated. The
      gate page `rua-load-shapes.yaml` renames only if §Q6's amendment entry is
      written in the same commit. A test asserts no source file outside the
      history/amendment set still names the old prefix, so it cannot drift back.
    notes: >
      Groomed 2026-08-06 (laptop) at the G22 session — the SME renamed
      `rua_inventory` to `bkup_inventory`, dropping the `rua` prefix. Producer
      side still carries `rua` everywhere; this item closes the divergence.
      NOW IS THE CHEAP MOMENT, and it will not come again: nothing has been
      loaded, so the staged kinds and `rua_*` envelope properties exist in NO
      graph. Renaming a property that is already on nodes is a migration;
      renaming one that has never been written is an edit.
      DELIBERATELY NOT DONE INSIDE THE GATE SESSION: it would rename the page
      being actively ruled on, mid-ruling. It is also a chore rather than a
      task with judgment in it — the only judgment is the not-renamed set above,
      which this acceptance already fixes.
      WITHDRAWN 2026-08-08 (user ruling), closed with NO rename performed: the
      groom over-read the G22-session file rename. The SME renamed
      `rua_inventory` to `bkup_inventory` as a WORKING MANEUVER — freeing the
      name so a fresh version could be written and compared against the bkup
      file — not as a directive to rename the rua_* family. The producer tree
      keeps the `rua` prefix; the acceptance above is retained as the record of
      what a real rename would have required (the §Q6 transfer mechanics stand
      as precedent if the question ever genuinely reopens).

  # ── Epic W — FCDO ontology alignment (main / ontology-mapper) — Phase 2 ─────
  # Groomed 2026-07-31 from the FCDO capture (internal/fcdo-reference/, 8abc05f)
  # and its ALIGNMENT-PLAN.md: align producer ontology to the internal governance
  # group's firmwide frameworks ON PLANNED ITEMS ONLY (user rule: don't add what
  # we won't use). Every confirmation routes through the SME gate; the plan's
  # explicit skip list is BINDING (Business Processes, mapping path expressions,
  # SHACL artifacts, JDI producer-side, FIBO, Data Authority classes, telemetry,
  # naming renames). The live-scrape [source] line stays parked in IDEAS.md —
  # company-side connector work, not producer-actionable.
  - id: G58
    epic: component-topology
    title: "Dead-script archival report: three dispositions, a stated coverage precondition, and the already-archived count"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: [G23]
    inputs:
      - drydocs_lineage/review.py
      - drydocs_lineage/extractors/rua_inventory.py
      - drydocs_lineage/extractors/code_repo.py
      - drydocs_lineage/extractors/dpl_registry.py
      - config/gate-prompts/rua-load-shapes.yaml
      - config/gate-log.md
      - tests/
    acceptance: >
      The report G22 §E3 named as the usage axis's use case — identifying
      unused, deprecated code for archival and removal — ships WITH the safety
      properties the gate ruled, and a test proves each one.
      (a) COVERAGE IS STATED ON THE REPORT ITSELF, never a footnote. Every run
      declares its body-copy coverage, and a run over a metadata-only bundle
      says so in its own output. A test asserts that a report generated from a
      body-less fixture carries the coverage statement AND that its unreferenced
      rows read "no CMD_LINE reference" rather than "nothing calls it".
      (b) THREE DISPOSITIONS, not two, each separately counted: genuinely dead
      (archive and remove), MISDEPLOYED (relocate, never delete — §E1's caveat
      that a script may have been deployed to the wrong server), and
      unreferenced-but-dynamically-called (keep). A test proves the three are
      distinct buckets and that no candidate is silently absent from all three.
      (c) THE MISDEPLOYMENT BUCKET IS SCOPE-GATED: emitted only where
      storage_scope is `local`, because under shared storage every host sees ONE
      file and the finding is meaningless. Unknown scope suppresses it and
      COUNTS the suppression (the never-silent house rule).
      (d) ALREADY-ARCHIVED IS REPORTED, not only deletion candidates: scripts
      carrying a code-repo occurrence and NO current server-extract occurrence
      are counted as already archived — the progress measure toward the target
      state. The inverse (server occurrence, no repo occurrence) is G24's
      existing never_committed bucket: cross-referenced, never recomputed.
      (e) NO AXIS PROVES ABSENCE (§H1): every "absent on axis X" row reads as
      "not observed by feed X", and the active_unknown count appears in the
      report rather than being dropped.
      Full suite green.
    notes: >
      Groomed 2026-08-07 (laptop) from Idea-80 + Idea-82 on user instruction to
      groom the two TOGETHER. They are one item rather than two because they
      turn on the SAME argument: an absence observed by one feed is not an
      absence in the world. Idea-80 supplies the dispositions and the coverage
      precondition; Idea-82 supplies the already-archived measure and the reason
      it needs no new flag.
      WHY IT IS SEPARATE FROM G23: G23 LOADS the bundle facts; this READS them.
      Folding the report into the loader is how a deletion-driving output gets
      built as a loader detail — the same distinction that keeps G55 separate
      from G23, and for the same reason.
      WHY (d) COSTS NO NEW VOCABULARY: after G22 §D2 ruled reified occurrence
      nodes carrying `origin`, "archived" is a SHAPE rather than a status —
      repo occurrence present, current server occurrence absent. That keeps it
      DERIVED FROM EVIDENCE rather than declared, the same move §G1 made when it
      put identity in the business key and made the URN a render, and it stays
      honest when a file comes back. DryDocs' existing word for the artifact-level
      fact is the TOMBSTONE (`removed_from_source_at`, the D7 sweep ruled at
      U13, live on :CodeModule today) — reuse the concept, and note that on
      :CodeModule it is a TIMESTAMP, not a status.
      THE SAFETY BAR IS WHY (a) AND (c) ARE ACCEPTANCE CLAUSES RATHER THAN
      GOOD PRACTICE: the output drives DELETION, so a false positive removes
      live code. §H2 ruled active=true-but-unreferenced "the most dangerous
      false positive available" precisely because §E3's blind spot (no body
      copies, so script-to-script calls are invisible) is exactly what hides the
      caller.
      ONE QUESTION DELIBERATELY NOT GROOMED INTO AN ITEM: what to CALL the
      estate-level end state ("the production server holds only actively used
      code"). It is a property of the ESTATE, not of any node, so a formal
      graph-side term would be a vocabulary decision riding the HITL gate — and
      NOTHING NEEDS ONE YET. Minting an item for a decision no build depends on
      is how the backlog inflates. Surface it at this build if the report wants
      a term; the plain-English statement of the goal is L24's job and needs no
      gate. Explicitly DO NOT reach for the 00-header.yaml lifecycle
      (planned|active|deprecated|removed): that enum governs vocabulary ENTRIES
      (edge meanings, i.e. schema) and a script on a server is an INSTANCE —
      reusing the four words would put `removed` = "deleted an edge definition"
      and `removed` = "deleted a file off a server" in one repo, the
      two-things-one-spelling collision §A2 caught on `group` and J32 made a
      standing rule.
      HOME: drydocs_lineage/review.py is the lineage SME surface (CLI
      `drydocs lineage-review`, ported at ADR 0002-C) — the same surface §A3's
      signed ruling already sends same-basename multi-mount duplicates to for
      SME merge, so the curation ladder exists.
      PRIORITY NOTE: Idea-80 carried an agent-PROPOSED `prio? High` that the
      user never ruled. Filed p2 for coherence with its own dependency chain
      (G22/G23/G55 are all p2) — it cannot start before G23 regardless. Raise it
      if the archival drive lands before the rest of the chain.
      DONE 2026-08-07 (desktop, same session as G23): BUILT as
      drydocs_lineage/archival.py — archival_report() over the staged
      LineageGraph, PURE, writes nothing; findings are review material
      (H2/P5), surfaced as an optional section on the lineage-review page
      (to_html archival=..., the E3 curation-ladder surface). Every
      acceptance clause has its test (tests/unit/test_lineage_archival.py,
      9 tests): (a) coverage stated ON the report — metadata-only runs say
      METADATA-ONLY RUN in their own output and dead rows read "no
      CMD_LINE reference observed", never a nothing-calls-it claim; (b)
      three distinct buckets (dead / misdeployed / dynamically_called via
      the who-references split: job-src rels = CMD_LINE feed, script-src
      rels = body-visible callers) + in_use counted, nothing silently
      absent; (c) misdeployment emitted ONLY under storage_scope local
      (all-records-local, conservative), unknown AND shared suppressed and
      counted; the row notes node_target polymorphism for the SME; (d)
      already-archived = G24 repo_only CROSS-REFERENCED from the
      CorroborationReport (never recomputed), never_committed likewise; no
      corroboration -> "archived-state not observed", not zero; (e)
      active_unknown passthrough (None = "registry axis not observed on
      this run"). The estate-level end-state TERM the notes deliberately
      left unminted stayed unminted — the report needed no term, plain
      counts suffice (L24 owns the prose goal). Suite 1698/3/5 — the 3 are
      the Idea-83 rich-ANSI env failures, untouched.


  - id: W1
    epic: fcdo-alignment
    title: "FCDO vocabulary crosswalk: DryDocs terms mapped to the firmwide framework terms as status: proposed, with a gate spec — mechanism-only rows"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [internal/fcdo-reference/ALIGNMENT-PLAN.md, drydocs_core/ontology/relationship_vocabulary.yaml, config/crosswalks/, config/gate-prompts/]
    acceptance: >
      config/crosswalks/fcdo-vocabulary.yaml exists carrying the ALIGNMENT-PLAN
      Phase-1 rows (ControlMJob<->OL Job, JobRun<->OL Run, DataAsset<->Dataset,
      READS_FROM/WRITES_TO<->prov:used/wasGeneratedBy with the Run-grain vs
      definition-grain note, loader envelope<->metadata envelope,
      lifecycle<->adms:status as documentation-only, SUBCLASS_OF/MAPS_TO<->rdfs
      bridging, RECONCILES_TO confidence<->skos:closeMatch+mappingConfidence),
      every row status: proposed and MECHANISM-ONLY (standard CURIEs; no
      internal URLs/names — those stay in internal/); a gate prompt
      config/gate-prompts/fcdo-crosswalk.yaml covers every row; rows whose
      evidence sits in the known capture holes (Descriptive Metadata, Data
      Quality, DPROD contracts, taxonomy property tables) are marked
      blocked-on-recapture, not proposed-as-ready. NOTHING flips confirmed
      except the SME gate session (gate-log entry). Schema/board tests green.
    notes: >
      Groomed 2026-07-31 from the same-day inbox idea (FCDO crosswalk = Phase 1
      of the alignment plan). Phase chosen: 2 (ontology mapping + HITL) over 11
      — this is term-mapping, not a per-source ledger (recorded per the groom
      rule). Same mechanism as the AutoSys/Airflow crosswalks (F1/F2 precedent).
      DONE 2026-08-05 (desktop, eaa4469): 8 rows under sibling schema
      drydocs.vocab-crosswalk.v1 (orchestrator runtime skips it deliberately),
      row 5 blocked-on-recapture (Descriptive Metadata hole); gate spec
      fcdo-crosswalk covers all rows; test_fcdo_crosswalk.py pins the
      proposed-only/mechanism-only/runtime-skip invariants. Gate SESSION is
      still open — it joins the AWAITING list, and row 5 stays unsignable
      until the fcdo-frameworks recapture.
  - id: W2
    epic: fcdo-alignment
    title: "FCDO property/enum alignment registered as planned on the planned items: Run properties + run-event enum, SKOS-required attrs on enum gates, ColumnShape property names"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [internal/fcdo-reference/ALIGNMENT-PLAN.md, drydocs_core/ontology/relationship_vocabulary.yaml, config/gate-prompts/, drydocs_core/schema/]
    acceptance: >
      The FCDO-aligned names are REGISTERED as planned/proposed on their three
      landing spots, each activating only at its host gate/build — never from
      this item: (1) the planned ControlMJobRun material carries the required
      Run property semantics (run UUID, nominalTime scheduled time, parent
      run/job link, errorMessage) and START/COMPLETE/FAIL/ABORT as the
      run-event value set, with wiring the three seeded :OlClass nodes (OL
      Run/Job/Dataset) via MAPS_TO listed as part of that build; (2) the open
      G27 etlprocess-kind-enum prompt (and the enum-gate idiom it sets) states
      the SKOS-required concept attributes (prefLabel, definition, identifier,
      created/creator) with the tom_roles/product_roles backfill named; (3)
      the oracle-schema-asset gate material names the ColumnShape property
      names (precision, scale, length, isNullable) for column facts. All
      entries status planned; vocabulary/schema tests green.
    notes: >
      Groomed 2026-07-31 — ALIGNMENT-PLAN Phase 2 (planned-items-only rule).
      Registration is startable now; each activation rides its host gate/build
      (prose dependency per the O31 precedent, not depends_on ids). SCOPE NOTE
      (user ruling 2026-07-31): the Run property semantics in (1) — run UUID,
      nominalTime, parent link, errorMessage, START/COMPLETE/FAIL/ABORT —
      would apply IF DryDocs becomes event-based / near-real-time monitoring,
      WHICH IT IS NOT. Our run data is batch history (CM_HIST / cm_avg_run
      supplements), not a live OpenLineage event stream — so item (1) stays a
      registered-planned naming alignment only, adopted so that IF an
      event-based mode ever arrives the names are already conformant; it is
      not a mandate to build run-event ingestion, and no gate should treat it
      as one.
      DONE 2026-08-05 (desktop, 46a2b8c): (1) landed on the ControlMJobRun
      note in relationship_vocabulary/10-node-classifications.yaml (Run
      semantics + event value set + :OlClass MAPS_TO wiring listed as part
      of that build, scope ruling restated); (2) landed as C3 on the G27
      prompt (SKOS attrs stated as the enum-gate idiom; tom_roles/
      product_roles named as backfill targets, follow-up rides the ruled
      option's build); (3) landed as a notes: block on the
      oracle-schema-asset map entry (ColumnShape names + maps-to-type
      pattern). Registrations only — every activation rides its host
      gate/build. Suite 1588 green.
  - id: W3
    epic: fcdo-alignment
    title: "fcdo-ontology-builder documented as an optional add-source-object Step-3 aid (structural profiling only, never bypasses the gate)"
    type: chore
    module: docs
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs: [internal/fcdo-reference/ALIGNMENT-PLAN.md, .claude/skills/add-source-object/]
    acceptance: >
      The add-source-object skill documents the optional aid for flat/
      denormalized sources: run on header-only/redacted extracts ONLY; reuse
      its SKOS enum extraction + HTML data-dictionary render; translate its
      generic classes through ontology-mapper into decision-matrix terms; and
      state plainly that it does NO data profiling (counts/nulls/grain/join
      coverage stay ours) and never bypasses the HITL gate. Cites the
      PAT-catalog worked example (ALIGNMENT-PLAN Phase 3).
    notes: >
      Groomed 2026-07-31 — ALIGNMENT-PLAN Phase 3 (process adoption, not
      ontology; docs module because the deliverable is a skill-doc change).
      DONE 2026-08-05 (desktop, 2e4e726): optional-aid block added under
      Step 3 of the skill — header-only/redacted extracts only, SKOS enum
      extraction + data-dictionary render reuse, ontology-mapper translation
      of its generic classes, no-data-profiling statement, never-bypasses-
      the-gate statement, PAT-catalog worked example cited.

  # ── appended at the 2026-08-02 groom run (weekly inbox + the self-doc session) ─
  # Nine items. TWO ARE RETROSPECTIVE CLOSES (U9, C21): that work landed at e3f65af
  # before any item existed, and committed code already cites "U9" as an id —
  # filter_ignored.py's first line, snapshot.ps1's U9 block, and the package-layer
  # gate page — an id nothing defined. The groom's own cross-check then found what
  # rode along unseen: G51. U10 IS A GATE SESSION (never auto-decided); U11 drafts a
  # gate prompt and decides nothing; C22 consolidates two inbox lines onto the same
  # three files rather than booking two passes over them.

  - id: U9
    epic: self-documentation
    title: "Scanner takes the WHOLE REPO by default + drops git-ignored paths, and the 101-file snapshot series retires to one"
    type: task
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-02
    depends_on: [U7, U8]
    inputs: [knowledge/depgraph-snapshots/snapshot.ps1, knowledge/depgraph-snapshots/filter_ignored.py, drydocs/loaders/code_snapshot.py, drydocs_core/models/code_snapshot.py, tests/unit/test_code_snapshot_loader.py]
    acceptance: >
      snapshot.ps1 scans the repository root by default (the hand-maintained root
      list survives only behind -CodeOnly, writing a DIFFERENT filename so the two
      shapes cannot collide); a post-processing step drops git-ignored paths through
      `git check-ignore`, so the rule set is the repo's own and cannot drift from it;
      exactly one snapshot stays committed; and the loader accepts a tree-shaped
      artifact, strips the repository prefix from file_id so a mode switch MERGEs
      instead of forking the ruled key, and SKIPS-AND-COUNTS kind:'dir' nodes rather
      than inventing a node class. Full suite green.
    notes: >
      Groomed RETROSPECTIVELY 2026-08-02 — the work landed at e3f65af on SME
      direction ("update the scanner to pull all files, so that we can create the
      tree. the old dep snapshots can be removed this was the intent") and three
      committed files already cite "U9" by id. No sibling-repo change was needed:
      depgraph already reported tree: true, and tree mode is a strict SUPERSET.
      WHY THE FILTER: depgraph excludes .git and .venv but knows nothing about
      .gitignore, so the first whole-repo scan collected 649 ignored nodes, 386 of
      them .ruff_cache — ~18% of the artifact. Filtered in snapshot.ps1 rather than
      the loader so the committed JSON, viewer.html and the graph all show the same
      thing. Two Windows traps, both load-bearing: text=True translates \n to \r\n
      into stdin so git receives '.env\r', and in line mode git QUOTES unusual names
      and hands back '".env\r"' — either alone made the filter report "no git-ignored
      paths" against a snapshot holding 386. `-z` with binary input kills both.
      TWO DEFECTS INTRODUCED AND FIXED IN-RUN, both live-verified: the key FORKED
      (tree file_ids arrive as 'drydocs/drydocs/cli.py'; loaded raw that made two
      :CodeModule nodes for one file, side by side, NEITHER SWEPT — caught by asking
      why the count read 2410 instead of 2200), and `project` lost its meaning (it
      was "the scan root, one of six", a concept that existed only because of the
      hand-maintained list; the first fix was worse than the bug, labelling
      README.md a scan root). Result: 1457 :CodeModule (was 210), 458 IMPORTS, and
      all 45 .cypher files present as nodes for the first time. The tree is in the
      ARTIFACT (251 directories, 1956 CONTAINS) and deliberately NOT in the graph —
      U10 owns that gate. Doc drift left behind: U12.

  - id: U10
    epic: self-documentation
    title: "Run the code-graph package-layer gate to sign-off: does the PACKAGE become a node, and how is one recognised offline"
    type: task
    module: ontology
    phase: 16
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [U9]
    inputs: [config/gate-prompts/code-graph-package-layer.yaml, config/gate-prompts/self-documentation-code-graph.yaml, config/gate-log.md, drydocs_core/ontology/relationship_vocabulary/, config/taxonomy-ontology-map/, knowledge/depgraph-snapshots/]
    acceptance: >
      All 30 confirmations of config/gate-prompts/code-graph-package-layer.yaml are
      ruled with the SME and recorded in config/gate-log.md — including §C2, the
      page's own "real decision" (how a package is recognised OFFLINE, given
      __init__.py presence is UNSOUND here: drydocs_core/schema is a live PEP 420
      namespace package with none, imported by the CLI), and the question the U9
      scanner change added: whether the containment level loads as :CodePackage, as
      the :Directory nodes the artifact now carries, or both. Then per the page's §I:
      the proposed terms (u1_has_package, u1_defines_package, and the u1_has_module
      revision) are registered `status: planned` ONLY; the taxonomy-ontology-map
      entry is AMENDED per §H4 rather than duplicated; the parent gate's §B1/§B2/§C4
      are annotated in place as re-opened-and-superseded-by-this-gate with the date;
      a BUILD item is added to backlog.yaml naming this gate as its dependency; and
      gates.json + the enforcement matrix regenerate. NOTHING is written to the graph
      at the gate itself.
    notes: >
      HITL — the sign-off is an SME session, never auto-decided (CLAUDE.md §6), and
      no build item is opened ahead of it because §I says the gate opens it.
      Trigger was the SME reading the LIVE GRAPH 2026-08-02: ":HAS_MODULE is
      overused ... I thought module described the next highest group under the
      project." The page corrects the vocabulary before answering — in Python a
      module IS what `import` binds, so drydocs/cli.py genuinely is the module
      drydocs.cli and the file nodes are correctly typed; the level the question
      points at is the PACKAGE, and the one place the model is factually wrong is
      __init__.py, which is not a module named __init__ but the thing that makes its
      directory a package. §A4 asks the SME to accept that correction FIRST, because
      §C is written against it. Not drift: the parent gate (signed 36/36 2026-07-27)
      named this exact cost in writing at §C1 and declined the two-level option at
      §B1(c) on COST, not correctness — a recorded, accepted cost that is later felt
      is precisely what a re-open is for. Fable: a new node class, two new edge
      types, and an ACTIVE vocabulary entry re-pointed.

  - id: U11
    epic: self-documentation
    title: "Draft the gate prompt for the artifact chain: which .py executes which .cypher, and which labels that .cypher writes (parent gate §H5, deferred)"
    type: task
    module: ontology
    phase: 16
    agent: main
    model: fable
    priority: p3
    status: todo
    depends_on: [U9]
    inputs: [config/gate-prompts/self-documentation-code-graph.yaml, config/gate-prompts/code-graph-package-layer.yaml, drydocs/loaders/, drydocs/loaders/cypher/, drydocs_core/ontology/relationship_vocabulary/]
    acceptance: >
      A drydocs.gate-prompt.v1 spec exists proposing the code -> artifact -> label
      chain the parent gate deferred at §H5, drafted the way the package-layer page
      was: mechanism only, every number verified against the committed artifact,
      nothing written, no vocabulary term past `status: planned`. It must put four
      things to the SME: (a) whether a .cypher file is an ordinary :CodeModule or its
      own class; (b) the edge names and directions for "this module executes that
      file" and "that file writes this label"; (c) how the join to the DOMAIN graph
      is expressed given a label is not a node — the thing that would finally answer
      "which module loads this job"; and (d) what the loader does with declarations
      it cannot resolve statically. gates.json + the enforcement matrix regenerate;
      the SIGN-OFF is a separate SME session.
    notes: >
      Groomed 2026-08-02. The chain is STATICALLY DERIVABLE — checked before
      grooming, not assumed: 26 of 27 `cypher_path` declarations resolve to a literal
      filename by AST, 39 labels are written across the 45 .cypher files, 16 are
      read-only, and 9 of 32 loader scripts yield no write-label at all (edge-only or
      migrations). That residue is exactly what (d) has to rule on, and it is the
      reason this cannot be a build item. Only possible NOW: .cypher files became
      graph nodes for the first time at U9 — the old scan saw Python only, so the
      artifact could not express the chain at all. G27/W1 precedent: drafting a
      prompt decides nothing.

  - id: U12
    epic: self-documentation
    title: "Make the ruled snapshot retention true in the script as well as the prose — the README documents a retired ritual and snapshot.ps1 still accumulates a series"
    type: chore
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: [U9]
    inputs: [knowledge/depgraph-snapshots/README.md, knowledge/depgraph-snapshots/snapshot.ps1, knowledge/depgraph-snapshots/filter_ignored.py]
    acceptance: >
      (a) snapshot.ps1 ENFORCES the ruled retention — newest all-files snapshot only
      (SME 2026-08-02) — instead of relying on a human to delete: today a second run
      on the same day writes `<project>-<date>-<HHmm>.json` beside the first, which
      is precisely how the 101-file series accumulated, so the ruling survives only
      until the next double-run. The -CodeOnly comparison shape keeps its own
      filename and is exempt (it is not the ritual). Whatever the mechanism, an
      unexpected extra file is not the outcome. (b)
      knowledge/depgraph-snapshots/README.md matches what the script now does: the
      command block documents the whole-repo default and -CodeOnly (the -Tree flag it
      currently shows was REMOVED), the meta example reads tree: true, the "focused
      scan = the seven package roots" paragraph is rewritten, the git-ignore step is
      documented beside U7's probe and U8's abs_path strip, and the Housekeeping
      "prune ... keep the last ~10" line is replaced by the ruled retention. (c) The
      three citations to files this repo no longer contains (tree-original.json,
      tree-this-version.json, and the drydocs1-20260621.json baseline) are removed or
      restated as history recoverable from git, and a fourth instrument-change marker
      records the all-files boundary in the same voice as the three already there —
      the node count goes 238 -> 1457 on unchanged code, and nobody should read that
      as growth.
    notes: >
      Groomed 2026-08-02 — drift left behind by U9's own commit and found by this
      groom, not by the inbox. p2 rather than p3 because this README is the surface
      the session-end ritual points at and three of its links now resolve to nothing.
      CLOSED 2026-08-04 (desktop). By close time the defect had reproduced FOUR times
      (twice 2026-08-03, twice 2026-08-04 — each caught by hand at the ritual), which
      is the item's own argument made empirically. (a) snapshot.ps1 now enforces the
      ruling itself: after a successful write+filter (deliberately AFTER, so a failed
      run can never delete the only good snapshot — source order pinned by test),
      every other <project>-<date>[-HHmm].json is deleted with a per-file retention
      message; the pattern is anchored so -CodeOnly (<project>-code-*) and the retired
      drydocs1-* names never match. PROVEN LIVE in proof order: the enforcement run
      deleted FIVE stale snapshots (20260802, three same-day 20260803s the manual
      ritual had missed, and 20260804-0920), leaving exactly one. (b) README trued up:
      command block = whole-repo default + -CodeOnly (-Tree flag gone), meta example
      reads tree: true with the one-root scan list, the seven-roots paragraph rewritten
      as the retired comparison shape, U8/U9 documented beside U7 as the three
      post-processing steps, Housekeeping's "keep the last ~10" replaced by the ruled
      retention with the recovery commands. (c) tree-original.json /
      tree-this-version.json / the drydocs1-20260621.json baseline all restated as
      history recoverable from git (recovery one-liner included); the 2026-07-28
      marker's tense fixed. Fourth instrument-change marker added in the existing
      voice: all-files default 2026-08-02 (U9, e3f65af), 238 -> 1457 nodes on
      unchanged code = the instrument seeing, not growth. NEW GUARD
      tests/unit/test_depgraph_snapshots.py (+3): exactly-one committed snapshot,
      script retention present AND after-the-write, README currency (no link to an
      absent .json). Suite 1410 -> 1413.
      Clause (a) was raised while regenerating the board AT the groom: the retention
      question (open since 2026-07-25) is now RULED, and a rule enforced only by
      whoever remembers it is the same shape as the hand-maintained scan-root list
      U9 just deleted. Widened from a docs chore to graph-infra for that reason.

  - id: U13
    epic: self-documentation
    title: "Review-plan seed queries ignore D7 tombstones — every query pack filters removed_from_source_at, or says why it wants the dead"
    type: bug
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: []
    inputs: [docs/reviews/code-graph-review-plan.md, .claude/skills/tech-debt/SKILL.md, drydocs/loaders/code_snapshot.py]
    acceptance: >
      Every seed query in docs/reviews/code-graph-review-plan.md (and the A1-A6 pack
      the tech-debt skill copied from it at U4) either filters
      `removed_from_source_at IS NULL` or carries a one-line note saying why
      tombstones belong in that answer. The demonstrated defect is the proof case:
      re-running the A3 fan-in ranking against the current snapshot no longer ranks
      the DEAD drydocs_core/controlm/__init__.py (tombstoned by S2's move to
      orchestration/controlm/) in the results. No graph or loader change — the D7
      sweep is working; only the query text is wrong.
    notes: >
      Groomed 2026-08-04 from the 2026-08-02 [bug] inbox line, demonstrated live:
      the unfiltered A3 query ranked the dead __init__.py at #6 with fan-in 13, one
      slot below its live replacement at 14 (graph: 1465 :CodeModule, 1457 live).
      The tombstones only became big enough to change an answer after U9's
      whole-tree scan + S2's package move landed in the same week.
      CLOSED 2026-08-04 (desktop, batch with U14 — one edit to the shared pack).
      Every seed query now filters removed_from_source_at IS NULL or says why the
      dead belong: tech-debt A1-A6 all filtered (both ends where two nodes anchor);
      the plan's table carries the filters + a standing-rules preamble; Phase 2
      units 1-2 are the "why the dead belong" cases and now say so (unit 1 RETURNS
      the tombstone stamp to split existed-then-removed from never-existed; unit 2
      queries IS NOT NULL deliberately); the groom-backlog skill's census +
      close-note checks got the same treatment (in-scope: U4 built them from the
      same plan). PROOF CASE DISCHARGED WITH A FINDING: the acceptance's A3 re-run
      shows NO dead module — but not only because of the filter. The 2026-08-03
      manual Neo4j wipe (G51 session) rebuilt the graph with ZERO tombstones
      (census 1467/1467 live, probed after a fresh load), so the demonstrated
      defect's specific tombstone no longer exists anywhere. The filter is still
      right — tombstones re-accumulate with every load after a move or delete —
      and the skill's rule notes the post-wipe zero honestly so nobody reads an
      empty tombstone census as the filter being unnecessary. No graph or loader
      change, exactly as scoped.

  - id: U14
    epic: self-documentation
    title: "Architect-persona metrics count 63 vendored .claude/skills scripts — scope the query pack to the real packages so baselines are comparable"
    type: bug
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-04
    depends_on: []
    inputs: [docs/reviews/code-graph-review-plan.md, .claude/skills/tech-debt/SKILL.md, knowledge/depgraph-snapshots/snapshot.ps1]
    acceptance: >
      The persona/tech-debt query pack scopes its architecture metrics (orphans,
      untested, fan-in/out baselines) to the repo's real Python packages — a region
      allow-list in the queries, NOT an exclude in the scanner (the U9 whole-tree
      scan is the ruled shape; the tree SHOULD show .claude/skills). With the scope
      applied, the orphan metric reads ~23 against the 24 baseline (not 77) and
      untested reads ~30 (not 130); the plan's baseline table is restated with the
      scope written next to each number so the next run compares like with like.
    notes: >
      Groomed 2026-08-04 from the 2026-08-02 [bug] inbox line. The pollution: 54 of
      the 77 raw orphans are Anthropic-shipped docx/pdf skill scripts under
      .claude/skills/. Fix placed in the QUERIES rather than the scan because the
      SME direction that created U9 was explicitly "pull all files, so that we can
      create the tree" — the artifact is right, the metrics mis-scope it. If U13
      lands first, apply both filters in one edit to the shared pack.
      CLOSED 2026-08-04 (desktop, batch with U13 — one edit to the shared pack).
      The allow-list is the SEVEN package roots the pre-U9 baselines were measured
      on (drydocs, drydocs_core, drydocs_api, drydocs_remediation, drydocs_lineage,
      drydocs_deepdoc, tests), bound as $packages in the queries, never the scan.
      LIVE NUMBERS (fresh load, 2026-08-04): raw .py orphans 77 with .claude=54
      exactly as groomed; untested raw .py 129 (the item's "130"); package-scoped
      untested 29 (the ~30 ✓). ONE ACCEPTANCE NUMBER CORRECTED BY EVIDENCE: the
      predicted ~23 in-package orphans do not exist — the 23 non-vendored orphans
      all live OUTSIDE the packages (agents/ 15, scripts/ 4, knowledge/ 3,
      UI-WIP/ 1), and the in-package orphan count against the 24 baseline is 0.
      Folding those regions into the allow-list to hit ~23 would break the OTHER
      baseline (untested balloons 29 -> 65), so the ruling shipped is: seven-root
      scope for baseline comparability, and the first-party non-package orphans
      (22, sans UI-WIP scratch) documented as a SEPARATE LABELED QUEUE in both
      the skill's A4 row and the plan's restated table — visible, never folded.
      The plan's baseline table now writes the scope + date next to every number
      (18 -> 29 fan-in, 24 -> 0 + queue 22, unprobed -> 29/129); A6 and A3's
      importer end documented as deliberately unscoped (agents/ coupling and
      fan-in are real). Offline-fallback note updated (snapshot = live tree, so
      no tombstone guard offline; scope applies as a file_id prefix filter).

  - id: U15
    epic: self-documentation
    title: "Snapshot meta.depgraph.dirty conflates untracked-files-present with instrument-differs-from-pin"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - knowledge/depgraph-snapshots/README.md
      - knowledge/depgraph-snapshots/snapshot.ps1
    acceptance: >
      The depgraph snapshot meta header computes its dirty flag from TRACKED changes
      only (git status --porcelain --untracked-files=no) or splits it into
      dirty_tracked / untracked_present; a snapshot taken with only untracked paths
      present reads as clean-tracked; the fix lands in the sibling depgraph
      instrument with the producer's snapshot ritual re-verified on this desktop
      (venue named per J18).
    notes: >
      Groomed 2026-08-07 from Idea-58 (the 20260805 snapshot: dirty true at exactly
      the pin, the dirt being three untracked paths). Third finding in the
      instrument-provenance class — the entry argues for one grooming pass over the
      whole meta header; this item fixes the flag and records that larger pass as a
      candidate rather than doing it.
      DONE 2026-08-09 (desktop): the acceptance offered tracked-only OR a split; BOTH were done, because either alone loses something. `dirty` now counts TRACKED changes only (`git status --porcelain --untracked-files=no`) and `untracked_present` is recorded beside it (`git ls-files --others --exclude-standard`), on BOTH header blocks. The name `dirty` was deliberately NOT renamed to dirty_tracked: it lands on :Project as git_dirty (code_snapshot.cypher) and the whole committed series carries it, so a rename breaks every reader to fix a meaning — what changed is that it now means what its readers already assumed. ACCEPTANCE CORRECTION, recorded rather than worked around: the item says 'the fix lands in the sibling depgraph instrument'. It does not and could not — the dirty flag is computed in the PRODUCER's snapshot.ps1 (pre-change lines 111 and 221), and nothing in the sibling was touched. SCOPE WIDENED for the same reason: the title names meta.depgraph.dirty, but meta.git.dirty carried the identical defect one screen away, so fixing only the named one would have left the bug in place under a different key. NEW OBSERVATION worth the record: on the INSTRUMENT side untracked_present is closer to provenance than housekeeping — depgraph is run out of that checkout, so an untracked .py there is code that can join the scan while belonging to no commit; the refusal and drift messages now print the two states separately. PROOF, venue named per J18: desktop, against the sibling depgraph repo (C:\coding\projects\depgraph, main @ a56d2fc = the pin, otherwise clean) with one untracked scratch file present — old conflated view 1 line, new tracked-only view 0 lines, helper returns {dirty:false, untracked_present:true}, which is the acceptance's reads-as-clean-tracked case exactly; scratch removed, sibling left clean. Three guards added to test_depgraph_snapshots.py (tracked-only computation incl. a regex for the bare --porcelain call, both blocks record untracked_present, README states the new meaning), all three PROVEN to fail on the reintroduced defect before being trusted (J26). test_depgraph_snapshots 7 -> 10 passed.
  - id: U16
    epic: self-documentation
    title: "Size the trade: bring the depgraph SCANNER in-house, or keep the sibling repo and the wrapper that has grown around it"
    type: task
    module: docs
    phase: 16
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - knowledge/depgraph-snapshots/snapshot.ps1
      - knowledge/depgraph-snapshots/README.md
      - docs/decisions/0002-c-depgraph-lineage-rehome.md
    acceptance: >
      A decision record lands PROPOSED in docs/decisions/ — never accepted from a groom;
      the user rules this — sizing both options with numbers rather than adjectives: what
      vendoring the scanner into the repo (drydocs_core/codegraph or a pinned package)
      costs in files, tests and lock changes; which depgraph surfaces DryDocs does not use
      and would inherit or drop; and what the status quo costs, which today is THREE
      producer-side post-processing steps wrapped around the sibling tool (U7's capability
      probe, U8's abs_path strip, U9's git-ignore filter), none of which could live
      upstream because each encodes something about THIS repo rather than about scanning.
      The record states a recommendation and the trigger that should force the call (a
      fourth wrapper step), and addresses the ADR 0002-C "absorb the assets, not the tool"
      precedent rather than ignoring it.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-42, whose own words are "a real trade, not a
      cleanup — size it before committing": this item SIZES and recommends, the user
      decides. Fable because it moves a component boundary. Precondition already
      satisfied — the depgraph fork is consolidated (depgraph 5006567, one branch), so
      there is a single revision to vendor from. Related but distinct: U15 fixes the
      dirty-flag defect that the split instrument makes easy to hit, and U7's 105-edge
      undercount is the defect class the in-housing argument is built on.
  - id: U17
    epic: self-documentation
    title: "DesignDoc.commit is the author's claim, not a git fact — say which one the tech-writer persona's staleness ranking uses"
    type: bug
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p3
    status: done
    depends_on: []
    inputs:
      - docs/reviews/code-graph-review-plan.md
      - docs/reviews/persona-tech-writer-2026-07.md
      - drydocs_core/models/doc_traceability.py
    acceptance: >
      The tech-writer persona's staleness ranking states which commit it ranks on — the
      commit the document CLAIMS to reflect (DesignDoc.commit, an author's assertion
      transcribed from the doc's own prose) or the commit that last TOUCHED the file (a
      git fact) — the plan records the rule WITH its reason, the query matches the rule,
      and the known divergence is used as the worked example: drydocs-startup-refresh-runbook
      carries a135a6d (2026-07-20) while its last touch is 554a4e8 (2026-07-31, Rev 5). If
      both readings are wanted, both are surfaced under distinct names; what may not
      survive is an undefined ranking.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-48. Both readings are defensible — which is
      precisely why the undefined state is the defect — so the item is allowed to pick,
      provided the reason is written where the next reader finds it. Epic U rather than L:
      the ranking belongs to the U1-U3 code-graph review personas, even though the
      :DesignDoc node itself is Epic L infrastructure.
      DONE 2026-08-09 (desktop): RULED — the re-verify queue ranks on the CLAIM, with the git fact reported beside it and the gap between them named as its own measure. Three names now exist where one word did: claim_lag (cited commit -> HEAD, the ranking key), touch_lag (last-touching commit -> HEAD, reported, never ranked on), citation_lag (cited -> last-touching, the 'edited but never re-verified' measure). WHY THE CLAIM WINS: the mandate is the status of documents joined to the code they CLAIM to describe, and a reader trusts the citation; ranking on touch_lag answers a different question and would hide this unit's own subject, since a doc edited ten times without ever being re-checked ranks as FRESH under it. WORKED EXAMPLE, re-measured at 4ecfca0 rather than copied from the item (the divergence widened a month): drydocs-startup-refresh-runbook cites a135a6d (2026-07-20, squash day), last touched by 180f4ae (2026-08-06) — claim_lag 792, touch_lag 122, citation_lag 670. The two readings put the SAME document at opposite ends of the queue, which is the entire argument for naming the key. The item's Rev-5/554a4e8 figures were the Idea-48-era snapshot and are superseded. citation_lag ratifies the label Run 2 had to invent in-flight ('stale cite, fresh content'). THREE DEGENERATE CASES RULED rather than left to whoever writes the query: no citation -> rank unknown, never fresh, never silently substitute touch_lag; citation unreachable from HEAD -> rank maximum, because an uncheckable claim is worse than one measured far behind; and the trap — a citation can RESOLVE and still not be an ancestor. VERIFIED, and machine-dependent (J18: desktop): all five pre-squash cites Run 2 listed (807e050, ac2ea2e, 97ee81c, 24d6a4b, 0e036ff) resolve as objects here because the local archive/old-history-2026-07-20 tag keeps them alive, and NONE is an ancestor of HEAD — so `git cat-file -t` passes them on this desktop and fails them on a fresh clone, and `rev-list --count` returns a meaningless number across the disjoint history. The check must be `git merge-base --is-ancestor`. No query-pack file exists for Phase 3 (the A1-A6 pack is Phase 1's, in the tech-debt skill; the documentation skill carries no graph queries), so the plan IS the query spec and now names the exact measures and commands. New guard file tests/unit/test_code_graph_review_plan.py (5 tests): the three measures are named, the ranking key is stated, the conflation cannot return, each degenerate case is ruled, the reachability check is ancestry not object existence. All five PROVEN to fail against the original Unit 2 text before being trusted (J26) — and that exercise caught a real defect in the first draft of the conflation guard: it was line-scoped, while the original defect spanned a line break, so it would have passed the very text it exists to catch.
  - id: C21
    epic: ontology-mapping
    title: "The schema meta-graph gets its own database and a bootstrap verb, because its exemplars cannot satisfy the domain graph's NODE KEYs"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done
    closed: 2026-08-02
    depends_on: [C8]
    inputs: [drydocs_core/ontology/schema_graph.py, drydocs_core/schema/schema_graph.cypher, drydocs/cli.py, tests/unit/test_schema_graph.py, tests/unit/test_schema.py]
    acceptance: >
      A `drydocs bootstrap-schema-graph` verb applies schema_graph.cypher to a
      database of its own and REFUSES (exit 2) if no :SchemaMeta node results; the
      meta-graph carries its own uniqueness constraint; constraints.cypher and
      test_schema.py's EXPECTED_CONSTRAINTS are untouched; the domain graph holds
      zero :SchemaMeta nodes afterwards; the generated file names its target database
      in its own header and the byte-match drift guard stays green.
    notes: >
      Groomed RETROSPECTIVELY 2026-08-02 (landed at e3f65af). The SME direction was
      "promote the meta-graph into bootstrap" — and doing that literally is
      IMPOSSIBLE, for a reason that is the direction's own point arriving as an
      error. Every exemplar MERGEs the REAL label beside the marker
      (:SchemaMeta:ControlMJob {name: ...}) because the real label is exactly what
      db.schema.visualization() reads; controlmjob_key is a NODE KEY, and a NODE KEY
      enforces EXISTENCE, not just uniqueness, while the exemplar carries only
      `name`. Proven LIVE on a throwaway label rather than inferred — "Node with
      label ZZProbe must have the properties (folder_id, job_id)" — then the probe
      constraint was dropped and zero nodes/constraints leaked. Dropping the real
      label would satisfy the constraint and destroy the feature. Hence two graphs,
      which is what the SME then said independently: "the schema graph is for the
      project labels and relationships with different constraints and the DryDocs
      project has different code and constraints, 2 different graphs". Verified after
      apply: 53 label nodes, 79 exemplar edges, visualization renders 54/288, and the
      domain graph's 17 :ControlMJob untouched. THE DATABASE IT TARGETS IS NOT
      PROVISIONED — G51.

  - id: G51
    epic: component-topology
    title: "The meta-graph database is named by shipped code and created by nothing — provision it, and widen the guard that let an unprovisioned name through"
    type: bug
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: [C21]
    inputs: [drydocs_core/schema/provisioning/01_databases.cypher, drydocs/cli.py, tests/unit/test_database_names.py, docs/decisions/0002-component-database-topology.md, config/gate-log.md, docs/design/drydocs-startup-refresh-runbook.md, .claude/skills/run-drydocs/SKILL.md]
    acceptance: >
      EITHER 01_databases.cypher creates the meta-graph database (with the ADR 0002
      topology record amended and test_database_names.py's
      test_provisioning_creates_the_expected_topology moved from four names to five
      in the SAME commit — that assertion is the deliberate speed bump, not an
      obstacle), OR the meta-graph is re-homed onto a provisioned database and
      drydocs/cli.py follows. Either way the name a shipped verb targets is one
      provisioning creates. AND the guard that should have caught this is widened:
      test_module_level_database_constants_are_provisioned keys on the exact
      identifier `DATABASE`, so a constant named SCHEMA_GRAPH_DATABASE walked
      straight past it — after this item, a module-level constant naming an
      unprovisioned database fails the suite whatever it is called. The SME direction
      that created a second graph ("2 different graphs", 2026-08-02) is recorded in
      config/gate-log.md, and the startup-refresh runbook + the run-drydocs skill
      name `bootstrap-schema-graph` in their chains.
    notes: >
      Groomed 2026-08-02 from the groom's own cross-check, not from the inbox. This
      is the Q7 family exactly — a database DECLARED by code and never provisioned —
      and it is live rather than theoretical: the standing fact of record is TWO
      MACHINES, TWO GRAPHS, so the desktop's `drydocs bootstrap-schema-graph` targets
      a database only the laptop has, created by hand during the C21 session. It
      fails loudly, which is the one mercy. THE GUARD FIX MATTERS MORE THAN THE
      CONSTANT: the constant is one line; the blind spot admits the next one, and it
      is the same family J26 sweeps — a guard whose docstring promises more than its
      pattern matches.

  - id: G59
    epic: component-topology
    title: "apply-supplements must not silently skip a supplement file that exists on disk"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done  # 2026-08-07 pm (desktop): CHAIN_EXCLUSIONS + unchained_supplement_files() in drydocs_core/schema/supplements.py (discovery lives with the registry so a consumer chain guards itself); 3 guards in tests/unit/test_supplements.py — silent-omission fails BY NAME (tmp-dir proof), exclusion hygiene (exists / not-chained / written reason). Suite green minus the two standing J33 ANSI fails
    depends_on: []
    inputs:
      - drydocs_core/schema/supplements.py
      - tests/unit/test_supplements.py
    acceptance: >
      A guard fails when a supplement file present in the schema directory is absent from
      the ordered chain, so a locally added supplement cannot be quietly omitted; a
      deliberate exclusion stays expressible and carries a written reason (the
      SCHEDULED_INGEST_EXCLUSIONS idiom), and the failure names the omitted file rather
      than reporting a count. The guard is portable — it must hold in a consumer repo
      whose chain carries supplements the producer does not have.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-52. The company runs two supplements the producer
      does not (Control-M quantitative resource pools, which feeds controlm_quantitatives;
      and the superseded platforms supplement, T12) — verified 2026-08-04 as genuinely
      company-local, not producer staleness. If they adopt apply-supplements as their one
      chain without adding those to their own list, resource-pools is omitted and whatever
      MATCHes its terms goes quiet, which is exactly how the pre-G29 three-verb block left
      load-software-registry MATCHing terms nothing had seeded. This guard catches the
      class on either side, which is why it is worth building producer-side.
  - id: G60
    epic: component-topology
    title: "Lineage file-ops pass reads CMD_LINE only — PRECMD/POSTCMD shell text carries the same file operations"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - drydocs_lineage/extractors/controlm_inventory.py
      - drydocs_core/orchestration/shell.py
      - drydocs_core/orchestration/controlm/variables.py
    acceptance: >
      The inventory extractor feeds pre/post-execution command text (the EMBEDDED_SHELL
      variables PRECMD / POSTCMD, including the observed POSCMD typo spelling) through the
      same G14 file-op grammar as CMD_LINE, with the SAME endpoints and NO new
      relationship types — so the mv/backup forms G14 cannot see today (parquet plus .tok
      to a backup path) become READS_FROM / WRITES_TO candidates. Coverage counters
      distinguish the CMD_LINE source from the pre/post source so the added yield is
      measurable, unparseable values are counted rather than dropped (the coverage house
      rule), and a unit test on a sample job whose POSTCMD moves a file proves the
      candidate is planned.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-20 clause (c). Verified at the groom: the
      extractor builds ONE command string per job (controlm_inventory.py, the `cmd`
      field) and nothing under drydocs_lineage/ references PRECMD or POSTCMD, while
      drydocs_core/orchestration/shell.py already parses that shell text and
      controlm/variables.py already names all three spellings — so this is a feed, not a
      new parser, and it stays inside G14's signed endpoints. Idea-20's other clauses stay
      in the inbox: (a) the ingestion-launcher jar classifier entry waits on a real
      sample, (d) cross-job runtime variable threading is context-graph work and a
      definition-level no-op.
  - id: G61
    epic: component-topology
    title: "Draft the gate prompt for two provenance gaps: artifact-not-in-source-control, and path-keyed Script identity where no pipeline id exists"
    type: task
    module: ontology
    phase: 6
    agent: main
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - internal/remediation/governance/nfr-consistency-and-greenfield.md
      - drydocs_core/ontology/relationship_vocabulary/
      - docs/restructure/03-hitl-sme-flow.md
    acceptance: >
      A gate prompt is drafted — drafting decides nothing (the G27 / W1 / N10 precedent),
      and nothing is written to the graph until sign-off — covering the two provenance gap
      classes the live FW-really-API case surfaced: (a) a flag for a payload script
      DEPLOYED on the execution host but ABSENT from source control, where a code search
      finds only the XML variable reference: what it is called, what it attaches to, and
      what a consumer may conclude from it; (b) Script identity for non-DPL jobs, which
      have no pipeline-id key at all: whether the PATH is the identity fallback, and
      whether the GUID-vs-path boundary is the kind discriminator. Every vocabulary or
      property term the prompt proposes is registered `planned` and stays planned until
      the gate signs.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-21. The anti-pattern itself is already codified
      (_FW-really-API plus design principle 8 — intent from resolved flow, flag the
      name-token disagreement); what is NOT settled is either gap class, and both are
      keying/meaning decisions, so they route through the gate rather than through a
      build. Checked at the groom: nothing under drydocs_lineage/ or config/ carries an
      in-source-control flag today, so the prompt starts from a clean sheet.
  - id: C22
    epic: ontology-mapping
    title: "Catalog loader sweep: the silent parent joins and the unconditional name SET that C17 fixed in products.cypher and nowhere else"
    type: bug
    module: drydocs-load
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 same session (desktop): (a) both parent joins take the products.cypher orphan shape — OPTIONAL MATCH, flag written EVERY run, unresolved id kept (pl.orphan_parent_lob_id / ap.orphan_parent_product_id); (b) coalesce(row.name, x.name) in all three files, AND the row models had to move with it — a required `name` would make a sparse refresh WORSE than blanking (whole-row reject, last-seen never advances), so ProductLineRow/ProductRow/AreaProductRow name went optional with ''-to-None normalization, ids stay required so optionality cannot leak into keying; (c) RULED EMIT — unresolved-parent counts ride the O28 envelope as `drydocs.loader/unresolved-parent` (warning, run-scoped via last_run_id, derived from the graph at close like rows_changed; the per-node flag stays the durable record), BaseLoader gains orphan_label + _count_unresolved_parents, envelope standard doc updated — the ruling recorded there and in status_items_for; namespaced type, no new envelope fields, no gate per the item's own analysis. Tests: parametrized orphan-shape + coalesce pins over all three files + null-name model + envelope item (+7 net, suite 1420/5). FOUND OUT OF SCOPE and inboxed (C17 precedent): dev_teams.cypher + catalog_lobs.cypher carry the same unconditional enrichment SETs, catalog_lobs blanking TODAY since its model already allows None.
    depends_on: [C17]
    inputs: [drydocs/loaders/cypher/product_lines.cypher, drydocs/loaders/cypher/area_products.cypher, drydocs/loaders/cypher/products.cypher, drydocs/loaders/catalog.py, drydocs/loaders/base.py, knowledge/standards/node-status-envelope.md, tests/unit/test_catalog_keying.py, tests/unit/test_node_status_envelope.py]
    acceptance: >
      (a) product_lines.cypher (parent :CatalogLOB) and area_products.cypher (parent
      :Product) take the products.cypher shape — OPTIONAL MATCH, an orphan flag
      written on EVERY run rather than only ON CREATE, and the unresolved parent id
      KEPT on the node. The defect: a hard MATCH placed AFTER the node MERGE leaves
      an unparented node still carrying `orphan: false` from ON CREATE, a flag no
      code path could ever set true. (b) All THREE catalog loaders stop blanking
      values on a sparse refresh — `SET x.name = row.name` becomes
      `coalesce(row.name, x.name)`, and the same treatment covers any other
      enrichment property a partial extract may omit. (c) The question C17 did not
      answer is answered and recorded: whether the unresolved-parent count is emitted
      as a `drydocs.loader/unresolved-parent` item in the O28 status envelope (a
      namespaced type, no new fields — inside the ruled contract, so no gate) or
      stays a node property only. Loader tests cover a row whose parent is absent and
      a row whose name is null.
    notes: >
      Groomed 2026-08-02 consolidating TWO inbox lines that land on the same files —
      one pass, one review (the L19 precedent): the 2026-08-01 [bug] parent-join
      sweep and the [source] company-comparison finding. C17 deliberately did not
      sweep ("changing four loaders' write behaviour on one item's authority is a
      drive-by") and inboxed it instead. VERIFIED AT THE GROOM rather than
      transcribed: the hard MATCH is live at product_lines.cypher:27 and
      area_products.cypher:22, and the unconditional name SET at products.cypher:28,
      product_lines.cypher:22 AND area_products.cypher:17 — so (b) covers three
      files, not the two the inbox line named. product_lines.cypher even carries a
      comment saying it is "the same silent-miss shape products.cypher just" fixed.
      The coalesce idiom is borrowed from the company comparison; no code is.

  - id: C23
    epic: ontology-mapping
    title: "The DQV quality seed floats: :Metric/:Dimension ship at bootstrap with no upstream measurement leg and no vocabulary entries — rule its disposition"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: done   # closed 2026-08-03 same session (desktop): SME RULED DEFER in-chat — seed stays as a reference catalog; revival trigger (first measurement feed = temporal-runtime freshness observations) recorded in ontology.cypher above the seed; vocabulary gap closed with four c23_* entries (IN_DIMENSION active, the three measurement-leg edges planned, new `quality` domain); gate-log entry dqv-seed-disposition; sdlc-neo4j-schema.md's declared-but-never-loaded row cites the ruling; EXPECTED_CONSTRAINTS unchanged
    depends_on: []
    inputs: [drydocs_core/schema/ontology.cypher, drydocs_core/schema/constraints.cypher, docs/history/LoadPlanV2.md, docs/reviews/sdlc-neo4j-schema.md, drydocs_core/ontology/relationship_vocabulary.yaml, docs/restructure/03-hitl-sme-flow.md]
    acceptance: >
      An SME-ruled disposition exists for the DQV measurement leg — exactly one of:
      (a) BUILD — the writers for the designed upstream chain (:QualityMeasurement
      -IS_MEASUREMENT_OF-> :Metric, COMPUTED_ON, HAS_QUALITY per LoadPlanV2 §DQV)
      are groomed as items naming their real feeds; (b) DEFER — the seed stays, with
      the trigger that revives it recorded where the next reader looks (ADR or
      ontology.cypher header); or (c) PRUNE — the seed rows, metric_name/
      dimension_name constraints (EXPECTED_CONSTRAINTS moves with them), and the
      LoadPlanV2 citation are retired together via the gate. Whatever the ruling:
      the vocabulary gap closes — IN_DIMENSION is a live bootstrap-written edge
      ABSENT from relationship_vocabulary.yaml, so it (and the three designed edges,
      if kept) get entries at the status the ruling assigns, through the HITL gate,
      never auto-decided. sdlc-neo4j-schema.md's declared-but-never-loaded row cites
      the ruling.
    notes: >
      Groomed 2026-08-03 by user directive, in-chat, minutes after the user wiped the
      local graphs: the one query that survived in memory was
      MATCH (:Metric)-[:IN_DIMENSION]->(:Dimension), and the user could not tell
      what the metrics were ASSOCIATED with — because nothing is. Verified at the
      groom rather than transcribed: ontology.cypher seeds 10 :Metric + 5 :Dimension
      + 10 IN_DIMENSION edges at bootstrap; the upstream leg LoadPlanV2 designed has
      NO writer (sdlc-neo4j-schema.md lists :QualityMeasurement/:Metric/:Dimension
      as constraints-declared-but-never-loaded); and none of the four DQV edge types
      appears in relationship_vocabulary.yaml — the seed predates the vocabulary
      discipline entirely. Model fable + HITL routing because every branch is an
      ontology decision (build = new edge semantics, prune = retiring ruled seed
      content); the TDQ metrics themselves (rowcount_match, freshness_sla, …) are
      exactly the control-file checks the temporal-runtime and TDQ planning already
      care about, so (a) has a real customer — that adjacency is for the ruling to
      weigh, not this groom.

  - id: C24
    epic: ontology-mapping
    title: "The C22 catalog sweep stopped at three files: dev_teams.cypher and catalog_lobs.cypher still blank enrichment values on a sparse refresh"
    type: bug
    module: drydocs-load
    phase: 2
    agent: main
    model: sonnet
    priority: p2
    status: done          # DONE 2026-08-05 (desktop) — coalesce on both files + DevTeamRow.name optional; the C22 pin EXTENDED to 5 loaders, not duplicated
    depends_on: [C22]
    inputs:
      - drydocs/loaders/cypher/catalog_lobs.cypher
      - drydocs/loaders/cypher/dev_teams.cypher
      - drydocs/loaders/catalog.py
      - tests/unit/test_catalog_keying.py
    acceptance: >
      Both loaders take the shape C22 ruled for the other three, and the two of them fail
      DIFFERENTLY, so the fix is not a copy-paste. (a) catalog_lobs.cypher's
      `SET l.code = row.code, l.name = row.name` becomes coalesce(row.x, l.x). This one
      loses data TODAY: CatalogLOBRow already declares code and name as `str | None`, so a
      sparse extract writes null over a stored value on every refresh. (b)
      dev_teams.cypher's `SET dt.name = row.name` gets the same treatment AND the row model
      moves with it — DevTeamRow.name is currently required (min_length=1), which C22 ruled
      is the worse failure mode, because a sparse refresh then rejects the whole row and
      last_seen_at never advances. name goes optional with ''-to-None normalization while
      team_id stays required, exactly as ProductLineRow/ProductRow/AreaProductRow were
      changed at C22. (c) C22's parametrized coalesce pin is EXTENDED to cover both files
      rather than duplicated, so the next loader added to this family is caught by the same
      test. Out of scope and deliberately untouched: the RECONCILES_TO edge, the Jira-board
      and parent-Product FOREACH blocks, and the orphan-flag question — neither loader
      writes an orphan flag and neither starts here.
    notes: >
      Found BY C22 and inboxed rather than swept, per the C17 precedent that a fix outside
      the acceptance's named file set gets its own item; C22's close note records the
      hand-off. VERIFIED at the groom (laptop, static read — no database needed):
      catalog_lobs.cypher:28-29 and dev_teams.cypher:16 carry the unconditional SETs;
      CatalogLOBRow.code/.name are Optional at drydocs/loaders/catalog.py:166-167 while
      DevTeamRow.name is required at :238.
      Why p2 and not p1: the blanking needs a SPARSE extract to fire, and today's catalog
      extracts are full refreshes — the same reasoning C22 carried. It is still real data
      loss the moment a partial extract is onboarded, and the fix is small and already
      designed.
      BUILT 2026-08-05 (desktop), all three clauses as written. (a) catalog_lobs.cypher
      coalesces BOTH `code` and `name` — the acceptance named `code` first for the right
      reason, and `name` had the identical defect one property over, so fixing one and not
      the other would have left the bug live in the more visible field (`code` is the
      human-readable LOB handle: AWMCIB, CCB). (b) dev_teams.cypher coalesces `name` and
      DevTeamRow.name went `str | None` with ''-to-None normalization, team_id still
      required. (c) The C22 parametrization now runs over FIVE loaders rather than three —
      extended, not duplicated, which is the clause that makes the sixth loader someone
      adds get caught without anyone remembering this item existed. Two new tests beyond
      the parametrization: the `code` coalesce (it has no `name`-shaped sibling in the
      generic pin) and the DevTeamRow model half, including that a row with no team_id
      still rejects — optionality must not leak into keying.
      Suite 1582 passed / 5 skipped. Two pre-existing RUF002/RUF003 lint errors on `×` in
      K15-era comments were fixed in passing; `ruff check .` is clean.

  - id: C25
    epic: ontology-mapping
    title: "Run the software-version-context HITL gate to sign-off: where a software VERSION fact attaches, and how an adhoc evidence email is referenced"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: done
    depends_on: []
    inputs: [config/gate-prompts/software-version-context.yaml, config/doc-source-registry.yaml, config/taxonomy/software-registry.yaml, drydocs_core/ontology/relationship_vocabulary/44-local-registry.yaml, config/gate-log.md]
    acceptance: >
      Sections A-E and G are ticked or explicitly deferred with a reason, transcribed to
      config/gate-log.md under a slug-named heading, and the gate's status flips from
      awaiting-sme. §A2 is answered FIRST and by the person who compiled the source table
      — whether a row asserts USAGE or AVAILABILITY — because if it is availability the
      USES_SOFTWARE label is wrong and §A3 requires the gate to PAUSE and return for a
      second draft rather than proceed through B-F on the wrong verb. §F (the
      application-level rollup) stays BLOCKED and is NOT signed here: it depends on K17.
      Any vocabulary entry confirmed is registered status: planned with its loader build
      groomed as a follow-up; no loader is built and nothing is loaded inside this item.
    notes: >
      Groomed 2026-08-05 (desktop) from the 2026-08-05 IDEAS entries. Sibling of C12/C14
      (the other USES_SOFTWARE rulings) — hence epic ontology-mapping, phase 2, not the
      K-series. model: fable: it registers a new vocabulary entry and rules a MERGE key.
      THE TWO THINGS THE GATE EXISTS TO PREVENT, both defects in the opening proposal:
      (1) Neo4j cannot attach a relationship to a relationship, so evidence attaches by
      property pointer, node-grain edge, or a reified assertion — never as an edge hung
      off the USES_SOFTWARE edge; (2) the evidence rows are (fid-name, install-path)
      rows, so writing the app-level edge directly bakes a mutable ownership join into a
      fact and discards the only grain at which it can later be re-derived. SEPARATELY
      WORTH KNOWING (corrected 2026-08-05 — an earlier draft of this note called it "a
      better USES_SOFTWARE source than this email", which overclaimed it): the Control-M
      SUB-APPLICATION field (PR<Appcode>-<Platform App Code>) declares WHICH PLATFORM an
      application runs on — documenting the platform is the naming standard's intent — so
      it is a standards-backed app→platform source at estate scale, good for a first-pass
      C1 container diagram. It is NOT a substitute for this email: it carries PRODUCT and
      never VERSION, so it does not touch the fact this gate is about. And it must be
      deduped to one edge per (application, product) — an app has many sub-applications
      all naming the same platform, so fanning them in at sub-application grain builds a
      super node on the product. Inboxed, not in this item; needs two software-registry
      product rows (DPL, Snowflake — see the correction below) first.
      MERGED IN 2026-08-05 (desktop, Idea-65's open residue): those TWO PRODUCT ROWS are
      now this item's prerequisite rather than a floating inbox line, because the gate
      cannot rule §C on a product vocabulary with holes in it. Two of the six framework
      codes have no `config/taxonomy/software-registry.yaml` row — DPL (the standing gap
      `invocation_patterns` already records, now with a name and a framework table behind
      it) and the SFS/Snowflake row. Registering them is taxonomy capture, reversible, and
      does not pre-empt any §C ruling: a product row says the product exists, not that any
      application uses it or at what version.
      CORRECTED 2026-08-05 (user, in-session): AWS Snowflake is a TARGET DB PLATFORM, not
      an ETL product — same family as AWS S3, AWS Glue tables, and AWS Iceberg tables. The
      DAT SRE table's "Snowflake ETL" label names the loads-into-Snowflake JOB FAMILY the
      SFS code marks, not a software product. So the missing registry row is `snowflake`
      the data platform (a DBMS-family category, sibling of `oracle-db`), never a
      "Snowflake ETL" tool row — and any app->platform edge derived from the SFS code
      reads "loads into Snowflake," not "runs a Snowflake ETL framework."

      DONE 2026-08-09 (desktop, SME in session): gate software-version-context SIGNED OFF for sections A-E and G; F NOT signed, still blocked on fid-identity-and-scope (K17). Full transcription in config/gate-log.md; the prompt's status flipped awaiting-sme -> signed-off with the decision block filled. THE HEADLINE, and it is the reason A2 is asked of the COMPILER rather than reasoned from the document type: A2 ruled USAGE, against this page's own recommendation, which said a readiness review usually inventories availability and cited the K7 OWNER-NOT-USER round as the warning. So A3 did NOT fire, USES_SOFTWARE is the right verb, and no second availability entry is needed. Had the gate reasoned it out instead of asking, it would have paused wrongly. CONFIRMED AS DRAFTED: A1, B1-B4, C1, C2, C4, D1, D3-D5, E1-E4. THREE RULINGS extended or differed from the draft: C3 never pads a missing component (absent is not zero -- 4.3.2 stays three components; version_raw keeps the verbatim token, and ragged comparison is the cost, paid openly); C5 adds an `evidence:` pointer block to the abinitio product row, because C2 correctly forbids observed versions from editing the curated ledger but left a row empty-because-nothing-is-known reading identically to empty-because-unreviewed (a pointer is not a version); D2 takes option (a), the evidence_doc_id property pointer -- (b) rejected as asserting the fid was sourced from the email rather than that THIS version claim was, (c) named as the upgrade path if a recurring feed arrives, with the pointer being what makes that migration mechanical. Q2 answered PARALLEL INSTALLS ARE REAL, which upgrades B2's multi-edge model from provisional to permanent: G3's 1:N holds, no uniqueness constraint, and NO `current` discriminator is needed -- the other answer would have shipped the load with a named gap. Q3 DEFERRED with its consequence stated rather than guessed: a symlinked estate would make install_path a poor MERGE key and move identity to (fid, version), a re-key, so the answer is worth having before the loader is built. PREREQUISITE DELIVERED FIRST (the notes' condition -- the gate cannot rule C on a product vocabulary with holes in it): `snowflake` and `dpl` product rows registered as taxonomy capture. DPL turned out not to be new modeling but a MISSING POINTER -- it was already an ETLProcess `engine` value (abinitio|dpl) while Ab Initio carried both an engine value AND a products: row, and that asymmetry had already cost the 2026-07-27 G26 guard catch, which removed the abinitio-dtlaunch-wrapper pattern row because dtlaunch.sh is the DPL spine and coverage_policy forbids a pattern row without a product id. SME supplied the expansion (DPL = Data Pipeline Library, registered in the acronyms block for the same reason AIS is there) and the mechanism (prepare/ingest/transform/provision pipeline types; Zilo is Spark-based, refining the 2026-07-16 note; EKS/GKP with Hortonworks heritage). versions: [] on dpl is deliberate -- the base image is datapipeline-apache-spark-3.5.1 tagged with the DPL image version, so 3.5.1 is the EMBEDDED SPARK and writing it here would record a dependency's version as the product's. VENDOR MODEL: new `in-house` vendor with NO publisher_url; a placeholder URL was considered and rejected as a value that resolves to nothing and is indistinguishable from a stale link. Guard narrowed to third-party vendors via an explicit allow-list, plus a second guard that a product of an exempt vendor must be type: internal so the exemption cannot launder a missing URL. Both PROVEN to fail on their defect -- and the FIRST proof round was worthless and is recorded as such: the registry file is CRLF, the injection patterns were LF, so neither defect was ever injected and both tests 'passed' against unmodified input. NOTHING LOADED, no loader built; reg_appuser_uses_software registered status: planned. Follow-ups carried out: the adhoc loader build, the C1 pattern rows (now unblocked for DPL), F re-run once K17 signs, and a PORT RELAY -- the producer is canonical for the two product rows, the in-house vendor and the DPL acronym; the SME began the same expansion company-side 2026-08-07 and stopped so the two would match. The relay is deliberately NOT written into docs/port-prompt.md yet: a port is in flight against a fetched head and that file is a hand-merge surface.
  - id: C26
    epic: ontology-mapping
    title: "Write the company-catalog divergence down where a port will read it, and reserve the four divergent catalog shapes as planned — without adopting any of them"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs: [.claude/skills/reconcile-port/SKILL.md, config/taxonomy-ontology-map/30-mappings-catalog.yaml, drydocs_core/ontology/relationship_vocabulary/42-local-catalog.yaml, config/taxonomy/lob-product-team.yaml, docs/restructure/IDEAS.md, config/gate-log.md]
    acceptance: >
      The catalog divergence between the company gate page (`internal/org/catalog/`, dated
      2026-06-25) and the producer catalog ontology is RECORDED in two places a future port
      cannot miss, and NOTHING is adopted. (a) A divergence-ledger entry in the reconcile-port
      skill naming all five differences: the `:SubLOB` + `HAS_SUB_LOB` grain we do not model,
      the widened `HAS_PRODUCT_LINE` range `(:SubLOB|:LOB)`, the label split
      `:LOB {lob_id, name}` vs our `:CatalogLOB {lob_id, code, name}`, the map-id pair
      (`sub-lob-org-unit` / `catalog-lob-reconciles-segment` vs our `lob-has-product-line` /
      `lob-reconciles-to-segment`), and the 5-field `pat_lob_sublob_productline.csv` grain our
      `lob-product-team.yaml` capture lacks a Sub-LoB Name column for. (b) The divergent
      relationship shapes are registered `status: planned` in the vocabulary and the catalog
      mapping file, each carrying the trigger that would confirm it — reserving the NAME so
      the port cannot collide on it, which is exactly the G34 glossary-scaffold argument, and
      asserting no meaning (that is what `planned` is for). (c) Two rulings are named in the
      entry as things the eventual gate must settle TOGETHER, not one at a time: the label
      ruling (`:LOB` vs `:CatalogLOB`) and the key ruling (the S3 `app_id` rename the company
      page pre-dates). (d) The two relayable findings are stated as relay items, not producer
      work: the page's functional-org target "Corporate" is ambiguous against our seeded
      `:BusinessSegment {code:"Corp"}` and MERGEs a phantom segment if read as a code, and the
      page's `drydocs/schema/ontology.cypher` path is period-correct for its date (pre-G2
      Phase-B relocate), so refresh it only if the prompt is revised. No loader changes, no
      taxonomy capture rewritten, no map entry flipped to `confirmed`.
    notes: >
      Groomed 2026-08-05 (desktop) on the user's direction to close out the catalog mapping
      area. This is the ACTIONABLE HALF of the parked 2026-07-27 IDEAS entry. The parked
      entry's trigger — the COMPANY gate's own sign-off — still gates ADOPTION, which is C27;
      it does not gate writing the divergence down, and leaving it written only in an inbox
      line is how a port silently picks a side. Two sibling IDEAS entries explicitly said
      "fold into that back-flow item; do not open a second one" and are absorbed HERE plus
      C27: the 2026-08-01 Sub-LoB entry (confirmed built company-side — `parent_sub_lob_id`
      anchoring under `MERGE (sl:SubLOB …)` with a `:LOB {lob_id}` fallback, both via
      HAS_PRODUCT_LINE) and the three producer-side gaps parked in the 2026-08-02 entry
      (`pat_app_links` stub governance, `pat_product_owners`, the `products` step-2a
      supplement fields).
      WHAT IS ALREADY SETTLED AND MUST NOT BE RE-OPENED: the C17 gate-log entry (2026-08-01)
      already records "NOT decided here … it already belongs to the parked company-catalog
      back-flow item", so this item is that entry's designated home — the ruling stays
      unmade, it just stops being unfindable. The gate MECHANICS matched all the way through
      (localStorage ticks, no-write-until-confirmed, `{confidence, authority, aliases}` on
      RECONCILES_TO, skos:closeMatch aliases, precedence winner `lob-product-team`) — content
      drifted, mechanism did not, so nothing about gate_pages.py is in question.
      ONE FREE RESULT worth taking while here: the company page shows CIB and AWM as SEPARATE
      LoBs with 1.0 exact matches, which resolves the LOB002 AWMCIB open question (legacy,
      0.5) in `lob-product-team.yaml` — a taxonomy capture fix, reversible, no edge meaning.

  - id: C27
    epic: ontology-mapping
    title: "Reconcile the catalog divergence through a gate once the company gate signs: the Sub-LoB grain, the LOB label ruling, and the app_id key in ONE pass"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [C26]
    inputs: [config/gate-prompts/, config/taxonomy-ontology-map/30-mappings-catalog.yaml, drydocs_core/ontology/relationship_vocabulary/42-local-catalog.yaml, drydocs/loaders/cypher/catalog_lobs.cypher, drydocs/loaders/cypher/product_lines.cypher, config/gate-log.md]
    acceptance: >
      A gate prompt is drafted and RUN, and its outcome transcribed to config/gate-log.md
      under a slug-named heading. It rules, in one session because they interact: (a) whether
      `:SubLOB` + `HAS_SUB_LOB` enter the producer ontology and with what range widening on
      HAS_PRODUCT_LINE; (b) the label ruling `:LOB` vs `:CatalogLOB` — the two sides write
      DIFFERENT labels for the same thing, so the company's `:LOB {lob_id}` fallback branch
      and our `catalog_lobs.cypher` cannot both be right; (c) the map-id reconciliation; and
      (d) how the S3 `app_id` key lands on shapes authored before the rename. Confirmed
      entries flip from `planned` to `confirmed` with their loader builds groomed as
      follow-ups; anything deferred is recorded with the consequence stated. The C22/C24
      coalesce and orphan-flag conventions are applied to any loader this touches, not
      re-litigated.
    notes: >
      TRIGGER, and it is external: the COMPANY gate's own sign-off. Producer sign-off is not
      company sign-off (the standing rule — their tracker is T1-T8/DD*), and adopting a shape
      from an UNSIGNED company gate page would import a proposal as if it were a ruling. So
      this item stays `todo` and simply does not become ready until C26 is done AND the
      trigger fires; do not "unblock" it by relaxing the dependency.
      WHY THE FLATTENING IS URGENT ENOUGH TO GATE RATHER THAN SHRUG AT: our chain runs
      BusinessSegment → CatalogLOB → ProductLine → Product → AreaProduct while the source runs
      LoB → Sub-LoB → Product Line → …, so we silently flatten one level — and the flattening
      is INVISIBLE rather than merely absent, because `parent_lob_id` on a product line will
      carry whatever the extract puts there. A sub-LoB id landing in a `:CatalogLOB`-keyed
      field MERGEs a phantom LOB that looks exactly like a real one. Corroborated three ways
      (SME statement 2026-08-01, the FCDO capture's "5-level hierarchy … native IDs at each
      level", and the company's own gate page).

  - id: L22
    epic: doc-infrastructure
    title: "Adopt the U.S. business-English style guide: wire it into the writing path, rewrite the executive overview, inventory the idioms elsewhere — without renaming mechanisms"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-03 same session (desktop): (a) guide wired into CLAUDE.md §6 + documentation skill; (b) overview rev 7 — 21 occurrences rewritten, HTML-stays-source decision recorded in the rev line, rev history kept verbatim; (c) whitepaper's 2 "rot" hits rewritten in .md + both renders, site-plan clean; (d) fence held — nothing renamed, candidate list at docs/style/l22-idiom-inventory.md (review spine, lexical spine, guardrail N, gate-confirmed, HITL `confirmed`)
    depends_on: []
    inputs: [docs/style/us-business-english.md, docs/overview/drydocs-executive-overview.html, docs/whitepaper/, CLAUDE.md, .claude/skills/documentation, UI-WIP/site-plan.md]
    acceptance: >
      (a) The committed guide (docs/style/us-business-english.md, landed with this
      groom) is WIRED into the writing path: CLAUDE.md's working agreements point at
      it for outward-facing prose, and the documentation skill references it — so
      the next doc Claude writes follows it without being told. (b) The executive
      overview is rewritten per the guide: its 21 counted occurrences of
      spine/plane/crosswalk/guardrail/gate-confirmed drop to zero outside
      fenced-mechanism references; NOTE it is HTML-only today (no .md source) —
      decide and record whether the rewrite establishes an .md source or edits the
      HTML directly (it is a NON-GOVERNED editorial surface per CLAUDE.md §6, so
      direct edit is legal; the decision just has to be written down). (c) The
      idiom inventory runs across the OUTWARD-FACING set only — overview,
      whitepaper, website seed content — each hit rewritten or fence-noted.
      (d) THE FENCE HOLDS: no mechanism is renamed by this item — the HITL status
      value `confirmed`, port-prompt guardrail numbering, and any identifier in
      code/config/schema keep their names; the sweep OUTPUTS a candidate-rename
      list (term, where it lives, what a rename would touch) for the user to rule
      on separately if wanted. "Crosswalk(s)" is NOT a hit and NOT a candidate:
      SME-approved 2026-08-03 (matches company-internal service/lookup
      terminology) — the guide records the exception. Governed surfaces (docs/design/* renders, gate
      pages, board) are OUT of scope — their prose changes ride their own rev
      process, per the VERBATIM rule.
    notes: >
      Groomed 2026-08-03 by user directive, the second of the day: the user read
      the executive overview, hit "spine" and friends, accepted them at the time,
      then had another LLM articulate why the idioms fail a U.S. enterprise
      audience — the resulting instruction set is committed verbatim-faithful as
      docs/style/us-business-english.md in this same groom (with a DryDocs scope
      fence added on top: several banned words are load-bearing mechanism names
      here, which is exactly why the user predicted this "will probably touch way
      more than documentation"). Style-vs-mechanism is the whole judgment of this
      item; the acceptance encodes it so sonnet can execute. The overview counts
      21 flagged occurrences today (measured at the groom). Memory updated in the
      same session so future sessions write to the guide from the start.

  - id: L23
    epic: doc-infrastructure
    title: "controlm-runbook-automation-SDLC skill: generate the long-form Application Run Book from the graph — the sibling the -excel skill already promises"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - docs/design/templates/sdlc-app-runbook.outline.yaml
      - docs/design/templates/sdlc-app-runbook.example.md
      - .claude/skills/controlm-runbook-automation-excel/
      - .claude/skills/controlm-runbook-automation/
      - tests/unit/test_doc_outline.py
    acceptance: >
      A `.claude/skills/controlm-runbook-automation-SDLC/` skill that emits an
      outline-conformant SDLC-Runbook .md per application/folder from the graph, proven by
      the validation that already exists: a document generated for a sample folder passes
      sdlc-app-runbook.outline.yaml validation with no hand-editing. The skill labels every
      outline section GRAPH-DERIVED or SME-RESIDUE, the way -excel's template-spec.yaml
      labels every column — per-workflow blocks (Control-M job, schedule, param file,
      src-stg-target schema@DB, table grain, task table), the schedule table (trigger files,
      alert windows) and the SLA table are graph-derived; contacts, vault safes and
      special-case recovery are named as SME capture with a placeholder shape. Where a query
      overlaps the -excel skill it is SHARED or cited, never re-written: two skills
      disagreeing about the same runbook fact is the failure this clause exists to prevent.
      Committed files stay mechanism-only; a FILLED runbook is Internal and lives in
      internal/ or internal-local/, the rule -excel already carries. The -excel SKILL.md's
      forward reference to this sibling resolves once it lands.
    notes: >
      Groomed 2026-08-04 (weekly groom). Not a speculative shape — the shipped -excel
      skill's own frontmatter names it: "a future -SDLC sibling owns the long-form
      Word/markdown run book". VERIFIED at the groom: both sibling skills exist
      producer-side (.claude/skills/controlm-runbook-automation/ and
      .claude/skills/controlm-runbook-automation-excel/, the latter carrying
      template-spec.yaml + generate_template.py + controlm-runbook-template.xlsx), and the
      doc type landed the same day at 995eb9a — sdlc-app-runbook.outline.yaml (385 lines),
      a 631-line worked example, and 32 lines of outline tests. Contract, exemplar and
      precedent are all in place; the generator is what is missing.
      Feeds are the ones -excel already uses (the Control-M graph, cm_avg_run, the
      escalation DB), and -excel's measured coverage — 31 of 35 job columns graph-derivable
      — is the honest expectation to carry over with one correction: the long form has MORE
      prose sections, so its SME residue is proportionally LARGER, not smaller. Filed under
      Epic L because the outline system and its doc types live there. This is a doc
      GENERATOR and stays distinct from drydocs-docmeta, which ingests documents.

  - id: L24
    epic: doc-infrastructure
    title: "State the production-estate target state as a goal in the executive overview"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done                        # 2026-08-07 (LAPTOP): overview rev 8. §06 gains "Target state: only actively used code on the production server" — stated as a GOAL ("a target the loops above move toward, not a description of today"), in plain English with NO coined term, with the coverage limit kept WITH the goal rather than exiled to §03's gaps section, and the three review outcomes named. Both revision surfaces updated + the header REVISIONS block; rev 1-7 kept verbatim. Style-checked against the banned-vocabulary list; tag balance verified
    depends_on: []
    inputs:
      - docs/overview/drydocs-executive-overview.html
      - docs/style/us-business-english.md
      - config/gate-log.md
    acceptance: >
      The executive overview states, as a named TARGET STATE goal, that the
      production estate should hold only actively used code — replaced code
      having been archived to git/storage and removed from the server.
      (a) It reads as a GOAL, not a shipped fact. The overview's own convention
      for not-yet-true material is explicit ("Dashed borders mark what is
      proposed, planned, or gate-pending; as each closes, it flips to solid and
      a revision line is added above") — honour it, or state the goal in prose
      that is unambiguously forward-looking.
      (b) It is stated in PLAIN ENGLISH per docs/style/us-business-english.md,
      with NO coined status term. Coining an estate-level word here would be a
      vocabulary decision, and that is deliberately not this item's to make
      (see G58's notes).
      (c) The honest limit rides with the goal rather than being left to the
      reader: measuring it depends on scan coverage, because an absence
      observed by one feed is not an absence in the world (G22 §H1). The
      overview already carries an "Honest about the gaps" section — that is the
      natural companion placement.
      (d) BOTH revision surfaces are updated: the "Revision N" line in the body
      and the rev-history entry at the foot, with prior rev lines kept verbatim
      (the L22 convention).
      Renders unaffected — this file is not renderer output (see notes).
    notes: >
      Groomed 2026-08-07 (laptop) from Idea-82, on explicit user direction that
      the target state appear in the executive overview as a goal and not only
      as a backlog item.
      TWO FACTS TO KNOW BEFORE OPENING THE FILE, both verified at the groom.
      (1) docs/overview/drydocs-executive-overview.html is HAND-AUTHORED HTML
      and its OWN SINGLE SOURCE — there is NO .md twin (unlike
      docs/whitepaper/), it is not renderer output, and
      scripts/render_design_doc.py does not cover docs/overview/. Edit the HTML
      directly; the file's own header says so, and L22 recorded the
      HTML-stays-source decision in its rev-7 line.
      (2) It is a NON-GOVERNED outward-facing doc (CLAUDE.md §6), so editorial
      and design treatment DO apply here — the publish-VERBATIM rule binds the
      design renders, gate pages and board.html, never this file. This is one of
      the few surfaces where an artifact-design pass is legitimate.
      PLACEMENT: no target-state section exists today. Nearest existing homes
      are "The loop that makes it a system, not an inventory" (the
      operational-outcome section) and "Honest about the gaps" (for (c)); a
      short section of its own is also reasonable.
      DEPENDENCY-FREE ON PURPOSE: stating a goal needs neither the report (G58)
      nor a formal term. If a coined term ever lands, it rides a gate and the
      overview follows it, not the other way round.
      CLOSED 2026-08-07 (laptop) as overview rev 8. THREE DECISIONS TAKEN AT THE
      BUILD, recorded because the acceptance left each open.
      (i) PLACEMENT: the end of §06's prose, not §03's "Honest about the gaps".
      §06 closes on what the loops achieve, so an outcome statement belongs
      there — and clause (c) asked for the limit to ride WITH the goal, which
      splitting the two across sections would have defeated. §03's gaps section
      is about VOCABULARY mapping gaps; a scan-coverage caveat would have read as
      off-topic there.
      (ii) THE FORWARD-LOOKING MARKER IS PROSE, not a dashed border. The
      dashed convention is a mermaid classDef (stroke-dasharray on diagram
      nodes) with NO prose equivalent in this file's CSS, so clause (a)'s second
      option was taken: the text says so outright.
      (iii) TENSE CORRECTED AGAINST REALITY at the build: G58 landed on desktop
      the same day (drydocs_lineage/archival.py), so the REPORT is real and the
      GOAL is what remains unreached. The section says the system can report
      progress as a number — verified against the shipped docstring rather than
      described from the backlog item.
      Filed under Epic L because the overview is a documentation surface; the
      report that MEASURES the goal is G58, in the component-topology epic with
      the rest of the rua chain.

  - id: L25
    epic: doc-infrastructure
    title: "Signed gate pages get riders, not edits: apply the step-83 rider pattern to the business-application-identity §D2 line-number drift and write the rule down"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - docs/restructure/03-hitl-sme-flow.md
    acceptance: >
      The business-application-identity gate page's §D2 citation
      (seal_applications.cypher 124/147/170 + seal_contacts.cypher 53, now
      152/175/198 + 55) gains a dated rider correcting the line numbers WITHOUT
      editing the signed text; the rule — a signed gate page is a historical record,
      drifted citations are corrected by rider, never in place — lands in
      docs/restructure/03-hitl-sme-flow.md where L19's doc-drift sweep will find it.
    notes: >
      Groomed 2026-08-07 from Idea-72. The rider default follows the step-83
      precedent the entry names; if the SME prefers in-place correction for pure
      line numbers, the rule doc is the place to record that instead.
  - id: L26
    epic: doc-infrastructure
    title: "The documentation skill has no white-paper type, though the repo has a white paper"
    type: chore
    module: docs
    phase: 10
    agent: main
    model: haiku
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - .claude/skills/documentation/SKILL.md
      - docs/whitepaper/drydocs-whitepaper.md
    acceptance: >
      The documentation skill gains a "White paper" document type alongside README, API,
      runbook, architecture and onboarding, with its section sequence stated (executive
      summary, problem, approach, architecture, governance, roadmap) and
      docs/whitepaper/drydocs-whitepaper.md named as the worked example that sequence was
      derived from; the skill's type list and its type-selection prose agree, so a future
      white paper is not re-derived from the architecture type by hand.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-8. The whitepaper was written by deriving
      structure from the architecture type plus general white-paper convention — the
      derivation worked, it just was never written down. The optional half of the entry
      (an Epic L outline, whitepaper.outline.yaml, so the render pipeline validates it) is
      deliberately NOT in this item: outlines govern design docs, and the whitepaper is
      explicitly a non-governed outward-facing document where editorial treatment is
      allowed (CLAUDE.md, governed renders).
  - id: J26
    epic: release-infrastructure
    title: "Guards that read committed text with a bare substring match — sweep the shape that made a file unable to describe its own mechanism"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done   # closed 2026-08-04 same session (desktop, J-series batch): inventory shipped as tests/unit/test_text_guard_conventions.py's module docstring — ~227 read_text sites classified by FAMILY with a disposition each (yaml/json-parsed and AST scans structural by construction; line-anchored counts structural since S3; PORT-MANIFEST walk structural — yaml rows + glob-to-regex + prefix append-only check; positive prose pins and escape-hatched prose bans noted as correctly-bare). SWEPT at this item: test_schema's label + supplement checks and ontology bare-CREATE ban now read strip_comments() text (closing the commented-out-MERGE false-pass, the G29 lesson), test_controlm_cypher's seven negative-assertion tests route through new _cypher_code/_sql_code helpers, test_catalog_keying's C22 guards extracted + comment-stripped. Regression proofs: products.cypher with the ENTIRE pre-C17 defect quoted verbatim in comments still passes its guards, and the shared _DECLARATION_RE ignores comment-quoted DDL (+3 tests net with J-batch siblings; suite 1427/5). test_database_names verified already-structural (G51 fixed the instance; its docstring cites this item as the class owner). Promise-vs-assertion sub-family covered in the inventory with the G51-tail bidirectional fix as the worked example; J28 closed the fourth family member same batch.
    depends_on: []
    inputs: [tests/unit/test_schema.py, drydocs_core/schema/constraints.py, tests/unit/test_port_reconcile_guards.py, tests/unit/test_database_names.py, scripts/render_board.py, tests/unit/test_text_guard_conventions.py, tests/unit/test_controlm_cypher.py, tests/unit/test_catalog_keying.py]
    acceptance: >
      Every guard that asserts over the TEXT of a committed file is inventoried, and
      each one either matches STRUCTURALLY (anchored at line start, or parsed) or
      carries a one-line note saying why a bare substring is correct there. At
      minimum the named candidates are checked: the render-drift checks, the
      PORT-MANIFEST walk, and test_database_names.py — whose module-constant guard
      keys on the exact identifier `DATABASE` and therefore missed a differently
      named constant (G51 owns that fix; this item owns the class). A regression test
      proves at least one swept guard now survives a file describing its own
      mechanism in a comment.
    notes: >
      Groomed 2026-08-02 from the 2026-08-01 [chore] line. The instance:
      test_schema.py::test_constraint_count counted `CREATE CONSTRAINT` ANYWHERE in
      the file, so documenting a DDL trap in a `//` comment inflated the count and
      simultaneously false-failed test_constraints_are_idempotent (the comment "has
      no IF NOT EXISTS"). S3 fixed it by anchoring both at line start, mirroring
      constraints.py::_DECLARATION_RE, which the bootstrap presence guard already
      used. The generalisation is the point: a guard that reads prose makes the file
      unable to explain itself, and this repo's whole doctrine is that the reasoning
      IS the audit trail.
      SECOND INSTANCE (merged 2026-08-04 from the G51-tail inbox line): the same
      day produced a sibling of the same family —
      test_databases_match_provisioning_script promised "exactly what
      01_databases.cypher creates" in its docstring and asserted only yaml ⊆
      cypher, so a database added to the DDL and not the config passed green. Made
      bidirectional at the G51 tail (aa0a0eb) and it FAILED on ['ddschema'] before
      the config fix — the audit proven, not argued. The sweep's inventory should
      cover promise-vs-assertion mismatches (docstring says equality, code checks
      subset), not only substring anchoring. J28 is the third family member (a
      classifier, kept separate as a bug with a proof case).

  - id: J27
    epic: release-infrastructure
    title: "Two .gitignore comments name the real organisation and its internal domain — apply the CLAUDE.md §3 rule, or record the exception once"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 same session (desktop, J-series batch): DEFAULT branch taken — both comment lines reworded to describe the corpus (employer-internal SDLC source docs; the reword says in-line why it names nothing), no exception recorded; the guardable shape WAS guardable, so J15's guard gained scan D: .gitignore may name NO domain-shaped token (empty allowlist; the test embeds no real value and was proven against the removed line before the reword landed — regex catches the exact string that was there).
    depends_on: []
    inputs: [.gitignore, PUBLISH-BOUNDARY.md, CLAUDE.md, tests/unit/test_publish_boundary_values.py]
    acceptance: >
      Neither .gitignore comment names the organisation or its internal domain: both
      are reworded to describe the CORPUS they explain (which is the useful half
      anyway), OR — if the user rules that a private producer repo may carry them —
      PUBLISH-BOUNDARY.md states that exception explicitly, once, naming the lines it
      covers. What is not acceptable is the current state, which is neither. If the
      ruling admits a guardable shape, J15's value-shape guard is extended to it.
    notes: >
      Groomed 2026-08-02 from the 2026-08-01 [chore] line — seen at J24 while
      rewording the retired-tier labels on those same lines and deliberately left
      alone there, because half-redacting one line while the next still carries the
      domain is worse than leaving both. NOT a decision item by default: CLAUDE.md §3
      already bans real org names outside internal/, and .gitignore sits at the root
      of a repo that is sometimes published — so the rule exists and this applies it.
      The exception branch stays available because the boundary is the user's to set.

  - id: J28
    epic: release-infrastructure
    title: "A gate with only partial rulings renders as CLOSED: render_gates.py classifies a prompt by any log entry that mentions its slug, never by whether its own sign-off was recorded"
    type: bug
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 same session (desktop, J-series batch): the classifier now decides by HEADING IDENTITY — an entry accounts for a prompt only when the slug appears in the entry's SLUGIFIED heading (prose headings like "SEAL attribution match policy gate" still match) and the heading does not say PARTIAL; body citations populate prompt_files for display but never close a gate; a prompt may also self-declare via a `# SIGNED OFF ...`/`# DEFERRED ...` marker line (the F1/F2 crosswalk prompts got the markers their siblings already carried — the log's prose headings predate the slug convention, and 13/17-confirmation full acceptances are on the gate-log record). PROOF CASE OVERTAKEN BY EVENTS, recorded honestly: seal-app-ref-edge-reshape was SIGNED at K7 2026-08-03 (heading-named, 24/24) AFTER this item was groomed, so it correctly does NOT render open — the mechanism is pinned instead on its exact pre-K7 three-entry pattern as a fixture test, plus a live pin that an open row for it now means the classifier regressed. The fix's LIVE catches: software-usage-patterns (a genuinely awaiting gate hidden behind a mini-gate citation — the current-day incarnation of the bug) and seal-tom-attribution-reshape (own entry explicitly PENDING, body-cited by K3's sign-off) both render open with the new "sign-off not recorded" title. gates.json 41→43 rows, 8→10 open; render deterministic (test_render_determinism untouched and green); +3 tests.
    depends_on: []
    inputs: [scripts/render_gates.py, tests/unit/test_gates_json.py, config/gate-log.md, web/src/generated/gates.json, config/gate-prompts/]
    acceptance: >
      A gate prompt whose sign-off has never been recorded renders as OPEN even when
      gate-log entries mention its slug. render_gates.py stops deciding "matched" by a
      bare substring scan of the section body (line 84 today) and distinguishes an
      entry that SIGNS a gate from one that merely CITES its prompt file: at minimum a
      partial-ruling entry and a different gate's sign-off entry must both leave the
      cited prompt in the open list. seal-app-ref-edge-reshape is the proof case — it
      must appear among the prompt-only/open rows after the fix, and does not today.
      A regression test in test_gates_json.py pins that case, gates.json and the board
      regenerate, and the render stays deterministic (test_render_determinism.py).
    notes: >
      Found 2026-08-03 while reviewing the Application ↔ Control-M attribution thread
      on user request. Three gate-log entries mention the slug — 2026-07-23
      (recorded), 2026-07-27 (a DIFFERENT gate's sign-off that cites the file), and
      2026-08-01 (PARTIAL RULING, recorded) — so the prompt counts as matched and is
      never emitted as prompt-only. The board therefore showed nothing open while that
      same 2026-08-01 entry says in as many words that the gate as a whole is still
      unsigned. Six weeks of governance work was invisible on the surface built to
      make it visible, and it took a manual read of the gate log to find.
      SAME FAMILY AS J26 — a guard or classifier that reads committed text with a bare
      substring match and promises more than it checks — and the same shape as the two
      found on 2026-08-03 (test_database_names.py keyed on the literal identifier
      DATABASE; test_databases_match_provisioning_script asserting a subset while its
      docstring promised equality). Kept separate from J26 because this one is a BUG
      with a named proof case, not an inventory sweep; fold it into J26 only if that
      lands first.

  - id: J29
    epic: release-infrastructure
    title: "Encoding standard for loader-read text formats: UTF-8 WITHOUT BOM on every .cypher/.sql/.csv, guarded"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done          # groomed straight to done 2026-08-04 (desktop, the G52 landed-in-one-pass precedent) — user directive during the M3-reload follow-up
    depends_on: []
    inputs: [tests/unit/test_file_encoding.py, internal/registry-redesign/samples/]
    acceptance: >
      No tracked or untracked-not-ignored .cypher/.sql/.csv file starts with a UTF-8
      BOM; a guard test walks the tree (the J22 tracked+untracked walk) and reds any
      offender with a fix message; existing BOM carriers in those formats are
      stripped; vendor .xsd captures are exempt with the reason recorded. Guard
      proven live on a BOM'd probe before close.
    notes: >
      SME ruling 2026-08-04 in-chat: UTF-8 was the SME's deliberate readability
      choice when asked; the BOM was never part of it — standardize all Cypher/CSV
      import surfaces on UTF-8 WITHOUT BOM now. DIAGNOSIS CORRECTED at build: the
      2026-08-04 M3-reload incident's BOM did NOT come from the provisioning files
      (byte-scan: every tracked .cypher and .sql is clean) — it was injected by a
      PowerShell 5.1 pipe into docker exec, which is precisely the writer-artifact
      class the guard fences (Out-File/'>' default UTF8-with-BOM, Excel CSV export).
      Real carriers found and stripped: the three internal/registry-redesign/samples
      CSVs (a BOM'd CSV read with plain utf-8 keeps U+FEFF glued to the first
      header, silently breaking populate_by_name matching). The nine vendor .xsd
      captures keep their BOMs: XML self-declares encoding, parsers consume the
      BOM, and they are VERBATIM-trust captures we do not edit. Guard =
      tests/unit/test_file_encoding.py, proven red on an untracked probe then green.
      The provision.ps1 cypher-shell-on-PATH gap stays inboxed separately.
      (Groomed 2026-08-04 as G54.)

  - id: J30
    epic: release-infrastructure
    title: "Dispose of wip/k9-laptop: lift anything worth keeping off the second K9 implementation, then delete the branch local and remote"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (laptop). PER-FILE COMPARISON, both vs their common base 3608ae5 (the claim commit — so neither is derived from the other): SHIPPED 17d9e08 = 658 insertions / 12 files; LAPTOP bfb2f0b = 447 insertions / 115 deletions / 8 files. Shipped wins on three counts and it is not close: (a) TESTS — +106 lines of store tests and +160 API vs the branch's +1 and +77, and the branch DELETED test lines it did not replace; (b) LAYERING — the branch duplicated APP_CODE_TIERS/APP_CODE_ORIGINS into drydocs_api, where shipped keeps them in drydocs_core and shares validate_app_code_row, so the API cannot drift from the store's own rule set; (c) RITUAL — shipped regenerated the enforcement matrix, re-rendered the board and closed the item; the branch did none of it. The branch's READMEs are longer but document `target_app_id`, a column main does not have, so lifting that prose would have introduced a wrong schema. ONE THING WAS WORTH KEEPING and it is LIFTED: `app_code_migration_report` + its store accessor — the K7 §B2 readback. Tier 3 was admitted ON THE CONDITION that the end state is DECLARED, and shipped built the v_dual_coded_migrations view plus the UI that writes declared_end_state but NO reader: the view's only consumer in the whole tree was a unit test. Landed as GET /mappings/app-code/migrations, adapted to main's view name (the branch queried v_app_code_migrations) and post-S4 shape, +2 tests, citing bfb2f0b in the docstring. BRANCH DELETED local + remote; tip was bfb2f0b "wip(k9): preserve the laptop's unpushed K9 implementation", reconstructible from this note and git history alone. Suite 1529 green.
    depends_on: []
    inputs: [CLAUDE.md, docs/restructure/backlog.yaml]
    acceptance: >
      A written per-file comparison of the two K9 implementations that says what the laptop
      branch does differently and whether it is worth keeping; anything worth keeping lands
      on main as its own commit citing the branch; then `wip/k9-laptop` is deleted both
      locally and on origin, and the deletion is recorded in this item's close note WITH the
      branch tip SHA, so the comparison stays reconstructible from history alone. "Nothing
      worth keeping" is a valid result — state it explicitly with the reason. Do not delete
      silently.
    notes: >
      Groomed 2026-08-04 (weekly groom). VERIFIED at the groom: the branch exists both
      locally and as remotes/origin/wip/k9-laptop at bfb2f0b, "wip(k9): preserve the
      laptop's unpushed K9 implementation"; main carries the desktop's shipped
      implementation at 17d9e08. The divergence is a REWRITE, not a rebase — mappings.py
      334 lines apart, mapping_store.py 348, net -726/+419 across 8 files — so the
      disposition is a comparison, not a merge.
      Sequencing note, NOT a dependency: J31 uses this branch as its worked example, so
      read J31 before deleting. Neither item blocks the other from starting.
      This is the second occurrence of the C19 collision class (the first: two sessions
      built C19 about ten minutes apart, 2026-07-28), and the two failed differently — C19
      was two sessions claiming nothing, K9 was a claim that WAS pushed, went dark, and got
      released as dead while the work existed locally. J31 owns that gap.

  - id: J31
    epic: release-infrastructure
    title: "The pull rule makes the CLAIM visible but not the WORK: from the other machine, a dead session and a working one look identical"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs: [CLAUDE.md, docs/restructure/backlog.yaml]
    acceptance: >
      CLAUDE.md §0's pull rule gains a work-visibility clause carrying a MECHANISM rather
      than an intention, answering three things: (1) WHEN an in-flight session pushes work
      — a named trigger (first substantive edit, or the end of any session that did not
      close its item), not "when convenient"; (2) WHERE it goes — the `wip/<id>-<machine>`
      branch shape the K9 recovery already used, so the convention is discovered rather
      than invented; (3) WHAT a session must check before RELEASING someone else's claim
      back to todo — at minimum `git branch -r --list "wip/<id>-*"` — so a claim with work
      behind it cannot be declared dead. The clause cites both occurrences as its evidence
      and states plainly what it does NOT fix: a session that dies before its first push
      stays invisible, and no convention changes that.
    notes: >
      Groomed 2026-08-04 (weekly groom) from the wip/k9-laptop inbox line, promoted
      SEPARATELY from the branch cleanup (J30) because the branch is a one-off and this is
      the rule. It EXTENDS a discipline the repo already ruled — J19 codified the
      pushed-claim rule after the 2026-07-28 C19 collision — rather than proposing a new
      direction, which is why it grooms as a chore instead of routing to the HITL gate. The
      exact wording stays the user's to confirm at build; what this groom fixes is that the
      gap now has an owner and an id.
      The cost is measured, not hypothetical: the laptop built a full K9 implementation and
      never pushed it, the desktop released the claim as dead (claim commit 3608ae5 was the
      tip and nothing followed) and rebuilt it, and main now carries 17d9e08 while bfb2f0b
      sits on a branch nobody planned. One push of a wip branch would have prevented the
      whole rebuild.

  - id: J32
    epic: release-infrastructure
    title: "Write down the registration / routing / attribution rule: a SEAL id in a field is not an attribution claim unless that field's job is to attribute"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs: [docs/RELATIONSHIP_GUIDE.md, knowledge/standards/, config/gate-prompts/fid-identity-and-scope.yaml, external/orchestration/autosys/README.md]
    acceptance: >
      A short standing rule lands in docs/RELATIONSHIP_GUIDE.md (or a knowledge/standards
      note it links, if that reads better) stating that ownership, routing and attribution
      are three different facts that all serialize as a SEAL id, and that the graph has
      exactly ONE place where the third is authored — the confirmed app-code mapping. It
      carries the three observed instances as evidence, not as hypotheticals: (1) a FID is
      REGISTERED to the platform application while its jobs are ATTRIBUTED elsewhere
      (`fid-identity-and-scope` §G); (2) an AutoSys failure alert carries TWO SEAL ids as
      escalation ROUTING (`SEAL=<a>_<b>` in the incident payload) — ingested naively that
      manufactures a job belonging to two applications; (3) the Control-M escalation DB
      routes by SEAL for the same reason. It also states the test to apply to any NEW
      source: ask what the field's job is, not what it contains. Prose only — no vocabulary
      entry, no map entry, no loader change.
    notes: >
      Groomed 2026-08-05 (desktop) from Idea-62. Promoted rather than parked because all
      three instances are already observed and written up, so there is nothing left to
      learn before writing it — and because the cost of NOT having it is concrete: each of
      the three was re-derived independently, and the AutoSys one is a load that has not
      happened yet, which is the cheapest possible moment to be holding the rule.
      Type is `chore` and NOT a gate: the rule describes how to READ a source field, which
      is an ingestion-discipline question, not a decision about what an edge MEANS. Nothing
      here changes a relationship type or a mapping status. If applying it later argues for
      a vocabulary change, that is the gate's business, not this item's.
      RELATED, deliberately not folded in: the same shape appears a fourth time as the
      "majority-not-always" caveat on the five cross-system joins in
      knowledge/standards/technology/folder-naming-convention.md. That one is about a join's
      RELIABILITY; this is about a field's MEANING. Cross-reference them, do not merge them.
    progress: >
      2026-08-05 — DONE (desktop, claimed same day). Landed in docs/RELATIONSHIP_GUIDE.md
      as § "The field-meaning rule — ownership, routing, and attribution (J32)", a sibling
      of the join-restatement methodology section (same class: a reading rule, not a
      relationship type). Carries the rule, the ask-what-the-field's-job-is test, the
      three observed instances with their in-repo citations (§G registration-vs-work; the
      AutoSys SEAL=<a>_<b> routing payload; cm_escalation_db ECOMPONENT routing), and the
      deliberate cross-reference to the folder-naming norms-not-invariants rule
      (meaning first, then reliability — apply both). One rider: the AutoSys README's
      "standing rule" mention now points at the guide section as the canonical home
      instead of only at §G. Prose only, exactly as scoped.

  # ── Epic X — ddlineage retirement (main, graph-infra) ────────────────────────
  # Groomed 2026-08-04 on the user's direction ("create a backlog epic to remove
  # it"), immediately after a confirmation pass proved the standing fact: ddlineage
  # is provisioned but WRITTEN BY NOTHING. The lineage writer pins DATABASE =
  # "drydocs" and raises TrustBoundaryError on anything else (ADR 0002 D2); the
  # four specs that once read ddlineage were repointed at G30; the read-set
  # allow-list in query_specs.py deliberately excludes it; test_database_names.py
  # proves every read target has a writer. The database's only remaining role is
  # to exist, empty, on every provisioned host. ADR 0002's residency clarification
  # says this topology changes "as an amendment, through the SME gate — not by
  # drift" — X1 is that amendment, recording the user's 2026-08-04 in-chat ruling.
  # Removal is cheap BY THE ADR'S OWN ACCOUNTING: "ddlineage is empty, so choosing
  # it later costs a design, not a data migration" — and recreating it later costs
  # one CREATE DATABASE line, while the design question (a :ControlMJob proxy-node
  # spine) was never answered and stays owned by the named revisit trigger.
  # Sequencing: X1 (decision record) → X2 (repo sweep) → X3/X4 (live drops, one
  # per machine — the G50/G52 venue-pinned precedent; the two machines hold
  # independent graphs, so one item cannot close both).
  - id: J33
    epic: release-infrastructure
    title: "Three CLI guard tests assert plain substrings against rich-colored output and fail on this desktop"
    type: bug
    module: drydocs-load
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - tests/unit/test_supplements.py
      - tests/unit/test_bootstrap_guard.py
    acceptance: >
      The three tests named in Idea-83 (test_supplements chain-order + unknown-name,
      test_bootstrap_guard declared-count) pass on the desktop where rich interleaves
      ANSI codes into captured CLI output — fixed by stripping ANSI in the assertions
      or forcing a non-terminal console under pytest, NEVER by loosening the asserted
      messages; a sweep finds any other test asserting substrings against rich-rendered
      output and applies the same treatment.
    notes: >
      Groomed 2026-08-07 from Idea-83. Fails under both `python -m pytest` and
      `poetry run pytest`, on a stashed clean tree, NO_COLOR=1 inert — a rich/typer
      console-detection difference on this desktop. The three failures have ridden
      every suite run since 2026-08-07 as "standing Idea-83 failures"; this item
      retires that caveat.
  - id: J34
    epic: release-infrastructure
    title: "PORT-MANIFEST company-row overlay seam: company-only tracked paths live in an include the guard unions, so a port cannot clobber them"
    type: task
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - PORT-MANIFEST.yaml
      - docs/port-prompt.md
    acceptance: >
      Producer PORT-MANIFEST.yaml stays canonical; company-only rows move to a
      separate company-side include whose rows the J16 guard
      (test_no_tracked_path_falls_through_silently) unions with the producer
      manifest; a test proves a producer-manifest-verbatim apply CANNOT drop a
      company include row; the grammar decision (include file vs marked section) is
      recorded in the manifest header; port-prompt.md documents the seam for the
      company session that re-adds the 89 dropped rows.
    notes: >
      Groomed 2026-08-07 from Idea-79 (PORT-REPORT-a14a8028 clobber audit: the take
      dropped the company default_ok section — 89 company-only paths fell through
      their own guard, the report's one conscious deferral). Include-file chosen over
      marked-section as the default: it is the D2-registry overlay precedent the
      entry itself names; revisit at build only if the company side objects.
      MERGED IN 2026-08-07 (pm groom, Idea-41): the grammar this item picks must also
      be able to express a PRODUCER-LOCAL file, because config/** is canonical-producer
      and config/dev-environment.yaml is now producer-local infrastructure (sibling repo
      path, expected instrument commit, container name, host ports) that a port applies
      wholesale — and the L16 runbook's Appendix A is a RENDER of it, so a producer value
      propagates into consumer documentation, which is the exact drift Appendix A was
      restated to prevent. The file has no id-keyed grain, so the plausible units are
      SECTIONS (`neo4j:` and `depgraph:` are environment-specific; `services:` ports are
      arguably shared). WHAT THIS ITEM MAY NOT DO: choose that file's disposition —
      per-entry row, canonical-company row, or split into a portable contract plus a
      local overlay (cleanest, most work). J16's own rule stands: a disposition is a
      decision, not a default, and it is the user's. This item only ensures the grammar
      can express the answer.
      DONE 2026-08-09 (desktop, 7d7c8db on feat/port-machinery-trio, --no-ff merged 09c1be1): overlay: block declares one never-port slot per side (include-file over marked-section — the D2 loader-source-overlay precedent, recorded in the manifest header); union_overlays() in test_port_reconcile_guards.py unions overlay rows AFTER producer rows + default_ok; acceptance proof test replaces the manifest wholesale (v1->v2) and the company overlay row survives by construction; Idea-41 rider satisfied (producer slot declared, dev-environment.yaml disposition deliberately NOT chosen); port-prompt standing-divergence bullet carries the one-time 89-row company migration. Rode along: manifest rows for the roadmap pair the J16 guard caught (roadmap.html canonical-company derived, roadmap.yaml evaluate).
  - id: J35
    epic: release-infrastructure
    title: "Port ledger: roll it through a deliberate range end, make the roll a structural closing step, and require SHA-stamped producer citations"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - docs/port-prompt.md
    acceptance: >
      (a) docs/port-prompt.md's ledger covers every producer commit after a14a8028
      through a NAMED deliberate end commit (the G22 gate session, the SEAL sample
      generator, the code-graph asset rulings, the .ksh SWO binding all classified);
      (b) the port prompt's closing sequence gains a mandatory ledger-roll step whose
      required fields (range, port commit, backup tag, acceptance numbers) are
      listed, so a port cannot close unrecorded — the three-in-a-row reconstruction
      failures (f71967db, 6713c142, 5f79d145) are named as the motivating class;
      (c) the prompt requires every producer-tree citation in a company report to
      quote the SHA it was read at, so a cached-ref read announces itself.
    notes: >
      Groomed 2026-08-07 consolidating Idea-56 (structural roll), Idea-76 (the
      pending range) and the design half of Idea-57 (SHA-stamped citations; the
      company-side credential fix stays with the company). Range/backup/acceptance
      data for the two historic unrecorded ports is unrecoverable — the roll records
      that gap rather than inventing values.
      DONE 2026-08-09 (desktop, 9673bcc on feat/port-machinery-trio, merged 09c1be1): (a) ledger rolled through NAMED end 5417ef10 — steps 55-105 collapsed to one-liners (55-83 via the reported-only ports, gap recorded not repaired; 84-105 at the two producer-verified ports), the unledgered a14a8028..5417ef10 range ledgered as steps 102-105 (G22 gate session incl. the .ksh SWO binding a4328dd, SEAL sample generator 180f4ae, code-graph asset rulings 5359b00+6a11064, remainder), live ledger = 106-115 classifying all 61 pending commits + the trio, file 1512->758 lines; (b) MANDATORY CLOSING SEQUENCE in the header with the four required fields (range, port commit, backup tag, acceptance numbers), f71967db/6713c142/5f79d145 named as the motivating class; (c) guardrail 8 requires SHA-stamped producer citations.
  - id: J36
    epic: release-infrastructure
    title: "Integration tests need a manual ryuk workaround on this desktop — decide the fix and make it automatic"
    type: bug
    module: drydocs-load
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: done  # 2026-08-07 pm (desktop): tests/integration/conftest.py defaults TESTCONTAINERS_RYUK_DISABLED=true via setdefault (overridable with =false) — full why in the docstring (J18 venue: producer Windows desktops; intermittent WinNAT excluded-range / stale-reaper port failure; ryuk only buys crash-reaping, ordinary teardown unaffected) + stale-container cleanup one-liners there and in internal/repo-README.md. PROVEN: e2e ran 4 passed / 29.8s cold with no manual step, env var unset in the shell
    depends_on: []
    inputs:
      - tests/integration/test_e2e_load.py
      - tests/conftest.py
      - internal/repo-README.md
    acceptance: >
      A testcontainers integration test (the J9 family or the G23 e2e) starts on this
      desktop with NO manual step: either TESTCONTAINERS_RYUK_DISABLED is set where the
      suite can see it (conftest, or a documented environment note that names the machine
      class per J18), or the underlying port-8080 conflict with the ryuk reaper is fixed
      and the env var is proved unnecessary. Whichever is chosen, the reason is written
      down next to the setting — a bare env var with no note reads as cargo cult — and the
      stale-ryuk-container cleanup is documented for the next occurrence.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-84, captured at the G23 e2e close on this
      desktop: startup failed with "Port mapping ... port 8080 is not available" for the
      ryuk reaper, reproduced identically on an existing J9 test, so pre-existing
      environment trouble rather than the new test; removing stale ryuk containers and
      setting the env var ran the new e2e green in 22s. Venue-specific by nature, so J18
      applies — whatever lands names the machine it was observed on.
  - id: J37
    epic: release-infrastructure
    title: "Guards that shell out and parse human-facing output: sweep them, and write the never-parse-a-render rule down"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - tests/unit/test_runbook_currency.py
      - CLAUDE.md
      - docs/style/us-business-english.md
    acceptance: >
      Every guard that shells out to a DryDocs command and parses its RENDERED output is
      enumerated, and each one is either converted to read the importable object (the N6
      fix: app.registered_commands instead of parsing `drydocs --help`) or carries a
      written reason why the render itself is the thing under test; the rule — never
      parse a render when the object is importable — lands in the repo's working
      agreements where the next test author will read it.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-54. The motivating guard failed twice at once on
      the laptop, having never actually executed there: text=True decodes cp1252 and
      cannot decode Typer's rich box character, so stdout came back None, AND the table
      rows start with U+2502 rather than '|', so the pattern would have matched nothing
      anyway — which makes every documented verb look unregistered. A guard that passes
      by accident on one machine and cannot run on the other is the class. J33 is its
      sibling on the assertion side (plain substrings vs ANSI-colored output).
  - id: J38
    epic: release-infrastructure
    title: "The standing company-side relay list lives in the port prompt, not only in the idea inbox"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - docs/port-prompt.md
      - config/taxonomy/software-registry.yaml
      - drydocs_core/run_log.py
    acceptance: >
      docs/port-prompt.md carries a named relay section a company session reads at every
      port, and the three standing relays are each in it with a current status or struck
      with a dated reason: (a) the AIS acronym expansion, whose producer home is
      software-registry.yaml#acronyms and whose company home is a different file — a
      transplant of the VALUE, never a same-file overwrite; (b) the run-log adoption asks
      (attach the tee in the XML EXTRACTOR stage, where the description_tokens WARN flood
      happens pre-loader, and consider a WARNING-summary-only console once the stream
      lands in a file); (c) the 2026-07-21 port-report heads-ups, each RE-VERIFIED before
      it is written down — the remote-URL/fetch item in particular, which later reports
      say is resolved. Nothing is relayed from memory; a stale relay is worse than none.
    notes: >
      Groomed 2026-08-07 (pm), consolidating Idea-18, Idea-24 and Idea-26 — three "relay
      next company session" notes that have sat in the inbox since 2026-07-21/22 with no
      surface that survives to a port. One item because they share the defect, not the
      content: the inbox is not a channel the other repo reads. Sibling of J35, which
      makes the ledger roll a structural closing step; this makes the relay list one.
      DONE 2026-08-09 (desktop, 9473dd3 on feat/port-machinery-trio, merged 09c1be1): STANDING RELAYS section above the tracker — R1 AIS value-transplant (producer home re-verified: software-registry.yaml#acronyms) + the 06-29 company gate-log backfill offer; R2 run-log adoption asks (extractor-stage tee, WARNING-summary console); R3 the 07-21 heads-ups re-verified (schema-graph sequencing trap -> reconcile-port skill cite; internal-backlog deletion still unconfirmed; remote-URL/fetch STRUCK with dated reason, resolved 2026-08-06); R4 the one-time J34 overlay migration. Section rules: re-verify at the carrying roll, strike with dates, never relay from memory.
  - id: J39
    epic: release-infrastructure
    title: "Disposition the six un-back-flowed company advances: reproduce mechanism-only, or ledger each with a reason"
    type: task
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - .claude/skills/reconcile-port/SKILL.md
      - docs/port-prompt.md
    acceptance: >
      Each of the six advances is DISPOSITIONED — reproduced producer-side
      mechanism-only (no company values) or recorded in the reconcile-port divergence
      ledger with the reason and the trigger that would change it: the snow-support
      schema supplement pair (hpsm_queue_key / sn_group_name) plus the snow-snowflake-itsm
      source stub; the drydocs_remediation DPL watch-drift rule and its tests; the
      graph_verify Assertion refactor; the docgen deviations against the finalized
      company TDD; the CONFLUENCE_BASE_URL config seam (mechanism = base-URL as config,
      the value stays company-side); and the controlm_folders.sql `J` table alias. None
      is left undecided, and anything that touches edge meaning is registered `planned`
      and routed to its gate rather than adopted here.
    notes: >
      Groomed 2026-08-07 (pm) from Idea-19 (bd7952f follow-up 3). Module docs and epic J
      because the deliverable is a disposition record per advance, not one component's
      code — the reproductions that follow are separate items in their own modules.
      Motivation, in the entry's own words: until these land, every port repeats the
      squash-reconcile instead of a clean linear apply. The 2026-07-20 bundle port went
      bidirectional (+288 producer / +148 company) precisely because they never came back.
  - id: X1
    epic: ddlineage-retirement
    title: "ADR 0002 amendment: retire ddlineage from the deployed topology — provisioned-for-later ends, recreate-if-ever-needed begins"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done   # closed 2026-08-04 (desktop, same session as the claim). ADR 0002 carries the dated amendment section (after G51's): the deployed set becomes drydocs/ddcontext/ddall/ddschema; the residency clarification's "stays provisioned and aliased" disposition named as the superseded line, everything above it left as written; the standing evidence restated from the record (writer pinned w/ TrustBoundaryError, G30 repointed readers, read-set exclusion, read-targets-have-writers guard) plus the honest observation that empty-but-provisioned is the exact gap the G28/G30 drift classes grew in; the reversal path states the DIRECTION change (reopening now recreates — one DDL line + the ddall alias — rather than finds it waiting) and the proxy-spine design question transfers INTACT to the named revisit trigger, which stands. gate-log RECORD entry on the G51 idiom (direction, not a gate — no ontology change, no graph write; enactment owned by X2-X4). ADR 0006 §1's enumeration got the as-of-date parenthetical (ddschema added G51, ddlineage retired X1 — the convention point stands) rather than a rewrite. gates.json re-rendered (the new log entry's row rode the default board render; its absence failed the J17 drift guards first, as designed). Suite 1542 green. X2 now next_ready.
    depends_on: []
    inputs:
      - docs/decisions/0002-component-database-topology.md
      - docs/decisions/0006-docmeta-component-and-doc-graph.md
      - config/gate-log.md
    acceptance: >
      ADR 0002 gains a dated amendment section recording the retirement: the decision
      (ddlineage removed from the provisioned topology, user ruling 2026-08-04 in-chat),
      the standing evidence (nothing writes it — writer pinned to drydocs with
      TrustBoundaryError, G30 repointed the four readers, the read-set excludes it, the
      read-targets-have-writers guard holds), and the reversal path (recreation = one
      CREATE DATABASE line + the ddall alias; the proxy-node-spine design question the
      original "provisioned-for-later" ruling deferred REMAINS deferred and transfers to
      the named revisit trigger — retirement answers where the database went, not that
      question). config/gate-log.md carries a record entry on the G51 idiom (a record,
      not a gate — no ontology change, no graph write). ADR 0006 §1's topology cross-ref
      updated if it enumerates ddlineage as deployed. History (gate-log entries, closed
      items, port archives, CHANGELOG) stays as written — the amendment supersedes, it
      does not rewrite.
    notes: >
      Groomed 2026-08-04 (desktop, user directive in-chat — the directive IS the SME
      ruling this amendment records; the ADR's own revisit clause routes topology change
      through an amendment, and G51's dated-section + gate-log-record shape is the
      precedent). Do NOT edit G53's notes: its enumeration ("drydocs + ddlineage +
      ddcontext + ddschema") is a snapshot in its own prose; the G53 plan, when written,
      reads the topology as it stands at that build.
  - id: X2
    epic: ddlineage-retirement
    title: "Repo sweep: ddlineage out of provisioning, code, config, tests, and live docs — history stays history"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (desktop, same session as the claim). The re-run census found 31 files; every live surface swept, history left as written (J23/J24 idiom — gate-log, gate prompts, CHANGELOG, port archive, IDEAS, exec-overview REV-HISTORY block, G52 volume-contents notes). Provisioning: DDL create + ddall alias removed (a NOTE comment marks the retirement + recreation path), provision.ps1 runs 02 twice with the retirement noted, smoke reads two constituents, README re-tabled. Code: cli DOC_SWEEP_DATABASES 4->3, deepdoc docstring topology, dev-environment databases map (lineage row removed; the test's key-set speed bump updated with it), query_specs read-set comment re-grounded on X1 + four spec comments tense-fixed, web registry.ts comment. Tests: topology anchor 5->4; ddlineage JOINED SUPERSEDED_NAMES with the escape regex widened to "retire" (no successor name, so "superseded" would be the wrong admission) — the guard then CAUGHT this item's own cli.py comment wrapping the admission onto the next line, proof it bites; docs-verify fixture tuple shrunk (no test keyed on it); startup-refresh runbook Rev 9 (topology enumerations + the provisioning-never-drops asymmetry written into step 4; pin bumped 8->9, it caught the edit); project-review table updated to the current four (ddschema row added, retirement parenthetical), review outline guidance updated; site-plan backs-onto + TS union fixed. Port ledger step 83 with the company caution (their live ddlineage survives a green provision run — manual drop by their hand, alias-first + zero-node probe, non-empty = defect report; SUPERSEDED_NAMES will red their local stragglers) + acceptance-gate head reference rolled to 1539/5. Both design-doc renders + board regenerated. Suite 1539/5, ruff + format clean (two pre-existing hygiene nits in touched files fixed in passing: F541 + format drift in test_dev_environment.py, format drift in filter_ignored.py). X3/X4 now next_ready.
    depends_on: [X1]
    inputs:
      - drydocs_core/schema/provisioning/01_databases.cypher
      - drydocs_core/schema/provisioning/provision.ps1
      - drydocs_core/schema/provisioning/README.md
      - drydocs_core/schema/provisioning/smoke_drydocs_all.cypher
      - drydocs/cli.py
      - drydocs_api/query_specs.py
      - drydocs_deepdoc/__init__.py
      - config/dev-environment.yaml
      - tests/unit/test_database_names.py
      - docs/port-prompt.md
    acceptance: >
      Provisioning no longer creates ddlineage nor aliases it into ddall (01_databases.cypher,
      provision.ps1, provisioning README, smoke_drydocs_all.cypher all agree); cli.py's
      DOC_SWEEP_DATABASES drops it; the drydocs_deepdoc docstring topology list and
      config/dev-environment.yaml stop naming it; query_specs.py's read-set comment block
      re-grounds on the X1 amendment (the exclusion rationale changes from "provisioned but
      written by nothing" to "retired at X1") without weakening the allow-list; the
      test_database_names.py topology anchor steps 5 -> 4 names and the whole guard family
      (query-specs, docs-verify, tier2) goes green against the shrunk topology; renders that
      embed the topology (enforcement matrix, board, design-doc runbooks with rev pins —
      bump any pinned rev the edit trips) are regenerated in the same commit; the port-prompt
      ledger gains a step carrying the company-side caution: their ddlineage exists live and
      their G30-repointed specs read drydocs, so the port hands them a provisioning diff plus
      an instruction to run their own X3-equivalent drop — producer sign-off is not company
      sign-off. Sweep scope is LIVE SURFACES ONLY (the J23/J24 idiom): gate-log, gate
      prompts, CHANGELOG, port archives, IDEAS audit trail, and closed-item notes keep the
      name as history. Full suite + ruff green.
    notes: >
      Groomed 2026-08-04. The reference census at the groom found ~30 files naming
      ddlineage; the inputs list is the live-surface subset — the sweep re-runs the census
      (git grep -il ddlineage) and classifies each hit live-vs-history rather than trusting
      this list, per the J24 "the item's own framing undersold it" lesson.
  - id: X3
    epic: ddlineage-retirement
    title: "Live drop, desktop: remove ddlineage from the ddall composite and drop it on the desktop container, venue-tagged"
    type: chore
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04 (desktop, same session as the claim). VENUE (J18): desktop, container `neo4jtest` (image neo4j:5.26-enterprise-ubi10 — the diverged image; the container was UP, so the drop path applied, not the recreate-by-verification path). Sequence as the acceptance ordered it: emptiness probe FIRST — `MATCH (n) RETURN count(n)` on ddlineage returned 0, confirming the ADR record before anything destructive; `DROP ALIAS ddall.ddlineage IF EXISTS FOR DATABASE`; `DROP DATABASE ddlineage IF EXISTS`; SHOW DATABASES then listed exactly ddall/ddcontext/ddschema/drydocs (+ the EE home db `neo4j` + system), all online; the post-X2 two-constituent smoke ran green over ddall (drydocs 2574 nodes, ddcontext 0 — success is that the federated read runs at all). FRESH OBSERVATION for the parked desktop-divergence IDEAS line: `ddschema` EXISTS on this container now (someone provisioned it since that note was written), so the divergence is down to the image pin (5.26 vs 2026.05.0) — the note's "ddschema missing" half is stale. Ops note: on Git Bash, `docker exec ... -f /tmp/x` gets MSYS path-mangled into the host Temp dir — MSYS_NO_PATHCONV=1 required (the J29 docker cp + -f protocol otherwise held). X4 (laptop twin) remains next_ready.
    depends_on: [X2]
    inputs: [drydocs_core/schema/provisioning/README.md, config/dev-environment.yaml]
    acceptance: >
      On the desktop container (venue named in the close note per J18): an emptiness probe
      against ddlineage returns zero nodes BEFORE anything destructive (the ADR says nothing
      writes it — verify, don't trust); the ddall composite's ddlineage alias is dropped
      first, then DROP DATABASE ddlineage; SHOW DATABASES afterwards lists the four-name
      topology plus system, ddall stays online, and smoke_drydocs_all.cypher passes against
      the post-X2 script. A non-empty probe result STOPS the item — that is a defect report
      (something wrote a database nothing should write), not a cleanup.
    notes: >
      Groomed 2026-08-04. Venue-pinned per the G50/G52 precedent — X4 is the laptop twin.
      DESKTOP CAVEAT: the desktop neo4jtest venue is the diverged one (5.26.27 vs the
      pinned 2026.05.0, ddschema missing post-G51 — the parked IDEAS decision). If the
      user resolves that divergence by recreating the container on the pinned image, the
      post-X2 provisioning never creates ddlineage and this item closes by verification
      (SHOW DATABASES + smoke) instead of by drop — either path meets the acceptance.
  - id: X4
    epic: ddlineage-retirement
    title: "Live drop, laptop: remove ddlineage from the ddall composite and drop it on the laptop container, venue-tagged"
    type: chore
    module: graph-infra
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done   # closed 2026-08-04. VENUE (J18): laptop, container `neo4jtest`, Neo4j 2026.05.0 EE, database `ddlineage` dropped from that DBMS. SPLIT EXECUTION — the agent ran the gate and the verification; the USER ran the two destructive statements, because the permission classifier blocked them for the agent on BOTH routes tried (PowerShell `docker exec … cypher-shell` and the neo4j MCP write-cypher tool). Recorded because it is the reproducible shape of this item, not a one-off: a live DROP is user-run here, so an agent claiming X-series drop work should plan to hand off rather than expect to finish alone. EMPTINESS PROBE (before anything destructive, as the acceptance orders): ddlineage MATCH (n) count = 0, MATCH ()-[r]->() count = 0 — the stop-condition did NOT fire, so this was a clean drop and not a defect report. db.labels() returned [DataAsset, ControlMJob], which is the label-TOKEN store the 02_proxy_constraints pass registers, not data — a token is not a node, and the probe the acceptance specifies is the node/rel count. ORDERING IS PLATFORM-ENFORCED, learned the hard way: DROP DATABASE attempted before the alias was REFUSED (42N82, 'cannot drop database with aliases'), so the acceptance's alias-first step is Neo4j's rule, not merely convention; that attempt changed nothing. Syntax trap for the next operator: it is `DROP ALIAS x IF EXISTS FOR DATABASE`, NOT `DROP ALIAS x FOR DATABASE IF EXISTS` (the wrong order parses as a different, failing statement). VERIFICATION after the user's drop: SHOW DATABASES lists exactly ddall (composite) / ddcontext / ddschema / drydocs + the EE home db `neo4j` + system, all online — the four-name topology; SHOW ALIASES FOR DATABASE lists exactly ddall.ddcontext and ddall.drydocs (no ddlineage remnant); and the post-X2 two-constituent smoke ran green over ddall, returning drydocs 2218 / ddcontext 46 — two columns, confirming X2's amended smoke is what is deployed. Epic X now closes: X1-X4 all done, producer side fully retired; the company side still owns its own X3-equivalent drop per X2's port-ledger step.
    depends_on: [X2]
    inputs: [drydocs_core/schema/provisioning/README.md, config/dev-environment.yaml]
    acceptance: >
      Same test as X3, run on the laptop container (venue named in the close note per
      J18): zero-node emptiness probe first, alias out of ddall, DROP DATABASE ddlineage,
      SHOW DATABASES shows the four-name topology with ddall online, smoke passes. A
      non-empty probe STOPS the item as a defect report.
    notes: >
      Groomed 2026-08-04. The laptop is the venue that ran the G52 cleanup and the
      2026-08-04 post-expiry write verification (six user databases online at that probe
      — five user DBs + ddall — so this drop shrinks that census by one; the eval-expiry
      matter is G53's context and explicitly NOT this item's concern).

  # ── Epic Y — backlog sharding (main / drydocs-plan) — phase 8 ────────────────
  # Groomed 2026-08-04 on the user's direction, un-parking the 2026-08-03 IDEAS
  # EPIC entry (kept-parked at the 2026-08-04 weekly groom, un-parked in-chat the
  # same day). The trigger class is measured, not hypothetical: agents regex-splice
  # a shared 12k-line mutable blob; the company port shipped a duplicate-`summary:`
  # key that pyyaml's last-wins parsing hid from test_backlog (phase-1 guard closed
  # that class, `c5b689e`, port step 55); and on 2026-08-04 alone the two machines
  # hit THREE same-afternoon rebase conflicts (X1-claim/V8, X2-close/V2,
  # X3-close/V3) — every one of them in the derived summary/next_ready block that
  # sharding removes from storage entirely. Phases here: Y1 design ruling ->
  # Y2 the shard build -> Y3 graph vocabulary via the gate -> Y4 the query
  # surface. Cross-repo law from the idea entry: BOTH repos must move together at
  # Y2 — the port carries the shard as a structure-breaking step, like S5's.
  - id: Y1
    epic: backlog-sharding
    title: "Sharding design ruled: one-item-per-file layout, derived roll-ups, claim mechanics, migration + port sequencing — an ADR, decided with the user"
    type: task
    module: docs
    phase: 8
    agent: main
    model: fable
    priority: p2
    status: in_progress   # claimed 2026-08-04 (desktop, pushed before work) — the ruling session runs in-chat this session
    depends_on: []
    inputs:
      - docs/restructure/IDEAS.md
      - docs/restructure/backlog.yaml
      - tests/unit/test_backlog.py
      - scripts/render_board.py
      - .claude/skills/groom-backlog/SKILL.md
      - PORT-MANIFEST.yaml
    acceptance: >
      An accepted ADR (next free number) that rules, with the user, each mechanism the
      IDEAS entry names as open: (1) FILE LAYOUT — backlog/items/<id>.yaml one item per
      file, plus where the non-item material lives (plan.phases, the modules registry,
      epic comment headers — headers are prose today and MUST survive as something a
      renderer can show); (2) DERIVED ROLL-UPS — summary counts and next_ready become
      render_board.py OUTPUT, never stored where agents append (the three 2026-08-04
      same-afternoon rebase conflicts are the exhibit: every one was in the stored
      roll-up block); (3) CLAIM MECHANICS — a claim becomes a one-file status flip,
      and the ADR states plainly what collision window remains (two sessions claiming
      the SAME item still race; disjoint items stop colliding entirely); (4) MIGRATION —
      one-shot splitter with an entry-level deep-equality proof against the monolith
      before deletion (the S5 proof pattern), and the monolith's tombstone guard;
      (5) PORT SEQUENCING — how both repos move together: the PORT-MANIFEST row shape,
      whether the company applies the splitter or receives the sharded tree, and what
      happens to a company-side port that is mid-flight when Y2 lands. Plan only —
      no file moves from this item.
    notes: >
      Groomed 2026-08-04 (desktop, user directive un-parking the IDEAS entry; next free
      epic letter Y — X taken by ddlineage-retirement the same day). The park condition
      is CARRIED FORWARD as a Y2 precondition, not a depends_on: the idea's ruling said
      the shard is better timed after the in-flight 40c35724..6713c14 port's
      PORT-REPORT is reviewed, because phase 2 rewrites exactly the files that port
      carries. Y1 is paper work and starts now; Y2 holds until that review is on the
      record. Model fable: this is a plan change re-shaping the claim channel both
      machines depend on.
  - id: Y2
    epic: backlog-sharding
    title: "The shard built: items split one-per-file, roll-ups derived by the board render, validators and groom skill re-pointed, monolith tombstoned"
    type: task
    module: drydocs-plan
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Y1]
    inputs:
      - docs/restructure/backlog.yaml
      - tests/unit/test_backlog.py
      - .claude/skills/groom-backlog/validate.py
      - scripts/render_board.py
      - tests/unit/test_port_manifest.py
      - PORT-MANIFEST.yaml
      - docs/port-prompt.md
    acceptance: >
      The Y1 ADR enacted in one migration commit: backlog/items/<id>.yaml per item with
      the non-item material where Y1 ruled it; the splitter's entry-level deep-equality
      proof recorded (every item field-identical to its monolith form) BEFORE
      backlog.yaml is deleted, and a tombstone guard refuses its resurrection (the S5
      monoliths-are-gone pattern); render_board.py DERIVES summary counts and
      next_ready from the item files — nothing stores them — and the board render is
      byte-deterministic as before; test_backlog.py and the groom skill's validate.py
      validate the sharded form (same checks, new reader) with the roll-up drift checks
      RETIRED as no-longer-possible rather than re-pointed; the groom-backlog skill,
      CLAUDE.md §0 pull rule, and backlog header prose describe the one-file claim
      flip; PORT-MANIFEST rows cover backlog/items/** with an entry_rule carrying the
      split shape (the S5 precedent) and the port-prompt gains a ledger step marked
      STRUCTURE-BREAKING with the company-side apply instruction Y1 ruled. Full suite
      green. PRECONDITION carried from the groom (prose, not depends_on): do not land
      until the in-flight port's PORT-REPORT review is on the record — this item
      rewrites exactly the files that port carries.
    notes: >
      Groomed 2026-08-04. The claim-collision shrink is the payoff to state at close:
      after this item, two machines claiming DISJOINT items touch disjoint files and
      cannot conflict; only a same-item race remains, and J31's wip-branch check
      already covers the visible half of that.
  - id: Y3
    epic: backlog-sharding
    title: "Backlog graph vocabulary via the gate: :BacklogItem + DEPENDS_ON registered planned, gate prompt drafted — projection semantics, git stays the claim channel"
    type: task
    module: ontology
    phase: 8
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: todo
    depends_on: [Y2]
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/
      - config/taxonomy-ontology-map/
      - config/gate-prompts/
      - docs/RELATIONSHIP_GUIDE.md
    acceptance: >
      Vocabulary entries for :BacklogItem and DEPENDS_ON (and the mapping rows)
      registered status: planned in the fragment registries, with the projection rule
      written into the entry notes verbatim: the graph is a DERIVED read surface only —
      git remains the sole source of truth and claim channel, a graph write is even
      less cross-machine-visible than an unpushed commit, and no loader may flip a
      status. A gate prompt in config/gate-prompts/ covers the label, the edge, the
      PROV classing, and the projection rule; NOTHING flips confirmed except the SME
      gate session (gate-log entry). Schema/vocabulary tests green.
    notes: >
      Groomed 2026-08-04. Routed through the gate because a new node label and
      relationship type are ontology decisions (CLAUDE.md §1 rule), even for a
      projection. p3: Y2 delivers the operational payoff; this phase is query
      convenience and can wait for a natural gate session.
  - id: Y4
    epic: backlog-sharding
    title: "The query surface: backlog loader writes the projection, next_ready becomes a one-hop Cypher, agents query instead of reading 12k lines"
    type: task
    module: drydocs-plan
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: [Y3]
    inputs:
      - backlog/items/
      - drydocs_api/query_specs.py
      - .claude/skills/groom-backlog/SKILL.md
    acceptance: >
      After the Y3 gate signs: a loader (registered in the load map, L17-guarded)
      projects the sharded items into :BacklogItem + DEPENDS_ON on the ruled database;
      a QuerySpec answers next_ready as a one-hop query whose result set is proven
      equal to render_board.py's derived list on the same tree (the derivation and the
      projection must never disagree); the groom skill's optional graph cross-check
      section re-points at the projection; the loader is idempotent and a full
      wipe-rebuild reproduces it. The projection stays read-only toward process: no
      code path anywhere flips an item status from graph state.
    notes: >
      Groomed 2026-08-04. Explicitly the trivial-after-Y2 phase from the idea entry;
      kept as its own item so Y2 can close without a Neo4j container running.

  # ── Epic Z — server location & geography (infrastructure export → map surfaces) ──
  # User directive 2026-08-08 (desktop chat), groomed direct-to-backlog: ingest the
  # infrastructure site's per-business-application server export, join server names
  # to Control-M job hosts, resolve load-balancer names to real servers via
  # nslookup, and surface locations on reusable console map modules.
  # STANDING CAUTION on every item: the export's data-center/location field is NOT
  # the Control-M field of the same name — the Control-M "DC" is a SCHEDULING
  # concept (its name encodes the default run time, e.g. T032-E0700-DMA), while
  # this source carries PHYSICAL geography (rack, building, city/state/country).
  # Never join or crosswalk the two by field name.
  - id: Z1
    epic: server-location
    title: "Register the infrastructure server export: per-application download, prod-filtered with prod + DR, server/OS/rack/data-center/geography fields"
    type: task
    module: config
    phase: 6
    agent: pipeline-config
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - config/source-registry.yaml
      - config/classification.yaml
      - config/taxonomy
      - PUBLISH-BOUNDARY.md
    acceptance: >
      A source-registry row exists for the infrastructure site's server export with
      its locator, download procedure (pulled PER BUSINESS APPLICATION, list filtered
      on prod, rows carry BOTH prod and DR servers), and classification set at
      registration (expected internal — server names, racks and building locations are
      operational metadata; the real export lands under internal/, never in a public
      path). The dataset contract enumerates the fields: server name, OS product +
      version (e.g. RHEL x.x — version context ties to the software registry and the
      C25 ruling), rack, data-center/building, city, state, country, prod/DR
      designation, owning business application. The registry row carries the standing
      caution verbatim: the data-center field is not the Control-M field of the same
      name. The hierarchy (application -> server -> rack -> data center -> city ->
      state -> country) is captured under config/taxonomy/ as PURE CLASSIFICATION —
      no meaning edges. A sanitized sample fixture is committed for tests.
    notes: >
      User directive 2026-08-08. Taxonomy first, ontology second (CLAUDE.md section 1):
      this item only registers and classifies; every edge meaning waits on Z2's gate.
      Server name is the join key to Control-M nodes, but the JOIN RULE is a Z2
      decision, not an import-time invention.
  - id: Z2
    epic: server-location
    title: "Server/location ontology gate: Server, DataCenter, geography grain, prod/DR, the Control-M node join rule, and the not-the-same-field ruling"
    type: task
    module: ontology
    phase: 6
    agent: ontology-mapper
    model: opus
    priority: p2
    status: todo
    depends_on: [Z1]
    inputs:
      - drydocs_core/ontology/relationship_vocabulary
      - docs/RELATIONSHIP_GUIDE.md
      - config/gate-prompts
      - config/taxonomy
    acceptance: >
      A gate prompt in config/gate-prompts/ plus vocabulary entries registered
      status: planned covering: the Server node (and whether OS/version hangs off the
      software registry per the C25 software-version-context ruling); DataCenter and
      the geography grain (nodes vs properties for rack / building / city / state /
      country); prod/DR designation (property vs node); the edge names
      (server-hosts-job, server-located-in, application-runs-on or their ruled
      equivalents); the server-name <-> Control-M node identity join rule with match
      tiers and evidence, following the K2 match-policy precedent; and an EXPLICIT
      ruling that the export's data-center field and the Control-M same-named field
      are distinct concepts that never crosswalk by name. Two findings from the
      2026-08-08 business-layer location experiment ride this gate as required
      confirmations (merged at the 2026-08-09 groom, from Idea-90): (i) real-world
      location data arrives at MIXED GRAIN — street address, city and country all
      appear as "the location" in one source — so the question is not only which
      levels exist but how a located node DECLARES the grain it carries; and (ii) the
      epistemic line between an enumerable site and an AGGREGATE PRESENCE CLAIM: a
      source stating "177 locations" is a claim ABOUT sites, and exploding it into
      177 nodes manufactures data, so the gate rules how such a claim is held without
      becoming fake sites. Nothing loads before
      sign-off; everything ambiguous routes to the SME session.
    notes: >
      User directive 2026-08-08. The world/country map module (Z5) keys on "a node
      label that has a location" — the gate should say WHICH labels carry location
      and in what shape, so the map component reads one contract instead of
      per-label special cases.
      MERGED 2026-08-09 (Idea-90, the business-layer location experiment written up
      in internal/context-graph-analysis/business-layer-location-experiment.md): the
      hand-applied W3C ORG + location pass over public annual-report sources produced
      a coherent business layer with no technical layer at all, which is worth knowing
      here for two reasons — it proves the Z5 map contract is satisfiable from the
      business layer alone (a located-nodes world map needs no servers), and it
      supplies the two location findings now written into the acceptance. The
      ORG-structure half of that experiment (org units = the LOB layer verbatim, plus
      an effective-dated org:ChangeEvent for a segment merge) is deliberately NOT here
      — it is a different subject and went to C28.
  - id: Z3
    epic: server-location
    title: "Server-inventory loader + Control-M node join: ingest the export, tie servers to job hosts, distinct-hosts-per-application query"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Z1, Z2]
    inputs:
      - drydocs/loaders
      - config/source-registry.yaml
      - tests/fixtures
    acceptance: >
      A loader ingests the registered export per Z2's signed shapes, idempotently,
      with the sanitized fixture e2e green. Server names join to Control-M
      nodes/job hosts under the gate's match tiers, with match evidence recorded
      rather than silent. A query (QuerySpec or CLI verb per the read-surface
      conventions) answers "the distinct job hosts for a given application" — the
      selection that feeds the Z4 resolver — and its result carries each host's
      resolved location when the join found one and an explicit unmatched marker
      when it did not, so coverage gaps are visible instead of dropped.
    notes: >
      User directive 2026-08-08. Prod filter applied at ingestion per the Z1
      contract; both prod and DR rows load, distinguished per the Z2 ruling.
  - id: Z4
    epic: server-location
    title: "Load-balancer nslookup resolver: resolve LB names to real servers, match against the inventory, attribute job locations"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Z3]
    inputs:
      - drydocs_lineage/collect
      - drydocs_lineage/extractors
      - tests/fixtures
    acceptance: >
      A collector-style probe (the bkup-collector precedent: shell collection,
      Python parsing) takes the distinct job hosts for an application (the Z3
      query), runs Linux nslookup against each name that is a load balancer,
      parses the resolved hostnames, and matches them against the ingested server
      list — so a job scheduled against an LB name resolves to the physical
      servers, and therefore the LOCATIONS, it actually runs on. The mechanism is
      sample-reproducible: tests run against a canned nslookup transcript fixture,
      never live DNS; the live run is company-side by construction. Output is an
      evidence file (which hosts resolved where, which did not resolve or did not
      match — unmatched is reported, not dropped); any graph write goes through
      the Z2-gated shapes only.
    notes: >
      User directive 2026-08-08. This is the piece that turns "the job runs on
      node X" into "the job runs in city Y" when X is a load-balancer alias. Also
      the natural module seam for the console: the resolver's OUTPUT feeds the Z5
      map panel; the resolver itself stays a collector, not a web dependency.
  - id: Z5
    epic: server-location
    title: "Reusable location-map console module: world + country map for any located node label, relationship dropdown with job/person/building icons"
    type: task
    module: drydocs-web
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Z3]
    inputs:
      - web/src/components
      - web/src/modules/registry.ts
      - drydocs_api
    acceptance: >
      A self-contained console map component, insertable into any page: given a
      node label that carries location (the Z2 contract), it renders a world map
      and a drill-down country map, and a selection dropdown shows the
      relationship from a job or person/team (person/team icon) to the city, with
      a building icon for a data center or office location. Map assets are
      embedded (SVG), no external tile or font fetch — the console must work
      offline, same rule as every committed render surface. Data arrives through
      a read-only QuerySpec (O33 guard); the component takes label + relationship
      as props so the next located label reuses it unchanged.
    notes: >
      User directive 2026-08-08. Explicitly designed as a module first, pages
      second — the directive names reuse as the point. First consumers: the
      application server-location view (via Z3/Z4 data) and the Z6 runtime map.
  - id: Z6
    epic: server-location
    title: "Global time-zone runtime map: folder/job start-to-end spans across a world time-zone view, rendered in the browser's local time zone"
    type: task
    module: drydocs-web
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [Z5]
    inputs:
      - web/src/components
      - drydocs_api
    acceptance: >
      Reusing the Z5 map core: for a selected folder or job, its runtime span
      (start to end) renders across a global time-zone map, and the display
      converts to the viewing browser's local time zone (read from the browser,
      never hardcoded). Spans that cross midnight or span zones render without
      truncation. Runtime data comes from the temporal sources already ruled
      (the cm_avg_run dataset family) where available; where a folder has no
      explicit time, the Control-M DC-name default-time semantics
      (e.g. T032-E0700-DMA = default 07:00 EST) may seed the span ONLY if
      labeled as a default, never presented as an observed runtime.
    notes: >
      User directive 2026-08-08. This is where the two meanings of "DC" earn
      their keep in one view: the SCHEDULING default time comes from the
      Control-M DC name, the PHYSICAL place from the Z1 export — the map shows
      both without conflating them.

  # --- 2026-08-09 weekly groom: the persona Run-2 findings (U18 docmeta scope
  # --- gap, U19 scanner sys.path roots, L27 cite guard, V11 docmeta runbook),
  # --- the four post-G22 data-profile gate SESSIONS Idea-85 asked to be given
  # --- ids (G62-G65), the company docmeta port divergence (J40), and the two
  # --- halves of the business-layer location experiment that are not Z2's
  # --- (C28 org structure, Q17 corpus reshape).
  - id: U18
    epic: self-documentation
    title: "The architect-persona metrics allow-list is missing drydocs_docmeta — ten modules are invisible to every A3/A4/A5 number"
    type: bug
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - .claude/skills/tech-debt/SKILL.md
      - docs/reviews/code-graph-review-plan.md
      - MODULE_MAP.md
      - tests/unit/test_module_boundary.py
    acceptance: >
      The $packages allow-list bound in the tech-debt skill's A1-A6 query pack names
      EIGHT package roots, adding drydocs_docmeta to the seven U14 measured, and the
      review plan's baseline table says the same eight; A3 and A5 are RE-BASELINED
      against the eighth root with the new numbers written next to the old ones so
      the next run compares like with like (U14's own rule); and the two docmeta
      connectors that carry no test — drydocs_docmeta/connectors/filedrop.py and
      connectors/web.py — are named in the A5 untested list rather than being absent
      from it. Both places are edited in one pass; the allow-list stays in the
      QUERIES, never in the scanner (the U9/U14 ruled shape).
    notes: >
      Groomed 2026-08-09 from Idea-91 (persona Run 2, U-arch F4). The package was born
      2026-08-04 at Q6 — the same day the U14 baselines were measured — and it already
      has a MODULE_MAP row and a test_module_boundary entry, so the gap is purely in
      the metrics scope. Same failure shape as Run 1's drydocs_api census miss, one
      package generation later: the allow-list is hand-maintained, so every new package
      is invisible until someone remembers it. Worth recording in the close note whether
      the list should be DERIVED from the modules registry instead of typed, which is
      the only fix that stops the third instance.
      DONE 2026-08-09 (desktop, neo4jtest, drydocs DB, graph loaded at 2d104ef): the allow-list is eight roots in BOTH places (tech-debt SKILL.md A1-A6 pack, code-graph-review-plan.md), and A3/A4/A5 are re-baselined with the old numbers written beside the new. SCOPE WIDENED, necessarily: A1 does NOT use $packages — it hardcodes its own upward-target list, which was missing drydocs_docmeta too, so 'does core import docmeta?' was a question the pack could not ask and A1's baseline of 0 was silence rather than evidence. Re-probed with the eighth root in the list: still 0. MEASUREMENTS, taken both ways on the SAME graph so the eighth root's contribution is separated from tree drift. A3: 29 (seven roots, 08-04) -> 31 (eight roots, 08-09) — but the top-15 is byte-identical at seven and eight roots, so ALL of that move is drift and NONE is the eighth root; docmeta's highest fan-in is connectors/base.py at 7 against a 13 cutoff. A4: 0 -> 0; every docmeta module has at least one importer. A5: 29 (seven roots, 08-04) -> 27 (seven roots, 08-09) -> 29 (eight roots, same graph). THE A5 NUMBER NEEDS ALL THREE READINGS, which is the whole point of the write-them-side-by-side rule: the eight-root figure lands back on 29 by COINCIDENCE — the tree lost two untested modules while the eighth root added exactly two — so quoting 29 -> 29 would report no change across two real movements in opposite directions. The two added are exactly the pair the item predicted: drydocs_docmeta/connectors/filedrop.py and connectors/web.py, now named in the A5 list. A6 aside worth keeping: docmeta is a target only (tests 10 edges, scripts 1) and imports nothing outside itself. ANSWER TO THE OPEN QUESTION the item asked to record — should the list be DERIVED rather than typed: YES, but NOT from the backlog `modules:` registry, which is a LOGICAL component list (drydocs-load/review/plan/docgen all map to the one `drydocs` root, and reference/taxonomy/config/docs are not Python at all). The right source is pyproject.toml [tool.poetry] packages plus the single non-package scan root `tests`. Verified decisive rather than asserted: drydocs_docmeta was added to pyproject in d647171, THE SAME COMMIT THAT CREATED THE PACKAGE, on the same day U14 typed seven roots — so the derivation would have been right from hour zero and ten modules would never have been invisible for five days. Built the cheap half rather than restructuring the queries: two guards in tests/unit/test_code_graph_review_plan.py — the typed scope in both surfaces must equal pyproject's packages + tests, and A1's own hardcoded list must cover every shipped package. Both PROVEN to fail with the eighth root removed (J26). This was the SECOND instance (drydocs_api was the first, at the U2 census); the guard is what stops the third. Related gap NOT fixed here and inboxed instead: Phase 3 unit 3's doc-coverage baseline still reads 'six scan roots' with per-root counts that predate both drydocs_api and drydocs_docmeta — same disease, different table, outside this item's stated surface.
  - id: U19
    epic: self-documentation
    title: "Depgraph scanner resolves no imports rooted off a directory that is its own sys.path root — scripts/ and agents/ show zero internal edges"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - knowledge/depgraph-snapshots/README.md
      - knowledge/depgraph-snapshots/snapshot.ps1
      - scripts/render_board.py
      - agents/
    acceptance: >
      The extractor resolves bare-name imports against PER-DIRECTORY sys.path roots
      (or an explicit alias map), and a fresh snapshot proves it on the two cases that
      fail today: scripts/render_board.py's seven sibling imports appear as
      scripts -> scripts IMPORTS edges (zero today), and the agents/ modules that
      import common.* / graph_qa.* against the agents/ root appear as
      agents -> agents edges (zero today). The first-party orphan queue is then
      re-counted and the review plan's 23-item figure is restated with the corrected
      number, since most of that queue is a false positive of this blind spot. Absolute
      imports from the same files must keep resolving — the U6 cross-root fix is not
      regressed. Venue named per J18.
    notes: >
      Groomed 2026-08-09 from Idea-92 (persona Run 2, U-arch F1). Consequence, stated
      plainly: until this lands, only the PACKAGE-scoped metrics are trustworthy and
      the orphan queue should not be worked from. The fix belongs in the SIBLING
      depgraph extractor rather than in a producer wrapper — U16 is sizing exactly that
      in-house-vs-sibling trade, and if this can only be done here as a fourth
      post-processing wrapper step, that IS the trigger U16 names for forcing the call;
      say which one it turned out to be in the close note.
      DONE 2026-08-09 (desktop, neo4jtest, drydocs DB): fixed in the SIBLING extractor, which answers the question U19 was told to answer for U16 — it did NOT have to become a fourth post-processing wrapper step, so the trigger U16 names for forcing the in-house-vs-sibling call did NOT fire, and U16 stays a sizing exercise rather than a forced decision. depgraph 6ee0af6 (main, clean descendant of the a56d2fc pin, PUSHED to origin before the pin bump so the laptop can reach it — a pin naming an unpushed commit is the 2026-08-04 two-machine divergence with extra steps); producer pin bumped in config/dev-environment.yaml. THE RULE IS THE INTERPRETER'S, not a heuristic: walk up from the file while the directory is a package (__init__.py present); the first ancestor that is not is what Python puts on sys.path. That yields the script's own directory for a loose script and the non-package parent for a module inside a package, which is exactly what `python -m pkg.mod` makes importable. DESIGN CHOICE worth the record: the extra root is APPENDED after the project roots, a deliberate departure from Python, which searches the script directory FIRST. Precedence only matters when one name resolves both ways, and there the existing answer is what every committed snapshot was built from — appending makes the change purely additive, so it can create a missing edge and cannot move one that already resolved. Pinned by a test with shared.py present at both locations rather than left to a comment. BOTH ACCEPTANCE CASES VERIFIED IN THE GRAPH: scripts->scripts 0 -> 7 (all seven of render_board.py's sibling imports, plus its cross-root drydocs/plan_board.py edge); agents->agents 0 -> 20, with graph_qa/pipeline.py resolving common/__init__.py, common/specs_catalog.py and three graph_qa siblings. Whole-snapshot edges 830 -> 878 across the bump. NOT REGRESSED, checked rather than assumed: package-scope metrics are identical before and after (A3 top-15 unchanged with base.py 31, A4 0, A5 29), because packages already resolved through the repo root — the blind spot only ever lived outside them; the U6 multi-root and relative-import tests all still pass. ORPHAN QUEUE RESTATED, which was the point: 22 -> 4, with agents/ going 15 -> 0. Roughly four in five of that queue was a scanner artifact, not dead code, which is why the plan carried a do-not-work-from-it warning. All four survivors are subprocess-invoked entry points rather than dead files (knowledge/depgraph-snapshots/filter_ignored.py and probe_instrument.py, both run as scripts by snapshot.ps1; knowledge/upgrade-plans/p0-benchmark/benchmark_p0.py; scripts/extract_office_text.py), so the honest reading is ZERO first-party dead modules. The item cited 23; the plan carried 22 (15+4+3) and 22 is what was restated. Six sibling tests: the three behavioural ones proven to fail with the fix disabled, the three regression/precedence ones pass either way, which is what makes them regression tests (J26). Sibling suite 44 -> 50. Fifth instrument-change marker added to the snapshots README with the measured deltas, per the standing rule that a cross-boundary edge diff is the instrument and not growth.
  - id: L27
    epic: doc-infrastructure
    title: "A design doc may not cite a commit that is not reachable from HEAD — make the render pipeline fail on a dangling commit citation"
    type: task
    module: drydocs-docgen
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [L19]
    inputs:
      - scripts/render_design_doc.py
      - tests/unit/test_doc_outline.py
      - docs/design/
    acceptance: >
      A guard — a unit test, or the design-doc renderer itself — FAILS when a governed
      design doc's commit citation is not reachable from HEAD, naming the document and
      the dangling hash. Its behavior in a history-less environment is stated and
      tested rather than left to chance: a shallow clone or a checkout without the
      commit's ancestry must SKIP with a stated reason, never pass silently and never
      fail spuriously in CI. A document with no commit citation at all is disposed of
      explicitly — either required or allowed, with the choice written down. The suite
      is green when the guard lands, which is why L19 (the one-time sweep) is a
      dependency rather than a sibling.
    notes: >
      Groomed 2026-08-09 from Idea-94's mechanism half (persona Run 2, U-tw). The
      sweep half stayed in L19, which is its home; this item exists because the entry's
      real finding is BEHAVIORAL — rev bumps happen, cite refreshes do not
      (drydocs-startup-refresh-runbook took seven bumps still citing squash-day
      a135a6d) — and a one-time sweep does not change a behavior. Related but distinct
      from U17, which decides WHICH commit the staleness ranking reads; this one only
      asks that whichever commit a doc claims actually exists.

  - id: L28
    epic: doc-infrastructure
    title: "Cite the external code references: register spcl/knowledge-graph-of-thoughts in reference/research/, and give the executive overview a cited-references footer"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: done   # DONE 2026-08-10 (desktop) same session: KGoT registered (note file + research README row + REGISTRY.yaml research: entries — the stub became a real list, Unity Catalog and Essential GraphRAG backfilled); ADR 0007 footnote 2 added; overview rev 9 = References block (14 refs, 4 groups) + 5 inline sup markers, both previously-uncited Databricks claims attributed; footer verified in headless Edge, light AND dark; suite 1836 passed
    depends_on: []
    inputs:
      - reference/research/README.md
      - reference/REGISTRY.yaml
      - docs/decisions/0007-agentic-qa-architecture.md
      - docs/overview/drydocs-executive-overview.html
      - agents/graph_qa/task_graph.py
      - agents/common/llm_ledger.py
    acceptance: >
      (a) spcl/knowledge-graph-of-thoughts (KGoT) has a full entry under
      reference/research/ — a dedicated note file on the databricks-unity-catalog.md
      precedent, carrying the verified citation (paper, authors, arXiv id, doi), the
      REPO LICENSE (BSD 3-Clause, (c) 2025 ETH Zurich), what DryDocs ADOPTED, what it
      REJECTED and why, and a crosswalk naming ADR 0007 plus the four agents/ files
      that already carry KGoT-derived design. Link-only — no pasted full text, per the
      research README's standing rule.
      (b) reference/research/README.md gains a row for it, and reference/REGISTRY.yaml's
      research: STUB (docs: + note: only, no entries today) becomes a real entry list
      with kgot as its first entry; the file's top-level classification: External is
      unchanged, so tests/unit/test_classification.py still passes.
      (c) ADR 0007 resolves its KGoT mention to a citation footnote beside the existing
      one; the ADR is ACCEPTED and its argument is NOT re-adjudicated.
      (d) docs/overview/drydocs-executive-overview.html gains a References block in its
      footer covering EVERY external work it leans on — KGoT and llm-graph-builder (the
      agents module), Databricks Unity Catalog (both the comparison AND the quoted
      line, uncited today), the three Neo4j layer-model articles, and the four ontology
      standards in the section-04 table. URLs reused from reference/research/README.md
      rather than re-sourced. No new CSS: the footer's .revs pattern and the existing
      `a` rule carry it, and it renders in both themes.
      (e) All THREE revision surfaces bump to rev 9 per the L22/L24 convention — the
      REVISIONS comment block at the top of the file, the masthead Revision/Date line,
      and a new rev line at the head of the footer history with prior lines verbatim.
    notes: >
      Raised 2026-08-10 (desktop) by direct user request, scope confirmed in session:
      full reference set for the overview footer, dedicated note file for the entry.
      WHY THIS IS A REAL GAP AND NOT HOUSEKEEPING: KGoT is already load-bearing and
      wholly uncited. ADR 0007 analyzes it by name as one of two reference
      architectures, and four source files carry its design in their docstrings —
      task_graph.py (the NetworkX task-scoped working memory), llm_ledger.py (the
      collect_stats JSONL cost ledger), and providers.py + schema_context.py, which
      name KGoT TRAPS we deliberately avoided (a hardcoded tool model; whole-graph
      state injected into every prompt). reference/research/README.md's own rule is
      "cite these in ADRs", so an ADR citing an unregistered work inverts the
      arrangement. The overview is a NON-GOVERNED outward-facing doc (CLAUDE.md 6), so
      editorial treatment is legal there — the publish-VERBATIM rule binds the design
      renders, gate pages and board.html, none of which this item touches. It is also
      hand-authored HTML and its own single source (no .md twin; L24 verified this).
      OUT OF SCOPE, deliberately: docs/whitepaper/ has no citations either — a separate
      item, not a widening of this one.
  - id: V11
    epic: sme-runbooks
    title: "SME runbook: drydocs_docmeta — connectors, cleaner, tokenizer, manifest, policy, registry and the live CLI verbs"
    type: task
    module: docs
    phase: 10
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [V1]
    inputs:
      - drydocs_docmeta/
      - docs/design/templates/runbook.outline.yaml
      - tests/unit/test_runbook_coverage.py
      - docs/decisions/0006-docmeta-component-and-doc-graph.md
    acceptance: >
      docs/design/drydocs-docmeta-runbook.md exists, outline-conformant, rendered and
      stale-render-clean, and drydocs-docmeta comes OFF the RUNBOOK_PENDING list in
      tests/unit/test_runbook_coverage.py — the frozen list shrinks, which is the only
      direction it is allowed to move. Covers: the two generic connectors (web and
      filedrop) and what each refuses, the cleaner and tokenizer contracts, the
      manifest/registry shape a corpus must satisfy before it loads, the curation
      policy tiers, and the live CLI verbs with a worked end-to-end capture on a
      sample corpus. Mechanism only — no real corpus values.
    notes: >
      Groomed 2026-08-09 from Idea-95 clause (c). The gap is already recorded in the
      coverage test, which carries drydocs-docmeta with the comment "BUILT at Q6; no
      V-item yet, so this list is where it is visible" — this item is that V-item. Same
      growth stage that produced the drydocs-core runbook after persona Run 1: a
      package reaches ten modules and live verbs, and its runbook is the thing nobody
      minted.
  - id: G62
    epic: component-topology
    title: "Run the rua-bundle data-profile gate: the SME names the load population, the agent profiles it, and only the walked population enters the curated load"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: in_progress               # 2026-08-13 (desktop): SME convened the session (the USER-GATED START); §A opened. No bundles on this machine — §B runs company-side; producer lands the transcription + regenerated gate surfaces.
    depends_on: [G22, G23]
    inputs:
      - config/gate-prompts/rua-bundle-data-profile.yaml
      - config/gate-log.md
      - config/source-registry.yaml
      - drydocs_lineage/
    acceptance: >
      USER-GATED START — the SME convenes the session; the pull loop SKIPS this item
      until then (the C12/J13 idiom). The drafted rua-bundle-data-profile page is
      walked in its ruled two-step order: section A the SME identifies the source data
      (which hosts' bundles, where they sit, which collector produced each — Internal,
      recorded company-side, never on the page), section B the agent profiles that
      population through the G20 extractor in STAGING with zero graph writes, and
      section C consumes the profile to admit or trim the population, dispose of the
      hash-absent and collision cases it counts, and clear the F1 sweep. Sign-off
      transcribed to config/gate-log.md; gates.json + the enforcement matrix
      regenerate. No population the page has not walked enters the G23 curated load.
    notes: >
      Groomed 2026-08-09 from Idea-85, which asked only for ids and scheduling — the
      four post-G22 data-profile prompts were drafted 2026-08-07 and unsigned, and an
      unsigned prompt with no backlog id is invisible to the pull loop. Sessions run
      COMPANY-SIDE because the identify-the-source-data step names real hosts and paths
      (Internal); what lands producer-side is the transcription and the regenerated
      gate surfaces. Model fable: the ruling decides what may be written to the graph.
  - id: G63
    epic: component-topology
    title: "Run the repo-manifest data-profile gate: name the repos, bless trusted_ref (the field only a human may set), then rule corroboration and the commit-date stub"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G22, G24]
    inputs:
      - config/gate-prompts/repo-manifest-data-profile.yaml
      - config/gate-log.md
      - config/source-registry.yaml
      - config/audit-fields.yaml            # path corrected 2026-08-12 (groom): the ledger is a FILE, never a directory
    acceptance: >
      USER-GATED START — the SME convenes the session; the pull loop SKIPS this item
      until then. The drafted repo-manifest-data-profile page is walked in its two-step
      order: section A the SME identifies the in-scope repos and, per repo, the one ref
      the team keeps clean (Internal, company-side); section B the agent runs the G24
      object sweep per repo in staging, zero graph writes, and brings back the census —
      object counts, ref tips, content-hash corroboration against server occurrences,
      clone-lag, and the commit-date evidence the audit stub waits on; section C sets or
      declines trusted_ref PER REPO (this page is the only venue where a human may set
      it), rules the corroboration thresholds, and closes or re-scopes the audit stub
      on that evidence. Sign-off transcribed to config/gate-log.md; gates.json + the
      enforcement matrix regenerate.
    notes: >
      Groomed 2026-08-09 from Idea-85. The load-bearing clause is trusted_ref: the
      registry field is null on purpose — server-extract-only truth until a human
      blesses a ref — and a mechanical sweep may NAME a candidate but may not bless
      one. Company-side session (real repo names and Bitbucket access are Internal).
  - id: G64
    epic: component-topology
    title: "Run the dpl pipeline-registry contract gate: validate the ASSUMED field contract against a real export and discharge tracker T13"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G22, G25]
    inputs:
      - config/gate-prompts/dpl-pipeline-registry-contract.yaml
      - config/gate-log.md
      - drydocs_lineage/extractors/dpl_registry.py
      - config/audit-fields.yaml            # path corrected 2026-08-12 (groom): the ledger is a FILE, never a directory
    acceptance: >
      USER-GATED START — the SME convenes the session; the pull loop SKIPS this item
      until then. The drafted dpl-pipeline-registry-contract page is walked: section A
      the SME identifies a real pipeline_id.json export (which SEALs, which registry DB,
      pulled when — Internal, company-side; the run names its venue per J18); section B
      the agent parses it against the ASSUMED contract and profiles it — per-field
      census (present / absent / present-but-empty), version and active-flag
      distributions, and GUID join coverage against the G15/G17 staged pipeline tokens
      — with zero graph writes; section C closes or re-scopes T13 on that evidence,
      closes or stands the dpl audit stub, and keeps the active flag the third usage
      signal G22 ruled it to be. Sign-off transcribed to config/gate-log.md; gates.json
      + the enforcement matrix regenerate. The registry row's confirmed flag is NOT
      re-opened.
    notes: >
      Groomed 2026-08-09 from Idea-85. This is the T13 discharge venue: the field
      contract has been assumed since G25 and never validated, which is exactly WHY
      the two dpl entries in audit-envelope-phase4 are ruled stubs — claiming that
      createdBy/lastModifiedBy exist, or that they do not, is a guess until a real
      export parses. Company-side session (GUIDs, SEAL ids and registry URLs are
      Internal).
  - id: G65
    epic: component-topology
    title: "Run the dpl dataset-registry contract gate: validate the ASSUMED contract, rule the audit stub, and rule per-SEAL-vs-bulk discovery precedence"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: [G22, G25]
    inputs:
      - config/gate-prompts/dpl-dataset-registry-contract.yaml
      - config/gate-log.md
      - config/precedence.yaml
      - drydocs_lineage/extractors/dpl_registry.py
    acceptance: >
      USER-GATED START — the SME convenes the session; the pull loop SKIPS this item
      until then. The drafted dpl-dataset-registry-contract page is walked: section A
      the SME identifies a real dataset_id.json export (which SEALs, per-SEAL API or
      bulk export, pulled when — Internal, company-side, venue named per J18);
      section B the agent parses and profiles it — field census against the ASSUMED
      contract, createdBy/lastModifiedBy evidence for the audit stub, and GUID join
      coverage against the G17 dataset_flow READS/WRITES candidates with their zone
      distribution and the catalog registration views' dpl_dataset identities — zero
      graph writes; section C confirms or amends the contract, closes or stands the
      audit stub, and rules per-SEAL-vs-bulk discovery precedence in
      config/precedence.yaml terms. Sign-off transcribed to config/gate-log.md;
      gates.json + the enforcement matrix regenerate.
    notes: >
      Groomed 2026-08-09 from Idea-85. Run SEPARATELY from its pipeline sibling (G64)
      on the prompt's own reasoning: different artifact, different assumed contract,
      different join target — datasets join the G17 MAC dataset_flow evidence and the
      catalog registration views, not the launcher tokens. Carries its own caveat, the
      2026-07-23 clone-lag ruling that the bulk export is the backup discovery source
      when per-SEAL pulls lag. Company-side session.
  - id: J40
    epic: release-infrastructure
    title: "Reconcile the company docmeta divergence BEFORE the next docmeta port: the colliding ADR number, the package path, and two files that exist only over there"
    type: task
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - git-readme.md
      - docs/port-prompt.md
      - PORT-MANIFEST.yaml
      - docs/decisions/0006-docmeta-component-and-doc-graph.md
      - drydocs_docmeta/
    acceptance: >
      A recorded reconcile decision, written where the next port session reads it
      (docs/port-prompt.md and/or git-readme.md, with PORT-MANIFEST rows adjusted to
      match), covering all three divergences by name: (a) the ADR NUMBER COLLISION —
      the company carries docmeta at 0005 while the producer's 0005 is the
      browser-to-Neo4j access path and its docmeta ADR is 0006, so the decision states
      which number is canonical on each side and how a reader of either repo learns
      that; (b) the PACKAGE PATH — company drydocs/docmeta/ (drydocs.docmeta) vs
      producer top-level drydocs_docmeta/ — converged, or deliberately divergent WITH
      the reason recorded; (c) the two company-only files, prompts.py and pipeline.py,
      dispositioned as back-flow candidates or as company-only with a reason. Until
      that decision exists, docmeta paths are marked in the manifest so a verbatim
      apply cannot silently take them.
    notes: >
      Groomed 2026-08-09 from Idea-87. This is the class a port silently clobbers: a
      straight producer-to-company take would overwrite the package path,
      renumber-or-duplicate the ADR, and DROP two files that exist only on the company
      side. Same failure mode as Idea-79/J34 (the PORT-MANIFEST company-overlay seam,
      built 2026-08-07) but a different artifact — J34 gives this item the mechanism to
      express (b) and (c); what J34 cannot decide is which ADR number is canonical, and
      that is a user/SME call this item is the venue for, not a groom decision. Sequence
      it BEFORE the next docmeta-touching port, not after.
  - id: J41
    epic: release-infrastructure
    title: "The port has a mandatory CLOSING sequence and no OPENING one: certify the base before a company session starts"
    type: task
    module: docs
    phase: 8
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: []
    inputs:
      - docs/port-prompt.md
      - .claude/skills/reconcile-port/SKILL.md
      - PORT-MANIFEST.yaml
      - scripts/render_board.py
    acceptance: >
      An OPENING SEQUENCE in docs/port-prompt.md beside the J35 closing sequence and
      carrying the same force, listing what the PRODUCER must finish before any company
      session may start: (1) full tests/unit/ green on the exact commit offered,
      venue-stamped per J18; (2) ledger rolled through it with coverage verified
      commit-by-commit; (3) every producer action triggered by company state landed —
      anything the company would otherwise have to do to a canonical-producer file;
      (4) renders current; (5) the base tagged port-base-YYYYMMDD and pushed. Plus the
      ONE-OWNER-PER-PHASE rule: a plan never crosses the repo boundary, and a phase whose
      owner differs from the one before it ENDS the plan. Backed by
      scripts/port_preflight.py running all five checks (report-only by default, --tag to
      create and push), with its ledger-coverage check parsing cited SHAs out of the step
      ledger and naming any uncited non-ritual commit. reconcile-port SKILL.md step 1
      gains the certified-tag check so a company session that never opens port-prompt.md
      still gets it. Guards in tests/unit/test_port_preflight.py, each proven to FAIL on
      an injected defect before being trusted (J26).
    notes: >
      Raised 2026-08-09 from a port that could not run solo. A company session was handed
      a thorough 8-phase plan and stopped at Phase 2, which asked it to remove three
      clean-add rows from PORT-MANIFEST.yaml — a canonical-producer file its own Phase 3c
      takes wholesale, so the edit would have been reverted in the same session. MORE
      INSTRUCTION DETAIL COULD NOT HAVE FIXED THAT: the phase was undoable by its
      assignee. Two more defects rode along, both invisible until the end because
      acceptance only ran on the applied result: test_bootstrap_guard failed on ANSI
      colour rather than behaviour (rich honours ambient FORCE_COLOR even under NO_COLOR)
      and IDEAS.md carried a duplicate Idea-101 from a two-session id collision —
      verified present at the pinned SHA, so the port would have failed its own zero-fail
      contract and both would have read as port-introduced. WHAT WENT RIGHT AND MUST NOT
      BE REGRESSED: the session STOPPED rather than improvising, on its own wait/flag
      rule. So this is not "the phased approach failed" — it is a plan with one
      misassigned phase correctly refusing to execute it. Keep the phases and the
      wait/flag rule; fix the ownership boundary and the uncertified base. The tag also
      retires the moving-base problem: the base moved 42 -> 45 commits while the session
      held, and every prompt had to carry an awkward "verify, the producer is still
      committing".
  - id: C28
    epic: ontology-mapping
    title: "Draft the business-layer org-structure gate prompt: W3C ORG units over the existing LOB layer, and how a re-org is recorded without rewriting history"
    type: task
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - internal/context-graph-analysis/business-layer-location-experiment.md
      - reference/standards/
      - drydocs_core/ontology/relationship_vocabulary/
      - config/taxonomy/lob-product-team.yaml
      - config/gate-prompts/
    acceptance: >
      A drydocs.gate-prompt.v1 spec in config/gate-prompts/ that DRAFTS ONLY — it
      decides nothing, and the SIGN-OFF is a separate SME session (the G27/U11/Q14
      precedent) — putting three questions to the SME with the experiment's evidence
      attached: (a) whether W3C ORG org-unit terms bind to the LOB layer the repo
      already holds (the experiment found the annual report's org units ARE that layer,
      "managed on an LOB basis") or to a new class, given the CLAUDE.md warning about
      inventing a label for convenience; (b) how a structural change is expressed —
      the experiment's effective-dated org change event for a segment merge — so that a
      re-org ADDS a fact instead of rewriting the previous one; and (c) what the
      as-of/effective-dating shape is for org membership, reusing the as-of-assertion
      idiom already ruled for FID ownership rather than inventing a second one. Every
      proposed term is registered `status: planned` in the relationship-vocabulary
      fragments with a decision-matrix entry; nothing is flipped, no loader writes an
      edge, and the location half of the experiment is explicitly out of scope (it
      rides Z2).
    notes: >
      Groomed 2026-08-09 from Idea-90's org half. MODULE/EPIC CHOICE RECORDED, since the
      entry itself offered two homes ("a business-layer gate or E-epic item"): filed
      under ontology-mapping / phase 2 because a W3C ORG binding is an ontology-mapping
      decision of exactly the kind phase 2 exists for, and NOT under Epic Z, which is
      about physical server location, nor Epic E (context graph), whose subject is
      task-scoped projections rather than a standing org model. If the SME wants it run
      as one combined business-layer session with Z2, that is a scheduling choice the
      two prompts can accommodate — the drafts stay separable because their subjects
      are. Evidence base is public-source only (annual-report material), so the prompt
      itself is mechanism-and-public-fact and stays Internal-Public.
  - id: Q17
    epic: docmeta
    title: "Decide the jpmc-reports corpus shape: reshape onto the lexical document spine, or keep the DataAsset slices — the decision now has a consumer"
    type: task
    module: drydocs-docmeta
    phase: 14
    agent: main
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - config/doc-source-registry.yaml
      - knowledge/upgrade-plans/docmeta-component.md
      - internal/context-graph-analysis/business-layer-location-experiment.md
      - docs/decisions/0006-docmeta-component-and-doc-graph.md
    acceptance: >
      A decision record lands PROPOSED in docs/decisions/ — never accepted from a groom;
      the user rules it (the U16 precedent) — stating whether the jpmc-reports corpus
      reshapes onto the lexical Document-to-Chunk spine or keeps its DataAsset slice
      shape, and what each choice costs. It must state: the consumer that now exists
      (the 2026-08-08 business-layer experiment wanted GraphRAG retrieval over this
      corpus and could not have it — the registered shape is not the lexical spine, so
      no vector retrieval is possible regardless of the database state); what happens to
      the registry row's graph_locator and its confirmed flag under each choice;
      whether the newer 2025/2026 editions sitting at the repo root ride the re-ingest;
      and the precondition, recorded honestly rather than assumed away — ddcontext was
      probed EMPTY on the desktop (neo4jtest, 2026-08-08), so any re-ingest gates on the
      provisioning ruling that Idea-49 is parked on. No corpus is re-ingested by this
      item.
    notes: >
      Groomed 2026-08-09 from Idea-90's first queued decision. The reshape has been
      "a P4+ decision" in the registry notes since Q5 with nothing to force it; what
      changed is that a real question was asked of this corpus and the shape was the
      reason it could not be answered. Fable because the choice moves a corpus between
      two graph shapes and touches the docmeta component boundary. The PDFs themselves
      were never committed (the root PDF gitignore precedent) and the one-off ingest
      script was removed 2026-07-22 — so "re-ingest" means through the docmeta
      component, not by recovering the script, and the decision record should say so.

  - id: C29
    epic: ontology-mapping
    title: "Capture the Control-M job-metadata standards (22 screenshots), audit what ontology already exists for their terms, and publish the sanitized token + variable register"
    type: task
    module: docs
    phase: 2
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: []
    inputs:
      - job-req-1.png
      - job-req-2.png
      - job-variable-1.png
      - job-variable-20.png
      - internal/controlm-config/reference/controlm-pipeline-stub-capture.md
      - knowledge/standards/technology/description-field-metadata-plan.md
      - internal/standards/technology/description-field-metadata-plan.md
      - drydocs_core/ontology/relationship_vocabulary/
      - config/gate-prompts/email-dl-contact-point.yaml
    acceptance: >
      Three deliverables, none of which decides anything the HITL gate owns. (a) A
      VERBATIM Internal capture at
      internal/controlm-config/reference/controlm-job-metadata-standards-capture.md of
      all 22 gitignored root screenshots (job-req-1..2, job-variable-1..20), following
      the controlm-pipeline-stub-capture.md precedent in the same directory: header
      naming classification, capture date and the source filenames, cuts marked with
      an ellipsis-in-brackets. It carries five parts -- REQ-1..REQ-4; NFR-CTM-001 v2;
      the File Name Component Standard; MFTS Route IDs / DPROD extension; Source
      Contact and PDN Downstream -- and MUST mark NFR-CTM-001 section 4 (the canonical
      variable registry) as NOT CAPTURED rather than papering over the gap, because
      the screenshots jump from section 3 to section 5 while sections 5 and 8 both
      depend on it. (b) In the same file, a components-by-job-type breakdown
      (FileWatcher first, then Publisher, then ETL/cmd, then folder-level) and an
      ontology-coverage audit with one row per term the documents name, each verdict
      citing file:line -- the ETL/cmd family resolves BUILT (FACT_REGISTRY at G16,
      m7_uses_artifact / m3_invokes), the description-token family resolves OBSERVED
      BUT NOT REGISTERED, and the roughly twenty ex: terms resolve NONE REGISTERED.
      (c) A sanitized token + variable register in the Internal-Public mechanism twin
      knowledge/standards/technology/description-field-metadata-plan.md with columns
      component / token or variable name / carrier / SQL column / ontology term /
      ontology class / vocabulary / status / notes, plus the file-name component
      standard, plus a migration row reconciling the C16 prefix governance against the
      bare spellings the documents actually use; real addresses, accounts, route ids
      and job names stay in the values twin under internal/. NO relationship-vocabulary
      writes and NO graph writes -- section 0b and the class question are gate
      territory. Instead the evidence lands as a rider section on the drafted,
      still-unsigned config/gate-prompts/email-dl-contact-point.yaml, because that
      gate's sections C2/C3 recommend the escalation DB and demote the Description
      field while these documents do the opposite. Publish-boundary check passes: real
      DL domains, the artifact host, the MFT account and the internal repo host appear
      ONLY under internal/.
    notes: >
      Claimed 2026-08-11 (desktop) from user direction with the screenshots attached.
      Filed under ontology-mapping / phase 2 rather than taxonomy-capture because the
      deliverable that matters is the term-by-term ontology verdict and the gate rider,
      not the hierarchy import; the transcription is the evidence those rest on. The
      screenshots stay gitignored by the root png rule -- the capture is the durable
      artifact, which is the whole reason this item exists. Sibling G66 opens the read
      seam so the DESCRIPTION field this standard depends on can actually be ingested;
      the two were split so the document work is not blocked behind code review. A
      collision worth carrying forward and recorded in the capture: the DPL stub
      integration plan's item E1 uses two literal DESCRIPTION strings as a
      machine-generated provenance discriminator, and this standard fills the same
      4000-char field with tokens -- exact-literal match and token block cannot both
      hold on a generated object.

  - id: G66
    epic: component-topology
    title: "Open the DESCRIPTION read seam: the Control-M XML extractor stages folder and job descriptions, and a pure parser shreds the pipe-delimited token grammar"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: []
    inputs:
      - drydocs_lineage/extractors/controlm_xml.py
      - drydocs_core/orchestration/controlm/variables.py
      - tests/unit/test_lineage_controlm_xml.py
      - knowledge/standards/technology/description-field-metadata-plan.md
    acceptance: >
      The G47 extractor stages the one field the description-metadata standard is built
      on. XmlFolderRecord and XmlJobRecord gain a defaulted description read through the
      existing attribute-synonym helper, the module docstring's ASSUMED XML CONTRACT
      block is amended to say so (adding an attribute without amending it is exactly the
      drift that block exists to prevent), and XmlDefsCoverage gains a
      descriptions_absent counter -- an adoption signal, not a skip reason, in keeping
      with the count-every-skip-by-reason discipline. A new pure module
      drydocs_core/orchestration/controlm/description_tokens.py carries a TOKEN_REGISTRY
      (token key -> canonical name, carrier, job type, controlled value set, target SQL
      column) and parse_description(): split on pipe, split each part on the FIRST colon
      only so a time-valued token survives, whitespace-tolerant on both sides because
      both spacings occur in the wild, the literal string NULL parsing to Python None to
      reproduce the Oracle NULLIF behaviour, semicolon-separated addresses staying inside
      one token value, and unknown keys RETURNED rather than dropped because C16's
      team-local class is legal and preserved verbatim though never load-bearing.
      Vocabulary violations are returned as findings, never raised -- the
      aliases-suggest-values-decide and WARN-stream discipline the FACT_REGISTRY next
      door already follows. Tests: the existing inline DEFTABLE fixture gains DESCRIPTION
      on a folder, a FileWatcher job and a Publisher job plus one job without one, with
      verbatim staging and the counter asserted; a new tests/unit/test_description_tokens.py
      covers every rule above. ZERO graph writes -- the extractor stays staging-only and
      test_staging_is_graph_free still passes; the full suite is green.
    notes: >
      Claimed 2026-08-11 (desktop) alongside C29, which supplies the token grammar this
      parses. Scoped deliberately to the READ half. Emitting a corrected XML -- the
      other half of the user's stated goal -- is NOT in this item: XmlDefinitionFormat
      in drydocs_remediation/formats.py raises NotImplementedError by design pending the
      vendor Folder.xsd, and writing an emitter now would mean inventing the vendor
      contract that the blocked message exists to refuse. Its real unblock is the DPL
      stub's schema (Idea-50), so the emitter is a separate follow-up rather than a
      guess bolted onto this one. Both dataclasses are frozen with defaulted trailing
      fields, so the added field is backward-compatible with every existing construction
      site.

  - id: C30
    epic: ontology-mapping
    title: "The Control-M greenfield job standard: one scope ladder and one naming rule across FileWatcher, Placement and Trust, publishable as the revised standards page"
    type: task
    module: docs
    phase: 2
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [C29]
    inputs:
      - internal/controlm-config/reference/controlm-job-metadata-standards-capture.md
      - internal/controlm-config/reference/controlm-pipeline-stub-capture.md
      - internal/remediation/governance/command-line-and-variables-standard.md
      - internal/remediation/governance/greenfield-recommendations.md
      - knowledge/standards/technology/description-field-metadata-plan.md
      - knowledge/standards/technology/filewatcher-postexec-token-cat.md
      - external/orchestration/bmc-controlm/controlm-variables.md
      - drydocs_core/orchestration/controlm/variables.py
    acceptance: >
      A twinned standard -- knowledge/standards/technology/controlm-greenfield-job-standard.md
      (mechanism, Internal-Public) and its values twin under internal/ -- written AS a standards
      page so the SME can publish it without rewriting, reconciling three sources that currently
      disagree: the 22-screenshot corpus (what jobs should carry), the DPL generator (what it
      emits for Placement and Trust), and one live SMART folder (what is deployed). It must
      carry: (a) the SCOPE LADDER -- flow-invariants at SMART-folder scope, dataset identity at
      sub-folder scope, only per-job values on the job -- presented as making explicit the split
      the generator already performs via the folder AUTOEDIT block it assumes but never emits,
      with the BMC resolution order as the warrant and the VARIABLE_INC_SEC caveat stated
      (folder scope resolves for job processing parameters; resolving inside a job SCRIPT needs
      the server setting); (b) the HYBRID NAMING rule -- file-name components keep the names
      that map 1:1 to the CM_JOB_FILE_NAME_STANDARD columns so the SQL parse is a straight
      column copy, while short derived handles carry the composed path into commands,
      generalizing the idiom REQ-3 already uses; (c) the CANONICAL rule -- one name per concept,
      UPPER_SNAKE, with the variable name, the STG_APP_FACT fact_type, the SQL column and the
      ontology property stem being the same token, and the launcher FLAG recorded as a
      per-framework binding, never as the variable's name; (d) a before/after variable register
      per job type, every row citing both the live value it replaces and the generator builder
      that emits it; (e) the description token set per job type under the rule that the
      description carries only facts with no runtime role and no other home -- which drops the
      corpus's per-job EMAIL_DL tokens, drops PDN_SNOW_QUEUE, retires the INBOUND/OUTBOUND route
      pair on watchers because a watcher is inherently inbound, and renames the watcher's ENV to
      FTS_ID because ENV already means the deployment environment on the command jobs; (f) the
      pipelineId carrier ruling -- the CMDLINE literal is the carrier, any PIPELINE_ID variable
      is removed, and the DPL join key is recovered by a single anchored -pipeline extractor
      declared as an exception to NFR section 10 rather than a reopening of it; (g) contacts
      recorded as documentation-only for runbook extraction, with the support-tier versus
      Production-Delay-Notification split preserved as distinct contact kinds, and with the open
      REQ-2 question -- whether DOMAIL is removed alongside SHOUT -- left for the SME rather than
      assumed. Publish-boundary check clean: the live screenshots add a service account, a
      run-as, a SEAL, a FID, an author SID and two GUIDs to the strings that may never leave
      internal/.
    notes: >
      Claimed 2026-08-11 (desktop) from SME direction with five live Control-M screenshots
      attached. THE REFRAME THAT MATTERS, and the reason this is not simply "fix the live
      folder": reading the generator capture before the live folder reverses most of the
      critique. The generator emits the canonical spellings (DATAFLOW, IMAGE, PROID, TIMEOUT)
      and orders DS_ID/DS_VER correctly; the live folder has the drifted ones. Every defect is
      hand-edit drift away from generator output, and it happens because the generator emits a
      PARTIAL job that depends on a folder AUTOEDIT block nothing declares -- so people hand-add
      the missing variables job by job and diverge. Fix the contract, not the instances. SME
      rulings recorded during design and binding on the write-up: notification is being REMOVED
      (the ServiceNow incident is the call to action, so nothing may wire a DL to a DOMAIL DEST
      or bind the unset NOTIFY); PDN is Production DELAY Notification to downstream business
      users and is not a support tier; no ServiceNow queue belongs in a Control-M variable
      because the escalation DB owns technician routing; the post-execution cat is TOK/CTL only
      and never a data file; and the live folder was mid-edit when captured, so some watcher
      findings are an unfinished conversion rather than deployed breakage and must not be
      written up as production defects.

  - id: G67
    epic: component-topology
    title: "Control-M conformance detectors R30-R40 on real staged XML, with the read bridge that unblocks the remediation module"
    type: task
    module: drydocs-remediation
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [G66]
    inputs:
      - drydocs_remediation/detect.py
      - drydocs_remediation/formats.py
      - drydocs_lineage/extractors/controlm_xml.py
      - drydocs_core/orchestration/controlm/variables.py
      - internal/remediation/standards-rules-registry.md
    acceptance: >
      The remediation module gains a working Gate-2 conformance pass over REAL staged
      definitions. A new read-only bridge turns the G47 extractor's XmlDefsExtract into a
      DefinitionSet, reusing XmlDefsExtract.scope_layers() rather than reimplementing scope
      resolution -- which retires a stale blocker, since XmlDefinitionFormat.load() still raises
      on vendor-schema grounds although G47 solved reading; dump() STAYS blocked because
      emitting importable XML is the half that genuinely needs the vendor schema. JobDefinition
      gains description / command_line / post_command and DefinitionSet gains sub-folder scope,
      all defaulted so no existing caller breaks. detect.py gains R30-R40 beside the existing
      R1, reusing the SAME Finding dataclass (test_no_shadow_definitions forbids a second class)
      and the same ratified=False discipline, because registry ratification is gate territory:
      unresolvable reference in the scope chain, orphan declaration, required declaration set
      per job type, exactly-one-carrier-per-fact, value contracts per canonical, scope hoisting,
      retyped composed path, adjacent references, vendor charset legality, the two-clause
      post-exec cat rule (required on TOK/CTL, FORBIDDEN on DAT), and REQ-2 zero SHOUT/DOSHOUT.
      Name drift and value swaps report as DIFFERENT classes -- drift produces silence, a swap
      produces a confidently wrong row, and the second must not be filed as a lint warning.
      R30-R40 are registered in the internal rules registry in the existing entry shape, all
      provisional. The proof that the standard is reachable and not merely describable: a
      synthetic DEFTABLE reproducing the live folder's shape raises every rule, and the same
      fixture rebuilt to the C30 ladder raises ZERO. Full suite green.
    notes: >
      Claimed 2026-08-11 (desktop) alongside C30, which supplies the rules this enforces.
      Deliberately NOT in scope: emitting corrected XML. Two detectors are deliberately absent
      and the absence is the point -- nothing binds the NOTIFY variable to a distribution list,
      because email is being removed as a mechanism and a rule that "fixed" the unset
      destination would re-wire it; and nothing asserts a ServiceNow queue on a Control-M
      object, because that routing belongs to the escalation DB via the EJOBNAME join. NOTIFY is
      reported as an ordinary unresolvable reference, nothing more.

  - id: C31
    epic: ontology-mapping
    title: "One Control-M Guidelines and Standards page to replace the out-of-date set, with the scope-and-variable-flow diagram and a worked folder-to-job resolution trace"
    type: task
    module: docs
    phase: 2
    agent: main
    model: opus
    priority: p2
    status: done
    depends_on: [C30]
    inputs:
      - knowledge/standards/technology/controlm-greenfield-job-standard.md
      - knowledge/standards/technology/description-field-metadata-plan.md
      - knowledge/standards/technology/filewatcher-postexec-token-cat.md
      - knowledge/standards/technology/folder-naming-convention.md
      - internal/remediation/standards-rules-registry.md
      - external/orchestration/bmc-controlm/controlm-variables.md
    acceptance: >
      ONE publishable, normative page a Control-M job author can follow end to end, replacing
      the fragmented corpus the SME now considers out of date -- NFR-CTM-001 v2, the file-name
      component standard, the MFTS route page, the contacts page and REQ-1..REQ-4 currently say
      overlapping and in places contradictory things across five documents. It must be written
      in MUST/SHOULD normative voice rather than as analysis, since the reader is authoring a
      job and not reviewing a decision, and it must carry: (a) a MERMAID diagram of the scope
      ladder and the variable flow -- what each of folder, sub-folder and job contributes, the
      vendor resolution order that joins them, and where the derived file handles sit; (b) a
      DETAILED worked folder-to-job trace showing a single reference resolving through the
      chain step by step, including the narrowest-scope-wins rule and what happens when a name
      is declared at no scope; (c) the normative rules for every job type, each cross-referenced
      to its enforcing rule id in the registry so a reader can see what is machine-checked;
      (d) the naming, casing and vendor-legality rules; (e) the description token sets, with
      contacts recorded as documentation-only. Mechanism-only and publishable: synthetic SEAL
      block 70001-70099 and example.invalid addresses throughout, real values staying in the
      C30 values twin. Rationale is NOT duplicated -- it stays in the greenfield standard, which
      this page links to, so the two cannot drift into two arguments.
    notes: >
      Raised and claimed 2026-08-11 (desktop) on SME direction: "my standards are out of date".
      DIVISION OF LABOUR, recorded so the two documents do not converge into one bloated page:
      this is the NORMATIVE page a job author reads, C30's greenfield standard is the RATIONALE
      and the decision record, and the rules registry is the ENFORCEMENT. Each rule here cites
      its registry id, so what is checked and what is merely advised are visible at a glance.
      The design-doc renderer treats a mermaid fence as a code block, so the markdown source is
      the diagram's real home and the page is also published as an artifact where the diagram
      renders -- the SME publishes onward from there.
      POST-CLOSE AMENDMENT 2026-08-11 (same day, no reopen -- an SME ruling on an open item the
      page itself raised): DOMAIL is REMOVED alongside the shouts, closing section 12 item 1.
      REQ-2 had left mail "out of scope"; the ruling extends REQ-2 rather than reinterpreting it,
      so the whole ON/NOTOK block goes and R40 widens from two tags to three. The evidence that
      made it cheap, and the reason no repair option was weighed: the mail destination has at
      least TWO spellings in the estate -- %%NOTIFY from the generator and %%EMAIL_GRP on a
      deployed on-prem load job's On-Do action -- and NEITHER is declared anywhere, so every such
      block already resolves to CTMERR and mails nothing. Deleting it switches off nothing that
      worked. Touched: the guidelines page section 8 + checklist + open items, C30's greenfield
      standard 5.3.1 (rationale) and its internal twin, R40 in the rules registry, the
      _check_notifications detector plus its fixture and test, the C29 capture (amendment note
      against the verbatim REQ-2 transcription, which is NOT rewritten), and gate prompt
      email-dl-contact-point G4 -- whose DOMAIL-only option no longer exists, so the question is
      re-posed rather than left to waste the SME's time at sign-off.

  - id: G68
    epic: component-topology
    title: "Folder-set PROFILE over a staged Control-M XML export: the FID/variable/email census plus the substitution slots the XML cannot answer"
    type: task
    module: drydocs-remediation
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: todo
    depends_on: [G67]
    inputs:
      - drydocs_remediation/xml_bridge.py
      - drydocs_remediation/detect.py
      - drydocs_remediation/formats.py
      - drydocs_lineage/extractors/controlm_xml.py
      - drydocs_core/orchestration/controlm/description_tokens.py
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
    acceptance: >
      A `profile()` over a DefinitionSet (so it runs on anything xml_bridge already stages --
      NO new parser) returning ONE JSON-serializable FolderSetProfile that answers "what is in
      this folder set" for a reader who already knows the process. Four censuses, each carrying
      WHERE-USED rather than bare distinct values, because the SME's next question is always
      which jobs: (a) SHAPE -- folders, sub-folders, job counts by type (file watcher /
      placement / trust / other), and the datasets inferred from the sub-folder ladder;
      (b) IDENTITY -- distinct FID, RUN_AS, SEAL and application values with the jobs carrying
      each; (c) VARIABLES -- every declared name by scope with its distinct-value count,
      reference count, and its R30/R31 state inline, so the census and the defect list are one
      table and not two the reader has to join by hand; (d) CONTACTS -- every EMAIL_DL_* value
      and every DOMAIL destination found, split by contact kind (support tier vs Production
      Delay Notification consumer) and labelled documentation-only, since R40 now deletes the
      block that would have used them.
      Then the half the census cannot produce: a SUBSTITUTION SLOT list -- the facts the XML
      does not carry and only the SME can supply (DEVX_KEY, the MFTS set FTS_ID / REC_ID / USER
      / DELIVERY_MECHANISM, EMAIL_DL_L2 / L3 / PDN, SOURCE_CONTACT). Each slot names its current
      value or records it ABSENT, its vocabulary or shape rule from the guidelines page, and the
      jobs it would apply to. A slot with no current value is "not supplied", NEVER a default:
      inventing one is how a proposal becomes a wrong fact nobody re-checks.
      The profile ASSERTS NOTHING about meaning -- it is a census, findings stay detect_all()'s
      output and ride alongside, and nothing here ratifies or writes a graph (the module's
      standing invariant). Committed fixtures are synthetic, since real FIDs, SEALs and DLs are
      Internal. The TRANSPORT is named in the item's commit rather than defaulted, per the O58
      precedent: a CLI verb writing a JSON artifact is the recommended first pass, because
      remediation writes no graph (so QuerySpecs are out by construction) and no upload path
      exists yet -- but if an API endpoint is chosen instead, the commit body records the choice
      and its cost against ADR 0005.
    notes: >
      Raised 2026-08-11 on SME direction, as the read half of the remediation loop the SME calls
      the bulk of the project's intent: read the Control-M estate (database primary, XML
      secondary), map the flow, and the first pass is already enough to generate the Excel
      runbook -- .claude/skills/controlm-runbook-automation-excel consumes the same facts, so
      this profile is a feed for it and not a parallel one.
      WHY A PROFILE AND NOT A REPORT: the SME is assumed to KNOW the process. What they cannot
      see is what the XML actually says -- which FIDs are in play, which variable names really
      exist and at what scope, which contacts are carried. Show them that, and they can supply
      the DEVX_KEY, the MFTS routes and the contacts that get substituted in. That division --
      machine reports what IS, SME supplies what is NOT THERE -- is what keeps this out of the
      guessing that produced the drift C30 documents.
      Deliberately NOT in scope: emitting corrected XML. XmlDefinitionFormat.dump() still raises
      pending the vendor schema, and the profile plus the SME's substitutions are a PROPOSAL,
      which is what the equivalence proof already consumes.

  - id: O59
    epic: web-console
    title: "The /remediation intake surface: an SME reads the folder-set profile (FIDs, variables, emails) and supplies the substitutions the XML cannot carry"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p1
    status: todo
    depends_on: [G68]
    inputs:
      - web/src/routes/ (the existing /remediation route from O17)
      - web/src/modules/registry.ts
      - config/taxonomy/ui-components.yaml
      - config/taxonomy/ui-tests.yaml
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - internal/remediation/standards-rules-registry.md
    acceptance: >
      /remediation gains a FILE-BACKED input beside its existing flow pane: given G68's profile
      artifact for a staged folder set, an SME sees three frames and fills one form.
      PROFILE frames render G68's four censuses -- shape, identity (FIDs / run-as / SEAL),
      variables by scope with their R30/R31 state inline, contacts split by kind and labelled
      documentation-only. FINDINGS renders detect_all() output grouped by rule id with severity
      visible, each row citing the rule id so the reader can reach the registry entry; the two
      failure classes are NOT flattened into one list, because name drift produces SILENCE (no
      row loads) while a value-contract breach produces a CONFIDENTLY WRONG row, and a page that
      ranks them together teaches the reader the wrong triage order.
      SUBSTITUTIONS is a form over G68's slot list: current value or ABSENT, the shape rule
      shown beside the input (FTS_ID matches its shape, DEVX_KEY is UPPER_SNAKE and hyphen-free,
      a DL is an address), and the affected job count. Its output is a PROPOSAL the SME can
      export -- never an applied change, and nothing on this page writes XML or a graph.
      Honesty rules inherited from O56/O58 and restated because they are what makes the page
      trustworthy: a slot with no value renders "not supplied", never a default or a guess; a
      census the profile did not compute renders "not profiled", never 0.
      Access designation 'sme' with the reason recorded. The O17 deviation STANDS and is not
      quietly reversed: the remediation frames bind no QuerySpec, because drydocs_remediation
      writes zero graph and minting :Finding / :FixBatch labels in a spec would smuggle an
      ontology decision past the HITL gate. Registered in ui-components.yaml + ui-tests.yaml
      with coverage pins, both themes, npm run build and oxlint clean.
    notes: >
      Raised 2026-08-11 on SME direction ("update the backlog for the UI-WEB remediate"), as the
      SME-facing half of G68. The remediation module page has existed since O17 (2026-07-21) on
      SYNTHESIZED fixtures with no real input path; this is that input path, and it arrives as a
      FILE rather than a graph binding, which is exactly why O17's no-spec deviation survives
      rather than being an embarrassment to fix.
      THE LOOP THIS CLOSES, in the SME's own framing: remediate bad coding and accidental typos
      today, and author new Control-M jobs that populate the flow automatically on ingest --
      no manual mapping step. The profile is what makes the second half credible: if the estate's
      real variable names and scopes are visible before the standard is applied, the standard is
      being written against what exists rather than against what a screenshot suggested.
      Deliberately NOT specified: whether the profile arrives by upload or by pointing at a
      generated artifact path. That follows G68's transport choice and must not be pre-empted
      here -- an upload widget built against a transport that turns out to be a CLI artifact is
      thrown away.

  - id: O60
    epic: web-console
    title: "/lineage swimlane data-flow view: lanes Control-M | Data Layer | File Server / Database, rendering job -> pipeline -> asset per data series"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - web/src/lineage/
      - web/src/routes/LineageRoute.tsx
      - UI-WIP/wireframes/wireframes.json
      - UI-WIP/wireframes/out/dataflow.svg
      - config/taxonomy/ui-components.yaml
      - config/taxonomy/ui-tests.yaml
    acceptance: >
      /lineage gains a swimlane layout (proposed lineage/SwimlaneView.tsx; the final
      filename is recorded at build) with three lanes -- Control-M | Data Layer |
      File Server / Database -- rendering job -> pipeline -> asset per data series:
      the FW job "detected by" join, the launcher->pipeline token join per the G15
      contract, and condition edges per the BMC baseline. READS/WRITES edges render
      DASHED and visibly marked as planned for as long as m3_reads_from /
      m3_writes_to carry status: planned in the relationship vocabulary -- drawing
      them skips no gate, and the view says so ON the surface, not only in code.
      Each rendered region traces to its WF-DFL-01..17 wireframe key so SME feedback
      (the FB-2026-08-13-01 convention in UI-WIP/wireframes/wireframes.json)
      re-attaches to a component that now exists. Demo data stays SYNTHESIZED (the
      demoLineage idiom -- no real folder/job/host values). Registered in
      ui-components.yaml + ui-tests.yaml with coverage pins, both themes,
      npm run build and oxlint clean.
    notes: >
      Groomed 2026-08-17 (targeted groom -- the UI / UI-WIP inbox entries only) from
      Idea-116, captured 2026-08-13 from SME chat while testing the wireframe feedback
      loop; traces to the user's original Full Circle Docs document-portal concept,
      §7 Business Flow Diagrams (transcription is machine-local at
      internal-local/fullcircle-docs-scan-20260813.md, by design -- never committed).
      The wireframe half was DONE at capture: UI-WIP/wireframes/out/dataflow.svg,
      keys WF-DFL-01..17, renderer spec v2 (lane + arrow primitives). This item is
      the React half only. Wireframe-first is the point: SME feedback cites WF-DFL
      keys before any component exists. No ontology decision rides here -- the
      planned-status caveat in the acceptance is what keeps the drawn READS/WRITES
      edges from becoming a done deal ahead of their gate.

  - id: O61
    epic: web-console
    title: "/ownership product roll-up view: which area a job/folder supports, rolling up AreaProduct -> Product -> ProductLine -> LOB with the framework vs app-tied shapes side by side"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - web/src/ownership/
      - web/src/routes/OwnershipRoute.tsx
      - web/src/components/MiniDag.tsx
      - config/taxonomy/ui-components.yaml
      - config/taxonomy/ui-tests.yaml
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
    acceptance: >
      The ownership module gains a product roll-up flow view (mermaid-style or
      MiniDag -- the shape choice is recorded at build): folder token = PAT
      AreaProduct as the leaf, rolling up AreaProduct -> Product -> ProductLine ->
      LOB, with the TWO roll-up shapes rendered SIDE BY SIDE -- framework
      applications (no direct SEAL; the AreaProduct token is the join) vs app-tied
      applications (carry SEAL; the Control-M sub-application is the join) -- the
      folder-name grammar as the leaf annotation and data classification beneath it.
      The dotted cross-branch "aligns to platform" edge renders as a VISUALLY
      DISTINCT annotation (dashed/dotted, labelled), because no confirmed graph
      relationship backs it today -- rendering it commits nothing and skips no gate,
      and the view says so. Demo data stays SYNTHESIZED (the demoOwnership idiom --
      no real org-taxonomy values from the machine-local transcription reach the
      committed tree). Registered in ui-components.yaml + ui-tests.yaml with
      coverage pins, both themes, npm run build and oxlint clean.
    notes: >
      Groomed 2026-08-17 (targeted groom -- the UI / UI-WIP inbox entries only) from
      Idea-122: the SME supplied a rendered mermaid example (screenshot + transcription
      machine-local at internal-local/ui-examples/, real org-taxonomy values -- never
      committed; the item's committed fixture is a synthesized twin of its SHAPE).
      Ties to the PRAOCG folder grammar and the G94 standard-selection decision tree
      -- a TIE, not a dependency: the view renders the roll-up from existing taxonomy
      and does not consume G94's selector, so depends_on stays []. module drydocs-web
      on the placement test (a React view, writes nothing); /ownership chosen over a
      new module page because the registry already names it "SEAL -> PAT -> team
      rollup" -- the exact chain this view draws. If build finds the ownership tabs
      too crowded, where the view lands within the console is the builder's call to
      record, not a new module decision.

  - id: O62
    epic: web-console
    title: "The Ask report view: a file-name search resolves to the owning application and process, and the report shows code repos and dev teams for BOTH processes so support can escalate"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - web/src/ask/
      - web/src/routes/AskRoute.tsx
      - drydocs_api/query_specs.py
      - config/taxonomy/ui-components.yaml
      - config/taxonomy/ui-tests.yaml
    acceptance: >
      The Ask surface gains a REPORT rendering for the file-name search use case:
      a user searches a file name (or table) and the result renders as a structured
      report naming the business application and process associated with it -- and
      when the chain crosses processes (the captured example: a file-transfer
      process fed by an event-based application), the report shows the code repos
      and development teams for BOTH processes, because escalation is the point.
      The traversal binds via a reviewed read-only drydocs_api QuerySpec
      (shortest-path from an Application anchor filtered on a fileName property --
      the captured example spans ~22 nodes across application / product / dev-team /
      scheduler folder+job / file-transfer route / pipeline / repo labels), never
      raw Cypher composed in the browser. The report carries the source -> node-label
      legend table the captured example shows, so a reader can tell which source
      system asserted each row. Absence renders honestly: a leg the graph cannot
      supply renders as "not found via <spec>", never as an empty section that reads
      as "none exists". Committed fixtures are SYNTHESIZED twins of the captured
      example's SHAPE -- no real SEAL ids, hosts, or repo URLs from the machine-local
      transcription. Registered in ui-components.yaml + ui-tests.yaml with coverage
      pins, both themes, npm run build and oxlint clean.
    notes: >
      Groomed 2026-08-17 (targeted groom -- the UI / UI-WIP inbox entries only) from
      Idea-123: the SME supplied a real captured example (screenshot + transcription
      machine-local at internal-local/ui-examples/, real SEAL ids/hosts/repo URLs --
      never committed). This is the concrete output target for the Ask route: report
      form of the same job -> pipeline -> asset chain O60 draws as a swimlane diagram
      -- the pair is deliberate, one chain, two presentations. module drydocs-web
      with the QuerySpec half explicitly in scope the way other O-series frames bind
      specs; if the spec grows past a straightforward reviewed traversal, splitting a
      drydocs-api item out is the builder's call to record. Whether this report also
      becomes an Ask-agent tool answer (the Idea-125 named-verbs direction) is NOT
      decided here -- this item renders the report; the agent-tool question stays in
      the inbox.

  - id: C32
    epic: ontology-mapping
    title: "The downstream fan-out: capture the refine-then-split stage (Snowflake strategic, Teradata and Oracle decom) and extend the standard past the four job types it currently knows"
    type: task
    module: docs
    phase: 2
    agent: main
    model: opus
    priority: p1
    status: done
    depends_on: [C31]
    inputs:
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - knowledge/standards/technology/controlm-greenfield-job-standard.md
      - internal/controlm-config/reference/controlm-job-metadata-standards-capture.md
      - internal/remediation/standards-rules-registry.md
    acceptance: >
      Eight live screenshots of a four-folder set for ONE source of record are captured Internal
      (values twin) and the MECHANISM they teach lands in the standard, which currently stops at
      the four ingestion job types and cannot describe what happens after TRUST. Required content:
      (a) the FAN-OUT flow itself as a mermaid diagram -- one refined dataset splitting into a
      strategic Snowflake path and two legacy decommissioning paths (Teradata, Oracle), with the
      stage numbering and the close/dummy jobs that join them; (b) the JOB-TYPE vocabulary
      extended past FW/PLCT/TRUST to the suffixes actually deployed, each with its role; (c) the
      grammar correction the set forces -- TARGET PLATFORM and CHANNEL are two axes, not one,
      proved by a job carrying both, so the 5.4 slot list is wrong as written; (d) LEGACY_NAME
      registered as a description token, since every job in the set carries its decommissioned
      predecessor's name in prose and that is the decom crosswalk with no other home; (e) the
      environment indirection -- a variable whose NAME is composed from another variable, so the
      resolver cannot resolve it statically -- written up as a supported pattern with its rule,
      not filed as a defect; (f) the quantitative-resource pools recorded as an INDEPENDENT
      platform signal, since they name the target platform without parsing a job name.
      Every defect the set shows is listed with its rule id where one exists and marked NEW where
      none does. Mechanism-only in knowledge/: synthetic SOR, entity, schema and job names on the
      C31 conventions; real values stay in the Internal twin. Publish-boundary check clean.
    notes: >
      Claimed 2026-08-11 (desktop) on SME direction: "this is an example of data landing and
      splitting into AWS Snowflake, Teradata (legacy decom path), and Oracle (legacy decom path)".
      WHY THIS IS NOT MORE OF C30: C30 and C31 modeled the INGESTION half -- watcher, placement,
      trust -- from a folder that ends where the data lands. This set is what happens next, and it
      is a different shape: one refined dataset FANS OUT to three targets on two different
      lifecycles, with the strategic path and the decom paths running side by side in the same
      estate. A standard that only describes ingestion cannot express "this load is on the way
      out", which is exactly the fact a support decision needs.
      ALSO CARRIES A CORRECTION I OWE (do not quietly drop it): C30's rationale for the DOMAIL
      ruling said the mail destination is "declared nowhere" and deletion therefore costs nothing.
      TRUE of the DPL-generated folders; FALSE here -- this hand-built Ab Initio folder declares
      NOTIFY, EMAIL_GRP and EMAIL_GRP_S, all three to one address, and the block would really
      send. The SME ruling stands unchanged; its REASON has to change from "costless" to
      "deliberate", and the standard must say so rather than carry an argument the next folder
      disproves.
      DONE 2026-08-11 (desktop). Capture at internal/controlm-config/reference/controlm-refined-fanout-capture.md
      (four folders, five jobs, verbatim commands, folder variables, dependencies, resource pools,
      authorship). Mechanism landed as guidelines section 6.3 with a mermaid fan-out diagram plus
      6.3.1 lifecycle, 6.3.2 resource pools as a second platform signal, 6.3.3 the datacenter-prefix
      environment indirection; 5.4 grammar corrected to separate PLATFORM from CHANNEL; 7.4 registers
      LEGACY_NAME; the section 8 rationale and both twins corrected per D1. Twelve defects listed,
      three of them NEW classes with no rule yet (duplicate declaration in one scope, folder-name
      delimiter drift, %%VAR-vs-${VAR} carrier collision) plus stale authored provenance -- all
      inboxed rather than detector-built here, since G67's pass is closed and a rule needs the
      registry entry first. Two open items added: the LIFECYCLE token (nothing carries "this load is
      being decommissioned", which is the fact a support decision needs most) and whether a job
      number identifies the stage or the job when a fan-out puts two targets at 0080. Suite 1881
      passed / 5 skipped; publish-boundary check clean (synthetic SOR/entity/legacy prefix in
      knowledge/, real values Internal).

  - id: G69
    epic: component-topology
    title: "R41-R44: the four fan-out defect classes get their registry entries and their detectors in the SAME change, so neither can exist without the other"
    type: task
    module: drydocs-remediation
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: todo
    depends_on: [C32]
    inputs:
      - internal/remediation/standards-rules-registry.md
      - internal/controlm-config/reference/controlm-refined-fanout-capture.md
      - drydocs_remediation/detect.py
      - drydocs_remediation/overview_readme.md
      - tests/unit/test_remediation_conformance.py
    acceptance: >
      Four defect classes C32 observed with no rule to hold them are registered AND detected in
      one change. The pairing is the item: the registry is the single source for both the
      validate gate and the greenfield gate, so a detector with no entry emits findings nothing
      can rank, nothing can turn into a fix, and -- the real problem -- nothing can SIGN OFF on.
      The entry is the thing that gets ratified.
      (a) R41 DUPLICATE DECLARATION IN ONE SCOPE -- one name declared twice in the same
      container with different values. Live: RPATH twice in one folder, a relative path and an
      absolute one. Severity must-fix, not should-fix: which value wins is an ordinal accident,
      so the job runs against whichever declaration happens to be last, and the reader of the
      folder sees both and cannot tell.
      (b) R42 FOLDER-NAME DELIMITER DRIFT -- one folder set mixing separators between the same
      positional fields, so a SQL parse splitting on the delimiter returns different field
      counts per folder. Live: two folders of one set using an underscore where their siblings
      use a hyphen. This is the rule whose finding must name the PARSE consequence, not the
      cosmetic one.
      (c) R43 CARRIER COLLISION -- one name resolved by two different resolvers in one command
      line: a Control-M %%VAR beside a shell ${VAR}. THE ENTRY MUST RULE WHICH CARRIER OWNS THE
      NAME BEFORE THE DETECTOR SHIPS, because the greenfield action differs completely by answer
      -- rename one carrier, or declare the shell export -- and a detector that fires without
      that ruling produces findings no one can act on. If the ruling is not available, R43 is
      registered status OPEN with no detector and the item says so; it does NOT ship a detector
      against an undecided rule.
      (d) R44 STALE AUTHORED PROVENANCE -- an authored "created by" field that travelled with a
      copied job, so it names the template's author rather than the job's. Detectable as one
      value across jobs whose independent audit trail names different users. Advisory severity,
      and the finding must say what to read INSTEAD (the creation/modification audit fields),
      since the value is not wrong so much as answering a different question.
      All four land status PROVISIONAL and emit ratified=False, per the module rule that only
      ratified rules may change a definition. Each gets its entry in the existing registry shape
      (check / engine / severity / status / source / greenfield action), a detector in the
      conformance pass, and a case in the drifted fixture -- with the greenfield fixture still
      raising ZERO findings, which is the assertion that the standard stays reachable. Suite
      green, ruff clean, publish-boundary clean.
    notes: >
      Raised 2026-08-11 (desktop) at SME request, immediately after C32 listed the four classes
      and deliberately did not build them. THE REASON THIS IS ITS OWN ITEM RATHER THAN A WIDENING
      OF G67: G67 shipped R30-R40 as a closed set with a signed-off shape. Adding rules to it
      means either reopening a done item or quietly growing it, and the second is exactly how a
      rule set stops matching the registry that is supposed to govern it -- the failure the
      pairing rule exists to prevent, committed by the change that implements the pairing rule.
      SCOPE DISCIPLINE, stated because three of these are easy and one is not: R41, R42 and R44
      are mechanical and would ratify quickly. R43 is a genuine question about which carrier owns
      a name, and its answer changes what a fix even looks like. Do not let the three easy ones
      carry the hard one over the line -- shipping R43 as provisional-with-detector when the
      ownership question is open would put a finding in front of an SME that has no defensible
      action attached to it.
      NOT IN SCOPE: ratifying any of them. That is the HITL gate's, and it wants real findings
      from a real folder set rather than fixture hits -- which is what G68's profile run produces.
      The governance walk-through these four are the worked example of lives in
      drydocs_remediation/overview_readme.md.

  - id: G75
    epic: component-topology
    title: "Back-flow the two homeless Control-M parsing mechanisms the company adapter carries: BMC compact-timestamp normalization and the PL-/PG- condition-scope rule"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done  # 2026-08-11 (desktop): normalize_export_timestamp (audit_time.py) + condition_scope/condition_identity (conditions.py), 34 tests. TWO FINDINGS ON THE WAY: (1) the captured original DOCUMENTS returning None for an unparseable value but falls through to returning its input, so only None/empty ever produced None — the J26 promise-vs-code family; the promise is implemented here and the Oracle pass-through preserved by matching the ISO-ish shape explicitly rather than by falling through. (2) condition_identity added so "two LOCAL conditions sharing a name in different DCs are distinct" is executable rather than prose a caller must remember. The JOBISN=1 pseudo-job is DISPOSITIONED in the port-prompt divergence ledger with its trigger (the first producer loader that models folder-level conditions), not built — it needs a caller that does not exist here.
    depends_on: []
    inputs:
      - internal/controlm-config/reference/controlm-xml-processor-capture.md
      - drydocs_core/orchestration/controlm/
      - drydocs_lineage/extractors/controlm_xml.py
      - .claude/skills/reconcile-port/SKILL.md
    acceptance: >
      Two pure functions the company's controlm_xml_adapter.py carries and the producer has NO
      equivalent of (verified 2026-08-11 by search, not assumed) land in
      drydocs_core/orchestration/controlm/ as mechanism, with the reasoning that makes each one
      correct written down rather than reconstructed later.
      (a) BMC COMPACT-TIMESTAMP NORMALIZATION. A BMC XML export stamps audit times as
      YYYYMMDDHHMMSS optionally suffixed with a zone token (UTC / Z / +hhmm) -- a form Neo4j
      datetime() cannot parse. The port must preserve all four decisions, because each one is a
      failure mode someone already hit: UTC and Z both normalize to Z while a NUMERIC offset is
      kept verbatim; an 8-digit date-only value becomes midnight; a value that is NOT the compact
      BMC form is HANDED THROUGH UNTOUCHED (the Oracle projections already deliver
      "YYYY-MM-DD HH:MM:SS" and the loaders' Cypher handles the space->T itself); and anything
      else unparseable returns None so the loaders' existing null-guard drops ONE ROW rather than
      a malformed string reaching the driver and aborting the whole load. That last clause is the
      one worth a test of its own -- it is the difference between a bad row and a dead load.
      (b) CONDITION SCOPE FROM THE NAME PREFIX. PL- = LOCAL (resolves within the emitting folder's
      DATACENTER) and PG- = GLOBAL (resolves across DCs by name), anything else UNKNOWN. The
      consequence is the part that must survive: two LOCAL conditions sharing a name in different
      data centers are DISTINCT, so any key derived from a condition name is incomplete without
      the data center, and UNKNOWN must stay a returned value rather than a raised error.
      Both are pure, both get unit tests covering every branch including the pass-through and the
      None, and neither writes a graph. NO company values: the two prefixes are a documented BMC
      naming convention, not internal data, and the docstrings cite the capture rather than
      quoting a real condition or pool name.
      THE THIRD MECHANISM IS DISPOSITIONED, NOT BUILT. The company adapter's folder pseudo-job
      (JOBISN=1, so folder-level PL-<folder>-OK OUTCONDs attach to a real node instead of
      dangling) is a LOADER idiom and needs a graph-write context the producer does not have --
      there is no producer XML-to-graph adapter to put it in. It is recorded in the reconcile-port
      divergence ledger with its trigger (the first producer-side loader that models folder-level
      conditions), per J39's disposition rule. Recording it is the deliverable; building it here
      would mean inventing the caller.
    notes: >
      Raised 2026-08-11 (desktop) at SME request, from the 19-screenshot capture of the company's
      three XML processors. WHY THESE TWO AND NOT THE ADAPTER: the adapter itself is the wrong
      base and the capture says why -- it encodes the description-token model C30 retired
      (INBOUND_ROUTE / OUTBOUND_ROUTE, ENV in its transfer-instance meaning, PDN_SNOW_QUEUE), it
      flattens the C30 scope ladder into one dict per job, and its FOLDER_ORDER_METHOD filter
      skips manual-order folders, which is right for a live-graph load mirroring the Oracle
      USER_DAILY IS NOT NULL filter and WRONG for a conformance pass, where hand-built and
      inactive folders are the whole drift population. That is not theoretical: the SME's own
      22-job export carries no FOLDER_ORDER_METHOD, so the adapter would skip the folder entire
      while the lineage extractor stages all 22 jobs.
      So the adapter is scaffold, per the SME's framing -- "use it for the scaffold not the goal,
      ontology and mapping have changed" -- and these two functions are the parts that are
      mechanism rather than ontology, correct whatever era they came from, and currently reachable
      only through a module we are declining to adopt.

  - id: G76
    epic: component-topology
    title: "Reproduce the Quantitative Resource pool classifier producer-side as a mechanism/values twin: the grammar and the ordered rule table are ours, the pool vocabulary is not"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done  # 2026-08-11 (desktop): resource_pool.py, mechanism-here / vocabulary-yours — DEFAULT_RULES is () and DEFAULT_APP_CODE_RE is None, so an un-configured deployment classifies everything unknown. 31 tests, all on SYNTHETIC pool names. Rule ORDER proven both directions (same rules + same input + reversed order = different category), unknown proven first-class with no label, the never-observed business_app branch kept WITH its speculative caveat. The subsystem/kind_suffix collision on two-token names is RULED intended and documented rather than copied blind. CATEGORY_LABEL carried as PROPOSED vocabulary only — minting those labels is gate territory and nothing here writes a graph.
    depends_on: []
    inputs:
      - internal/controlm-config/reference/controlm-xml-processor-capture.md
      - drydocs_core/orchestration/controlm/folder_name.py
      - drydocs_core/orchestration/controlm/description_tokens.py
      - config/taxonomy/
    acceptance: >
      A producer drydocs_core/orchestration/controlm/resource_pool.py reproducing the company
      classifier's MECHANISM with its vocabulary supplied as DATA. A QUANTITATIVE element on a job
      declares that the job consumes N slots from a named pool, and pool names follow
      <APP_CODE>-<SUBSYSTEM>[-<MODIFIER>]*-<KIND>; the classifier parses that into a frozen
      PoolClassification (name, category, app_code, subsystem, kind_suffix, secondary_label) with
      no I/O.
      THE SPLIT IS THE ITEM. The company module compiles its vocabulary into the source -- the
      match regexes name real target-database, controller, distribution and host tokens, and the
      app-code regex hardcodes a real two-letter prefix. Those are Internal and may not land in
      the repo. The producer copy carries the grammar, the classification contract and an EMPTY
      or synthetic rule table, with the real table loaded from config (the values twin lives under
      internal/). A test proves the module classifies correctly against a synthetic table and
      returns unknown for everything when the table is empty -- which is also the honest default
      for a repo that does not have the vocabulary.
      THREE PROPERTIES ARE LOAD-BEARING AND MUST SURVIVE THE REWRITE, each with its own test:
      (a) RULE ORDER IS CORRECTNESS, NOT STYLE. Rules are first-match-wins and one of them matches
      a token ANYWHERE in the name, so it must be evaluated after the more specific ones -- a pool
      that satisfies both reads as the earlier category. A config table must therefore be an
      ORDERED SEQUENCE; a mapping keyed by category would silently lose the ordering and the loss
      would be invisible until a misclassified pool reached the graph.
      (b) unknown IS A FIRST-CLASS OUTCOME, NOT AN ERROR. It returns with secondary_label None so
      the loader adds no label and logs a WARN, which is how misses surface in CI. Same
      aliases-suggest / values-decide discipline the FACT_REGISTRY next door follows. classify()
      always returns, including on empty and whitespace input.
      (c) A SPECULATIVE CATEGORY STAYS LABELLED SPECULATIVE. The company's business_app fallback
      has never fired -- its own comment says "None observed yet, but reserve the slot so the
      contract is complete". Carry the branch WITH that honesty or drop it; keeping the branch and
      losing the comment ships a never-observed category as though it were evidence.
      One inconsistency is RULED here rather than copied blind: subsystem and kind_suffix are both
      derived from the same token split, so a two-token name yields the same token in both fields.
      Decide whether that is intended and say so in the docstring; do not reproduce it silently.
      No graph writes; the loader-side CONSUMES_FROM_POOL edge and the five secondary labels are
      NOT in scope -- the labels are an ontology decision and go through the HITL gate, not a
      back-flow. This item ships the classifier and its contract only.
    notes: >
      Raised 2026-08-11 (desktop) at SME request, from a complete 130-line capture the SME
      supplied specifically so this could be reproduced. The module has NO producer counterpart,
      and neither does controlm_quantitatives.py, which consumes it -- verified by path check.
      WHY IT IS WORTH REPRODUCING WHEN THE ADAPTER IS NOT: this one is a parser plus a lookup
      table, and the era problem that disqualifies the adapter does not apply -- pool-name grammar
      is not the description-token model and did not change at C30. It also gives a SECOND,
      INDEPENDENT platform signal: C32 established that a quantitative pool names the target
      platform separately from the job name, so the two cross-check, and a job whose name and pool
      disagree is a finding neither source could raise alone. That is the standing use for it, and
      it is why the classifier is worth having before the loader edge exists.
      The company docstring cites a company-side path for its design rationale, so the producer
      copy needs its own written warrant rather than a dangling citation.

  # ── appended at the 2026-08-11 groom run (weekly inbox groom) ──────────────
  # Sources: the eleven ungroomed 2026-08-09..08-11 inbox captures. Port/guard
  # family → J42-J47; the C25 loader follow-up → C33; the review-plan root list
  # → U20; the deployment-grain thread → K22/K23 (its register-line finding
  # merged into G70). Idea-101 merged into J43. Two questions stay OPEN in the
  # inbox for a user/SME ruling: Idea-104 (MFT route-id shape) and Idea-105
  # (the DESCRIPTION field's two claimants).
  - id: J42
    epic: release-infrastructure
    title: "Nothing checks the backlog union rule a port promises: diff the two repos' item-id sets at the port base and fail the report on a gap"
    type: task
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - PORT-MANIFEST.yaml
      - docs/restructure/backlog.yaml
      - tests/unit/test_backlog.py
      - scripts/port_preflight.py
      - .claude/skills/reconcile-port/SKILL.md
      - docs/port-prompt.md
    acceptance: >
      A PORT-TIME check -- not a unit test, because the producer tree cannot see the
      consumer's -- that reads both repos' docs/restructure/backlog.yaml at the RECORDED
      PORT BASE, diffs the items[].id sets, and FAILS the port report when
      producer-minus-consumer is non-empty, naming every missing id. An explicit
      allow-list carries ids deliberately not carried across, each with its reason, so
      that "no difference" and "a difference somebody accepted" are distinguishable in
      the output rather than both rendering as silence. It runs from the reconcile side,
      where both trees are reachable, and is named in .claude/skills/reconcile-port/SKILL.md
      and docs/port-prompt.md so a session that never opens the manifest still runs it.
      Proven to FAIL on an injected defect -- one id deleted from the consumer copy --
      before it is trusted (J26).
    notes: >
      Groomed 2026-08-11 from Idea-96. PORT-MANIFEST.yaml states the union rule
      unconditionally for docs/restructure/backlog.yaml ("Union the items; NEVER regress
      a status ... or drop an entry"), and tests/unit/test_backlog.py enforces plenty
      about the file -- schema, roll-up arithmetic, next_ready, unknown depends_on -- but
      every one of those checks looks at ONE copy in isolation. The union is a claim about
      TWO copies and nothing compares them, so a port that quietly under-delivers items
      leaves both sides internally consistent and green. Textbook J26: a rule written in
      prose and enforced by nobody. The near miss that makes it worse -- the dependency
      guard would have caught it only if a surviving item happened to depend on a missing
      one, so whether the gap is visible at all is luck rather than design. Mechanism only;
      the numbers and ids from the occurrence stay in the port report, not here.
  - id: J43
    epic: release-infrastructure
    title: "PORT-MANIFEST rows say who wins but not what must be SIGNED first: add a gate_bound precondition, and settle the derived-render disposition in the same pass"
    type: task
    module: docs
    phase: 8
    agent: main
    model: fable
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - PORT-MANIFEST.yaml
      - tests/unit/test_port_manifest.py
      - .claude/skills/reconcile-port/SKILL.md
      - drydocs_core/ontology/relationship_vocabulary/
      - config/gate-log.md
      - docs/plan/board.html
    acceptance: >
      Two gaps in the same manifest vocabulary, closed together because both are "the
      disposition set is missing a value" on one file with one reviewer.
      (a) A `gate_bound:` key on the rows whose CONTENT assumes a signed gate -- starting
      with drydocs_core/ontology/relationship_vocabulary/** -- naming the gate id, plus a
      RECONCILE-TIME check that refuses to activate an entry whose named gate is unsigned
      ON THE RECEIVING SIDE. The rule it encodes is written into the manifest in these
      words: "identical to base" and "per-entry equivalent" are BOTH insufficient tests
      for a gate-bound file, because status and id-set parity is not field-and-gate
      parity. The check is proven to fail on an injected defect before it is trusted (J26).
      (b) The DERIVED-RENDER disposition is decided across ALL the derived rows at once
      -- docs/plan/board.html, docs/plan/roadmap.html, the design-doc .html renders --
      either as a new `derived` disposition meaning REGENERATE from the reconciled tree,
      or by keeping canonical-company WITH the reason recorded in the manifest. Splitting
      one row away from the board.html precedent is explicitly refused; whichever exit is
      taken, the manifest says which and why.
      This item RECORDS preconditions and changes no gate's status: writing a gate_bound
      value signs nothing and unsigns nothing.
    notes: >
      Groomed 2026-08-11 from Idea-100, with Idea-101 MERGED IN as clause (b). The
      occurrence, recorded honestly: the company's initial vocabulary reconcile at
      PORT-REPORT-0d3761a9 ACTIVATED the G55 rua-load-shapes lineage flips, because K8
      (seal-app-ref-edge-reshape) IS signed company-side and the files looked takeable --
      rua-load-shapes is a DIFFERENT gate and is still unsigned there. They reverted all
      three vocabulary fragments, and the G23/rua code ported inert because it is
      gate-bound IN CODE and refuses planned labels, so the code's own guard caught what
      the manifest could not express. It was caught by a human re-reading their own work,
      which is not a control. Clause (b) rides here rather than as its own item: canonical
      -company reads "keep what you have" while every company note on those rows says
      REGENERATE, and keeping a stale render is as wrong as taking the producer's. The
      roadmap.yaml row had the same class of defect and was clear enough to fix outright
      (evaluate -> per-entry); these were left because splitting a single row away from the
      board.html precedent would create a worse inconsistency than the imprecision.
  - id: J44
    epic: release-infrastructure
    title: "The unclosed-fence guard stops at docs/: say where the boundary sits for captured and vendored markdown, and fix the one orphan that is ours"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - tests/unit/test_markdown_fences.py
      - tests/unit/test_runbook_currency.py
      - SDLC-Docs/extracted/issue-driven-capture-loop.md
      - internal/fcdo-reference/
      - .claude/skills/data-context-extractor/references/
    acceptance: >
      The J41 sweep scanned all 507 tracked .md files and found six unclosed fences; one
      was ours and is fixed, and tests/unit/test_markdown_fences.py guards docs/** only.
      This item disposes of the other five EXPLICITLY, one of two ways, with the choice
      recorded where the guard lives: widen the scope to all tracked markdown with a named
      carve-out list -- the FOREIGN_PATHS / HISTORICAL_PATHS idiom
      test_runbook_currency.py::test_every_path_a_document_names_exists already uses --
      each entry carrying its reason; or leave captures unguarded and write the boundary
      into the guard's docstring so the next sweep does not re-litigate it. Either way,
      internal/fcdo-reference/CONFLUENCE-TRANSCRIPT.md and TRANSCRIPT-1-ONTOLOGY.md and
      the .claude/skills/data-context-extractor/references/ material are NOT edited to
      satisfy a guard: editing somebody else's capture is a provenance decision, not a
      formatting one, and the carve-out exists to say exactly that.
      SDLC-Docs/extracted/issue-driven-capture-loop.md is a trailing orphan and is either
      fixed or carved out on its own merits, said either way. If the scope widens, the
      widened guard is proven to fail on an injected unclosed fence inside the new scope
      (J26).
    notes: >
      Groomed 2026-08-11 from Idea-103. The decision is small but it is a real one, and it
      is the interesting half of the entry: the reason those five were left alone at J41
      was not effort. Two of them are CAPTURED transcripts and one is vendored skill
      material, so "make the guard pass" and "keep the capture verbatim" are in direct
      conflict, and the repo's answer to that conflict should be written down once instead
      of re-argued at every sweep. Deliberately p3 -- nothing is broken today; what is
      missing is a stated boundary.
  - id: J45
    epic: release-infrastructure
    title: "Relay the producer-canonical DPL and Snowflake registry entries into the port prompt's standing relay list, now that the port that blocked it has merged"
    type: chore
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - docs/port-prompt.md
      - config/taxonomy/software-registry.yaml
      - config/gate-log.md
    acceptance: >
      docs/port-prompt.md's STANDING RELAYS section (J38) gains the C25 relay as a
      numbered entry beside R1-R4: the producer is canonical for the `dpl` and `snowflake`
      product rows, the `in-house` vendor (no publisher_url, guard narrowed to third-party
      vendors) and the acronym DPL = "Data Pipeline Library". It is written as a VALUE
      TRANSPLANT ACROSS FILES, never a same-file overwrite -- the standing R1 AIS lesson,
      which applies identically because the company's home for these is a different file.
      The entry states the deliberate producer-first divergence with a waiting consumer:
      the SME began the same expansion company-side on 2026-08-07 and STOPPED so the two
      copies would match. Every existing relay is RE-VERIFIED at the same touch per the
      section's own rule, and the other post-port items ride along -- the staged clean-add
      rows, the ledger roll, and striking R4 if the J34 overlay migration has landed.
      Nothing is relayed from memory; a stale relay is worse than none.
    notes: >
      Groomed 2026-08-11 from Idea-99. Deliberately NOT written into docs/port-prompt.md
      when it arose: a port was in flight against a fetched head and that file is a
      hand-merge surface, so a relay added mid-port lands in someone's conflict resolution
      instead of their checklist. FIRST ACTION IS THEREFORE A CHECK -- confirm the port has
      merged; if it has not, the item waits rather than repeating the mistake it exists to
      avoid.
  - id: J46
    epic: release-infrastructure
    title: "De-flake the run-log collision test: it races the clock instead of freezing it, so the -2 suffix it asserts only appears when both calls land in the same second"
    type: bug
    module: drydocs-core
    phase: 8
    agent: main
    model: haiku
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - tests/unit/test_loader_run_log.py
      - drydocs_core/run_log.py
    acceptance: >
      tests/unit/test_loader_run_log.py::test_naming_convention_and_collision_suffix
      passes DETERMINISTICALLY because the clock is frozen (or the timestamp source is
      injected) across the two claim_log_path() calls, not because the two calls usually
      land inside the same second. What is under test is unchanged -- the second claim of
      an existing name gets the -2 suffix, and the YYYYMMDD-HHMMSS naming assertion stays.
      Proof it was a race and not a rewrite: with the collision logic removed, the
      frozen-clock version still fails. Any sibling assertion in the same file that depends
      on wall-clock timing gets the same treatment, or an explicit note saying why it does
      not need it.
    notes: >
      Groomed 2026-08-11 from Idea-106. Observed failing once and passing on the
      immediately following identical run (2026-08-11, desktop, during the C30/G67
      close-out) -- the shape that costs an hour later, when it fails inside someone else's
      suite and reads as a real defect introduced by their change. module drydocs-core
      because claim_log_path lives in drydocs_core/run_log.py; epic J because this is suite
      hygiene, not a run-log capability change.
  - id: J47
    epic: release-infrastructure
    title: "No guard asserts a PORT-MANIFEST path still exists, and the override-ordering check only knows four hardcoded rows"
    type: bug
    module: docs
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - PORT-MANIFEST.yaml
      - tests/unit/test_port_manifest.py
      - tests/unit/test_runbook_currency.py
    acceptance: >
      Two guards in tests/unit/test_port_manifest.py, each proven to FAIL on an injected
      defect before it is trusted (J26).
      (a) EXISTENCE: every non-glob row resolves to a path that exists and every glob row
      matches at least one path, unless the row is allow-listed with a reason -- the
      FOREIGN_PATHS / HISTORICAL_PATHS idiom
      test_runbook_currency.py::test_every_path_a_document_names_exists already uses, which
      this manifest needs because it legitimately names company-only paths
      (drydocs/docmeta/**, drydocs/scrapers/**) and globs that match nothing producer-side.
      (b) ORDERING, DERIVED rather than listed: any row whose path is a strict prefix-match
      of a LATER glob row is an ordering defect, replacing
      test_overrides_precede_their_broader_glob's hardcoded list of four overrides against
      config/**.
    notes: >
      Groomed 2026-08-11 from Idea-107 -- captured 2026-08-11 under the id Idea-86, which
      a 2026-08-07 [source] entry already held, and RENUMBERED at this groom (the older
      entry keeps the id; commit d05811a's message is the only surface carrying the
      short-lived spelling). That is the second two-session id collision in IDEAS.md after
      the duplicate Idea-101 J41 records. FOUND AT G75, not by a test: the row
      drydocs_core/controlm/** had pointed at a path that has not existed since the S2 /
      ADR 0008 relocate under orchestration/, so every module in the Control-M package was
      silently falling through to the generic drydocs_core/** evaluate-on-collision row
      instead of the canonical-producer row it was written for. Nothing failed, because
      test_port_manifest.py checks uniqueness, dispositions, notes and pins -- never
      existence. The ordering half was verified the same way: at G75 the order had to be
      fixed by hand. Sibling of J43, which adds a precondition key to the same file;
      separable because this is a producer-side unit-test guard while J43 changes the
      manifest vocabulary and the reconcile behaviour.

  - id: J48
    epic: release-infrastructure
    title: "Finish the worktree render fix: rule each of the seventeen remaining repo-root anchors as repo-content or package-internal, and guard the classification"
    type: chore
    module: drydocs-core
    phase: 8
    agent: main
    model: sonnet
    priority: p2
    status: done          # done 2026-08-12 (desktop), same session as the claim
    depends_on: []
    inputs:
      - drydocs_core/repo_paths.py
      - tests/unit/test_repo_paths.py
      - drydocs/gate_pages.py
      - drydocs/graph_verify.py
      - drydocs/review_labels.py
      - drydocs/source_mappings.py
      - drydocs/seal_samples.py
      - drydocs/port_preflight.py
      - drydocs/loaders/batch_port_orchestrator.py
      - drydocs/loaders/bmc_docs.py
      - drydocs/loaders/doc_traceability.py
      - drydocs/loaders/essential_graphrag.py
      - drydocs_core/precedence.py
      - drydocs_core/source_registry.py
      - drydocs_core/manual_mappings.py
      - drydocs_core/mapping_store.py
      - drydocs_core/orchestration/crosswalk.py
      - drydocs_core/orchestration/shell.py
      - drydocs_core/ontology/schema_graph.py
      - scripts/external_vendor_scrape.py
    acceptance: >
      Every module listed in inputs that anchors a default path on a raw
      Path(__file__) repo root carries an EXPLICIT one-line classification, and the
      classification is enforced rather than asserted.
      (a) RULED: each site is either repo-CONTENT (defaults resolve docs/, config/,
      knowledge/, internal/ or another tracked tree) -- in which case it routes through
      drydocs_core.repo_paths.repo_root(<its existing __file__ anchor>), passing that
      anchor as the required fallback -- or package-INTERNAL (drydocs_core/schema/*.cypher,
      the relationship_vocabulary fragments, and anything else that genuinely travels with
      the installed package), in which case the __file__ anchor STAYS and a one-line comment
      at the site says why. Skipping a site is not a disposition; "correct as written" is
      recorded as the package-internal ruling.
      (b) PROVEN: for every site ruled repo-content, a test drives a REAL git worktree the
      way tests/unit/test_repo_paths.py already does for the board render and asserts the
      resolved default lands inside the worktree, with the main tree byte-identical
      afterwards. Each new guard is shown to FAIL without its fix before it is trusted (J26).
      (c) COVERAGE, derived not listed (the S10 precedent): a test re-derives the set of
      repo-root anchor sites from the source tree rather than trusting an enumeration, so a
      NEW module that anchors a repo-content default on a bare Path(__file__) reds that test
      instead of passing by omission.
      (d) The rule itself is stated once, in drydocs_core/repo_paths.py's docstring, and
      CLAUDE.md's session-end ritual is unchanged -- this item adds no verb and no flag.
      Full suite green.
    notes: >
      Groomed 2026-08-12 (pm) from Idea-109, whose FIX half already landed the same day at
      841dc6e5 without a backlog item: drydocs_core/repo_paths.py plus adoption in
      plan_board / plan_ideas / plan_roadmap and 13 tests, which closed the reported symptom
      (a worktree-isolated agent running the session-end render ritual wrote board.html,
      ideas.html and roadmap.html into the MAIN tree). This item is the residue that fix
      deliberately did not sweep, and it is a SWEEP WITH A JUDGEMENT, not a mechanical
      replace -- the entry is explicit that the ~17 remaining sites are NOT all bugs.
      WHY IT IS WORTH FINISHING RATHER THAN LEAVING: the failure is silent in both
      directions. The original damage was a TORN render -- the five sibling scripts
      render_board.py invokes anchor on their own __file__ and were always correct, so half
      the output landed in the worktree and half in main, which is why nobody noticed for
      a day across two independent agent runs. Any un-ruled site reproduces exactly that
      shape the first time an agent runs in a worktree.
      MODULE CHOICE (recorded per the groom rule): drydocs-core, because the convention,
      its single implementation and its guard all live in drydocs_core/, even though the
      sites themselves span drydocs/, drydocs_core/ and scripts/. It is not a boundary
      change -- MODULE_MAP.md's core/component invariant is untouched, since callers import
      core, which is the existing direction.
      LIKELY DISPOSITIONS, offered so the sweep is not re-derived from scratch and NOT
      binding: the four drydocs/loaders/* roots and gate_pages / graph_verify /
      review_labels / source_mappings / seal_samples / port_preflight read repo content and
      probably adopt; drydocs_core/ontology/schema_graph.py's DEFAULT_VOCAB_PATH and
      DEFAULT_OUTPUT_PATH both name package-internal resources and probably keep;
      scripts/external_vendor_scrape.py is a script that already resolves out of the
      caller's own tree, which is the correct anchor. precedence / source_registry /
      manual_mappings / mapping_store are the genuinely mixed cases -- config/ is repo
      content, but mapping.db is runtime state -- so rule them one at a time.
      No graph write, no ontology decision, no gate: this is path resolution only.

      DONE 2026-08-12 (desktop). 27 sites judged: 24 modules newly route through
      repo_root() (27 call sites in the tree, 3 of them the pre-existing plan_* fix), and
      three RULED and left as-is, recorded at the site rather than skipped:
      ontology/schema_graph.py (vocabulary fragments + generated .cypher are package
      resources), scripts/external_vendor_scrape.py (not an installed package, so __file__
      already names the caller's tree -- and it CANNOT adopt: those two lines put the root
      on sys.path before drydocs_core is importable), and drydocs_core/config.py, the one
      site where following the caller would be a REGRESSION -- .env is untracked
      machine-local credentials that a worktree never receives, so a worktree run would
      find no .env at all. The mixed case the notes flagged came out repo-content:
      var/mapping.db is derived FROM the committed YAML/CSV beside it, so a worktree
      reading its own config/ and writing main's var/ is the torn split this item exists
      to prevent; gitignored is not the same as shared.
      THE INPUTS LIST WAS SHORT BY FOUR, all the same defect in the same editable install:
      drydocs_docmeta/registry.py, drydocs_docmeta/policy.py, drydocs_api/intake.py, and a
      _repo_relative() helper INSIDE a function body in drydocs_api/mappings.py that no
      file-level read would surface -- clause (c)'s derived guard found it after every
      listed file had already been read, which is the argument for (c) in one incident.
      (b) is tests/unit/test_repo_paths.py::test_swept_defaults_resolve_inside_a_real_worktree:
      a real git worktree, nine constants across four packages, plus a CONTROL --
      cli.DEFAULT_SAMPLES_DIR names the same directory as seal_samples.DEFAULT_SAMPLES_DIR
      and must NOT move (a runtime read default must still answer from a wheel; the other is
      a build script's write target). So a blanket search-and-replace FAILS this test, which
      makes it a check on the judgement and not just on the edit. Both directions proven by
      injection (J26): reverting precedence.py fails it, over-sweeping cli.py fails it.
      ONE MECHANISM FINDING worth carrying: the first draft of that test ran the probe with
      `python -c`, which puts the CWD on sys.path -- so the worktree's own drydocs/ shadowed
      the editable install and EVERY import came back worktree-relative, control included.
      The test now runs a probe FILE outside the worktree, reproducing the incident's real
      condition (sys.path[0] is the script's directory, never the cwd).
      (c) test_no_module_anchors_repo_content_on_dunder_file parses every .py in all seven
      installed packages and fails any Path(__file__) chain landing on the repo root without
      repo_root(). Landing at the root IS the signal, so it needs no allow-list of the ~40
      legitimate package-internal anchors. Ledger grew 7 -> 40 constants. MODULE_MAP.md
      gained the repo_paths.py row it never had. Suite 2092 passed / 8 skipped.

  - id: C33
    epic: ontology-mapping
    title: "Build the adhoc software-version loader the software-version-context gate authorized and deliberately did not build"
    type: task
    module: drydocs-load
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - config/gate-prompts/software-version-context.yaml
      - config/gate-log.md
      - config/taxonomy/software-registry.yaml
      - config/doc-source-registry.yaml
      - config/manual-loads/manifest.yaml
      - drydocs_core/ontology/relationship_vocabulary/44-local-registry.yaml
    acceptance: >
      The gate signed the SHAPE and nothing else: reg_appuser_uses_software is registered
      status: planned, no loader exists, and the adhoc-sme-email corpus stays
      confirmed: false for that reason alone. This item builds exactly what sections A-E
      signed -- the loader itself (MERGE key per section Q3 below, edge properties per
      B3, as_of from the email's sent date), the :Document minted from the hand-recorded
      citation, the evidence: block's as_of filled in on the abinitio product row,
      registration in config/manual-loads/manifest.yaml per E4, and the C1 install-path
      pattern rows in the invocation_patterns shape.
      SECTION Q3 IS SETTLED BEFORE THE MERGE KEY IS WRITTEN, not after. The gate deferred
      it with the consequence stated: if the estate re-points installs by symlink,
      install_path is a poor key and identity moves to (fid, version), which is a RE-KEY
      rather than an edit. If Q3 cannot be answered, the item STOPS at that question and
      says so -- a guessed identity key is the failure the identity gate's D2/C3 exist to
      prevent.
      Two things this build must NOT do: write the section F application-level rollup
      (blocked on K17, and not behind a flag), and auto-append observed versions to the
      curated versions: list (C2). The vocabulary entry flips planned -> active only if
      its gate says so; nothing here signs anything.
    notes: >
      Groomed 2026-08-11 from Idea-98, the first of the follow-ups C25's close note carries.
      model fable because the deliverable lands a MERGE KEY -- the identity decision the
      gate deferred -- while the code around it is ordinary loader work. The observable
      close signal is the corpus flipping confirmed: false -> true on adhoc-sme-email,
      which is exactly why it must not flip on a guessed key.
  - id: U20
    epic: self-documentation
    title: "The code-graph review plan still says six scan roots: restate its doc-coverage unit on eight package roots, or derive the list the way U18 derived the metric scope"
    type: bug
    module: docs
    phase: 16
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - docs/reviews/code-graph-review-plan.md
      - tests/unit/test_code_graph_review_plan.py
      - pyproject.toml
      - .claude/skills/tech-debt/SKILL.md
    acceptance: >
      Phase 3 unit 3 of docs/reviews/code-graph-review-plan.md no longer reads "Six scan
      roots x DesignDoc coverage" with per-root counts that predate BOTH drydocs_api and
      drydocs_docmeta (tests 85, drydocs 41, drydocs_core 35, lineage 12, remediation 7,
      deepdoc 3). It is restated on the EIGHT package roots U18 established, the per-root
      doc coverage is RE-MEASURED rather than re-typed (venue named per J18), and every
      other "six scan roots" phrase in the same document is swept with it -- the Phase 1
      mandate is a second instance, and the U9 scope note already says the snapshot is the
      whole repo tree. The item also DECIDES, and records, whether a per-root count belongs
      in prose at all: if it stays, it comes under tests/unit/test_code_graph_review_plan.py
      the way U18 anchored the typed scope on pyproject's packages; if it goes, the
      document says where the number is derived instead. A third hand-typed root list in
      one document is the argument for deriving, and this item is where that argument is
      settled rather than restated.
    notes: >
      Groomed 2026-08-11 from Idea-97, which U18's own close note inboxed rather than swept
      in -- same disease, one table over. U18 widened the A1-A6 metric scope to eight roots
      and guarded the typed list against pyproject.toml, but that guard is anchored on the
      $packages literal while this unit hard-codes its own root list in prose, so it sat
      outside both the guard's reach and U18's stated surface. Left deliberately, filed
      honestly, and cheap to close.
  - id: K22
    epic: seal-attribution
    title: "Capture the Deployment Module CI class -- the identifier the source carries and we discard -- without moving attribution off the application"
    type: task
    module: ontology
    phase: 9
    agent: ontology-mapper
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - knowledge/upgrade-plans/servicenow-replica-evidence.md
      - docs/restructure/03-hitl-sme-flow.md
      - docs/RELATIONSHIP_GUIDE.md
      - drydocs_core/ontology/relationship_vocabulary/
      - config/taxonomy-ontology-map/
      - config/gate-prompts/
    acceptance: >
      A PROPOSAL registered status: planned and routed VIA THE GATE as a rider on an
      existing SEAL/CI gate -- not a gate of its own, because nothing here changes an
      attribution subject. Nothing is loaded, no vocabulary entry is flipped active, and if
      the gate does not sit, the drafted rider IS the deliverable. It carries five things,
      in this order.
      (1) WHAT IS ALREADY RULED AND IS NOT REOPENED: one application, many deployments is
      correct (SME-confirmed), and everything DryDocs maps is off the APPLICATION -- so
      :BusinessApplication stays the attribution subject and seal-tom-attribution-reshape's
      subject does NOT move. This item captures an identifier the source carries and we
      discard; it does not re-home attribution.
      (2) THE KEY, which is why the item exists at all: each Deployment Module CI carries
      its OWN unique CI id, so the CI id is the business key and app_id(seal_id):deployment_id
      is a HUMAN-READABLE NAME. A loader keying on a bare deployment_id MERGEs distinct
      deployments together -- the identity gate's D2/C3 failure on a new axis -- so the
      proposal states the key explicitly and refuses the composite-name-as-key reading.
      (3) THE CI TOPOLOGY as the SME gave it: Business Application [Instantiates] Deployment
      Module, inverse [Instance of], and above it [Contained by] area product. "Deployment"
      and "module" are ONE thing.
      (4) THE LABEL: adopt the CONCEPT and pick our own stable name -- C10's standing advice,
      proven twice on this instance, since the vendor's own class label moved (Application
      Service -> Service Instance at Yokohama) and this instance's inverse label
      ([Instance of]) already differs from the one the public material uses
      ([Instantiated by]).
      (5) THE CORRECTION IT MUST CARRY rather than quietly drop: the module reference being a
      form default applies to TRANSACTIONAL records -- a Change, an Incident or a KB article
      must name a deployment module and people accept the default. The CI itself is real,
      with its own id, its own place in the chain and KB articles attached. So the grain is
      sound, and only the COUNTING OF TRANSACTIONS PER MODULE is not.
    notes: >
      Groomed 2026-08-11 from Idea-102; the same entry's register-line finding was MERGED
      into G70 in this pass, and its KB-article thread became K23. The evidence and open
      questions 8 and 9 live in knowledge/upgrade-plans/servicenow-replica-evidence.md
      (K21). WHY THE ITEM IS SMALL: the expensive reading was the plausible one. K21 found
      u_seal_deployment_id sitting beside u_seal_application_id on the CSDM Application
      Service row and never on cmdb_ci_business_app, which is exactly the shape of this
      repo's past grain corrections (K1/K2, and the 2026-07-22 move of SEAL attribution from
      job level to the folder->batch :Port) -- and it read that way for about an hour. The
      SME's caveat is what shrank it, and the caveat is the more valuable half. The earlier
      reading of section G15 -- that it needed no grain -- would have discarded a real CI
      class on the strength of a defaulted foreign key, which is why (5) is written into the
      acceptance rather than left in a note.
  - id: K23
    epic: seal-attribution
    title: "Are ServiceNow KB articles linked at Deployment Module grain ASSERTED or DEFAULTED? -- the check that decides whether the kb_* family is a real pull candidate"
    type: task
    module: taxonomy
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - knowledge/upgrade-plans/servicenow-replica-evidence.md
      - config/source-registry.yaml
      - reference/platforms/
    acceptance: >
      A recorded verdict in knowledge/upgrade-plans/servicenow-replica-evidence.md, where
      open questions 8 and 9 already sit, answering ONE question with evidence rather than
      plausibility: is the KB-article-to-Deployment-Module link ASSERTED by an author, or
      DEFAULTED by the form the way transactional records default their module? The check
      comes FIRST because the defect it looks for would hit KB articles identically to
      Changes and Incidents, and a graph built on a defaulted foreign key attaches
      documented fixes to the wrong deployment while looking perfectly populated.
      On the strength of that verdict, a RECOMMENDATION -- promote the kb_* family from
      ring 3 to a pull candidate, or leave it in ring 3 with the reason -- routed to the
      gate that rules the pull scope, never enacted here. NOTHING ACTIVATED: no source row
      written to config/source-registry.yaml, no loader, no edge. Any edge the
      recommendation implies is registered status: planned and goes through
      docs/RELATIONSHIP_GUIDE.md and the HITL gate.
    notes: >
      Groomed 2026-08-11 from Idea-102's separate thread. The SME called KB articles at
      deployment grain "more meaningful", and a documented fix attached to the deployment
      that has the incident is squarely what a production-support knowledge graph is for --
      which is why it earns an item rather than a line inside K22. Deliberately an
      ASSESSMENT and not an ingestion: ring 3 is where the family sits today, and the value
      of the item is that it is allowed to come back "leave it there" with evidence.

  - id: K24
    epic: seal-attribution
    title: "Two different questions are both numbered Q6 on the FID gate page, and four other files cite 'Q6' as though it were one question"
    type: bug
    module: config
    phase: 9
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - config/gate-prompts/fid-identity-and-scope.yaml
      - tests/unit/test_gates_json.py
      - config/source-mappings/psgmgr.yaml
      - config/source-registry.yaml
      - docs/k16-fid-census-company-prompt.md
    acceptance: >
      REPRODUCED AT THE 2026-08-12 GROOM, no database and no company data (J18): a scan of every
      file in config/gate-prompts/ parsing the leading Qn of each open_questions entry reports
      exactly one collision in the whole tree -- fid-identity-and-scope.yaml, 9 questions, id Q6
      used TWICE. One Q6 is the SME answer landed 2026-08-12 (CM_DEF_VJOB.OWNER holds the NAME,
      the join normalizes the directory side only); the other is an older, still-open question
      about whether the directory exposes an application-level roll-up. The gate this page belongs
      to is K17, which is the next thing walked on this thread, so the ambiguity is about to be
      written into a sign-off.
      (a) RENUMBER THE UNANSWERED ENTRY ONLY -- the roll-up question becomes the next free id
      (Q8; Q7 was raised the same day) and the ANSWERED Q6 keeps its number, because four other
      files already cite "Q6" by number for the answer and not for the roll-up question:
      config/source-mappings/psgmgr.yaml (the OWNER column note), config/source-registry.yaml
      (the psgmgr dataset note), docs/k16-fid-census-company-prompt.md, and backlog item K16's
      own "RUN Q6 FIRST" line. Verify each of those four still reads correctly after the edit;
      any citation found pointing at the roll-up question is re-pointed in the same commit.
      (b) A GUARD so this cannot recur silently: a test in tests/unit/test_gates_json.py (which
      already iterates config/gate-prompts/*.yaml) fails when any gate page reuses a question id
      inside open_questions, naming the file and the duplicated id. Guard added FIRST, watched to
      fail on the current tree, then the renumber turns it green -- the J26 shape.
      DECIDES NOTHING THE GATE OWNS: no question is answered, closed, or reworded, no edge
      semantics are touched, and no answer status changes. This is identifier hygiene on a page
      whose ids are quoted from four other files.
    notes: >
      Raised at the 2026-08-12 groom, not from the inbox -- found while re-checking the
      fid-identity-and-scope page after the same-day --run-as bind fix (887a0e7), which answered
      Q6 and raised Q7 in the same edit; the older Q6 was simply never renumbered when the new
      one was appended. Left DEPENDENCY-FREE rather than wired ahead of K17: the renumber touches
      only the entry nobody cites, so it is safe in either order, and blocking a p1 gate on a p2
      identifier fix would cost more than the ambiguity. SEQUENCING PREFERENCE, not a dependency:
      land this BEFORE the K17 session, so the sign-off record cites an id that means one thing.

  - id: G81
    epic: component-topology
    title: "DATA LOSS: every path the system reads or writes must be DECLARED in configuration with a mode, and no write path may contain a read path"
    type: bug
    module: drydocs-core
    phase: 6
    agent: main
    model: opus
    priority: p0
    status: todo
    depends_on: []
    inputs:
      - drydocs_core/data_root.py
      - config/source-registry.yaml
      - config/dev-environment.yaml
      - docs/decisions/0012-data-named-load-surface.md
      - docs/reviews/persona-python-architect-2026-08.md
    acceptance: >
      INCIDENT, 2026-08-11: the SME's internal folder holding extracted CSV source data was
      OVERWRITTEN. Recovery is from a backup, so the data is not lost, but the class is live and
      it fires again the moment the backup is restored to the same place. This is the most
      severe defect class the repo has produced — every other failure this week was a wrong
      READ; this one destroyed a source.
      (a) RECONSTRUCT FIRST, do not guess. Establish what wrote over what, from the code, and
      record it. The write-capable surface to walk: drydocs_core/data_root.py declares ten-plus
      path helpers IN PYTHON, several taking create=True so they mkdir, and rua_extracted_dir()
      names an "extracted" area that a re-run may legitimately rebuild. Reconstruction is part
      of the deliverable, not a preamble to it — a guard written against a guessed mechanism
      guards the wrong thing.
      (b) DECLARE EVERY PATH IN CONFIGURATION, not in code, each carrying a MODE:
      read (source data the system may NEVER write), write (outputs it owns and may rebuild),
      scratch (disposable). Three families the SME named, all currently scattered: LOGS
      (DRYDOCS_LOGDIR, env-only), LOADER INPUT DATA (per source), and REMEDIATION OUTPUT (the
      greenfield artifact and Jira handoff). Moving the declaration out of Python is the point:
      today a path is whatever a helper returns, so no reader can enumerate what the system
      touches, which is why an overlap was invisible until it destroyed something.
      (c) THE INVARIANT THAT WOULD HAVE PREVENTED IT: no write-mode path may EQUAL OR CONTAIN a
      read-mode path. Guarded, with the failure naming both paths. This is the whole defect in
      one sentence and it is mechanically checkable.
      (d) NO SILENT DEFAULT ROOT. resolve_data_root() falls back to ~/data/DryDocs whenever
      DRYDOCS_DATA_ROOT is unset or empty, so a missing environment variable silently relocates
      every read and write to a plausible-looking wrong place — the SAME defect family as G78's
      fixture-directory default, one layer down. An unset root must FAIL, naming the variable.
      (e) Any path a create=True helper may build is write-mode BY CONSTRUCTION. A read-mode
      path must be reachable through a helper that cannot create, cannot clean and cannot write.
      Ties to ADR 0012: a source declaring acquisition file-drop needs a declared location, and
      that location is read-mode by definition — so (b) supplies what the ADR's acquisition axis
      assumes exists.
    notes: >
      Raised 2026-08-11 (desktop) from an SME incident report. p0 on IRREVERSIBILITY, which is
      the axis that separates it from G78: a wrong load is re-runnable, a destroyed source
      directory is recoverable only as far as the last backup, and if that backup had been
      stale or absent the loss would be permanent. G78 is louder; this one is worse.
      SME DIRECTION ON SEQUENCING: the layout must be planned with the ARCHITECT PERSONA, and
      it must be planned WITH the loader restructure rather than after it — G79 moves loaders
      between chains and ADR 0012 puts acquisition on the source entry, so both touch exactly
      the paths this item declares. Doing the declaration afterwards means declaring a layout
      that was just rearranged. The persona review is docs/reviews/persona-python-architect —
      a layout question, not a graph-modelling one, so the Python architect rather than the
      Neo4j architect.
      SCOPE FENCE: this declares paths and guards the overlap. It does not relocate anyone's
      data — a config change that silently moved a root would be the same class of defect the
      item exists to close.

  - id: G78
    epic: component-topology
    title: "A load step that cannot find its input MUST FAIL — today it silently skips, and a default run loads synthetic fixtures while reporting success"
    type: bug
    module: drydocs-load
    phase: 6
    agent: main
    model: opus
    priority: p0
    status: todo
    depends_on: []
    inputs:
      - drydocs/cli.py
      - drydocs/loaders/base.py
      - config/source-registry.yaml
      - tests/unit/test_load_sequence_surfaces.py
    acceptance: >
      TWO DEFECTS, one failure. (1) A chain step whose expected input file is absent logs
      "No sample for ..., skipping" and the run reports SUCCESS. (2) refresh-reference's only
      input option is --samples-dir, which DEFAULTS to the bundled fixture directory, so a
      run with no arguments loads synthetic rows into a real graph and calls it done.
      Together they produce the worst outcome available: not a failed load, a load that
      SUCCEEDED with the wrong data -- the confidently-wrong-row class promoted to the load.
      REPRODUCED ON BOTH SIDES, not theorised. Producer: REFRESH_REFERENCE_CHAIN expects
      seal_application_data__sample.csv and seal_contact_data__sample.csv and NEITHER EXISTS
      ANYWHERE IN THIS REPO (find returns nothing), so the SEAL half of the reference load has
      never run on bundled fixtures and nothing has ever said so. Company-side: the same skip
      dropped dev_teams, and a real-data run silently loaded fixtures instead (SME session
      2026-08-11).
      FIX, in three parts:
      (a) A missing REQUIRED input FAILS the step, naming the file and the path searched. A
      step may declare an input optional; optional then means written down, not inferred from
      a file's absence.
      (b) Real runs stop defaulting to the fixture directory. Production data must not have to
      be named *_sample.csv in a directory called samples to be loadable -- that is a semantic
      contradiction the operator is currently forced into. Implement the interface the TDDs
      ALREADY DESCRIBE and neither side built: --source <source-id> resolving its location from
      config/source-registry.yaml, with --samples-dir retained for fixtures only.
      THE CORRECT BEHAVIOUR IS ALREADY IN THE REPO, ONE VERB AWAY, and this clause is closer to
      making two commands agree than to designing an interface (SME-confirmed 2026-08-11:
      "--csv works fine changing to a different path"). `drydocs load <name> --csv <path>` takes
      an ARBITRARY path -- no directory convention, no filename convention, no fixture default --
      and when neither --csv nor --sql is given it PRINTS AND EXITS 2 rather than guessing. It
      also runs the confirmed-gate before any write. So the defect is not "loaders cannot take a
      path"; it is that the SEQUENCED chain verb behaves worse than the manual verb beside it.
      Copy the single-loader contract up into the chain: explicit path or fail, never a
      plausible default. Do NOT rebuild what `load` already does correctly.
      (c) The run summary states, per step, WHICH path it read and how many rows it loaded, so
      "it succeeded" is falsifiable at a glance.
      (d) COVERAGE RIDER, merged from Idea-119 at the 2026-08-13 groom and scoped to this item's
      own blast radius: the loaders whose step contract this change touches gain a DIRECT test
      import. FIVE have none today -- drydocs/loaders/business_segments.py, controlm.py,
      controlm_dependencies_derived.py, controlm_hosts.py and seal_contacts.py (A5 measured
      2026-08-13 at bb9788b6: 31 package modules with no direct test import, of which these five
      are loaders). A test that imports the loader and exercises the missing-input path satisfies
      this clause and the guard below at once. NOT a general coverage sweep: five named files,
      because the tests belong with the fix that touches them.
      Guard: a test proves a chain step with a missing required input FAILS BY NAME, and that a
      run against the fixture directory is distinguishable in the summary from a run against a
      declared source.
    notes: >
      Raised 2026-08-11 (desktop) from a company-side diagnosis the SME relayed, then reproduced
      producer-side. p0 because it is silent, it is live on both machines, and every downstream
      claim about what the graph contains rests on it. Independent of the naming epic -- this is
      correctness, that is generalization; do not couple them.
      THERE IS A WORKING ESCAPE HATCH TODAY, and an operator blocked on this should be told
      rather than left waiting for the fix: `drydocs load <name> --csv <arbitrary path>` loads a
      single loader from any location, gated, with no fixture fallback. It is per-loader rather
      than per-chain, so a full reference load is several invocations, but it is CORRECT where
      the chain verb is not. That also bounds the item: the contract to copy already exists and
      is one verb away, so this is two commands being made to agree, not an interface to design.
      MERGED 2026-08-13 (targeted groom) from Idea-119, whose own proposed disposition was to
      fold the five untested loaders into this item rather than file a coverage sweep. Read A5
      for what it is: a DIRECT-IMPORT proxy, so fixture-driven and subprocess coverage do not
      show in it. Clause (d) therefore asks for an import-bearing test on the path this item
      changes, not for proof that a loader was untested in every sense.
      STANDING DIVERGENCE FOUND WHILE CONFIRMING IT, and it is load-bearing for any shared
      fixture: the producer convention is DOUBLE underscore (catalog_lobs__sample.csv), the
      company convention is SINGLE (catalog_lobs_sample.csv) -- confirmed by the SME. The same
      file therefore cannot serve both sides, and a file copied across is silently skipped
      rather than rejected. Record it in the port-prompt divergence ledger at this item.

  - id: G79
    epic: component-topology
    title: "Split refresh-reference by SOURCE — one command per subject, and wire the two loaders that belong to no chain at all"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: todo
    depends_on: [G78]
    inputs:
      - drydocs/cli.py
      - config/source-registry.yaml
      - config/taxonomy/lob-product-team.yaml
      - scripts/render_load_map.py
    acceptance: >
      refresh-reference bundles SEVEN loaders across THREE unrelated sources with different
      natural refresh cadences. That bundle has no organising principle, which is why a loader
      can fall out of it and nothing notices. Split it so each command covers ONE source.
      (a) The split, by subject rather than by chain accident: the product catalog tier
      (catalog LOBs, product lines, products), the organisation tier (dev teams, team roles),
      and the business-application tier (applications, contacts). These are SEPARATE COMMANDS
      even where two share a source system, because the SME's cadence argument is per subject:
      business applications are not created daily, the operational estate can be.
      (b) WIRE THE TWO ORPHANS. pat_team_roles is gate-confirmed (C9, 2026-07-18), has a loader
      class and cypher, is reachable as `drydocs load pat_team_roles` -- and belongs to NO chain
      and NO profile, so no operator path has ever run it. area_products is in the same state.
      Either wire each into its subject command or record a written reason it is excluded; the
      one thing not allowed is the current silence. area_products has a plausible reason ready
      to be written down (lob-product-team.yaml records area_products: 0 with the standing
      area-product-missing open question); pat_team_roles has none that this item could find.
      (c) Cadence moves OFF the hand-assigned profile tuples and ONTO the source registry entry,
      so load_profile() DERIVES its steps by filtering on cadence instead of reading a literal
      maintained by hand. This is what makes "the source dictates how often it refreshes" true
      in code rather than true in a comment.
      (d) Acquisition mode joins it on the registry entry (file-drop | db-pull | api-pull |
      repo | scrape), so the anticipated CSV-to-database move for business-application data is a
      registry edit plus an adapter swap, with the loader, the command name, the sequence
      position and the cadence all unchanged.
      (e) PRESERVE ONE LOAD-ORDER INVARIANT ACROSS THE SPLIT, and state it where the split
      happens: seal_applications loads BEFORE any loader that can mint a
      :BusinessApplication. Producer satisfies it today by accident of chain order rather
      than by declaration, and the split is exactly when an accident gets lost. Producer is
      additionally safe because pat_product_mapping.cypher MERGEs on the SAME neutral app_id
      key seal_applications uses -- so no stub, no second node, no ordering dependency. The
      company is NOT safe: pat_app_links.cypher still MERGEs on the pre-S3 seal_id with
      is_stub=true, mints a null-app_id node the uniqueness constraint cannot match, and the
      subsequent SET collides (SME hit it live 2026-08-11; RELAY-8, a T23 residue). Once
      commands are keyed to sources, the constraint is expressible as a declared dependency
      rather than a position in a tuple -- state it that way, so the next reorder cannot
      silently break it.
      NAMING IS DELIBERATELY OUT OF SCOPE -- this item splits and wires using CURRENT names.
      Renaming is GN1/GN2 and is frozen-and-ported as one unit; doing both at once makes an
      already wide port surface unreviewable.
    notes: >
      Raised 2026-08-11 (desktop) on SME direction. Depends on G78 because splitting a chain
      that silently skips its inputs just gives you more places to skip silently.
      The SME asked whether product and org structure can be separate commands while sharing
      sources -- yes, and that is the point: the COMMAND is keyed to the subject, the SOURCE is
      a property of the data, and two subjects may legitimately draw on one source system. What
      must not happen is the reverse, which is what exists now: one command spanning three
      sources, so nothing can say whether the command is complete.
      RIDER NOTE 2026-08-13 (targeted groom, from Idea-119): two of the five loaders named in
      G78 clause (d) are ones THIS split re-homes -- seal_contacts (a REFRESH_REFERENCE_CHAIN
      member) and business_segments (refreshed inside the same command, ahead of the chain
      tuple). If G78's coverage clause has not closed by the time this runs, the tests move WITH
      the loaders into their new subject command rather than being left pointing at a command
      that no longer exists.

  - id: G80
    epic: component-topology
    title: "The unchained-loader guard: a loader registered but in no chain and no profile fails the suite, and the load map renders it as a defect"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - drydocs/cli.py
      - scripts/render_load_map.py
      - tests/unit/test_supplements.py
      - web/src/generated/load-map.json
    acceptance: >
      G59 built exactly this guard for SUPPLEMENTS -- unchained_supplement_files() fails when a
      supplement exists on disk but is absent from the chain. There is NO equivalent for
      LOADERS, which is why pat_team_roles and area_products sit in LOADER_REGISTRY, reachable
      by name, belonging to no sequence, with nothing turning red.
      (a) A guard fails when a loader is registered but appears in no chain and no profile,
      NAMING the loader rather than reporting a count -- the G59 shape. A deliberate exclusion
      stays expressible and carries a WRITTEN REASON, the SCHEDULED_INGEST_EXCLUSIONS idiom.
      (b) The same two facts become defect classes in web/src/generated/load-map.json, beside
      the sourceless_loaders and map_entries_without_registry_source lists it already carries:
      unchained loaders, and steps whose declared input is absent (G78's other half). O57
      already renders that JSON's defect lists rather than dropping them, so this is what makes
      the console page an honest inventory instead of a tidy one.
      (c) Portable: the guard must hold in a consumer repo whose chains legitimately differ --
      the company chain carries sub_lobs where the producer carries pat_product_mapping, so a
      guard that hardcodes producer membership fails there for being right.
    notes: >
      Raised 2026-08-11 (desktop). Dependency-free deliberately: the guard is worth having
      BEFORE G79 moves loaders between chains, because that is exactly when one goes missing.
      Same family as G59 and as J47 (no guard asserts a PORT-MANIFEST path exists) -- three
      instances now of "a thing is declared, nothing checks it is reachable".

  - id: GN1
    epic: generic-naming
    title: "ADR 0012 — name loaders, commands and sources by the DATA, not the tool: the three-axis model and the deprecation policy"
    type: task
    module: docs
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: done  # 2026-08-11 (desktop): ADR 0012 written + indexed. Clause 5 is the finding the session produced rather than restated: the three layers do NOT move together — CLI verbs and LOADER_REGISTRY keys are producer-canonical, but source registry ids are COMPANY-CANONICAL under registry v2 with retirement governed by the D4 refusal list, so renaming pat:* is a cross-repo reconciliation minting new retired-id entries, NOT an edit. A unilateral producer change there forks the registry on identity, which is exactly what D4 exists to prevent. Four alternatives recorded with reasons, including the SaaS-exemption the SME raised — declined using the SME's own acquisition-may-change argument applied one level up.
    depends_on: []
    inputs:
      - docs/decisions/0010-internal-source-term-abstraction.md
      - drydocs/cli.py
      - config/source-registry.yaml
      - PORT-MANIFEST.yaml
      - docs/port-prompt.md
    acceptance: >
      An ADR settling the naming BEFORE any rename moves, because the alternative is renaming
      twice. It must carry:
      (a) THE MOTIVATION IN THE SME'S OWN TERMS -- company jargon entered a repo that was meant
      to be generic from the start (seal, pat, m1/m3 milestone ids). This is the standalone-
      generalization goal, not cosmetics, which is why it earns an ADR and an epic rather than
      a rename commit.
      (b) THE THREE-AXIS MODEL: the command names the SUBJECT (the data), the source registry
      entry carries CADENCE and ACQUISITION, and the profile is DERIVED by filtering on cadence.
      Today all three are collapsed into one LoadStep with hand-assigned profile tuples.
      (c) THE THREE BANDS: prepare / load / verify. Only the load band is source-keyed; forcing
      check or bootstrap into a source scheme is the same category error inverted. m1-verify and
      m3-verify carry the same defect as seal and pat -- milestone ids meaning nothing to a
      reader -- and are renamed with everything else.
      (d) THE SAAS QUESTION, ruled not dodged. The SME flagged ServiceNow and Jira as possible
      exceptions because they are widely known. The ADR should record the counter-argument and
      the ruling: the subject names the data (cmdb, work-items), the source id names the tool,
      because the SME's own CSV-to-database argument applies one level up -- if CMDB data later
      arrives from a different ITSM, load-servicenow is wrong and load-cmdb is still right.
      (e) DEPRECATION POLICY. CLI verb names are a PUBLIC CONTRACT -- company crons, scripts and
      the run-drydocs skill call them by name. New names ship with old ones as deprecated
      aliases for one port cycle, then drop. Precedent exists: cli.py already carries legacy-verb
      aliasing for supplements.
      (f) THE SOURCE-ID LAYER IS NOT A UNILATERAL PRODUCER CHANGE, and the ADR must say so or
      GN2 will discover it the hard way. Registry v2 is a COMPANY-CANONICAL SURFACE per the
      standing divergence ledger, and retired ids are governed by the D4 refusal list. Renaming
      pat:* source ids is a cross-repo reconciliation with new retired-id entries, not an edit.
      The ADR rules which of the three layers move together and which move separately.
      (g) The alternatives considered and rejected, with reasons -- an ADR that records only the
      chosen option is a design note wearing a decision's clothes.
    notes: >
      Claimed at raise, 2026-08-11 (desktop), SME direction: "I agree with the ADR approach and
      everything ... the rename may require its own standalone epic, with a frozen change and
      port." NEW EPIC generic-naming, prefix GN. NOTE ON THE PREFIX: the free single letter was
      T, and it was NOT taken -- T1-T22 is the cross-repo TRACKER id space cited throughout
      docs/port-prompt.md, so T-series backlog items would collide semantically with T15/T18/T22.
      That is the same class of collision that produced the G70 duplicate this morning; declining
      it cost one letter.

  - id: GN2
    epic: generic-naming
    title: "Execute the rename as ONE frozen unit and port it as one unit — data-named commands, loaders and source ids"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: todo
    depends_on: [GN1, G79]
    inputs:
      - drydocs/cli.py
      - drydocs/loaders/
      - config/source-registry.yaml
      - docs/port-prompt.md
      - PORT-MANIFEST.yaml
    acceptance: >
      The mechanical rename ADR 0012 authorises, executed as a SINGLE frozen change and ported
      as a SINGLE unit -- the SME's condition, and the right one: a wide rename landing in
      pieces leaves the two repos disagreeing about verb names for as long as the pieces take.
      Scope, in the layer order the ADR rules: CLI verbs (with deprecated aliases per the policy),
      LOADER_REGISTRY keys and loader class names, milestone-id verify commands, and -- only if
      the ADR rules them in the same unit -- source registry ids via the cross-repo path.
      Every derived surface regenerates and is proven current: load-map.json, load-map.html,
      ingest.sh, the startup runbook Appendix B (held by test_load_sequence_surfaces), the
      load runbook, and the skills that name verbs.
      MUST NOT overlap an in-flight port. The change freezes other producer work, lands, is
      ported, and only then does normal work resume.
    notes: >
      Raised 2026-08-11 with GN1. depends_on G79 as well as GN1 so the split lands under the OLD
      names first: splitting and renaming together makes the port diff unreviewable, and the
      split is the part that carries behavioural risk while the rename is mechanical.

  - id: C34
    epic: ontology-mapping
    title: "dcat:theme — declare the EXISTING lob-product-team tree as a SKOS concept scheme and draft its gate, instead of minting a second subject vocabulary"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: opus
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - config/taxonomy/lob-product-team.yaml
      - config/taxonomy-ontology-map/
      - drydocs_core/ontology/relationship_vocabulary/
      - config/gate-prompts/
      - docs/decisions/0011-single-database-contingency.md
    acceptance: >
      THE FINDING THAT SHAPES THIS ITEM, verified before it was raised rather than assumed: the
      hierarchy the SME proposed for subject classification -- LOB, Product Line, Product --
      ALREADY EXISTS as config/taxonomy/lob-product-team.yaml (CatalogLOB > ProductLine >
      Product > DevTeam, B3, captured 2026-06-20), and the SME's vernacular examples are
      already ProductLine rows in it. This item does NOT mint a subject vocabulary; it declares
      the one that exists.
      (a) THE SKOS DECLARATION -- taxonomy, layer 1, NO gate, lands immediately. The file is a
      concept scheme in all but name: classification_only, hierarchical via parent_lob /
      parent_product_line, id-keyed, labelled. Declare it a skos:ConceptScheme with the parent
      links as skos:broader so dcat:theme has something standards-valid to point at. SKOS is
      already precedent in this very file, which cites skos:closeMatch for RECONCILES_TO.
      (b) THE GATE PROMPT, drafted not signed -- dcat:theme is a MEANING edge. It must pose:
      (b1) RESIDENCY, which BLOCKS the cross-corpus half. The design puts structured and
      unstructured context in TWO DATABASES, and a Neo4j relationship cannot span databases
      (Q8 -- the same constraint blocking G32's estate join), so one edge cannot reach one
      shared Concept from both sides. Three shapes: duplicate the scheme per database with
      identical concept IRIs; hold the scheme in one and carry the theme on the other as an
      IRI-valued PROPERTY joined application-side; or single database (ADR 0011). DECIDABLE NOW
      REGARDLESS, and the prompt says so: make the concept IRI the join key and keep it stable,
      so the vocabulary is portable whichever way residency rules and no work is stranded.
      (b2) IS a Product a skos:Concept, or does it HAVE one? ProductLine and Product are
      business entities with owners, teams and applications; dual-typing them is legal RDF and
      muddies what the node is. The alternative is a concept per product with skos:exactMatch.
      Rule it -- do not let a loader decide by accident.
      (b3) ANNOTATION DEPTH is the real control on grain: annotate at ONE level and derive the
      rest by skos:broader, so tagging a folder at Product yields its ProductLine and LOB free.
      That stops a deep vocabulary from multiplying annotations, and it beats capping vocabulary
      size, which forces a lie the moment the business genuinely has more rows than the cap.
      (b4) THE CARDINALITY ASK IS PREMATURE AT THE TIER NAMED, and the prompt must say why
      rather than silently declining. The SME asked for a limit under 5 at the AreaProduct
      level; lob-product-team.yaml records area_products: 0 and carries a STANDING OPEN QUESTION
      (area-product-missing) on whether that tier applies outside production PAT at all. Rule
      the tier before capping it. What CAN be ruled now is the ANNOTATION cap -- how many themes
      one subject may carry -- enforced as a DETECTOR, not a schema constraint, because a folder
      carrying six themes is evidence the folder does too much or the tagger hedged, and that is
      a finding worth reading rather than a write to refuse.
      (c) UNCLASSIFIED IS FIRST-CLASS, and out-of-scope is a DIFFERENT value from not-yet-
      classified. External-vendor and unrelated pages are permanently out of scope; an
      unclassified internal page is pending. Collapsing them makes coverage unmeasurable,
      because the number stops distinguishing backlog from scope. Same discipline as
      resource_pool.classify returning unknown with no label and a WARN.
      No graph writes, no ratification: the scheme declaration ships, the edge is the gate's.
    notes: >
      Raised 2026-08-11 from an SME design session that began on Idea-105 and opened a second
      thread. THE ORIGINAL INTENT: folder-level runbooks are >90% automatable, and the roll-up
      axis was going to be "Data Series" -- an entity that is not commonly defined. Teams think
      in source name instead, which is why the existing ProductLine tier fits: it is both the
      vernacular people already use AND already captured. The roll-up therefore falls out of the
      concept scheme without Data Series ever being defined, which is the part to protect in
      review.
      WHY IT REACHES THE CONTEXT GRAPH (SME observation, and it is right): a theme is a
      PROJECTION AXIS, so "everything in this subject, right now" becomes a layer-4 slice
      instead of needing a new entity to hang it on. Themes are multi-valued by nature, so a
      process serving two subjects appears in both slices with nothing duplicated -- which a
      single Data Series field could never have done.
      SCOPE FENCE: declares and drafts. Does not populate real values (Internal), does not write
      the edge, does not build UI.

  - id: G77
    epic: component-topology
    title: "One theme vocabulary, two corpora: dcat:theme on the Control-M folder block AND the scraped-document envelope, joined by concept IRI"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: todo
    depends_on: [C34, G84]
    inputs:
      - config/taxonomy/lob-product-team.yaml
      - drydocs_core/orchestration/controlm/description_tokens.py
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - knowledge/standards/technology/description-field-metadata-plan.md
    acceptance: >
      One vocabulary classifies BOTH corpora -- that is the point, and it is what ties the
      structured estate to the unstructured one.
      (a) CONTROL-M, folder scope: a THEME token inside the DD1| folder block (guidelines
      section 7.5), registered in TOKEN_REGISTRY like any other token, multi-valued on the
      documented inner separator, values checked against the concept scheme with an unrecognised
      value RETURNED as a finding rather than raised -- the standing aliases-suggest /
      values-decide discipline. This is the folder-level block C29 originally intended for a
      Data Series roll-up that was never commonly defined.
      (b) SCRAPED DOCUMENTS: the same field on the document metadata envelope. The two-corpus
      architecture already rules that the vendor and Confluence corpora stay EPISTEMICALLY
      SEPARATE and merge only through a shared metadata envelope -- a theme is exactly such a
      field, which is why it fits rather than being bolted on.
      THE DISCIPLINE THAT MUST BE WRITTEN DOWN OR SOMEONE WILL BUILD PAST IT: the join is at the
      CLASSIFICATION, never the content. A document and a folder sharing a theme are both ABOUT
      that subject; that is not an assertion that the document describes the folder, and no edge
      may imply it. Trust tiers do not merge because two things share a subject.
      (c) The unclassified / out-of-scope split from C34 (c) is implemented on the scraper side,
      where it actually bites, and coverage reports the two apart.
      (d) Joined by CONCEPT IRI, never by label -- so this item works unchanged under one
      database or two, and label drift between corpora cannot fork the join.
      Zero graph writes: the field, its validation and the coverage split. The edge waits on C34.
    notes: >
      Raised 2026-08-11 with C34, same session. Depends on C34 because a theme field populated
      against an undeclared scheme is free text with a schema-shaped name.
      DELIBERATELY NOT RAISED YET: the SME-facing classification UI, which the session described
      as possibly two pages. Building a classification surface before C34 rules residency and
      the Product-vs-Concept typing is the O59 mistake in a new place -- a widget built against
      a shape that turns out otherwise is thrown away. It grooms once C34 signs, and the
      two-page question then answers itself from the residency ruling rather than from taste.
      RAISED AT THE 2026-08-12 GROOM, and the reason depends_on grew: clause (a) reads a THEME
      token "inside the DD1| folder block", and nothing in the parser knows what that block is.
      G84 builds the block gate; this item registers a token on top of it. Without the
      dependency, two sessions implement the same sentinel differently in one file.

  - id: G83
    epic: component-topology
    title: "The C30 ruling never reached the parse contract — the description token registry is two greenfield tokens short and four retired ones long, so a conformant watcher reports seven false findings"
    type: bug
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p1
    status: done
    depends_on: []
    inputs:
      - drydocs_core/orchestration/controlm/description_tokens.py
      - tests/unit/test_description_tokens.py
      - knowledge/standards/technology/controlm-greenfield-job-standard.md
      - knowledge/standards/technology/description-field-metadata-plan.md
      - tests/unit/test_remediation_conformance.py
      - docs/port-prompt.md
    acceptance: >
      REPRODUCED AT THE GROOM, sample-only -- no database, no machine, so anyone can re-run it
      (J18). Parse the C30-conformant watcher description
      "DELIVERY_MECHANISM: MFTS_AGENT | USER: <acct> | FTS_ID: FTS2 | REC_ID: <id>,<id> | SOURCE_CONTACT: <dl>"
      and validate it as JobType.FILE_WATCHER: SEVEN findings come back on a description that is
      exactly what the ruled standard requires -- unknown_key on FTS_ID and REC_ID (the two
      tokens C30 introduced) and missing_required_token on ENV, INBOUND_ROUTE, OUTBOUND_ROUTE,
      EMAIL_DL_L3 and EMAIL_DL_L2 (the four C30 retired or moved to folder scope). TOKEN_REGISTRY
      still encodes the C29 capture; C30 ruled the set on 2026-08-11 and only the standards page
      moved. The module's own docstring promises it is "the single source both the parser and the
      published register read from, so code and document cannot drift" -- that promise is what
      broke, and a conformance model that reports conformant jobs as defective is worse than no
      model, because the findings look authoritative.
      (a) ADD the greenfield tokens: FTS_ID (watcher; vocabulary FTS1/FTS2/FTS6/FTS7 per the SME
      ruling carried in C30) and REC_ID (watcher; multi-valued on comma, a SOURCE-SYSTEM
      REFERENCE feeding dprod:inputPort -- explicitly NOT a route id).
      (b) DO NOT DELETE THE RETIRED ONES. The deployed estate still carries them and this
      parser's job is to read what is there: ENV-on-a-watcher, INBOUND_ROUTE, OUTBOUND_ROUTE and
      PDN_SNOW_QUEUE keep their entries plus a LIFECYCLE MARKER naming the ruling that retired
      them, so a legacy description parses them as a known-retired token instead of as an
      unregistered key. Deleting them would silently reclassify real estate data as somebody's
      private C16 team-local annotation, which is the one reading that governance cannot afford.
      (c) COMPLETENESS BECOMES ERA-AWARE: required_tokens() must not demand a retired token, so a
      job authored to C30 validates clean while a legacy job still validates against what it was
      authored to. The existing route-id exemption for non-MFTS_AGENT mechanisms stays as it is.
      (d) CONTACTS CHANGED CARRIER, they did not vanish. EMAIL_DL_L2 / EMAIL_DL_L3 are
      folder-scope documentation under C30, and EMAIL_DL_PDN is a THIRD member the
      FOLDER_VARIABLES table does not carry at all. Register the three at folder carrier under
      the C30 spellings and record that the folder-variable twins were spelled L2_EMAIL_DL_NM /
      L3_EMAIL_DL_NM before the rename, so a live extract authored either way still resolves.
      (e) RE-POINT THE STANDARD-OF-RECORD line in the docstring at the greenfield standard, with
      the metadata plan named as the earlier capture rather than deleted, and add a test that
      asserts the registry and the standard agree on the per-job-type set -- so the next ruling
      cannot land in prose alone. That test is the deliverable; the registry edit is the easy
      half.
      WHAT THIS MUST NOT DECIDE, stated in the acceptance so the evidence is not lost with the
      entries: marking the route pair retired is NOT an answer to whether the real MFT route id
      is the numeric form or the MFTS_RT_* string (Idea-104, still the SME's). Keep the note that
      records the two unreconciled shapes on whichever entry survives.
      Zero graph writes. Every token stays proposed; no relationship-vocabulary entry is
      ratified and no property term is bound.
    notes: >
      Raised at the 2026-08-12 groom (desktop) from a cross-check of C30 (done, 2026-08-11)
      against the code it ruled -- not from an inbox line. WHY IT IS p1 rather than housekeeping:
      G68 is in next_ready and its folder-set PROFILE reads this contract, so a census run before
      this lands reports the retired set as missing on every conformant folder and the two new
      tokens as unknown; and G67's own conformance fixtures ALREADY emit "FTS_ID: FTS2", which
      the parser today calls unregistered. The drift is therefore live in the test tree, not
      hypothetical.
      WHY IT IS A BUG AND NOT A DESIGN QUESTION: C30 is done and SME-ruled, so nothing here
      decides anything -- the ruling simply never reached the module. The one judgment call is
      clause (b), retire-in-place versus delete, and it is made here rather than left open
      because the parser's stated job is reading a 4000-char field as the estate actually filled
      it. A greenfield standard governs what gets AUTHORED next; it cannot retroactively unwrite
      ~240K deployed descriptions.
      PORT CARE: docs/port-prompt.md records that description_tokens.py is UNION-MERGED, not
      wholesale -- producer holds parse_description / required_tokens / validate, the company
      adapter holds parse_tokens / classify_job_role, and the union survives only because the two
      name sets do not collide. Do not rename an exported symbol here; add to the registry and
      the completeness rule instead.
      SIBLING: G84 edits the same module and the same test file. Both are dependency-free; if
      both are open, claim them together and land one commit (the G75+G76 batch precedent).

  - id: G84
    epic: component-topology
    title: "Teach the description parser the DD1| sentinel — three disjoint description populations, so legacy prose stops manufacturing tokens and the compliant marker stops being reported as a defect"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done
    depends_on: []
    inputs:
      - drydocs_core/orchestration/controlm/description_tokens.py
      - tests/unit/test_description_tokens.py
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - config/gate-prompts/email-dl-contact-point.yaml
      - internal/controlm-config/controlm-pipeline-stub-integration-plan.md
    acceptance: >
      REPRODUCED AT THE GROOM, sample-only (J18): parse_description
      ("DD1|DELIVERY_MECHANISM: MFTS_AGENT | FTS_ID: FTS2") returns an unparseable_segment
      finding FOR THE MARKER ITSELF -- the parser reports the one thing the standard makes
      mandatory as a defect. And parse_description("NOTE: rerun after 08:00 | contact: ops team")
      returns two "tokens" (NOTE, contact) with two unknown_key findings, so legacy waterfall
      prose is indistinguishable from a legitimate C16 team-local annotation. Guidelines section
      7.5 specifies the fix; nothing implements it.
      (a) ONE CONSTANT, ONE PREDICATE. The marker lives in a single module-level constant and the
      test is startswith at position 0 -- no strip, no substring search. Section 7.5 gives both
      reasons and both are load-bearing: a description that QUOTES the convention in prose cannot
      false-positive, and startswith is the cheapest predicate available to a SQL scan over the
      whole estate.
      (b) THE PARSE RESULT CARRIES THE POPULATION -- tagged / generator-literal / untagged -- and
      the token grammar applies only to the tagged one. The marker itself never becomes a token
      and never becomes a finding. Untagged is not a defect and must not be reported as one:
      untagged means unread.
      (c) THE DIGIT IS A VERSION, NOT A TEMPLATE ID. Parse it as a version and report it, so DD1
      and a future DD2 are read side by side through a grammar migration. The module must state
      that the marker MUST NOT select a job template -- TASKTYPE derived plus the registered
      JOB_ROLE token do that (section 7.2) -- because spending the version slot on template
      identity leaves the first grammar change with no way to announce itself.
      (d) COVERAGE, NOT COMPLIANCE: a helper reports tagged / total over a set of descriptions.
      The measure of this standard is tagged folders divided by folders, a number that grows,
      NOT "how much of our metadata is wrong", a number that never closes. Fewer than ten folders
      carry the standard today; that is a sample, not a shortfall, and the acceptance should not
      pretend otherwise.
      (e) FOLDER SCOPE IS DOCUMENTED, NOT ENFORCED. Record in the module why folder scope is
      preferred -- get_description() is generator-owned, so a tagged block on a GENERATED JOB is
      overwritten at the next regeneration and the tag does not protect it. The parser reads
      whatever it is given; the note exists so nobody tags jobs and loses the block.
      THE FENCE, and it is the reason this item is small: this implements a READ gate over free
      text and ratifies nothing. Exit (d) is recorded at gate email-dl-contact-point as rider
      section G6 with a written warrant, and THE GATE STILL RULES IT -- so the marker stays in
      exactly one constant and a different ruling is a one-line change, not a rewrite. Zero graph
      writes; every token stays proposed.
    notes: >
      Raised at the 2026-08-12 groom (desktop) from Idea-105, which the 2026-08-11 SME design
      session resolved on an exit none of its three recorded ones described. THE PROBLEM IT
      SOLVES: two things claim the same 4000-char DESCRIPTION field -- the DPL generator's
      literal, which the stub plan's E1 discriminates on an EXACT match, and the company token
      standard captured at C29. Adding a token block to a generated object breaks E1; requiring
      the literal leaves the token block nowhere to go. A versioned sentinel PARTITIONS the field
      instead of choosing between them: the three populations become disjoint, both readers keep
      working, and NOTHING ALREADY DEPLOYED MIGRATES -- strictly cheaper than losing the metadata
      where the estate is densest, invalidating E1 on every generated object, or finding a new
      carrier the generator stamps.
      IT ALSO RETIRES C29's PROPOSED GENERATED_BY TOKEN, which is why no item mints one: absence
      of the tag on a literal-match description already IS the provenance signal, so a token
      asserting it would be a second carrier for one fact. Verified at the groom that
      TOKEN_REGISTRY never grew one.
      CONSUMER: G77 clause (a) registers a THEME token INSIDE this block and now depends on this
      item for that reason.
      COMPANY RELAY: their copy of the gate prompt is canonical-company and did not take the
      producer edits, so RELAY-7 carries the rider across. Nothing in this item ports on its own.
      SIBLING: G83 edits the same module and the same test file -- if both are open, claim them
      together and land one commit.

  # ── 2026-08-12 remediation-fix-module groom (xml_io epic; plan reactive-finding-shannon) ──
  - id: G85
    epic: web-console
    title: "Agent lookup beside the fix diff — cite the vendor→standards→team chain from the verified BMC corpus"
    type: requirement
    module: drydocs-web
    phase: 12
    agent: main
    model: opus
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - external/orchestration/bmc-controlm/SOURCE-MANIFEST.md
      - drydocs_remediation/module-requirements.md
      - web/src/remediation/FixDiff.tsx
      - scripts/render_remediation_diff.py
    acceptance: >
      DETERMINISTIC FIRST, AGENT SECOND (user direction 2026-08-12): every remediation
      decision stays with the rules engine and the xml_io post-conditions; the agent
      never decides. It answers "why is this the rule?" and "does the vendor actually
      say that?" beside the SME diff, walking the three tiers that are ALREADY the
      ratified precedence order (CLAUDE.md section 4) -- (1) VENDOR: what BMC says the
      object is, from external/orchestration/bmc-controlm/ (the BASELINE); (2) PLATFORM
      STANDARDS APPLIED: how knowledge/standards/ + the R1-R28 rules registry refine it;
      (3) TEAM: who owns the thing being changed (LOB->Product->Team, SEAL/escalation
      routing). So the surface is a visualization of the existing precedence chain, not
      a new concept. HARD REQUIREMENTS: (a) every answer cites its corpus doc AND its
      trust level -- an answer sourced from a [GROUNDED search-snippets-only] capture
      (e.g. the XML-definition stub) must say so, never present it as vendor ground
      truth (the SOURCE-MANIFEST trust ladder is the contract); (b) read-only, no graph
      write -- consistent with the remediation module invariant; (c) precomputed material
      rides the generated-artifact pattern; a live agent call is its own seam, never a
      QuerySpec; (d) the FULL BMC scrape is loaded first -- declared as this feature's
      substrate in drydocs_remediation/module-requirements.md section 3, with the 403-
      blocked fetch list re-run from a network that reaches documents.bmc.com. GATED
      BEHIND the fix-diff surface existing (it reviews a diff; without one it reviews
      nothing). Precedents: the Q&A console for the answer surface; docmeta for
      citation handling.
    output: web/ (drydocs-web) + agents/ integration seam
    notes: >
      Groomed from the 2026-08-12 user follow-up ("an agent query against the verified
      bmc documentation, to show vendor, then the platform standards applied, then the
      team"). Scope-noted as its own epic slice, larger than the defect fixes it rode
      in with; most useful once a real (non-demo) fix diff exists to review beside it.

  - id: G86
    epic: web-console
    title: "Calendars: can a RULE_BASED_CALENDAR be validated, and can it render as an actual calendar in the console?"
    type: task
    module: drydocs-web
    phase: 12
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - external/orchestration/bmc-controlm/controlm-calendars.md
      - drydocs_remediation/xml_io.py
      - tests/unit/fixtures_controlm_xml.py
    acceptance: >
      TWO QUESTIONS, ANSWERED SEPARATELY (a spike ruling counts; building both is not
      required to close this): (1) VALIDATION -- now that RULE_BASED_CALENDAR(S)
      elements survive import verbatim (xml_io splicing keeps them as bytes; the F3
      fixture carries one), can a calendar definition be checked for internal
      consistency, and can a job's schedule be validated against the calendar it
      names? Deliverable: a written ruling with a worked synthetic example, or the
      validator itself. (2) RENDERING -- can it draw as an actual calendar in the
      console? CONSTRAINTS ALREADY KNOWN from the vendor corpus
      (controlm-calendars.md): three calendar types (Regular / Periodic / Rule-Based)
      with four RBC rule types and eight exception policies, so "render a calendar"
      is really three renderers -- scope a spike to RBC only first; and calendars are
      defined against EXPLICIT YEARS, so a projection may only extend as far as the
      calendar's year coverage -- a UI that silently projects past it is WRONG and the
      spike must show the boundary behavior. Related prior art: the calendar-
      projection plan (scheduling plane exact, prereq plane out of scope) and the
      data-center-naming default-time finding (DC names encode a default EST run
      time for folders without an explicit one; real DC names are Internal).
    output: docs/ ruling or web/ (drydocs-web) spike
    notes: >
      Groomed from the 2026-08-12 user follow-up ("add a backlog task to test if the
      calenders can be validated or rendered as a calendar in the ui"). Split
      validate-vs-render on purpose: validation is graph/model work, rendering is a
      console surface, and either can ship without the other.

  - id: G87
    epic: ontology-mapping
    title: "Tech-debt: force the epoch-tag vocabulary ids (m3_/p2_/m7_/u1_/u2_/g22_/c23_/prov_/sosa_) onto the domain-derived scheme"
    type: chore
    module: ontology
    phase: 2
    agent: main
    model: opus
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/00-header.yaml
      - config/taxonomy-ontology-map/
      - drydocs_lineage/model.py
      - drydocs_lineage/writer.py
    acceptance: >
      Gate vocabulary-domains-and-id-policy §B3 (SIGNED 2026-08-12) ruled FORCED
      fall-off, not natural attrition: every epoch-tag id migrates to the domain-derived
      scheme (scheduler_*, business_application_*, ...) as ADD-NEW + DEPRECATE-OLD pairs
      -- never a rename in place, because ids are the join key across four ledgers.
      DONE MEANS: (a) each old entry is status deprecated with a note naming its
      replacement id; (b) the taxonomy-ontology-map vocab_id joins point at the new ids;
      (c) schema_graph.cypher regenerated; (d) the SQLite mirror rebuilds (content hash);
      (e) the hardcoded id table drydocs_lineage/model.py VOCAB_IDS and every test pin
      (test_yaml_fragments dispositions, test_schema_graph expected sets, lineage-writer
      vocab_id asserts) updated with a comment citing this item; (f) the port guard stays
      green (no id vanishes -- old entries remain as deprecated audit rows); (g) the
      append-only gate log keeps old spellings as history, untouched. Fold G88's
      relocation into the same pass where the same entries are touched.
  - id: G88
    epic: ontology-mapping
    title: "Tech-debt: relocate the Employee/Membership/Attribution entries into the human domain"
    type: chore
    module: ontology
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: [G87]
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/41-local-business-application.yaml
      - drydocs_core/ontology/relationship_vocabulary/42-local-catalog.yaml
    acceptance: >
      Gate vocabulary-domains-and-id-policy §A3 (SIGNED 2026-08-12) registered the
      human domain with NO entries moved. This item does the move: the people/org
      entries (the attribution/membership family in business_application and catalog)
      relocate to a human fragment (e.g. 49-local-human.yaml) with domain: human.
      Depends on G87 BY DESIGN: ids change there, so relocating in the same pass avoids
      touching the same entries twice; if G87 is descoped this item may run alone with
      ids unchanged (domain + fragment move only). schema_graph.cypher regenerates;
      the fragment sort order stays stable.
  - id: G89
    epic: ontology-mapping
    title: "Tech-debt: resolve the m3_triggers conflict — a planned edge is live-consumed by a QuerySpec"
    type: chore
    module: drydocs-api
    phase: 2
    agent: main
    model: sonnet
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - drydocs_api/query_specs.py
      - drydocs_core/ontology/relationship_vocabulary/40-local-scheduler.yaml
    acceptance: >
      Recorded at gate vocabulary-domains-and-id-policy §C3 (2026-08-12): m3_triggers
      is status planned (no loader may write it; the lineage writer refuses it as
      gate-bound) yet a live QuerySpec MATCHes [:TRIGGERS] -- the spec returns empty
      rather than wrong, but the surface implies data that cannot exist. RESOLVE ONE
      WAY: (a) demote the spec to demo-only / remove it until the m3_triggers build, or
      (b) prioritize the m3_triggers build (its shape was confirmed 2026-07-15; the
      cmdline-derivation caveat is in its note). A ruling plus the matching code change
      closes this; do not flip the entry active without its supplement + loader.
  - id: G90
    epic: structure-remediation
    title: "Build the fix-tracking loader the remediation-fix-tracking gate authorized (§C1)"
    type: requirement
    module: drydocs-load
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_remediation/changes.py
      - config/gate-prompts/remediation-fix-tracking.yaml
      - drydocs_core/ontology/relationship_vocabulary/20-property-terms.yaml
    acceptance: >
      Gate remediation-fix-tracking SIGNED 2026-08-12. A dedicated drydocs-load loader
      consumes drydocs.remediation.fix-tracking.v1 change-sets: UNWIND $batch, MATCH on
      the NODE KEY ((folder_id, job_id) / folder_id) -- NEVER MERGE-creating a target
      (a missing target is an error, not a node to invent), SET the three ruled
      properties (remediation_fix_id, remediation_status, remediation_status_date),
      REMOVE them on rejection. Standard JobRun provenance applies. The §D1 dd: local
      property_terms entries land with this build (section 0b, prose definitions,
      revisit standard terms at RDF export). The emitter stays no-graph-write -- the
      AST guard and corroborate regex are untouched. Enum drift is already refused at
      emission (FIX_STATUS_ENUM); the loader validates the same enum on intake.
  - id: G91
    epic: ontology-mapping
    title: "Rule the held planned-entry review (gate §C1b/§C1d) and transcribe the outcome"
    type: task
    module: ontology
    phase: 2
    agent: main
    model: fable
    priority: p2
    status: done   # 2026-08-18 (laptop): gate CLOSED 5/5 — 2 keep-planned, 1 activated, 1 deprecated-redundant, 1 re-shaped onto qualified attribution; m3_ ids retired for the two §C1b entries; scheduler_runs_as raised; rulings transcribed to config/gate-log.md
    depends_on: []
    inputs:
      - docs/reviews/vocabulary-planned-review.md
      - config/gate-log.md
    acceptance: >
      HITL: the SME rules each of the five entries held at gate
      vocabulary-domains-and-id-policy (m3_depends_on_file, m3_executed_by,
      catalog_has_area_product, catalog_area_product_has_dev_team,
      catalog_dev_team_has_membership) keep-planned or deprecate, in
      docs/reviews/vocabulary-planned-review.md. The ruling is transcribed to
      config/gate-log.md as a follow-up entry citing the gate slug; any deprecation
      lands with deprecated_at + a note citing that entry; gates.json and the board
      regenerate. Note the review file flags catalog_dev_team_has_membership as a
      re-shape candidate (the SEAL-side HAS_MEMBERSHIP chain was deprecated for
      qualified attribution) -- a re-shape ruling routes through RELATIONSHIP_GUIDE
      as a NEW proposal, not an edit of the held entry.

  # ── 2026-08-12 targeted groom (Idea-112 / Idea-113 / Idea-114) ──────────────
  - id: G92
    epic: component-topology
    title: "File-op operands are parsed from RAW shell text, so a variable path and its resolved twin become two different data assets — resolve the job's scope chain before the file-op parse"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [G14, G46, G60]
    inputs:
      - drydocs_lineage/extractors/controlm_inventory.py
      - drydocs_core/orchestration/controlm/resolver.py
      - drydocs_core/orchestration/controlm/variables.py
      - drydocs/cmdline_staging.py
      - tests/unit/test_lineage_inventory.py
    acceptance: >
      REPRODUCED AT THE GROOM, sample-only, no database (J18): the G60 pre/post pass calls
      parse_command(value) on the RAW variables-CSV var_value, and _file_op keys the asset as
      asset_id(_FILE_OP_ASSET_KIND, location) from that verbatim operand -- so a job whose
      POSTCMD moves "%%R_PATH/out.dat" and a job whose CMD_LINE moves "/data/r/out.dat" plan
      edges to TWO DataAsset nodes for ONE file. The CMD_LINE pass has the same defect; only
      the pre/post pass makes it visible, because pre/post text is where the variable forms
      concentrate.
      (a) RESOLVE, THEN PARSE. The extractor builds the job's folder -> job scope chain and
      runs the shell text through the ONE resolver
      (drydocs_core/orchestration/controlm/resolve_layers / resolve_command_line, whose stated
      guardrail is "no caller may re-implement substitution"), then parses the RESOLVED text.
      No second substitution engine, no regex twin, no local %%-stripping helper -- a new
      substitution path in this extractor fails this item outright.
      (b) RAW STAYS BESIDE RESOLVED, never replaced -- the G46 derived-fact shape. The
      candidate carries both the verbatim operand and the resolved location, so a wrong
      binding is auditable rather than baked in.
      (c) CANONICAL RESIDUE IS NOT A FAILURE. {ODATE}-class tokens survive resolution by
      design and stay symbolic in the operand; they are counted as EXPECTED residue,
      distinct from an unresolved user variable, which is counted as a miss. Neither is
      dropped (the coverage house rule).
      (d) COUNT THE RESOLUTION QUALITY on the ResolveCoverage precedent
      (drydocs/cmdline_staging.py): per-run counters for resolved / residue /
      nothing-to-substitute / no-scope-chain, reported through the existing ExtractCoverage
      summary line so the yield is measurable in the same place the prepost_* counters
      already land.
      (e) SCOPE CHAIN COMES FROM THE SAME CSV the shell text does. The aliased projection
      carries var_scope (FOLDER | JOB); the raw export does not, and there the folder rows
      are the header row where JOB_NAME equals the folder name (the drydocs/staging.py
      is_folder_header rule). Both shapes must feed the chain, and a row with NO resolvable
      scope chain is counted, never silently parsed raw.
      ENDPOINTS AND MEANING ARE UNCHANGED: same READS_FROM / WRITES_TO, same
      ControlMJob -> DataAsset shape from the 2026-07-15/16 gate EDIT, no new relationship
      type, m3_reads_from / m3_writes_to stay planned. Zero graph writes; a unit test on a
      sample job proves the resolved and raw spellings of one path converge on ONE asset id.
    notes: >
      Groomed 2026-08-12 (targeted groom) from Idea-112, raised working through the G60
      result with the user. Verified at the groom rather than taken from the entry:
      _prepost_pass parses var_value directly (controlm_inventory.py), resolve_layers /
      resolve_command_line already return each definition's resolved_value plus substitution
      provenance (G46, done), and PRECMD/POSTCMD are themselves SETVAR definitions -- so the
      scope chain that resolves the rest of the job resolves them in place. This is a FEED
      change, not a new parser and not a new resolver, which is why it is sonnet: every
      semantic decision it depends on was made at G14, G46 and G60.
      MODULE: drydocs-lineage, chosen because the change point is the extractor's pass; core's
      resolver is CALLED, not modified. If core turns out to need a public helper for the
      scope-chain assembly, that is a drydocs-core follow-up item, not a widening of this one.

  - id: G93
    epic: component-topology
    title: "A finished remediation batch reports no numbers — carry the run's counts into the Jira fix-package explanation through the run-log contract"
    type: task
    module: drydocs-remediation
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_remediation/jira.py
      - drydocs_remediation/detect.py
      - drydocs_core/run_log.py
      - drydocs_lineage/extractors/controlm_inventory.py
      - tests/unit/test_remediation_conformance.py
    acceptance: >
      REPRODUCED AT THE GROOM, sample-only (J18): render_handoff() emits Findings, Scope,
      Change, Equivalence evidence, Acceptance and Rollback -- and not one count of what the
      run actually processed. The reviewer reading the ticket cannot tell whether the batch
      examined nine jobs or nine hundred, so "we fixed it" carries no denominator. Meanwhile
      the extractors already model exactly this (ExtractCoverage.summary() with the G60
      prepost_* source split, XmlDefsCoverage, findings counted by rule) -- the pattern
      exists, the handoff just never reads it.
      (a) THE COUNTS RIDE THE RUN LOG, NOT THE CONSOLE. A remediation batch opens a
      LoaderRunLog (drydocs_core/run_log.py) and closes it with its counts in the summary,
      the same contract the loaders already use; the handoff is rendered FROM that recorded
      summary. Console output is a view of the run, never its record.
      (b) NUMBERS ARE RECORDED, NOT RECOMPUTED AT RENDER TIME. HandoffPackage carries the
      run's counts as data; render_handoff stays a pure deterministic template (the module's
      existing house rule). A test proves the rendered body reports the recorded numbers even
      when the finding list handed to the renderer is a subset -- otherwise the explanation
      silently re-derives a denominator from whatever survived filtering.
      (c) WHAT THE SECTION CARRIES, at minimum: objects examined, objects changed, findings
      by rule id and by severity, the ratified / unratified split (a warn-only finding must
      not inflate a fix count), and anything skipped WITH ITS REASON -- unparseable is
      counted, never dropped.
      (d) ABSENCE IS EXPLICIT. A package assembled without a run summary renders the section
      as not recorded rather than omitting it, so a missing measurement never reads as a
      clean run.
      (e) IT DOES NOT RESTATE THE EQUIVALENCE PROOF. proven / compared jobs stay in their own
      section with the three-valued PASS / DIVERGED / NOT PROVEN verdict; coverage counts what
      the run looked at, the proof states what it demonstrated, and merging them would let a
      big denominator read as evidence.
      Zero graph writes. No new relationship type, no vocabulary entry, no gate.
    notes: >
      Groomed 2026-08-12 (targeted groom) from Idea-113, user direction the same day. Verified
      at the groom: render_handoff has no counts section, and nothing under drydocs_remediation/
      imports run_log at all today (drydocs/cli.py is the only LoaderRunLog caller) -- so the
      batch-side run log is part of this item, not an assumed prerequisite.
      MODULE: drydocs-remediation, chosen because both change points (jira.py, the batch entry
      point) are there; drydocs_core/run_log.py is ADOPTED unchanged. If the run-log contract
      itself needs a field it does not have, that is a drydocs-core follow-up, not this item.
      WHY IT MATTERS BEYOND TIDINESS: this is the support -> dev handoff of the runbook
      automation use case. The ticket is where a support engineer's evidence becomes a
      developer's instruction, and an explanation without counts cannot be audited later
      against the run that produced it.

  - id: G94
    epic: component-topology
    title: "Which standard does a job validate against? A selection resolver on job type, job role and ETL engine — and an executable proof that the DD version digit never selects one"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [G84]
    inputs:
      - drydocs_core/orchestration/controlm/description_tokens.py
      - drydocs_core/orchestration/shell.py
      - config/launcher-registry.yaml
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - tests/unit/test_description_tokens.py
    acceptance: >
      Today the token set is selected by ONE dimension: required_tokens(JobType) over
      FILE_WATCHER / PUBLISHER / BOTH. User direction 2026-08-12 rules the real selection a
      DECISION TREE -- a FileWatcher validates against the FileWatcher standard; a COMMAND job
      selects on its ETL ENGINE FIRST (DPL, Ab Initio, Informatica -- names the launcher
      registry already classifies as invocation_type DPL / ABINITIO / INFORMATICA) and falls
      back to a GENERIC standard carrying only the shared tokens (the DevX project key, the
      EMAIL_DL contacts) when no engine is identified.
      (a) ONE FUNCTION, ONE ANSWER: a selector takes the derived TASKTYPE, the registered
      JOB_ROLE token and the launcher classification, and returns a STANDARD IDENTITY plus the
      token set that identity requires. Selection is separated from validation -- validate()
      keeps taking what it is given, so the tree can be re-ruled without touching the parser.
      (b) THE GUARDRAIL BECOMES A TEST, which is the point of the item: guidelines section 7.5
      and G84 clause (c) rule the DD digit a grammar VERSION that MUST NOT select a template or
      a standard. A test asserts that the SAME job under "DD1|" and under a hypothetical "DD2|"
      selects the SAME standard, so the next person who reaches for the version slot as a
      selector reds a guard instead of shipping it.
      (c) THE ENGINE BRANCH IS DECLARED, NOT INVENTED. Per-engine standards get their identity
      and their branch; the CONTENT of each engine's token set is NOT ruled here -- an engine
      with no ruled set inherits the generic set and REPORTS that it did, so an unruled engine
      is visible rather than silently generic. Inventing per-engine required tokens in this
      item is out of scope and fails it.
      (d) THE GENERIC SET IS DERIVED, NOT RETYPED: it is the shared subset already carried in
      the registry (JobType.BOTH plus the FOLDER_VARIABLES entries), so it cannot drift from
      TOKEN_REGISTRY.
      (e) UNSELECTABLE IS AN ANSWER. A job whose engine cannot be classified and whose job role
      is absent returns the generic standard WITH a reason, and coverage counts the reasons --
      the adoption-not-compliance measure G84 established.
      Zero graph writes; every token stays proposed; no relationship-vocabulary entry and no
      property term is touched. Storage of the selected standard is explicitly NOT here -- see
      G95.
    notes: >
      Groomed 2026-08-12 (targeted groom) from Idea-114, clause 1 (the decision tree). Verified
      at the groom: config/launcher-registry.yaml already emits DPL / ABINITIO / INFORMATICA
      invocation types through classify_executable(), so the engine dimension needs no new
      classifier; JOB_ROLES is already the C30 discriminator set (PUBLISHER / PLACEMENT /
      TRUST_INGEST); and required_tokens() is currently keyed on JobType alone, which is the
      gap. depends_on G84 because the selector must read the population and the version the
      sentinel parse produces, and G84 is where both were built.
      WHY THIS HALF IS BUILDABLE AND THE OTHER HALF IS NOT: choosing WHICH standard applies is
      derivable from data the repo already has. Choosing WHERE a standard lives, what its id
      is, and whether a team may hold its own profile is a contract change to the parse
      contract -- that is G95, and it goes to the gate first.

  - id: G95
    epic: component-topology
    title: "Draft the gate prompt for standard IDENTITY and its carrier: can a validation profile be stored per team, and what may legitimately select a standard?"
    type: task
    module: config
    phase: 6
    agent: main
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - drydocs_core/orchestration/controlm/description_tokens.py
      - knowledge/standards/technology/controlm-guidelines-and-standards.md
      - config/taxonomy/lob-product-team.yaml
      - docs/restructure/03-hitl-sme-flow.md
      - config/gate-log.md
    acceptance: >
      A gate prompt is DRAFTED -- drafting decides nothing (the G27 / W1 / N10 / G61
      precedent), nothing is written to the graph, and no entry leaves `planned` until the
      SME signs. The prompt must put four questions, each with the evidence already in the
      repo:
      (a) IDENTITY: does a standard get a stable id, and what is its shape? Today the
      per-job-type sets are CODE-AS-DATA in TOKEN_REGISTRY, whose own docstring calls itself
      "the single source both the parser and the published register read from, so code and
      document cannot drift" -- guarded by the registry-vs-standard agreement test. Giving a
      standard an id outside that module is a CONTRACT CHANGE and the prompt must say so
      plainly, with what the agreement test would then be guarding.
      (b) CARRIER: user direction 2026-08-12 proposes a config table (SQLite was floated) keyed
      by standard id, holding a validation profile BY TEAM, with a platform / product hierarchy
      later. The prompt lays the candidate carriers side by side -- versioned YAML under
      config/ (the launcher-registry precedent: schema-pinned, test-guarded, teams add rows
      without a code change) versus a database table -- against what each costs at review time
      and at port time, and asks the SME to rule. It does not pre-pick one.
      (c) WHAT MAY SELECT A STANDARD, restated as a fence rather than assumed: section 7.5 and
      G84 rule the DD digit a grammar VERSION and forbid it selecting a template or a standard,
      so a standard id needs its OWN carrier. The prompt asks the SME to ratify that fence
      explicitly, because a per-team registry is exactly the pressure that would break it.
      (d) DIVERGENCE POLICY: if a team may hold its own profile, what may it change? Adding a
      team-local token is the C16 annotation case the parser already knows; RELAXING a required
      token of the company standard is a governance decision, and the prompt must ask whether
      that is permitted at all, and if so who ratifies it.
      Every term or entry the prompt proposes is registered `planned` and stays planned until
      the gate signs. The deliverable is the prompt file plus its gate-log stub -- no code, no
      registry, no schema.
    notes: >
      Groomed 2026-08-12 (targeted groom) from Idea-114, clause 2 (identity + storage). Split
      from G94 deliberately: the selection tree is derivable from data the repo already has,
      while identity and carrier change the parse contract and are gate-relevant, which the
      inbox entry itself says. Promoting the DRAFT rather than the build is the standing
      pattern -- a prompt decides nothing, and it puts the question in front of the SME with
      the evidence attached instead of leaving it as an inbox line nobody can act on.
      MODULE: config, chosen because the artifact under discussion is a registry / profile
      store and config/ is the declared home for precedence, registries and crosswalks (the
      launcher-registry.yaml precedent is the closest existing shape). The prompt DESCRIBES a
      change to a drydocs-core module; it does not make one, so it is not filed there.
      RELATED: G94 (the selector this would eventually feed), G77 (a THEME token inside the
      same DD1 block), the etlprocess-kind-enum rider (engine vocabulary, which is where the
      engine names would have to agree if per-engine standards get ids).

  - id: G96
    epic: component-topology
    title: "Control-M API-call framework the deploy/pull shell scripts can call — generic .sh, config-resolved in/out directories, calls discovered from the reference corpus (folder, job, calendar)"
    type: task
    module: drydocs-core
    phase: 6
    agent: main
    model: opus
    priority: p2
    status: done   # 2026-08-13 (desktop): BUILT — drydocs_core/adapters/controlm/ (api.py call surface: folder/job/variable/calendar + condition seam; config templates for corpus-gap syntax; exit-3 reported capability gaps; README + API-CALLS.md discovery reference; sample cfg mechanism-only; 15 tests). The .sh wrappers + filled cfg stay company-side by design
    depends_on: []
    inputs:
      - drydocs_core/adapters/
      - drydocs_core/data_root.py
      - external/orchestration/bmc-controlm/controlm-api-folder-reference.md
      - external/orchestration/bmc-controlm/controlm-api-job-properties.md
      - external/orchestration/bmc-controlm/controlm-api-connection-profiles.md
      - external/orchestration/bmc-controlm/controlm-api-installation.md
      - external/orchestration/bmc-controlm/controlm-calendars.md
      - config/classification.yaml
    acceptance: >
      A producer-side API-call framework the Control-M shell scripts can call, built
      MECHANISM-ONLY (the scripts themselves — controlm_common.sh, controlm_deploy_lite.sh,
      controlm_folder_pull.sh with a filled controlm_deploy.cfg — are company-side; verified
      2026-08-12 that this tree carries no .sh at the root and no adapters/scripts/
      directory, so what lands here is the framework and the generic sample, never the
      instance).
      (a) FIRST, THE NOTE, before any code: record the convention that keeps the .sh
      GENERIC — an internal shell-script home and a directory that stores the API artifacts,
      with the INPUT and OUTPUT directories resolved from CONFIG, never hardcoded. The
      producer mechanism for that convention already exists and is named in the note:
      drydocs_core/data_root.py (remediation_incoming_dir / remediation_outgoing_dir /
      controlm_xml_dir under DRYDOCS_DATA_ROOT, logs under DRYDOCS_LOGDIR), with the
      company-side script home recorded as the vendor-namespaced scripts directory beside
      the adapters and the filled config staying untracked (sample committed, real values
      never). The note lives where the next builder looks: the framework module docstring
      plus a README beside the sample config.
      (b) THE FRAMEWORK: a Python call surface under drydocs_core/adapters/ that a generic
      wrapper .sh can invoke per operation, taking its endpoint/profile from config and its
      in/out directories from the data-root conventions, returning machine-readable results
      the scripts can test. Connection-profile handling is mechanism-only: shape from the
      corpus doc, no real endpoints, hostnames or credentials anywhere in the tree
      (classification boundary; secrets discipline).
      (c) THE DISCOVERY, in the item because the user put it there: search the knowledge
      graph — and where the graph does not yet hold the vendor corpus, the corpus files
      directly — for EXAMPLE API CALLS usable for FOLDER, JOB and CALENDAR operations, and
      write the candidates up as a short mechanism reference (operation, call shape, corpus
      source) that the framework's per-object surface is then built against. The corpus
      already carries controlm-api-folder-reference.md, controlm-api-job-properties.md and
      controlm-calendars.md plus the connection-profile and installation pages, so the
      search has ground truth to cite.
      (d) VERSION GUARDRAIL, so the framework does not assume an API the company does not
      run: the company orchestrator is 9.0.21.300 XML-first (the JSON/SaaS API files were
      demoted to conceptual reference once that was established), so the discovery step must
      state per candidate call whether it is available at that version, and the framework's
      contract must degrade honestly — an unavailable call is a reported capability gap,
      not a silent fallback.
      Tests cover the config resolution (in/out dirs from data-root, no repo-tree writes),
      the generic-wrapper contract, and the discovery reference existing with per-call
      version notes. Zero graph writes.
    notes: >
      Added STRAIGHT to the backlog by user direction 2026-08-12 ("add this straight to the
      backlog if possible G96") — the inbox was deliberately skipped, so this entry carries
      its own capture context. The driving scenario: the company-side remediation deploy/pull
      scripts (folder pull to the remediation incoming zone, deploy of fixed XML from the
      outgoing zone) should call an API surface instead of growing bespoke logic per script,
      and the .sh layer stays generic because the config supplies the directories. The
      company session of the same day planned the scripts' home (vendor-namespaced scripts
      directory beside the adapters, out-of-repo landing zones, sample cfg committed and the
      filled cfg ignored) — that plan is company-side; this item is its producer mechanism
      twin, on the standing mechanism-not-instance sanitization rule. MODULE drydocs-core on
      the company plan's own placement reasoning: both lineage and remediation consume it,
      and shell scripts do not trip the Python import boundary. RELATED: G86 (calendar
      validate/render spike — the calendar half of the discovery feeds it), G93 (the run-log
      counts the deploy flow should log), the controlm-runbook-automation skill (the
      support→dev fix package this deploy path serves). PORT CARE: nothing here ports on its
      own; the company side keeps its scripts, the producer keeps the framework, and the
      sample cfg must never acquire real values on either side.

  - id: U21
    epic: self-documentation
    title: "IMPORTS edges are never retracted, so fan-in inflates permanently and test debt under-reports — the code-snapshot load sweeps nodes but not edges"
    type: bug
    module: drydocs-load
    phase: 16
    agent: main
    model: sonnet
    priority: p1
    status: todo
    depends_on: []
    inputs:
      - drydocs/loaders/code_snapshot.py
      - drydocs/loaders/cypher/code_snapshot.cypher
      - drydocs/loaders/base.py
      - drydocs/loaders/cypher/stale_edge_cleanup.cypher
      - knowledge/depgraph-snapshots/
      - .claude/skills/tech-debt/SKILL.md
    acceptance: >
      After a `drydocs load-code-snapshot` run, the graph's IMPORTS edges EQUAL the loaded
      snapshot's IMPORTS edges for every module that snapshot CONTAINED. Today they do not,
      and the edge only ever grows.
      THE DEFECT, reproduced 2026-08-13 (desktop, neo4jtest, drydocs DB) against the graph
      freshly reloaded from that day's snapshot: `drydocs/loaders/seal_attribution.py` still
      carries an IMPORTS edge to `drydocs/loaders/base.py`; the file contains ZERO occurrences
      of the string "base" (K8 removed the import at 4df4df2) and the snapshot records no such
      edge. The edge SURVIVED a full re-run, so this is not staleness and a refresh does not
      fix it. Size at that reading: 985 live IMPORTS edges against 982 in the snapshot -- three
      ghosts, one of which put `loaders/base.py` at fan-in 32 where the tree says 31, i.e. it
      distorts the repo's most-cited change-risk metric. A5 is affected in the DANGEROUS
      direction: a module whose test import was DELETED keeps the ghost edge and still counts
      as tested, so test debt reads better than it is.
      THE FIX.
      (a) PER-SOURCE RETRACTION, NEVER A GLOBAL DELETE. For each module the loaded snapshot
      INCLUDED, the edges this loader wrote that the snapshot no longer asserts are retracted;
      edges touching a module the snapshot did NOT include are untouched, and edges written by
      any other loader are untouched. The scoping discipline already exists one file away --
      base.py's D7 mark pass refuses to run without either `full_extract` or a scope property,
      logging why -- and this clause is that discipline applied to relationships.
      (b) DECIDE AND RECORD: marked or deleted. The node half MARKS (removed_from_source_at +
      removed_by_run_id) rather than deleting, which is what makes U13's "every query pack
      filters tombstones" rule necessary. Whichever this item picks, it states the reason, and
      if edges are marked then the read paths that consume IMPORTS (the tech-debt skill's A3/A5
      queries, the review-plan seed queries) filter them the same way U13 made node queries
      filter -- a tombstoned edge that no query excludes is the same defect wearing a property.
      (c) THE COUNT IS REPORTED, never silent: edges retracted (and reactivated, if marked)
      appear in the load summary beside nodes_marked_removed, per the STG_PARSE_QUALITY house
      rule the node pass already follows.
      (d) GUARDS, both of them: loading snapshot A (edge present) then snapshot B (edge absent
      for a module B contains) leaves no live edge; and loading a snapshot that OMITS a module
      entirely leaves that module's edges alone. The first proves the sweep works, the second
      proves it cannot over-reach.
      (e) VERIFY ON THE REAL GRAPH at close, venue named per J18: live IMPORTS count equals the
      newest snapshot's, the seal_attribution -> base ghost is gone, and the A3 top fan-in is
      RE-MEASURED rather than re-typed.
    notes: >
      Groomed 2026-08-13 (targeted groom -- node labels and Cypher loaders) from Idea-118.
      SAME FAMILY AS U13, one grain down: the D7 sweep tombstones removed MODULES and nothing
      sweeps removed EDGES, so the import graph is append-only by construction. The repo holds
      exactly one edge-retraction precedent -- `drydocs/loaders/cypher/stale_edge_cleanup.cypher`
      (D1: delete condition edges for changed jobs before re-asserting them) -- and a grep of
      the tracked tree on 2026-08-13 finds NO live caller for it, so the pattern exists as a
      file rather than as a mechanism. Worth reading it anyway: it is the shape (scope to the
      keys this run touched, delete, re-assert) this item generalizes.
      MODULE drydocs-load: the change is in the loader and its Cypher, not in the depgraph
      instrument -- the snapshot is CORRECT, the load is what never subtracts.
      PAIRS WITH U22, found in the same session and from the same cross-check (the loaded graph
      vs the snapshot it was loaded from): U22 detects a load that never RAN, this fixes a load
      that runs and never RETRACTS. Neither substitutes for the other -- U22's freshness check
      would have reported this graph as current, because it was.

  - id: U22
    epic: self-documentation
    title: "The code graph can go stale for weeks and nothing says so: compare the graph's newest last_seen_at against the newest snapshot's captured_at"
    type: bug
    module: graph-infra
    phase: 16
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs/loaders/code_snapshot.py
      - knowledge/depgraph-snapshots/README.md
      - knowledge/depgraph-snapshots/snapshot.ps1
      - tests/unit/test_code_graph_review_plan.py
      - .claude/skills/tech-debt/SKILL.md
    acceptance: >
      THE INCIDENT, 2026-08-13 (desktop, neo4jtest, drydocs DB): every :CodeModule carried
      `last_seen_at = 2026-08-02T23:06:42Z` from ONE run id -- loaded once on Aug 2, never
      refreshed, ELEVEN DAYS. The session read A3's top fan-in as 28, which is the Aug-2 value
      (the tech-debt skill's own baselines run 28 -> 29 on 08-04 -> 31 on 08-09), and it looked
      current. Same class as G78: not a failed read, a read that SUCCEEDED with the wrong data,
      sitting underneath every architecture and debt decision. `drydocs load-code-snapshot`
      repairs it in one command; nothing COMPARES the graph against the snapshot series.
      (a) THE COMPARISON EXISTS AS CODE, not as a habit: max(:CodeModule.last_seen_at) in the
      drydocs database against the newest `knowledge/depgraph-snapshots/drydocs-*.json`
      meta.captured_at, reporting the drift, the run id that last loaded, and WHICH snapshot it
      compared -- so "the graph is current" is falsifiable rather than assumed.
      (b) WARN, NOT FAIL -- ruled at this groom, and the reason is the argument CLAUDE.md
      already makes for snapshot.ps1's CI check: recording repo structure and passing a gate are
      unrelated jobs, and this check must never block a suite run or a snapshot on a machine
      with no database. It surfaces where the wrong number was actually consumed -- the
      tech-debt skill's read path -- and may ALSO ride the session ritual beside the CI check;
      the item states where it put it and why.
      (c) THE UNIT-TESTABLE HALF IS THE COMPARISON, not the freshness: a test drives it over
      fixtures -- fresh, stale, no snapshot on disk, empty graph, database unreachable -- and
      asserts each verdict, so the mechanism is guarded on a machine with no database.
      tests/unit/test_code_graph_review_plan.py is the offered home (it already fails when the
      typed package allow-list and pyproject.toml disagree, so the shape is there); a different
      home is fine with the reason written down.
      (d) DATABASE-UNREACHABLE IS A DISTINCT VERDICT, never "fresh". The failure this item
      exists to stop is a check that reports green because it could not look.
      OUT OF SCOPE, deliberately: auto-running the load. A stale graph is REPORTED, never
      silently refreshed -- a check that repairs what it measures can never be trusted to have
      measured anything.
    notes: >
      Groomed 2026-08-13 (targeted groom -- node labels and Cypher loaders) from Idea-117, which
      explicitly left the warn-vs-fail call to the groom. RULED WARN in clause (b); it is
      reversible at the item if the builder finds a fail-safe home, but the default must not be
      a red suite on a laptop with no container running.
      MODULE graph-infra rather than docs, chosen on SUBJECT and recorded here because both were
      plausible: the fact under test is the loaded graph's currency against the snapshot series
      (U15 / U19's family), not the review plan's prose (U13 / U20's). The likely code home
      straddles them -- a unit test file the docs items also touch -- which is exactly why the
      subject and not the file decided it.
      PRIORITY p2, not the entry's High: the condition is repaired by one existing command and
      was repaired the day it was found, so what is missing is DETECTION rather than capability.
      U21 is the p1 of the pair because it needs a fix nothing in the repo can do today.
      Idea-120 (a metrics JSONL beside each snapshot) stays inboxed and is NOT this item -- a
      trend ledger answers "is it getting better", this answers "is what I am reading current".

  - id: U23
    epic: self-documentation
    title: "Nothing joins a loader to the Cypher it executes — draft the gate rider for a loader-to-cypher-file edge, the .cypher nodes that carry zero edges"
    type: task
    module: ontology
    phase: 16
    agent: ontology-mapper
    model: fable
    priority: p3
    status: todo
    depends_on: []
    inputs:
      - config/gate-prompts/self-documentation-code-graph.yaml
      - drydocs_core/ontology/relationship_vocabulary/
      - config/taxonomy-ontology-map/
      - docs/RELATIONSHIP_GUIDE.md
      - docs/restructure/03-hitl-sme-flow.md
      - drydocs/loaders/
      - knowledge/depgraph-snapshots/README.md
    acceptance: >
      A gate rider is DRAFTED and nothing else. Drafting decides nothing (the G27 / W1 / N10 /
      G95 precedent): no vocabulary entry leaves `planned`, no edge is written, and if the gate
      does not sit, the drafted rider IS the deliverable. VIA THE GATE is the whole point -- a
      new relationship type is an ontology decision under CLAUDE.md section 6, never an import
      convenience. It carries four things.
      (a) THE FACT, RE-MEASURED rather than copied from the 2026-08-02 note that raised it:
      `drydocs/loaders/` holds .py loaders, their .cypher files and their .sql side by side; the
      .cypher files are :CodeModule nodes with NO edges at all, while each loader names its
      Cypher as a literal path in Python (the CYPHER_DIR / "<name>.cypher" idiom). The prompt
      states the current counts and the date it counted them.
      (b) THE EDGE QUESTION, put as a question: what does the edge MEAN -- "executes",
      "reads as a resource", something narrower -- and does the .sql beside it carry the same
      edge or a different one? Direction, cardinality, and whether one label with a role
      property beats two labels (the documented RUNS_ON-overload risk is the standing worked
      example on this side).
      (c) THE EMITTER, because the edge has nowhere to come from today: the depgraph scanner
      does not emit it, so the prompt says where it would -- a static read of the CYPHER_DIR
      literal, in the instrument (the sibling repo, which means an expected_commit bump) or in
      the loader that reads the snapshot -- and what each costs. This is the clause that stops
      the rider being signed with no path to a first row.
      (d) WHAT IT UNLOCKS AND WHAT IT DOES NOT, stated plainly so the item is never judged
      against a promise it did not make: gate `self-documentation-code-graph` section H5 already
      records that :CodeModule to the OPERATIONAL graph -- "which module loads this Control-M
      job" -- is NOT delivered by that gate and needs a second item, precisely because the
      module-to-label mapping lives in the loaders' Cypher rather than in their imports. A
      loader-to-cypher edge is the FIRST half of that join and not the join; the prompt says so.
    notes: >
      Groomed 2026-08-13 (targeted groom -- node labels and Cypher loaders) from Idea-47, which
      had been parked with "gate section H5's named future item" as its trigger. Promoted as a
      DRAFT and only a draft: the entry's own two blockers are unchanged -- the instrument emits
      nothing and the loader could not load the edge if it did -- so what is groomable is the
      QUESTION, put to the SME with the evidence attached, rather than a build that would have
      no input.
      MODULE ontology: the deliverable is a relationship-vocabulary decision. The emitter change
      the rider scopes is drydocs-load / graph-infra work and belongs to whatever item the gate
      produces, not to this one.
      p3 on the entry's own Low priority -- the nodes have sat edgeless since 2026-08-02 with no
      consequence beyond an unanswerable query, and the H5 question it feeds is not scheduled.

  - id: G97
    epic: component-topology
    title: "The m7 payload build: launcher and payload stop sharing the INVOKES fold — USES_ARTIFACT edges plus script_role and the artifact properties on :Script"
    type: task
    module: drydocs-lineage
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_lineage/writer.py
      - drydocs_core/ontology/relationship_vocabulary/40-local-scheduler.yaml
      - drydocs_core/schema/constraints.cypher
      - config/gate-log.md
      - config/launcher-registry.yaml
      - tests/unit/test_lineage_writer.py
    acceptance: >
      NO GATE IS OPENED OR REOPENED -- this builds what two signed gates already ruled.
      cmdline-nfr-vetting (2026-07-21, SME-2 / SME-3) ruled the DISTINCT label rather than a
      single label with a role, and adopted the :Script refinements with it; rua-load-shapes
      (2026-08-07, 28/28, applied at G55) ACTIVATED m3_invokes and m7_uses_artifact in the same
      breath, on the stated reasoning that activating INVOKES alone would land payloads in the
      1..n fold first and force an in-graph migration. G16's own notes name this item: "the m7
      build (payload migration to USES_ARTIFACT, script_role stamping) is its own follow-up item
      once the writer endpoint work lands."
      (a) THE SPLIT: the writer emits USES_ARTIFACT from the job to the PAYLOAD artifact the
      launcher dispatches, while INVOKES keeps the LAUNCHER only. The discrimination comes from
      the G16 FACT_REGISTRY canonicals (ETL_ARTIFACT_URI / ETL_ARTIFACT_KIND / ETL_ARTIFACT_SHA
      and the ETL_PLATFORM pair) under the aliases-suggest-values-decide contract, plus the G15
      launcher properties -- NEVER from a variable NAME alone, which is the 2,384-variable gap
      analysis' one durable finding: names lie.
      (b) :Script GAINS script_role {launcher, payload} and the gate-adopted artifact properties
      (platform / artifact_uri / artifact_kind / platform_flags / script_path). The property
      already exists -- the writer stamps script_role='profile' for the rua profile case (G23,
      section C2) -- so this EXTENDS a value set rather than inventing a property. Script
      identity stays PATH-keyed; that ruling is not touched.
      (c) ENDPOINT CLASS PER EDGE: to_node is Script | ETLProcess per rua-load-shapes section
      B2 -- one edge meaning, two endpoint classes, the endpoint RECORDED on the edge -- with
      the raw evidence string (the command line or variable form that produced it) kept VERBATIM
      per section B3, which is what keeps the endpoint choice re-checkable later.
      (d) EXISTING GRAPHS: state the position rather than assume a clean load. Where a live
      graph already folded payloads into a 1..n INVOKES set, either an idempotent re-load
      (MERGE-keyed) or a named migration step covers it -- whichever, it is written down and the
      counts are reported. A silent double-representation (payload on both labels) is the one
      outcome that must be impossible.
      (e) COUNTS AND A WARN STREAM: invocations classified launcher, classified payload, and
      UNCLASSIFIED. An unclassified invocation stays exactly where it is today and is reported;
      it is never promoted to a payload on a guess.
      TESTS: a fixture command line carrying both a launcher and a payload yields exactly one
      INVOKES and one USES_ARTIFACT edge with script_role stamped on both endpoints; a
      payload-less command line yields no USES_ARTIFACT edge at all; the vocabulary guard
      (write_curated raises GateBoundVocabularyError on a non-active entry) still holds. Full
      suite green.
    notes: >
      Groomed 2026-08-13 (targeted groom -- node labels and Cypher loaders) from Idea-23, whose
      parked trigger -- "once the writer's ETLProcess endpoint work makes the edge landable" --
      has FIRED, verified two ways at the groom rather than assumed. (1) m7_uses_artifact is
      `status: active` in drydocs_core/ontology/relationship_vocabulary/40-local-scheduler.yaml,
      activated 2026-08-07, where the inbox entry recorded it as `planned`. (2)
      drydocs_lineage/writer.py MERGEs :ETLProcess on its token and carries the union-endpoint
      machinery, so the endpoint class the edge needs exists.
      ALSO VERIFIED ABSENT, so this is a real build and not a re-close: the writer emits NO
      USES_ARTIFACT edge today and stamps script_role only for the rua profile case -- a grep of
      writer.py on 2026-08-13 returns the profile stamp and nothing else.
      MODULE drydocs-lineage: the change point is the writer's plan/write surface. The G14 / G16
      extractor facts it consumes are already produced; core's parser is CALLED, not modified.
      RELATED: G23 (the curated rua load this extends), G55 (the activation), G16 (the fact
      feed), G12 (which already lands INVOKES on :ETLProcess for the wrapper-payload expansion
      and is the precedent for clause (c)).

  - id: G98
    epic: component-topology
    title: "The corporate-backbone gate prompt: :Company and both HAS_BUSINESS_SEGMENT* edges execute, are constrained and are live in the graph, but were never registered in the relationship vocabulary"
    type: task
    module: ontology
    phase: 6
    agent: ontology-mapper
    model: opus
    priority: p2
    status: done   # 2026-08-17 (laptop): gate corporate-backbone-vocabulary SIGNED OFF 19/19; consequences applied — :Company registered org:FormalOrganization, new 49-local-corporate.yaml with both edges status:planned, the §D3 endpoint guard built (found 8 unregistered endpoint labels + CAN_ACT_AS, carried as declared debt), the §B2 agreement check in m3-verify
    depends_on: []
    inputs:
      - drydocs_core/schema/ontology.cypher
      - drydocs_core/schema/constraints.cypher
      - drydocs_core/ontology/relationship_vocabulary/10-node-classifications.yaml
      - drydocs_core/ontology/relationship_vocabulary/42-local-catalog.yaml
      - config/taxonomy-ontology-map/30-mappings-catalog.yaml
      - config/doc-source-registry.yaml
      - docs/RELATIONSHIP_GUIDE.md
      - config/gate-log.md
    acceptance: >
      config/gate-prompts/corporate-backbone-vocabulary.yaml drafts the rulings as
      confirmations to be SIGNED, never pre-decided. GATE-BOUND: drafting and rendering
      this page registers nothing.
      THE FINDING IT EXISTS FOR: (:Company {name:"JPMC"})-[:HAS_BUSINESS_SEGMENT]->
      (:BusinessSegment) is MERGEd by ontology.cypher:205-232, carries a uniqueness
      constraint (constraints.cypher:29), is documented as THE corporate hierarchy across
      four .claude/skills/data-context-extractor/ files, and is live in the graph — while
      10-node-classifications.yaml registers BusinessSegment and CatalogLOB and NOT
      Company, and no fragment registers HAS_BUSINESS_SEGMENT or its _HISTORICAL twin.
      NOT a regression: git log -S "Company" over the vocabulary returns nothing, so it
      is an M0 seed that predates the registry — there is no prior ruling to honour or
      overturn, which is exactly why it needs one.
      (a) THE LABEL: does :Company enter 10-node-classifications.yaml, and under which
      class? BusinessSegment is already org:FormalOrganization / prov Agent, and the
      note there records that the doc said FormalOrganization while the supplement
      comment said org:Organization — the same choice recurs here and the clause must
      state both readings rather than inherit one.
      (b) THE TWO EDGES: HAS_BUSINESS_SEGMENT and HAS_BUSINESS_SEGMENT_HISTORICAL enter
      as status: planned per RELATIONSHIP_GUIDE.md. The clause must ask whether the
      effective-dated pair is TWO edge types or ONE type discriminated by effective_to,
      because the seed already writes both shapes and the vocabulary would ratify
      whichever it is told.
      (c) THE SINGLETON QUESTION, which is why this is not purely clerical: :Company is
      a one-node class in a repo built to be generic and portable to another company
      (ADR 0012's standalone-generalization goal). Rule whether the label is right at
      all, or whether the segment backbone should hang off an existing org class — and if
      :Company stays, whether "JPMC" is seed data or configuration.
      (d) THE GUARD, separable from the ruling and buildable WITHOUT it: nothing
      cross-checks an edge's from_node/to_node against the registered label set —
      test_taxonomy_ontology_map.py checks label UNIQUENESS and test_yaml_fragments.py
      checks fragment KEYS, so a wholly absent endpoint raises nothing and RECONCILES_TO
      passes only because its endpoint happens to be registered. Record that the guard
      does not wait on this page.
      (e) PUBLISH BOUNDARY, ruled 2026-08-17 and recorded so it is not re-litigated: the
      source is External-PUBLIC (config/doc-source-registry.yaml#jpmc-reports —
      classification: External, public SEC filings / IR PDFs, source_url present), so the
      JPMC literals in ontology.cypher are publishable. This clause CONFIRMS that reading
      rather than reopening it; clause (c) is about portability, not sensitivity.
      NOTHING enters the vocabulary active at this gate, and nothing is transcribed to
      config/gate-log.md until ticked.

  - id: G99
    epic: ontology-mapping
    title: "Rewrite pat_team_roles.cypher onto qualified attribution — the G91 entry-5 consequence; the loader is fenced do-not-run until it lands"
    type: task
    module: drydocs-load
    phase: 6
    agent: main
    model: sonnet
    priority: p2
    status: done   # 2026-08-18 (laptop): loader rewritten onto qualified attribution (attribution_id on the SEAL subject|FAMILY|role|person idiom), both entries flipped planned -> active, DO-NOT-RUN banner removed, membership_id constraint dropped with its last writer (53 -> 52)
    depends_on: []
    inputs:
      - drydocs/loaders/cypher/pat_team_roles.cypher
      - drydocs/loaders/catalog.py
      - drydocs_core/ontology/relationship_vocabulary/42-local-catalog.yaml
      - drydocs_core/schema/catalog_ontology_supplement.cypher
      - drydocs_core/schema/constraints.cypher
      - config/gate-log.md
    acceptance: >
      G91 (2026-08-18) re-shaped the DevTeam staffing leg onto qualified attribution and
      DEPRECATED catalog_dev_team_has_membership. pat_team_roles.cypher still writes the
      retired shape and carries a DO-NOT-RUN banner; this item makes it match the ruling.
      (a) THE NEW SHAPE: DevTeam -[:QUALIFIED_ATTRIBUTION]-> (:Attribution) with
      -[:HAD_ROLE]-> (:Role) and -[:HAS_AGENT]-> (:Employee). The HAS_AGENT hop REUSES
      seal_attribution_has_agent — do NOT mint a catalog twin, the C8 meta-graph refuses
      duplicate (from, label, to) triples and schema_graph.py:248 raises on it.
      (b) attribution_id needs a deterministic key on the same idiom as the SEAL and Cabinet
      loaders, so a re-run is idempotent; the estate is truncate-and-reload, so the key must
      be derivable from the row, never from a counter.
      (c) FLIP catalog_dev_team_qualified_attribution and catalog_dev_team_attribution_had_role
      planned -> active WITH their supplement refs in the same change (the flips-are-follow-ups
      pattern), and remove the DO-NOT-RUN banner in that commit and no earlier.
      (d) RULE :Membership's fate. It has no live writer once this lands. Either drop the
      membership_id constraint WITH the loader change, or record why it stays. Do not drop it
      ahead of the loader — that is the S3 ordering trap run backwards, and constraints.cypher
      already carries that caution.
      (e) The canonical :Role rows the catalog supplement seeds are the register the HAD_ROLE
      leg resolves against; confirm the PAT engineering-role spellings against that register
      rather than inventing values at load.
    notes: >
      Raised 2026-08-18 at the G91 close. Not a gate item — the ontology is ruled; this is the
      build that makes the loader agree with it. Until it lands the loader is inert BY INTENT,
      which is a deliberate state and not a bug to be "fixed" by running it.

  - id: Q19
    epic: docmeta
    title: "Re-run the P0 retrieval benchmark with a real persona in the loop — the 12/12 was scored with the author as text2cypher stand-in AND judge"
    type: task
    module: docs
    phase: 14
    agent: main
    model: opus
    priority: p2   # SME asked for the item 2026-08-18; priority proposed, confirm at pull
    status: todo
    depends_on: []
    inputs:
      - knowledge/upgrade-plans/docmeta-p0-verdict.md
      - knowledge/upgrade-plans/p0-benchmark/benchmark_p0.py
      - knowledge/upgrade-plans/p0-benchmark/benchmark_p0_results.json
      - docs/design/graph-retrieval-benchmark-explainer.md
      - config/doc-source-registry.yaml
    acceptance: >
      SME direction 2026-08-18: "the accuracy was 100% in the graph, but in practice our own
      personas are not getting that close based on the last few days of gotchas." The P0 record
      (verdict Q3, commit 0e036ff era) reports traversal 12/12 WITH TWO DISCLOSED CAVEATS that
      are now the live question: the author hand-wrote the Cypher (text2cypher stand-in) and the
      author judged the answers, with 2 adjudication corrections.
      (a) SAME 12 questions, SAME corpus — bmc-docs, whose id was retained at the 2026-08-18
      corpus-id rename precisely so this record stays citable — but the Cypher is generated by
      an AGENT PERSONA at run time, never hand-written; the persona sees the schema and the
      question, nothing else.
      (b) GRADING IS INDEPENDENT of whoever generated the queries (a second session or a
      mechanical scorer with the original regex ground truths; adjudications recorded, never
      silently applied — the P0 record's own discipline).
      (c) PUBLISH THE DELTAS against the original per-question table (recall, tokens, latency),
      in the same explainer family; a persona score BELOW 12/12 is the expected and honest
      outcome, not a failure of the run — the point is to measure the gap the stand-in hid.
      (d) CITE THE MOTIVATING GOTCHAS so the item survives its context: the Idea-128 GitNexus
      lesson (a tool claiming epistemic `exact` with impactedCount=1 while grep found missed
      call sites) and the recent persona misses the SME observed. The benchmark's job is to make
      that gap a NUMBER.
      (e) SCOPE FENCES: O31 (regenerate benchmarkData.ts from a real harness) is RELATED, NOT a
      dependency — its own notes say the P0 script is not a reusable harness; do not block on
      it, and do not build the harness here. And GRADES.md:169's re-run prerequisite belongs to
      the OTHER experiment (graph-vs-files, code graph) — do not conflate the two.
    notes: >
      Direct add 2026-08-18 (SME direction at the corpus-naming plan; the C9 precedent). The
      original test is the reason bmc-docs kept its name in the id-grammar rename — this item is
      the other half of that decision: keep the record citable, then re-test its headline claim
      under honest conditions.

  - id: G100
    epic: ontology-mapping
    title: "ITSM technician-group gate: ratify the SnowRole register (SENG / ASUP) and the ServiceNow staffing attribution, then build the OOTB lookup"
    type: task
    module: ontology
    phase: 6
    agent: ontology-mapper
    model: opus
    priority: p2
    status: todo
    depends_on: []
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/50-local-itsm.yaml
      - drydocs_core/ontology/relationship_vocabulary/10-node-classifications.yaml
      - config/gate-log.md
      - docs/RELATIONSHIP_GUIDE.md
    acceptance: >
      The vocabulary half LANDED 2026-08-18 on SME direction (G100 registration commit):
      :ServiceNowGroup + :SnowRole node classes, itsm_group_qualified_attribution and
      itsm_attribution_had_role, all status: planned, in a new `itsm` domain. This item is
      the GATE that ratifies them plus the build behind it.
      (a) RATIFY THE REGISTER as a closed set on the K5 fixed-scheme precedent: SME direction
      is developers -> SENG, production support -> ASUP. Rule whether those two are the whole
      set or a seed, and whether the tier numbering that pairs with them in the estate's group
      naming is modelled at all producer-side (it is deliberately NOT asserted today).
      (b) CONFIRM THE ONE-SPINE RULING: technicians resolve to :Employee on the SID, NOT to a
      second person class. The company model reaches people through :Worker {sid}; the producer
      does not mint that. This is what makes "which roles does this person hold across PAT,
      SEAL and ITSM" one traversal instead of three.
      (c) THE HAS_AGENT HOP IS REUSED, never twinned (C8 refuses duplicate (from,label,to);
      schema_graph.py raises). Confirm rather than re-derive.
      (d) FENCE HOLDS: the group -> application link (SUPPORTED_BY / HAS_SUPPORT_QUEUE /
      RESOLVED_BY) belongs to the company's SIGNED snow-hpsm-queue-to-group gate. This family
      models WHO STAFFS A GROUP only. Do not re-mint a competing group->app shape — the trap
      G98 named when it warned G35 off the same thing.
      (e) THE BUILD, after sign-off: an OOTB lookup over sys_user_group / sys_user_grmember /
      sys_user (no custom table), resolving members to :Employee by SID and emitting the
      attribution triple. Entries flip planned -> active WITH their supplement in the same
      change (flips-are-follow-ups).
      Nothing loads before sign-off; SnowRole concepts stay proposed.
    notes: >
      SOURCE NOTE 2026-08-18 (SME), and it changes what clause (e) builds against: the
      ServiceNow extracts are being re-sourced INTERNALLY from hand-pulled CSV/YAML onto SQL
      over the Snowflake replica views. So the lookup here targets the SOURCED feed —
      snow@[db].[schema].<servicenow_table> per the grammar the `snow` system row already
      rules — not a CSV shape that is being retired underneath it. Name the dataset for the
      ServiceNow TABLE, never the V_-prefixed view wrapper (same row). Row models do not
      move; the SQL aliases columns onto existing field names. Full note: Idea-132, which
      also carries the two NET-NEW areas (per-CI TOM responsibilities, incidents) that are
      NOT part of this gate and need their own.
      Raised 2026-08-18 alongside the registration, at the G91 close. FOURTH family on the
      qualified-attribution pattern (SEAL K4, PAT products K5, PAT dev teams G91, ITSM here) —
      the shape is settled, so this gate rules the REGISTER and the identity join, not the
      modelling pattern. The SME's stated use case is a support team reaching applications
      through PEOPLE (members holding an operate-manager role) rather than through a develops
      edge, which is why the single :Employee spine is the load-bearing part.

  - id: G102
    epic: component-topology
    title: "Execute the G32 fold — ADR 0011 clause-1 guards FIRST, then the one-database build: registry re-target, watermark re-key on trust_default, DESCRIBES delete+reload, sweep rewrite"
    type: task
    module: graph-infra
    phase: 6
    agent: main
    model: opus
    priority: p1
    status: done   # 2026-08-18 (laptop): guards -> fold -> live apply, all in one session. Guards first per the gate (structural exclusion 30/30 sites, write-boundary allowlist of two, audit spec expected-0); topology {drydocs, ddschema}; watermark re-keyed on declaration + per-row trust_default (B-DURABLE); all 7 rows re-targeted; R3 delete+reload VERIFIED LIVE (laptop, neo4jtest, drydocs DB: 27->0->27 DESCRIBES, 374 chunks, :Uncertain=0); the stranded book copy caught by the transitional sweep, reloaded (1 doc/43 chunks), ddcontext+ddall DROPPED on this machine; docs-verify 2 loaded/1 missing/4 unshaped clean; m3-verify all green. COMPANY-SIDE OWES: the same per-machine sequence (R3 + reload + drop), and cli --database default fix rides the port
    depends_on: []
    inputs:
      - config/gate-log.md
      - docs/decisions/0011-single-database-contingency.md
      - config/doc-source-registry.yaml
      - drydocs_api/query_specs.py
      - drydocs_api/exports.py
      - drydocs_core/schema/provisioning/01_databases.cypher
      - drydocs/cli.py
      - tests/unit/test_database_names.py
      - tests/unit/test_load_map_json.py
    acceptance: >
      Gate document-content-topology SIGNED 32/32 on 2026-08-18; this item is the apply, and
      its ORDER is the ruling's hard precondition — the guards land and pass BEFORE anything
      folds. (a) GUARDS FIRST (ADR 0011 clause 1): the QuerySpec ground-truth exclusion
      generated STRUCTURALLY into the registry (never 30 hand edits), the deepdoc writer
      boundary (every uncertain write carries :Uncertain — the writer is a 17-line stub, so
      the discipline is built in before the first write), and the live audit spec counting
      :Uncertain nodes reachable from ground-truth traversals, expected 0. (b) THE FOLD:
      ddcontext's four corpora re-home to the one database; ddschema does NOT fold (clause 2
      never fires, deliberate); provisioning + dev-environment + test_database_names.py match
      the ruled set; ddall's fate recorded (one constituent = retire or keep as alias, say
      which). (c) B-DURABLE: WATERMARK re-keys on each source's declared trust_default per
      row — WATERMARKED_DATABASES has no subject after the fold, so this is forced, and the
      ddall latent instance dies in the same change. docs-verify's wrong-db check re-points
      at the realm label rather than silently retiring. (d) REGISTRY: all seven target_db
      values land on the one database (final corpus names per the 2026-08-18 id grammar);
      cli.DOC_SWEEP_DATABASES rewrites; traversable-until-move retires from the docs-coverage
      ladder. (e) R3 IN ONE SESSION PER MACHINE: delete the 27 DESCRIBES edges and reload
      bmc-docs in the same session (J18 — per-machine graphs; the company side repeats it).
      (f) test_load_map_json's pinned not-queryable set {dddocs} FAILS at this apply BY
      DESIGN — treat it as the task it is: the set becomes empty, and softwareModel.ts
      inGraphLabel + the docs-coverage detail lines stop saying "not provisioned (G32)".
      Full suite green.
    notes: >
      Staged at the G32 sign-off. GUARDS-FIRST is not a preference: the gate recorded the
      physics-to-discipline loss as accepted ONLY on the precondition that the three clause-1
      guards precede the fold. The docmeta P4 revision (knowledge/upgrade-plans/
      docmeta-p4-revision-single-db.md) already drafts the single-db load path and stays
      gate-bound on ITS own items — do not conflate its embedder work with this apply.

  - id: Q20
    epic: docmeta
    title: "Q16 clause (b) successor — check FIRST whether the live DESCRIBES edges already satisfy the product->documentation pointer under the fold"
    type: task
    module: docs
    phase: 14
    agent: main
    model: sonnet
    priority: p3
    status: done   # 2026-08-18 (laptop): SATISFIED-BY-FOLD, recorded in config/gate-log.md same date — the traversal exists live (27 DESCRIBES docs from product controlm, verified laptop/neo4jtest/drydocs); the DECLARATION half stays Q18's (describes_product field; G32 done, Q14 still owed); no pointer build needed, nothing new minted
    depends_on: [G102]
    inputs:
      - config/gate-log.md
      - drydocs/docs_coverage.py
      - drydocs/loaders/cypher/bmc_docs.cypher
    acceptance: >
      Minted at the G32 sign-off (gate Q7/Q16(b)): after G102 lands, verify whether the
      reloaded (:Document)-[:DESCRIBES]->(:SoftwareProduct) edges already give an agent the
      product->documentation traversal Q16 clause (b) wanted — Q16's own "cheapest possible
      answer, check FIRST". If yes: record it in the gate log as satisfied-by-fold and close.
      If no: re-scope the pointer as a build item THEN, with the check's findings as its
      inputs. Ends the prose-only trace open since Q16 closed partial on 2026-08-07.
    notes: >
      Depends on G102 because the check is meaningless before the delete+reload lands the
      edges under the ruled shape.

  - id: Q18
    epic: docmeta
    title: "The one loaded software-to-docs edge has no registry declaration behind it — a describes_product field the loader READS instead of a hardcoded constant"
    type: task
    module: drydocs-load
    phase: 14
    agent: main
    model: sonnet
    priority: p2
    status: todo
    depends_on: [G32, Q14]
    inputs:
      - drydocs/loaders/bmc_docs.py
      - drydocs/loaders/cypher/bmc_docs.cypher
      - config/doc-source-registry.yaml
      - config/taxonomy/software-registry.yaml
      - tests/unit/test_doc_registry.py
    acceptance: >
      THE GAP: the live (:Document)-[:DESCRIBES]->(:SoftwareProduct) edges for `controlm` are
      asserted by a Python module constant -- `SUBJECT_PRODUCT_ID = "controlm"` in
      drydocs/loaders/bmc_docs.py -- while the corpus's doc-source-registry entry carries only
      `taxonomy_path`, which no file maps to a product id. So the ONE working software-to-docs
      traversal in the estate is unreproducible from the ledger, and a report cannot honestly
      infer the declaration (the /software page refuses to, deliberately -- O56 honesty rule 4).
      (a) doc-source-registry entries may carry `describes_product`, OPTIONAL because not every
      corpus describes a product, and the bmc-docs entry declares its product id.
      (b) The loader READS the registry. The constant is REMOVED, not shadowed -- a fallback
      constant beside a declared field is how the two silently disagree later.
      (c) A GUARD fails when `describes_product` names an id that is not a product in
      config/taxonomy/software-registry.yaml. This is the missing half today and the same
      "declared, and nothing checks it is reachable" family as G80 and J47.
      (d) THE EDGE TERM IS NOT DECIDED HERE. Q14 rules which term carries a documented entity;
      whatever it rules is what this loads, VIA THE GATE. If Q14 keeps DESCRIBES, this item is a
      declaration change only and writes no new edge type.
      (e) RESIDENCY IS NOT DECIDED HERE EITHER. G32 rules which database the doc corpora live
      in, and a Neo4j relationship cannot span databases -- so WHERE this edge can be written
      follows that ruling. The item states the consequence for the bmc-docs corpus rather than
      picking a database.
      (f) Q16 CLAUSE (b) LANDS HERE OR IS RESTATED: Q16 closed PARTIAL with clause (b) -- the
      pointer reaching the GRAPH -- explicitly unshipped and blocked behind Q14 behind G32. This
      item either closes it or says in writing what is still missing.
    notes: >
      Groomed 2026-08-13 (targeted groom -- node labels and Cypher loaders) from Idea-88, which
      had been parked because it touches a loader AND a gated corpus behind two open rulings.
      Promoted WITHOUT overriding that parking: depends_on [G32, Q14] reproduces it exactly, so
      the item stays out of next_ready until both rule, while the work itself becomes visible in
      the database instead of living only in an inbox file. This is the difference between
      filing a blocked item and pre-empting a gate.
      MODULE drydocs-load (matching Q16, its sibling): the change is a loader reading a registry
      field. The registry SCHEMA edit is config work the same item carries, as Q5's ledger items
      already do.
      WHAT IS ALREADY TRUE AND MUST NOT BE UNDONE: the DESCRIBES join is an OPTIONAL MATCH inside
      a FOREACH guard, and the loader already refuses when the software registry is absent from
      the database entirely and counts documents that landed with no edge. That is the honest
      behaviour this item feeds a declared id INTO -- do not replace those checks with a required
      match.

  - id: N12
    epic: source-column-mappings
    title: "Declare every registry dataset's ACQUISITION PATH: manual drops name their format + landing-zone directory, automated pulls name api-or-db"
    type: task
    module: config
    phase: 11
    agent: pipeline-config
    model: sonnet
    priority: p1   # raised from the groom's proposed p2 — SME confirm 2026-08-18, close-out directed
    status: done   # 2026-08-18 (laptop): gate follow-through: all 29 dataset rows classified (15 automated/db, 14 manual incl. the archive enum widening for the rua bundle), schema declaration + 3 enforcing tests; commit a10b0191
    depends_on: []
    inputs:
      - config/source-registry.yaml
      - config/schemas/source-registry.schema.json
      - tests/unit/test_source_registry.py
      - drydocs_core/source_registry.py
      - drydocs_core/data_root.py
    acceptance: >
      Every DATASET row in config/source-registry.yaml carries a first-class `acquisition:` block
      stating HOW the data arrives, replacing today's smear across three half-fields that do not
      compose into an answer (`adapter` -- csv/oracle/yaml/markdown/json, 8 rows carrying `~`;
      `connector` -- doc corpora only; `locator` -- free-shape, each source inventing its own key).
      (a) `mode: manual | automated` on EVERY dataset row, including the `adapter: ~` ones: the
      build CLASSIFIES the existing rows, it does not add the field only to new ones. That per-row
      classification IS the payoff -- "what is still hand-fed?" becomes a query instead of an audit.
      (b) MANUAL rows carry `format: csv | ascii | json` plus `drop_dir:`, whose COMMITTED value is
      the landing-zone CONVENTION -- DRYDOCS_DATA_ROOT-relative, the `controlm-xml/` precedent,
      resolved by drydocs_core/data_root.py. The real directory lives ONLY in the internal twin
      (the `locator.extract: ~` discipline, unchanged): no real path, host or share name is ever
      committed (Scan D / J27 class).
      (c) AUTOMATED rows carry `via: api | db` plus the pull's coordinates BY REFERENCE, never
      restated -- for `db` the row's own dataset id already names the qualified object
      ({origin}@{db}.{schema}.{table}); for `api` the row names the CALL SURFACE, with the G96
      Control-M adapter (drydocs_core/adapters/controlm/api.py) as the worked example.
      (d) ENFORCEMENT IS THE TEST, NOT THE SCHEMA. config/schemas/source-registry.schema.json is
      shape-only and permissive by design (S6 -- no `additionalProperties: false`), so a block added
      to the YAML alone would validate silently. The schema DECLARES the block (enums + the
      conditional required keys) and tests/unit/test_source_registry.py is what FAILS: every
      dataset row has `acquisition`; manual implies `format` + `drop_dir`; automated implies `via`;
      and a committed `drop_dir` that is absolute or otherwise escapes the landing-zone convention
      fails the boundary check.
      (e) SCOPE FENCE -- config/source-registry.yaml DATASET rows only. SYSTEM rows are unchanged,
      and the doc corpora in config/doc-source-registry.yaml are OUT of scope here (they already
      carry `connector:`); extending the field to that ledger is a separate call.
      (f) `mode: manual` IS THE EXPECTED FIRST STATE, NEVER A DEFECT (SME framing 2026-08-18):
      every new source has an exploratory phase -- profiling, ontology work, mapping, trial Neo4j
      loads -- and a .csv or .json file is the natural manual loader for it. The field RECORDS where
      a source sits in that lifecycle (manual exploratory -> automated productionized), so no test,
      report or render may present `manual` as a violation or as debt to be eliminated.
      (g) TWO FENCES. This item RECORDS mode -- it builds NO watcher, mover or copier for the drop
      directories. And it does NOT rule the FLIP (who flips manual -> automated, on what evidence,
      and how that relates to the override -> source-corrected flip): that ruling is N13's, via the
      gate. Full suite green.
    notes: >
      Groomed 2026-08-18 from Idea-133 (SME ask the same day), with the exploratory-phase framing
      supplied in-session. Idea-132 is the live driver: the ServiceNow extracts are moving
      hand-pulled-CSV -> SQL over the replica, and TODAY that transition has no field to flip -- it
      shows up only as prose in `notes:`. Idea-115 caught this class once already (the rua copy
      path existed only as a derived expression; the fix was to make both ends name each other).
      MODULE/PHASE/EPIC follow N10, which pins exactly this combination for registry-schema work
      (the v2 registry is N7/N9's artifact); agent pipeline-config per its config layer.
      HITL-FREE BY CONSTRUCTION: this adds a config field recording an operational fact -- no edge
      is created, no relationship meaning changes -- and the field itself is SME-directed
      (2026-08-18), the J23 pre-decided-ruling precedent. THREE AXES, ONE ROW, ALL DIFFERENT:
      `confirmed` = semantic gate sign-off; the wired/ready flag N10 proposes = pipeline-wiring
      readiness; `acquisition.mode` = how the data physically arrives. If N10's gate signs, the two
      fields COMPOSE on the same row -- neither subsumes the other -- and whichever lands second
      says so in the registry header. Follow-on deliberately out of scope: giving the load-map and
      console surfaces an honest manual-vs-automated lens once the field exists.

  - id: N13
    epic: source-column-mappings
    title: "Rule the pending-source-correction lifecycle flip ONCE: draft the gate prompt covering both the acquisition manual->automated flip and the override->source-corrected flip"
    type: task
    module: config
    phase: 11
    agent: main
    model: fable
    priority: p1   # raised from the groom's proposed p2 — SME confirm 2026-08-18, close-out directed
    status: done   # 2026-08-18 (laptop): gate pending-source-correction SIGNED 12/12 same session (one vocabulary; two-hands override retirement; age-ordered no-deadline union report; K7 §E2 exemption confirmed with the D2 boundary rule); prompt + sign-off transcribed
    depends_on: []
    inputs:
      - config/gate-prompts/
      - config/gate-log.md
      - config/overrides/seal-contact-overrides.csv
      - drydocs_api/mappings.py
      - config/source-registry.yaml
      - docs/restructure/03-hitl-sme-flow.md
    acceptance: >
      A gate prompt is DRAFTED (G27 / W1 / U11 / N10 precedent -- drafting decides nothing) for ONE
      ruling covering BOTH flips, because they are the same lifecycle shape: (i) ACQUISITION -- a
      source recorded `acquisition.mode: manual` (N12) becomes `automated` when the pull is built;
      (ii) OVERRIDE -- a steward correction carried because the SOURCE is wrong (O24's SEAL L1/L2
      operate-manager overrides: origin-flagged source-vs-user rows feeding a source-corrections
      report to the AO who holds the privilege to fix SEAL) clears when the source is actually
      corrected.
      THE HOLE THE PROMPT NAMES: the K7 gate ruled §E2 "overrides may be PERMANENT in this domain
      ... do NOT implement the corrected-in-source flip here; it belongs to the domains where
      permanence is temporary" (config/gate-log.md; K9's acceptance repeats it) -- and NO ITEM OWNS
      THAT FLIP TODAY. O24 likewise left "whether overrides ever influence graph-side attribution"
      explicitly out of scope as a gate question.
      THE PROMPT PUTS TO THE SME: (a) whether ONE shared vocabulary covers both -- a
      placeholder/pending flag meaning "we model this correctly ourselves while the source is wrong
      or hand-fed" -- carrying the same always-visible origin discipline O24 established; (b) what
      EVIDENCE flips it in each domain and WHO may flip it (a steward action, a load observing the
      corrected source value, or a build landing); (c) whether an unflipped placeholder is REPORTED
      (the source-corrections report is the precedent) and when it becomes stale; (d) which domains
      are permanent-by-nature and therefore exempt per §E2 -- the folder-to-application platform-code
      case is the SME's own worked example of permanence.
      NOTHING CHANGES UNTIL THE GATE SIGNS: this item builds no registry field, no store column and
      no report, and N12 is unaffected -- N12 records mode and mints no pending-correction flag. The
      prompt lands in config/gate-prompts/ in the house shape; the sign-off is an SME session.
    notes: >
      Groomed 2026-08-18 as N12's sibling, on SME direction that the acquisition manual->automated
      flip and the override->source-corrected flip are ONE lifecycle shape and should be ruled once,
      not twice. Raised as a SIBLING rather than folded into N12 as a clause on purpose: folding it
      would make a buildable config item undeliverable without an SME session, and K9 deferred this
      flip AT a gate (§E2), so it comes back at one. Fable: the ruling sets a boundary spanning the
      source registry (config) and the steward mapping store (drydocs-api) and decides what a flag
      MEANS in both. Module config / phase 11 / epic N because the artifact is a
      config/gate-prompts/ prompt and the registry is the anchor; Epic K was the alternative and was
      rejected -- the ruling is cross-domain, not SEAL-attribution build work. Dependency-free by
      design: the prompt can be drafted before or after N12 lands.

  - id: N14
    epic: source-column-mappings
    title: "The pending-source-correction UNION REPORT: one age-ordered list of every live placeholder — the override rows awaiting a source fix and the registry rows still hand-fed"
    type: task
    module: drydocs-api
    phase: 11
    agent: main
    model: sonnet
    priority: p1   # directed at dispatch 2026-08-18 — SME-confirmed, not proposed
    status: todo
    depends_on: [N12, N13, O24]
    inputs:
      - drydocs_api/mappings.py
      - drydocs_core/mapping_store.py
      - config/overrides/seal-contact-overrides.csv
      - config/source-registry.yaml
      - drydocs_core/source_registry.py
      - config/gate-log.md
    acceptance: >
      ONE report class lists every LIVE placeholder across BOTH domains the signed
      pending-source-correction gate joined (config/gate-log.md 2026-08-18, SIGNED 12/12; N13).
      (a) BOTH DOMAINS, ONE REPORT (gate §A1 -- a single `pending_source_correction` vocabulary;
      two domain-local flags were offered and declined): the ACTIVE seal-contact override rows
      (`seal_contact_override.status = 'active'`, the v_source_corrections feed O24 built) AND
      every config/source-registry.yaml dataset row whose `acquisition.mode` is `manual` (N12).
      One class and one query answer "what are we carrying?" -- not two reports printed side by
      side.
      (b) ORDERED BY AGE (§C1), with the age BASIS recorded per domain because it is asymmetric:
      override rows already carry `authored_on`; a manual registry row carries NO dated field
      today, so this build PICKS one basis and documents it (a declared `acquisition.since:`
      written when a row is classified, or the row's first-classified commit date) and applies it
      uniformly. Rows with no determinable age sort into an explicit bucket, never silently.
      (c) NO CLOCK, NO PRESSURE (§C2): no deadline, no SLA, no alerting, no per-row review_by
      dates -- per-row clocks were offered at the gate and DECLINED, because they manufacture
      urgency the exploratory-phase framing says is false. The report is read at the SME's cadence.
      (d) NEVER A GATE (§C3): it never gates a load, never blocks CI, never fails a test -- a
      placeholder is recorded state, not an error. And per N12 clause (f), `acquisition.mode:
      manual` is the EXPECTED first state of a source: no column header, wording or styling may
      present a manual row as a defect, as debt, or as a violation.
      (e) THE EXEMPTION IS ENFORCED, NOT MERELY DOCUMENTED (§D1 + K7 §E2): permanent-by-nature
      domains are EXCLUDED -- concretely the steward-defined app-code mapping store
      (`app_code_mapping`, whose DDL already carries no status column precisely because its
      overrides may be permanent). A test asserts an app_code_mapping row can never appear. Per
      §D2 the report reads each store's DECLARED pending-vs-permanent property rather than
      hardcoding the one exemption, so a new store declares its nature at creation.
      (f) SCOPE FENCE (§A3): config and stores ONLY. Nothing graph-side expresses pending-ness --
      no node, property or edge -- because that would be a new RELATIONSHIP_GUIDE proposal and its
      own gate. The report writes neither source of record.
      (g) SURFACE: it ships in the O24 report family -- a read-only drydocs_api endpoint returning
      the exportable artifact (markdown + manifest fields, the source_corrections_report shape). A
      console pane is a FOLLOW-ON and is not required here. Tests cover both domains appearing,
      the age ordering, the exemption, and the empty case. Full suite green.
    notes: >
      Groomed 2026-08-18 from Idea-134 -- one of the two builds the signed gate authorized but did
      not build (its own terminus clause: "this gate rules the LIFECYCLE; it builds nothing").
      MODULE/PHASE/EPIC, choice recorded: the code lands in drydocs-api because that is where the
      mapping store's read endpoints and the O24 report already live, but the ITEM stays in Epic N
      / phase 11 with the ruling that authorizes it (N13) and the registry half it reads (N12) --
      the registry is Epic N's anchor, and filing the report in Epic O would separate the build
      from its gate. If a console pane follows, it rides /mappings the way O24's did (Epic O
      territory at that point). HITL status: NO new gate -- the lifecycle is ruled 12/12 and this
      item implements it, deciding nothing new; sonnet because the acceptance is a written
      pass/fail test over a signed ruling. Coordination, not a dependency: N15's retirement
      CANDIDATE flag is a natural extra column here, so whichever of N14/N15 lands second adds it.
      VERIFIED AT THE GROOM, not recalled: the clause anchors come from the signed gate entry in
      config/gate-log.md, and the app_code_mapping DDL comment plus the v_source_corrections view
      were read out of drydocs_core/mapping_store.py.

  - id: N15
    epic: source-column-mappings
    title: "Agreement-candidate detection: a load that sees the source finally match an override proposes retiring it for a steward to confirm — and can never retire it itself"
    type: task
    module: drydocs-load
    phase: 11
    agent: main
    model: sonnet
    priority: p1   # directed at dispatch 2026-08-18 — SME-confirmed, not proposed
    status: todo
    depends_on: [N12, N13, O24]
    inputs:
      - drydocs/loaders/seal_contacts.py
      - drydocs/loaders/base.py
      - drydocs_core/mapping_store.py
      - config/overrides/seal-contact-overrides.csv
      - drydocs_api/mappings.py
      - config/gate-log.md
    acceptance: >
      The second build the signed pending-source-correction gate authorized (config/gate-log.md
      2026-08-18, 12/12; N13): the load that consumes a source carrying an overridden value
      notices when the source has caught up.
      (a) FIRST CONCRETE SURFACE, NAMED: the seal-contact override domain --
      drydocs/loaders/seal_contacts.py reading the SEAL contact extract against the
      `seal_contact_override` rows O24 materializes from config/overrides/seal-contact-overrides.csv.
      When a loaded SEAL row's holder now EQUALS the override's corrected holder for the same
      (app_seal_id, role_name), that override is surfaced as a RETIREMENT CANDIDATE.
      (b) TWO HANDS, STRUCTURALLY (§B2/§B3): the load PROPOSES and a STEWARD confirms. It rides
      the draft mechanism that already exists rather than inventing a second one --
      drydocs_core.mapping_store.add_draft and the commit-by-replace override draft path -- so an
      unattended run can only ever leave an OPEN DRAFT. Auto-retire was offered at the gate and
      DECLINED (coincidental agreement must not silently retire a correction that was masking a
      different defect), so a test asserts a detection run leaves every
      `seal_contact_override.status` value untouched.
      (c) HISTORY IS KEPT (§B4): confirming a candidate ARCHIVES the override row dated with its
      agreement evidence (which load run, which source value, observed when) -- the row is never
      deleted and the correction stays readable afterwards. Today's status CHECK is
      ('active','corrected-in-seal'); extending that enum and/or the evidence columns is part of
      this item and rides the committed-CSV -> mapping.db materialization, so the committed list
      stays the source of record and the store stays rebuildable.
      (d) FENCES: the detection writes NO graph (§A3; O24's rule that overrides never write the
      graph is unchanged), never fails a load, never blocks CI, and never treats "no candidates"
      as an error (§C3 class). The K7 §E2 permanent-by-nature domains are not scanned at all --
      the app-code mapping store has no corrected-in-source lifecycle BY RULING (§D1).
      (e) SCOPE FENCE ON UI: this item builds the detection, the proposal record and the read path
      a surface can consume. Rendering the candidate flag on the /mappings pane or as a column in
      N14's union report is a FOLLOW-ON, not this item. Tests cover agreement detected,
      disagreement ignored, an already corrected-in-seal row skipped, and the no-flip assertion.
      Full suite green.
    notes: >
      Groomed 2026-08-18 from Idea-134 as N14's sibling. TWO ITEMS, NOT ONE, because the gate's
      two authorized builds land in different modules -- a report/export surface versus loader-side
      detection -- and each is independently startable; neither blocks the other, and both are p1
      per the dispatch. MODULE, choice recorded: drydocs-load because the detection owns a RUN
      CADENCE (it happens during a load), which is the MODULE_MAP placement test; the store schema
      and its helpers land in drydocs_core/mapping_store.py where the store already lives, and any
      endpoint or pane exposure is drydocs-api / drydocs-web follow-on work. SONNET, NOT FABLE,
      deliberately: this DOES change a store schema, but the gate already made that decision
      (§B2/§B4, signed 12/12 2026-08-18) and the build implements a signed ruling against a written
      pass/fail test -- do not re-tier it on the schema-touch alone. HITL status: no new gate; the
      only thing that would need one is graph-side pending-ness, which §A3 fences out.
      VERIFIED AT THE GROOM, not recalled: the draft mechanism (add_draft plus the
      commit-by-replace override draft path), the status CHECK ('active','corrected-in-seal') and
      the thin seal_contacts loader shape were all read out of the tree.
  - id: G101
    epic: ontology-mapping
    title: "Tech-debt: migrate the seal_* vocabulary ids onto the domain-derived scheme — the family §B3's enumeration left out"
    type: chore
    module: ontology
    phase: 2
    agent: ontology-mapper
    model: sonnet
    priority: p3
    status: todo
    depends_on: [G87, G88]
    inputs:
      - drydocs_core/ontology/relationship_vocabulary/41-local-business-application.yaml
      - drydocs_core/ontology/relationship_vocabulary/00-header.yaml
      - config/taxonomy-ontology-map/20-mappings-seal.yaml
      - config/taxonomy-ontology-map/30-mappings-catalog.yaml
      - config/taxonomy-ontology-map/40-mappings-docs.yaml
      - config/precedence.yaml
      - drydocs_core/schema/ontology_supplement.cypher
      - drydocs_core/schema/schema_graph.cypher
    acceptance: >
      THE GAP: gate vocabulary-domains-and-id-policy §A2 renamed the domain seal ->
      business_application, and §B1 made the id prefix DOMAIN-DERIVED. §B3 then force-migrated
      the epoch-tag ids by explicit census -- m3_ 21, p2_ 5, m7_ 1, u1_/u2_ 5, g22_ 1, c23_ 4,
      prov_ 1, sosa_ 4 -- and that census does not contain seal_. So the 10 seal_* ids in
      41-local-business-application.yaml are neither epoch-tag (not in §B3) nor conformant to
      §B1: they carry a domain prefix for a domain that no longer exists under that name. G87
      migrates by that census and inherits the omission. This item closes it, and closes it
      the same way -- ADD-NEW + DEPRECATE-OLD, never a rename in place (§B3: ids are the join
      key across the taxonomy-ontology-map, the generated schema_graph.cypher, the SQLite
      mirror and the append-only gate log).
      SCOPE IS 5 OF THE 10, and the split is the work of this item, not a detail of it:
      (a) 4 need NOTHING and must be left alone -- seal_requires_scheduler, seal_has_membership,
      seal_of_role, seal_held_by are already status: deprecated. Add-new + deprecate-old is a
      no-op on an entry with nothing loading it: minting a replacement id for an edge that has
      no data invents a row whose only content is that it was never used. They stay as the
      audit trail their notes make them.
      (b) 1 is CARVED OUT -- seal_app_attributed_to_employee (planned) is claimed by the open
      seal-tom-attribution-reshape gate, which §C1c of the id-policy gate already deferred to
      rather than ruling over. Its id migrates with that gate's outcome or after it, never here.
      (c) 5 MIGRATE, and their target prefix is decided by WHERE G88 leaves them, which is why
      this item sits behind G88 and not just G87. seal_has_port and seal_had_primary_source are
      structural business-application edges and take business_application_*. The attribution
      family -- seal_qualified_attribution, seal_attribution_has_agent, seal_attribution_had_role
      -- is exactly the people/org family G88 relocates to domain: human, so it takes human_*
      IF G88 rules it across. Running this before G88 would mint business_application_* ids and
      then need a SECOND add-new + deprecate-old cycle on the same three edges the moment the
      domain moves, which is the double-touch G88's own acceptance exists to avoid.
      DONE MEANS: each of the 5 has a new domain-derived id, its old entry status deprecated
      with a note naming the replacement; the structural vocab_id joins repoint -- the
      qualified_form.vocab_id / C7 entry-level list in 30-mappings-catalog.yaml that names
      seal_attribution_has_agent, the 20-mappings-seal.yaml entries, the 40-mappings-docs.yaml
      seal_had_primary_source pointer, and the config/precedence.yaml seal_had_primary_source
      reference; ontology_supplement.cypher and schema_graph.cypher regenerate; the SQLite
      mirror rebuilds; web/src/generated/enforcement-matrix.json regenerates; the port guard
      stays green (no id vanishes). PROSE references do NOT change: gate-prompts/, config/
      gate-log.md, docs/reviews/ and the note: fields in 42-local-catalog.yaml,
      45-local-docs.yaml and 50-local-itsm.yaml cite the old spellings as PRECEDENT and are
      history -- rewriting them would edit the record the gate log is append-only to protect.
    notes: >
      Raised 2026-08-18 from a prefix census of the vocabulary fragments run against §B3's own
      enumeration. The census matches the gate exactly on all eight epoch tags (21/5/1/5/1/4/1/4),
      so the vocabulary has not drifted since 2026-08-12 -- which is what makes the seal_ omission
      an omission rather than drift. Found while confirming for the user that the m3_ -> scheduler_
      rename was already backlogged; it is, as G87, and this is the sibling gap that read of G87
      surfaced.
      WHY p3 AND NOT p2 LIKE G87: G87 is the join-key migration that 42 ids and the lineage
      writer's hardcoded VOCAB_IDS table depend on, and every new entry written under an epoch
      tag grows it. This is 5 ids, none of them referenced by drydocs_lineage/model.py or
      writer.py (checked -- no seal_ pins there), and it cannot start until G87 and G88 land
      anyway. Small, blocked, and it does not compound.
      PREFIX IS business_application_*, NOT an abbreviation. The one entry in the fragment that
      already conforms is business_application_port_has_contact_point, and the whole point of
      "domain-derived" (§B1) is that the prefix is mechanically the `domain:` field -- an
      abbreviation makes it a memorised lookup and reintroduces precisely the opacity m3_ had.
      REJECTED ALTERNATIVE: a short bus_app_* prefix. It is 12 characters cheaper on ids that
      reach 43 characters, and that is a real cost, but it would (i) contradict §B1 of a SIGNED
      gate, (ii) make the one already-conformant entry non-conformant, and (iii) break the
      derivation rule for a saving on a string nobody types -- ids are join keys read by
      machines, not identifiers typed at a prompt. If the length is genuinely the problem the
      honest fix is to rename the DOMAIN (fragment filename, all 11 entries' domain: field, the
      header enum, §A2's ruling) so derivation still holds -- a bigger gate change, not a
      prefix preference, and not this item.
      REJECTED ALTERNATIVE: fold into G87. Defensible -- same mechanism, adjacent entries -- and
      it is the cheaper path if all three run in one sitting. Kept separate because G87's scope
      is a SIGNED census and quietly widening it past what §B3 enumerated would make the item
      stop matching the ruling it cites, which is the audit trail. This item names the gap as a
      gap instead.

summary:
  todo: 124  # 2026-08-19 (laptop): -3 Q9+Q10+Q11 batch-claimed. Prior: 2026-08-18 (laptop): -1 G31 claimed. Prior: 2026-08-18 (laptop): -1 Q20 claimed in_progress (recomputed from items). Prior: 2026-08-18 (laptop): -1 G102 claimed in_progress. Prior: 2026-08-18 (laptop): +2 G102 (the fold apply, guards first, p1) + Q20 (the Q16(b) check-first, behind G102). Prior: 2026-08-18 (laptop): count corrected +1 — the Q19 add incremented from a stale base (the G101 PR's +1 had landed between reads); recomputed from items, the step-134 rule. Prior: 2026-08-18 (desktop, direct add): +1 — G101, migrate the 5 live seal_* vocabulary ids onto the domain-derived scheme. Gate vocabulary-domains-and-id-policy §A2 renamed the domain seal -> business_application and §B1 made the prefix domain-derived, but §B3's forced-migration census (m3_ 21, p2_ 5, m7_ 1, u1_/u2_ 5, g22_ 1, c23_ 4, prov_ 1, sosa_ 4) does not list seal_ — so those ids are neither epoch-tag nor conformant, and G87 inherits the omission. Scope is 5 of 10: 4 are already deprecated and need nothing (add-new+deprecate-old is a no-op with no data behind it), 1 is carved out to the open seal-tom-attribution-reshape gate per §C1c. depends_on [G87, G88] because G88 decides whether the attribution family lands in domain human, and that decides its prefix — running ahead of it forces a second add-new+deprecate-old cycle on the same three edges. Rejected a short bus_app_* prefix: contradicts §B1, unconforms the one already-conformant entry (business_application_port_has_contact_point), and trades the derivation rule for characters nobody types. SAME COMMIT, no count change: G87 title corrected to carry §B3's full nine-prefix census (prov_/sosa_ were missing) and bumped p3 -> p2 — it is the join-key migration 42 ids plus drydocs_lineage VOCAB_IDS ride on, it blocks G88 and now G101, and every entry written under an epoch tag meanwhile grows it. Count RECOMPUTED FROM ITEMS after a rebase onto the laptop's N14/N15 groom — never merged textually. Prior: 2026-08-18 (targeted groom, Idea-134): +2 — N14, the pending-source-correction UNION REPORT (one age-ordered list of every live placeholder: the active override rows plus the registry rows still carrying acquisition.mode: manual), and N15, AGREEMENT-CANDIDATE DETECTION (a load that sees the source finally match an override proposes retiring it; it can never perform the retirement itself). Both p1 at SME direction; both dependency-satisfied on arrival (N12, N13 and O24 are all done), so both enter next_ready. These are the two builds the signed pending-source-correction gate authorized and deliberately did not build. Prior: 2026-08-18 (laptop): -2 N12+N13 batch-claimed in_progress at p1 (SME close-out direction). Prior: 2026-08-18 (targeted groom, Idea-133): +2 — N12, the declared ACQUISITION PATH per registry dataset row (mode manual|automated; manual names format + landing-zone drop_dir, automated names via api|db by reference), and N13, the gate prompt that rules the pending-source-correction FLIP once across both domains — acquisition manual->automated and O24/K9's override->source-corrected, which K9 §E2 deferred to "the domains where permanence is temporary" and no item owned. Both dependency-free -> next_ready. Prior: 2026-08-18 (desktop, direct add): +1 — S12, the environment-drift guard: for every poetry.lock package also installed in the running interpreter, the versions must match, failing the session once with sys.prefix and the offenders named. Raised because poetry honours an inherited VIRTUAL_ENV ahead of the in-project venv, and the resulting ~20 red CLI tests are not real — click >= 8.3 against typer 0.12. p2 not p3: the same wrong env carries ruff 0.15.11 against the load-bearing exact 0.5.7 pin, so a format run from it lands a diff CI blocks — the J10 gate that already ran red for 100+ runs unnoticed. Guards DRIFT, not the venv path (rejected: passes a bad install in the right folder, fails a good one elsewhere). Follow-up to the PR #3 venv note. Prior: 2026-08-18 (desktop, direct add): +1 — S11, extract drydocs_plan/, drydocs_docgen/ and drydocs_port/ as first-party packages: the three components declared in `modules:` and enforced as logical groups by test_module_boundary.py are the only ones with no physical directory, so the guard carries by hand what a package root would carry by construction. Rejected drydocs_core as the destination — core is the DOMAIN core and an HTML renderer plus a headless-Chromium shell-out would spend the no-presentation-opinion guarantee that makes it importable everywhere. depends_on S8 so cli.py is a thin composition root before the files move. Raised from a module-ownership read of drydocs/, inbox skipped by user direction. Count RECOMPUTED FROM ITEMS after a concurrent-push rebase onto the laptop's G91/G99 run — never merged textually. Prior: 2026-08-18 (laptop): -1 G99 claimed in_progress. Prior: 2026-08-18 (laptop): +1 G100 — the ITSM technician-group gate (vocabulary registered same day on SME direction). Prior: 2026-08-18 (laptop): +1 G99 — the pat_team_roles rewrite, the G91 entry-5 consequence. Prior: 2026-08-18 (laptop): -1 G91 claimed in_progress — the held planned-entry review walk. Prior: 2026-08-17 (desktop, targeted groom — UI / UI-WIP inbox entries only): +3 — O60, O61, O62 (Idea-116/122/123, the three the 2026-08-13 groom deliberately left for a console-epic run). Prior: 2026-08-13 (desktop, targeted groom — node labels and Cypher loaders): +5 — U21, U22, U23, G97, Q18. Prior: 2026-08-13 (desktop): -1 G96 claimed in_progress — user shaped the build (api.py per-object call surface; .sh/config company-side). Prior: 2026-08-13 (desktop): -1 G62 claimed in_progress — the SME convened the rua-bundle data-profile session (USER-GATED START satisfied). Prior: 2026-08-12 (desktop, direct add): +1 G96 — the Control-M API-call framework the deploy/pull shell scripts call (generic .sh, config-resolved in/out dirs, folder/job/calendar calls discovered from the reference corpus; 9.0.21-availability guardrail). Added straight to the backlog by user direction, inbox skipped. Prior: 2026-08-12 (desktop, targeted groom Idea-112/113/114): +4 G92-G95 — G92 resolve the job's variable scope chain before the file-op parse (the G60 pre/post feed parses raw var_value, so %%R_PATH/out.dat and its resolved twin become two DataAssets), G93 carry the run's counts into the Jira fix-package explanation through the run-log contract, G94 the standard-selection decision tree (job type -> ETL engine -> generic fallback) with the DD-digit-never-selects guardrail as an executable test, G95 the gate prompt for standard identity + per-team carrier. Prior: 2026-08-12 (desktop, evening): -3 G60/G83/G84 claimed in_progress — G83+G84 batch-claimed together per their sibling notes (same module, same test file, one commit), G60 alongside as the same session's lineage pickup. Prior: 2026-08-12 (desktop): -1 J48 claimed in_progress — the Idea-109 residue sweep. Prior: 2026-08-12 (desktop, pm groom): +1 J48 — the Idea-109 residue: seventeen repo-root anchor sites still resolve on a raw Path(__file__), each needing a repo-content vs package-internal ruling. Prior: 2026-08-12 (desktop, vocab-ratification groom): +5 G87-G91 — the two gates' follow-ups: G87 forced id migration + G88 human-domain relocation + G89 m3_triggers QuerySpec conflict (tech-debt, gate §B3/§A3/§C3), G90 fix-tracking loader build (gate §C1), G91 rule the held planned-entry review file. Prior: 2026-08-12 (desktop, xml_io epic groom): +2 G85/G86 — the user's remediation follow-ups: G85 agent lookup beside the fix diff (vendor→standards→team over the verified BMC corpus; deterministic decides, agent cites), G86 calendar validate/render spike (RBC survives import verbatim now, so the question is finally askable). Both dependency-free -> next_ready. Prior: 2026-08-12 (desktop, groom): +1 K24 — two different questions are both numbered Q6 on the fid-identity-and-scope gate page (the SME answer landed 2026-08-12 and the older, still-open roll-up question), while four other files cite "Q6" by number; reproduced at the groom with a scan of every gate prompt, exactly one collision in the tree. Dependency-free -> next_ready. Prior: 2026-08-12 (LAPTOP): -1 J13 done — all four publish-ceiling value classes ruled (gate-log RECORD). Prior:  # 2026-08-12 (desktop, groom): +2 G83/G84 — the Control-M DESCRIPTION parse contract. G83 (p1 bug): C30 ruled the token set on 2026-08-11 and only the standards page moved, so a C30-conformant watcher description reports SEVEN false findings (FTS_ID/REC_ID unknown; ENV, both route ids and both EMAIL_DLs reported missing) — reproduced at the groom, sample-only. G84: the DD1| sentinel from Idea-105's SME resolution, which the parser today reports as an unparseable segment. Both dependency-free -> both enter next_ready; G77 gained a depends_on G84 because its clause (a) reads a token INSIDE that block. Prior: 2026-08-11 (LAPTOP): +1 G82 — the PAT team-report projection is missing, so the dev-team load has never run on real data. Prior: 2026-08-11 (desktop): +1 G81 — p0 DATA LOSS: the SME's internal folder of extracted CSV source data was OVERWRITTEN (recovering from backup). Paths are declared in Python, not config; no mode separates read from write; resolve_data_root() silently falls back to ~/data/DryDocs when the env var is unset. p0 on IRREVERSIBILITY — G78 is louder, this is worse. Prior: 2026-08-11 (desktop): +5 from the loader-redesign session — G78 (p0 BUG: a load step that cannot find its input silently skips, and a default run loads fixtures while reporting SUCCESS; reproduced BOTH sides), G79 (split refresh-reference by source + wire the two orphan loaders), G80 (the unchained-loader guard, the G59 shape), and NEW EPIC generic-naming: GN1 the ADR (claimed at raise) + GN2 the frozen rename. Prior: 2026-08-11 (desktop): +2 C34/G77 raised from the SME theme design session — C34 declares the EXISTING lob-product-team tree as a SKOS scheme (the hierarchy the SME proposed already exists, verified before raising) and drafts the dcat:theme gate incl. the two-database residency blocker; G77 puts the one vocabulary on both corpora. Only C34 is ready; G77 depends on it. Prior: 2026-08-11 (desktop, weekly groom): +10 J42-J47, C33, U20, K22, K23 raised from the inbox (Idea-96..Idea-107). Prior: 2026-08-11 (desktop): -2 G75+G76 batch-claimed (pushed before work — SME direction to take the dependency-free items; both land in drydocs_core/orchestration/controlm/ and share a test idiom, the K12+K14 batch precedent). THESE TWO WERE RAISED AS G70/G71 AND RENUMBERED: the laptop allocated G70-G74 at the G35 sign-off over ids this desktop had already pushed (dbb57e2), so the file briefly carried two different G70 and G71 items. The LAPTOP set keeps the ids because config/gate-log.md cites G73/G74 in a SIGNED-OFF gate record and a sign-off citation must not be falsified; the desktop pair moved. Prior (LAPTOP): 2026-08-11 (LAPTOP): +5 G70-G74 raised at the G35 sign-off (the follow-up set; G70 is the one-unit vocabulary reshape, the rest separable). Prior: 2026-08-11 (desktop): +1 G69 raised at SME request — R41-R44 registered AND detected in one change (the entry is what gets ratified, so a detector without one cannot be signed off). Prior: 2026-08-11 (desktop): +2 G68/O59 raised on SME direction — the folder-set PROFILE seam (FID/variable/email census + the substitution slots the XML cannot answer) and the /remediation intake surface that puts it in front of an SME. Only G68 is ready; O59 depends on it. Prior: 2026-08-10 (LAPTOP): -1 G35 claimed (pushed before work — the released TOM-roles gate, pulled on the machine holding its K21 evidence). Prior: 2026-08-10 (desktop): +1 G35 RELEASED to todo for the laptop (SME direction) -- awaiting-HITL in_progress -> pullable; the drafting half is the deliverable. Prior: 2026-08-10 (desktop): -1 O53 claimed (SME ruling — adopt experiment ALPHA code set, re-applied on the post-O47 tree). Prior: 2026-08-10 (desktop, 04:07 session): -1 O47 claimed (pushed before work — intake page shell, slice 3). Prior: 2026-08-10 (desktop, 04:07 session): -1 G53 claimed (pushed before work — single-DB contingency ADR). Prior: 2026-08-10 (desktop, 04:07 session): -1 G50 claimed (pushed before work — rollback-container + orphan-volume cleanup). Prior: 2026-08-10 (desktop, 04:07 session): -1 G49 claimed (pushed before work — MCP-server verification + GDS disposition, SME-directed). Prior: 2026-08-09 (LAPTOP): -1 K21 claimed (pushed before work — taken on the laptop within the hour it was raised, which is where the DBeaver screenshots live and where they stay; the four evidence files are gitignored by the root /*.png rule). Prior: 2026-08-09 (desktop): +1 K21 raised on SME direction — the LAPTOP item to mine the ServiceNow replica screenshots into the TOM mapping (working query, view inventory, and whether the pull scope is right against the public ServiceNow CMDB docs). 2026-08-09 (desktop): -1 C25 claimed (pushed before work — the software-version-context HITL gate, at SME request; the SME is present for the run). 2026-08-09 (desktop): -4 U15/U17/U18/U19 claimed (pushed before work — the four buildable Epic U instrument items, taken as one series at SME direction; U10/U11 skipped as HITL gate work and U16 deferred until U19 reports which side its fix landed on, which is the trigger U16 names). 2026-08-09 (desktop): -1 O52 claimed (pushed before work — the always-null holder_sid QuerySpec column; picked as a port-surface-free item while the 5417ef10..0d3761a9 port runs company-side). 2026-08-09 (desktop, weekly groom): +11 promoted from the inbox — U18 (docmeta missing from the metrics allow-list), U19 (scanner sys.path-root blind spot), L27 (dangling design-doc commit citation guard), V11 (docmeta SME runbook), G62-G65 (the four post-G22 data-profile gate SESSIONS drafted 2026-08-07 and left without ids by Idea-85), J40 (company docmeta port divergence), C28 (business-layer org-structure gate DRAFT), Q17 (jpmc-reports corpus shape decision). Ten of the eleven are dependency-free -> next_ready; L27 waits on L19 deliberately, because the guard turns the suite red until the sweep runs. FOUR of the ten are USER-GATED gate sessions (G62-G65) that a producer session cannot pull — they enter next_ready because the computation is mechanical, and each item says so in its first acceptance sentence. Prior: 2026-08-09 (desktop): -3 J34+J35+J38 batch-claimed (pushed before work — the port-machinery trio, built as ONE bundle because all three edit docs/port-prompt.md and would conflict apart; user directive from the incremental-port plan review: trio first, so the next port applies with the overlay seam, the mandatory ledger roll and the relay section already in the prompt). 2026-08-08 (desktop): +6 Epic Z groomed direct-to-backlog from the user directive (server location & geography: Z1 register export, Z2 ontology gate, Z3 loader+join, Z4 nslookup resolver, Z5 map module, Z6 time-zone runtime map); only Z1 is dependency-free -> next_ready. Prior: 2026-08-08 (desktop): -1 G57 withdrawn-closed (user ruling: the G22-session file rename was a comparison maneuver, not a family-rename directive; no rename performed — see the item note). Prior: 2026-08-07 (desktop pm): -1 J36 claimed; -1 G59 claimed. Prior (LAPTOP, groom): +2 promoted from the Q16-session gaps — O57 (a console page for the 28 pipeline sources / 15 systems / 17 retired ids / 17 sequence steps in load-map.json that no web/ code reads) and O58 (a console surface for docs-verify, whose multi-database sweep a single-database QuerySpec cannot serve). Both dependency-free -> both enter next_ready. Earlier: 2026-08-07 (LAPTOP): -1 Q16 claimed in_progress (pushed before work), acceptance WIDENED at the claim from documentation-block-only to every product. Earlier: -1 K16 claimed in_progress (pushed before work) — the FID census METHOD; counts stay company-side. Earlier: -1 L24 claimed in_progress (pushed before work) — the last piece of the G22-session thread. Earlier: 2026-08-07 (desktop, pm groom): +15 promoted from the OLD open tail of the inbox (Idea-1 .. Idea-55, plus Idea-84) — J36 (ryuk workaround), J37 (never parse a render), J38 (standing company relay list; consolidates Idea-18/24/26), J39 (six un-back-flowed company advances), G59 (apply-supplements completeness guard), G60 (PRECMD/POSTCMD file ops), G61 (provenance gate draft), N11 (load-map guard is one-directional), D10 (XML-vs-replica precedence gate draft), U16 (depgraph in-housing sizing), U17 (DesignDoc.commit ranking), O54 (code-resident matrix row), O55 (react-router advisory), L26 (white-paper doc type), R14 (ADK common). All 15 are dependency-free -> all enter next_ready. Earlier: 2026-08-07 (desktop, groom): +10 promoted from the Idea-56..83 inbox cohort — J33 (rich-ANSI test fix), J34 (PORT-MANIFEST company overlay), J35 (port-ledger roll, structural + SHA citations; consolidates Idea-56/76/57-design-half), N10 (wired/ready gate draft), D9 (precedence internal refines chain), U15 (depgraph dirty flag), O52 (holder_sid always-null), O53 (HeroArt orphan), L25 (gate-page rider rule), K20 (K5-amendment gate draft, p1). Earlier: -1 A4 claimed (pushed before work; sixth pull of the oldest-non-HITL run). Earlier: -1 S6 claimed (pushed before work; fifth pull of the oldest-non-HITL run). Earlier: -1 S7 claimed (pushed before work; fourth pull of the oldest-non-HITL run). Earlier: -1 O32 claimed (pushed before work; third pull of the oldest-non-HITL run). Earlier: -1 O30 claimed (pushed before work; second pull of the oldest-non-HITL run). Earlier: -1 O29 claimed (pushed before work; oldest non-HITL next_ready item, the 2026-07-23 UI groom cohort — user direction to finish the oldest unblocked items). Earlier: -1 G58 claimed (pushed before work; the archival report reads what G23 loaded). Earlier: -1 G23 claimed (pushed before work; the loader half of the G22/G55 pair). Earlier: -1 G55 claimed (pushed before work; the G22 flips-are-follow-ups item). Earlier: 2026-08-07 (LAPTOP, groom): +2 G58 + L24 — Idea-80 and Idea-82 groomed TOGETHER on user instruction (they turn on the same coverage argument). G58 = the dead-script archival report (three dispositions, a stated coverage precondition, the already-archived count); depends_on G23, so it does NOT enter next_ready. L24 = state the production-estate target state as a goal in the executive overview; dependency-free -> enters next_ready. Earlier: 2026-08-06 (desktop): -2 O45 + O46 claimed together (pushed before work; fifth pull of the desktop session) — O45 = the context-type vocabulary (config/taxonomy/context-types.yaml + guard + generated artifact for the intake dropdown); O46 = the intake API + origin-flagged store (multipart upload to the data root, digests, classification stamp, thread-delta detection, legal-transitions map). Earlier: 2026-08-06 (desktop, Idea-78 groom): +7 O45-O51 — the SME Context-Intake page slices (plan UI-WIP/sme-intake-page-plan.md; the Q10 assignment surface). O45 (context-type taxonomy) + O46 (intake API/store) are dependency-free and enter next_ready; O47-O51 chain behind them. NO slice writes the graph — the load stays Q10's behind G32→G31, the assignment edge behind Q10's gate. Earlier: 2026-08-06 (desktop): -2 N8 + O42 claimed together (pushed before work; third+fourth pulls of the desktop session) — N8 = rule the four load-map entries citing unregistered source ids (register/re-point/exempt per the acceptance's per-entry rulings); O42 = the remaining half of the web/ inventory: real .ts/.tsx import edges via a first-party TS resolver in the depgraph instrument (sized against python_imports.py — same extractor shape), plus MODULE_MAP/README records and a reload. Earlier: 2026-08-06 (LAPTOP): +2 from the G22 session on SME direction — G56 (collector captures the mount table, so shared-vs-local storage is DERIVED not declared) and G57 (rename the rua_* family to bkup_*). BOTH dependency-free -> both enter next_ready. Earlier same day: +1 G55 — apply the G22 vocabulary consequences (the flips-are-follow-ups half), groomed at the session on user instruction. depends_on G22, so it does NOT enter next_ready. Earlier same day: -1 G22 claimed in_progress at the gate session. The claim is LATE — the session opened and ruled A1/A2/H3 before the item was flipped; recorded rather than hidden, because the claim channel's whole job is telling the other machine what is being worked on. Earlier: 2026-08-05 (LAPTOP groom, rebased onto the desktop's J32+K19 closes): +1 O44 — the four-column ownership reconciliation pane (SME direction in-chat during G35). depends_on G35, so it does NOT enter next_ready. Counts RECOMPUTED from items at the rebase, never merged textually; the upstream side of this block was taken and the number re-derived. Earlier: 2026-08-05 (desktop): -1 J32 claimed (pushed before work - the registration/routing/attribution rule write-down; second pull of the session). Earlier: -1 K19 claimed (pushed before work - the as-of/reused-code detection; its K18 dependency closed yesterday, and J13 skips itself by the user-gated-start clause). Earlier: 2026-08-05 (LAPTOP): -1 G35 claimed (pushed before work - the TOM-roles gate PROMPT; it is the next ready item by the pull rule, J13 being skipped by its own user-gated-start clause). Earlier: 2026-08-05 (desktop, rebase-merge): -1 K18 claimed desktop-side (platform-tier fan-out bug; disagreement rule added at claim) merged with the LAPTOP same-day -1 G32 claim - counts recomputed from items. LAPTOP: -1 G32 claimed (pushed before work - the document/content topology + ddcontext charter gate, user direction; it unblocks G31/G38 and through G31 the three Q-series corpus items). Earlier: 2026-08-05 (desktop): -2 W2/W3 claimed (pushed before work - the Epic W tail). Earlier:# 2026-08-05 (desktop): -1 W1 claimed (pushed before work - the FCDO vocabulary crosswalk). Earlier:                                        # 2026-08-05 (desktop, IDEAS review): +3 groomed from the numbered-inbox pass (K18 tier derivation + platform row kind, K19 app-code reuse as-of, J32 the registration/routing/attribution rule). Three MERGES landed in existing items instead of new ones: J13 gains the platform-vocabulary ruling scope (Idea-38), C25 gains the two software-registry product rows as a prerequisite (Idea-65), G34 gains the acronym-catalog content shape (Idea-35). 2026-08-05 (desktop, mapping close-out): +3 groomed (S10 the null-app_id loader refusal, C26 the catalog divergence record, C27 its gate) -2 claimed in_progress (S10, C24). 2026-08-05 (desktop): -1 K15 closed — the job-grain /mappings pane RETIRED (user confirmed the acceptance's open direction in-session: a folder and its jobs carry the SAME app code, so a job-grain grid repeats one folder-level fact). Rebased onto the laptop's X4+N6 closes; counts recomputed from items, never merged textually. Earlier:2026-08-04 (LAPTOP, latest): +/-0 N6 closed (it was claimed the same session). Earlier: 2026-08-04 (LAPTOP, latest): -1 N6 claimed (pushed before work - the two-load-sequences ruling, user direction after X4). Earlier: 2026-08-04 (LAPTOP, latest): -1 X4 claimed (pushed before work — the laptop ddlineage drop, the X3 twin). Earlier 2026-08-04 (desktop): +4 Epic Y groomed by user directive (un-parking the 2026-08-03 backlog-sharding IDEAS entry in-chat, same day the weekly groom kept it parked) — Y1 the sharding ADR ruling session (dependency-free -> next_ready; the park condition rides Y2 as a prose precondition: the in-flight port PORT-REPORT review), Y2 the shard build behind it, Y3 the :BacklogItem/DEPENDS_ON vocabulary via the gate, Y4 the query surface. The three same-afternoon roll-up rebase conflicts (X1/V8, X2/V2, X3/V3) recorded as the fresh exhibit. Earlier 2026-08-04 (LAPTOP): +1 V3 closed — drydocs-load-runbook.md, restating no sequence (the N6 constraint). Counts recomputed at a rebase against the desktop's X3 close. Earlier 2026-08-04 (LAPTOP, latest): -1 V3 claimed (pushed before work). Counts recomputed at a rebase against the desktop's X3 claim. Earlier 2026-08-04 (desktop, latest): -1 X3 claimed (pushed before work — the desktop live drop, unblocked by the X2 close earlier this session). Earlier: -1 X2 claimed (pushed before work — the ddlineage live-surface repo sweep, unblocked by the X1 close earlier this session). Earlier 2026-08-04 (LAPTOP): -1 V2 claimed (pushed before work — the drydocs-core SME runbook). Earlier 2026-08-04 (desktop, latest): -1 X1 claimed (pushed before work — the ADR 0002 ddlineage amendment, same session that groomed the epic; user directive "pick up X1"; claim rebased over the laptop's concurrent V8 claim — disjoint items, both machines pushed before work, the rule held). Earlier 2026-08-04 (LAPTOP): -1 V8 claimed (pushed before work — the drydocs-api SME runbook, unblocked by V1). Earlier 2026-08-04 (desktop, latest): +4 Epic X groomed by user directive ("create a backlog epic to remove it", ruled in-chat right after the confirmation pass proved nothing loads ddlineage) — X1 the ADR 0002 amendment (the decision record; only dependency-free item, enters next_ready), X2 the live-surface repo sweep behind it, X3/X4 the venue-pinned live drops (desktop/laptop, the G50/G52 one-item-per-machine precedent). Earlier 2026-08-04 (LAPTOP, latest): -1 G54 claimed (pushed before work — make provision.ps1 exec-aware). Earlier 2026-08-04 (LAPTOP, latest): -2 J30+V1 batch-claimed (pushed before work — user directive "finish K9, then V8 or V1"; K9 is done so the open K9 business is J30's branch disposition, and V8 depends_on V1 so the V-order is fixed by the dependency). Earlier 2026-08-04 (LAPTOP, latest): -1 S4 claimed (pushed before work — the console draft substrate, ADR 0009 F3; picked as the next ready item by priority: J13 is dependency-free at p1 but its acceptance orders the pull loop to SKIP it pending a user term-list confirmation, and G32/G35 are p1 HITL gate sessions needing the SME). Earlier 2026-08-04 (LAPTOP weekly groom, latest): +6 promoted from the eight new same-day inbox lines (A4 the dcat registry path that resolves to nothing, C24 the two catalog loaders C22's file set excluded, G54 provision.ps1's host-PATH cypher-shell, L23 the -SDLC runbook skill the -excel skill's own frontmatter already promises, J30 the wip/k9-laptop disposition, J31 the claim-visible-but-work-invisible gap the second C19 collision exposed). All six dependency-free or behind a done dep, so all six enter next_ready. Same groom, 1 MERGED and no item: the sdlc-neo4j-schema.md HAS_APPLICATION drift became L19 clause (e), per its own "belongs to the cron stream, not piecemeal" instruction. 2 KEPT PARKED with triggers re-checked and NOT fired: the controlm-pipeline-stub twins (groom when the internal build lands; epic placement is a user call) and the desktop venue divergence (a user decision — recreate on the pinned image vs re-point the config; the laptop was verified correct at this groom, so it is desktop-only). Earlier 2026-08-04 (LAPTOP, latest): +1 G53 groomed on user direction (multi-DB -> single-DB consolidation contingency; dependency-free, so it enters next_ready). Earlier 2026-08-04 (desktop): -1 S5 claimed (pushed before work — same non-HITL loader-focus directive as the G36+L20 batch, taken after both closed to keep the claim window short).  2026-08-04 (desktop, latest): -2 G36+L20 batch-claimed (pushed before work — user directive "pick up non-HITL items, focus cypher loaders"; G36 = constraints.cypher index drop, L20 = feedback stray-file loader guard; S5 noted as the follow-on candidate). Earlier same day: 2026-08-04 (desktop, Control-M inbox groom, latest): +1 K15 — the legacy /mappings job-application pane (K11's inboxed finding: coverage grid + assign flow still read the retired job-grain edge, so every row shows "unresolved" and drafts are refused; retire-or-re-bind, direction confirmed with the user at build). Same groom, no other items: the ctlm_id ripple moved to the audit trail as consumed (K14/K9/port-62), the compact-timestamp trigger re-checked NOT fired (controlm_xml.py still consumes no temporal fields), m7_uses_artifact still planned, the rest of the Control-M inbox entries confirmed parked on their stated triggers. Earlier 2026-08-04 (LAPTOP): -1 Q12 closed WITHOUT ever being claimed todo->in_progress: its dependency Q6 was claimed and built in the same session, and Q12 turned out to be half-shipped already (the refusal landed 2026-07-31 ahead of its dep) with only the config-binding clause outstanding — splitting that across a second claim cycle would have been ceremony, not safety. Earlier 2026-08-04 (LAPTOP): -2 Q6+Q13 batch-claimed (laptop, pushed before work — user directive "pick up the Q series"; Q13 is a RECONCILIATION claim, its pipeline having landed 2026-07-31 unclosed, and Q6 is the component Q12 waits on). Earlier 2026-08-04 (latest): -2 M3+M4 batch-claimed (desktop, pushed before work — user directive "pickup M3, then M4"; both are doc-06 envelope gate items that share one in-chat SME session, the K12+K14 batch precedent). Earlier 2026-08-04: -4 J18+J26+J27+J28 batch-claimed (desktop, pushed before work — user directive "finish out the J series"; J13 deliberately NOT claimed, its acceptance user-gates the start on a term-list confirmation the user has not supplied). Earlier 2026-08-04: -1 C22 claimed (desktop, pushed before work — the catalog loader sweep C17 inboxed; user directive "finish C22"). Earlier 2026-08-04: -2 U13+U14 batch-claimed (desktop, pushed before work — the two review-plan query-pack bugs share one file set, and U14's own notes say apply both filters in one edit; the R6+R7/K12+K14 batch precedent; user directive "U13 then U14"). Earlier 2026-08-04: -1 U12 claimed (desktop, pushed before work — the snapshot-retention enforcement; its defect reproduced a FOURTH time this morning at the K13 ritual; user directive "fix U12"). Earlier 2026-08-04: -1 K13 claimed (desktop, pushed before work — the §G6 catalog_has_application back-flow semantics; user directive "pick up K13"). Earlier 2026-08-04: -1 K11 claimed (desktop, pushed before work — the steward mapping screen, the gate's §F2/§G authoring surface; user directive "work K11"). Earlier 2026-08-04 (later): -1 K10 claimed (desktop, pushed before work — the port activation cutover, the gate's §G4/§G5 build). Earlier 2026-08-04 (after K8 closed): -2 K12+K14 batch-claimed (desktop, pushed before work — the K-epic's two remaining dependency-free non-HITL items; the R6+R7 batch-claim precedent). Earlier 2026-08-04 (after the groom): -1 K8 claimed (desktop, pushed before work — user directive to process non-HITL ready items to completion; K8 picked first as the p1 loading half the K9 store unblocked). Earlier 2026-08-04 weekly groom: +2 (U13 the review-plan tombstone-filter bug, U14 the vendored-skills metric pollution — both from the 2026-08-02 inbox lines, both dependency-free with written pass/fail tests, neither HITL). Same groom, no items: the G51-tail guard evidence MERGED into J26 (second family instance, promise-vs-assertion), and five inbox entries moved to the audit trail as resolved by the K7 sign-off + K9 build (the 2026-07-22 defined-mapping mega-entry, the 2026-07-21 two-pattern model, the 2026-07-27 orchestrator-mapping act, the composite-key ruling pair, the G51 tail); the tier-2 platform-code enumeration re-inboxed slim (SME to supply rows). The backlog-sharding epic proposal KEPT PARKED by user ruling at this groom (phases 2-3 are a cross-repo plan change; phase 1's guard already shipped). Earlier 2026-08-03 (night, after K9 closed): +1 K14 groomed by user directive — the composite-key standard's conformance sweep. TWO SME rulings arrived in-chat during the K9 session: the ctlm_id dot composite (folder_id.job_id, the psgmgr convention, P2 §B precedent) IS the standard for serialized job identity, and key-cell pairs join with ':' not ';' (the semicolon is the SQL statement terminator). The standard doc + the parser flip were BUILT at the groom (zero manual CSVs committed = free migration window, taken); K14 owns the remaining value-form sweep (known stray: the web tray's colon-joined React key). Earlier 2026-08-03 (night, later still): -1 K9 re-claimed (desktop, pushed before work — same session that released the dead remote claim; the item is untouched, so this is a fresh start, not a resume). Earlier 2026-08-03 (night, later): +1 K9 claim RELEASED unbuilt back to todo — the remote claude.ai session that claimed it was interrupted by an API failure before any work landed (its claim commit 3608ae5 was the tip; nothing followed), so the claim was reset from the desktop rather than left to block the pull rule. Earlier 2026-08-03 (night, after the K7 close-out): -1 K9 claimed (desktop, pushed before work — the defined-mapping store, the AUTHORING half the gate's §F2 opened; picked because K8 and K11 both depend on it, so it is the one item in the new six that unblocks others). Earlier 2026-08-03 (night, later still): +6 opened BY the K7 gate at sign-off (K8 folder-grain loader, K9 defined-mapping store, K10 port active_state cutover, K11 steward screen, K12 the :ControlMApplication capture the map had to waive, K13 the catalog_has_application back-flow) and -1 K7 closed. Earlier 2026-08-03 (night, later): -1 K7 claimed (desktop, pushed before work — the Application ↔ Control-M attribution close-out gate SESSION, run in-chat with the user as SME directly after the groom that created it). Earlier 2026-08-03 (night): +2 groomed by user directive — K7 the Application ↔ Control-M attribution CLOSE-OUT gate SESSION (spec reached v3 on 07-27; no item ever owned RUNNING it, so the work had no claim channel) and J28 the render_gates.py classifier that kept that gate off the open-gates surface. Earlier 2026-08-03 (evening): -1 C23 claimed (desktop, pushed before work — the DQV-seed gate session, run in-chat with the user as SME, directly after L22 closed). Earlier: -1 L22 claimed (desktop, pushed before work — user directive: finish L22 then C23). Earlier 2026-08-03 (later still): +1 L22 groomed by user directive — adopt the U.S. business-English style guide (committed same groom), rewrite the exec overview, inventory idioms elsewhere, with the mechanism-name fence written into the acceptance. Earlier 2026-08-03 (later): +1 C23 groomed by user directive — the DQV quality seed floats (no upstream measurement leg, no vocab entries); noticed by the user post-wipe via the one surviving query. Earlier 2026-08-03: -1 G51 claimed (desktop, pushed before work — the unprovisioned meta-graph database + the guard that missed it; picked by user direction alongside a manual Neo4j data wipe, since the fix makes the wipe-rebuild path actually create ddschema). Earlier 2026-08-02 weekly groom: +7 todo (U10 the package-layer gate SESSION, U11 the second gate PROMPT, U12 the snapshot-README drift, G51 the unprovisioned meta-graph database + the guard that missed it, C22 the catalog loader sweep, J26 substring-match guards, J27 the .gitignore org names). Earlier 2026-08-02: -1 S2 claimed (laptop, pushed before work — the orchestration parent, ADR 0008 accepted as proposed at S1). Earlier 2026-08-01: -1 J24 claimed in_progress (desktop, pushed before work - the retired-tier residual sweep; picked over the other four retire-worded open items after a survey found each of those needs a decision first). Earlier 2026-08-01: -1 S3 claimed (laptop, pushed before work — the BusinessApplication identity cutover, gate-signed 2026-07-27 and ADR-amended at S1 the same day; it is BUILD work, not HITL). Earlier 2026-08-01: -1 S1 ruled (laptop; the three ADRs accepted — 0008/0009 as proposed, 0010 with amendments — which unblocked SIX items at once: S2/S3/S4 at p1 and S5/S6/S7 behind them). Earlier: -1 Q7 claimed (laptop). Earlier: +1 R7 claim RELEASED unbuilt back to todo — a pre-build survey found it needs a fetcher the producer does not have (Q6 owns the web/filedrop connectors, and is itself parked); Q6 added to its depends_on, so it leaves next_ready. A split (freshness-now / rescrape-later) is the user's call, not an agent's. Earlier 2026-08-01: -2 R6+R7 batch-claimed in_progress (desktop, pushed before work — the next two non-HITL ready items; O26 skipped, it carries an explicit SME HOLD and the runbook-template session has not ruled). Earlier 2026-08-01: -1 C17 claimed in_progress (desktop, pushed before work — the PAT grain-keying gate item; user supplied the decisive SME fact in-chat). Earlier 2026-08-01: -1 O27 claimed in_progress (desktop, pushed before work). Earlier 2026-07-31 (pm): +1 O43 — seed the remaining 9 UI test suites + rule on a UI runner (the Process/Testing model admitted for internal use; ui-tests.yaml + the component->module->suite->cases join built same session, 2/11 suites seeded from verified close notes). +1 O42 — the 62 first-party React components are in NO machine-readable inventory (verified: the code-graph snapshot scans Python roots only, MODULE_MAP omits drydocs-web by construction, and the software registry is the third-party ledger so they correctly do not belong there); dependency-free -> next_ready. Earlier (feat/external-vendor-scraper): +1 Q16 documentation currency (software-registry `documentation:` pointer -> docs corpus + version drift; the CONFIG half + its two guards built same session, Q16 owns the report + the graph pointer). Q13 restated STANDALONE per the user ruling (own converter/loader/verb, capture manifest as the inter-stage contract) rather than widening load-bmc-docs. Earlier same session: +3 the vendor-docs load plan (knowledge/upgrade-plans/vendor-docs-agent-navigation.md) — Q13 layer-1 navigable spine (taxonomy only, dependency-free -> next_ready), Q14 layer-2 entity-spine gate PROMPT (drafts only; sign-off is a separate SME session), Q15 agent-navigation QuerySpecs; Q14/Q15 both wait on Q13 so they argue from loaded evidence. Layer 3 (estate join) deliberately has NO item — it is blocked on G32's residency ruling because a relationship cannot span Neo4j databases (Q8). Earlier 2026-07-31 (pm, user-requested during the BMC doc-tree feasibility probe): +1 Q12 doc-capture scale guardrail — NOT next_ready (depends on Q6, which brings the web connector); Q6's acceptance gained the matching blocking clause so an unguarded scraper cannot land as an intermediate state. 2026-07-31 weekly groom (pm, after the N7 gate + N9 build closed): +2 (J24 the retired-tier residual in forward-looking specs — J23's own leftovers; J25 the paste-ready L7 ratification snippet, discharging cross-repo tracker T11). Also at that groom, NO new items: the 2026-07-20 runbook load-doc-traceability line MERGED into L21's acceptance, and M3's acceptance was fixed inline where it still cited the retired Internal-Confidential tier. N9 claimed in_progress 2026-07-31 (desktop, pushed-claim discipline — the gate-authorized build, same session as the N7 close). N9 groomed 2026-07-31 at the N7 close (the gate-authorized build; dependency-free -> next_ready). N7 claimed in_progress 2026-07-31 (desktop, pushed-claim discipline — the registry-v2 HITL gate session, user opened it in-chat with Q2/Q4 pre-rulings). J23 claimed in_progress 2026-07-31 (desktop, pushed-claim discipline). 2026-07-31 groom: +4 (J23 classification collapse to 3 levels — user ruling pre-decided, registry-plan Phase 1; W1-W3 new Epic W fcdo-alignment: crosswalk + gate spec, planned property/enum registration, ontology-builder skill note). J22 claimed in_progress 2026-07-30 (desktop, pushed-claim discipline, right after the weekly groom). 2026-07-30 weekly groom: +8 (N7 registry-v2 design session — user directive, fable/HITL; N8 the four unregistered map-source ids from N4's day-one render; J22 the J16 guard's untracked blind spot; R11-R13 the R5 follow-up trio — live smoke, stub-ADK fixture, ADK event-persistence check; G49/G50 the two time-parked infra chores whose triggers fired — port review done + neo4jtest probation week up). R5 claimed in_progress 2026-07-30 (desktop, after R4+R3 closed same session). R3 claimed in_progress 2026-07-30 (desktop, right after R4 closed; R5 queued next). R4 claimed in_progress 2026-07-30 (desktop session, non-HITL lane; laptop holds the HITL lane). J19 claimed in_progress 2026-07-30 (laptop session, pushed-claim discipline — the item that codifies the discipline itself; desktop session open concurrently). N5 claimed in_progress 2026-07-29 pm right after N4 closed (pushed-claim discipline). N3+N4 claimed in_progress 2026-07-29 (batch claim, pushed before work; N4 rides its N3 dep inside the chain — the O39 precedent). 2026-07-29 groom (user directive): +10 (new Epic V sme-runbooks — V1 coverage rule + V2-V10 per-module runbooks; only V1 dependency-free, the rest wait on its mapping dispositions; N4 widened in place with the taxonomy/ontology-map joins — merge, not a new item). G48 claimed in_progress 2026-07-29 once G46/G47 closed (pushed-claim discipline). 2026-07-29 groom: +3 (G46 resolver cmd-line API, G47 controlm-xml-export seam, G48 resolve-cmdline-staging — the XML-fed resolution chain; precedence ruling parked as an inbox question, guardrail 3). G42 claimed in_progress 2026-07-28 (catalog seam start; pushed-claim discipline). G45/C20/J20/R10 claimed in_progress 2026-07-28 evening (batch claim, pushed before work). 2026-07-28 evening groom: +4 (G45 rua listing fallback back-flow, C20 K4-comment scope, J20 matrix render fold, R10 agent-runtime chores; R3 caller-identity merged in place). Earlier 2026-07-28 pm load-map groom: +4 (N3-N6 — the generated loaders/order/commands/source-target surface; only N3 is dependency-free, N4->N5 chain and N6 waits on N3's declared sequence). # 2026-07-28 pm post-UI-merge groom: +15 (U7/U8 instrument hardening, O35 WF-LND landing, O36 dot clip, O37-O41 DataLens/DSI continuity, B5 stage taxonomy, R9 agent query surface, J18/J19 process, L20 stray-file guard, L21 runbook Rev 4).                                       # C15/C16/L18/D8/J17 claimed in_progress 2026-07-28 (batch claim, pushed-claim discipline). P5 claimed in_progress 2026-07-28 pm (pushed-claim discipline). 2026-07-28 pm epic-close-out groom: +7 (G42-G44 Snowflake data-catalog seam from the loader plan; L18 comma-split bug, L19 doc-drift sweep, D8 bootstrap guard, J17 gates.json ritual — all from same-day inbox lines), U5 executed in-run (-1, Epic U closed 6/6); prior: U4 closed 2026-07-28 pm (right after U6 unblocked it); U6 groomed + built + closed same day (net 0); +4 at the 2026-07-28 inbox groom (C19 ontology-comment fix, J16 port-manifest fall-through guard, L17 doc-loader prereq guards, O34 web npm advisories); all four closed same day (O34 via its stop clause), L16 closed alongside; +5 2026-07-27 groom (J14/J15/G36/O33/G38), +2 PAT review (C17/C18), +5 code-graph-review (U1-U5), +2 cmdline seam (G39/G40, built+closed same day)
  in_progress: 6  # 2026-08-19 (laptop): +3 Q9+Q10+Q11 claimed. Prior: 2026-08-18 (laptop): -1 G31 done (re-scoped post-fold, delivered same session). Prior: 2026-08-18 (laptop): +1 G31 claimed. Prior: 2026-08-18 (laptop): -1 Q20 done (satisfied-by-fold, checked same session it was claimed). Prior: 2026-08-18 (laptop): +1 Q20 claimed (recomputed from items). Prior: 2026-08-18 (laptop): -1 G102 done — the fold applied end to end. Prior: 2026-08-18 (laptop): +1 G102 claimed. Prior: 2026-08-18 (laptop): -1 G32 done — document-content-topology SIGNED 32/32. Prior: 2026-08-18 (laptop): -2 N12+N13 done — built and gate-signed the same session they were claimed. Prior: 2026-08-18 (laptop): +2 N12+N13 claimed. Prior: 2026-08-18 (laptop): -1 G99 done — rewritten same session it was claimed. Prior: 2026-08-18 (laptop): +1 G99 claimed. Prior: 2026-08-18 (laptop): -1 G91 done (gate closed 5/5). Prior: 2026-08-18 (laptop): +1 G91 claimed. Prior: 2026-08-17 (laptop): -1 G98 done — gate corporate-backbone-vocabulary SIGNED 19/19 same session it was claimed. Prior: +1 G98 claimed in_progress — the corporate-backbone vocabulary gate (:Company + both HAS_BUSINESS_SEGMENT* edges execute but were never registered); claimed and pushed BEFORE work per the C19 rule. Prior: 2026-08-13 (desktop): -1 G96 done — framework built same day it was claimed. Prior: 2026-08-13 (desktop): +1 G96 — the Control-M API-call framework build. Prior: 2026-08-13 (desktop): +1 G62 — SME convened the data-profile gate session; §B runs company-side, producer lands the transcription. Prior: 2026-08-12 (desktop, evening): -1 G60 DONE (91bbf7bb). Prior: 2026-08-12 (desktop, evening): -2 G83+G84 DONE (the sibling commit, 3c4ef5d5). Prior: 2026-08-12 (desktop, evening): +3 G60/G83/G84 claimed (G83+G84 = the description_tokens siblings, one-commit batch; G60 = the PRECMD/POSTCMD lineage feed). Prior: 2026-08-11 (desktop): -1 GN1 DONE (ADR 0012). Prior: 2026-08-11 (desktop): +1 GN1 claimed at raise (the ADR must settle naming before GN2 moves anything). Prior: 2026-08-11 (desktop): -2 G75+G76 DONE (the two Control-M mechanism back-flows). Prior: 2026-08-11 (desktop): +2 G75+G76 batch-claimed. Prior (LAPTOP): 2026-08-11 (LAPTOP): -1 G35 SIGNED OFF and closed. Prior: 2026-08-11 (desktop): -1 C32 DONE (fan-out capture + standard extension). Prior: 2026-08-11 (desktop): +1 C32 claimed (pushed before work — the downstream fan-out capture + standard extension, SME-directed). Prior: 2026-08-11 (desktop): -1 C31 closed same session (the consolidated guidelines page, published as an artifact). Prior: 2026-08-11 (desktop): +1 C31 raised and claimed (pushed before work — the consolidated guidelines page; new item, so todo is unchanged). Prior: 2026-08-11 (desktop): -2 C30/G67 closed same session (greenfield standard twin + R30-R40 detectors with the reachability proof). Prior: 2026-08-11 (desktop): +2 C30/G67 raised and claimed in one act (pushed before work — the greenfield job standard + the R30-R40 conformance detectors; new items, so todo is unchanged). Prior: 2026-08-11 (desktop): -2 C29/G66 closed same session (capture + register + gate rider; extractor DESCRIPTION field + token parser, 29 tests). Prior: 2026-08-11 (desktop): +2 C29/G66 raised and claimed in one act (pushed before work — the 22-screenshot Control-M job-metadata standards capture + the DESCRIPTION read seam; new items, so todo is unchanged). Prior: 2026-08-10 (LAPTOP): +1 G35 claimed (pushed before work). Prior: 2026-08-10 (desktop): -1 G35 released to todo. Prior: 2026-08-10 (desktop): -1 O53 closed same session. Prior: 2026-08-10 (desktop): +1 O53 claimed. Prior: 2026-08-10 (desktop): -1 L28 closed same session. Prior: 2026-08-10 (desktop): +1 L28 claimed (pushed before work — cite the external code references; KGoT entry + overview references footer). Prior: 2026-08-10 (desktop, 04:07 session): -1 O47 closed same session. Prior: 2026-08-10 (desktop, 04:07 session): +1 O47 claimed. Prior: 2026-08-10 (desktop, 04:07 session): -1 G53 closed same session. Prior: 2026-08-10 (desktop, 04:07 session): +1 G53 claimed. Prior: 2026-08-10 (desktop, 04:07 session): -1 G50 closed same session. Prior: 2026-08-10 (desktop, 04:07 session): +1 G50 claimed. Prior: 2026-08-10 (desktop, 04:07 session): -1 G49 closed same session. Prior: 2026-08-10 (desktop, 04:07 session): +1 G49 claimed. Prior: 2026-08-09 (desktop): -1 J41 closed same session it was claimed. Prior: 2026-08-09 (desktop): +1 J41 claimed (pushed before work — the port OPENING sequence; raised by a port that could not run solo, and by RELAY-5 turning out to assert company state the producer could not see). Prior: 2026-08-09 (LAPTOP): -1 K21 closed same session it was claimed. Prior: 2026-08-09 (LAPTOP): +1 K21 claimed (pushed before work). Prior:2026-08-09 (desktop): +1 C25 closed — gate software-version-context SIGNED OFF (A-E, G; F blocked on K17), with the snowflake/dpl prerequisite rows registered first. 2026-08-09 (desktop): -1 C25 claimed (pushed before work — the software-version-context HITL gate, at SME request; the SME is present for the run). 2026-08-09 (desktop): +1 U19 closed — the scanner resolves per-directory sys.path roots; orphan queue 22 -> 4 and agents/ 15 -> 0. 2026-08-09 (desktop): +1 U18 closed — metrics scope widened to eight package roots and A1/A3/A4/A5 re-baselined; the typed list is now guarded against pyproject.toml. 2026-08-09 (desktop): +1 U17 closed — the staleness ranking is defined (ranks on the claim; git fact and the gap reported beside it). 2026-08-09 (desktop): +1 U15 closed — snapshot `dirty` narrowed to tracked changes, untracked_present recorded beside it on both header blocks. 2026-08-09 (desktop): -4 U15/U17/U18/U19 claimed (pushed before work — the four buildable Epic U instrument items, taken as one series at SME direction; U10/U11 skipped as HITL gate work and U16 deferred until U19 reports which side its fix landed on, which is the trigger U16 names). 2026-08-09 (desktop): +1 O52 closed — the always-null holder_sid column; the fix is one word, the deliverable is the pair of guards (both proven to fail on the reintroduced defect). 2026-08-09 (desktop): -1 O52 claimed (pushed before work — the always-null holder_sid QuerySpec column; picked as a port-surface-free item while the 5417ef10..0d3761a9 port runs company-side). 2026-08-09 (desktop): +3 J34+J35+J38 closed — the port-machinery trio BUILT as one bundle (branch feat/port-machinery-trio, --no-ff merge 09c1be1): the overlay seam, the ledger roll through 5417ef10 + mandatory closing sequence + SHA citations, and the standing relay section. 2026-08-09 (desktop): -3 J34+J35+J38 batch-claimed (pushed before work — the port-machinery trio, built as ONE bundle because all three edit docs/port-prompt.md and would conflict apart; user directive from the incremental-port plan review: trio first, so the next port applies with the overlay seam, the mandatory ledger roll and the relay section already in the prompt). 2026-08-07 (desktop pm): J36 and G59 each claimed and closed same session. Prior (LAPTOP, groom): -1 Q16 closed as a PARTIAL done — clause (a) shipped, clause (b) (the pointer reaching the graph) is NOT done and stays blocked behind Q14/G32, stated in the close note rather than implied by the status. Earlier: 2026-08-07 (LAPTOP): +1 Q16 claimed. Earlier: -1 K16 -> blocked (see blocked line). Earlier: +1 K16 claimed. Earlier: -1 L24 done (see done line). Earlier: +1 L24 claimed. Earlier: 2026-08-07 (desktop): -1 G58 done same session. Earlier: +1 G58 claimed. Earlier: -1 G23 done same session. Earlier: +1 G23 claimed. Earlier: 2026-08-07 (desktop): -1 G55 done same session. Earlier: # 2026-08-07 (LAPTOP): -1 G22 done — gate rua-load-shapes SIGNED OFF, 28/28. Earlier: 2026-08-06 (desktop): -1 O46 done (see done line). Earlier: 2026-08-06 (desktop): -1 O45 done (see done line). Earlier: 2026-08-06 (desktop): +2 O45 + O46 (see the todo line). Earlier: 2026-08-06 (desktop): -1 N8 done (see done line). Earlier: 2026-08-06 (desktop): -1 O42 done (see done line). Earlier: 2026-08-06 (desktop): +2 N8 + O42 (see the todo line). Earlier: 2026-08-06 (LAPTOP): +1 G22 — the rua load-shapes gate session, opened and partially ruled; stays in_progress until §J signs. Earlier: 2026-08-05 (desktop): +1 J32 claimed then -1 CLOSED same session (the field-meaning rule). Earlier: +1 K19 claimed then -1 CLOSED same session (mapping-age detection). Earlier: 2026-08-05 (LAPTOP): +1 G35 (the TOM-roles gate). NOTE this is the SECOND gate prompt awaiting the same SME - G32 is drafted and unsigned. Deliberate: the PROMPT half needs no SME, only the walk does, so drafting ahead of the ruling queues work for one sitting rather than blocking on it. Earlier: 2026-08-05 (desktop, rebase-merge): +1 K18 (desktop) alongside the LAPTOP +1 G32 - same-day cross-machine claims, both pushed-before-work. LAPTOP: +1 G32 claimed LAPTOP-side; Y1 remains the DESKTOP's claim. Earlier: 2026-08-05 (desktop): -2 W2/W3 BUILT same session (46a2b8c, 2e4e726). Earlier: -1 W1 BUILT same session (eaa4469). 2026-08-05 (desktop): -1 C24 BUILT — the catalog-mapping close-out lands with the BusinessApplication one. 2026-08-05 (desktop): -1 S10 BUILT same turn it was groomed (the loader refusal); C24 still claimed. 2026-08-05 (desktop): +2 S10 and C24 claimed (pushed before work) — the two buildable residues of the BusinessApplication/catalog mapping areas. 2026-08-04 (LAPTOP, latest): -1 N6 closed. Earlier: 2026-08-04 (LAPTOP, latest): +1 N6 claimed LAPTOP-side; Y1 is the DESKTOP's claim, untouched here. Earlier: 2026-08-04 (LAPTOP, latest): -1 X4 closed — the laptop ddlineage drop landed. Earlier: Y1 claimed 2026-08-04 (DESKTOP — the sharding ruling session, in-chat) + X4 in flight LAPTOP-side (gate passed, drop blocked on a permission — see its status note); E1 gate-deferred. Earlier: V3 claimed AND closed 2026-08-04 LAPTOP-side; V3 claimed 2026-08-04 (LAPTOP — the next V-series runbook) + X3 claimed AND closed same day desktop-side (the live drop on neo4jtest — probe 0, alias out, dropped, smoke green; see its close note); E1 gate-deferred. Earlier: X2 claimed AND closed 2026-08-04 same session (desktop; the ddlineage live-surface sweep — see its close note); V2 closed the same afternoon LAPTOP-side, so only E1 (gate-deferred) remains. Earlier: X1 claimed AND closed 2026-08-04 same session (desktop; the ADR 0002 ddlineage-retirement amendment + gate-log record — see its close note). Still open: V8 claimed 2026-08-04 (LAPTOP, pushed before work — the drydocs-api SME runbook; disjoint file sets, the only reason two machines hold claims at once) + E1 gate-deferred. Earlier: Q6 CLOSED the same session it was claimed (drydocs_docmeta/ built; Q12 rode with it). Q13 CLOSED the same session it was claimed (a reconciliation claim — the pipeline was already built 2026-07-31); Q6 still in flight. Q6+Q13 batch-claimed 2026-08-04 (LAPTOP, pushed before work — the Q-series pickup; a disjoint epic/file set from the desktop's M3+M4 gate session, which is the only reason two machines held claims at once) + E1 gate-deferred. Same day, desktop: M3+M4 batch-claimed AND both closed 2026-08-04 same session (desktop; both gates ran in-chat and SIGNED — audit-envelope-phase4 13/13 with the SME evidence reversing the page's SEAL recommendation, envelope-property-terms 10/10 — see their close notes). Only E1 remains, gate-deferred. Earlier: J18+J26+J27+J28 batch-claimed AND all four closed 2026-08-04 same session (desktop; the J-series close-out — see their close notes; J13 skipped, user-gated start). Only E1 remains, gate-deferred. Earlier: C22 claimed AND closed 2026-08-04 same session (desktop; the catalog loader sweep — orphan shape + coalesce + the EMIT ruling; see its close note). Only E1 remains, gate-deferred. Earlier: U13+U14 batch-claimed AND closed 2026-08-04 same session (desktop; one edit to the shared pack — tombstone rule + seven-root scope, live-probed, one acceptance number corrected by evidence — see their close notes). Only E1 remains, gate-deferred. Earlier: U12 claimed AND closed 2026-08-04 same session (desktop; retention enforced in the script + proven live on five stale files, README trued, +3 guard — see its close note). Only E1 remains, gate-deferred. Earlier: K13 claimed AND closed 2026-08-04 same session (desktop; the §G6 back-flow — support reading onto every producer surface, first map entry created, false loader claim removed — see its close note). Only E1 remains, gate-deferred. Earlier: K11 claimed AND closed 2026-08-04 same session (desktop; the steward cascade pane + the §G1 orchestrator-edge authoring it turned out to own — see its close note). Only E1 remains, gate-deferred. Earlier: K10 claimed AND closed 2026-08-04 same session (desktop; the active_state cutover — see its close note). Only E1 remains, gate-deferred. Earlier: K12+K14 batch-claimed AND closed 2026-08-04 same session (desktop; the app-code taxonomy capture + the composite-key value-form sweep — see their close notes). Only E1 remains, gate-deferred. Earlier: K8 claimed AND closed 2026-08-04 same session (desktop; the folder-grain attribution loader — see its close note). Only E1 remains, gate-deferred. Prior note: K8 claimed 2026-08-04 (desktop, pushed before work — the folder-grain loader + K2 job-edge migration; user directive to run the non-HITL ready queue). Earlier: K9 claimed AND closed 2026-08-03 same session (desktop; the defined-mapping store — app_code_mapping table + grid/migration views in mapping.db, the app-code-mapping console domain with O24-verbatim draft endpoint, K2_SHAPE flipped to the ruled folder-grain edge, template rekeyed to app_code). Only E1 remains, gate-deferred. Prior note: K9 re-claimed 2026-08-03 (desktop, pushed before work — the same session that released the dead remote claim; fresh start on an untouched item). Earlier: K9's claim RELEASED unbuilt 2026-08-03 (the remote session that claimed it died on an API failure before any work landed — see its notes); only E1 remains, gate-deferred. Prior note: K9 claimed 2026-08-03 (desktop, pushed before work — the laptop session closed out at 1bab45a and handed over; K9 is the AUTHORING half of the app-code mapping, K8 the loading half behind it). E1 remains gate-deferred. Earlier: C23 claimed AND closed 2026-08-03 same session (desktop; the DQV-seed gate ran in-chat — SME ruled DEFER, trigger recorded, vocabulary gap closed with the four c23_* entries). L22 claimed AND closed 2026-08-03 same session (desktop; the style-guide adoption — guide wired into CLAUDE.md + documentation skill, overview rev 7 rewritten with the source decision recorded, whitepaper's two hits fixed across .md + both renders, candidate-rename list shipped with the fence intact). Only E1 remains, gate-deferred. Earlier same day: G51 claimed AND closed 2026-08-03 same session (desktop; provisioning DDL + guard widening, built in proof order — the widened guard was run BEFORE the DDL and failed on exactly SCHEMA_GRAPH_DATABASE/ddschema, then the DDL turned it green). Only E1 remains, gate-deferred. S2 claimed AND closed 2026-08-02 same session (LAPTOP; the orchestration parent — one git mv, two file splits, and the crosswalk consumer the config had been waiting on since 2026-07-14). Only E1 remains, gate-deferred. Prior note: S2 claimed 2026-08-02 (LAPTOP, pushed before work — drydocs_core package move; the desktop's last session closed J24 and touched docs/api, no overlap). Earlier: S3 claimed AND closed 2026-08-01 same session (LAPTOP; the BusinessApplication identity cutover — phases 1+2 + the column ledger, live-verified on a scratch database). Only E1 remains, gate-deferred. Earlier: J24 claimed AND closed 2026-08-01 same session (desktop). Prior note: J24 claimed 2026-08-01 (DESKTOP, pushed before work; concurrent with the laptop S3 claim - J24 is docs/skills/templates + drydocs_api classification vocabulary, deliberately not the loader-Cypher/schema/web surfaces S3 holds). S3 claimed 2026-08-01 (LAPTOP, pushed before work; desktop session open concurrently — S3 is loader-Cypher/schema/API/web, deliberately not the R-epic or catalog surfaces the desktop has been working). Earlier: Q7 claimed AND closed 2026-08-01 same session (LAPTOP, concurrent with a desktop R-epic session — claimed deliberately in a module that session was not touching, and it added files rather than editing agents/ or web/; zero collision at merge). Only E1 remains, gate-deferred. R7's claim RELEASED unbuilt 2026-08-01 (blocked on a fetcher — see its notes); only E1 remains, gate-deferred. R6 claimed AND closed 2026-08-01 same session (desktop; Tier-2 bounded loop). R7 still open. R6+R7 batch-claimed 2026-08-01 (desktop, pushed-claim discipline — next two non-HITL ready items). E1 remains gate-deferred. C17 claimed AND closed 2026-08-01 same session (desktop; PAT grain keying — §a/§b/§c ruled + built, gate as a whole still unsigned). E1 remains gate-deferred. O28 claimed AND closed 2026-08-01 same session (desktop; node status envelope, contract + live producer + both UI surfaces). O27 claimed AND closed 2026-08-01 same session (desktop; QuerySpec authoring conventions). E1 remains gate-deferred. J10 CLOSED 2026-08-01 (laptop; stages 1-5 in one sitting after the user ruled the two open calls — ruff check AND ruff format --check both exit 0, CI blocks on both. Company side not started and not blocked). Only E1 remains, gate-deferred. Prior note: J10 claimed 2026-07-31 (desktop; stage 0 only — stages 1-5 wait on the internal port's PORT-REPORT).                                # N9 claimed AND closed 2026-07-31 same session (desktop; the registry v2 build — systems/datasets/retired shipped, suite 1204). E1 remains. N7 claimed AND closed 2026-07-31 same session (desktop; the registry-v2 gate session ran in-chat — D1-D4 + Q1-Q6 all ruled, migration table = per-row residual at N9). E1 remains. J23 claimed AND closed 2026-07-31 same session (desktop; classification collapse to 3 levels). E1 remains. J22 claimed AND closed 2026-07-30 same session (desktop; guard walk widened to untracked). E1 remains. R5 claimed AND closed 2026-07-30 same session (desktop — the whole R4->R3->R5 run in one day). R3 claimed AND closed 2026-07-30 same session (desktop; agent telemetry — ledger + :AgentRun + admin spec). E1 remains. R4 claimed AND closed 2026-07-30 same session (desktop; ephemeral session specs). E1 remains gate-deferred (J19 closed 2026-07-30: pull rule documentation — CLAUDE.md §0 + backlog.yaml header + acceptance test all updated to make the pushed-claim discipline explicit). J21 claimed AND closed 2026-07-30 same session (registry pk check + binding guard, 087a685). N3+N4+N5 all claimed AND closed 2026-07-29 same session (the load-map chain). E1 remains. E1 (gate deferred both sides). O35-O41 UI sweep claimed + closed 2026-07-29 same session. + old-note: batch claim 2026-07-29 (pushed-claim discipline; O39 rides its O38 dep inside the sweep). G46/G47/G48 — the whole XML-fed resolution chain — claimed + closed 2026-07-29 same session. G42 claimed + closed 2026-07-28 same session. 2026-07-28 evening batch claim fully closed: J20, R10, C20, G45 all done same evening
  blocked: 1  # 2026-08-07 (LAPTOP): +1 K16 — producer half delivered (method + tests + company prompt); the COUNTS are Internal and cannot be produced in this repo, which holds no directory extract. Unblocks on the company-side run; K17 waits on that, not on the method
  done: 322  # 2026-08-18 (laptop): +1 G31 — the D1 business-key spine, guarded. Prior: 2026-08-18 (laptop): +1 Q20 — Q16(b) satisfied by the fold, trace closed end to end. Prior: 2026-08-18 (laptop): +1 G102. Prior: 2026-08-18 (laptop): +1 G32 — the topology gate signed; the fold is ruled, the apply is G102. Prior: 2026-08-18 (laptop): +2 N12 (acquisition blocks) + N13 (pending-source-correction gate signed 12/12). Prior: 2026-08-18 (laptop): +1 G99 — pat_team_roles rewritten onto qualified attribution. Prior: 2026-08-18 (laptop): +1 G91 — held planned-entry review closed 5/5. Prior: 2026-08-17 (laptop): +1 G98 — corporate-backbone gate signed 19/19 and consequences applied. Prior: 2026-08-13 (desktop): +1 G96 — Control-M API-call framework built (call surface + discovery reference + guardrail; .sh/config company-side). Prior: 2026-08-12 (desktop, evening): +1 G60 — the PRECMD/POSTCMD feed (91bbf7bb): shell-variable rows (SHELL_VAR_NAMES incl. the POSCMD typo) join by (folder_id, job_id) and run through the same G14 file-op grammar as CMD_LINE, same endpoints, no new rel types, pre/post invocations deliberately not emitted; prepost_* coverage keeps the yield measurable; nothing depended on G60. Prior: 2026-08-12 (desktop, evening): +2 G83+G84 — the description_tokens sibling commit (3c4ef5d5): C30 reaches the parse contract (FTS_ID/REC_ID registered, six tokens retired in place, era-aware completeness, folder-carrier contacts both spellings, registry-vs-standard agreement test) + the DD1| sentinel (three populations, version-not-template, sentinel_coverage; gate §G6 already carried the ruling, no gate edit). G77 stays blocked on C34. Prior: 2026-08-12 (desktop): +1 J48 — the Idea-109 residue sweep, 25 anchors adopted + 3 ruled and left. Prior: 2026-08-12 (LAPTOP): +1 J13. Prior:  # 2026-08-11 (desktop): +1 GN1 — ADR 0012, the data-named load surface. Prior: 2026-08-11 (desktop): +2 G75+G76. Prior: 2026-08-11 (LAPTOP): +1 G35 closed — gate tom-roles-enumeration-and-cardinality SIGNED OFF at a live SME walk; 14 register lines ruled, 5 residuals named, follow-up set groomed as G70-G74. Prior: 2026-08-11 (desktop): +1 C32. Prior: 2026-08-11 (desktop): +1 C31 closed same session. Prior: 2026-08-11 (desktop): +2 C30/G67 closed same session. Prior: 2026-08-11 (desktop): +2 C29/G66 closed same session. Prior: 2026-08-10 (desktop): +1 O53 closed — HeroArt removed via the SME-adopted experiment ALPHA code set, coverage pin fixed at adoption. Prior: 2026-08-10 (desktop): +1 L28 closed — KGoT registered in reference/research/ (paper + BSD-3-Clause + adopted-vs-rejected), REGISTRY.yaml research: stub became a real entry list, ADR 0007 gains a citation footnote, executive overview rev 9 carries a 14-entry References section. Prior: 2026-08-10 (desktop, 04:07 session): +1 O47 closed — /intake slice 3 live (sections 1-3 end-to-end vs O45/O46; 4-8 placeholders; sme persona both sides of the roster-drift guard). Prior: 2026-08-10 (desktop, 04:07 session): +1 G53 closed — ADR 0011 single-database contingency written (PLANNED, SME-gated execution). Prior: 2026-08-10 (desktop, 04:07 session): +1 G50 closed — 4 dangling volumes removed attachment-verified; ee container + named volumes already absent; neo4jtest untouched. Prior: 2026-08-10 (desktop, 04:07 session): +1 G49 closed — MCP server re-registered user-scope + live-verified (1447/1434 via read-cypher), GDS 423 procs = available-unused. Prior: 2026-08-09 (desktop): +1 J41 closed — the port OPENING sequence (opening seq + one-owner rule + port-base tag + relay basis tags + scripts/port_preflight.py + reconcile-port skill check). Prior: 2026-08-09 (LAPTOP): +1 K21 closed — the ServiceNow replica evidence mined into knowledge/upgrade-plans/servicenow-replica-evidence.md; nothing activated, ring-1 dataset rows drafted in the doc rather than written to the registry. Prior: 2026-08-09 (desktop): +1 C25 closed — gate software-version-context SIGNED OFF (A-E, G; F blocked on K17), with the snowflake/dpl prerequisite rows registered first. 2026-08-09 (desktop): +1 U19 closed — the scanner resolves per-directory sys.path roots; orphan queue 22 -> 4 and agents/ 15 -> 0. 2026-08-09 (desktop): +1 U18 closed — metrics scope widened to eight package roots and A1/A3/A4/A5 re-baselined; the typed list is now guarded against pyproject.toml. 2026-08-09 (desktop): +1 U17 closed — the staleness ranking is defined (ranks on the claim; git fact and the gap reported beside it). 2026-08-09 (desktop): +1 U15 closed — snapshot `dirty` narrowed to tracked changes, untracked_present recorded beside it on both header blocks. 2026-08-09 (desktop): +1 O52 closed — the always-null holder_sid column; the fix is one word, the deliverable is the pair of guards (both proven to fail on the reintroduced defect). 2026-08-09 (desktop): +3 J34+J35+J38 closed — the port-machinery trio BUILT as one bundle (branch feat/port-machinery-trio, --no-ff merge 09c1be1): the overlay seam, the ledger roll through 5417ef10 + mandatory closing sequence + SHA citations, and the standing relay section. 2026-08-08 (desktop): +1 G57 (withdrawn-closed, no rename). Prior: 2026-08-07 (desktop pm): +2 G59, J36. Prior (LAPTOP, groom): +2 — Q16 (partial close, see the in_progress line and the item note) and O56, the /software console page, which was BUILT before any item claimed it (9b4cf59) and is retro-groomed `done` so the ledger carries the surface. Nothing depended on either, so no item is unblocked by these closes. Earlier: 2026-08-07 (LAPTOP): +1 L24 — executive overview rev 8, the target-state goal stated in §06. Nothing depended on it, so no item is unblocked by its close. Earlier: 2026-08-07 (desktop): +1 A4 closed (the dcat per-standard README — resolution (a), DCAT is load-bearing). Earlier: +1 S6 closed (six config-family JSON Schemas + the core-free guard). Earlier: +1 S7 closed (the folder-vs-module naming rule, written once). Earlier: +1 O32 closed (the light pass — one cascade bug, the O36 class, fixed at the root). Earlier: +1 O30 closed (App.css retired to a comment-only stub). Earlier: +1 O29 closed (trust-tier legend live on both canvases).   # 2026-08-07 (desktop): +1 G58 (the archival report; see its close note). Earlier: 2026-08-07 (desktop): +1 G23 (the rua curated load; see its close note). Earlier: 2026-08-07 (desktop): +1 G55 (G22 vocabulary consequences applied). Earlier: # 2026-08-07 (LAPTOP): +1 G22 — the rua load-shapes gate SIGNED OFF 28/28 after a two-day A→J walk; G23 (curated load) and G55 (vocabulary consequences) BOTH enter next_ready, and they are deliberately separate items so the flip is never made silently as a loader detail. Earlier: 2026-08-06 (desktop): +1 O46 — intake API + origin-flagged store shipped (fd29f3a): drydocs_api/intake.py + FastAPI shell + context-intake landing zone; thread-delta detection, legal-transitions map, sha256/relative-key storage seam; 31 offline guards, ADR 0009 write guard amended with the argued evidence-landing exemption. O47 (deps O45+O46) becomes ready. Earlier: 2026-08-06 (desktop): +1 O45 — context-type vocabulary shipped (a4108fc): config/taxonomy/context-types.yaml v1 + generated context-types.json on the default render run + 7-test guard incl. drift and seed-value permanence. Earlier: 2026-08-06 (desktop): +1 N8 - the four unregistered-source map entries ruled (two re-points, one stub registration, one displayed exemption); the N4 day-one render defect is closed. Earlier: 2026-08-06 (desktop): +1 O42 - web/ import edges live via the instrument's new ts-imports extractor (option (a); depgraph a56d2fc); earlier history on the todo line. Prior note:                                        # 2026-08-05 (desktop): +1 J32 — the field-meaning rule written down in RELATIONSHIP_GUIDE.md (nothing depended on it). Earlier: +1 K19 — mapping-age detection (detect-and-report only; the effective-dating question stays gate material, see the item's progress note). Nothing depended on it, so no item is unblocked. Earlier: +1 C24 — catalog_lobs/dev_teams coalesce their enrichment values; the C22 pin now runs over five loaders. 2026-08-05 (desktop): +1 S10 — the four :BusinessApplication MERGE loaders now refuse a pre-cutover graph before any write. 2026-08-05 (desktop): +1 K15 closed — spec deleted, domain flipped unavailable-but-LISTED (a vanished tab reads as a bug), ~495 lines of route removed, two tests INVERTED into retirement guards. Earlier:2026-08-04 (LAPTOP, latest): +1 N6 closed - the two load sequences retired; ingest.sh and the runbook's Appendix B are now PROFILES of cli.CANONICAL_LOAD_SEQUENCE, the ingest.sh subset is RULED deliberate with per-step reasons, and the surface-inward completeness guard closes the non-loader-verb gap. Earlier: 2026-08-04 (LAPTOP, latest): +1 X4 closed — the laptop ddlineage drop, venue-tagged (neo4jtest, 2026.05.0 EE); zero-node probe first, the USER ran the two destructive statements (permission-blocked for the agent), four-name topology + two-constituent smoke (2218/46) verified. Epic X complete producer-side. Earlier: 2026-08-04 (desktop, latest): +1 X3 closed — the desktop ddlineage drop, venue-tagged (neo4jtest: probe 0 nodes, alias out of ddall, DROP DATABASE, four-name SHOW DATABASES, smoke green; ddschema observed PRESENT — the parked divergence note is half-stale). Earlier 2026-08-04 (desktop, latest): +1 X2 closed — the ddlineage repo sweep (topology anchor 5->4, SUPERSEDED_NAMES gains ddlineage w/ the "retire" admission token, runbook Rev 9, port-ledger step 83 w/ company drop protocol). Earlier 2026-08-04 (LAPTOP, latest): +1 V2 closed — drydocs-core-runbook.md; core off RUNBOOK_PENDING. Counts RECOMPUTED at a rebase against the desktop's X2 claim (both landed, no number hand-merged). Earlier 2026-08-04 (LAPTOP, latest): +1 V8 closed — drydocs-api-runbook.md authored distinct; drydocs-api off RUNBOOK_PENDING. Counts RECOMPUTED at a rebase against the desktop's X1 close (both landed, neither number hand-merged). Earlier 2026-08-04 (desktop, latest): +1 X1 closed — the ADR 0002 amendment retiring ddlineage (dated section + gate-log RECORD on the G51 idiom + ADR 0006 as-of-date parenthetical; gates.json render caught the new row first, as designed). Earlier 2026-08-04 (LAPTOP, latest): +1 G54 closed — provision.ps1 is exec-aware; a Docker-only host provisions with no flags and no host cypher-shell. Earlier 2026-08-04 (LAPTOP, latest): +1 V1 closed — the SME-runbook coverage rule is now a TEST, not a question someone has to remember to ask (the trigger: the mapping-store runbook was stale in two directions at once and only a person asking caught it). Unblocks V2-V10. Earlier 2026-08-04 (LAPTOP, latest): +1 J30 closed — wip/k9-laptop disposed. The parallel K9 lost on tests, layering and ritual, but carried ONE thing shipped lacked: the K7 §B2 readback for declared_end_state (shipped had the view and the authoring UI, no reader). Lifted as GET /mappings/app-code/migrations, branch deleted local+remote. Earlier 2026-08-04 (LAPTOP, latest): +1 done S4 closed same session it was claimed — the console draft substrate (ADR 0009 rule 5): draft table + rebuild carry-over, both write endpoints off commit-by-replace, promote emits an additive diff, console moved to draft-then-promote. Earlier 2026-08-04 (desktop, latest): +1 S5 closed same session (d84d86b — the two monolith registries split into per-domain fragment dirs behind drydocs_core/yaml_fragments.py).  2026-08-04 (desktop, latest): +2 G36+L20 closed same session as claimed (G36 = ea66764 index drop w/ recorded no-index decision, live-verified neo4jtest/drydocs; L20 = c075ee2 feedback stray-file guard).  2026-08-04 (LAPTOP, latest): +2 Q6 (the docmeta component — both generic connectors, producer-authored against the T21 shape, 46 offline tests) and Q12 (its numbers moved from module literals into config/doc-capture.yaml, now read by BOTH doors). Earlier 2026-08-04 (desktop): +1 J29 — the UTF-8 no-BOM standard, groomed straight to done (SME ruling in-chat; three sample CSVs stripped, guard proven live on a probe, the M3-reload BOM diagnosis corrected to a PowerShell-pipe writer artifact). Earlier 2026-08-04 (LAPTOP): +1 Q13 — the vendor-docs pipeline, built 2026-07-31 and never closed; the close verification found three silent-reporting defects and fixed them (rows_changed structurally 0, corpus_id vs capture id, bare-stem doc_id). Earlier 2026-08-04 (desktop): M3 (the doc-06 Phase-4 envelope gate — all four sources RULED stubs with recorded whys and revisit triggers; the SME's two-era lifecycle-date evidence reversed the drafted recommendation; internal-twin clause discharged) + M4 (the property-term gate — dct trio + contributor, property_terms section 0b created, dct: namespace + dcmi-terms standards stub, three drift guards). Same day, LAPTOP: +1 G52 — the laptop Docker cleanup, landed done in one pass (a chore whose whole content is destructive verification; claiming it forward would have been ceremony). Removed the 5.26 images, eleven orphan volumes, and the retired neo4j-drydocs-ee data volume that outlived its container; the last of those forced runbook Rev 8, since Appendix A was still promising a rollback copy that no longer exists here. G50 (the desktop's equivalent) is deliberately untouched. Earlier 2026-08-04 (desktop): the J-series close-out — J18 (live-verification venue convention: pull-rule step 5 + CLAUDE.md agreement; P3/G33 retro-tagged, P5 the worked example), J26 (text-guard sweep: family inventory in test_text_guard_conventions.py, strip_comments on every negative cypher/SQL assertion, self-description regression proofs), J27 (.gitignore comments reworded to describe the corpus, scan D domain-shape guard proven against the removed line), J28 (render_gates heading-identity classifier: citations and PARTIAL rulings never close a gate; two genuinely-awaiting gates surfaced open, the superseded proof case pinned as a fixture). Earlier 2026-08-04 (desktop): C22 (the catalog loader sweep — the orphan shape swept into product_lines + area_products, coalesce on all three name SETs with the row models made sparse-refresh-tolerant, and the C17-deferred envelope question RULED EMIT: `drydocs.loader/unresolved-parent` rides the O28 envelope, per-node flag stays the durable record; dev_teams/catalog_lobs same-shape finding inboxed). Earlier 2026-08-04 (desktop): U13 (tombstone filter on every seed query — pack, plan, groom-skill checks; the A3 proof case discharged with a finding: the 2026-08-03 wipe left zero tombstones, filter guards recurrence) + U14 (seven-root $packages allow-list in the queries; live numbers 77/54 and 129 reproduced, scoped untested 29; the predicted ~23 in-package orphans corrected by evidence to 0 + a separate 22-candidate first-party non-package queue). Earlier 2026-08-04 (desktop): U12 (snapshot retention ENFORCED in snapshot.ps1 — after-the-write, pattern-anchored, proven live on five stale files incl. three same-day 20260803s the manual ritual missed; README trued to the all-files instrument incl. the fourth marker 238->1457; +3 guard tests). Earlier 2026-08-04 (desktop): K13 (the §G6 catalog_has_application back-flow — support reading reconciled onto the vocabulary, a NEWLY CREATED ontology-map entry, the supplement, the extractor skill, and the K11 pointers; C9 condition untouched, vocabulary stays planned). Earlier 2026-08-04 (desktop): K11 (the steward mapping cascade — the §G act built end to end: cascade pane, the missing draft route, the §G1 confirmed USES_SOFTWARE authoring in both attribution writers, §G2 declared-origin stamps, four cascade specs, TC-12). Earlier 2026-08-04 (desktop): K10 (port activation cutover — active_state declared|confirmed with derived confirmation, the never-read boolean retired). Earlier 2026-08-04 (desktop, later): K12 (app-code taxonomy capture — the waiver closed) + K14 (composite-key value-form sweep — two strays converted, one newly found). Earlier 2026-08-04 (desktop): K8 — the folder-grain attribution loader (see K8's close note; suite 1399, ruff clean, renders + schema graph regenerated; the publish-boundary guard's real catch fixed in the same commit). Earlier 2026-08-03 (desktop, night): K9 — the K7 defined-mapping store built as the O24 pattern rules (§E1 verbatim): committed CSV config/overrides/app-code-mappings.csv -> app_code_mapping table in mapping.db (tier + origin CHECKed, per-row fail-closed validation shared between ingestion and the draft endpoint so an artifact can never be refused at materialization) -> the app-code-mapping console domain with v_app_code_grid (code-level rows before per-folder resolutions, defined/override adjacent and origin-flagged per §B3) and v_dual_coded_migrations (§B2 — every tier-3 row with its DECLARED end state, so a stalled migration cannot hide). The two ruled asymmetries vs O24 are built in, not glossed: rows ARE the source of record (§E2 — no machine feed exists), and overrides may be PERMANENT (no status column, no corrected-in-source flip — rationale is required instead, because the why must travel with a permanent row). matched-fallback is REFUSED at authoring (derived at load, disclosed on the edge — §B3). K2_SHAPE flipped to the ruled edge (ControlMFolder -[BELONGS_TO_APPLICATION {seal_app_ref}]-> Port) and draft_changeset rekeyed with it (app_code authoring keys, app-codes-to-apps filenames, honest K8-queue note); the retired job-grain entry shape now fails closed rather than being silently remapped. Manual-loads TEMPLATE rekeyed to app_code=<CODE> -> app_id=<APPID>; the parser DELIBERATELY still enforces the job-grain shape until K8, pinned by test with the rationale written in. Suite 1384 (+28), enforcement matrix re-rendered (its drift guard fired first, as designed). Earlier 2026-08-03 (desktop, evening, after L22): C23 — the DQV-seed gate ran in-chat and the SME ruled DEFER: the 10-metric/5-dimension catalog stays as reference data, BUILD declined honestly (no measurement feed exists — writers would have nothing to write), PRUNE declined because the catalog is not orphaned (the shipped SOSA Result and cm_avg_run vocabulary notes already reference dqv:QualityMeasurement, and freshness_sla is a seeded metric name — the temporal-runtime observations are the first real customer). The user's post-wipe question ("what are the metrics associated with?") now has its answer written where the next reader looks: ontology.cypher carries the ruling + revival trigger above the seed, and the vocabulary gap that predated the discipline is closed — c23_in_dimension ACTIVE (registered retroactively), c23_is_measurement_of / c23_computed_on / c23_has_quality PLANNED per LoadPlanV2 §4.4, new `quality` domain. Gate-log entry dqv-seed-disposition; no graph write, no constraint change. Earlier 2026-08-03 (desktop, evening): L22 — the style guide is now part of the writing path, not a memo: CLAUDE.md §6 carries the prose-style agreement and the documentation skill opens with it, so the next doc follows the guide without being told. The exec overview went to rev 7 with all 21 flagged occurrences rewritten (spine→backbone, planes→layers, decays/rots→goes stale/becomes outdated, gate-confirmed→validated through the SME gate, UK spellings Americanized) and TWO decisions written down where the next reader looks: the HTML stays the single hand-authored source (no .md established — non-governed editorial surface), and content claims were NOT refreshed (ADR statuses predate S1/S2 landing — that is a content revision, not a style pass). The whitepaper needed exactly two edits ("cannot rot"→"cannot go stale", "Metadata rot"→"Metadata drift"), applied identically to the .md and both renders; the site plan was already clean. The fence held: docs/style/l22-idiom-inventory.md ships the candidate-rename list (review spine, lexical spine, guardrail N, gate-confirmed, HITL `confirmed`) with what each rename would touch — notably "guardrail N" is cited by number in company PORT-REPORTs, so it is recommended KEEP. Earlier 2026-08-03 (desktop): G51 — ddschema provisioned + the guard widened, in PROOF ORDER: test_database_names' identifier match went from exact-`DATABASE` to any `*DATABASE*` constant FIRST, run, and it failed on exactly `drydocs/cli.py: SCHEMA_GRAPH_DATABASE = 'ddschema'` with zero false positives (the three existing constants stay matched); THEN 01_databases.cypher gained `CREATE DATABASE ddschema IF NOT EXISTS` (deliberately NOT aliased into ddall — schema description, not estate data) and the suite went green. Topology anchor moved 4 -> 5 names in the same commit, as the acceptance demanded. ADR 0002 amended (dated section), the SME "2 different graphs" direction RECORDED in gate-log (a record, not a gate — no ontology change, no graph write), runbook Rev 6 names bootstrap-schema-graph in step 3 + Appendix B (rev pin bumped 5 -> 6 in the same change and it caught the edit, as designed), run-drydocs skill chain updated. Timed with the user's manual Neo4j wipe: the post-wipe rebuild now creates ddschema at provisioning instead of relying on the hand-made database G51 existed to end. Earlier 2026-08-02 weekly groom: +2 RETROSPECTIVE closes for work that landed at e3f65af before an item existed — U9 (whole-repo scanner by default, the git-ignore filter, the 101-file series retired to one, the loader's tree acceptance and the two defects fixed in-run) and C21 (the schema meta-graph into its own database behind `drydocs bootstrap-schema-graph`, the NODE KEY blocker proven live on a throwaway label). Groomed retrospectively rather than left invisible because committed code cites "U9" as an id in three files. Earlier 2026-08-02 (laptop): S2 — drydocs_core/orchestration/ as the parent of controlm/. The item's real value was crosswalk.py: config/crosswalks/*.yaml had been gate-confirmed since 2026-07-14 with NO runtime reader, so `fidelity: no-equivalent` was prose nothing could enforce and an unmapped concept could be rendered as the nearest Control-M label. It now RAISES. Two corrections to the ADR, both recorded in it: the Control-M field routing it scheduled out of commands.py was NOT IN THAT FILE (docstring only — the routing lives in variables.py and staging.py), so fields.py is deliberately thin rather than padded to fill a planned module; and action item 4's guard had to be narrowed, because 'nothing outside orchestration/controlm/ imports Control-M modules' fails on its first run — the loaders ingest Control-M and are right to. paths.py needed a SEAM not a cut: PathDialect lets the vendor supply its token vocabulary while the neutral half keeps the shape, so a second orchestrator adds a dialect instead of forking. Zero graph change. Earlier 2026-08-01 (laptop): S3 — the BusinessApplication identity cutover. seal_id -> app_id as the canonical MERGE key across all 8 key-bearing sites in ONE change (a partial flip would not fail, it would SILENTLY DOUBLE the node — uniqueness ignores nulls), seal_id retained as a dual-written alias, :Port NODE KEY following as port_app_key(parent_app_id, kind). The gate's §D1 trap was PROVEN rather than quoted: on a scratch database, re-declaring a constraint with a DIFFERENT key under the SAME name succeeded and left the old definition live — so the DROP is load-bearing. Two of the eight line numbers in the acceptance had drifted since the gate (the SET of sites had not). :Port deliberately got no alias — the gate measured that parent_seal_id has zero readers, and a NODE KEY cannot be null. Phase 2 put app_id on the wire (6 QuerySpecs, mappings.py, four web files) while the committed FILE formats stayed put and are aliased at the boundary: manual_mappings.py refuses a row whose target_key is not literally `seal_id=`, so renaming it would stop every committed manual-load CSV from parsing — that belongs to §G3's retirement gate, and ADR 0010 action item 6 now carries it. match_method stayed 'seal' (§B2(ii)); the only fixture change went the other way, correcting an invented 'seal_var' the graph never writes. The §B1(c) column ledger landed with both its uncomfortable facts written in: only ONE of its 92 column names is an SME-confirmed source header, and the row model and the registry disagree about whether the source is a psgmgr view or a Reports export. 2026-08-01 (desktop): J24 retired-tier residual sweep. The item's own framing undersold it: the retired tier was LIVE in shipped code (two registered specs, the export prefix map + banner) and in TWO web copies of the vocabulary that neither J23 nor this item listed. Ephemeral ceiling RE-POINTED not deleted (rule preserved over value). Ruled the open unsigned-prompt question the C17 session left: sweep it — its Registry ref line assigns a tier to a future entry, so it is not merely descriptive — which pulled in the two other unsigned prompts, one of which (rua-load-shapes F1) asks the SME a question whose two-tier premise J23 removed: FLAGGED for re-framing, not re-decided. Guard asserts the api and config vocabularies AGREE rather than scanning for a token, so it survives the next change too. Suite 1336.  2026-08-01 (laptop): S1 — ADRs 0008/0009/0010 ruled. 0008 and 0009 ACCEPTED as proposed; 0010 ACCEPTED WITH AMENDMENTS because accepting it as written would have ratified `id_authority`, a property its own gate WITHDREW on 2026-07-27 — the ADR's body carried a "do not build from this as written, amendment owed at S1" banner, and S1's acceptance requires editing to the ruling rather than annotating around it. Unblocked S2/S3/S4 (p1) + S5/S6/S7. Earlier 2026-08-01 (laptop): Q7 docs-verify — the doc-source registry reconciled against the graph for the first time. Found `dddocs` DECLARED BY TWO CORPORA BUT NEVER PROVISIONED (01_databases.cypher creates drydocs/ddlineage/ddcontext/ddall only) — reported as db-absent, not failed, because the topology is G32's open ruling. Also forced a design fact into the open: the registry is corpus-keyed and the graph is not, so every entry now declares `graph_locator` and the check stays registry-driven instead of hard-coding three loader shapes. Earlier 2026-08-01 (desktop): R6 Tier-2 bounded enhance/solve loop — a PURE controller (llm/retrieve callables, not the pipeline) reusing Tier-1 retrieval so the <=2 fix loop and read-only pre-flight are inherited, not reimplemented. Two bounding calls the acceptance left open: a tie or unparseable vote counts as SOLVE (voting's failure mode is a loop that won't stop), and the token budget bounds EXPLORATION not the run (evidence already bought still gets one final answer call, recorded as exhausted; with no evidence and no budget the loop makes zero calls). The R1 in-process residency proof is STRUCTURAL — task_graph.py has no driver/session/db-name/Cypher and the test reads the source. Fixed a real gap named only in passing: the ledger recorded iteration=1 for every call, so per-iteration cost — the thing that makes the caps tunable — did not exist. Web: the d3 placement engine moved to lib/forceLayout.ts instead of being copied; TaskGraphPane keeps its own visual language so a synthesized sub-question is never mistaken for a real graph node. Earlier same day: C17 PAT grain keying — the item's own premise was wrong and the user's SME fact corrected it: every catalog grain (LoB, Sub-LoB, Product Line, Product, Area Product) carries a NUMERIC id at source, so the team report's name-only Product Line is a report projection, not a source limitation. §a rules the id-carrying extract outright (name -> id demoted to a bounded migration path that must report ambiguity); §b records Supporting -> area_product_id / Sponsoring -> sponsored_area_product_id, co-populated so the two independent cypher blocks were already right; §c rules Sponsoring Product Line out of scope while name-only. The fact also exposed a live defect the rider never asked about — pydantic v2 does not coerce int -> str, so a numerically typed read of the real feed rejected EVERY catalog row; attribution.py already had the coercion, the catalog loaders never got it. Corollary: having ruled the join is by id, products.cypher's silent parent MATCH became OPTIONAL MATCH + a per-run orphan flag that keeps the unresolved id (the old `orphan` flag could never be true). Sibling loaders and the Sub-LoB gap both INBOXED rather than swept. Earlier same day: O28 node-status envelope — one shape {type, level, message, error?} for every health signal, always DERIVED, extended by NAMESPACED TYPES never new fields. Producer (BaseLoader, pure fn), wire (loads.status-items.v1), and both UI surfaces (inspector section + hub spoke glyphs) share one contract. Stored as a PROPERTY not an edge — a relationship would have been an ontology decision needing the HITL gate for a signal with no ontological meaning, which would have made every future producer gate-bound. No all-clear item, so healthy stays distinguishable from never-observed. Earlier same day: O27 QuerySpec authoring conventions — drydocs_api/AUTHORING.md, three rules with rationale, linked from query_specs.py. The registry audited CLEAN on all three, so the deliverable is the rule + the guard, not a fix list. Three corrections to the item's own framing: the ref-grammar namespace is the DECLARED UNIQUENESS SCOPE, not the data center (that holds for 1 of 6 kinds — jobs and conditions are folder-scoped), and the doc's table is now held to constraints.cypher by a test; rule (a)'s non-violations had to be stated (a NODE-KEY property join is identity resolution, not meaning re-derivation) or the rule would push authors into worse Cypher; and the element-id guard went to guard.py covering BOTH the registry and the LLM-authored ephemeral path, because the acceptance's "registry invariant" would have guarded the only path that cannot fail. Earlier 2026-08-01 (laptop): J10 ruff convergence, producer side COMPLETE — 1,017 findings to 0, 284 of 328 files reformatted, CI now blocks on both `ruff check` and `ruff format --check`. Half the post-format residual (177 of 362) turned out to be VENDORED skill scripts and became an extend-exclude; 30 findings in our own code were fixed rather than ignored. Three things the sweep surfaced that a bulk-ignore would have buried: ruff's F841 unsafe fix leaves dead calls behind (6 across 3 files), a `pytest.raises(Exception)` that never proved what its name claimed, and a double-encoded "â†’" masquerading as a RUF003 style nit. The J16 fall-through guard caught .git-blame-ignore-revs having no PORT-MANIFEST row — now an explicit never-port. Earlier 2026-07-31 (pm, oldest-first pull): L21 runbook Rev 5 — the collapse found a real defect, not just verbosity: step 3 and Appendix B both listed the three PRE-G29 per-file verbs (base/seal/catalog) and OMITTED registry, so anyone following this runbook literally never seeded the registry supplement that load-software-registry (two lines below it, in the same runbook) MATCHes — the exact G29 'runs and seeds nothing' class, living in the doc that teaches the procedure. Both blocks now run the one verified `apply-supplements` chain (whose per-term verification makes a green run EVIDENCE the terms landed); load-doc-traceability named in the ingest step (shipped by L7 after Rev 2 was signed, never added); rev pin in test_doc_traceability_loader.py bumped 4->5 (it caught the bump, as designed). Numbering note: the item says 'Rev 4' because it was groomed before Rev 4 (plugins) landed the same day — this is that revision, one number on. + J25 the L7 ratification snippet committed as port-prompt §6a — the gap was DURABILITY: port-prompt claimed the snippet was 'provided in the producer session 2026-07-21' and a repo-wide search for 0252d29 / PORT-REPORT-6fd3270 found only prose, so the company gate pack cited an artifact no company session could open, and T11 had read `pending` since. Split written into the block itself: producer-gate facts (6 classes + 6 doc_* ids, all verified against gate-log / relationship_vocabulary.yaml / taxonomy-ontology-map.yaml; commit 0252d29 confirmed reachable) are PRE-FILLED; check results + sign-off are bracketed placeholders because they describe the COMPANY tree — an entry citing numbers nobody ran is worse than no entry. T11 row now points at §6a. The company still signs its own ratification. Suite 1269. Earlier 2026-07-31 (desktop): N9 source-registry v2 BUILD (15 systems / 28 datasets / 17 retired ids; doc-ledger union; D2 overlay seam; derived URNs; 14 loaders re-bound; ledgers/captures/map/audit re-keyed; cm_avg_run stale-note correction; Q6 amendment in gate-log; suite 1204). Earlier 2026-07-31 (desktop): N7 registry-v2 gate session run in-chat (D1-D4 + Q1-Q6 all ruled w/ 2 SME amendments — seal_id placeholder, origin@db.schema.table grammar; T19 pat:product-catalog naming note to the divergence ledger; migration table = explicit per-row residual at the N9 build; gate-log source-registry-v2 + prompt authored). Also 2026-07-31 (desktop): J23 classification collapse (3 levels; 7 source-registry + 1 doc-registry + 3 taxonomy relabels w/ confidential-handling comments kept; both test assertions updated; CLAUDE.md/PUBLISH-BOUNDARY/config-README tables collapsed; internal READMEs restated; history left as history; suite 1188). Earlier 2026-07-30 (desktop): J22 the J16 guard's untracked blind spot closed same day it was groomed (walk = tracked + --others --exclude-standard, pure fall_through_orphans + mechanics pin, [untracked] tagging + .gitignore resolution in the message; live-proven with a pre-add probe failing and the clean tree passing). Earlier 2026-07-30 (desktop): R5 Ask spoke (/ask for every persona — streamed steps via on_step+SSE, control-part owner-token handshake, citations w/ trust+classification, SpecGrid-on-eph-ref explorer/export, metrics chip; wire-level live e2e + headless render check), R3 agent run telemetry (per-call JSONL ledger w/ price map in DRYDOCS_LOGDIR — full question text ledger-only; :AgentRun kind='qa' via dedicated ddcontext writer that refuses drydocs; console.agent-runs.v1 admin spec; hashed caller-identity slot filled from ADK ctx), R4 ephemeral session-scoped QuerySpecs (eph.<hash> refs, agent-key-gated registration, frozen params, TTL+capacity bounds, one manifest code path, /raw-cypher untouched; pipeline explore_ref hook + urllib client, live wiring rides R5). Earlier 2026-07-30: J19 pull rule documentation (pushed-claim discipline codified in CLAUDE.md §0 + backlog.yaml header + items inputs/acceptance updated), J21 source-registry hardening (duplicate-id refusal + shipped-registry uniqueness pin + loader-field/N3-binding agreement guard + autosys/airflow stale-note refresh; 087a685). Earlier — 2026-07-29: N3+N4+N5 the load-map chain — N5 human surface (docs/plan/load-map.html, placement decided plan-not-design, ritual-wired, print-ready) — N4 render (load-map.json, 18 sources joined, rides the default board render; caught 4 map entries citing unregistered source ids on day one) on top of N3 load-map joins declared (source_id on all 25 loaders + one named exemption; chains lifted to consumed constants; CANONICAL_LOAD_SEQUENCE; 11-test guard). Earlier 2026-07-29 UI sweep O35-O41: O36 root cause = unlayered *{padding:0} reset out-cascading layered Tailwind utilities (preflight keeps the reset; box-sizing only retained); O37 radius tokens (lg collapsed onto md=10px, App.css/index.css radii tokenized); O41 status-vocab map + O38 IdChip/StageBadge convention (UI-WIP/ui-conventions.md) adopted inspector+loads; O39 runtime-view env-template slot (mechanism only, renders nothing unset); O40 StatTiles click-to-filter live on loads (Failed tile 4->1 verified); O35 category-first landing per SME FB-01/FB-02 (hub demoted, name renders once, modules+towers pick-lists, data-wf keys kept). Both themes screenshot-checked; build+lint green container AND device. # 2026-07-29: G48 resolve-cmdline-staging (store v3 + resolution_quality provenance; verbatim-beside-derived; precedence evidence counted never ruled), G47 controlm-xml-export seam (taxonomy-first defs+ordered-variables staging, scope_layers handoff proven against the resolver), G46 resolve_command_line on the shared resolver (one scope-chain walk, substitution provenance with winning scope, %%VAR-launcher round-trip proven). 2026-07-28 late: G42 Snowflake data-catalog seam (source registered + catalog_dir landing zone + SnowflakeCatalogExtractor staging both views, three-shape discrimination, sentinel counted, latest-per-GUID, origin routing, urn fact column; G43 cross-checks unblocked). Earlier 2026-07-28 evening: G45 rua listing fallback (presence dispatch, 561-script company incident's producer parity), C20 K4-comment re-scope (SEAL-loader-specific deprecation; live catalog writers named), R10 agent-runtime alignment (google-adk pinned >=2,<3 + ADR 0007 revisit date-stamp), J20 one-render-entry-point part 2 (matrix rides the default board render). Earlier: C16 key-prefix governance in the metadata plan (drydocs./system/team-local classes), C15 inverse_label on all 78 vocab entries (presentational only, schema-test enforced), L18 depth-0 cell split (the K2 shear healed; conformance guard over the real matrices), D8 bootstrap constraint guard (names-not-counts presence check after apply, live 51/51), J17 gates.json folded into the default-paths board render (drift closed at the ritual level). Earlier 2026-07-28 pm: P5 maintenance-window query (patch-window CLI + union-not-sum window math + findings feeder; claimed-then-closed same day — EPIC P COMPLETE 5/5), U5 groom-skill graph cross-check subsection (executed inside the close-out groom — EPIC U COMPLETE 6/6), U4 tech-debt graph-evidence section (hardened A1-A6 pack + offline fallback, all probes run live), U6 depgraph scanner blind-spot fix (shared-namespace extract_many + drydocs_api root; 205 modules / 370 IMPORTS, all 24 U1 orphan false-positives resolved). Earlier 2026-07-28: G41 glue base-table inventory seam (built + closed same day), L17 doc-loader prereq guards (same), O33 SchemaMeta exemplar audit (22 specs + runs_on write path; live-proven), O34 npm advisories (postcss cleared; react-router escalated via the stop clause), U1/U2/U3 code-graph persona reviews run 1 (scanner blind-spot headline; backlog verified truthful; doc board + citation sweep queued), C19 ontology-comment fix (groomed + closed same day), J16 port-manifest fall-through guard (same), L16 runbook container/port refresh. 2026-07-27 closes: J14 SEALID split, P3 hosts+RUNS_ON, G21 rua code-ops, G24 code-repo seam, G26 launcher-registry config, Q8 bmc-docs prereq, C18 shadow-model guard, J15 value-shape boundary guard, G33 code-snapshot loader (gate signed + built same day), G39 cmdline staging store + G40 parse (groomed am, built pm)
  next_ready: [K20, N10, N11, O23, Q14, Q15, O26, O31, R7, R8, S8, S9, S12, G34, G70, G74, L19, D9, D10, O43, O44, O48, O50, O54, O55, O57, O58, B5, R9, V4, V5, V6, V7, V9, V10, R11, R12, R13, R14, G56, U10, U11, U16, G61, C26, L23, L25, L26, J31, J33, J37, J39, Z1, V11, G63, G64, G65, J40, C28, Q17, G68, G69, J42, J43, J44, J45, J46, J47, C33, U20, K22, K23, G81, G78, G80, C34, G82, K24, G85, G86, G87, G89, G90, G92, G93, G94, G95, U21, U22, U23, G97, O60, O61, O62, G100, N14, N15, Q19, G38]  # 2026-08-18 (targeted groom, Idea-134): +N14, +N15 — the two builds the pending-source-correction gate authorized at its 12/12 sign-off the same day; both are dependency-satisfied on arrival (N12, N13, O24 all done) and independent of each other, so either can be pulled first. Neither is a gate — the lifecycle is already ruled and these implement it. Prior: 2026-08-18 (targeted groom, Idea-133): +N12, +N13 — both dependency-free on arrival (the acquisition-path build and the gate prompt that rules its flip together with O24/K9's override->source-corrected flip). Prior: 2026-08-18 (desktop, direct add): +S12 — dependency-free on arrival (the guard needs nothing built first; poetry.lock and tests/conftest.py both exist). S11 is deliberately NOT here: it depends_on S8, which is todo. Prior: 2026-08-17 (desktop, targeted groom — UI/UI-WIP): +O60 +O61 +O62 — all three dependency-free on arrival; O61's tie to G94 and O62's tie to Idea-125 are recorded as ties in their notes, not as depends_on, so neither waits. Prior: 2026-08-13 (desktop, targeted groom): +U21 +U22 +U23 +G97 — all four dependency-free on arrival. Q18 is deliberately NOT here: it depends on G32 (in_progress) and Q14 (todo), which is the inbox entry's own parking reproduced as a dependency rather than overridden. Prior:# 2026-08-13 (desktop): -G96 (claimed in_progress). Prior: 2026-08-12 (desktop, targeted groom): +G92 +G93 +G94 +G95 — all four ready on arrival: G93 and G95 are dependency-free, G92's three deps (G14, G46, G60) are all done, and G94's G84 closed the same day. Prior: 2026-08-12 (desktop, evening): -G60 -G83 -G84 (batch-claimed in_progress, desktop; G83+G84 claimed together per their sibling notes). Prior: 2026-08-12 (desktop, pm groom): +J48 (dependency-free — the repo_paths seam it extends already landed at 841dc6e5). Prior: 2026-08-12 (desktop): +G85 +G86 (both dependency-free; G85 is gated behind a real fix diff EDITORIALLY, not by depends_on — the demo diff exists and the corpus load is its own declared input). Prior: 2026-08-11 (LAPTOP): +G82 (dependency-free — both loaders, both sources and the fixture are already producer-side; only the projection is missing). Prior: 2026-08-11 (desktop): +G81 (dependency-free, p0). Prior: 2026-08-11 (desktop): GN1 done — GN2 still waits on G79, deliberately, so the split lands under the OLD names. Prior: 2026-08-11 (desktop): +G78 +G80 (both dependency-free; G78 is the p0 correctness bug and G80 the guard that catches its family). G79 waits on G78, GN2 on GN1+G79. Prior: 2026-08-11 (desktop): +C34 (dependency-free; the SKOS declaration half is taxonomy and needs no gate). G77 stays out — depends_on C34. Prior: 2026-08-11 (desktop, weekly groom): +10 — every item raised at this groom is dependency-free, so all ten enter next_ready on arrival. Prior: 2026-08-11 (desktop): -G75 -G76 (the renumbered back-flow pair, claimed in_progress and pushed before work). RECOMPUTED FROM ITEMS at the concurrent-push repair, never merged textually — origin/main carried COMMITTED CONFLICT MARKERS in this block (<<<<<<< Updated upstream / >>>>>>> Stashed changes), so backlog.yaml did not parse on the trunk and neither side's list was correct: the desktop side predated the G35 close, the laptop side predated the back-flow raise. +O44 enters here, unblocked by G35 done. Prior (LAPTOP): +G74 at the G35 sign-off
  # 2026-08-04 (Epic X groom): X1 dependency-free -> next_ready (claimed same day);
  # X2 waits on X1, X3/X4 on X2 (the live drops must not run ahead of the decision
  # record + sweep).
  # 2026-08-04 (Control-M groom): K15 groomed dependency-free (K8/K9/K11 all done) -> next_ready.
  # 2026-08-05 (desktop groom): +3 promoted from the same-day IDEAS entries — C25 (run the
  # software-version-context gate) and K16/K17 (the FID census, then the fid-identity-and-scope
  # gate). C25 + K16 are dependency-free -> next_ready; K17 waits on K16 BY DESIGN: the census
  # produces the disagreement breakdown the gate cannot sign without.
  # 2026-08-05 (desktop): K15 CLOSED same session it was reviewed — leaves next_ready by
  # closing, not by being claimed. Nothing depended on it, so no item is unblocked by it.
  # 2026-08-04: Q6 done -> R7 enters next_ready. R7 was RELEASED UNBUILT on
  # 2026-08-01 with the reason "no fetcher exists to rescrape with" (01c657c);
  # that fetcher now exists. Q12 left next_ready by closing, never by being
  # claimed — see the todo-line note.
  # 2026-08-04: Q13 done -> Q14, Q15 and Q16 all enter next_ready (each depends on
  # Q13 alone — the gate DRAFT, the QuerySpecs and the currency report). Q14 argues
  # from the loaded spine, so it wants a machine holding the capture; Q15 and Q16
  # are buildable anywhere.
  # 2026-08-04: Q6 and Q13 left next_ready (batch-claimed in_progress, LAPTOP — the Q-series
  # pickup). Q12 stays OFF next_ready: it depends on Q6 and becomes ready when it closes,
  # even though the guardrail it specifies already exists in scripts/ (see the Q12 notes).
  # 2026-08-04: M3 and M4 left next_ready (batch-claimed in_progress, desktop — the shared envelope-gate session).
  # 2026-08-04: J18, J26, J27, J28 left next_ready (batch-claimed in_progress, desktop; J13 stays — ready by deps but user-gated by its own acceptance).
  # 2026-08-04: C22 left next_ready (claimed in_progress, desktop).
  # 2026-08-04: U13 and U14 left next_ready (batch-claimed in_progress, desktop).
  # 2026-08-04: U12 left next_ready (claimed in_progress, desktop).
  # 2026-08-04: K13 left next_ready (claimed in_progress, desktop).
  # 2026-08-04: K11 left next_ready (claimed in_progress, desktop).
  # 2026-08-04: K10 left next_ready (claimed in_progress, desktop).
  # 2026-08-04: K12 and K14 left next_ready (batch-claimed in_progress, desktop).
  # 2026-08-04: K8 left next_ready (claimed in_progress, desktop).
  # 2026-08-04 weekly groom: U13/U14 dependency-free -> next_ready.
  # 2026-08-03 (night, after K9): K14 groomed dependency-free -> next_ready
  # (the composite-key conformance sweep; the standard + parser flip built at
  # the groom).
  # 2026-08-03 (night, later still): K9 closed -> K8 (deps K7+K9 both done) and
  # K11 (dep K9) enter next_ready — the loading half and the steward screen.
  # 2026-08-03 (night, later still): K9 left next_ready again (re-claimed, desktop).
  # 2026-08-03 (night, later): K9 back in next_ready — claim released unbuilt
  # (the remote session that claimed it died on an API failure; no work landed).
  # 2026-08-03 (night): K9 left next_ready (claimed in_progress). K8 and K11 stay
  # off it — both depend on K9 and become ready when it closes.
  # 2026-08-03 (evening): C23 and L22 left next_ready (claimed in_progress; L22 closed same session).
  # 2026-08-03 (evening): L22 left next_ready (claimed in_progress).
  # 2026-08-03 (later still): L22 dependency-free -> next_ready.
  # 2026-08-03 (later): C23 dependency-free -> next_ready.
  # 2026-08-03: G51 left next_ready (claimed in_progress).
  # 2026-08-02 weekly groom: all 7 new items are dependency-free or ride a
  # dependency that closed the same day (U9 for the three U-items, C21 for G51,
  # C17 for C22) -> all 7 next_ready. HITL status: U10 IS a gate — the sign-off is
  # an SME session and its §I is what opens the build item, so no build item was
  # groomed ahead of it. U11 DRAFTS a gate prompt and decides nothing (G27/W1
  # precedent). The rest are HITL-free: G51 is provisioning + a guard, C22 applies
  # the shape C17 already ruled, J26/J27 are hygiene with written pass/fail tests.
  # Two merges, no items: the company-side connector shape into Q6's acceptance,
  # and the two S3-falsified design-doc statements into L19 as clause (d).
  # 2026-08-01: S3 done -> G36 becomes ready (it drops the three never-consulted
  # :BusinessApplication indexes, which had to wait for the key cutover to settle
  # which properties are actually bound on). J24 closed the same day on the desktop;
  # both sessions' summary deltas are merged here at the rebase.
  # 2026-07-31 weekly groom (pm): both new items -> next_ready — J25 is
  # dependency-free, and J24's only dep (J23) closed earlier the same day.
  # Neither is HITL: J24 APPLIES the recorded J23 ruling to the specs J23
  # deliberately left out of scope (it decides nothing new), and J25 is
  # authoring a snippet whose required contents port-prompt §6 already
  # specifies. The company still signs its own ratification entry — J25
  # produces the paste-ready block, never the sign-off.
  # 2026-07-31 groom (am): all 4 new items dependency-free -> next_ready. J23 is
  # pre-decided (user ruling — no gate); W1/W2 draft proposed/planned entries
  # ONLY (the FCDO crosswalk sign-off is an SME gate session, never auto-decided;
  # W2's activations ride their host gates as prose deps); W3 is a skill-doc chore.
  # 2026-07-30 weekly groom: all 8 new items are dependency-free -> next_ready.
  # N7 is the design SESSION (fable, HITL-gated) — the schema change itself never
  # lands from a groom; N8 closes a live render defect under the current shape
  # (J21's guards cover the interim) and coordinates with N7's re-mapping.
  # 2026-07-30: the desktop R4->R3->R5 run closed all three same day — R6 (Tier-2 loop),
  # R7 (freshness/rescrape), R8 (eval + promotion feed) all become ready; Epic R's
  # remaining items are now the whole unblocked tail.
  # 2026-07-29 groom: V1 dependency-free -> next_ready; V2-V10 wait on V1's
  # coverage-map dispositions (extend-vs-author decisions), so they stay out.
  # 2026-07-28 pm post-UI-merge groom: all 15 new items dependency-free except
  # O39 (waits on O38's IdChips — the link surface). O37 coordinates with O30
  # informally (no hard dependency; whichever lands second re-verifies).
  # 2026-07-28 pm epic-close-out groom: G42 (catalog seam start), L18, L19, D8,
  # J17 all dependency-free -> next_ready; G43 waits on G42, G44 on G43 (the
  # gate's evidence pack). U5 left next_ready by execution — Epic U is the run's
  # close-out: 6/6 done. Reopened-by-honesty: L gains L18/L19 (both are Epic L
  # domain defects/upkeep — truthful homes beat epic aesthetics), D gains D8.
  # 2026-07-28 pm: U6 closed same day it was groomed; U4 closed right behind it
  # (the query pack transcribed against the now-truthful graph, hardened with
  # the O33 SchemaMeta guards). U5 = the last open Epic U item.
  # 2026-07-27 code-graph-review groom: U1-U3 = the three personas of
  # docs/reviews/code-graph-review-plan.md, dependency-free -> next_ready;
  # U4/U5 = the optional skill edits, gated on their phase (U1/U2) proving the
  # queries earn their keep. All read-only; none HITL.
  # 2026-07-27 groom (weekly inbox): 5 promoted, 1 merged, 1 resolved. None of
  # the five touch edge semantics — no gate needed: J14 is a publish-boundary
  # file split (fable because the boundary decision rides in it), J15/G36/G38/O33
  # have written pass/fail tests. The deepdoc charter ruling rides G32's gate as
  # its clause (e). J14 + O33 are dependency-free → next_ready; J15 waits on J14,
  # G36 rides S3's bootstrap re-run, G38 waits on G32's ruling.
  # 2026-07-25 groom (pre-UI structure review + the 07-24/07-25 inbox): new Epic S
  # (S1-S9) from docs/reviews/architecture-structure-review-2026-07-25.md, plus G28
  # (multi-DB naming drift) and G29 (supplement-verb consolidation).
  # HITL status: S1 is a DECISION item (rule on ADRs 0008/0009/0010) but NOT a gate —
  # no edge semantics; S3 IS gate-bound (a property-term binding on the canonical
  # :BusinessApplication node) and stays `proposed` in the ontology map until sign-off.
  # S2/S4/S5/S6/S7 wait on S1's ruling. S8 (cli.py), S9 (docs tree), G28 (DB names)
  # and G29 (supplements) need no ruling and are ready now — S9 is branch + port-
  # sequenced work by construction, never a direct-to-main commit.
  # Pre-UI ordering inside the epic, per the review's §5: S3 (app_id/id_authority)
  # first despite scoring 7th — its cost rises with every console route that reads
  # seal_id — then S2 (orchestration parent, before query specs harden an
  # orchestrator assumption into a public contract), then S4 (draft table, before the
  # console grows write surfaces).
  # 2026-07-23 groom (full inbox sweep): L16 (doc-only, no deps), O29 (deps O10/O18
  # both done), O30/O31/O32 (no deps) all land HITL-free — none touch edge semantics
  # or ontology; O31 is workable now but genuinely blocked on the docmeta eval
  # harness existing in practice (dependency recorded as prose in its notes, not a
  # depends_on id, per the groom instruction).
  # HITL-free groom (2026-07-21 pm; O26 pulled 07-22): P3, J10, Q6, J13, G18, G19, G26
  # are workable with NO gate session — P3/G21 implement already-signed rulings
  # (CM_HOSTS 07-09, cmd-line 07-15); G18..G21+G24+G25 are staging-only pre-G22 (no
  # graph writes by design). HITL-required from next_ready: M3, M4 (per-source gates),
  # O23 (IS a gate), O26 (SME hold 2026-07-22: runbook template shape goes through a
  # HITL template session first — do not build the view ahead of it).
  # Chain after G18+G19 land: G20 -> G21, G24; G25 after G19. Q6 = external-work-in -> branch.
  # 2026-07-22 Backstage groom: O27/C15/C16/O28 are HITL-free (C15 display-only by
  # design; nothing touches edge semantics); G27 DRAFTS the kind-enum gate prompt —
  # the sign-off itself is an SME session, never auto-decided.
  # 2026-07-23 agentic-Q&A groom: R1 IS a gate (ADR 0007 review + three axis rulings)
  # — HITL-required; R2..R8 are build items behind it, all read-only by construction
  # (O20 cited in R5/R6 acceptance). R1 SIGNED OFF same day (gate-log entry) — R2 is
  # HITL-free and ready; note the axis-C ruling reshapes R2's model wiring (provider
  # adapter for local Anthropic / company Azure OpenAI; Gemini demo agents unchanged
  # until R2 touches them).
