# DryDocs — finish the base, open the Team Edition, cut the churn

_Planning session 2026-09-09, desktop (Lane A), Fable 5.1. Planning, every gate sitting, every ADR and the one grooming pass run on Fable 5.1; build units carry `model: sonnet` unless stated; chores `haiku`; the two existing `opus` items keep their tier. Adversarially reviewed: three design perspectives, six refuters; the corrections are folded in and named where they changed a decision._

## STATUS — measured 2026-09-10 at `9c427293`, not carried

This section is re-derived by command at every update; nothing in it is remembered. It exists
because the plan was written into a file that was never committed, and a later session reading
the repo could therefore see only the eleven `docs/reviews/modules/` sweep reports and concluded
that was all there was. The plan, and both ultra-review records, are committed with this edit.

**Phases 0, 1 and 3 are COMPLETE. Phase 2 is PARTIAL and Phase 4 is three-quarters done.**
`9c427293` is level with `origin/main`. Three `wip/*-laptop` branches are open on the remote,
which is the Phase 2 residue: that phase runs across both lanes, and only Lane A's half landed.

**The correction, stated because the block it replaces was wrong in a way worth naming.** The
2026-09-09 measurement read Phase 2 as done on the strength of PORT8, PLAN12 and CFG7 — which
are units 2.1 through 2.4, every one of them Lane A's. The phase's own heading is "THE BASE:
the finish line and the build-now fixes (A parallel B)", and its finish line is a test that does
not exist: `tests/integration/test_e2e_synthetic.py` is unwritten, and LOAD13, LOAD14 and DOC12
are `in_progress` on the laptop with branches to match. Reading a two-lane phase off one lane's
commits is how a phase gets marked done while its deliverable is still being built.

| Phase | State | Evidence |
|---|---|---|
| 0 — close the burst | **done** | nine `--no-ff` merges (`c25bca79`..`f06a5f00`), six claims settled at `1bd22b50`; DOC4 and S11 `done`, E1 and G62 on `hold:`, L19 and MM7 released |
| 1 — ADR 0021, ultra review, one mint pass | **done, one tail** | ADR 0021 drafted `981a76f6` and **ACCEPTED** at sitting 1; 58 items minted `7ed742d2`; the Team Edition epic exists; both handoffs reissued `41e018fc`. TAIL: the grooming pass over the two ultra-review records is not run |
| 2 — the base: finish line and build-now fixes (A ∥ B) | **partial — Lane A done, Lane B in flight** | LANE A done: PORT8 `2425c206`, PLAN12 `903060e2`, CFG7 `e2bf8d53`; tag `port-base-20260909` at `9da9d617`. LANE B open: LOAD13 (the finish-line test), LOAD14 (the coverage report) and DOC12 (the Excel run book from the graph) all `in_progress`, branches `origin/wip/{load13,load14,doc12}-laptop`; `tests/integration/test_e2e_synthetic.py` does not exist yet |
| 3 — SITTING 1 | **done** | `783a7a4e`: `registry-wiring-readiness` SIGNED 18/18, `source-descriptor-axes` SIGNED 13/13, ADR 0021 ACCEPTED, nineteen gates DEFERRED with named re-arms |
| 4 — post-sitting builds | **3 of 4** | CFG13 `df068d46` (+ `321d0ae2`), CORE10 `eb18afde`, CORE18 `done`. REMAINS: unit 4.4 (the duckdb/openpyxl dependency groups and the base-finish tag), which closes on Phase 2's finish line rather than on its own |
| 5 — SITTING 2, the identifier | **not started** | N23, J70 both `todo`; needs SME time. Units 5.1 and 5.2 are drafting and can run before the sitting |
| 6 — SITTING 3, the data-flow record | **not started** | MM13, ONT4 `todo` |
| 7 — the ninth roll | **not started** | GRAPH5 (the shim removal) `todo` |

**Out of band, landed today and not in the phase table.** L23 merged from Lane B (`1f1fa050`):
the long-form SDLC run book generator, whose pass fixed five sections that printed confident
answers no source supplied. PLAN13 gained clause (e) from that lane's close report. WEB23 was
minted from a red build: the console's initial-chunk ceiling has 808 bytes of headroom, and was
already past its margin before CFG13 arrived.

**The ultra review ran twice, locally, and neither record was committed until now.** The plan's
ruling 2 called for `/code-review ultra`; that cloud command reviews a BRANCH, capped at about
500 changed files and 8,000 changed lines, so it does not fit a whole-tree review of `main` and
was not the right instrument for this step. What ran instead, on this same commit:
`docs/reviews/ultra-review-2026-09-09.md` (67 agents, 10 dimensions, 19 files, 56 findings
raised, 21 confirmed, 35 refuted) and its companion
`docs/reviews/ultra-review-2026-09-09-unread-surface.md` (61 agents, 12 dimensions over exactly
the surface the first run's completeness critic declared unread, lineage SQL excluded by
instruction). They are disjoint by construction and are the input to Phase 1's outstanding
grooming pass. Nothing has been minted from either.

**Next, in order.** Finish Phase 4 (CORE18, then unit 4.4). Run the one grooming pass over both
review records. Draft 5.1 and 5.2 so sitting 2 is ready when the SME has time; Phase 5 cannot
close without that sitting.


## Context

**Where the tree is.** Local `main` `fe557fce`, origin `d995f5c0` (two Lane B claims ahead: DOC8, then L23 at 11:52 today). Seven Lane B `wip/*-laptop` branches are `done` on-branch and each merges into origin cleanly, but V4, V5 and V6 conflict with each other on `tests/unit/test_runbook_coverage.py` (one hand-resolution); `wip/premise-drift-laptop` is a review handed to this lane; `wip/l23-laptop` is IN FLIGHT and stays out of the merge set. Seven of the eleven module-sweep reports live only on `review/module-sweep` (+40 commits, docs only, merges clean). Worktrees: `main`; `review-sweep` (the sweep's standing venue — keep); `ui-workstream` (another session's checkout, on `land/source-registration-poc`, clean and fully merged); `../DryDocs-poc` (`feat/source-registration-poc`, one commit already on `main` as `fcc3c8bb`).

**Lane A's queue is nearly done.** 11 of 14 items in `docs/lane-a-handoff.md` are `done`; N18 (stale `in_progress`, no `wip/` branch), J70 and Y3 remain. Four `in_progress` claims have no `wip/` branch: E1 (its gate was SME-DEFERRED 2026-07-14 — not dead, mis-stated), G62 (a gate session convened 2026-08-13, never concluded), L19 and MM7 (dead tips under J31).

**The user's goals**: finish the base end to end; open a Team Edition mechanism serving three use cases; cut churn and rework; improve the A/B handoff; improve the company ports. The SME gates are the user's own blockers.

**What blocks what — measured, not assumed.** 38 non-done items are gate-bound across ~22 gates; the SME's real blocker list is 22 drafted-unsigned prompts (the console shows 16 because seven "recorded" drafts render under Signed off — C39's defect). But **the base's end-to-end path does not wait on any gate**: use case 1 runs on the ten synthetic stand-ins, and of the nine `confirmed: false` rows exactly one (`snow:cmdb-ci-classes`) is in that set — a semantic hold no wiring field can clear. So the finish line is desk work; the gates are governance and port hygiene that run beside it, not in front of it.

**Three gates rule ONE string** (the dataset id and its URN under an edition): N23's grammar (prompt not drafted), J70's `schema-identifier-publish-ceiling-teams-edition`, and ADR 0015 Phase 0 (no prompt; "blocks all code"; two halves — URN tenancy AND the D6 memory-scope name through the 0002 Q8 test). Signed apart they can contradict; N23's own notes refused to split four ideas for the same reason.

**"Prose dictating config", precisely.** `confirmed:` on a registry row carries two facts (meaning signed; pipeline built) and the second lives only in YAML comments. The gate that rules it, `registry-wiring-readiness` (N18 owns the run), is 19 days old and its premises have moved in every section: the census is 30 rows / 21 true not 29 / 20; `hr_phone_exp` became a wiring hold when K17 signed (4 semantic / 4 wiring / 1 neither); ADR 0017 ruled its D2 fence SCOPED and a `binding:` field landed (G125); O90 shipped the four-state wiring cross as a pure function (its D3 "separate item" already happened); and **clause C's port argument is stale in the opposite direction** — `config/source-registry.yaml` has had its own per-entry row since J71 (2026-09-01, `PORT-MANIFEST.yaml:1392-1431`) under which `confirmed` and its stamps are SIDE-ruled and never cross, so today neither the wiring fact NOR the signed semantics transfer. The split would let semantics travel again while wiring stays per side. The PoC's descriptor discipline (`config/source-descriptors.yaml`: closed axes, derive → override → refuse, per-entry from day one, read by `drydocs_core/source_descriptors.py`) is the shape the fix should take, with one physical fact the rider must state: core imports nothing from components, so a `wired` value DERIVED from loader registration (`SOURCELESS_LOADERS` lives in `drydocs/cli_shared.py`) cannot be computed in core — it is declared per side with a reason, or derived behind a load-component seam.

**The module sweep's cycle conclusion** is one defect shape found seven times — a result that cannot distinguish checked-and-clean from not-checked — with the correct answer implemented independently six times and zero written conventions. Sharpest instance, verified in code: `drydocs/port/port_preflight.py:359` `_git` uses `check=False` and returns stdout only, so an unresolvable base yields an empty range and both range checks pass clean. ~20 grooming candidates sit unminted because only Lane A mints.

**Team Edition — built vs promised.** Built: the id grammar, `edition:` declared in `config/dev-environment.yaml`, `config/taxonomy/editions.yaml` (two synthetic rows), the extend-never-override domain guard (CFG1), venues. Not existing: ADR 0015 `PROPOSED`; no Phase 0 prompt; the follow-up table never groomed; no epic; no copier; `edition_registry.py` has no production consumer; three of the six D4 instance-owned files (`config/taxonomy/business-application.yaml`, `oracle-schemas.yaml`, `config/data-zones.yaml`) fall to the `config/**` producer-wins default.

**The three use cases against the tree:**

| Use case | Needs | Exists |
|---|---|---|
| 1. Simple flow, minimal docs, end to end | ingest → graph → runbook, provably | `tests/integration/test_e2e_load.py` on bundled samples; stand-ins for 10 of 30 datasets; the 2-tab Excel TEMPLATE only (no generator from the graph); L23 (long-form) `todo`; `drydocs/docs_coverage.py` for one object class; `runbooks.*` QuerySpecs in `drydocs_api/query_specs.py` |
| 2. File → table FIELD-level mapping, in Excel | a manual xlsx source; a field-grain node the base refuses | `format` axis has no `xlsx`; no `:CatalogField` (G34 reserved only the logical `CatalogElement`, explicitly not the physical half); `config/source-mappings/` is DryDocs' OWN column ledger; `openpyxl` is a dev dependency; an edition may EXTEND the ontology (ADR 0015 A4) never override; a base mints, an instance requests (edition-grain gate B2) |
| 3. Legacy → modern (Oracle SQL lift-and-shifted to Snowflake; only test = parallel-run data match) | capture the legacy flow with context before it is lost; hold legacy↔modern and the match evidence | the deepdoc data-flow record (gate `data-flow-overview` drafted 2026-08-21, never walked — MM11; its evidence grammar E1/E2/E3 unruled); `SnowflakeCatalogExtractor` built (G42), rows unwired; only `docs_supersedes` as a succession edge; `equivalence.py` three-valued compare at metadata grain; no parity primitive |

## Decisions taken in this session (user rulings, 2026-09-09)

1. **Gate first, then build**: hold the wiring gate restated against today's facts, with the descriptor questions in the SAME sitting, then build once.
2. **Ultra review after Lane A's close, before the base-finish build**, so its findings join the one grooming pass.
3. **First Team Edition instance = use case 2**; the base on stand-ins is instance 1; two instances prove the template.
4. **Eighth port roll at Lane A's close, after the preflight fix** — small rolls.

## The shape

```
Phase 0  close the burst (A) ............................ no SME time
Phase 1  ADR 0021 draft · ultra review · ONE mint pass (A)  no SME time
Phase 2  THE BASE: finish line + build-now fixes (A ∥ B) → 8th roll ... no SME time
Phase 3  SITTING 1  wiring + descriptor axes (+ ADR 0021 accepted, deferrals ruled)
Phase 4  post-sitting builds: `wired` · the outcome type · Idea-309 (A)
Phase 5  SITTING 2  the identifier → ADR 0015 ACCEPTED → TE phases 0–2, UC2
Phase 6  SITTING 3  the data-flow record → UC3 structured half
Phase 7  9th roll — company apply stays a third session
```

Pens: Lane A (desktop) holds `backlog · port · adr · gates · snapshot`, every gate prompt, every ADR, `PORT-MANIFEST.yaml`, `MODULE_MAP.md` (rendered), `config/source-registry.yaml`, `config/source-descriptors.yaml`, `config/data-zones.yaml`; Lane B (laptop) builds code+tests, and the fence is PER FILE as the existing handoff states it — `config/taxonomy/ui-components.yaml`, `config/source-mappings/psgmgr.yaml` and `config/doc-source-registry.yaml` stay Lane B's by the 2026-09-09 rulings. In-flight work is always `wip/<id>-<machine>` (J31); no `feat/` branches for minted items.

---

## Phase 0 — Close the burst (Lane A, one sitting, no SME time)

These are lane-close mechanics, not backlog items — nothing here is minted.

| # | Unit | Model | Done when |
|---|---|---|---|
| 0.1 | `git pull --ff-only` (lands the DOC8 and L23 claims; target `d995f5c0` or later) | — | `main..origin/main` empty |
| 0.2 | Merge the 8 finished `wip/` branches `--no-ff` in this order (never `wip/l23-laptop`, which is in flight): `premise-drift`, `v6`, `v4`, `v5` (the one hand-resolve in `tests/unit/test_runbook_coverage.py`: three `RUNBOOK_PENDING` deletions, three `RUNBOOK_DISPOSITION` insertions at one anchor), `doc9`, `mm12`, `rem2`, `doc8`. No manifest rows are owed at this merge (S11 and N17 are unstarted) | sonnet | 8 merge commits on first-parent; DOC9/REM2/MM12/V4/V5/V6/DOC8 read `done` |
| 0.3 | Merge `review/module-sweep` `--no-ff`; KEEP the branch and `.claude/worktrees/review-sweep` (DOC4 is a STANDING sweep; the branch accumulates, a human merges when findings are wanted in trunk) | haiku | all 11 cycle-1 reports resolve on main |
| 0.4 | Render ONCE (`render_board.py` + `render_design_doc.py docs/design/*.md` — DOC9's byte guard and the four new runbooks need it); full suite + `ruff check .` + `ruff format --check .` once; push; `gh run list` at the sha | sonnet | roadmap and design-page guards green at the pushed sha; CI green, not UNVERIFIED |
| 0.5 | Claims: E1 → `hold:` (`since/by/until` — the Y7 shape at `items/G64.yaml`) with the 2026-07-14 SME deferral of `sosa-jobrun-observation` as its reason; G62 → `hold:` (session convened 2026-08-13, not concluded; until the next rua bundle pull); L19 → `todo` with its partial-delivery note; MM7 → `todo` with a dated J31 note AND a venue note (its inputs are desktop-local, so it is a Lane A item — never queued to the laptop). N18 stays `in_progress` (sitting-1 vehicle). DOC4 → `done` with the cycle-1 conclusion quoted (its acceptance: closes when the first report has landed). **S11 → `done` as SUPERSEDED**, note citing ADR 0018 Option C (top-level `drydocs_plan/` etc., REJECTED 2026-09-02) and LOAD1 (`drydocs/{plan,docgen,port}/` exist) — its premise is dead, and its hand-maintained-tuples clause is inboxed as an idea if still true | haiku | Ready strip honest; the note edits ride the 0.4 render |
| 0.6 | PoC residue — two of three are the user's call at execution: `git worktree remove ../DryDocs-poc` (clean, superseded); `feat/source-registration-poc` needs `-D` (its one commit is not reachable from main — the squash at `fcc3c8bb`), which DOC7 forbids without the user saying so, so ASK, and record the scoped diff (`de0eeb6f..main -- drydocs/source_registration drydocs_core/source_descriptors.py config/source-descriptors.yaml`, main ahead) in the message; `land/source-registration-poc` is the `ui-workstream` worktree's checked-out branch — that worktree belongs to the other session, so it is repointed by THAT session, and the branch is deleted after. Fix the TDD's four "ADR 0017 PROPOSED" mentions to ACCEPTED 2026-08-30 | haiku | `git worktree list` = main, review-sweep, ui-workstream; the TDD's front matter carries one dated line |
| 0.7 | Delete the 8 merged remote `wip/` branches (not `wip/l23-laptop` if it exists — L23 is in flight); tell Lane B to `git fetch --prune` + `git branch -d` (DOC7); SNAPSHOT once at this push (the close ritual: merge, delete, render once, snapshot once — one writer, not one per burst) and again after Phase 1's body commit | haiku | `git branch -r --list "origin/wip/*"` shows only in-flight branches; snapshot `meta` carries the pushed sha |

Neither handoff file retires here (Lane B still holds L23 in flight and Q28, O48, G86, G85, N17 unstarted; `--check` will read 7 done / 1 in_progress / 5 todo once S11 closes); both are regenerated in Phase 1.

## Phase 1 — Draft, review, one mint pass (Lane A)

Exit: ADR 0021 drafted (PROPOSED — its acceptance is an SME touch, batched into sitting 1); the ultra record under `docs/reviews/`; one stub commit (all new ids, final titles, render) and one body commit; both handoff files regenerated; one snapshot.

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 1.1 | **ADR 0021** — a result names what it did not check: ONE shared three-outcome type in `drydocs_core` (checked-clean / findings / not-checked-with-reason) and the rule that a probe returning a bare bool is refused. Names the six existing correct sites as precedent (`drydocs_remediation/equivalence.py`, `drydocs_lineage/archival.py`, `drydocs_api/schemas.py` `truncated`, `port_preflight.py:495`, `plan_board.py`, deepdoc scaffold status); adoption scoped to first adopters, the six keep the NAME only. Index line reserved first (stub-then-body) | fable | `docs/decisions/0021-*.md`, `docs/decisions/README.md` | status PROPOSED; nothing implements it until sitting 1 accepts |
| 1.2 | **`/code-review ultra`** — user-triggered on the Phase 0 tip (J63); saved as a dated record under `docs/reviews/` (`evaluate`, no manifest row owed); nothing minted from it before 1.3 | cloud (opus) | `docs/reviews/ultra-review-<date>.md` | findings deduped against the sweep's list by subject |
| 1.3 | **The ONE mint pass** — allocator ids (`validate.py --next-id --module <m>`), stub commit with FINAL titles + render (Y6), then bodies; a sonnet pre-pass extracts one deduped candidate list from the 11 reports + the premise-drift review + the ultra record. Mint WORK units only: (a) the sweep's ~20 candidates by module series (dedupe: "completeness convention" ×5 → ADR 0021's instrument item; "shim removal" ×2 → one item for the 18 shims; "`_git` shape" → one item + slot 8's amendment); (b) the premise-drift three; (c) Idea-309 → item; (d) the **Team Edition EPIC** `epics/team-edition.yaml` with phase items 0 (draft the identifier prompt with the Phase 0 clauses), 1, 2, 4, 5, 6 from ADR 0015's table verbatim — items 1–6 carry `depends_on: [<phase-0 item>]` and NO `gates:` until the prompt file exists (a `gates:` slug must resolve to `config/gate-prompts/<slug>.yaml` at the commit or `test_declared_gates_are_lists_of_known_prompt_slugs` is red); phases 3 and 3a deliberately NOT minted (two real instances company-side; 3a recorded open); (e) the UC2/UC3 riders and the `source-descriptor-axes` prompt-drafting item; (f) the lane-mechanics items (1.5); (g) a run item for `console-auth-boundary` (the one drafted gate with no owner — the class the 2026-08-27 sweep exists to catch). Every gate-bound item whose prompt exists carries `gates:`; every item carries `model:`; `depends_on` never points base → instance. The `wired` BUILD item is NOT minted here — a drafted gate decides nothing, and B1/B2/B5 decide its shape; it is minted from sitting 1's record (D2: "the build is its own backlog item with its own acceptance") | fable (+ sonnet pre-pass) | `docs/restructure/backlog/items/*`, `epics/team-edition.yaml`, `IDEAS.md` (mark groomed), renders | `validate.py` ALL CHECKS PASS; `test_backlog.py` green at the stub and the body |
| 1.4 | Regenerate both handoff files: `handoff.py --suggest`, then `handoff.py --lane B --machine laptop --queue <Phase 2 Lane B list> --other-queue <Lane A list>` (generation is the default mode; there is no `--generate`); snapshot ONCE after the body commit (J64) | sonnet | `docs/lane-{a,b}-handoff.md`, `knowledge/depgraph-snapshots/` | `--check` clean on both; CI green at the sha |
| 1.5 | **Lane mechanics** (minted in 1.3): (i) the generated Lane B block tells the receiver to capture ideas in `docs/restructure/ideas/pending-<branch>.md` (PLAN4 d exists) and Lane A's close gains "run `validate.py --mint-pending` on each merged pending file before the render" — with `pending-<branch>.md` carved OUT of the `backlog` pen in the `PENS` table and the fence (today `docs/restructure/ideas/` is fenced to Lane A wholesale), so the minting bottleneck closes without a second pen; (ii) `--suggest`/`--check` gain a remote-ref pass (the allocator's `remote_ids()` pattern): items `done` on a pushed `wip/` branch, and claims older than 48 h with no branch, are printed; (iii) the no-mint rule graduates from the string literal at `handoff.py:456` to CLAUDE.md §0 with its derivation (mint → stub render → B holds no render pen); (iv) `test_lane_handoff.py` pins (i) and (ii) | sonnet (A — `handoff.py` is never-port and the skill is A's) | `.claude/skills/lane-handoff/scripts/handoff.py`, `SKILL.md`, `tests/unit/test_lane_handoff.py`, `CLAUDE.md` §0 | generated file carries both sentences; a test pins them |

## Phase 2 — THE BASE: the finish line and the build-now fixes (A ∥ B, no SME time) → the 8th roll

**The finish line, declared** (no document declared one before): from a clean clone with Docker, `build_synthetic_sources.py --extract` → `bootstrap` → `apply-supplements` → `ingest-controlm` (SOURCE mode on the stand-ins) → `m3-verify` → the coverage report → both runbooks generated from the graph; every step green or SKIPPED-with-reason (J78), never silent; venue-stamped (J18); ONE named residual — `snow:cmdb-ci-classes` is a semantic hold and its loader is skipped with that reason. The wiring gate is not in this path.

**Lane A, in this order:**

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 2.1 | **Port preflight fails closed** — a local fix on the EXISTING `CheckResult` + SKIPPED idiom at `:495` (no new type yet; ADR 0021's type is adopted here in Phase 4): a base-resolves check (`git rev-parse --verify`) runs first; `_git` surfaces a non-zero exit; `range_commits`/`added_documents` distinguish "no commits" from "git failed"; the report renders NOT CHECKED, `passed=False`; `--tag` refuses on it; a unit test pins "unknown base → not certified" and "empty range on a real base → checked-clean". Re-run over `port-base-20260905..port-base-20260908`, record the verdict as a postscript to RELAY-46 | sonnet | `drydocs/port/port_preflight.py`, `scripts/port_preflight.py`, `tests/unit/test_port_preflight.py`, `.claude/skills/reconcile-port/SKILL.md` (checklist: the verdict word in the certification; NOT CHECKED blocks the tag; re-certification postscript rule) | test green; the 0908 range re-certified |
| 2.2 | **The `inputs:` guard** in `tests/unit/test_backlog.py`, spec = premise-drift review §4 verbatim: non-done items only; assert against `git ls-files -z` (bytes both sides, reason in the docstring — J76); gitignored inputs classified by `git check-ignore -z` and skipped with reason (LIN3, MM5, MM7, MM9); malformed entries fail loudly; `INPUT_EXEMPTIONS` shrink-only with ≥40-char reasons on `test_path_exemptions_carry_a_reason_and_are_still_cited`'s shape; the five item fixes in the SAME commit (C42:19, C44:55 → `drydocs_core/orchestration/controlm/description_tokens.py`; CFG4:14,32 → `docs/design/ui-exploration/two-track-ui-plan.md`; DOC5:17 `docs/adr/` → `docs/decisions/`; Y4:15 → `docs/restructure/backlog/items/`). NOT extended to `acceptance:`; gate prompts NOT guarded | sonnet | `tests/unit/test_backlog.py`, `.claude/skills/groom-backlog/validate.py` (mirror), five item files | ships green with its own proof |
| 2.3 | One-off sweep of the 19 gate prompts + 2 sweepable items for the S5 pair, `[AMENDED 2026-09-xx; was …]` markers; signed pages get riders never edits (L25). `config/gate-prompts/**` is canonical-company, so this and every later prompt edit owes ONE relay line at the next roll | haiku | `config/gate-prompts/*.yaml` | validator green; relay line queued |
| 2.4 | **The 8th roll**: `scripts/port_preflight.py --base port-base-20260908 --tag` after 2.1–2.3 and Phase 0/1 are on origin; the roll carries the J77 red-commit line (mandatory from the eighth roll); RELAY lines only where a hand-step is owed (the gate-prompt sweep; the preflight's verdict word if `PORT-REPORT` shape changes); the tracker gains the company's 0908 apply bullet as `[SME-REPORTED]` with the J35 fields as reported — nothing asked back. The shim removal is NOT in this roll | sonnet | `docs/port/port-prompt.md`, `PORT-MANIFEST.yaml` | verdict CHECKED; tag pushed; `docs/port/port-dispositions.md` re-rendered |

**Lane B burst** (the regenerated queue; inputs disjoint from Lane A's; every close runs the J57 family + `test_skip_guard_policy.py` + `test_never_port_citations.py`):

| Order | Item | Model | Pen | Note |
|---|---|---|---|---|
| 1 | **UC1 finish-line test**: `tests/integration/test_e2e_synthetic.py` — the chain above on the tracked bundle, auto-skips with reason without Docker or the bundle, three outcomes only | sonnet | `code:drydocs-load` | can start on today's main |
| 2 | **UC1 coverage report**: extend `drydocs/docs_coverage.py`'s existing shape (declared / loaded / blocker, `None` for not-probed) to the stand-ins' object classes (folders, jobs, datasets, docs) — the base's own statement of what is not loaded; READS `config/source-registry.yaml` and `source-descriptors.yaml`, writes only its module and tests; no ADR 0015 D1 citation (that ADR is unratified) — TE Phase 4 generalizes this later | sonnet | `code:drydocs-load` | the `docs-coverage` verb generalized, not a second verb |
| 3 | **L23** (already in flight on `wip/l23-laptop`) + a NEW sibling item: `generate_runbook.py` beside `generate_template.py` filling the 31 graph-derivable columns of the 2-tab Excel from the `runbooks.*` QuerySpecs; a manual cell is marked "needs capture", never blank-as-if-fetched; demonstrable on the stand-ins; queries SHARED with L23 | sonnet | `code:docs` | UC1's missing deliverable |
| 4 | CORE: boundary guard learns `importlib.import_module("<literal>")` / `__import__`; declare `drydocs.port.reconcile_before → drydocs_remediation.detect` with its reason in `DECLARED_COMPONENT_IMPORTS`; the `tests.unit.*` reach at `reconcile_before.py:216` handled as its own question; `MODULE_MAP.md` re-RENDERED, never hand-edited; `reconcile_before.py` is the one `drydocs/port` file Lane B touches, and only after 2.1 is on origin | sonnet | `code:drydocs-core` | the copier WILL read `component_map` (it does not exist yet) |
| 5 | REV: `graph_verify` per-suite precondition (NOT RUN, not PASS, on an empty graph; the four unanchored suites gain anchors) · REM: rule-coverage denominator beside findings + delete `test_remediation_changes.py:323` · API: driver failures → 503 (pass the exception CLASS, never its message) · LOAD: `:JobRun` load scope (notes say whether a gate rider is needed) | sonnet | per module | four of the seven recurrences; they adopt the ADR 0021 type only after Phase 4 |
| 6 | O48, Q28 (as ruled 2026-09-09) | sonnet | `code:drydocs-api`, `code:config` | unchanged |
| 7 | CORE: triage the 18 raw source-reading guards against J66 (table: reads-code / reads-prose / reads-render); conversions in one commit; the mandatory-`source_scan` ruling as a note for Lane A | sonnet | `code:graph-infra` | a sample becomes a list |
| 8 | **UC3 capture, gate-free**: register the legacy-capture corpus as a `config/doc-source-registry.yaml` row — the producer ships the SHAPE as a synthetic row; transcripts machine-local, cited by file name; `sme_notes` `$FR/$UC/$OQ` harvest is the method; SQL examples land as `:Chunk`s citing sources, never as facts; NO evidence-grammar choice presupposed (E1/E2/E3 are sitting 3's) | sonnet | `code:drydocs-docmeta` | the "before it is lost" half needs no gate |
| 9 | (S11 is closed as superseded in 0.5 — no package extraction happens; `drydocs/port` stays Lane A's) | — | — | — |
| 10 | WEB: the shared completeness hook (p1) → retrofit IntakeRoute, LocationMap, FileReport (p2) → `npm run test:coverage` where `node_modules` exists and read the three thresholds together (p3); each adds its `config/taxonomy/ui-components.yaml` row (O42). **Only if the UI-testing session is not building web** — that ledger has three writers otherwise; else these stay with that session under its own `code:drydocs-web` pen | sonnet | `code:drydocs-web` | confirm the lane at execution |
| 11 | N17 (writes only `config/source-mappings/psgmgr.yaml`), then G86, then G85 (opus, p3 — take last or leave and say so) | sonnet / opus | `code:config`, `code:drydocs-web` | unchanged rulings |

The ADR 0018 shim removal (18 shims: 17 module files + `drydocs/publishing/__init__.py`; 49 citing files repo-wide, 23 in code packages, re-measured by command at pull) is minted in 1.3 and is a **Lane A** unit — its citation sweep touches `PORT-MANIFEST.yaml`, `docs/port/*`, ADR 0018 and its README, backlog items and `MODULE_MAP.md`, four Lane A pens — built after the 8th roll on a `wip/<id>-desktop` branch when no Lane B branch is open under `drydocs/`, for the 9th roll; the company holds the subpackage move (it applied `port-base-20260905`), so the removal is announced by relay, never discovered.

## Phase 3 — SITTING 1: what a dataset declares (governance and port hygiene, beside the base)

Exit: `config/gate-log.md` carries a `registry-wiring-readiness` entry reading SIGNED, SME-DEFERRED with a named trigger, or BLOCKED with a named blocker (silence is the non-outcome); the `source-descriptor-axes` entry beside it; ADR 0021's acceptance recorded; the deferral rulings recorded as SME outcomes; follow-up build items minted in the same entry. Prep starts the day Phase 0 pushes; it does not wait on the mint pass.

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 3.1 | **Stale-premise sweep** of `registry-wiring-readiness.yaml` (N18's BEFORE clause), markers not rewrites, counts by command: meta.Source 30 / 19; census 4 semantic / 4 wiring / 1 neither; the `catalog` rows' activation gate DEFERRED to the Logical Container trigger; D2 ruled SCOPED (ADR 0017, 2026-08-30) and `binding:` live; O90's cross shipped; `source-connection-and-run-identity` drafted after it on the same file — sequencing stated; **clause C restated on the corrected premise**: `confirmed` is already per-side under J71 (no manual pin remains), so the question is whether the split lets SEMANTICS transfer again (canonical-producer) while wiring stays per side — C1–C4 re-asked on that footing, since they were never signed | fable (+ haiku for the census) | `config/gate-prompts/registry-wiring-readiness.yaml`, `config/source-registry.yaml`, `PORT-MANIFEST.yaml:1392-1431` | no signed clause re-asked; every moved fact carries a dated marker |
| 3.2 | **Draft `source-descriptor-axes`** — a SEPARATE short prompt (the wiring page's own D3 refuses fold-ins), held in the same sitting, decides nothing: (a) are the five axes the right five; (b) **the home and derivation of `wired`**, both options at equal weight: a registry field (`source-registry.yaml` is per-entry; the field needs its own `per_side_fields` entry and the entry_rule names it) vs a descriptor axis (the descriptor row keys only `overrides:` and `synthetic.datasets:`, so a `wired` block ALSO needs a `per_side_fields` entry — per-entry is not per-side); in both, DECLARED per side with a ≥40-char reason when false, REFUSED otherwise; derivation is offered only behind a load-component seam because core cannot see loader registration; B1's single-field alternative stated with O90's cross as its shipped evidence; (c) do axis values reach the graph (an ontology question, routed to its gate if yes); (d) informational: `xlsx` joins `format` as a classification change; (e) the DataHub emission today publishes the overloaded `confirmed` as a catalog property — the sign settles what the tag means Does NOT re-ask whether a catalog is adopted — `snowflake-data-catalog.yaml` is that page and is deferred. Cites the precedent for a per-side registry fact: `source-registry-v2` D2's "per-side loader overlay", RULED 2026-07-31 | fable | `config/gate-prompts/source-descriptor-axes.yaml`, `config/source-descriptors.yaml`, `docs/design/source-registration-tdd.md` | validates; its run item exists (1.3 e) |
| 3.3 | **Hold sitting 1** — both prompts, one session (the 2026-09-02 shape: the rulings that matter in one message, the rest confirmed-as-drafted and reopenable under C40); in the same message the SME accepts or amends ADR 0021, and rules the deferrals below as SME outcomes; record; mint the follow-ups; N18 closes on the record. N24/N25 ride only if the SME has the time | fable | `config/gate-log.md`, `items/N18.yaml` | one entry per prompt, all outcomes legal |
| 3.4 | **Deferral rulings** (SME outcomes, one message, recorded on the 2026-09-07 shape with a named re-arm trigger each; a Lane A session never writes one alone): `rua-bundle-data-profile` (G62: next rua bundle pull), `source-connection-and-run-identity` interim (re-arm: sitting 3's G4 or the first automated DB-backed source the base needs), `tech-partner-attach-level`, `standard-identity-and-carrier`, `replica-derivation-edge`, `email-dl-contact-point`, `document-supersession`, `controlm-folder-identity-grain`, the DPL trio, `repo-manifest-data-profile`, `script-provenance-gaps`, `etlprocess-kind-enum`, `software-usage-patterns`, `code-graph-package-layer`, `bmc-docs-example`, `software-version-context`, `console-auth-boundary` — each deferred or scheduled by the SME. NOT in this list: `remediation-fix-tracking` and `server-location-ontology` (SIGNED — G90 done, LOAD6 is a build); `controlm-definition-precedence` (D11 was ARMED by user ruling 2026-09-07 — its own session, not folded); the three deferred 2026-09-07 to the Logical Container trigger (unchanged) | fable | `config/gate-log.md` | gates.json reads them deferred; C39's declared-status fix stays its own item |

## Phase 4 — Post-sitting builds (Lane A)

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 4.1 | **Build `wired` per the ruling** (exactly the signed shape): the field or the axis; the `per_side_fields` entry and the row's entry_rule naming `wired` (+ its reason) land in the SAME commit as the schema change (C3); `require_confirmed()` refuses unless confirmed AND wired and its message names WHICH failed and what clears it; no row's load state changes (D1); the wiring-hold rows carry `wired: false` + reason; only the comments that carried the WIRING fact retire — comments carrying ruling citations (e.g. `hr_phone_exp`'s K17/K26 record) stay; a RELAY tells the company the two standing pins retire; the load-map's wiring column reads the new value | sonnet (A, `config` + `port` pens) | `config/source-registry.yaml` or `config/source-descriptors.yaml`, `config/schemas/*.json`, `drydocs_core/source_registry.py`, `drydocs_core/source_descriptors.py`, `PORT-MANIFEST.yaml`, `docs/port/port-prompt.md`, tests | every row that loaded before loads after; `test_port_manifest.py` + `test_port_reconcile_guards.py` green |
| 4.2 | **ADR 0021's instrument**: the type in `drydocs_core` (imports nothing) with a guard reading code (AST via `tests/source_scan.py`, J66) that fails a probe returning a bare bool; first adopters = `port_preflight.py` (replacing 2.1's local third state — the one deliberate double edit, tiny) and `docs_coverage.py`; the six named sites adopt the NAME only | sonnet | `drydocs_core/<outcome>.py`, `drydocs/port/port_preflight.py`, `drydocs/docs_coverage.py`, `tests/unit/*`, `MODULE_MAP.md` (rendered) | boundary test green; guard has a positive and a negative case |
| 4.3 | **Idea-309**: `internal/server-inventory/` moves to a data-root zone (README stays as pointer); `bundle.py`'s hardcoded `repo/` prefix moves into `config/data-zones.yaml`; the non-overlap invariant covers all three declaration sources; `landing_zones.py`'s three named exceptions are true again; `psgmgr-mirror/`, `synthetic/`, `datahub/` listed and left alone (clause 3). The larger `internal/` reorganization is inboxed for the TE Phase 1 carve | sonnet | `config/data-zones.yaml`, `drydocs/source_registration/bundle.py`, `drydocs_core/landing_zones.py`, `tests/unit/test_data_zones.py`, `config/source-registry.yaml` | `test_data_zones.py` sees all three sources |
| 4.4 | Declare `duckdb` in an optional dependency group (imported lazily, declared nowhere) and decide `openpyxl`'s group (dev today; runtime if item 3's generator ships in-product); tag the base finish: a dated line in `docs/restructure/01-project-plan.md`, the roadmap's module stages re-judged, `VERSIONING.md`'s next bump named | haiku / fable | `pyproject.toml`, `docs/restructure/01-project-plan.md`, `docs/restructure/roadmap.yaml` | `test_plan_roadmap.py` green |

## Phase 5 — SITTING 2: the identifier under an edition → ADR 0015 ACCEPTED → Team Edition phases 0–2 and use case 2

Exit: one gate-log entry ruling N23 (a)–(i) and the publish ceiling together, recording ADR 0015 ACCEPTED (with the 2026-09-04 amendment; 3a stays open); the id migration minted as ONE frozen unit; the template proven on XMPL and SMPL in CI.

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 5.1 | **Draft N23's ONE id-grammar prompt** (after sitting 1; N23 is gate-bound on it) with two Phase 0 clauses: (h) shared-vs-owned object classes DECLARED as a closed value (the descriptor idiom; no default), the tenant segment's position relative to the carrier slot (clause c), the tenant-free canonical form for shared infrastructure; (i) the D6 memory-scope name (`ddmemory` or in-DB scoping) through the 0002 Q8 test — Phase 0 has both halves and is not closed by (h) alone; every id-bearing surface counted by command at pull. The TE phase items gain `gates: [<this slug>]` when the file lands | fable | `config/gate-prompts/<n23-slug>.yaml`, `items/N23.yaml`, the TE phase items | validator + `test_backlog.py` green |
| 5.2 | Stale-premise sweep of `schema-identifier-publish-ceiling-teams-edition.yaml` (J70's BEFORE): counts re-measured; ADR 0015's amendment recorded; C4 marked ruled-together with N23 (b); D1's `cm_escalation_db` legibility residue minted as its own item | fable | that prompt, `items/J70.yaml` | markers only |
| 5.3 | **Hold sitting 2** in this order — N23 (a)–(c) first (the carrier slot, which the tenant segment's position depends on), then (h) and (i), then (d)–(g), then the publish ceiling; if time runs out, (a)–(c), (h), (i) must sign or ADR 0015 stays PROPOSED and Phase 1 does not start; ADR 0015 flips to ACCEPTED with deciders and date; the id migration minted as ONE frozen unit (the GN2 shape; un-redaction and `SourceEntry.urn` in the same commit per ADR 0017 §1); J70 closes on the record; nothing renames at the sitting | fable | `config/gate-log.md`, `docs/decisions/0015-*.md`, `docs/decisions/README.md` | one entry, three rulings |
| 5.4 | **TE Phase 1 as a DECLARATION, not a second tree**: the D2 cut as an edition-cut list `component_map.py` reads + an import-closure guard (dynamic imports included, from Phase 2 item 4); `backlog_store` KEPT per A1; nothing deleted in the producer tree | sonnet (B) | `drydocs_core/component_map.py`, `tests/unit/test_module_boundary.py` | guard fails if a kept module imports a cut one |
| 5.5 | The file-class manifest DERIVED from `PORT-MANIFEST.yaml` dispositions (canonical-producer → canonical-template; canonical-company / per-entry company-owned fields → instance-owned; derived → derived), committed as a derived render (J43); add the three missing D4 instance-owned rows now (`business-application.yaml`, `oracle-schemas.yaml`, `data-zones.yaml`) | sonnet (A, `port` pen) | `PORT-MANIFEST.yaml`, `scripts/render_*` | a per-entry file with a field split appears in BOTH classes with the split named |
| 5.6 | **TE Phase 2** — after 5.4, 5.5 AND 5.7 (the fragment-placement ruling decides what the template scaffolds): `copier.yml`, the questionnaire (team, LOB, edition code = tenant segment, orchestrator, warehouse, doc platform, ticket system, composite alias), `_exclude` from 5.5, the checksum guard on the frozen class, the `CLAUDE.local.md` overlay seeded from the questionnaire; CI generates XMPL and SMPL and runs their suites; `copier` declared; `groom-backlog --scope` ↔ `--edition` reconciled in the ADR's dated section | sonnet (B), the reconciliation fable | `copier.yml`, `config/taxonomy/editions.yaml`, `config/dev-environment.yaml`, `drydocs_core/edition_registry.py` (first production consumer), `.github/workflows/ci.yml` | two synthetic instances generated in CI, suites green, disjoint id prefixes |
| 5.7 | **Rule where an edition's vocabulary fragment lives** (ADR 0015 dated amendment): an instance-owned file inside the frozen fragment directory, a declared `editions/` sub-root, or the instance's own tree; the reader resolves the ruled root; CFG1 still refuses base-id reuse from there; states that an edition fragment holds only edition-minted domain rows | fable (A, `adr` pen) | `docs/decisions/0015-*.md`, `drydocs_core/ontology/domain_registry.py`, `tests/unit/test_domain_registry.py` | one dated section, three options, one ruling |
| 5.8 | **Use case 2's base contribution**: `xlsx` on the `format` axis and the registry schema enum (classification, no gate); a stage-only field-map extractor in `drydocs-load` (`field_map_xlsx.py`): rows keyed (source dataset id, source field, target dataset id, target field, rule), refuses a workbook whose headers do not match the declared shape, ZERO graph writes (test pins it); a synthetic stand-in workbook in the descriptor's synthetic plan; its dataset id minted AFTER sitting 2 (the N22 precedent) | sonnet (B) | `config/source-descriptors.yaml`, `config/schemas/source-registry.schema.json`, `drydocs/loaders/field_map_xlsx.py`, `tests/unit/test_field_map_xlsx.py` | on the closed list; stages, never writes |
| 5.9 | **ONT proposal for use case 2** (a base mints, an instance requests — edition-grain gate B2): reserve `:CatalogField` (the physical half of the G34 pattern, key `field_id`) and one planned edge `DataAsset-[:HAS_FIELD]->CatalogField` as `status: planned` in the base fragments; state that the MAPPING edge `(:CatalogField)-[:MAPS_TO {rule}]->(:CatalogField)` is an edition domain row, never a base term. The flip to confirmed rides ONT3's Logical Container prompt — NEVER an edit to `snowflake-data-catalog.yaml` (the 2026-09-07 record says the deferred pages stay on disk unchanged) — so **ONT3 (draft the Logical Container prompt; ontology, fable, Lane A) is scheduled in this phase** as the re-arm's vehicle; the re-arm run item is minted when that prompt lands | fable (A, `gates` pen) | `10-node-classifications.yaml`, `42-local-catalog.yaml`, ONT3's prompt | both entries planned, loader `~`, supplement `~`; ONT3 done |
| 5.10 | A synthetic XMPL domain row in `config/taxonomy/domains.yaml` with its own fragment (per 5.7) carrying the MAPS_TO edge — the extend-never-override rail exercised with a real fragment, beside the existing test fixture; the DevOps team's REAL edition mints its own code and domain company-side at its own gate; the producer never names it | sonnet (A) | `config/taxonomy/domains.yaml`, the XMPL fragment | CFG1 guard green with the new fragment |

## Phase 6 — SITTING 3: the data-flow record → use case 3's structured half

Exit: `data-flow-overview` SIGNED / DEFERRED / BLOCKED with the succession/parity rider ruled in the same entry; the blockers behind MM8/MM9/MM10 named AND scheduled (MM7 opus on B, MM5 on B, MM6 ontology fable on A); N19's re-arm decision written.

| # | Unit | Model | Files | Done when |
|---|---|---|---|---|
| 6.1 | MM13 — fix the prompt's item numbers (MM3 → MM7, nine times) with `[AMENDED; was MM3]`; its acceptance (d) relay line queued for the next roll | sonnet | `config/gate-prompts/data-flow-overview.yaml` | validator green; relay line queued |
| 6.2 | Profile the feed-grain inventory sheet for `<DATAFLOW>` values recurring under more than one application; record the COUNT and its command on the prompt's provenance block (a number, no names); the sheet stays machine-local, cited by file name | sonnet | same prompt | zero is evidence for K1, non-zero for K2 — the SME says which |
| 6.3 | **Clause G, the succession/parity rider** (pre-picks nothing, presupposes no evidence grammar): G1 a DECLARED `flow_id` with `flow_id_source: declared` and its owner when no launcher names the flow; G2 EXTENDS the page's existing Q6 (where a SUPERSEDED producer lives) rather than re-asking it: `arch_supersedes` (`DataFlow → DataFlow`, `prov:wasRevisionOf`, pairwise chains, current = no incoming edge) registered `planned` in `43-local-architecture.yaml` on `docs_supersedes`'s shape in the same commit; G3 the parallel-run match report is EVIDENCE under whichever of clause E's E1/E2/E3 the SME rules, never a node class; G4 whether parity evidence needs the `:JobRun` input digest — if yes, that is `source-connection-and-run-identity`'s named re-arm; the parity-as-observation reading belongs to E1's EXISTING deferred page `sosa-jobrun-observation.yaml` (re-presented as its own page when the SME says so — never re-drafted as a rider here) | fable | the prompt, `43-local-architecture.yaml`, `docs/design/deepdoc-data-flow-overview.md` | asks four things, pre-picks none |
| 6.4 | Stale-premise sweep (MM11's BEFORE): MM3 done, MM7 released; the S5 shard citations corrected; extract vintage names the sheet's pull date. Nothing marked "already signed" — no clause of this page has been ruled | fable | prompt, `config/gate-log.md` | markers only |
| 6.5 | **Hold sitting 3** — A–F + G in one session; the `:AppDataFlow` pattern doc adopted or retired; the evidence-present guard sequenced before MM10 (G102 precedent); MM11 closes on the record; N19's re-arm written under the entry | fable | `config/gate-log.md` | MM10/MM8/MM9 blockers named and scheduled |
| 6.6 | MM7 re-claimed on Lane A (opus; desktop-local inputs) → MM5 (B) and MM6 (ontology, fable, A) → MM10 writes the UC3 record: legacy `:DataFlow` (declared) and modern `:DataFlow` joined by SUPERSEDES, the parity evidence resolving to a registered document, all `:Uncertain` through the deepdoc boundary; promotion is a gate write, never a label strip | opus / sonnet / fable per item | `drydocs_lineage/`, `drydocs_deepdoc/` | `test_uncertain_boundary` green |
| 6.7 | UC3 source rows as SHAPE — AFTER sitting 2 (a dataset id minted before the grammar signs is re-minted after): `oracle:schema-inventory` and `snowflake:schema-inventory` carry the wiring reason per sitting 1; a synthetic parity-report entry lives only in the descriptor's synthetic section | sonnet (A) | `config/source-registry.yaml`, `config/source-descriptors.yaml` | no load state changes |

## Phase 7 — The 9th roll (after the shim removal, `wired`, and the id migration are on main)

One structural roll instead of two back-to-back applies while the company is mid tech-debt cleanup. The relay names each step and the commit it needs (PORT1's rule), the J77 line, the shim list (`git diff -M` reproduces it consumer-side), the per-side `wired` entry, and the relay lines every gate-prompt edit accrued. The company apply remains a third session in a different repo with one pen; its failing tests come back as ideas in the producer inbox, never as edits. Nothing is asked back; the 38 un-itemized failures were already asked for once (hand prompt `42789fd6`) and stay THEIR ledger's open condition.

## Model assignment

| Kind | Model | Why |
|---|---|---|
| Gate prep, stale-premise sweeps, riders, the three sittings, ADR 0021, ADR 0015 amendments, the mint pass, the TE epic, the deferral rulings' transcription | **fable** | boundary decisions and prose the SME rules on |
| Builds: preflight, inputs guard, the outcome type, `wired`, Idea-309, e2e proof, coverage report, runbook generators, web hook, boundary guard, S11, shims, copier, field-map extractor | **sonnet** | scoped, specified, guarded |
| Chores: claim releases, residue removal, census refresh, gate-prompt sweep, dependency groups | **haiku** | mechanical with a stated acceptance |
| MM7, G85 | **opus** | their existing `model:` |
| `/code-review ultra` | cloud (opus) | user-triggered, once on the Phase 0 tip |
| The sonnet pre-pass before the mint | sonnet | extracts one deduped list so fable frames, not reads |

`.claude/agents/backlog-groomer.md` pins opus and agent front matter takes Claude Code aliases only; the mint pass runs in-session on Fable following the skill's mechanics, and the agent file is left as it is.

## What this plan refuses

- Building `wired`, the outcome type, or any TE code before its ruling: the D2 fence (scoped), ADR 0021 unaccepted, ADR 0015 unratified — the G125-depends-on-ADR shape.
- Deriving `wired` inside `drydocs_core` from loader registration (core imports nothing from components).
- Folding new questions into the wiring page (its D3 refuses it) — the descriptor questions are their own short prompt in the same sitting.
- Holding the three identifier gates apart, or closing Phase 0 on the URN half alone (the D6 memory-scope half is clause (i)).
- Carving a second tree for TE Phase 1; a declared list + guard + copier `_exclude` is the cut.
- Building field-level mapping or a `:CatalogField` write path in the base; editing a deferred gate page on disk; minting an edition-owned vocabulary entry into a base fragment (a base mints, an instance requests).
- Building a parity primitive before sitting 3; presupposing E3 or any evidence grammar; modelling legacy → modern as `reg_derived_from`; a parity NODE class.
- Extending the `inputs:` guard to `acceptance:`; guarding gate prompts — sweep once.
- Minting lane-close mechanics as items; minting a `gates:` slug whose prompt file does not exist; minting a dataset id before sitting 2 signs.
- Writing a DEFERRED heading for a signed gate (G90, LOAD6), deferring an armed one (D11), or recording any deferral without the SME — a deferral is an SME outcome.
- Giving Lane B a mint pen, a render, `IDEAS.md`, `MODULE_MAP.md` (rendered), `PORT-MANIFEST.yaml`, or a Lane A file in its `files:` — hand-backs go in item notes (the J59 clause (c) shape).
- `-D` on any branch without the user; touching another live session's worktree (`ui-workstream`); deleting `review/module-sweep` or its worktree.
- Removing the ADR 0018 shims in the 8th roll, or from Lane B (four Lane A pens in the sweep); reviving S11's package extraction (ADR 0018 Option C, rejected); find-and-replace on any registered id; renumbering; minting from the tree.
- Naming or guessing the company's edition code, Area Product, or team names producer-side; asking the company for anything back.
- Running the ultra review on today's tree or after the base is finished (user ruling 2).

## Assumptions the user may overrule (stated, not blocking)

1. **Use case 3's data-read ceiling**: the Team Edition holds the VERDICT and the evidence document of the parallel-run match; the row-level comparison runs company-side, read-only, in the venue holding both databases (the `corroborate.py` boundary and the publish boundary). If a TE component should compute the match, that is a clause G5 for sitting 3, not a code decision.
2. **G62** goes on `hold:` (session convened, not concluded) rather than back to `todo`; **E1** on `hold:` with its 2026-07-14 deferral.
3. **Web items (Phase 2 row 10)** go to Lane B only if the UI-testing session is not building web; otherwise they stay with that session.
4. **`feat/source-registration-poc`** is deleted with `-D` only on your say-so at execution; the `ui-workstream` worktree is repointed by its own session.
5. **N24 / N25** ride sitting 1 only if the SME has the time; **N19** is re-armed by sitting 3's G4 or by the first automated DB-backed source the base needs.

## Verification

- **Phase 0**: `git log --oneline --first-parent -12` shows the 9 merge commits (8 `wip/` + the sweep) and no `l23` merge; `unset VIRTUAL_ENV; poetry run pytest -q`, `ruff check .`, `ruff format --check .` green; `git diff --quiet docs/plan/board.html docs/design/*.html web/src/generated/*.json`; `gh run list --branch main --limit 5` green at the pushed sha (not UNVERIFIED — J78); `git branch -r --list "origin/wip/*"` empty; `handoff.py --check` on both files reports the expected residue.
- **Phase 1**: `python .claude/skills/groom-backlog/validate.py` → ALL CHECKS PASS; `test_backlog.py` (incl. `test_declared_gates_are_lists_of_known_prompt_slugs`), `test_plan_roadmap.py`, `test_plan_ideas.py` green at the stub AND the body commit; one snapshot whose `meta` carries the body sha.
- **Phase 2**: `tests/unit/test_port_preflight.py` proves unknown base → NOT CHECKED; the re-run over `port-base-20260905..port-base-20260908` recorded; the `inputs:` guard passes with zero exemptions beyond the five fixes; `tests/integration/test_e2e_synthetic.py` green in the Docker CI job and SKIPPED-with-reason without Docker; the runbooks generated from the stand-ins pass `test_doc_outline.py` and the Excel spec; `scripts/port_preflight.py --tag` prints CHECKED; the J57 family + `test_skip_guard_policy.py` + `test_never_port_citations.py` green at every Lane B close.
- **Phase 3**: each gate-log entry carries one of the three legal outcomes; the deferral headings render as deferred in `gates.json`; ADR 0021's status block records the acceptance.
- **Phase 4**: `require_confirmed()`'s message names the failed fact (pinned by a test); every row that loaded before loads after; `PORT-MANIFEST.yaml` guards green and `per_side_fields` names `wired`; the outcome-type guard has a positive and a negative case; `test_data_zones.py` sees all three sources.
- **Phase 5**: one gate-log entry, three rulings; ADR 0015 ACCEPTED; CI generates XMPL and SMPL and their suites pass; `test_domain_registry.py` exercises the XMPL fragment; `test_field_map_xlsx.py` pins zero graph writes.
- **Phase 6**: the gate-log entry; `arch_supersedes` present as `planned` with `docs_supersedes` untouched; `test_uncertain_boundary.py` green after MM10.
- Every commit: `git branch --show-current` immediately before; explicit-path staging; the pens line in the first commit of each session.

## Critical files

`docs/lane-a-handoff.md`, `docs/lane-b-handoff.md`, `.claude/skills/lane-handoff/scripts/handoff.py` · `config/gate-prompts/registry-wiring-readiness.yaml`, `source-descriptor-axes.yaml` (new), `<n23-slug>.yaml` (new), `schema-identifier-publish-ceiling-teams-edition.yaml`, `data-flow-overview.yaml`, `config/gate-log.md` · `config/source-registry.yaml`, `config/source-descriptors.yaml`, `drydocs_core/source_registry.py`, `drydocs_core/source_descriptors.py` · `drydocs/port/port_preflight.py` · `tests/unit/test_backlog.py` · `tests/integration/test_e2e_synthetic.py` (new), `drydocs/docs_coverage.py` · `docs/decisions/0015-team-edition-template.md`, `docs/decisions/0021-*.md` (new) · `PORT-MANIFEST.yaml` (rows + `per_side_fields`), `docs/port/port-prompt.md` · `drydocs_core/component_map.py`, `tests/unit/test_module_boundary.py` · `.claude/skills/controlm-runbook-automation-excel/` · `drydocs_core/ontology/relationship_vocabulary/{10-node-classifications,42-local-catalog,43-local-architecture}.yaml` · `config/taxonomy/{editions,domains}.yaml`.
