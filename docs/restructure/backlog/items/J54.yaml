id: J54
epic: release-infrastructure
title: VERSIONING.md describes a backlog schema and a backlog file that no longer exist, so the release
  ritual instructs a reader to cite a tombstone
type: chore
module: docs
phase: 8
agent: main
model: haiku
priority: p3
status: done
depends_on: []
inputs:
- VERSIONING.md
- docs/restructure/backlog/README.md
acceptance: |
  (a) THE TWO STALE FACTS. `VERSIONING.md` names the backlog config schema at its PREVIOUS version, and its release-notes section tells the author to cite epic and item ids from `docs/restructure/backlog.yaml`. The schema is now v3 (ADR 0013) and that file is a tombstone — the backlog is one item per file under `docs/restructure/backlog/items/`. Both are corrected, and the ids-to-cite instruction points at the sharded tree.

  (b) THE REST OF THE FILE IS READ, NOT JUST GREPPED. Any other statement in the ritual that names a path, a command or a guard is confirmed against the tree in the same pass; a currency fix that leaves the next stale line for the next reader has moved the problem rather than fixed it. The close note lists what was checked and found correct, not only what changed.

  (c) OUT OF SCOPE, DELIBERATELY: the v0.3.0 tag disposition and whether a v0.4.0 is owed. Those are release decisions for the user (see the inbox question groomed alongside this item) and this chore must not enact either — it does not push a tag, re-cut one, or change the version string in `pyproject.toml`.
notes: |
  Groomed 2026-08-26 from Idea-163 clause 3, which offered the choice of folding this into the tag work or treating it as ordinary doc drift. Treated as doc drift because clauses 1 and 2 of that entry are user decisions with no default, and blocking a two-line currency fix behind a release call would leave the ritual actively misleading for as long as the call takes.

  Module `docs`, epic release-infrastructure, phase 8 — VERSIONING.md is release-ritual documentation.

  p3/haiku: two corrections against facts already established by ADR 0013.
  DONE 2026-09-08 (desktop, Lane A). No edit to VERSIONING.md in this close, and that is
  the finding: (a) had ALREADY landed on 2026-08-31 in e0134168 ("three stale pointers in
  the public-surface list"), committed against Idea-163 rather than this item, so the item
  stayed open over a file that was current. That commit fixed both facts named here - the
  schema line reads `drydocs.backlog.v3` and the changelog cross-reference reads
  `docs/restructure/backlog/items/<id>.yaml` - and a third the item did not name: the
  graph-model surface pointed at `relationship_vocabulary.yaml` as one file, now the
  per-domain fragments directory (S5).
  (b) The rest of the file, read line by line against the tree at a5a8ad52 and found
  correct: `pyproject.toml` `[tool.poetry] version` is 0.3.0 (:1, :3); `v0.3.0` is an
  ANNOTATED tag (git cat-file -t = tag, 2026-07-09) and the only v* tag - the other 18
  tags are port-base-* and archive/*, outside this policy; `plan.yaml` phase 8 "Planning &
  release infrastructure" carries `release: v0.3.0`, the example the file gives; the six
  schema families all exist - drydocs.backlog.v3 (backlog_store.py, test_backlog.py),
  drydocs.source-registry.v2, drydocs.taxonomy-ontology-map.v1, drydocs.classification.v1,
  drydocs.source-mapping.v1 (config/*.yaml, config/taxonomy-ontology-map/) and
  drydocs.doc-outline.v1 (docs/design/templates/*.outline.yaml) - listed by family, so a
  version bump inside one does not stale the line; `relationship_vocabulary/` is a
  directory of fragments; CHANGELOG.md has `## [Unreleased]` and `## [0.3.0] - 2026-07-09`
  in Keep-a-Changelog shape; the five ritual steps name no path that has moved; the
  History paragraph (0.1.0 untagged, 0.2.0 skipped, v0.3.0 first tag) matches the tag list.
  (c) Untouched, as required: the v0.3.0 tag, the version string, and whether Unreleased
  has earned v0.4.0 - the user's release call, still open in the inbox (Idea-163).
  Method note for the next currency item: grep the file's paths AND `git log -- <file>`
  before claiming - the fix may have shipped under a different id.
