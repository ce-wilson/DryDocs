id: GRAPH5
epic: component-topology
title: "Remove the eighteen ADR 0018 re-export shims in one item - the 'roll after next' trigger fired on 2026-09-08 and action item 6 is still unchecked - with the citation sweep and the relay line the company needs"
type: task
module: graph-infra
phase: 6
agent: main
model: sonnet
priority: p2
status: todo
hold:
  since: 2026-09-11
  by: user
  until: the company completes the port-base-20260908..port-base-20260910b apply and reports it
  reason: 'The shims are the compatibility layer the consumer is standing on RIGHT NOW. Their 2026-09-11 remaining-work report puts the drydocs.graph_verify to drydocs.review.graph_verify relocation in the work still ahead of them, so removing the old paths producer-side sets a clock on a transition they have not finished. The trigger this item names has fired (2026-09-08) and the removal is correct - the ruling is only about WHEN, and the user ruled: after the port completes. Nothing blocks it technically; zero tracked modules import the old paths.'
depends_on: []
inputs:
- docs/decisions/0018-component-identity-declared-not-directory.md
- drydocs/publishing/__init__.py
- drydocs/review/
- drydocs/plan/
- drydocs/port/
- drydocs/docgen/
- PORT-MANIFEST.yaml
- docs/port/port-prompt.md
acceptance: |
  ONE item for all eighteen shims (seventeen module files carrying "Removed at the roll after next"
  plus drydocs/publishing/__init__.py), never per slot: (a) every shim listed with its citing files
  before removal, the count re-measured by command at pull (49 files cite ADR 0018 repo-wide, 23 in
  code packages, at the mint); (b) removed on one branch when no other wip branch is open under
  drydocs/; (c) suite, boundary and J57 guards green at one commit; MODULE_MAP.md re-rendered; ADR
  0018 action item 6 checked; (d) the relay line for the ninth roll written in the notes - the old
  import paths stop resolving at the next port, announced, never discovered. NOT in the eighth roll.
  Lane A: the sweep touches the port, adr and backlog pens.
notes: |
  Minted 2026-09-09 (Lane A mint pass, desktop; pen backlog). review-agents-2026-09-08.md candidate 2 (L2-1) and seams-2026-09-08.md candidate 5; slots 7 and 8
  deliberately declined to re-report it.
