id: G102
epic: component-topology
title: 'Execute the G32 fold — ADR 0011 clause-1 guards FIRST, then the one-database build: registry re-target,
  watermark re-key on trust_default, DESCRIBES delete+reload, sweep rewrite'
type: task
module: graph-infra
phase: 6
agent: main
model: opus
priority: p1
status: done
depends_on: []
inputs:
- config/gate-log.md
- docs/decisions/0011-single-database-contingency.md
- config/doc-source-registry.yaml
- drydocs_api/query_specs.py
- drydocs_api/exports.py
- drydocs_core/schema/provisioning/01_databases.cypher
- drydocs/cli.py
- tests/unit/test_database_names.py
- tests/unit/test_load_map_json.py
acceptance: |
  Gate document-content-topology SIGNED 32/32 on 2026-08-18; this item is the apply, and its ORDER is the ruling's hard precondition — the guards land and pass BEFORE anything folds. (a) GUARDS FIRST (ADR 0011 clause 1): the QuerySpec ground-truth exclusion generated STRUCTURALLY into the registry (never 30 hand edits), the deepdoc writer boundary (every uncertain write carries :Uncertain — the writer is a 17-line stub, so the discipline is built in before the first write), and the live audit spec counting :Uncertain nodes reachable from ground-truth traversals, expected 0. (b) THE FOLD: ddcontext's four corpora re-home to the one database; ddschema does NOT fold (clause 2 never fires, deliberate); provisioning + dev-environment + test_database_names.py match the ruled set; ddall's fate recorded (one constituent = retire or keep as alias, say which). (c) B-DURABLE: WATERMARK re-keys on each source's declared trust_default per row — WATERMARKED_DATABASES has no subject after the fold, so this is forced, and the ddall latent instance dies in the same change. docs-verify's wrong-db check re-points at the realm label rather than silently retiring. (d) REGISTRY: all seven target_db values land on the one database (final corpus names per the 2026-08-18 id grammar); cli.DOC_SWEEP_DATABASES rewrites; traversable-until-move retires from the docs-coverage ladder. (e) R3 IN ONE SESSION PER MACHINE: delete the 27 DESCRIBES edges and reload bmc-docs in the same session (J18 — per-machine graphs; the company side repeats it). (f) test_load_map_json's pinned not-queryable set {dddocs} FAILS at this apply BY DESIGN — treat it as the task it is: the set becomes empty, and softwareModel.ts inGraphLabel + the docs-coverage detail lines stop saying "not provisioned (G32)". Full suite green.
notes: |
  Staged at the G32 sign-off. GUARDS-FIRST is not a preference: the gate recorded the physics-to-discipline loss as accepted ONLY on the precondition that the three clause-1 guards precede the fold. The docmeta P4 revision (knowledge/upgrade-plans/ docmeta-p4-revision-single-db.md) already drafts the single-db load path and stays gate-bound on ITS own items — do not conflate its embedder work with this apply.
annotations:
  status: '2026-08-18 (laptop): guards -> fold -> live apply, all in one session. Guards first per the
    gate (structural exclusion 30/30 sites, write-boundary allowlist of two, audit spec expected-0); topology
    {drydocs, ddschema}; watermark re-keyed on declaration + per-row trust_default (B-DURABLE); all 7
    rows re-targeted; R3 delete+reload VERIFIED LIVE (laptop, neo4jtest, drydocs DB: 27->0->27 DESCRIBES,
    374 chunks, :Uncertain=0); the stranded book copy caught by the transitional sweep, reloaded (1 doc/43
    chunks), ddcontext+ddall DROPPED on this machine; docs-verify 2 loaded/1 missing/4 unshaped clean;
    m3-verify all green. COMPANY-SIDE OWES: the same per-machine sequence (R3 + reload + drop), and cli
    --database default fix rides the port'
