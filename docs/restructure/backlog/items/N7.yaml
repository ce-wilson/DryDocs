id: N7
epic: source-column-mappings
title: 'Source-registry schema v2 design session: split source SYSTEM from extracted DATASET, ruling the
  per-side loader overlay, URN identity handle, and reconcile same-id guard together (HITL gate)'
type: task
module: config
phase: 11
agent: main
model: fable
priority: p1
status: done
depends_on: []
inputs:
- config/source-registry.yaml
- drydocs_core/source_registry.py
- config/taxonomy-ontology-map.yaml
- PORT-MANIFEST.yaml
- git-readme.md
- internal/registry-redesign/REGISTRY-PLAN.md
acceptance: |
  A design doc + gate prompt exist covering the four coupled decisions as ONE design, and an SME gate session rules each (active, or explicitly deferred with reason): (1) two-level identity — source SYSTEM (connection/locator/ classification carrier) vs extracted DATASET (gate/crosswalk/feeds_taxonomy/ ontology-domain carrier, each with its OWN confirmed state), loaders binding to the DATASET, datasets keyed or tagged by ontology domain (DPROD | ORG | SWO | data-platform | code); (2) the per-side loader->source_id config overlay that wins over class defaults (the company T19 deferral's producer follow-up — adopt or rule against); (3) the URN cross-repo identity handle; (4) the reconcile same-id/changed-meaning guard. No registry schema, mapping, or loader-binding change lands before sign-off (J21's hardening of the CURRENT shape is the interim guarantee). The catalog-pat != pat-catalog id collision is recorded in the producer divergence ledger REGARDLESS of the ruling. Output feeds the company T19 gate review rather than racing it.
notes: |
  Groomed 2026-07-30 from the user directive (chat, captured same day): the flat id conflates the source system with the extracted dataset — PAT yields both DPROD datasets and ORG datasets; Control-M yields multiple extraction domains — which is exactly how catalog-pat vs pat-catalog became same-string-different- meaning across repos (T19) and why feeds_taxonomy lists keep widening instead of splitting. Absorbs the 2026-07-29 N7-overlay inbox candidate (this id was reserved for it; the directive bundled it into the one design session). Fable: registry schema v2 is a schema/boundary decision. Company blockers informing the design: 8 company loaders gate on pat-catalog while the producer class says catalog-pat (value-level guard gap — a wrong-but-resolving source_id passes), and ~13 company-only loaders carry no source_id. Direction, NOT decided — everything routes through the gate. 2026-07-31 the user expanded the design input (chat + saved samples): THREE distinctions — software we use/trace (software-registry.yaml, not a source) vs ingested data sources (system + dataset two-level) vs REPLICA datasets whose id combines origin@carrier:artifact (SEAL loaded into psgmgr = seal@psgmgr:CM_ESCALATION_DB); documents keep their own ledger; dataset rows adopt the FCDO Data Authority SOR/ADS vocabulary as a field; classification simplifies to 3 levels for now (External / Internal-Public / Internal). Plan of record + target-column worksheet + SNOW-CMDB / DataHub / SDLC-metamodel samples: internal/registry-redesign/ (REGISTRY-PLAN.md; Phase 0 re-home done, Phase 1 classification collapse may land pre-gate, Phases 2-3 = this item's session and gate). CLOSED 2026-07-31: gate session RUN IN-CHAT same day — D1-D4 and Q1-Q6 ALL RULED (gate-log entry source-registry-v2; prompt config/gate-prompts/source-registry-v2.yaml authored from the session record). SME amendments: D1 seal_id = standing placeholder field (real value internal-twin only); Q1 grammar = {origin}@{db}.{schema}.{table} — the qualified locator REPLACES the informal nickname, all dots after @, lowercase, born-here datasets keep {system}:{artifact}. Q2 URN env always prod; Q3 derived: true + authority omitted (FCDO SOC checked, does not fit); Q4 snow registers now as a SaaS system; Q5 design-docs pipeline-side only; Q6 signed-off gates TRANSFER across renames. T19 naming note recorded in the port-prompt divergence ledger: replacement name = pat:product-catalog + pat:people-report (matches neither legacy string). RESIDUAL explicitly deferred: the 18-row migration table confirms per-row at the N9 BUILD (previously signed rows transfer per Q6; rest land confirmed: false). Divergence-ledger recording done; output feeds company T19, not a port.
