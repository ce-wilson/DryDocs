id: U27
epic: self-documentation
title: 'snapshot.ps1''s CI check asks `gh run list --branch main`, so from any branch HEAD can never
  appear and the verdict degrades to no-run-yet permanently — and the verdict function still has no tests'
type: bug
module: graph-infra
phase: 16
agent: main
model: sonnet
priority: p2
status: done
depends_on: []
inputs:
- knowledge/depgraph-snapshots/snapshot.ps1
- tests/unit/test_depgraph_snapshots.py
- knowledge/depgraph-snapshots/README.md
acceptance: |
  (a) THE BRANCH PIN: the caller asks CI about the branch it is actually on — `git rev-parse --abbrev-ref HEAD` in place of the literal `main` (line 166 at this groom: `gh run list --branch main --limit 10 --json ...`). Today the script fetches main's ten newest runs and matches the LOCAL HEAD sha against them, so from a worktree, an epic slice, an agent fork or any `feat/` branch — exactly the sessions CLAUDE.md tells you to branch for — HEAD is never in the answer and every run takes the yellow "no run yet for HEAD" path until merge. The worked example is the branch this was filed from: CI was GREEN on `feat/ui-workstream` and invisible to a query pinned to `main`. A detached HEAD degrades to something honest (a named skip), never a crash.

  (b) FIVE FIXTURES FOR `Get-CiVerdict`, which its own design comment already claims is "a pure function of (runs, head) so every branch is exercisable without a network" and which has NO tests: green-at-head, failed-at-head-with-an-older-success-in-the-list, in-progress-at-head, no-run-yet-for-head, and EMPTY runs. The empty case is the one that would have caught the 2026-08-20 false GREEN.

  (c) THE TESTS MUST NOT BREAK CI: there is no Pester in this repo and the CI runner's PowerShell availability is not established, so the check SKIPS CLEANLY where no PowerShell is present, with a message naming what is missing (the U26 precedent for `test_probe_instrument.py`'s skip). The mechanism is the implementer's — pytest invoking pwsh with the function dot-sourced, or extracting the verdict so it can be driven directly — the five named cases are the acceptance, not the harness.

  (d) WARN-ONLY IS UNCHANGED (U24 clause d): this reports, it never blocks the snapshot.

  (e) VERIFIED BY RE-RUN, NOT BY READING, with the venue named (J18): one snapshot run FROM A BRANCH whose printed verdict line is quoted in the close note, plus one from the main checkout.
notes: |
  Groomed 2026-08-22 (laptop) from Idea-156. That entry was filed on `feat/ui-workstream` as that branch's Idea-152 and re-filed at 156 on main, because 151 and 152 were both taken there by unrelated entries while the branch sat unmerged — the allocator-band hazard playing out inside one side.

  HALF OF THE ORIGINAL REPORT IS ALREADY FIXED AND IS NOT THIS ITEM. Recorded here only so it is not re-done: defect (1) — `Get-CiVerdict` never enumerating the runs array, so `$mine[0]` was the whole ten-run array and `conclusion -eq "success"` was truthy if ANY recent run passed — landed at 22b8ad7 and properly at 5c0308e / U24, which pins one run object and reads `status` and `conclusion` as scalar strings, with the PS 5.1 ConvertFrom-Json trap written down at the line.

  Module graph-infra / phase 16 / epic self-documentation by the U7, U12, U15, U24, U25 and U26 precedent — every snapshot.ps1 item in this backlog lands there. Sibling of U26 (done) in the same script: that one lost the JSON write from a worktree, this one mis-reports the step immediately before it, and both are invisible from the main checkout. Mechanism only, no gate.

  p2 rather than p3 because the check exists precisely to stop a repeat of Idea-111 (CI red for 100+ consecutive runs while sessions pushed past it), and a check that structurally cannot see branch work is the same failure with a green-looking wrapper.
  DONE 2026-09-08 (desktop, Lane A; built on wip/u27-desktop per J31, merged --no-ff).
  (a) The caller reads `git rev-parse --abbrev-ref HEAD` and lists runs for THAT branch;
      `gh run list --branch main` is gone from snapshot.ps1 (pinned by
      test_the_caller_lists_runs_for_the_branch_head_is_on_never_main_by_literal, code only,
      J66). A detached HEAD prints a DarkGray named skip - "no branch to list runs for" -
      and points at `gh run list --commit <sha>`; nothing throws. The branch reaches the
      verdict as -Branch, so "ZERO runs on main", "newest on main is" and "main is RED" now
      name the branch that was actually asked about.
  (b) Get-CiVerdict moved, unchanged in logic, to knowledge/depgraph-snapshots/ci_verdict.ps1
      and snapshot.ps1 dot-sources it; the design comment's "pure function" claim is now
      enforced (test_the_verdict_file_reads_nothing_but_its_arguments: no `& git`, `& gh`,
      $LASTEXITCODE, Push-Location, Get-Content, Get-Command in its code). Five fixtures in
      tests/unit/test_ci_verdict.py, each a JSON file fed through the live caller's own
      `ConvertFrom-Json ... | ForEach-Object { $_ }` idiom so the PS 5.1 unrolling trap is
      exercised rather than bypassed: green-at-head (Green, exact text);
      failed-at-head-with-an-older-success-in-the-list (Red, the branch named, HEAD's verdict
      not the list's); in-progress-at-head (Yellow); no-run-yet-for-head (Yellow UNVERIFIED,
      newest run's state and the branch named); EMPTY runs (Yellow UNVERIFIED "ZERO runs on
      <branch>", never GREEN). Two more the fixtures imply: gh exit 4 is a DarkGray named
      skip distinct from an empty list (J76), and cancelled-at-head is UNVERIFIED not RED
      (J78). Probed first on PS 5.1: ConvertFrom-Json "[]" piped through ForEach-Object
      yields Count 0, so the EMPTY fixture reaches the ZERO-runs branch and not the
      no-run-yet one.
  (c) Harness is pytest running `pwsh` then `powershell` via shutil.which with -NoProfile
      -NonInteractive -ExecutionPolicy Bypass; where neither is on PATH the file skips with a
      message naming both shells (the U26 shape). Desktop runs it under Windows PowerShell
      5.1.26100; the CI legs (ubuntu-latest and windows-latest) both carry pwsh, so the
      fixtures execute there rather than skipping - the wip/u27-desktop run is the proof.
      test_powershell_keeps_non_ascii_out_of_quoted_strings already rglobs
      every *.ps1, so ci_verdict.ps1 is under the CP1252 guard without a change;
      test_refusal_and_ci_messages_report_the_measurement_not_a_guess now reads both files
      because the CI messages moved.
  (d) Warn-only unchanged: the block is still inside the try/catch that downgrades any
      failure to `ci check skipped`, the verdict is still Write-Host, and nothing between it
      and the write reads it.
  (e) Verified by re-run, both from the PowerShell tool on the desktop, README/snapshot.ps1
      edited in the tree for the first and clean for the second:
      - main checkout at 311d7386 (U27 code uncommitted, so the J64 refusal followed):
        "ci: GREEN at HEAD 311d738"
      - branch wip/u27-desktop at 8d769863, 47 seconds after the push, clean tree, full run
        to the write: "ci: run for HEAD 8d76986 is in_progress - re-check before you close
        the session" - the line the literal-main pin could never have produced, because
        8d769863 was on no main run list. The snapshot that run wrote (header commit
        8d769863, branch wip/u27-desktop) was discarded with the retention deletion and the
        ledger row it made; the ritual snapshot is taken from main after the merge.
  README's CI paragraph names the branch pin, the detached-HEAD skip, ci_verdict.ps1 and
  the five fixtures. Guards: test_ci_verdict (10), test_depgraph_snapshots (13),
  test_module_boundary, test_render_determinism, test_no_render_parsing, test_source_scan,
  test_runbook_currency, test_probe_instrument, test_skip_guard_policy, test_backlog
  (155 passed); ruff check and format --check clean at the 0.5.7 pin.
