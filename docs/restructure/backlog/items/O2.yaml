id: O2
epic: web-console
title: 'Mock persona sign-in (user + admin) gating the web console: My Apps user view; admin-only Cypher
  sandbox, governance placeholder, env toggle'
type: task
module: drydocs-web
phase: 12
agent: main
model: sonnet
priority: p2
status: done
depends_on: []
inputs:
- web/src/
- UI-WIP/drydocs-landing-dark.html
- UI-WIP/DryDocs_UI_Development_Specs.md
- UI-WIP/icons.md
acceptance: |
  Client-side mock auth in web/ (no new npm deps, no real security): with no session the app shows only a persona sign-in screen carrying a MOCK/SYNTHESIZED tag; signing in as the synthetic user persona (jdoe4821) lands on a read-only, SYNTHESIZED-labeled My Apps view (synthetic apps with SNOW permission badges rolling up to a CTO tower) and cannot reach the Cypher console even by editing the URL hash; signing in as the synthetic admin persona reaches the existing Cypher sandbox (both bolt and ADK flows unchanged), a Posture & Governance placeholder, and a cosmetic Prod|UAT|Dev env toggle; the session persists across reload via localStorage and sign-out clears it; the ADK userId is the persona id (no more hardcoded local-dev); a persistent banner labels the whole thing MOCK pending the O1 access-path ADR; npm run lint and npm run build pass; no real SIDs/secrets in web/ (publish boundary).
notes: |
  Groomed 2026-07-13 from the web-login ask (planned session, user-confirmed: mock fidelity / scoped-user-full-admin gating / new branch off main). Scope guard: login/shell/role-gating plus a minimal My Apps view ONLY — not the UI-WIP design-pass port (that stays O1). Deliberately NOT depends_on O1: the mock must neither wait on nor pre-empt the bolt-vs-thin-API access-path ADR — real authn/authz arrives with that decision. Hash-based view switching (no react-router) and no vitest (pure-TS auth module gated by tsc + runtime drive) — both revisitable post-O1. Model sonnet per the matrix (written acceptance; not a boundary decision). DONE 2026-07-13 (same day, branch feat/web-login-mock, commits B-E): lib/auth.ts (localStorage session, role re-derived from PERSONAS — forged/garbage blobs degrade to sign-in) + lib/views.ts (single canSee registry; unauthorized deep links normalize to the role default) + data/myApps.ts (SYNTHESIZED, mirrors the dd270b2 mockup) + SignIn/Shell/MyApps/Governance components + App orchestrator; ADK userId = persona id. Verified: build+lint green; headless-Edge CDP drive 27/27 (sign-in-only fresh state, user hash-guard fallback, reload persistence, sign-out, admin all-views + env toggle, ADK request URL carries asmith7734, bolt wiring fires, corrupt-blob degradation); publish-boundary grep clean (synthetics only). Live-DB rows check env-gated (Docker down) — bolt flow moved verbatim in commit C, wiring proven by connection attempt.
