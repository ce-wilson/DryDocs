id: CFG1
epic: ontology-mapping
title: The domain registry as data - config/taxonomy/domains.yaml, one row per domain with its authority, vocabulary_fragment REQUIRED, the header comment becomes a pointer
type: task
module: config
phase: 2
agent: main
model: fable
priority: p1
status: done
depends_on: []
gates:
- ontology-domain-registry-and-edition-grain
inputs:
- docs/reviews/restructure-design-review-2026-09-02.md
- drydocs_core/ontology/relationship_vocabulary/00-header.yaml
- drydocs_core/ontology/relationship_vocabulary/
- tests/unit/test_classification.py
- config/gate-prompts/ontology-domain-registry-and-edition-grain.yaml
- config/gate-log.md
acceptance: |
  Gate ontology-domain-registry-and-edition-grain §B1-§B3 (SIGNED OFF 2026-09-02). Today the 13 domains exist as a comment block in
  00-header.yaml lines 74-79 and a guard that pins the list; "registered" means someone edited
  the comment. This item makes the registry DATA.

  (a) NEW config/taxonomy/domains.yaml, schema drydocs.domains.v1, one row per domain: `id`
  (the join key, never renamed), `title`, `vocabulary_fragment` (REQUIRED per §B3 - the
  4x-local-*.yaml file; a row without one fails), `minted_by` (producer | company | <edition>),
  `registered_at`, `authority` (the gate or ADR that ruled it; every row cites one), `status`
  active | deprecated with `superseded_by` (the G87 shape). NO `series` column - PLAN1 gave the
  series to the module.

  (b) SEEDED WITH THE 13 AS THEY ARE, each citing its real ruling: eleven from
  vocabulary-domains-and-id-policy (2026-08-12), `infrastructure` from server-location-ontology
  (Z2, 2026-08-19), `human` from §A3 of the 08-12 gate. Nothing invented; the file writes down
  rulings that exist. Fragment entry counts at mint: scheduler 53, business_application 10,
  catalog 16, architecture 24, registry 4, docs 20, all 2, context 8, quality 8, corporate 5,
  itsm 2, infrastructure 3, human 20.

  (c) THE HEADER BECOMES A POINTER: 00-header.yaml's domain block says "see
  config/taxonomy/domains.yaml" and the guard that pinned the closed list reads the file
  instead (J37: the importable object, never the comment). A domain named in any fragment
  entry must exist in the registry, active; a registry row's fragment must exist on disk.

  (d) PORT: a per-entry manifest row on the tom-role-vocabulary pattern (PORT-MANIFEST.yaml:685
  precedent) - rows union by id, each base's own mints stay, the header/schema/field list
  crosses whole. UNNAMED FIELDS clause per J71.

  (e) THE ONTOLOGY IS COMMON AND BASE-OWNED (SME rider, 2026-09-02): the declared ontology sits
  at the base of everything so aggregate roll-up and cross-edition query work against ONE
  vocabulary; an edition may EXTEND it (its own rows, minted_by = the edition, authority = its
  gate) and never override a base row. The registry carries that as a guard: an edition row
  may not reuse a base id, and a base row is never deprecated by an edition. THE GUARD
  SHIPS WITH A FIXTURE THAT PROVES IT FIRES (review F7, docs/reviews/restructure-design-review-2026-09-02.md): no
  edition row exists in this repo and none will (the producer never names the company's
  code, CFG2 c), so a guard with no positive case is vacuous here and first runs
  unobserved at the company. A test-only fixture injects a synthetic edition row - the
  code from CFG2's synthetic sample - and asserts both refusals, on the pattern every
  detector in test_port_manifest.py follows (test_the_coupling_detector_catches_an_uncoupled_pair,
  :264: a detector and the companion that proves it catches).

  (f) What this item does NOT do: mint any domain (ONT1 and ONT2 do the first three), define
  a mint command (a domain is minted by a gate record plus a row; the allocator question is
  name collision across remote refs and history, which `validate.py` already answers for ids
  and can answer for a name - add `--check-domain <id>` only if ONT1 needs it).

  tests/unit green; classification of the new file = Internal-Public (mechanism, no values);
  render clean.
notes: |
  BUILT 2026-09-04 (desktop, Lane A; claimed 8deee035, step 1 of the intake review's
  sequence). config/taxonomy/domains.yaml (schema drydocs.domains.v1, Internal-Public,
  13 rows) + config/schemas/domains.schema.json (S6 family, malformed fixture = a row
  with no vocabulary_fragment) + drydocs_core/ontology/domain_registry.py (the reader,
  on the tom_role_vocabulary shape) + tests/unit/test_domain_registry.py (15 guards) +
  the 00-header.yaml pointer + the per-entry PORT-MANIFEST row with its UNNAMED FIELDS
  clause (added to test_port_manifest's ENTRY_FILES so J71 totality covers it) + the
  MODULE_MAP row. 3211 unit tests green on the desktop.

  ONE CORRECTION TO THIS ITEM'S OWN ACCEPTANCE, made because (b) says "nothing invented;
  the file writes down rulings that exist": (b) attributed ELEVEN domains to the
  2026-08-12 gate. The tree says nine plus `human` (§A3) = ten. `corporate` was
  registered at gate corporate-backbone-vocabulary §B3 (G98, SIGNED 2026-08-17; the
  schema enum gained it at 1099d68d the same day) and `itsm` on SME DIRECTION at the
  G100 build (2026-08-18, b17f24e5) ahead of its gate, which is still a todo item with
  no prompt. The rows cite the real rulings; `itsm` carries `authority_status: pending`
  - a field the acceptance did not name, added so a row can be honest about a gate that
  has not run instead of naming a record that does not exist - and a guard fails the
  day the log signs `itsm-technician-group` with the row still saying pending.

  WHAT "THE GUARD THAT PINNED THE LIST" TURNED OUT TO BE: not a test - the closed enum in
  config/schemas/relationship-vocabulary.schema.json (S6, 08-12 §D2). Kept, because the
  S6 split gives the schema editor-time checking, but demoted to a RENDER of the
  registry: test_the_schema_enum_is_a_render_of_the_registry holds them equal, so a
  domain minted in one place only is a red test. Two guards the acceptance did not ask
  for and the measurement justified: every entry sits in ITS OWN domain's fragment (true
  for all 175 at mint - the partition IS the file, §B3), and registered fragments <->
  fragments on disk is a bijection (an unregistered fragment cannot appear).

  (e) fixture: the synthetic edition code is `XMPL` (tests/unit/test_domain_registry.py
  SYNTHETIC_EDITION) - CFG2's synthetic sample adopts it so the two fixtures agree.
  Both refusals fire (reuse of a base id; a base row superseded by an edition row) and
  the positive case (an edition row with its own id) passes.

  (f) held: no domain minted, no mint command. ONT1/ONT2 add to SEEDED in the test when
  they mint - the pin is deliberate so a dropped row is red, not a diff.
