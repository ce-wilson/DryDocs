id: G62
epic: component-topology
title: 'Run the rua-bundle data-profile gate: the SME names the load population, the agent profiles it,
  and only the walked population enters the curated load'
type: task
module: drydocs-lineage
phase: 6
agent: main
model: fable
priority: p2
status: todo
depends_on:
- G22
- G23
gates: [rua-bundle-data-profile]
inputs:
- config/gate-prompts/rua-bundle-data-profile.yaml
- config/gate-log.md
- config/source-registry.yaml
- drydocs_lineage/
acceptance: |
  USER-GATED START — the SME convenes the session; the pull loop SKIPS this item until then (the C12/J13 idiom). The drafted rua-bundle-data-profile page is walked in its ruled two-step order: section A the SME identifies the source data (which hosts' bundles, where they sit, which collector produced each — Internal, recorded company-side, never on the page), section B the agent profiles that population through the G20 extractor in STAGING with zero graph writes, and section C consumes the profile to admit or trim the population, dispose of the hash-absent and collision cases it counts, and clear the F1 sweep. Sign-off transcribed to config/gate-log.md; gates.json + the enforcement matrix regenerate. No population the page has not walked enters the G23 curated load.
notes: |
  Groomed 2026-08-09 from Idea-85, which asked only for ids and scheduling — the four post-G22 data-profile prompts were drafted 2026-08-07 and unsigned, and an unsigned prompt with no backlog id is invisible to the pull loop. Sessions run COMPANY-SIDE because the identify-the-source-data step names real hosts and paths (Internal); what lands producer-side is the transcription and the regenerated gate surfaces. Model fable: the ruling decides what may be written to the graph.
  RELEASED 2026-09-09 (Lane A close, desktop). git branch -r --list "wip/g62-*" is empty
  (J31 check, recorded); the session the annotation records was convened 2026-08-13 and never
  concluded, and section B runs company-side where the bundles are. On hold (Y7) rather than
  in_progress so the Ready strip and lane-handoff --suggest stop reading it as live work.
hold:
  since: 2026-09-09
  by: user
  until: the next rua bundle is pulled and the SME re-convenes the rua-bundle-data-profile session
  reason: 'USER-GATED START - a convened-but-unconcluded gate session; the pull loop SKIPS this item until the SME re-convenes it.'
annotations:
  status: '2026-08-13 (desktop): SME convened the session (the USER-GATED START); §A opened. No bundles
    on this machine — §B runs company-side; producer lands the transcription + regenerated gate surfaces.'
