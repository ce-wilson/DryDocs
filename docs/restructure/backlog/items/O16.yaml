id: O16
epic: web-console
title: 'Loads module page: loader->JobRun timeline over run/reject/coverage data frames'
type: task
module: drydocs-web
phase: 12
agent: main
model: sonnet
priority: p2
status: done
depends_on:
- O9
- O11
inputs:
- UI-WIP/site-plan.md
- web/src/
- drydocs/loaders/
acceptance: |
  /loads instantiates the shared template with the ReUI Timeline as this module's canvas (site-plan §3 row 8): loader -> :JobRun provenance rendered as a run timeline; data-frame tabs (Runs / Rejects / Drift-coverage) bind to QuerySpecs over the BaseLoader :JobRun envelope; /loads/run/:runId deep links resolve; empty states honest on databases with no runs; both themes.
notes: |
  site-plan.md P3 wave 1 (module 8). DONE 2026-07-21 (Epic-O finish session): LoadsRoute on the shared template — LoadsTimeline (dot-and-rail vertical timeline in the ReUI idiom, status theme-tokened, per-run counts incl. rejected/ marked-removed/reactivated) fed by NEW spec loads.runs.v1; sibling specs loads.rejects.v1 (rows_rejected > 0 — never silent drops) and loads.drift-coverage.v1 (the D7 mark counters). /loads/run/:runId preselects the run (crumb + item highlight). LIVE-VERIFIED beyond the demo path: the local drydocs DB carries real :JobRun envelopes (bmc_docs + software_registry loads, 2026-07-09) and the smoke screenshot shows the timeline + Runs grid rendering them with the LIVE badge and full export toolbar — the first module page verified against real graph rows. Demo runs remain the honest fallback for empty DBs. Build+lint+spec tests green.
annotations:
  status: DONE 2026-07-21 (Epic-O finish session) — see notes
