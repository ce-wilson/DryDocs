id: G116
epic: component-topology
title: 'Run the standard-identity-and-carrier gate: stable id, carrier, DD-digit fence, divergence policy'
type: task
module: config
phase: 6
agent: main
model: fable
priority: p2
status: todo
depends_on: [G95]
gates: [standard-identity-and-carrier]
inputs:
- config/gate-prompts/standard-identity-and-carrier.yaml
- config/gate-log.md
acceptance: |
  A sign-off session for gate `standard-identity-and-carrier` is HELD and its outcome recorded in
  config/gate-log.md as SIGNED, SME-DEFERRED with a named re-arm trigger, or
  BLOCKED with a named blocker -- silence is the one non-outcome. BEFORE the
  session: a stale-premise sweep of the drafted page against current facts (the
  C28/K20 moved-premise model -- G91 half-staled K20's clause 3 in nine days;
  restate honestly, never re-ask signed clauses, L25 riders-not-edits), and the
  extract-vintage precondition where the page keys on an extract (name the pull;
  older pull => re-pull first -- 03-hitl-sme-flow.md convention, 2026-08-27).
  AFTER: follow-up build items minted for every flip the ruling authorizes, or
  an explicit none-needed note in the sign-off entry.
notes: |
  Minted 2026-08-27 (Lane A groom pass, the run-the-gate sweep): 13 of 20
  drafted-unsigned gates had NO item owning the sign-off -- the drafting item
  closes done and the session is never scheduled. Drafting item G95 closed done when the prompt landed; nobody owned the RUN.
  This item is the sign-off session's claim surface; the session itself is
  HITL (agent: main).
  PRE-GATE SWEEP 2026-08-28 (desktop, Lane A): premises CURRENT. Verified against the
  tree: G94 still todo (the page correctly says the selector is unbuilt); TOKEN_REGISTRY
  still the single-source code-as-data with everything proposed (description_tokens.py);
  no standard id minted anywhere (grep clean); ADR 0009/0014 as cited. One premise
  STRENGTHENED, not moved: the B-section port-cost statement (the alias episode) now has
  a newer third instance — the tom-role-vocabulary per-entry manifest row (2026-08-27) —
  the SME may cite it at the session; the question itself is unchanged, so the page is
  not edited (a sweep restates moved premises only). Session is runnable as drafted.
