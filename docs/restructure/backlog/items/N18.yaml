id: N18
epic: source-column-mappings
title: 'Run the registry-wiring-readiness gate (the wired/ready split, Idea-81 close)'
type: task
module: config
phase: 11
agent: main
model: fable
priority: p2
status: in_progress
depends_on: [N10]
gates: [registry-wiring-readiness]
inputs:
- config/gate-prompts/registry-wiring-readiness.yaml
- config/gate-log.md
acceptance: |
  A sign-off session for gate `registry-wiring-readiness` is HELD and its outcome recorded in
  config/gate-log.md as SIGNED, SME-DEFERRED with a named re-arm trigger, or
  BLOCKED with a named blocker -- silence is the one non-outcome. BEFORE the
  session: a stale-premise sweep of the drafted page against current facts (the
  C28/K20 moved-premise model -- G91 half-staled K20's clause 3 in nine days;
  restate honestly, never re-ask signed clauses, L25 riders-not-edits), and the
  extract-vintage precondition where the page keys on an extract (name the pull;
  older pull => re-pull first -- 03-hitl-sme-flow.md convention, 2026-08-27).
  AFTER: follow-up build items minted for every flip the ruling authorizes, or
  an explicit none-needed note in the sign-off entry.
notes: |
  Minted 2026-08-27 (Lane A groom pass, the run-the-gate sweep): 13 of 20
  drafted-unsigned gates had NO item owning the sign-off -- the drafting item
  closes done and the session is never scheduled. Drafting item N10 closed done when the prompt landed; nobody owned the RUN.
  This item is the sign-off session's claim surface; the session itself is
  HITL (agent: main).

  BEFORE CLAUSE DONE 2026-09-09 (Lane A, desktop, at 9da9d617): the stale-premise sweep of
  config/gate-prompts/registry-wiring-readiness.yaml landed as dated markers (the counts by
  command: 30 rows, 21 true, 9 false, regrouped 4 wiring / 4 semantic / 1 neither; the port
  premise of clause C inverted since J71; D2 ruled scoped 2026-08-30; D3's separate item was
  O90 and its guard forbids a wired field until this page signs) and a new section E carrying
  the corrected port question and the sequencing with source-connection-and-run-identity. The
  sibling page source-descriptor-axes (CFG8) is held in the same sitting. The extract-vintage
  precondition does not apply: the page keys on a committed file. AFTER clause: the session
  itself, then the follow-ups minted from the record.
