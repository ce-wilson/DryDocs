id: C6
epic: ontology-mapping
title: Register REQUIRES_SCHEDULER (BatchProcessing → SchedulerKind) as planned; gate before wiring
type: task
module: ontology
phase: 2
agent: ontology-mapper
model: sonnet
priority: p3
status: done
depends_on: []
inputs:
- drydocs_core/ontology/relationship_vocabulary.yaml
- docs/RELATIONSHIP_GUIDE.md
- docs/restructure/03-hitl-sme-flow.md
acceptance: |
  REQUIRES_SCHEDULER (:BatchProcessing → :SchedulerKind), which appears in README/plans but is NOT in relationship_vocabulary.yaml, is registered there status: planned with its from/to nodes + a PROV mapping (or a recorded null-PROV/infra rationale); the post-load step that would WRITE the edge is NOT wired until the HITL gate confirms it; gate routing recorded (gate-log or map open-question). Drift guards green.
notes: |
  Groomed 2026-07-09 from the IDEAS line (2026-07-01 Control-M naming review). Verified 2026-07-09 still absent from relationship_vocabulary.yaml. Ontology/edge-meaning decision ⇒ the gate, never auto-decided; re-opens phase 2 alongside C5. DONE 2026-07-09: registered seal_requires_scheduler (BatchProcessing → SchedulerKind, status: planned, prov_maps_to: ~ null-PROV/infra — same family as SCHEDULED_ON, loader NOT wired) in relationship_vocabulary.yaml, plus a proposed `requires-scheduler` entry in config/taxonomy-ontology-map.yaml (summary proposed 6→7) carrying the 3 gate open-questions (domain node :Application-vs-:BatchProcessing per README.md:263 vs LoadPlanV3; SchedulerKind node_classifications gap; null-PROV confirmation). No graph write until the HITL gate. Drift guards green (test_schema.py skips planned entries; prov_matrix unaffected). 2026-07-09 user clarifications (same day, both captured as gate open-questions + IDEAS, nothing wired): (a) SEAL two-port model is (:Application)-[:HAS_PORT]->(:Port {kind}) — EventProcessing (events/API) + BatchProcessing (batch); REQUIRES_SCHEDULER is batch-only, so from_node refined BatchProcessing→Port (the batch :Port). (b) :SchedulerKind slated for deprecation → :AisCapability + :AiTool (not yet defined), so the to_node target is provisional and re-targets at the gate — SchedulerKind-deprecation captured as its own IDEAS item.
