id: U14
epic: self-documentation
title: Architect-persona metrics count 63 vendored .claude/skills scripts — scope the query pack to the
  real packages so baselines are comparable
type: bug
module: docs
phase: 16
agent: main
model: sonnet
priority: p2
status: done
closed: 2026-08-04
depends_on: []
inputs:
- docs/reviews/code-graph-review-plan.md
- .claude/skills/tech-debt/SKILL.md
- knowledge/depgraph-snapshots/snapshot.ps1
acceptance: |
  The persona/tech-debt query pack scopes its architecture metrics (orphans, untested, fan-in/out baselines) to the repo's real Python packages — a region allow-list in the queries, NOT an exclude in the scanner (the U9 whole-tree scan is the ruled shape; the tree SHOULD show .claude/skills). With the scope applied, the orphan metric reads ~23 against the 24 baseline (not 77) and untested reads ~30 (not 130); the plan's baseline table is restated with the scope written next to each number so the next run compares like with like.
notes: |
  Groomed 2026-08-04 from the 2026-08-02 [bug] inbox line. The pollution: 54 of the 77 raw orphans are Anthropic-shipped docx/pdf skill scripts under .claude/skills/. Fix placed in the QUERIES rather than the scan because the SME direction that created U9 was explicitly "pull all files, so that we can create the tree" — the artifact is right, the metrics mis-scope it. If U13 lands first, apply both filters in one edit to the shared pack. CLOSED 2026-08-04 (desktop, batch with U13 — one edit to the shared pack). The allow-list is the SEVEN package roots the pre-U9 baselines were measured on (drydocs, drydocs_core, drydocs_api, drydocs_remediation, drydocs_lineage, drydocs_deepdoc, tests), bound as $packages in the queries, never the scan. LIVE NUMBERS (fresh load, 2026-08-04): raw .py orphans 77 with .claude=54 exactly as groomed; untested raw .py 129 (the item's "130"); package-scoped untested 29 (the ~30 ✓). ONE ACCEPTANCE NUMBER CORRECTED BY EVIDENCE: the predicted ~23 in-package orphans do not exist — the 23 non-vendored orphans all live OUTSIDE the packages (agents/ 15, scripts/ 4, knowledge/ 3, docs/design/ui-exploration/ 1), and the in-package orphan count against the 24 baseline is 0. Folding those regions into the allow-list to hit ~23 would break the OTHER baseline (untested balloons 29 -> 65), so the ruling shipped is: seven-root scope for baseline comparability, and the first-party non-package orphans (22, sans docs/design/ui-exploration scratch) documented as a SEPARATE LABELED QUEUE in both the skill's A4 row and the plan's restated table — visible, never folded. The plan's baseline table now writes the scope + date next to every number (18 -> 29 fan-in, 24 -> 0 + queue 22, unprobed -> 29/129); A6 and A3's importer end documented as deliberately unscoped (agents/ coupling and fan-in are real). Offline-fallback note updated (snapshot = live tree, so no tombstone guard offline; scope applies as a file_id prefix filter).
