id: G117
epic: component-topology
title: 'Run the script-provenance-gaps gate: tri-state scm_status + path-keyed Script identity'
type: task
module: ontology
phase: 6
agent: main
model: fable
priority: p2
status: todo
depends_on: [G61]
gates: [script-provenance-gaps]
inputs:
- config/gate-prompts/script-provenance-gaps.yaml
- config/gate-log.md
acceptance: |
  A sign-off session for gate `script-provenance-gaps` is HELD and its outcome recorded in
  config/gate-log.md as SIGNED, SME-DEFERRED with a named re-arm trigger, or
  BLOCKED with a named blocker -- silence is the one non-outcome. BEFORE the
  session: a stale-premise sweep of the drafted page against current facts (the
  C28/K20 moved-premise model -- G91 half-staled K20's clause 3 in nine days;
  restate honestly, never re-ask signed clauses, L25 riders-not-edits), and the
  extract-vintage precondition where the page keys on an extract (name the pull;
  older pull => re-pull first -- 03-hitl-sme-flow.md convention, 2026-08-27).
  AFTER: follow-up build items minted for every flip the ruling authorizes, or
  an explicit none-needed note in the sign-off entry.
  Sweep note: G97's build already RUNS on the proposed (storage_scope,
  normalized_path) key -- the sweep states what is already de-facto so the SME
  rules with that on the record, not as a surprise.
notes: |
  Minted 2026-08-27 (Lane A groom pass, the run-the-gate sweep): 13 of 20
  drafted-unsigned gates had NO item owning the sign-off -- the drafting item
  closes done and the session is never scheduled. Drafting item G61 closed done when the prompt landed; nobody owned the RUN.
  This item is the sign-off session's claim surface; the session itself is
  HITL (agent: main).
  PRE-GATE SWEEP 2026-08-28 (desktop, Lane A): premises CURRENT. The page was drafted
  AFTER its two feeder closes (G97 payload build, G56 mount capture — both 2026-08-25,
  and the page's own timing note says so); G97 confirmed done; scm_status implemented
  nowhere (grep clean — the proposal is still a proposal); the two map rows
  (script-scm-status, script-identity-binding) present in 50-mappings-architecture.yaml.
  Session is runnable as drafted.
