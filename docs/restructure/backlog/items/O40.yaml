id: O40
epic: web-console
title: 'DL-10 StatTiles click-to-filter: optional onSelect + active tile scopes the adjacent list (DSI''s
  KPI-card→filter idiom)'
type: task
module: drydocs-web
phase: 12
agent: main
model: sonnet
priority: p3
status: done
depends_on: []
inputs:
- web/src/components/StatTiles.tsx
- web/src/loads/
- internal/dsi-reference/
acceptance: |
  StatTiles accepts an optional onSelect + active-tile id; the active state is token-only and keyboard-operable (aria-pressed, focus-visible); adopted on one live surface where tiles filter the content below (first candidate: loads timeline, tile = status filters runs); both themes verified; build + lint green.
notes: |
  Groomed 2026-07-28 pm from the DSI addendum DL-10.
annotations:
  status: DONE 2026-07-29 UI sweep (commits 77f68c5..c8b0e67)
