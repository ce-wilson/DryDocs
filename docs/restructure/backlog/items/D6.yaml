id: D6
epic: config-loaders
title: 'Make the README quick start verify clean: 2 active sample folders have no jobs, failing m3-verify'
type: chore
module: drydocs-load
phase: 3
agent: main
model: haiku
priority: p3
status: done
depends_on: []
inputs:
- drydocs/data/samples/controlm_folders__sample.csv
- drydocs/data/samples/controlm_jobs__sample.csv
- drydocs/cli.py
acceptance: |
  EITHER folders 161020 and 160501 each gain at least one sample job row (conditions/ dependencies CSVs and any fixture-count tests updated to match) OR m3-verify downgrades the "active folders contain at least one job" invariant to a WARNING for the CSV sample adapter (kept failing for Oracle runs) — either/or decided at execution. Result either way: the README quick-start chain (bootstrap -> supplements -> refresh-reference -> ingest-controlm -> m1-verify -> m3-verify) exits 0 against the bundled samples on a fresh database. Full suite green.
notes: |
  Groomed 2026-07-15 pm from the same-day inbox chore (seen at the first live EE bootstrap: empty=2 total=7, exit 1 — pre-existing sample-data gap, not a loader bug). Sample rows are SYNTHESIZED — keep the publish boundary (no real names). MERGED 2026-07-18 (EE re-bootstrap inbox line, 2026-07-16, its own suggestion): the quick-start/bootstrap sequence this item makes verify clean also gains the demonstrable-content reloads — load-software-registry + load-bmc-docs (+ optionally load-essential-graphrag, ddcontext) — so a fresh container PROMPTS their reload instead of silently losing corpora (found at Q3: the fresh O6 container had no bmc-docs corpus; the P0 spike re-ran both loads, proving the gap and the two-command fix). DONE 2026-07-18: either/or decided as ADD JOBS (invariant stays strict) — folders 161020/160501 gained two synthesized jobs each (PROV + AUT-TRUST pairs, naming grammar kept, publish boundary clean); fixture counts updated (smoke.ps1 + run-drydocs SKILL.md 13->17; the depgraph-oracle parser test extended: 17 INVOKES, 10 ABINITIO + 7 SHELL, 16 distinct children); README expected m3-verify output reconciled to live (17 jobs, empty=0, 8 distinct WAS_INFORMED_BY edges from the 10 dependency rows, apps=0 sample-mode note) and quick-start gained step 3b (the merged demonstrable-content reloads with the re-bootstrap warning). VERIFIED LIVE: ingest-controlm + m3-verify exit 0, every invariant yes incl. "active folders contain at least one job" empty=0.
annotations:
  status: DONE 2026-07-18 (main fable session, D5-D7 batch)
