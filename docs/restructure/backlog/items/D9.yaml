id: D9
epic: config-loaders
title: 'Precedence: express the internal refines chain (vendor <- platform team <- support group) instead
  of one flat internal tier'
type: task
module: config
phase: 3
agent: main
model: fable
priority: p2
status: done
depends_on: []
inputs:
- config/precedence.yaml
- knowledge/standards/technology/folder-naming-convention.md
acceptance: |
  A proposal lands for how config/precedence.yaml expresses two internal levels (the DAT SRE platform standard refining the BMC baseline, the HLT support-group standard refining DAT) so that where the two internal standards disagree — folder grammar, position-6 meaning, whether the app code carries the SEAL — the file records which wins; the ordering decision itself routes through the HITL gate (docs/restructure/03-hitl-sme-flow.md) and precedence.yaml changes only when confirmed; the position-6 "frequency = legacy" claim in the publishable folder-naming standard is verified against both source standards and corrected or annotated per-standard if still wrong.
notes: |
  Groomed 2026-08-07 from Idea-64 (SME framing: Vendor -> Company/Platform team -> Lower support group; both internal levels sit at precedence tier 2 today). Fable: conflict-resolution semantics. The refines: frontmatter chain in knowledge/standards is the input evidence.
  DONE 2026-09-07 (desktop, Lane A) - FOLDED INTO D11 (user ruling in-chat); no proposal was
  written here and nothing is ruled. The ordering question is real and unchanged; what changed
  is where it is asked: D11 runs the controlm-definition-precedence gate over the same
  config/precedence.yaml, and one SME sitting on one file beats two. The prompt gained section D
  (D1 the two-level chain, D2 conflict-wins, D3 the position-6 verification this acceptance
  asked for, D4 the reading against the edition axis - the 2026-09-02 edition-grain ruling makes
  platform-team-vs-support-group the same shape as base-vs-edition, which is the reason this
  item did not survive as a separate proposal). The prompt's original 'D9 stays separate' line
  is kept with a dated postscript, not edited. Inputs carried to D11: config/precedence.yaml and
  knowledge/standards/technology/folder-naming-convention.md.
