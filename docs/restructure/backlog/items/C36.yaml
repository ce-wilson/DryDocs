id: C36
epic: ontology-mapping
title: 'Run the email-dl-contact-point gate (the SS G5 direction RECORD is not a sign-off)'
type: task
module: docs
phase: 2
agent: main
model: fable
priority: p2
status: done
depends_on: [C29]
gates: [email-dl-contact-point]
inputs:
- config/gate-prompts/email-dl-contact-point.yaml
- config/gate-log.md
acceptance: |
  A sign-off session for gate `email-dl-contact-point` is HELD and its outcome recorded in
  config/gate-log.md as SIGNED, SME-DEFERRED with a named re-arm trigger, or
  BLOCKED with a named blocker -- silence is the one non-outcome. BEFORE the
  session: a stale-premise sweep of the drafted page against current facts (the
  C28/K20 moved-premise model -- G91 half-staled K20's clause 3 in nine days;
  restate honestly, never re-ask signed clauses, L25 riders-not-edits), and the
  extract-vintage precondition where the page keys on an extract (name the pull;
  older pull => re-pull first -- 03-hitl-sme-flow.md convention, 2026-08-27).
  AFTER: follow-up build items minted for every flip the ruling authorizes, or
  an explicit none-needed note in the sign-off entry.
  The 2026-08-12 RECORD captured direction only; the page carries a company-side
  J51 entry_rule (section-by-section producer-intent apply) -- the sweep checks
  both sides' copies tell one story before the SME reads it.
notes: |
  Minted 2026-08-27 (Lane A groom pass, the run-the-gate sweep): 13 of 20
  drafted-unsigned gates had NO item owning the sign-off -- the drafting item
  closes done and the session is never scheduled. Drafting item C29 closed done when the prompt landed; nobody owned the RUN.
  This item is the sign-off session's claim surface; the session itself is
  HITL (agent: main).
  DONE 2026-09-07 (desktop, Lane A) - FOLDED INTO C44 (user ruling in-chat); nothing here is
  signed. C44 convenes the MFTS route ruling at this same gate and already re-poses the G5
  RECORD as explicit clauses, so two items owned one session; C44's acceptance gained clause
  (h) at this close - the session is HELD and its outcome recorded, the exact clause this item
  carried. The 2026-08-12 G5 RECORD stays a RECORD until that session signs it. C29 (the
  drafting item) is unchanged.
