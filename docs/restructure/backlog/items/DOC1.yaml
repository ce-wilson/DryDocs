id: DOC1
epic: release-infrastructure
title: Amend ADR 0015 D2/D6 - Team Edition ships a thin instance-owned backlog, inbox and board; the base backlog stays canonical-template; groom-backlog ships with a mandatory --scope
type: task
module: docs
phase: 8
agent: main
model: fable
priority: p1
status: done
depends_on: []
gates:
- ontology-domain-registry-and-edition-grain
inputs:
- docs/reviews/restructure-design-review-2026-09-02.md
- docs/decisions/0015-team-edition-template.md
- docs/decisions/README.md
- config/gate-prompts/ontology-domain-registry-and-edition-grain.yaml
- config/gate-log.md
acceptance: |
  Gate ontology-domain-registry-and-edition-grain §D1 (SIGNED OFF 2026-09-02). ADR 0015 D2 (line 119, Cut row) removes
  plan_board/plan_ideas/plan_roadmap and backlog_store from Team Edition - "Plan/port/backlog
  are the producer's own project management" - and D6 follows it (groom-backlog
  producer-only). D1 of the same ADR makes the completeness ledger GENERATE per-team work,
  and generated work needs a place to land. The SME ruled: amend.

  (a) AMEND D2: Team Edition ships a THIN, INSTANCE-OWNED backlog - items + inbox + a
  per-instance board - under the copier `instance-owned` file class D4 already defines. The
  BASE backlog stays `canonical-template`, frozen; an instance never writes under base
  `items/`. backlog_store leaves the Cut row and joins the kept set, with a scope filter.

  (b) AMEND D6: `groom-backlog` ships in the template, with a MANDATORY `--scope
  <edition>`; the aggregate (all-editions) board is something only a base renders.

  (c) STRENGTHEN D4 with the SME's 2026-09-02 rider: the declared ontology is COMMON and
  base-owned - "at the base of everything" - so that aggregate roll-up and cross-edition
  query run against one vocabulary; an edition extends, never overrides (CFG1 e). D4 already
  says vocabulary change happens in the base; the amendment says WHY, and names the
  aggregate query as the thing the rule protects.

  (d) GUARDS named in the amendment, built by their own items when TE builds: an instance
  item carries its edition segment (PLAN2); `depends_on` may point instance -> base, never
  base -> instance; an instance never commits an aggregate render; an edition vocabulary row
  never reuses a base id.

  (e) THE AMENDMENT IS A DATED SECTION under the ADR's decisions, not an edit of D2's original
  text - the ADR is PROPOSED, and the record of what changed and why (this gate, §D1) is the
  point. docs/decisions/README.md index line updated. Related rulings recorded in the same
  section: §C1-§C3 (the edition is a declared segment keyed to area_product_id) so the ADR's
  D3 tenancy paragraph and the id grammar say the same thing. AND ONE OPEN QUESTION, written
  as open (review F8, docs/reviews/restructure-design-review-2026-09-02.md): (b) says the aggregate board is
  something only a base renders, but instances are copier-generated repositories and the
  base has no declared way to read N instance backlogs - the graph side has its answer (the
  ddestate composite, D3) and a backlog roll-up would most naturally ride the same rail
  (items loaded as nodes, rendered from the composite) rather than a multi-repo file read.
  That is a Team Edition design question for the ADR's follow-up list; the dated section
  records it so the amendment does not read as though the roll-up exists.

  No code; the ADR still awaits its own Phase 0 gate (the URN shared-vs-owned question).
notes: |
  BUILT 2026-09-04 (desktop, Lane A; claimed 683f1901, step 2 of the intake review's
  sequence). docs/decisions/0015-team-edition-template.md: an AMENDED banner at the top on
  the 0002/0006 shape (pointer only), the dated section "Amendment 2026-09-04" under
  Decision after D6 with A1 (D2: the thin instance-owned backlog; backlog_store to the kept
  set with a scope filter; the plan renders follow it; port_preflight/port_backlog_union
  stay cut), A2 (D6: groom-backlog ships with a mandatory --scope, and why mandatory - the
  I6 failure one level up), A3 (D3 and the id grammar say the same thing; the Area Product
  grain with the SME's definition; the company base mints its own code; the band retires;
  the Idea rider still to sign), A4 (D4's reason in the SME's words: the aggregate query is
  what the frozen ontology protects; extend-never-override, built at CFG1), A5 (the four
  guards, each a test in the frozen class, built by their own items), the F8 open question
  recorded as open with the composite rail named as the likely answer, and "what this does
  not do" (no code; Phase 0 still blocks). The follow-up table gains row 3a for the roll-up
  question; relates_to gains the gate log; docs/decisions/README.md index line updated.
  D2/D3/D4/D6 original text untouched (e). Doc guards green (runbook currency, fences,
  560-test doc subset).
