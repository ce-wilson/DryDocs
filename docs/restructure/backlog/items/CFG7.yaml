id: CFG7
epic: release-infrastructure
title: "One-off sweep, not a guard - the nineteen gate prompts and two sweepable items still citing the S5-retired file names get the current directory paths with dated AMENDED markers"
type: chore
module: config
phase: 8
agent: main
model: haiku
priority: p2
status: done
depends_on: []
inputs:
- config/gate-prompts/
- docs/restructure/backlog/items/
acceptance: |
  The nineteen prompt files citing config/taxonomy-ontology-map.yaml or
  drydocs_core/ontology/relationship_vocabulary.yaml (both directories since S5) are corrected with
  the sanctioned [AMENDED 2026-09-xx; was <old-path>] shape (the precedent is
  code-graph-package-layer.yaml at ecc1622c); the two non-done items too; signed pages get riders,
  never edits (L25); the validator is green; and because config/gate-prompts/** is canonical-company,
  one relay line for the next roll is written in this item's notes. Explicitly a sweep: a bare-path
  guard would fail on the marker recording its own fix.
notes: |
  Minted 2026-09-09 (Lane A mint pass, desktop; pen backlog). premise-drift-2026-09-09.md section 6 ask 2 (25 of 30 broken citations are this one pair).

  DONE 2026-09-09 (Lane A, desktop). Nineteen prompts carry a dated header RIDER (a comment
  block after each page's leading comment naming which of the two S5-retired file names the page
  cites and what each is now); no clause text moved, so L25 holds on the signed pages; updated:
  set to 2026-09-09 on all nineteen per the per-side rule. The three open items whose notes cite
  the names (K26, E1, L19) were left alone as records of the past. RELAY-52 names the nineteen
  for the company, whose canonical-company copies win. test_config_identity_header and
  test_gate_pages green; the identity-header guard confirms the rider sits above the keys.
