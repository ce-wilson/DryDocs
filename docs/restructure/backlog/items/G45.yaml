id: G45
epic: component-topology
title: rua_inventory stages the metadata-only scripts.csv listing instead of silently dropping every script
  — presence-dispatch with scripts.tsv precedence (back-flow, mechanism-only)
type: bug
module: drydocs-lineage
phase: 6
agent: main
model: sonnet
priority: p2
status: done
depends_on: []
inputs:
- drydocs_lineage/extractors/rua_inventory.py
- tests/unit/test_lineage_rua.py
- config/gate-prompts/rua-load-shapes.yaml
acceptance: |
  _stage_scripts dispatches by presence: scripts.tsv (v2, richer) WINS when both files exist; else scripts.csv (path|script|permission|date|size, pipe-delimited) stages METADATA-ONLY — abs_path joined from path+script, no sha256 so hash_missing counts, no body copy so script_copies_missing counts (a LISTING is a fact, never implied content); neither file present files sections_optional_absent. Reader refactor keeps one delimited-read path (the company _read_delimited shape or equivalent). Synthetic fixtures pin BOTH the csv fallback (rows stage, counters assert) and the tsv-wins precedence; zero real values anywhere (mechanism-not-instance); staging-only — no loader, no graph write (G22 stays the terminus); full suite green.
notes: |
  Groomed 2026-07-28 evening from the back-flow [bug] line: company session fixed theirs same day (561 scripts across two production bundles were ALL silently dropped pre-fix; staging-only, terminus held). Producer parity keeps bundles cross-ingestible ahead of the T14 collector convergence; the v1-tag-wearing-v2-payload collector caveat rides T14, not this item. Gate-page premise 2 + provenance already carry the hash-absent consequence for G22. CLOSED 2026-07-28 evening: _stage_scripts is a presence dispatch (_stage_scripts_tsv unchanged v2 path WINS; _stage_scripts_csv NEW — joined abs path = the rua_script identity, permission/date/size mapped to perms/mtime/size, hash_missing + script_copies_missing counted per row; neither file -> optional-absent exactly as before, v1 test still green); _read_tsv is now a thin wrapper over _read_delimited(sep). 3 synthetic-fixture tests (fallback stages + counters; tsv-wins with the csv-only row NOT staged; malformed rows counted). Staging-only — no loader, no graph write, G22 stays the terminus. Suite 1108 green.
