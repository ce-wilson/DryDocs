id: G89
epic: ontology-mapping
title: 'Tech-debt: resolve the m3_triggers conflict — a planned edge is live-consumed by a QuerySpec'
type: chore
module: drydocs-api
phase: 2
agent: main
model: sonnet
priority: p3
status: done
depends_on: []
inputs:
- drydocs_api/query_specs.py
- drydocs_core/ontology/relationship_vocabulary/40-local-scheduler.yaml
acceptance: |
  Recorded at gate vocabulary-domains-and-id-policy §C3 (2026-08-12): m3_triggers is status planned (no loader may write it; the lineage writer refuses it as gate-bound) yet a live QuerySpec MATCHes [:TRIGGERS] -- the spec returns empty rather than wrong, but the surface implies data that cannot exist. RESOLVE ONE WAY: (a) demote the spec to demo-only / remove it until the m3_triggers build, or (b) prioritize the m3_triggers build (its shape was confirmed 2026-07-15; the cmdline-derivation caveat is in its note). A ruling plus the matching code change closes this; do not flip the entry active without its supplement + loader.
notes: |
  DONE 2026-08-21, resolved a THIRD way the item did not list, because reading the spec against the
  vocabulary showed the conflict was worse than §C3 recorded: runbooks.series.v1 MATCHed
  (:ControlMJob)-[:TRIGGERS]->(:ETLProcess), but the planned entry (m3_triggers, now
  scheduler_triggers after G87) is registered Script -> ETLProcess -- so even after the build the
  spec would have returned empty. Neither (a) demote nor (b) prioritize was needed: the fact the
  surface wants (which job launches which ETL process, and what it lands) is already an ACTIVE edge,
  scheduler_invokes = ControlMJob -[:INVOKES]-> Script | ETLProcess (G55). The spec now MATCHes
  [:INVOKES]; description reworded to say what it returns; column ids unchanged (the web SpecGrid is
  column-agnostic, RunbooksRoute.tsx untouched). The TRIGGERS entry stays planned and untouched; its
  build, when it comes, adds the wrapper-script hop as a second spec rather than rewriting this one.
  Nothing flipped active; no supplement or loader claimed.
