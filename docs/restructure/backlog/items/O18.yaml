id: O18
epic: web-console
title: 'Docs module page: Document->Chunk corpus map with trust/provenance tiers visible per row'
type: task
module: drydocs-web
phase: 12
agent: main
model: sonnet
priority: p3
status: done
depends_on:
- O9
- O11
inputs:
- UI-WIP/site-plan.md
- web/src/
- drydocs/loaders/cypher/bmc_docs.cypher
- drydocs/loaders/cypher/essential_graphrag.cypher
acceptance: |
  /docs instantiates the shared template over the bmc-docs lexical corpus: Document -> Chunk map in the graph pane; data-frame tabs (Documents / Chunks / Trust-provenance) with the trust tier (VERBATIM / GROUNDED / SYNTHESIZED) visible as a column, never hidden; QuerySpecs against the docs corpus database; /docs/document/:docId deep links resolve; both themes.
notes: |
  site-plan.md P3 wave 2 (module 6). Corpus exists (document_id/chunk_id constraints, bmc-docs load gate-accepted 2026-07-08). DONE 2026-07-21 (Epic-O finish session): DocsRoute on the shared template — MiniDag Document->Chunk map (FIRST_CHUNK/NEXT_CHUNK/ PART_OF/DESCRIBES shape); three NEW specs docs.documents.v1 / docs.chunks.v1 / docs.trust-provenance.v1 with the trust tier (VERBATIM/GROUNDED/SYNTHESIZED) as a visible column, never hidden (effective tier = chunk override else document default); /docs/document/:docId deep links resolve. LIVE-VERIFIED against the real corpus: the smoke screenshot shows all 27 bmc-docs documents (GROUNDED tiers, External classification, chunk counts) with the full export toolbar. Suite + build + lint green.
annotations:
  status: DONE 2026-07-21 (Epic-O finish session) — see notes
