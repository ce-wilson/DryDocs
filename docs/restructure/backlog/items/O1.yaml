id: O1
epic: web-console
title: 'Web console design pass: wireframes, a real C4/graph rendering, and the browser↔Neo4j access decision'
type: requirement
module: drydocs-web
phase: 12
agent: main
model: fable
priority: p2
status: done
depends_on: []
inputs:
- web/
- docs/decisions/
acceptance: |
  The web/ front-end leaves its throwaway-test-page state: wireframes committed; a decision recorded (ADR or design doc) on the browser↔Neo4j access path — bolt-from- browser vs a thin API — with rationale; the console renders a REAL C4 / graph view of the knowledge graph (NVL or an agraph-successor in web/, against the local EE DB) rather than the test page; drydocs-web's build/run is documented. No secrets in web/ (publish boundary).
notes: |
  Groomed 2026-07-09 from the 2026-07-03 IDEAS note (updated 2026-07-09 with the active branches). Marked in_progress: a design pass is actively in flight (branches feature/ui-dark-landing-myapps + origin/feat/web-console-design-pass, untracked docs/design/ui-exploration/). Plan change (user-approved): created module drydocs-web + phase 12. The architecture is still open — the bolt-vs-thin-API call is the crux of this item. MERGED 2026-07-13 (groom, IDEAS 2026-07-12 artifact-design review sub-item 2): the design pass applies the artifact-design skill's "UI, not a document" checklist + its AI-default-looks list as the review lens over the docs/design/ui-exploration/ mockups. RE-TIERED opus -> fable on this groom touch (the G3 groom-touch policy; the bolt-vs-thin-API call is a boundary decision, the matrix top slot).
progress: |
  2026-07-14 — DESIGN-LANGUAGE PORT LANDED (branch feat/web-login-mock, on top of the O2 mock login): the dd270b2 dark mockup is now the React app — Landing (hero/features/ tower cards), TowerDrill (cypher/graph/schema/preview/matrix, all SYNTHESIZED via data/towers.ts), dark token system (index.css, single-theme by design), IBM Plex, GraphSvg renderer, My Apps rollup graph; role-gated tower drilling (user = own tower only). web/ has left its throwaway-test-page state. Verified 27/27 headless-Edge CDP drive + screenshots. REMAINING for O1: the browser<->Neo4j access-path ADR (the crux; forced by any real login — mock deliberately doesn't decide it), a REAL C4/graph view against the live EE DB (NVL or successor) instead of the synthesized SVGs, wireframes committed, build/run docs. 2026-07-14 (later) — ADR 0005 on main, status PROPOSED (docs/decisions/0005): the session's sanitized-evidence draft was REWRITTEN by the SME and landed via the other machine (b5235f4) — thin API is the DEPLOYMENT shape; bolt-from-browser survives only as a dev-mode adapter (dev flag + admin) behind ONE GraphAccess seam in web/src/lib/; no VITE_NEO4J_PASSWORD ever committed. Main's version is canonical (branch draft superseded at the 2026-07-14 reconcile merge). The acceptance's "decision recorded" ticks when 0005 flips ACCEPTED; on accept, groom the thin-API component + GraphAccess seam refit + live graph view into Epic O. NOTE: the landed design pass predates the seam — no new bolt usage (pages are synthesized), but the seam refit is the first code consequence of acceptance. 2026-07-14 (evening) — ADR 0005 ACCEPTED (O3 done): the "decision recorded" acceptance clause is SATISFIED. O1's remainder = O6 (live C4/graph view through the seam, via O4+O5); wireframes committed (docs/design/ui-exploration, e5608ff); build/run docs ride O5. DONE 2026-07-15 — O6 landed, closing the umbrella: every acceptance clause now holds — throwaway state left (O1 design pass), wireframes committed (docs/design/ui-exploration), decision recorded (ADR 0005 ACCEPTED, thin API; O3), REAL C4/graph view against the local EE DB (O6: agraph-successor = d3-force + in-repo SVG in web/, NVL decision recorded on O6), build/run documented (web/README Graph-view section + O5's api run docs), no secrets in web/ (publish-boundary greps clean at O2/O4/O6).
