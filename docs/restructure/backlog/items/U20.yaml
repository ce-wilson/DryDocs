id: U20
epic: self-documentation
title: 'The code-graph review plan still says six scan roots: restate its doc-coverage unit on eight package
  roots, or derive the list the way U18 derived the metric scope'
type: bug
module: docs
phase: 16
agent: main
model: sonnet
priority: p3
status: done
depends_on: []
inputs:
- docs/reviews/code-graph-review-plan.md
- tests/unit/test_code_graph_review_plan.py
- pyproject.toml
- .claude/skills/tech-debt/SKILL.md
acceptance: |
  Phase 3 unit 3 of docs/reviews/code-graph-review-plan.md no longer reads "Six scan roots x DesignDoc coverage" with per-root counts that predate BOTH drydocs_api and drydocs_docmeta (tests 85, drydocs 41, drydocs_core 35, lineage 12, remediation 7, deepdoc 3). It is restated on the EIGHT package roots U18 established, the per-root doc coverage is RE-MEASURED rather than re-typed (venue named per J18), and every other "six scan roots" phrase in the same document is swept with it -- the Phase 1 mandate is a second instance, and the U9 scope note already says the snapshot is the whole repo tree. The item also DECIDES, and records, whether a per-root count belongs in prose at all: if it stays, it comes under tests/unit/test_code_graph_review_plan.py the way U18 anchored the typed scope on pyproject's packages; if it goes, the document says where the number is derived instead. A third hand-typed root list in one document is the argument for deriving, and this item is where that argument is settled rather than restated.
notes: |
  Groomed 2026-08-11 from Idea-97, which U18's own close note inboxed rather than swept in -- same disease, one table over. U18 widened the A1-A6 metric scope to eight roots and guarded the typed list against pyproject.toml, but that guard is anchored on the $packages literal while this unit hard-codes its own root list in prose, so it sat outside both the guard's reach and U18's stated surface. Left deliberately, filed honestly, and cheap to close.
  DONE 2026-08-21 (desktop). Phase 1 mandate restated on the EIGHT package roots (pyproject packages +
  tests, the U18 scope the test anchors); Phase 3 unit 3 no longer reads "Six scan roots x DesignDoc
  coverage (baseline: tests 85, drydocs 41, ...)". DECIDED AND RECORDED: the per-root count does NOT
  belong in prose -- derive it. Module count per root = the $packages-scoped graph query written into
  the unit (quoted with the venue and the U22 freshness line); doc coverage per root =
  tests/unit/test_runbook_coverage.py's per-module disposition, the only derived, guarded statement
  of which root has a living document. Stated honestly: the graph carries no DesignDoc->module edge
  (:Component.ref is free text), so "coverage" is not a graph query today and the unit says so.
  RE-MEASURED rather than re-typed (desktop MSI, neo4jtest / drydocs, graph FRESH per
  code-graph-freshness against drydocs-20260821-0340.json), .py modules, tombstone-filtered: tests 165,
  drydocs 57, drydocs_core 56, drydocs_lineage 17, drydocs_api 13, drydocs_remediation 11,
  drydocs_docmeta 10, drydocs_deepdoc 3 -- recorded HERE, not in the plan, which is the point. The
  remaining "six-root" phrases in the plan are dated baselines in the A3/A4 rows and the U9 scope note
  ("the snapshot is the WHOLE repo tree, not six scan roots") -- history, left as written. Guard:
  test_no_hand_typed_per_root_count_list_survives_in_the_plan.
