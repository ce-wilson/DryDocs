id: D8
epic: config-loaders
title: 'Bootstrap constraint-count guard: count SHOW CONSTRAINTS against constraints.cypher declarations
  and refuse loudly on mismatch (the supplement-chain guard pattern)'
type: task
module: drydocs-core
phase: 3
agent: main
model: sonnet
priority: p2
status: done
depends_on: []
inputs:
- drydocs_core/neo4j_client.py
- drydocs_core/schema/constraints.cypher
acceptance: |
  drydocs bootstrap counts applied constraints (SHOW CONSTRAINTS) against what constraints.cypher declares and FAILS LOUDLY on a mismatch, exactly the guard family _apply_supplement_chain already implements for supplements; the success message reports the count, never a bare "Constraints applied."; a test proves the mismatch path refuses.
notes: |
  Groomed 2026-07-28 from the same-day [bug] inbox line: apoc.cypher.runMany silently no-ops DDL, so pre-D5 bootstraps printed success while creating nothing (initial commit -> D5, reproduced in a throwaway container). The post-D5 split is sound (51/51) — this item is the missing structural check, the same "succeeds loudly, does nothing" family as G29/G30/Q8/L17. CLOSED 2026-07-28: NAMES carry the check, not counts (foreign/provisioning constraints never false-fail; a partial landing names its missing). declared_constraint_names in drydocs_core/schema/constraints.py (refuses anonymous + duplicate declarations), Neo4jClient.constraint_names, guard wired into bootstrap before the ontology seed; 7 tests in test_bootstrap_guard.py incl. the lands-nothing refusal; live-verified on the desktop container: "Constraints applied (51/51 declared present)."
