id: Q24
epic: docmeta
title: 'Execute the vendor-docs-entity-spine §E4 authorization: flip bmc-docs-controlm-utilities confirmed:
  true and run the FIRST live layer-1 load — DESKTOP-bound (the capture lives in its data root)'
type: task
module: drydocs-load
phase: 14
agent: main
model: sonnet
priority: p2
status: todo
depends_on: []
inputs:
- config/doc-source-registry.yaml
- config/gate-log.md
- drydocs/loaders/
- knowledge/upgrade-plans/vendor-docs-agent-navigation.md
acceptance: |
  The flips-are-follow-ups half of gate vendor-docs-entity-spine §E4 (SIGNED OFF 2026-08-22, 21/21 — config/gate-log.md): (1) config/doc-source-registry.yaml's bmc-docs-controlm-utilities row flips confirmed: false -> true, its comment citing the gate and date; (2) the first live layer-1 load runs ON THE DESKTOP (the capture is in its machine-local DRYDOCS_DATA_ROOT — this item is venue-BOUND, not venue-preferenced) via the Q13 convert+load pipeline, unmodified; (3) the close note carries the J18 venue tag and the loaded counts (expect 1016 :Document / 1017 TOC-derived rows; the one fragment-anchor node mints NO phantom Document), and rows_changed is NONZERO on first load and ZERO on the idempotence re-run (the Q13 delta-tail exists precisely to make this claim falsifiable); (4) drydocs docs-verify reports the corpus PRESENT via the registry graph_locator (corpus_id = bmc-docs-controlm-utilities — the Q13 defect-2 check running against a real graph for the first time). LAYER 1 ONLY: no entity nodes, no DESCRIBES/SEE_ALSO edges (Q25), and the two TOC-edge vocabulary entries (docs_in_section / docs_subsection_of) flip planned -> active WITH this load, supplement blocks landing in the same commit (the G55 no-active-without-supplement invariant).
notes: |
  Minted 2026-08-22 at the vendor-docs-entity-spine sign-off (gate-log terminus; same-day draft + sign — the Q14 close explains why one page carries both the entity-spine rulings and the layer-1 doc-graph confirmation: G32 Q6 left the doc-graph gate unsigned and no other item owned it). The gate AUTHORIZED the flip; this item PERFORMS it (K2/G55 flips-are-follow-ups). DESKTOP-BOUND per J18: the laptop has no payload (Q13 close), so a laptop session must not pull this item past the claim step. Q13's three close defects are the checklist for (3): the delta-tail ordering, the registry-driven corpus_id, the capture-scoped doc_id.
