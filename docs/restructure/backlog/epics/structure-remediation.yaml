id: structure-remediation
letter: G/S
title: Pre-UI structure remediation
order: 18
groom_log:
- date: '2026-07-25'
  note: |-
    ── Epic S — Pre-UI structure remediation (main; ADRs 0008/0009/0010) ──────
    Source: docs/reviews/architecture-structure-review-2026-07-25.md — a scored
    /architecture + /tech-debt pass over the tree, run because the console is being
    built and the API contract + graph property names harden once routes and
    fixtures depend on them.

    Grouped as its own epic rather than folded into G (component-topology) because
    these share ONE review document, ONE phased plan (its §5), and THREE ADRs whose
    acceptance gates the build items — the board should show that sequencing as a
    unit. Each item still carries its correct existing plan phase, so the roadmap
    strip is unaffected.

    Findings deliberately WITHOUT an item:
      F4  / F9 / F10-part  DONE 2026-07-25 (432ea43 boundary-guard fix, bbf29cf gitignore)
      F5  drydocs/ 4-component flat namespace — deferred to Phase C by ADR 0002-a-1;
          the review's §6 "what NOT to do" says explicitly not to reopen it mid-UI-build
      F15 two test roots — tests/ (pytest) and graph-tests/ (YAML acceptance runner)
          are two different mechanisms, not duplication; scored lowest, recorded only
      F11 depgraph-snapshot retention — a user call about audit history, re-inboxed
  attached_at: S1
- date: '2026-08-23'
  note: |-
    ── 2026-08-23 groom (laptop) — S13, S8's import-order residue ──────────────
    Not from the inbox: found while checking which Ready-to-pull items were safely
    dispatchable. All SIX per-domain command modules S8 split out on 2026-08-21 fail to
    import as the FIRST import of a fresh interpreter — each opens with `from drydocs
    import cli as _root`, so the root's body runs to its closing merge loop and reaches
    back for an `app` the still-importing module has not defined yet. `import drydocs.cli`
    works, and that is the ONE import CLAUDE.md's smoke test names, so nothing went red.
    The visible symptom is a FALSE RED: `pytest tests/unit/test_repo_paths.py` alone is
    4 failed / 45 passed while the full suite is green at 2328. p2, because the shipped
    CLI is unaffected — every other way in is not. The item requires the guard to run each
    import in its own SUBPROCESS, since an in-process check cannot fail once sys.modules
    is primed, which is the mechanism that hid it. S8 itself is not reversed: the module
    boundary stands, only the root's import-time reach-back is wrong.
  attached_at: S13
- date: '2026-08-26'
  note: |-
    Groomed 2026-08-26 from Idea-159. Four tests in test_repo_paths.py pass in the suite and
    fail when the file runs alone (a circular import between a CLI domain module and the
    composition root, hidden by import order). Reproduced at this groom on the desktop:
    4 failed, 45 passed. Pre-existing, not introduced by G79; filed under S because the S8
    CLI split produced the shape.
  attached_at: S15
- date: '2026-09-02'
  note: 'Groomed 2026-09-02 from Idea-241. S16 minted: the composition root discovers an optional consumer
    command module, so drydocs/cli.py stops being an evaluate collision and its manifest row can become
    canonical-producer. The COMPANY-side half of that idea (adopting the S8 split, re-homing 26 company-only
    verbs, dropping stale inline copies) is their port action and is deliberately not in the item; the
    process lesson is J73.'
