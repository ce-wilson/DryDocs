id: mind-map
letter: MM
title: Mind-map — graph-seeded investigation and the data-flow overview record
order: 26
groom_log:
- date: '2026-08-21'
  note: |-
    ── Epic MM — mind-map (deepdoc leaves the placeholder) — Phase 17 ───────────
    Born 2026-08-21 from Idea-153: a 48-frame production deep-dive (email → Control-M
    client → Jira → Bitbucket → Confluence → chat assistant) transcribed verbatim
    machine-local and synthesized mechanism-only in
    docs/design/deepdoc-data-flow-overview.md. User rulings at the groom: the epic is
    a research/retrieval stream, not data lineage (hence MM, not a DF letter); scope
    reaches integration into the unstructured knowledge graph with ontology-proposed
    labels, a reliability weight, SME input, and a provenance breadcrumb per finding;
    Fable owns the synthesis, the Opus "3-tool update plan" is an input (MM3/MM4).
    Sanitization rule for every tracked artifact: SEAL → APP_ID, Jira keys → <JIRA-nnnn>,
    the DevX project/space → Auto.
  attached_at: MM1
- date: '2026-08-28'
  note: |-
    ── 2026-08-28 groom (ui-workstream worktree) ───────────────────────────────
    MM11 acronym candidates as a new class on the MM3 extractor, carrying the sentence
    they were found in, landing in the mind-map state file (Idea-190). ONLY the
    extractor half was promoted: where a loaded acronym finally lands — graph nodes
    with a LOADER_REGISTRY row, or a proposal into the config glossary — is an
    unruled fork with different modules and different gate exposure, and it stays
    parked in IDEAS.md as a question for the user.
  attached_at: MM11
