id: component-topology
letter: G
title: Modular architecture / component topology
order: 6
groom_log:
- date: '2026-06-26'
  note: |-
    ── Epic G — Modular architecture / component topology (main + ADR 0002) ───
    Groomed 2026-06-26 from ADR 0002 follow-ups 1–6 (docs/decisions/0002-*.md), accepted
    via the SME gate (config/gate-log.md, 2026-06-26). Edition = Neo4j Enterprise (committed).
  attached_at: G1
- date: '2026-07-16'
  note: |-
    (G12/G13 groomed 2026-07-16 — the two pre-flip writer build blockers recorded by the
     2026-07-15 lineage vocab gate + resolved/refined by the 2026-07-16 cmdline-lineage-review.)
  attached_at: G12
- date: '2026-07-21'
  note: ── appended at the 2026-07-21 groom run (weekly-style inbox groom) ───────
  attached_at: G17
- date: '2026-07-21'
  note: |-
    ── rua-extract ingest phase (groomed 2026-07-21 from the company-side
       collector run + PuTTY readout; mechanism-only — real host/user values
       stay company-side / internal-local) ──────────────────────────────────
  attached_at: G18
- date: '2026-07-22'
  note: |-
    ── appended at the 2026-07-22 groom (Backstage catalog assessment T1–T8;
       source = docs/design/ui-exploration/backstage-catalog-assessment.md — the shallow clone was
       deleted the same day, the assessment doc is the durable record) ────────
  attached_at: G27
- date: '2026-07-26'
  note: |-
    ── appended at the 2026-07-26 groom run (docs-residency design session) ───
    The topology half of that session. G32 is the ruling; G31 is the prerequisite
    every corpus move depends on; G33 is independent and startable now.
  attached_at: G31
- date: '2026-07-28'
  note: |-
    ---- 2026-07-28 pm groom (epic close-out run): Snowflake data-catalog seam
    (G42-G44, from docs/generic-snowflake-data-catalog-ingestion-plan.md) +
    fresh 07-28 inbox findings (L18/L19/D8/J17). U5 executed in-run. ----
  attached_at: G42
- date: '2026-07-28'
  note: |-
    --- 2026-07-28 evening groom (post-rebase): the rua listing fallback (G45),
    --- the K4-comment scope fix (C20), the render-entry-point fold part 2
    --- (J20), and the agent-runtime alignment chores (R10). R3 gained the
    --- caller-identity merge in place.
  attached_at: G45
- date: '2026-07-31'
  note: |-
    ── Epic W — CDO ontology alignment (main / ontology-mapper) — Phase 2 ─────
    Groomed 2026-07-31 from the CDO capture (internal/cdo-reference/, 8abc05f)
    and its ALIGNMENT-PLAN.md: align producer ontology to the internal governance
    group's firmwide frameworks ON PLANNED ITEMS ONLY (user rule: don't add what
    we won't use). Every confirmation routes through the SME gate; the plan's
    explicit skip list is BINDING (Business Processes, mapping path expressions,
    SHACL artifacts, JDI producer-side, FIBO, Data Authority classes, telemetry,
    naming renames). The live-scrape [source] line stays parked in IDEAS.md —
    company-side connector work, not producer-actionable.
  attached_at: G58
- date: '2026-08-12'
  note: ── 2026-08-12 targeted groom (Idea-112 / Idea-113 / Idea-114) ──────────────
  attached_at: G92
- date: '2026-08-21'
  note: |-
    ── 2026-08-21 groom (laptop) — Idea-152's runtime substrate, filed here for want of
       its own epic ──────────────────────────────────────────────────────────────
    G104-G109 are the six-item chain Idea-152 asked for in writing: the ADR (G104,
    Proposed only — acceptance is the user's), then core settings + dictConfig (G105),
    `drydocs prune-logs` (G106), per-component LoaderRunLog (G107), the API audit line
    (G108) and the data-zone declaration (G109). No runtime-substrate epic exists and a
    groom cannot mint one, so the chain sits in component-topology / phase 6 — the epic
    that already owns cross-component structural rulings and already holds an ADR-drafting
    item (G53). A dedicated epic is PROPOSED TO THE USER; taking it moves `epic:`, not ids.
    G110 is unrelated: the residue of Idea-141's poetry-group review (verdict
    do-not-recommend), namely MODULE_MAP contradicting the boundary test and four unpinned
    agent-runtime dependencies.
  attached_at: G104
- date: '2026-08-26'
  note: |-
    Groomed 2026-08-26 from the IDEAS inbox (Ideas 171, 176, 175, 164, 169). Five items,
    four of them the deliberate residue of a finished item rather than new work: G111 the
    data-zone env-field gap that ADR 0014's log-root fix does not cover (Idea-171's residue,
    clauses 1/3/4 having been ruled into the ADR on 2026-08-25); G112 the G92 scope chain
    widened to the G97 artifact pass, with the INVOCATION half recorded as a gate question
    and deliberately not built (Idea-176); G113 the consumer half of G56, where one shared
    export on N hosts is currently flagged as N unconfirmed identities (Idea-175); G114 the
    superseded-database guard widened to the two packages the 2026-08-24 fold sweep actually
    found stale names in (Idea-164); G115 the per-data-center Control-M extraction bind and
    run recipe (Idea-169, user direction 2026-08-24), whose first multi-DC LOAD is gated on
    the new P6 identity probe.
  attached_at: G111
- date: '2026-08-30'
  note: >-
    2026-08-30 weekly groom: G127-G131 minted. G128 and G129 are both G125 follow-ups the build recorded rather than hid - the resolver migration it declined, and the environment doctor its declared list finally makes possible.
  attached_at: G127
