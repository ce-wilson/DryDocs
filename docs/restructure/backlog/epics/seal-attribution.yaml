id: seal-attribution
letter: G/K
title: SEAL application attribution
order: 10
groom_log:
- date: '2026-07-01'
  note: |-
    ── Epic K — SEAL application attribution (seal_app_ref) — Phase 9 ────────
    Groomed 2026-07-01 from the company reconciliation answers (SME-run prompt).
    KEY CORRECTION: the edge is SPEC-LEVEL on BOTH sides — company FR-NS-013/UC-NS-005
    docs read ACTIVE but no loader/vocab/gate exists there either. job.APPLICATION is
    NOT reliable SEAL identity; the feed is STG_APP_FACT semantic facts (C3/C4 variable
    normalization: fact types SEAL/FID/APP_NAME/ALIAS). Company gate sequence adopted:
    vocabulary registration -> match-policy gate -> loader activation -> verify thresholds.
  attached_at: K1
- date: '2026-07-08'
  note: |-
    ── Epic L — Documentation infrastructure (main) — Phase 10 ───────────────
    Groomed 2026-07-08 from the deterministic-documentation design conversation.
    THREE senses of "deterministic", unified by ONE contract: a canonical per-doc-type
    OUTLINE (docs/design/templates/*.outline.yaml) whose stable section/requirement
    ANCHORS are the id namespace the render pipeline emits, the traceability matrix
    keys on, and HITL markup (digital save-button OR scanned pen notes) re-attaches to.
    Source of truth = the .md; HTML/PDF are DERIVED (kills today's three-hand-maintained-
    copies drift on the Control-M TDD). Sequence (updated 2026-07-08 by the user): TDD
    (L1) -> render/feedback pipeline (L3-L7) -> Runbook (L8, the capstone that proves the
    system generalizes to a 2nd doc type); BRD is a LATER phase (inbox-parked, WIP upstream).
    NOT the planned drydocs-docmeta component: that INGESTS docs into the graph; Epic L
    AUTHORS ours. They meet only at L7 (docs-as-context, through the gate).
  attached_at: K9
- date: '2026-08-26'
  note: |-
    Groomed 2026-08-26 from Idea-160. G79 wired the team-roles loader into the team chain, so
    a real source-mode refresh resolves three files from the PAT people-report landing zone
    while the G82 projection emits two. K27 picks one disposition with a reason — extend the
    projection, or declare the file a hand-drop and say so where the operator will look.
  attached_at: K27
- date: '2026-08-27'
  note: |-
    Groomed 2026-08-27 from Idea-177. K28 corrects fid_census.py's module docstring, which
    still opens "Gate fid-identity-and-scope is DRAFTED AND UNSIGNED" although the gate signed
    33/33 on 2026-08-19 (K17). The docstring's ORDERING argument — the census runs before the
    gate, doc 09 section D2 — stays as history rather than being deleted with the stale state.
  attached_at: K28
- date: '2026-08-28'
  note: |-
    Groomed 2026-08-28 from Idea-182. K30 is G82's return leg: the projection G82 built pins
    its header map to believed spellings, and its test fixture writes the SAME beliefs, so
    code and test agree and can both be wrong against the real export. seal_ids is mapped but
    not required, so a case-different column degrades to empty and a run that prints success
    writes zero dev-team-to-application edges. Two halves are producer-verifiable today (make
    every mapped field's absence loud; resolve jira_board_id, which maps a header this report
    does not carry); the re-pin waits on the landed header list and the item says blocked is a
    legitimate outcome rather than guessing the spellings.
  attached_at: K30
