id: web-console
letter: G/O
title: Web console / graph visualization
order: 14
groom_log:
- date: '2026-07-09'
  note: |-
    ── Epic O — Web console / graph visualization (main) — Phase 12 ───────────
    Groomed 2026-07-09 (user decision: promote to a module + phase — the web/ front-end
    design pass is in flight on branches). NEW module drydocs-web (JS/TS under web/, not a
    Python package, so absent from MODULE_MAP.md/test_module_boundary.py). This is the
    browser surface over the graph — distinct from every Python component.
  attached_at: O1
- date: '2026-07-14'
  note: (O3–O6 groomed 2026-07-14 from ADR 0005's Action items, at the design-pass merge.)
  attached_at: O3
- date: '2026-07-17'
  note: |-
    O8–O11: the site-plan build-out (docs/design/ui-exploration/site-plan.md, groomed 2026-07-17 from
    the IDEAS UI DECISION line — single-track ReUI, Salt dropped). Module choice:
    the EXISTING drydocs-web / drydocs-api registry entries, not a new drydocs-ui
    module (site-plan §6's suggested id superseded by this groom).
  attached_at: O8
- date: '2026-07-21'
  note: |-
    O15–O22: the site-plan P3 extension (groomed 2026-07-21 from chat + the new
    docs/design/ui-exploration corpus: DryDocs_UI_Development_Specs.md, gemini-wire-frame.md, icons.md,
    layout-anatomy-checklist.md now on disk). Covers every remaining console
    surface buildable WITHOUT a gate; the chain terminates at O20 — the HITL gate
    deciding the UI write-surface boundary. Until O20 signs off, every console
    surface is read-only / artifact-only (the O13 doctrine).
  attached_at: O15
- date: '2026-07-23'
  note: |-
    Groomed 2026-07-23 from docs/design/ui-exploration/two-track-ui-plan.md Track 1 (T1-5/T1-6/T1-7/
    T1-8) — the "UI acceleration session" groom candidates, misfiled below the
    audit trail in IDEAS.md and folded back into the inbox groom.
  attached_at: O29
- date: '2026-08-05'
  note: ── appended at the 2026-08-05 groom (SME direction, in-chat during G35) ────
  attached_at: O44
- date: '2026-08-06'
  note: |-
    ── appended at the 2026-08-06 groom (Idea-78 — the SME Context-Intake page plan,
       docs/design/ui-exploration/sme-intake-page-plan.md; user direction in-session. The console surface
       for the Q10 unstructured email corpus: seven page sections, NO graph writes
       anywhere — corpus load stays Q10's (blocked G32→G31), the assignment edge is
       gate-registered planned per Q10's own acceptance.) ──────────────────────
  attached_at: O45
- date: '2026-08-12'
  note: ── 2026-08-12 remediation-fix-module groom (xml_io epic; plan reactive-finding-shannon) ──
  attached_at: G85
- date: '2026-08-21'
  note: |-
    ── 2026-08-21 groom (laptop) — the four console defects captured 2026-08-20 ──
    O64 Ask loses the last completed turn on navigation (Idea-143), O65 dark mode never
    reaches the shared rail/Source controls (Idea-145, filed p2 against a proposed High —
    presentation, not correctness), O66 /ownership renders relationship names behind the
    nodes (Idea-147), O67 ModuleIcon's switch has no exhaustiveness guard (Idea-149, the
    trap its own comment already documented). Ideas 143-147 were minted on the
    feat/ui-workstream branch, which is why 147 collided once and Idea-148 carries the
    re-issue note.
  attached_at: O64
- date: '2026-08-26'
  note: |-
    Groomed 2026-08-26 from Idea-172 (SME direction 2026-08-25). The log-estate panel lands
    on the O12 admin page by the SME's own placement rather than in a new epic. Depends on
    G111 as a hard dependency, not a courtesy: without it the panel would show the wrong
    directory for the very kind the SME asked to see. The debug-tier Cypher DISPLAY question
    is an SME review, parked as an inbox question at this groom; the panel may report that
    kind's size and retention and must not render its contents.
  attached_at: O68
- date: '2026-08-28'
  note: |-
    ── 2026-08-28 groom (ui-workstream worktree) — the O66 aftermath ───────────
    O77 /ownership now paints the relationship chips ON TOP of the nodes, so the node
    names became the unreadable half (Idea-188) — filed as a NEW item rather than
    reopening the done and verified O66, whose acceptance genuinely holds; the missing
    clause (node names readable too, both themes) travels in O77 instead. O78 MiniDag
    never adopted RelEdge, so five routes still draw labels behind the nodes
    (Idea-189), deliberately ordered AFTER O77 so it does not inherit the inverted
    occlusion on all five at once.
  attached_at: O77
