# docs/decisions — architecture decision records (ADRs)

The durable record of *why the architecture is what it is*. Each ADR states the
decision, the options considered, and — deliberately kept discoverable — **the
alternatives that were rejected**, so they are not re-litigated in a future
session that never opens the ADR body.

| ADR | Decision |
|---|---|
| [0001 — ontology base scope](0001-ontology-base-scope.md) | PROV-O/ORG/DPROD-grounded ontology base; what the graph's edges are allowed to mean |
| [0002 — component & database topology](0002-component-database-topology.md) | Monorepo of components on `drydocs-core`; Enterprise multi-DB (`drydocs` ground truth vs `drydocs_context` uncertain) joined by a composite |
| [0002-a — drydocs-core extraction plan](0002-a-drydocs-core-extraction-plan.md) | Staged core/component split behind the `drydocs_core` shim; boundary enforced by `tests/unit/test_module_boundary.py` |
| [0002-b — spinoff rebase checklist](0002-b-spinoff-rebase-checklist.md) | `controlm-spinoff` → `drydocs-remediation` re-home flow |
| [0002-c — depgraph lineage re-home](0002-c-depgraph-lineage-rehome.md) | `depgraph@feat/controlm-lineage` absorbed as `drydocs-lineage`; one parser in core (delta fold = backlog G8, done) |
| [0003 — "Application" naming disambiguation](0003-application-naming-disambiguation.md) | Source terms verbatim on source nodes; `:Application` stays the SEAL-keyed canon; reconcile by relationship; bind renderers substitute in code regions only (comments/strings verbatim); BMC labels take the `ControlM` prefix (`JobFolder` → `ControlMFolder`) |
| [0004 — "Vendor" means the brand](0004-software-registry-vendor-terminology.md) | Software registry: `:Vendor` (org:Organization) = brand only; `:SoftwareProduct.role` absorbs the Tier-1/Tier-2 split; `MADE_BY` → prov:wasAttributedTo, `USES_SOFTWARE` local; `vendor-bmc` tooling id → `bmc-docs`; icons stay Brands |
| [0005 — browser ↔ Neo4j access path](0005-browser-neo4j-access-path.md) | Thin API is the deployment shape (server-side creds, read-only + DB routing, SSO home, layer-4 projections); bolt-from-browser survives only as a dev-mode adapter behind one `GraphAccess` seam in `web/src/lib/` |
| [0006 — docmeta component & doc-graph](0006-docmeta-component-and-doc-graph.md) | docmeta is its own component (deepdoc consumes, never folds in); new `dddocs` DB (live `dd*` convention); vocabulary reconciled — `HAS_CHUNK` superseded by the active lexical shape, `HAS_DOCUMENT`/`GOVERNED_BY` planned, chunk-DESCRIBES gate-deferred; curation ladder → HITL gate; P0-verdict adoptions |
| [0007 — agentic Q&A architecture](0007-agentic-qa-architecture.md) | ACCEPTED 2026-07-23 (R1). Tiered read-only agent (QuerySpec router → schema-grounded text2cypher → bounded graph-of-thoughts loop) in `agents/`; every executed Cypher exposed via ephemeral session specs; per-run question/context/memory/token telemetry (`:AgentRun` + JSONL ledger); stale-source rescrape queue; enhance branch writes task-scoped context only — O20 stands |
| [0008 — orchestration module boundary](0008-orchestration-module-boundary.md) | ACCEPTED 2026-08-01 (S1), as proposed. Add `drydocs_core/orchestration/` as a parent and move `controlm/` under it — do **not** rename `controlm/`, ~1,100 of its 1,725 lines are AutoEdit semantics; lift the neutral shell/path parsers to the parent; `config/crosswalks/` gains a runtime consumer that raises on `fidelity: no-equivalent`; `:ControlM*` graph labels unchanged (ADR 0003 rule 4) |
| [0009 — configuration substrate](0009-configuration-substrate.md) | ACCEPTED 2026-08-01 (S1), as proposed. Git YAML stays the source of truth (the HITL diff, the commit-range port, the classification tests, and deterministic renders all read files); `var/mapping.db` stays the derived read model and widens; monolith YAML splits by domain + JSON Schema; UI writes go to a `draft` table and are promoted **as a YAML diff onto a branch**, never written to the committed file |
| [0010 — internal source terms](0010-internal-source-term-abstraction.md) | ACCEPTED-WITH-AMENDMENTS 2026-08-01 (S1) — body edited to the `business-application-identity` gate ruling (2026-07-27, 22/22). The rule is **TWO-PART**: identity takes a neutral name (`:BusinessApplication.app_id` replaces `seal_id` as the canonical key), evidence keeps the source's own term (`ATTRIBUTION_TIERS 'SEAL'`, `match_method: 'seal'` STAY — they record what another system wrote). `id_authority` WITHDRAWN (one permanent registry; the authority is declared once in the taxonomy file), `app_urn` DEFERRED with a named trigger. Cutover is 8 key-bearing sites in ONE atomic change — constraints ignore nulls, so a partial flip silently doubles the node. Build is S3; PAT needs no action (it never leaked into code) |
| [0011 — single-database contingency](0011-single-database-contingency.md) | PLANNED 2026-08-10 (G53) — the fold-down plan if Enterprise multi-DB becomes unavailable (NOT triggered by the laptop eval expiry). 0002 stays ACCEPTED; this is the shelf copy: trust boundary re-keyed from database to `:Uncertain` label + mandatory trust property + three guards (physics -> discipline, loss stated plainly); ddschema exemplars fold by moving real labels to `describes_label`; ddall consumers enumerated and re-keyed; dev-environment.yaml is the single switch point. Execution is SME-gated |

| [0012 — data-named load surface](0012-data-named-load-surface.md) | ACCEPTED 2026-08-11 (GN1) — company jargon (`seal`, `pat`, `m1`/`m3`) entered a repo meant to be generic, so this is the standalone-generalization goal, not cosmetics. THREE AXES, currently collapsed into one `LoadStep`: the command names the **subject**, the source registry entry carries **cadence** and **acquisition**, and a profile is DERIVED by filtering cadence — which is what makes "the source dictates the refresh rate" true in code rather than in a comment. Three bands (prepare / **load** / verify); only load is source-keyed. SaaS tools get NO exemption — the subject names the data (`load-cmdb`), the source id names the tool, on the SME's own acquisition-may-change argument applied one level up. Verbs are a PUBLIC CONTRACT: new names ship with deprecated aliases for one port cycle (precedent: the supplement legacy-verb aliasing). **Clause 5 is the one GN2 would otherwise learn the hard way** — the three layers do not move together: CLI verbs and loader keys are producer-canonical, but **source registry ids are COMPANY-CANONICAL under registry v2 + the D4 retired-id list, so renaming `pat:*` is a cross-repo reconciliation, not an edit.** Explicitly does NOT fix the silent-skip bug (G78, p0) or split any chain (G79, lands under the OLD names so the port diff stays reviewable). Execution is GN2, frozen and ported as one unit |
| [0013 — backlog sharding](0013-backlog-sharding.md) | ACCEPTED 2026-08-20 (Y1, decided with the user). `backlog.yaml` (21k lines, 466 items) is the only claim channel between two machines and two repos, and the 2026-08-04 triple rebase conflict was entirely in its stored roll-up block. FIVE MECHANISMS RULED: (1) one **standalone mapping per item**, **flat** `backlog/items/<id>.yaml` — flat on graph grounds: `epic:` is a field and Y4 mints `IN_EPIC` from it, a per-epic path would duplicate the fact; `plan`/`modules` get their own files, `updated:` is dropped for git; (2) the 57 epic header comments become **data** in `backlog/epics/<epic>.yaml` (`groom_log`), renderable and a real `:Epic`; (3) `summary`/`next_ready` are **deleted from storage** and derived by the board — the two roll-up guards invert to "no stored roll-up"; (4) a claim is a one-file status flip; the residual same-item race is one small git conflict resolved by "status never regresses"; (5) splitter + S5 deep-equality proof + tombstone, and **the splitter ports as code: each side shards its own per-entry-unioned monolith** at the first post-Y2 port, never receives the other's tree; after that the manifest row is per-file. Execution is Y2, which holds until the in-flight port's PORT-REPORT is reviewed |
| [0014 — runtime substrate](0014-runtime-substrate.md) | **ACCEPTED 2026-08-25 (chad.wilson) WITH FOUR AMENDMENTS.** Logs, log level, retention and the data root become one per-machine settings group. RECONCILED WITH ADR 0009 as an exception it already permits, not an amendment: rule 1 scopes git-text source-of-truth to "anything an SME gates, a port carries, or a classification test guards", and a log directory is none of the three — `PORT-MANIFEST.yaml` marks the sibling `dev-environment.yaml` `canonical-company` precisely so local paths never cross, and `.env` is gitignored with `!.env.example` re-included as the template. THE GOVERNING PRINCIPLE the SME ruled on is broader than the question it answered — *everything configurable, nothing hardcoded* — and it has two halves that do not compete: nothing is buried in a Python literal, and WHERE a declaration lives depends on whether the value is a per-machine operational fact (env/`.env`) or a domain fact (committed YAML, ADR 0009). THE FOUR AMENDMENTS: (1) the settings group is per-KIND in `config/log-kinds.yaml`, not one global set — `kind` was three disagreeing hardcoded literals, so nothing could be configured per kind while no declaration said what the kinds are; (3) the naming rule DERIVES from that declaration (`<kind>.<name>.<stamp>.<ext>`, stamp granularity from `rotation`, ext from `format`) and the ledger exception is WITHDRAWN rather than defended — the drafted rule matched 5 of 86 real files, so the ledger was never the one exception; (4) `prune-logs` reads retention FROM the declaration, not from flags or literals; (5) the `drydocs_api` deferral is resolved OUT of scope — it is the web console's backend and has no batches — and (6) the audit line is widened to routes that WRITE, not only routes that execute Cypher, so `intake.py` is not the one filesystem-touching surface with no trail. Clause 2 as drafted; clause 7's forward list corrected against G109, which had already executed it. Executed by G105-G108; G109 done 2026-08-24 |
| [0015 — Team Edition template](0015-team-edition-template.md) | PROPOSED 2026-08-27 (not yet gated). Team Edition = copier-templated per-team instances (`auto`, `homelending`, `creditcards`, ...) generated from a cherry-picked base along the MODULE_MAP boundary; tenancy is an alias + a URN segment, never a tenant-aware module (instance DBs stay `drydocs` + `ddschema`; one `ddestate` composite federates); PORT-MANIFEST dispositions become the template file classes (frozen `canonical-template` incl. ontology + guards, `instance-owned` scaffolded once, `derived` regenerated); the completeness ledger (generalized `docs_coverage`) is the spine, and reconstructed requirements are `:Uncertain` by construction, promotable only through the HITL gate. the agent layer (CLAUDE.md, module-matched skills, subagents, seeded instance memory overlay) ships in the same file classes, with GRAPH-NATIVE agent memory on the neo4j-labs/agent-memory three-tier plan (short-term / long-term POLE+O / reasoning traces; MCP seam; scope name gate-tested per 0002 Q8) — memory stays a draft buffer, promoted through the gate; plus a living sources-and-tooling-watch register (GraphRAG/agent space moves fast: every cherry-picked practice is cited with a watch trigger, fed into the doc-source registry + 0007 rescrape loop). Phase 0 = the URN shared-vs-owned gate, which blocks all code |
| [0016 — jpmc-reports corpus shape](0016-jpmc-reports-corpus-shape.md) | **PROPOSED 2026-08-27 (Q17)** — awaiting the user's ruling. The first-ever doc ingestion (:DataAsset slices seeding BusinessSegment context, pre-docmeta) vs the lexical Document→Chunk backbone every corpus since uses. The consumer that forced it: the 2026-08-08 business-layer experiment wanted GraphRAG retrieval and the SHAPE made it impossible regardless of database state. Option A (recommended): reshape through docmeta — locator becomes corpus_id-matched, confirmed stays false until a doc-graph gate signs, the 2025/2026 editions ride the re-ingest (desktop payload), and the corpus becomes the P4 end-to-end path's second candidate (External-PUBLIC: zero publish-boundary risk). Option B keeps the slices and the permanent inability to answer retrieval questions. The old ddcontext/Idea-49 provisioning precondition is recorded OVERTAKEN by the G32 fold. No corpus is re-ingested by the record. (0015 LANDED 2026-08-29 from wip/o69-o70-desktop; it was an in-flight desktop draft when this row was written.) |
| [0017 — source-binding substrate](0017-source-binding-substrate.md) | **PROPOSED 2026-08-29 (G124)** — awaiting the user's ruling. Half the location problem is solved: 15 `acquisition.mode: manual` datasets resolve through `drop_dir`. The other 15 are `automated` and bind to NOTHING — `locator.service: ~` with a comment, and nothing resolves a comment — so `landing-zones --check` runs clean over the rows it knows and is silent on the rest. FIVE RULINGS: (1) the three-part URN key (platform, name, environment) has DataHub's known ceiling and `[db]` is already the missing instance coordinate — record it, do NOT mint the axis, so the eventual fix is configuration and not an id migration; (2) the binding is one row per ORIGIN inherited by its datasets (OpenLineage's namespace/name split; ~6 rows for 15 datasets), and a landing zone is the same shape with a `file` namespace, so the two halves may never grow concepts each other cannot express; (3) committed YAML REFERENCES environment variables and never holds a value, through ONE expansion function with ONE error — which is what makes G81 (d)'s mandatory root implementable in one place instead of the seven that disagree today; (4) credentials are named connection PROFILES (the Purview shape, not OpenMetadata's derived paths) because one Oracle account reads eight `psgmgr` views; (5) the fresh-clone default — no credential file, no accounts, every login refused — is recorded as a PROPERTY, because it is what a convenience commit removes. RECONCILED with 0009 and 0014 by reusing the line 0014 already drew: the MAP is committed git text (an SME knows it, the port carries it, every row needs a `classification`), only the VALUES are per-machine. VENUE ARGUED, NOT ASSUMED: the review asked for this text inside 0014, but it was authored on a branch whose history lacks 0014's 2026-08-25 acceptance, and a source-to-connection binding is a domain fact where 0014 scopes itself to per-machine operational facts. Executed by G125 |
| [0018 — component identity is a declared map](0018-component-identity-declared-not-directory.md) | **PROPOSED 2026-09-02** — awaiting the user's ruling. Five components are name-lists inside one `drydocs/` package and ADR 0015's copier classifies by path: the declaration becomes an importable map in core (`component_map.py`) that the boundary test, `MODULE_MAP.md` (rendered) and the copier all read; `drydocs-port`/`PORT` joins `modules.yaml`; the five unowned top-level dirs get owners; routing docs render or carry a path guard. Subpackages only on a 0002-A trigger, never mid-apply. |

## Rejected alternatives worth not re-litigating (ADR 0002, "Options Considered")

Three roads deliberately not taken — each was rejected for a *structural* reason,
not taste; re-proposing one means arguing against the reason, not rediscovering it:

1. **Community single DB** (+ `reliability` property + `:Candidate` label
   namespace) — rejected because it **commingles trust**: uncertain and curated
   nodes share one transaction domain and one bad query promotes noise into
   ground truth. The trust boundary must be structural (separate DBs), not a
   property filter.
2. **One capability, two `--mode` flags** — rejected because a single process
   writing to either DB on a flag is exactly the commingling risk the multi-DB
   split removes; **a flag bug crosses the trust boundary**. Two components
   (`drydocs-lineage` → `drydocs`; `drydocs-deepdoc` → `drydocs_context`) make
   the boundary structural.
3. **Polyrepo** (one repo per component, core published as a dependency) —
   rejected *for now*: stronger isolation but heavy version/dependency
   management for a small team. Monorepo + `drydocs-core` gives DRY sharing with
   independent run/deploy. Revisit only if a component needs an independent
   release cadence.

(A fourth D1 variant — separate DBMS instances + app-side join — fell for
violating the "no app-side data layer" invariant; the Enterprise composite does
the join natively.)

Full context, trade-off analysis, and consequences: [ADR 0002](0002-component-database-topology.md).
