# drydocs-lineage — module review, 2026-09-08

reviewed_commit: a8a1c2f8
reviewed_branch: review/module-sweep (worktree review-sweep; origin/main already merged — the merge at the start of this firing was a no-op, main has not moved since slot 2 closed)
reviewed_port_base: port-base-20260905 — producer-side review
slot: 3 of 10 (slug `lineage`)
lenses: system-design, tech-debt (the standing sweep's two; architecture was a one-off on the web slot)
scope: the dotted prefixes of `COMPONENT_GROUPS['lineage']`, read from `drydocs_core/component_map.py`
  (J37, the importable object) at this firing — one prefix: `drydocs_lineage`
prior report: first pass

- **Reviewed at:** commit `a8a1c2f8` on `review/module-sweep`, port base `port-base-20260905`; venue MSI. *Absent here reads as not-yet-ported, not as broken (docs/style/review-provenance.md).*

Opened before any module code was read (section 4 step 2): this file is the durable
memory for the firing, so a session that compacts mid-review resumes here rather than
starting again. Sections below are filled in order and each is committed as it closes.

## Measurements

Taken before any module code was read.

| measure | value |
|---|---|
| first-party Python in scope | **8,086 lines**, 19 files (plan table said 6,984 on 2026-09-05 — **+1,102 in three days, +16%**) |
| shape | 6 top-level modules + `extractors/` (11 extractors + `__init__`) + `collect/` |
| largest unit | `writer.py` — **1,277 lines, 16% of the module in one file** |
| next largest | `extractors/controlm_inventory.py` 863 · `rua_inventory.py` 728 · `dpl_mac.py` 479 |
| `collect/` | not Python: 2 `.sh`, 1 `.conf`, 2 `.md` — collection runs outside the package |
| tests naming the module | **36 files** (unit + integration) |
| scoped suite | **532 passed, 1 skipped**, 7 deselected, 43s |
| `ruff check drydocs_lineage/` | clean |

**Growth is the first thing worth noting.** The sweep plan sized this slot at 6,984 lines
on 2026-09-05 and it is 8,086 today — the plan's own advice is that the table is a sizing
aid and to re-measure when a slot feels wrong, and at +16% in three days this one is
moving faster than `core` did (+893 in two days, slot 1's note). Not a finding; a reason
the slot ordering may want revisiting at slot 10.

**One measurement correction, recorded because it would mislead a later firing.** A first
pass counted "72 test files naming the module" and a scoped `pytest` over that list
collected **nothing** — the list was half `__pycache__/*.pyc`. The real count is **36**
test files and the suite is green. A skip count or file count taken from a `grep -rl`
over `tests/` must exclude `__pycache__` or it silently doubles.

## Lens 1 — system design

**This module's write boundary is the strongest read so far in the sweep**, and its one
consequential gap is not in what it writes — it is a declared state that nothing can
reach, and a counter that reports that state as a real zero.

### L1-1 — `dns-resolved` is a counted tier with no writer, so its metric is permanently 0

The server-resolution pass declares four outcomes for joining a Control-M node name to
an inventory `:Server` (`drydocs/loaders/server_resolution.py:10-18`): **T1 exact**,
**T2 normalized**, **T3 `dns-resolved`** and **UNMATCHED**. Its summary query counts all
of them, including:

```
sum(CASE WHEN 'dns-resolved' IN tiers THEN 1 ELSE 0 END)  AS matched_dns_resolved
```
(`server_resolution.py:54`)

T3 is documented as deliberately not built in that file — *"NOT BUILT here: the Z4
nslookup collector feeds the same edge + evidence shape from its canned-transcript
evidence file"* (`:16-17`). **The Z4 half exists and is closed.**
`drydocs_lineage/extractors/lb_resolution.py` is 396 lines, `Z4` is `status: done`
(2026-09-05), and it has a test (`tests/unit/test_lineage_lb_resolution.py`) and a shell
collector (`collect/lb_resolve.sh`).

**But nothing consumes it.** Verified three ways on this tree:

| check | result |
|---|---|
| production importers of `lb_resolution` | **0** (`source_scan.imported_modules` over `drydocs_lineage/`, `drydocs/`, `drydocs_core/`) |
| exported from `extractors/__init__.py` | **no** |
| in the `staging.py` extract chain | **no** |

Its sibling in the same "shell collects, Python parses" pattern is wired both ways:
`rua_inventory` is package-exported AND consumed by `archival.py`. So the inconsistency
is internal to the module's own convention, not a missing convention.

**Consequence.** `matched_dns_resolved` is structurally always zero, and it sits in a
summary beside `matched_exact` and `matched_normalized`, which are real. A reader sees
"0 resolved by DNS" and reads it as *no load-balancer aliases resolved* — the honest
statement is *this tier has no writer*. Meanwhile the jobs behind an LB alias stay
UNMATCHED and cannot be placed on a map, which is verbatim the gap
`lb_resolution.py`'s own docstring says it fills: the gate declared T3 *"for exactly
this and left unbuilt; this module is its evidence half."* The evidence half shipped;
the consuming half was never itemized. **Z3 and Z4 are both `done`, so no open item
covers it.**

**Cheapest correction:** one item to wire the Z4 evidence into the resolution pass —
either the pass reads the evidence file, or a `lineage-` verb emits the
`RESOLVES_TO_SERVER {match_tier:'dns-resolved'}` edges the shape already defines. Until
then the honest interim is one line: have the summary distinguish a tier with no writer
from a tier with no matches, so the zero stops asserting something false.

### L1-2 — what is genuinely strong, recorded so a later firing does not re-audit it

**The write boundary refuses rather than degrades**, which is the opposite of the
failure mode found in slots 2 and 9. `writer.py` states five contracts and enforces each
(`:1-35`): curated-only (only `confirmed` rels are written, and every one must already
exist in the graph), canonical identity (a `folder/job_name` fallback id is REFUSED,
`:171-177`), a `TrustBoundaryError` on trust violation, a `GateBoundVocabularyError` for
any label not `active`, and — the part worth copying elsewhere — **`plan_curated` is
PURE and computes exactly what a live load WOULD refuse, without a database** (`:336`),
so the plan print and the load cannot disagree.

**`archival.py` is the best epistemic writing in the sweep so far.** Because its output
drives deletion, it makes absence structurally unsayable: *"no axis proves absence"* —
referenced / present-on-server / registry-active are positive-only observations, every
"absent" statement reads as "not observed by feed X", a metadata-only run states its own
body-copy coverage in its own output, and the misdeployment finding is SUPPRESSED AND
COUNTED under unknown mount scope rather than emitted on a guess (`:1-26`). That is the
product thesis implemented at the one place where getting it wrong deletes live code.

**The module states its own past gap.** `staging.py:5-9` records that "nine extractors
and a curated writer existed for weeks with no verb that ran the chain and no verb that
loaded it" — LIN1 built the missing verbs. L1-1 is that same shape recurring one layer
out, which is worth noting: the module has already been bitten by *built but unreachable*
once and fixed it, and the next instance arrived through a different door.

## Lens 2 — technical debt

Instruments first. **This module carries essentially no conventional debt** — the
cleanest counts in the sweep so far — so the one finding here is a contract implemented
twice rather than anything the linters can see.

| hatch | count |
|---|---|
| `# type: ignore` | **0** |
| `# noqa` | **0** |
| `cast(` | **0** |
| `# pragma: no cover` | **0** |
| `TODO` / `FIXME` / `XXX` | **0** |
| `: Any` | 4 (all in `writer.py`) |

`ruff check drydocs_lineage/` passes; the scoped suite is 532 passed / 1 skipped across
36 test files. For comparison, slot 2 (`load`) carried 6 `type: ignore` and 9
`pragma: no cover` over roughly twice the lines, and slot 1 (`core`) was also reported
as carrying very little. On this axis `lineage` is the cleanest slot measured.

### L2-1 — the bundle-envelope contract exists twice, and the copies have already diverged

Two extractors implement the same "shell collects a bundle, Python parses its provenance
envelope" pattern, and Z4's own item body says it was built **"on the rua precedent"** —
so the precedent was copied rather than extracted. The copies do not agree about the
thing the envelope is for.

- `extractors/lb_resolution.py` reads the envelope (`read_meta`, `:291`) and
  **validates the collector version**: `meta['schema']` is checked against
  `KNOWN_COLLECTOR_VERSIONS` (`:66`), and a newer collector appends a coverage note —
  *"collector schema X is newer than this parser knows … parsed on section presence, as
  designed"* (`:341-345`).
- `extractors/rua_inventory.py` reads the same shape (`_read_envelope`, `:361-378`) and
  **never checks the version**. It is careful in every other respect — a missing
  `meta.txt` increments `coverage.meta_missing`, a missing envelope field increments
  `coverage.meta_fields_missing` — but the schema value it parses is carried forward as
  a node property and compared against nothing. Its own docstring names `meta.txt` as
  *"the PROVENANCE ENVELOPE (schema version, …)"* (`:14`), so the version is known to
  matter.

**Consequence, and it is a provenance consequence rather than a maintenance one.** If
the rua collector's envelope advances — and `rua_inventory.sh` has already been through
a v3 mount-table addition, per `_read_mounts`'s "v3, OPTIONAL" — a newer bundle parses
**silently**, and its output carries exactly the same confidence as a version-matched
parse. The LB path, in the same package, in the same pattern, would have said so in its
coverage notes. For a component whose job is to say where a fact came from and how far
to trust it, "was this parser built for this bundle version" is precisely the question
that must not be silently assumed.

**Cheapest correction, in the order I would take them:** add the
`KNOWN_COLLECTOR_VERSIONS` check to `_read_envelope` — `RuaCoverage` already has the
notes channel to report it, so this is a few lines and no new concept. Extracting one
shared envelope reader for both is the better shape and the larger change; it is worth
doing at the THIRD collector, not the second, and this report is the record that the
second exists.

### What this lens did NOT assess, said plainly

`writer.py` is 1,277 lines, 16% of the module in one file, and I did not assess whether
that size is cohesion or accretion. Lens 1 read its contract surface and found five
enforced contracts and a pure planner, which is evidence of design rather than sprawl —
but "reads well at the top" is not a structural assessment, and calling it either way
from what I read would be an impression. A later firing that wants this should read the
file end to end rather than trust this paragraph.

## Ranked

Two findings, and they are the same defect at two depths: a thing that was built, and a
thing that was assumed, each invisible because the surface around it looks complete.

1. **L1-1 — `dns-resolved` is counted but has no writer.** `server_resolution.py:54`
   sums `matched_dns_resolved` beside two tiers that are real; nothing can ever produce
   it, because Z4's evidence half (`lb_resolution.py`, 396 lines, `done`) has zero
   production importers, is not exported from `extractors/__init__.py`, and is not in
   the staging chain — while its sibling `rua_inventory` is wired both ways. Ranked
   first because the zero is not neutral: it asserts "no load-balancer aliases resolved"
   when the truth is "this tier has no writer", and the jobs behind those aliases stay
   unplaceable — the exact gap the module says it fills. No open item covers it (Z3 and
   Z4 are both `done`).
2. **L2-1 — the envelope contract is implemented twice and already disagrees.**
   `lb_resolution` validates the collector version and notes a mismatch;
   `rua_inventory._read_envelope` never checks it. A newer rua bundle parses silently at
   full apparent confidence. Ranked second because it is latent — it costs nothing until
   the rua collector version moves, and then it costs trust silently.

**Not ranked:** the strengths in L1-2 (the refusing write boundary, `archival.py`'s
"no axis proves absence") and the near-zero debt counts in Lens 2. They are recorded so a
later firing does not re-audit them, not because anything is owed.

## Cross-links

**Between the lenses in this firing.** They agree here, unlike slot 2. Lens 2 found the
cleanest module measured and Lens 1 found the strongest write boundary — and both
findings are the same shape anyway: **something that exists but is not connected**
(L1-1), and **something connected but not checked** (L2-1). Neither is visible to a
linter, a test, or a reader of the file that contains it; both are only visible from the
seam between two files.

**To slot 2 (`load`, 2026-09-08, this morning).** Its L1-1 was *a scoped load is
unprovable* — the `:JobRun` records no scope, so a sample is indistinguishable from a
full load. This slot's L1-1 is the same disease in the reporting layer: a counter that
cannot distinguish "no matches" from "no writer". **Both are a number that means two
things and says only one.** Slot 2 could not see this and neither could slot 3 alone.

**To slot 9 (`web`, 2026-09-05).** Its top finding was a partial answer that reads like a
complete one. That is now three slots in a row — web, load, lineage — reaching the same
root cause down three different paths (a truncating console, an unrecorded load scope, an
unpopulated tier). **This is no longer a coincidence and should be slot 10's headline
question**, not a per-slot observation.

**To slot 1 (`core`, 2026-09-07).** Core owns the driver seam; this module's writer is
its most disciplined consumer — the refusal stack in `writer.py` is what a caller looks
like when it does not trust itself to degrade gracefully. If slot 10 wants a positive
model for the seam, it is here.

## Candidates for grooming

Four. None minted — the backlog pen is Lane A's and this firing holds neither it nor an id.

1. **Wire the Z4 evidence into the server-resolution pass.** `drydocs-load` +
   `drydocs-lineage` (the seam is the point). Either the pass reads the evidence file or
   a `lineage-` verb emits `RESOLVES_TO_SERVER {match_tier:'dns-resolved'}`. The shape is
   already defined by the signed `server-location-ontology` gate, so this is a build, not
   a ruling.
2. **Make the zero honest in the interim.** `drydocs-load`, p3, independent of 1 and much
   cheaper: have the resolution summary distinguish a tier with no writer from a tier
   with no matches, so `matched_dns_resolved: 0` stops asserting something false. Worth
   doing even if 1 is scheduled, because 1 is a build and this is a line.
3. **Validate the collector version in `rua_inventory._read_envelope`.**
   `drydocs-lineage`, p3. `RuaCoverage` already carries the notes channel; the pattern to
   copy is 60 lines away in `lb_resolution.py`.
4. **At the THIRD collector, extract one bundle-envelope reader.** `drydocs-lineage`.
   Explicitly NOT now — two copies is a precedent, three is a convention nobody wrote
   down. This report dates the second so the third knows what it is joining.

## Cross-links

*(step 6 — to the other lens in this firing, and to earlier slots)*

## Candidates for grooming

*(step 6)*
