# seams — cross-module review, 2026-09-08

reviewed_commit: 0272aa77
reviewed_branch: review/module-sweep (worktree review-sweep; origin/main already merged — it had not moved since slot 8 closed)
reviewed_port_base: port-base-20260908 — producer-side review
slot: 10 of 10 (slug `seams`) — **the final slot of the first cycle**
lenses: system-design, tech-debt (the standing sweep's two; architecture was a one-off on the web slot)
scope: per plan section 2, slot 10 is NOT a module — it is
  `tests/unit/test_module_boundary.py` and `drydocs_core/component_map.py` themselves,
  plus the three questions section 2 assigns it: whether the module boundary invariant
  actually holds, whether two modules disagree about a shared contract, and whether a
  finding recorded in one earlier slot has a twin somewhere else.
prior report: first pass
inputs: the nine module reports in this directory — `web-2026-09-05`, `core-2026-09-07`,
  and the seven from 2026-09-08 (`load`, `lineage`, `api`, `remediation`, `review-agents`,
  `port-docgen-docmeta`, `plan-deepdoc-libs`)

- **Reviewed at:** commit `0272aa77` on `review/module-sweep`, port base `port-base-20260908`; venue MSI. *Absent here reads as not-yet-ported, not as broken (docs/style/review-provenance.md).*

Opened before any code was read (section 4 step 2): this file is the durable memory for
the firing, so a session that compacts mid-review resumes here rather than starting again.

**What this slot must not do.** It is the last firing of the cycle and it has nine reports
in front of it, which makes restating them the easiest possible failure. The plan says
slot 10 reviews *what per-module slots structurally cannot see*. So this report earns its
place only where it says something none of the nine could: a boundary measured rather than
assumed, a contract two modules read differently, or a finding whose twin is somewhere its
own slot could not look. Anything that is just a summary belongs in a cover note, not a
review.

**Three carried items, already settled by earlier slots and NOT to be re-derived here:**
1. **The eighteen ADR 0018 shims** — slot 6 found the removal trigger fired; slots 7 and 8
   declined to re-report and recommended ONE cross-component grooming. This slot inherits
   that recommendation and does not re-count them.
2. **The `_git` carry-over** — slot 8 searched six packages and closed it: two correct
   sites outside `drydocs/port/`, no other outlier. Slot 7's candidate 2 narrows to its
   one site. Settled; not re-run.
3. **The plan table's sizing** — five of eight slots came in over (up to +49%). Slots 6
   and 7 both flagged it for this slot. It is a maintenance question about the plan, and
   it is answered here rather than in a module report.

## Measurements

*(step 3 — the boundary instruments, run rather than read; raw numbers as they arrive)*

## Lens 1 — system design

*(step 4)*

## Lens 2 — technical debt

*(step 5)*

## Ranked

*(step 6 — the ranked list; its presence is what marks this report complete)*

## Cross-links

*(step 6 — to the other lens in this firing, and to earlier slots)*

## Candidates for grooming

*(step 6)*
