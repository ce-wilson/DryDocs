# drydocs-load — module review, 2026-09-08

reviewed_commit: c2045ebe
reviewed_branch: review/module-sweep (worktree review-sweep; a merge of origin/main into that branch, taken at the start of this firing)
reviewed_port_base: port-base-20260905 — producer-side review
slot: 2 of 10 (slug `load`)
lenses: system-design, tech-debt (the standing sweep's two; architecture was a one-off on the web slot)
scope: the dotted prefixes of `COMPONENT_GROUPS['load']`, read from `drydocs_core/component_map.py`
  (J37, the importable object) at this firing — 18 prefixes:
  `drydocs.chain_inputs`, `drydocs.cli`, `drydocs.cli_consumer`, `drydocs.cli_docs`,
  `drydocs.cli_ingest`, `drydocs.cli_plan`, `drydocs.cli_schema`, `drydocs.cli_shared`,
  `drydocs.cli_variables`, `drydocs.cli_verify`, `drydocs.cmdline_staging`,
  `drydocs.code_graph_freshness`, `drydocs.docs_coverage`, `drydocs.loaders`,
  `drydocs.pat_projection`, `drydocs.seal_samples`, `drydocs.snapshots`, `drydocs.staging`
prior report: first pass

- **Reviewed at:** commit `c2045ebe` on `review/module-sweep`, port base `port-base-20260905`; venue MSI. *Absent here reads as not-yet-ported, not as broken (docs/style/review-provenance.md).*

Opened before any module code was read (section 4 step 2): this file is the durable
memory for the firing, so a session that compacts mid-review resumes here rather than
starting again. Sections below are filled in order and each is committed as it closes.

## Measurements

Taken before any module code was read.

| measure | value |
|---|---|
| first-party Python in scope | **15,080 lines**, 46 files (plan table said 14,538 on 2026-09-05 — +542 in three days) |
| `drydocs/` total (all prefixes) | 21,929 lines |
| largest unit | `loaders/` — 7,028 lines across 30 files, 47% of the slot |
| next largest | `cli_ingest.py` 1,131 · `cli.py` 1,087 · `cli_shared.py` 1,005 |
| non-Python assets in `loaders/` | 41 `.cypher`, 17 `.sql`, 2 `.md` — the query surface is data, not code |
| suite (this venue) | **3,573 passed, 41 skipped**, 18 deselected, 4m47s |
| `ruff check drydocs/` | clean |
| `tests/unit/` | 250 files, 105 of them naming a `drydocs.` module |

**Venue note (J18).** This worktree's venv omits the optional `api` group, so **19 of the
41 skips are `fastapi` absent** — against 15 skips in the `ui-workstream` worktree on the
same commit. None of the 19 is a `drydocs-load` test (fastapi is `drydocs-api`), so the
slot's own coverage is unaffected, but a later firing comparing skip counts across venues
should not read the difference as drift.

### One non-finding, recorded so it is not re-raised

`drydocs.cli_consumer` is declared in `COMPONENT_GROUPS['load']` and **no such module
exists in this tree** — `import drydocs.cli_consumer` raises `ModuleNotFoundError`. That
is correct and deliberate: S16 / RELAY-24 made `cli_consumer.py` the CONSUMER's optional
composition surface, shipped by a consumer tree (the company port, a Team Edition
instance) and never by the producer. `tests/unit/test_cli_import_order.py:44` adds it to
`CLI_MODULES` only `if importlib.util.find_spec(...) is not None`, and
`test_cli_registry.py` states the producer ships none. This is exactly the shape
`docs/style/review-provenance.md` warns about — absent here reads as not-yet-ported, not
as broken — and it cost one lookup to confirm, which is cheaper than the finding it would
otherwise have become.

## Lens 1 — system design

Headline: **the load path does not truncate, and cannot prove it didn't.** The two
halves are one story — every default is honest, and none of that honesty reaches the
graph a consumer reads.

### L1-1 — a scoped or sampled load writes a `:JobRun` indistinguishable from a full one

`BaseLoader._open_run` (`drydocs/loaders/base.py:588`) opens the run node with exactly
six properties — `run_id`, `kind='load'`, `loader`, `source`, `started_at`, `status` —
and `_close_run` (`:606`) adds row counts and a final status. **No scope reaches it.**

The scope exists and is well built: `_scope_binds` (`drydocs/cli_shared.py:925-932`)
returns `folder_filter`, `run_as`, `developer_sid`, `row_cap`, `data_center_filter`,
`data_center_code`. Those are SQL binds and stop there. So a run that extracted a
1,000-row `--row-cap` sample of a ~240k-row table produces a run node identical in
shape to one that carried the population; only the row count differs, and a row count
cannot distinguish "a sample of a big table" from "a small table".

**Consequence, and it is the product thesis rather than a correctness bug.** DryDocs
exists to answer *what runs, what it depends on* — and a support engineer reading the
graph cannot tell whether the absence of a job means it does not exist or means the
load was scoped past it. The one lens question this module must answer well ("can a
consumer tell whether an answer came from the graph, a fixture, or a constant") is
answerable for SOURCE (`run.source`, set by every template from `$source_label`) and
not for COMPLETENESS.

**Where the scope does survive:** `caller_stamp()` (`drydocs_core/run_log.py:95`)
returns `"drydocs " + " ".join(sys.argv[1:])` into the run-log file's `script:` header,
so the invocation IS recorded — in a file, under `DRYDOCS_LOGDIR`, on the machine that
ran it. That is a real audit trail and it is not the graph.

**Cheapest correction:** one `SET` in `_open_run` carrying the non-null scope binds.
Deliberately NOT the raw `caller_stamp()` string: argv can carry `--run-as <FID>`, and
CLAUDE.md §3 keeps a tenant FID out of a publishable surface, so the correction records
the bind NAMES that were non-null plus the `row_cap` VALUE — enough to answer "was this
scoped?" without putting an identity in the graph. If that trade is contentious it is a
gate question (the scope property is new vocabulary), not a code change.

### L1-2 — the compounding half: the only record of scope is best-effort

`_open_run_log` (`base.py:393-399`) states its contract plainly: *"Best-effort by
contract: an unwritable log directory downgrades to a WARNING and the load proceeds
without a log — the audit trail is never the reason a load fails."*

That is the right call in isolation — a load should not fail because a log directory is
read-only. But combined with L1-1 it closes the loop the wrong way: the graph does not
carry the scope, the log does, and the log is allowed to be absent. **A capped load on a
machine with an unwritable `DRYDOCS_LOGDIR` leaves no record of its scope anywhere.**
Neither half is a defect alone; the pair is.

This is the same shape as the `web/` slot's top finding — a partial answer that reads
like a complete one — arriving by a different route, which is an argument for slot 10.

### L1-3 — what is genuinely clean, recorded so a later firing does not re-audit it

- **No default truncation anywhere in the load path.** `--row-cap` defaults to `None`
  (`cli_shared.py:989`, `typer.Option(None, ...)`) and the SQL documents `NULL =
  unlimited` (`sql/controlm_variables.sql:42`). Of 41 `.cypher` files, **zero** cap
  their writes; the two `LIMIT`/cap greps that fired are a prose "accepted limit" in
  `code_snapshot.cypher:15` and the documented `:row_cap` bind. Sampling is opt-in,
  named, and never the default.
- **The two invocation surfaces are deliberate and documented in place.**
  `LOADER_REGISTRY` (22 entries, 14 modules, `cli_shared.py:123`) is the ad-hoc `load`
  surface; `cli_ingest.py` imports a further 11 loader modules directly as their own
  verbs. `cli_shared.py:348-358` states the split and the reachability rule, so a reader
  who asks "is the registry the list of loaders?" is answered at the site rather than by
  inference. Not a finding.
- **A duplicate-preserving read is defended by a comment that cites its evidence**
  (`sql/controlm_variables.sql:57-60`): duplicate `(job, variable-name)` definitions are
  legitimate and observed, so the extract does not dedupe. That is the repo's rule about
  comments carrying rulings, working.

### Two non-findings, recorded so the next firing does not spend the same lookups

Both cost real time this firing and both are correct as they stand.

1. **`drydocs.cli_consumer`** — declared in `COMPONENT_GROUPS['load']`, no module in this
   tree. Deliberate: S16/RELAY-24 made it the CONSUMER's composition surface, guarded
   conditionally by `find_spec` (`tests/unit/test_cli_import_order.py:44`). Absent here
   is not-yet-ported, not broken.
2. **`loaders/email_concerns.py`** (169 lines) — the only module in the package with no
   production importer. Not dead: it is the Q21 writer the signed `email-folder-assignment`
   gate authorized (`status: done`), and its intended caller is **Q22**, the SME
   email-assignment surface, `status: todo` and in a different module (`drydocs-web`).
   Its docstring cites the sign-off clause by clause and explains its own state — the
   best-documented file read this firing.

## Lens 2 — technical debt

**Core carries very little conventional debt; this slot carries less.** The instruments
were run first (Measurements above) and they come back clean: `ruff check drydocs/`
passes, the suite is green, and there is no suppressed-but-unenforced threshold in the
slot. What follows is what the numbers actually show.

### L2-1 — the escape hatches are few, and they cluster where the lens predicts

Counted across `drydocs/` (slot prefixes; the `review/`, `plan/` and `port/` hits other
greps return belong to slots 6-8 and are excluded):

| hatch | count |
|---|---|
| `cast(` | **0** |
| `# noqa` | 2 |
| `# type: ignore` | 6 |
| `# pragma: no cover` | 9 |
| `: Any` / `-> Any` | 41 / 16 |

**`Any` is not debt here.** 17 of the 41 are in `loaders/catalog.py`, and every one is a
pydantic v2 `field_validator` signature — `_ids(cls, v: Any) -> Any` receives the raw
pre-validation value by construction. That is the idiom, not a missing type.

**The `type: ignore` cluster is real and is one seam.** Three of six sit in
`loaders/app_identity.py` (94 lines) as `# type: ignore[misc]` on `self: BaseLoader`
(`:57`, `:62`, `:64`) — a mixin declaring the base it will be mixed into. The remaining
three are single, local and explained at the site (`base.py:355` `[union-attr]`,
`catalog.py:431` `[arg-type]`, `server_inventory.py:63` `[override]`). Six suppressions
across 15,080 lines, half of them one mixin seam, is a low number and a located one.

### L2-2 — the duplication is boilerplate around a correct helper, not copied logic

The recurring repo shape the lens names — *a correct helper exists, and the call sites
reimplement its body* — appears here in its milder form: the call sites reimplement the
WRAPPER, not the body.

`loaders/catalog.py` (486 lines) carries **14 `@field_validator` decorators**, of which
**7 are `_ids` and 4 are `_sparse_name`**, and every one of those 11 is a two-line
delegation to a helper defined once in the same file:

```
@field_validator(...)          # `_catalog_id`  is defined at :73
@classmethod                   # `_name_or_none` is defined at :114
def _ids(cls, v: Any) -> Any:
    return _catalog_id(v)
```

So the abstraction is already right and already shared; what repeats is four lines of
pydantic ceremony per model class, ~44 lines across seven row models. **Consequence is
maintenance, not correctness**: a change to how a catalog id is coerced is one edit
today, but a new row model is four more lines of ceremony that a reviewer must read to
confirm it delegates rather than diverges — and one that quietly did not delegate would
look identical at a glance.

**Cheapest correction:** a shared annotated alias
(`CatalogId = Annotated[str, BeforeValidator(_catalog_id)]`), which deletes the decorator
block per model and makes divergence impossible to express rather than merely unlikely.
This is a refactor with a test suite already covering it, not a redesign — but it is
also genuinely optional, and recording it as "known, costed, not urgent" is a legitimate
outcome.

### L2-3 — dead weight: none found

- **No unimported module in the slot.** All 29 non-`__init__` loader modules have a
  production importer once relative imports are counted (`loaders.X` as
  `source_scan.imported_modules` renders them), except `email_concerns`, which is Q21's
  completed writer awaiting its Q22 caller — staged, not stranded (see Lens 1).
- **No one-file directory, no misplaced file.** `loaders/` holds its 41 `.cypher` and 17
  `.sql` beside the code that runs them; the query surface is data and is stored as data.
- **`# pragma: no cover` (9) are not hiding a coverage hole** — the suite is green and
  the slot has 105 test files naming a `drydocs.` module against 46 source files.

### A measurement caveat worth stating

Coverage was NOT measured for this slot. `pytest --cov` was not run because the repo's
coverage threshold is declared on the WEB side (`web/package.json` `test:coverage`) and
no Python coverage gate exists to compare against; a number with no threshold behind it
would be an impression wearing a number's clothes. If a later firing wants this, the
honest first step is deciding what the threshold is, not running the tool.

## Ranked

Most consequential first. Only the first two would change what a consumer of this
product can trust; the third is maintenance.

1. **L1-1 + L1-2 together — a scoped load is unprovable.** The `:JobRun` carries no
   scope (`base.py:588`), the invocation survives only in a best-effort run-log file
   (`run_log.py:95`; `base.py:396-399` allows that file to be absent), so a `--row-cap`
   or `--folder` load can leave no record of its scope anywhere a graph consumer can
   reach. Ranked first because it is the product thesis rather than a bug: DryDocs
   answers "what runs", and this is the one question it cannot currently answer about
   its own answers. Fix is one `SET` of the non-null bind names plus the `row_cap`
   value — NOT raw argv, which can carry a tenant FID (CLAUDE.md §3).
2. **L1-2 alone, if 1 is deferred.** Even without the graph-side property, the pair is
   breakable from the other end: making the run-log write failure loud (still
   non-fatal) would mean "no log" stops being silent. Cheaper than 1 and strictly less
   good, so it is the fallback and not the plan.
3. **L2-2 — 11 duplicated validator wrappers in `catalog.py`.** ~44 lines of pydantic
   ceremony delegating to two helpers defined once in the same file. Maintenance only;
   an `Annotated[..., BeforeValidator(...)]` alias makes divergence inexpressible. Fine
   to leave, worth recording.

**Not ranked, deliberately:** the two non-findings (`cli_consumer`, `email_concerns`)
and the three clean results in L1-3 / L2-3. They are in the report so the next firing
does not re-derive them, not because anything is owed.

## Cross-links

**To Lens 2 in this firing.** The two lenses disagree in a way worth stating: Lens 2
finds a module in good health — 0 casts, 6 suppressions across 15,080 lines, no dead
weight — while Lens 1 finds its most consequential gap. That is not a contradiction, it
is the shape of this slot: **the debt is not in the code, it is in what the code declines
to record.** A tech-debt lens alone would have reported "clean" and been wrong about the
thing that matters.

**To slot 1 (`core`, 2026-09-07).** That report's headline was that core owns the driver
seam and it is the weakest of three. This slot is the driver seam's largest consumer:
every loader reaches the graph through it, and L1-1 lives exactly at the boundary where
`drydocs-load` decides what to hand `drydocs-core` to write. The two findings are the
same seam from either side.

**To slot 9 (`web`, 2026-09-05).** Its top finding was a partial answer that reads like a
complete one, in the console. L1-1 is that finding's twin in the load path, reached
independently: the console can only render what the loader recorded, so a graph that
cannot say "this was a sample" guarantees a console that cannot say it either. **Two
slots, two lenses, one root cause** — which is the case for slot 10 (`seams`) making
this its subject rather than either module owning it alone.

**Forward to slot 10.** The pattern to test there: whether "completeness is not recorded"
recurs at other module boundaries, or is specific to the load path.

## Candidates for grooming

Four, in the order a groomer would take them. None is minted here — the backlog pen is
Lane A's and this firing holds neither it nor an id.

1. **Record the load scope on the `:JobRun`.** `drydocs-load`. One `SET` in `_open_run`
   carrying the non-null scope-bind names and the `row_cap` value. Note in the body that
   raw `caller_stamp()` is the wrong payload (FID exposure) — that decision is the
   substance of the item, and a builder who misses it ships a publish-boundary defect.
   May need a gate: a new property on a PROV Activity is vocabulary.
2. **Make an absent run log visible.** `drydocs-load`. Keep the load non-fatal, raise the
   downgrade from WARNING to a summary line the operator sees at the end, so "this ran
   with no audit trail" is not discoverable only by noticing a missing file.
3. **Collapse `catalog.py`'s validator ceremony to annotated aliases.** `drydocs-load`,
   p3. ~44 lines, tests already cover it.
4. **Decide whether a Python coverage threshold exists.** Cross-module, and probably slot
   10's question rather than this slot's. Today `test:coverage` is web-only, so no Python
   slot can report coverage against anything.

## Cross-links

*(step 6 — to the other lens in this firing, and to earlier slots)*

## Candidates for grooming

*(step 6)*
