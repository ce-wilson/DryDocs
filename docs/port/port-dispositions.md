# Port dispositions — the apply order

<!-- GENERATED by scripts/render_port_dispositions.py — do not edit by hand.
     PORT-MANIFEST.yaml owns disposition (J68); this file only sorts by it. -->

Range `port-base-20260901..HEAD` — **8 changed paths** in **4 classes**.

Work the classes in the order below. Within a class the paths are alphabetical, not prioritized — the class carries the rule, the path does not.

| Class | Paths | Rule |
|---|---:|---|
| `canonical-producer` | 3 | take wholesale — `git checkout <producer-ref> -- <path>` |
| `never-port` | 1 | NO ACTION by rule — never crosses, in either direction |
| `per-entry` | 2 | MERGE BY ENTRY — read the row's `entry_rule` before touching the file |
| `derived` | 2 | REGENERATE LAST, never carry — the J43 rule |

## `canonical-producer` — 3 path(s)

**take wholesale — `git checkout <producer-ref> -- <path>`**

- **`.claude/**`** — 1 path(s)
  - `.claude/skills/reconcile-port/SKILL.md`
- **`PORT-MANIFEST.yaml`** — 1 path(s)
  - `PORT-MANIFEST.yaml`
- **`tests/unit/test_port_manifest.py`** — 1 path(s)
  - `tests/unit/test_port_manifest.py`

## `never-port` — 1 path(s)

**NO ACTION by rule — never crosses, in either direction**

- **`docs/port/**`** — 1 path(s)
  - `docs/port/port-prompt.md`

## `per-entry` — 2 path(s)

**MERGE BY ENTRY — read the row's `entry_rule` before touching the file**

- **`docs/restructure/backlog/items/*.yaml`** — 2 path(s)
  - `docs/restructure/backlog/items/J68.yaml`
  - `docs/restructure/backlog/items/J69.yaml`
  - *entry_rule:* THE ENTRY IS THE FILE (ADR 0013 Clause 6): one id, one path, so disjoint ids are ordinary git adds/modifications and need no hand-merge. The same id on both sides is one small conflict resolved by the CROSS-REPO rule (Clause 4, F4 ruling 2026-08-20): the CONSUMER's `status` stands — a port never writes status, because the same id names the same work with two independent completions (the company runs its own gates, loads, suite) — and the producer's status + date fold into `notes` as information. Never drop a file; never regress a status. `annotations:` (harvested monolith comments) is informational and unions by key. No roll-up lives here to recount.

## `derived` — 2 path(s)

**REGENERATE LAST, never carry — the J43 rule**

- **`docs/plan/board.html`** — 1 path(s)
  - `docs/plan/board.html`
- **`docs/plan/roadmap.html`** — 1 path(s)
  - `docs/plan/roadmap.html`
