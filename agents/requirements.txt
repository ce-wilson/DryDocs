# drydocs-agents — ADK agent service (own venv, NOT part of the poetry env).
# python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
google-adk
neo4j
python-dotenv
litellm       # graph_qa provider adapter (R1 ruling: anthropic local / azure company)
pyyaml        # graph_qa schema grounding reads relationship_vocabulary.yaml
